server {
    listen 80;

    location / {
        default_type text/plain;
        return 200 "llm-mock-ok\n";
    }

    location /mockserver/status {
        default_type text/plain;
        return 200 "mockserver ok\n";
    }
}
