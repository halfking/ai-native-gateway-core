# 24h 代码审计报告（2026-08-17）

审计窗口：baseline `5b6ec115a` (≈2026-08-16 17:00) → HEAD `ea4f49452` (2026-08-17 16:14)。
范围：100+ 提交，435 个文件，+33205/-3857（178 个 .go 源文件 + 37 个 SQL migration）。

## 审计方法

`go build ./...` + `go vet ./...` + `staticcheck ./...`（全程通过），然后把 421 条
staticcheck 报告与近 24h 改动文件求交集，再用 `git blame` 把每一条违规按作者日期
归类为 **NEW-in-24h**（本次引入，必须修）vs **pre-existing**（既有，不在审计窗口，保持现状）。
并行用只读探查代理对四个新域做重复实现核验：
`requestjourney` / `nodehealth` / `credentialquota` / `credentialfpslot`。

## A. 已修复（本次提交）

### 1. dispatch outcome 死链（NEW in-24h，commit 7be15b8be / 0ea3b42c9）
`domains/streaming/executors/executor_dispatch.go`

- `dispatchCtx.outcomes` 字段 + `outcomeMu sync.Mutex`
- `dispatchRequestOutcome` 结构
- `appendOutcome()` / `outcomeSnapshot()` / `recordDispatchOutcomes()`
- 两处 `dctx.appendOutcome(...)` 写入点（dispatchForward 成功/失败分支）

证据：`appendOutcome` 有调用者（line 559/572 写入），但唯一读取者
`outcomeSnapshot()` 本身是 U1000 死函数；而 `recordDispatchOutcomes()`（负责把 outcomes
回写 `URSMv2.RecordRequest`）也无任何调用者。即整条数据流"只写不读"。
URSM v2 在 dispatch 路径上的 per-attempt 记账实际上由 reducer 路径承担
(`executor_nodehealth.go:184` 的 `EffectUpdateURSM`)，所以删除不影响记账。

清理后净 -98 行；附带移除 `sync` / `ursmv2api` 两个不再被引用的 import。

### 2. body cache 死守护与死辅助函数（NEW in-24h，commit a74986525）
`admin/logs_body_cache.go`

- `bodyFetchCache.mu sync.Mutex` —— 注释称"guards ttl-expiry check vs concurrent Put"，
  但底层 `cache.LRU` 已自带 `sync.Mutex` 且所有方法都加锁（`domains/ursm/v2/cache/lru.go:13`）。
  `mu` 全程无人 `Lock()`，是死守护。
- `shouldCacheErr(err) bool` —— 函数注释自认"此 helper 暂未调用"，staticcheck U1000 确认。

### 3. ST1005 错误串大小写（NEW in-24h，commit 0ea3b42c9）
`domains/requestjourney/redis_store.go:186` — 错误串 `"Redis journey identity mismatch"` →
`"redis journey identity mismatch"`（Go 惯例：错误串首字母不大写，且不打专有名词）。

## B. 需要人工决策的架构性发现（未自动改动）

下列涉及"宣称接入但未接入"或重复实现，超出自主清理边界，留待评审决策。

### B1. `domains/credentialquota/` —— 全包零生产导入者（NEW in-24h，commit 492aa2166 + 7b086ed52）
- `grep '"github.com/kaixuan/llm-gateway-go/domains/credentialquota"'`（非测试、非自身）
  **返回 0 行**。该包 8 个文件、~1000 行代码仅被自己的 `credentialquota_test.go` 覆盖。
- commit `7b086ed52` 提交信息明确声称 "Wires the per-credential, per-client quota package into the dispatch and survival paths"
  以及 "credentialquota.AcquireWithQuota is exposed via credentialfpslot"，但核实发现：
  - `credentialquota` 包中**不存在名为 `AcquireWithQuota` 的方法**（公开 API 只有 `Service.Acquire/Renew/Release`）；
  - 真正的 `AcquireWithQuota` 在 `credentialfpslot/quota.go`，该方法同样**仅被自己包的测试调用**，
    生产 dispatch 路径走的是 `Manager.Acquire`（`executor_dispatch.go:477`，非 quota 路径）；
  - 对应的 DB 表 `public.credential_client_quota` 已在启动时 `ensureCredentialClientQuotaSchema`
    建好（schema + RLS + 触发器齐全），但同样**无人读取**。
- 这是带着 schema 副作用的"宣称接入但实际未连通"的半完成 WIP
  （commit message 自述为 picked up from cross-author stash）。建议：
  真正接入 `credentialquota` 并把 dispatch 的 FpSlot 获取路径切成 `AcquireWithQuota`，
  **或**暂缓建表/暂缓合并避免给 245/154 上线无主 schema。

#### 决策（2026-08-17 follow-up：采用暂缓方案 C，已落地）

未选完整接入（option A，属于多日功能开发），也未删除 `domains/credentialquota`
包或 schema 函数本身（option B 会破坏跨作者 stash 的还原路径，超出本审计边界）。
采用可逆的暂缓（option C）：

1. `db/db.go`：`ensureCredentialClientQuotaSchema` 的 bootstrap 调用注释停用，
   旁边留 TODO 说明恢复条件——dispatch 路径真正接 `AcquireWithQuota` 且
   `domains/credentialquota` 存在生产 importer。函数体保留，恢复作者补一行调用即可重新启用。
2. `settings/specs.go`：`CredentialClientQuotaSpecs()` 注册注释停用，避免在管理后台露出
   一个**当前没有任何读取者**的 knob（`credential_client_quota.mode` 全仓无 reader）：
   默认 `shadow` 会让运维误以为"forcement 已在只记录模式开启"，事实是连 shadow 记账都没接入。
3. `domains/credentialquota/` 与 `credentialfpslot/quota.go` 本身一字未动，
   包测试全绿；接入只需：dispatch 调用点由设置值构造 `credentialfpslot.Quota{...}`
   传给 `Manager.AcquireWithQuota`，并把 `domains/credentialquota.PolicyResolver`
   作为生产数据源注入（目前 `PGSource` 仅被自己包测试调用，零生产 importer）。

恢复约束：解除暂缓的两处（`db/db.go` 附近 TODO、`settings/specs.go:41` 附近注释）
必须同时恢复，并配套接入 `credential_client_quota.mode` 的 reader，
否则会回到"宣称 shadow-on 但无记账"的同态错配。

### B2. `nodehealth/reducer.go` 与既有 reducer 重复（→ 已决策落地，见 E.1/E.2）
- `domains/nodehealth/reducer.go`（291 行 NEW）与 `domains/ursm/v2/reducer/reducer.go`
  对同一"outcome → node 健康等级迁移"做两套实现且永久错误集合不一致：
  nodehealth `{Auth,Quota,ModelBinding}`（`reducer.go:273-275`）vs
  ursm/v2 `{auth, auth_revoked, model_not_found, quota_permanent}`（`reducer.go:46-51`）。
  ursm/v2 reducer 自述已弃用、零调用者，权威实现是 `record_request.lua`；
  nodehealth reducer 实际仅被 V2 dispatch 路径接（`executor_dispatch.go:465`）。
- 与 `executor.go` 旧内联路径的成功/失败副作用编排（Circuit.Record / StateObserver /
  invalidate cache / probe scheduler）功能等价，只是改用结构化 `effectsFor` 表。
  两条路径互斥（按是否走 dispatch 2 选 1），不会双发，但等价控制逻辑存在两份。
  建议：旧 Execute 路径退役时同步删除 `executor.go` 中被替代的内联块，避免长期双份维护。

### B3. `requestjourney` 与既有 lifecycle/tracing 三套并列（→ 决策文档见 E.3）
近 24h 新增的 `domains/requestjourney/*`（~2000 行）与既有两套请求生命周期追踪功能重叠：
1. `domains/dispatch/state_transition_logger.go` + 全局 helper（`LogRouteDecisionGlobal` 等）
   —— **写同一张表 `request_state_transitions`**。migration 529 显式扩展该表并加约束
   `event_type IS NULL OR transition_type IS NOT NULL`，使一行要么是老 transition row
   要么是 journey row；但两套 writer 仍在 route/retry/node_switch/terminal 四点同时写。
2. `internal/trace/*` + `admin/request_trace.go` —— 另一套"Redis→PG per-request 事件流"
   （同样有 Recorder/Redis Lua append-trim/PG flush/admin endpoint），独立 key namespace、
   独立 Lua、独立序列化。requestjourney 故意不带 body/任意 metadata（隐私切分），
   但 recorder+store+query+admin-handler 四层架构与 trace 平行重复。

并存三个 admin endpoint（`/requests/{id}/transitions`、`/requests/{rid}/trace`、
`/request-journeys`）回答同一问题，dashboard 实时流入口现在也变得两可
（`admin/logs.go listLogs` vs `admin/request_journey.go serveList`）。
建议中长期把 `StateTransitionLogger` 迁入 `requestjourney`，保留 `internal/trace`
的富内容（含 body）追踪为唯一带 body 的旁路。

## C. 不在本审计窗口的既有问题（保持现状）

按 `git blame` 作者日期判定为 pre-existing 的违规未改动，清单：
- `errDispatchFpSlotSaturated`（U1000，2026-08-11，在改动的 executor_dispatch.go 内但先于窗口就无引用）
- `admin/routing.go` routingHandler/isRoutable/blockReason 等多组（2026-07-01）
- `admin/handler.go` `lifecycleJobs` 字段（2026-07-13）
- `domains/streaming/handler.go:6247` SA5011 / `handler.go:3877` SA4011（2026-07）
- `admin/log_management.go:271` S1002 / `admin/live_stream_sse.go:665` S1033（2026-07）
- `admin/providers.go` SA4010 append（2026-07）
- SA1019 deprecated（`domains/credentialstate`、`h2c`、`flags.AutoEmbeddingRoute` 等）
  跨整个 gateway/streaming 的引用——这些是历史已弃用 API 的存量调用者，并非本次新增。

## 验证

- `go build ./...`：通过
- `go vet ./...`：通过
- 受影响包测试：`./domains/streaming/executors/...`、`./admin/...`、
  `./domains/requestjourney/...` 全部 PASS
- staticcheck 复跑：三个改动文件无 NEW-in-24h 报告；仅剩 pre-existing 的
  `errDispatchFpSlotSaturated`（不在窗口）。

## D. 复核与自洁修正（2026-08-17 follow-up）

对本次审计工作本身做二次复核，发现两处审计自身的回归并就地修复：

### 4. B1 park 引入的新 U1000（自身回归，已修）
`db/db.go:3767 ensureCredentialClientQuotaSchema` 函数体由 commit `7b086ed52`
（NEW-in-24h）引入，被本次 B1 决策 C 通过注释停用其唯一 bootstrap caller 后
（commit `55669d6ff`）变为新的未调用方法，staticcheck U1000 命中。此回归由审计
自身行为引入，按审计原则必须就地清理且不跨会话遗留。修复保留函数体以便恢复
作者一行解除 park：在函数声明上方加 `//lint:ignore U1000` directive 暂时静默，
并附 TODO 指向 B1 段；恢复者补回 `db/db.go:201` 处的调用、同步恢复
`settings/specs.go:45` 的 spec 注册后，应一并移除该 directive。

### 5. A2 清理遗漏的 SA9003 空分支（原审计漏报，已修）
`admin/logs_body_cache.go:88` 的 `if body == nil && resp == nil { // 仅注释 }`
来自同一 NEW-in-24h commit `a74986525`，staticcheck SA9003 命中。原 A2 只删了
同文件 `shouldCacheErr`，未处理该空分支——属审计漏报。修复：把 if 改为前置普通
注释 + `_ = body` 显式标注无副作用，去掉空分支。语义不变（nil/nil ErrNoRows
路径仍走下方 entry 构造并落 LRU），符合原函数 rule 22。

### 6. B2/B3 复核结论（现状未变，留待人工决策）

B2 — `domains/ursm/v2/reducer` 已确认全仓零生产 importer（仅 record_request.go
注释引用；grep imports 返回空），nodehealth reducer 仍是 V2 dispatch 唯一接入实现，
`executor.go` 旧内联副作用块与 nodehealth reducer effect 表功能等价但分立两份。
冲突面本次远程未改：nodehealth/reducer.go 在 `495ef00dd` 仅加 dedup 上界，未触及
两套并行的现状。退役旧内联块仍属"V2 稳定后"动作，不在自主边界。

B3 — 远程 commit `76baa1d9d` 重构 requestjourney 的 recorder（per-store fan-out
+ outbox replay + 三源 Detail 合并），属包内部强化，未触及与
`state_transition_logger` / `internal/trace` 三套并列的事实，也未合并 admin
endpoint；三个 endpoint（`/requests/{id}/transitions`、`/requests/{rid}/trace`、
`/request-journeys`）仍在。架构决策未变。

### 7. 本次复核验证

- `go build ./...`、`go vet ./...` 全绿
- staticcheck `U1000,SA9003` 在 `./db/.../admin/...` 范围内对 B1 park 自身泄漏的
  `ensureCredentialClientQuotaSchema`、`admin/logs_body_cache.go` 空分支均无命中
- `./db/... ./settings/... ./admin/... ./domains/credentialquota/... ./credentialfpslot/...`
  测试全部 PASS

## E. B2/B3 决策落地记录（2026-08-17 第三会话，所有者已拍板）

> 本段落记录于 B2 执行 commit；推送前远程推进了 9 个提交（含所有者本人的
> `834a822b6` nodehealth binding 隔离与 `ee2565046` 会话级失败日志），已 merge
> 进本次提交：executor.go 冲突区（旧循环内的新观测代码）按退役决策取删除侧，
> `ee2565046` 的 candidate_failure_logs 写入意图（含 V358 session_id 列）已移植到
> `forwardForDispatch` 失败出口（generic + mid-stream interruption 两分支），
> E.2-1 按 `834a822b6` 后的复测结果更新。

### E.1 决策与执行：B2 全量退役（B2a + B2b，所有者选"一次做完"）

所有者在四个选项中选定：B2a（删零 importer 死包）与 B2b（退役旧同步候选循环）
一次做完，并接受两项行为变更——移除 dispatch_v2 kill-switch 回退路径、移除
URSMv2 运行时 Redis 降级（`isURSMv2Authoritative()` 的 Ready() 回退）。B2b 的
"稳定"判定标准同步选定为**运行时指标判定**：Prometheus `URSMv2LegacyFallback`
回退率告警（alerts.yml:166，阈值 20%）连续 7 天静默 + dispatch_v2 未被任何环境
关闭。该标准用于本决策的事后验证与同类退役的先例。

执行内容（详见 commit）：
1. **B2a**：删除 `domains/ursm/v2/reducer/`（3 文件，全仓零生产 importer，
   权威实现为 `record_request.lua`）；`record_request.go` 注释同步改指向。
2. **B2b 主体**：删除 `executor.Execute` 的旧同步候选循环（~1400 行）与仅服务于
   它的 async 202 兜底机制（`shouldAsyncFallback`/`startAsyncRetry`/`runAsyncRetry`/
   `buildAsyncSuccessEntry`，~850 行）、`PredictiveTTFBSkipper`+`predictive_ttfb.go`、
   `NodeTracker`/`NodeFailoverConfig`（`node_failover.go` 保留 hotconfig 全局与
   `NodeTimeout`，内联其读取）、`RunWithCredential`（executor_common.go，独立死代码，
   审计 B2 点名的 Circuit.Record 等价双份）、`RequestLogEmitter` 接口+字段+wiring
   （runAsyncRetry 删除后零生产读者）。
   `Execute` 现为 dispatch-only：pipeline 未 wire 返回
   `dispatch pipeline not wired`（装配错误，main_dispatch.go 无条件 wire）。
3. **B2b 闸门机器**：删除 `dispatch_v2.enabled` spec、`IsDispatchEnabled`/
   `SetDispatchEnabled`/transition-handler 机制、boot sync 与 admin 热切换分支、
   7 处快照 `Enabled` 读点改 literal `true`（admin UI 兼容）。`KILL_DISPATCH_V2`
   本就是幽灵注释（不在 feature_switches 列表），相关注释已清理。
   `allow_model_change` 闸门原样保留。
4. **B2b 降级语义精化**：`legacyWritersEnabled()` 从"运行时 Ready() 判定"改为
   **纯配置判定**（`URSM_V2_MODE != authoritative`）。效果：authoritative 模式下
   Redis 故障窗口内不再回退旧 writers（状态写入由 URSMv2 LRU mirror 兜底、恢复后
   probe 校正——所有者已接受）；`URSM_V2_MODE=off` 的**配置级**回滚路径
   （`scripts/rollback/ursm_v2_to_legacy.sh`）完整保留——`StateObserver`/
   `Recorder`/`RoutingStateShadow` 字段与 nodehealth 适配器、dispatch 路径的
   legacy-writer 分支均按配置语义保留。
5. **B2b 自洁**：删除因主体删除而新孤立的 6 个助手（`emitTraceExec`/
   `buildEnhancedErrorContext`/`pendingTenant`/`contentTypeFor`/
   `classifyContentFilterError`/executors 版 `strPtr`）；staticcheck 对
   改动包复跑无新增命中（pre-existing 清单见 C 段及 E.4）。

### E.2 B2b 揭示的三个行为事实（dispatch 既有语义，非本次引入）

测试迁移到真实 dispatch 路径后暴露的差异——均为 dispatch_v2 默认 ON 以来的
**既有生产行为**，此前因测试从未 wire pipeline 而被旧循环掩盖：

1. **MNF 的 per-request 语义差异（合并 `834a822b6` 后复核更新）**：dispatch mover
   对 `model_not_found` 持续同凭据重试（诊断实证：5 次调用全打 credential 101，
   不切 102/103），而旧循环按 TierFallback 切到下一凭据。初测（修复前）该场景止步于
   "circuit open" 死路；合并所有者的 binding 隔离修复（model_binding 失败不再记
   凭据级熔断）后复测：同凭据重试耗尽预算后 mover 会 markTriedCredential 并重路由，
   场景以**成功**收场——差异仍在但不再是失败死路，持续坏 binding 场景由
   `SetBindingUnavailable` + probe 收敛。是否需要更早的兄弟切换移交 dispatch 所有者；
   旧语义测试 `TestExecute_GLM51_TriesThirdCandidateAfterTwoModelNotFound` 随循环退役。
2. **sole-candidate circuit fail-open 不存在**：旧循环对唯一候选在熔断打开时仍放行
   尝试；dispatch 路径无此例外（`errDispatchCircuitOpen` 直接拒绝）。对应测试
   `TestSoleCandidate_FailOpenOnCircuitOpen` 已随循环退役。若需 fail-open 属产品决策。
3. **MM-1/MM-2 出站附件重写在 dispatch 路径缺失（已修）**：`AttachmentURLRewriter`/
   `AttachmentURLFetchFallback` 的应用块只存在于旧循环——dispatch 默认 ON 后该功能
   在生产**实际已失效**（main.go 仍 wire 钩子、设置开关仍在，但零消费者），4 个
   attachment 测试靠跑旧循环维持假绿。本次已把该块移植进 `forwardForDispatch`
   （multi-key 轮换之后、协议执行之前），dispatch-only 路径下功能恢复生效。

其余迁移策略：`newOverloadTestExecutor` 等测试构造器统一 wire 真实
`NewDispatchPipeline()`（测试助手 `wireDispatchPipelineForTest`），Execute 驱动的
测试全部改走生产唯一路径；解析 executor.go AST 找"候选循环"的元测试
（`TestCandidateLoopFailsOverForEveryRetryableKind`）随循环退役。

### E.3 B3 决策文档（只写文档不动代码，所有者选定的推进方式）

**现状盘点（本会话核实）**：
- `Log*Global` 四个全局 helper 中**仅 2 个有真实调用点**：
  `LogRouteDecisionGlobal`（`domains/streaming/handler.go:3004`）、
  `LogRetryGlobal`（`internal/streamretry/wrapper.go:222`）；
  `LogNodeSwitchGlobal`、`LogErrorGlobal` 当前零调用点（node_switch 事件已由
  dispatch failover 的 `emitNodeSwitch` 覆盖，terminal error 走 request_logs）。
- dispatch pipeline 已通过 `SetObservationSink(gatewayRequestJourneySink)` 向
  requestjourney 投影 observation（retry/node_switch/model_switch/no_route 等），
  即 dispatch 侧事件源已在合一方向上；**缺口只在 handler 层的 route_decision
  与 streamretry 层的 retry 两个写入点**仍直写 `state_transition_logger`。
- 三个 admin endpoint 并存未变：`/api/admin/requests/{id}/transitions`
  （admin/handler.go:921）、`/api/admin/requests/{rid}/trace`（handler.go:803 子路由，
  super_admin）、`/api/admin/request-journeys/`（cmd/gateway/main.go:4992-4995）。

**两 PR 方案（维持原建议，按所有者决定仅立项不动码）**：
- **PR1（事件点同位迁移）**：把 `handler.go:3004` 与 `wrapper.go:222` 两个调用点
  改为经 requestjourney recorder 写同一张 `request_state_transitions` 表（migration
  529 的兼容约束已保证行类型二分）；同时删除 `LogNodeSwitchGlobal`/`LogErrorGlobal`
  两个零调用 helper 与 `state_transition_logger_globals.go`。PR1 期间两个 endpoint
  并存观察，journey 侧查询兼容旧行（529 约束下老 transition row 仍可读）。
- **PR2（endpoint 收敛）**：观察 PR1 上线后 journey 查询正确性，撤
  `/api/admin/requests/{id}/transitions` 注册（handler.go:921）与
  `handleRequestTransitions`，流式入口统一到 `admin/request_journey.go serveList`；
  `internal/trace`（含 body 富内容）保持为唯一带 body 旁路不动。
- **前置协调**：requestjourney 作者本会话数小时前仍在推进（`76baa1d9d`/
  `2be880018`），PR1 动其包邻域，动手前先与作者对齐 recorder API 的稳定面。
- **风险**：`admin/handler.go:287` auditLogger（V3.2-LP5 节点操作审计）也异步写
  `request_state_transitions`，属第三写入方；PR1 范围应把它显式纳入（保持写、只迁
  事件点）或说明保留原因，避免"三套变两套"的报告口径再次失真。

### E.4 CI 现状纠正与棘轮闸门（已落地）

交接文档"仓库当前只跑 go vet、staticcheck 阈值未上"**与事实不符**：
`sessionforensics-ci.yml` 的 Lint job 自 `ab0448162` 起就运行 golangci-lint v2
（`.golangci.yml` 已启用 staticcheck/unused/errcheck/ineffassign/depguard），真实
问题是主分支存量违规过大（仅 credentialquota/db/admin 三个包组即 224 项：
unused 50 / staticcheck 28 / errcheck 78 / gofmt 41 / …），该 job 形同虚设。

本次修复（所有者选定方案）：lint job 改**棘轮模式**——checkout `fetch-depth: 0`
+ 计算 ratchet 基线（push 事件取 `github.event.before`，PR 取 HEAD~1，零 SHA 回退
HEAD~1）+ `--new-from-rev=$LINT_RATCHET_BASE`，只拦新增违规；存量随清理收敛。
注意：`--new-from-rev` 对 gofmt/goimports 类格式化问题同样生效（本仓 gofmt 存量
41 处不阻塞，但新推送的未格式化文件会被拦）。

### E.5 监控项（滚动）

- park 复活监控维持：`git grep -nE 'ensureCredentialClientQuotaSchema\(migCtx\)|append\(out, CredentialClientQuotaSpecs' origin/main`
  （本会话核实：无复活，命中均为 park 注释行本身）。
- B2b 上线后观察：① `URSMv2LegacyFallback` 告警 7 天静默判定（E.1 标准）；
  ② MNF 场景的跨请求收敛是否如预期（E.2-1）；③ MM-1/MM-2 重写恢复后网关 URL
  附件的内联流量（E.2-3，此前生产为零）。
- executors 包 pre-existing U1000（本会话核实 HEAD 时即无调用者，不属本次自洁）：
  `executorMustMarshal`、`context_summarize.go` 三件套、`header_profile.go` 三件套、
  `router.go` 的 `planLegacy`（自注释"已成死代码"）/`planWithURSMv2`/
  `filterAvailableWithStateManager`、`domains/dispatch/metrics.go metricStatsDrop`。
  后续清理时一并处理，棘轮闸门不会追溯拦截。
