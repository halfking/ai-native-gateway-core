{
  "schema_version": 1,
  "identity_kind": "request_id",
  "fixture_purpose": "pin exact-bytes JSON shape for request_id (one HTTP request)",
  "exact_bytes": true,
  "tenant_id": "tenant_freeze_req_001",
  "gateway_instance_id": "gw_freeze_instance_a1b2c3d4",
  "request_id": "req_freeze_20260822_0001",
  "produced_at": "2026-08-22T00:00:00Z",
  "shape": {
    "request_id": "string (server-allocated, monotonically unique per gateway instance)",
    "tenant_id": "string (resolver-derived, mandatory)",
    "gateway_instance_id": "string (16-char or longer host fingerprint)",
    "produced_at": "RFC3339 UTC timestamp"
  },
  "forbidden_fields": [
    "request_body",
    "request_headers",
    "auth_token",
    "user_content",
    "system_prompt",
    "api_key"
  ],
  "consumers": [
    "domains/requestjourney.JourneyEvent.RequestID",
    "internal/liveactions.ActionEvent.RequestID",
    "ingress.IngressEvent.RequestID"
  ],
  "validation_rules": [
    "request_id MUST be non-empty",
    "request_id MUST be unique within gateway_instance_id scope",
    "request_id MUST NOT leak into cross-tenant reads"
  ]
}