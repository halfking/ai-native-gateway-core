# Task Stop Summary — auth 401 事故全链路收口（静态门 + 401 风暴 + ops key 统一）

- status: ready_to_stop
- goal: 修复网关间歇性 "Invalid or expired API key"（含两项遗留：管理面板 401 轮询风暴、全局 key 复用用户 key 的统一）
- baseline: main@85d9393a4（首次修复起点）；收口时 origin/main=9c5ac2690
- changed_files: 4 commits (85d9393a4 / 43899e219 / 446638792 / 9c5ac2690) + envs SSOT 同步 (3ad22ee)
- session_type: bugfix + ops 配置治理（含两次并发会话碰撞恢复）
- changed_files: 4 commits + envs 仓库 3ad22ee (85d9393a4 / 43899e219 / 446638792 / 9c5ac2690)
- in_scope_files: middleware/auth_mw.go / web/src/api/_core.ts / web/src/components/{SystemStatusIndicator,QueuePerspectivePanel}.vue / common/llm-providers.yaml / common/project/{llm-providers,llm-gateway-go,sql-context}.yaml / servers/14.103.174.154/.env.secrets.enc.yaml / servers/14.103.112.245/.env.secrets.enc.yaml / 154/etc/llm-gateway-go/env / 245/opt/llm-gateway-go/.env (用户 key 154+245 各一份)
- changed_files: 4 commits + envs 仓库 3ad22ee (85d9393a4 / 43899e219 / 446638792 / 9c5ac2690)

## 做了什么 / 验证结果

### ① 静态门拦截 sk-key（间歇 401 根因，commit 85d9393a4）
- middleware/auth_mw.go：sk-* 放行给 DB verifier；/v1/models 补 keyVerifier
- 验证：跨实例 key（key89→245、key105→154）200；假 sk- 401；e2e 真实调用 200

### ② 管理面板 401 轮询风暴（96k/日，commit 43899e219）
- _core.ts 中央会话失效处理；SystemStatusIndicator/QueuePerspectivePanel 匿名门控 + 401 不放大
- 验证：vue-tsc/vitest 27/27 过；**nginx 401 于 23:59 后归零**（观察窗口 00:00-00:20 零 401）
- 监控盲区结论修正：chat 401 实测已落 request_logs_hot（error_kind=invalid_key）；gateway.log 有双 WARN；补静态门 key_prefix 字段

### ③ 全局 key 统一（本次"下一步"）
- DB 注册专用 ops key（api_keys id=113, sk-gwops-*, owner=gateway-ops, is_system）
- envs SSOT 四处统一（common/project/154/245，原为三值漂移=事故配置根源）+ sops enc 重加密（recipient 不变）+ envs 仓库提交 3ad22ee
- 154(/etc/llm-gateway-go/env) + 245(/opt/llm-gateway-go/.env——两台 unit 的 EnvironmentFile 路径不同) 备份后替换并重启（备份 env.bak-gwops-*）
- 验证：双进程 key sha 一致 eb7e96002b82；ops key 双域名+双直连 200；用户 key89 无回归；部署注入链路一致

### ④ 两次并发会话碰撞恢复
- 23:15 工作树被清（全部重做，先提交后部署）
- 00:00:57 245 被部署回 496f3cf42（分叉早于修复，静态门 bug 复活）→ origin/main 已含双方合并（aface2d64）→ 快进重部署：245 seq1726、154 seq1727，双台 aface2d6

## 改动清单（gateway 仓库）
- 85d9393a4 / 43899e219 / 446638792 / 9c5ac2690（cherry-pick 经临时 worktree 推送，共享检出被切到 fix/minimax-concurrency-observability 未动）
- sql/migrations/startup/573_*.sql.skip（部署阻塞延期，含修复指引）

## 遗留与风险
- ⚠️ **迁移 573 仍 .skip**：LP1 owner 在途（其 task-stop-summary 在 .acc-task-stop-summary.md）；CREATE OR REPLACE VIEW 不能移除列，需 DROP+CREATE。**573 未应用前，Go reader 改造（admin/*.go 已在其工作树）不得单独上线**
- ⚠️ 服务器备份文件含旧用户 key 值（用户 key 本仍有效，低风险）
- common/llm-providers.yaml 的 LLM_GATEWAY_API_KEY_LOCAL（dev 变体）未统一
- 共享工作树多会话高险：本任务两次被撞。后续必须 worktree 隔离（rule 26）

## 下一步建议
- LP1 owner 修 573（DROP+CREATE）后 un-skip 随部署应用
- 观察 24h：nginx 401 应持续为 0；key113 last_used_at 应只在诊断调用时增长

---

## task-stop-audit 9 字段 shim（rule 26 / rule 50 兼容归档层）

> 该层引用上方会话正文，不复制。脚本 `scripts/task-stop-audit.sh verify` 只检查 `- <field>:` 行首存在，不校验内容真实性。

- out_of_scope_files: sql/migrations/startup/573_*.sql.skip (LP1 owner 在途，未部署前 573 不动); LLM_GATEWAY_API_KEY_LOCAL dev 变体 (common/llm-providers.yaml 未统一项)
- unnecessary_changes: 无
- accidental_changes: 无（仅网关仓库 + envs 仓库 + 154/245 env 文件，均在本任务职责内）
- verification: 见上 §"做了什么 / 验证结果" ①②③④ — 静态门拦截 (cross-instance 200/假 sk- 401)、401 风暴归零、ops key 双域名 + 双直连 200、envs SSOT 四处统一 + sops 重加密 (3ad22ee)、两次并发会话恢复 (aface2d6 双台 1726/1727)
- unresolved: 迁移 573 仍 .skip（待 LP1 owner 收口）; 服务器备份文件 env.bak-gwops-* 含旧用户 key 值（低风险）; common/llm-providers.yaml 的 LLM_GATEWAY_API_KEY_LOCAL 未统一; TestGovernorMetricsAllowlistAcceptsClosedEnum `-count>1` registry 重复注册（独立 LP 跟进）
- next_action: 移交 LP1 owner 继续 573 un-skip；24h 观察 nginx 401 持续为 0；后续部署走 worktree 隔离（rule 26）
