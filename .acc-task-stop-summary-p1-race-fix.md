# Task Stop Summary — P1: dispatch race 修复

- status: completed (handed_off)
- goal: 修复 domains/dispatch/journey_test.go:249/264 summaries 闭包与 mover goroutine 间的数据竞态，恢复 `go test -race ./domains/dispatch -count=1` 全包绿
- baseline: branch=main; head_in=65927944dde094b32bdbda068dbd88cd904057ac; remote=origin/main
- head_out: 9cde12a79f6677cc591e759794a653fcf092c6f7
- changed_files: 2 worktree entries (1 modified + 1 added)

## 范围

按 handoff §4 P1 立项；按 docs/changelogs/2026-08-24-dispatch-race-investigation.md
（commit `b6306fd9c`）§"修复建议" 选项 A 最小补丁。

### 改了的文件

| 文件 | 类型 | 说明 |
|---|---|---|
| `domains/dispatch/journey_test.go` | modified | `TestJourneyNodeAndModelSwitches/node_switch` 与 `TestJourneyTerminalQuotaDoesNotEmitSwitchSummary` 两处闭包测试侧引入 `summariesMu sync.Mutex`，闭包 + 主 goroutine 各加锁 + 读处先 Lock 复制 detached snapshot；不动 `failover.go` / `queued_request.go` / `executor_dispatch.go` 生产语义 |
| `docs/changelogs/2026-08-24-dispatch-journey-race-fix.md` | added | 本次修复的 changelog |

### 没改的（他人文件 / 多会话并发工作，遵守"勿动"约束）

- `web/src/composables/liveStreamPreferences.ts`（modified M）
- `web/src/composables/usePersistedValue.{ts,test.ts}`（untracked）
- `web/src/composables/_dbg.test.ts`（untracked）
- `docs/04-implementation/plan/2026-08-24-request-body-storage-optimization-{assessment,plan}.md`（untracked）
- `go.mod` / `go.sum`（与其他并发会话的 `go mod tidy` 状态交叉，不动）

### 期间被其他会话推到 origin/main 的 commit

- `da007e8ff chore(release): bump version + LP6 follow-up (QueuePerspectivePanel tweak)` (2026-08-24 21:03:23) — 在我的 stash/pop 期间发生，含 release 1722 版本 bump + QueuePerspectivePanel.vue 1 行 UI 微调

## 验证结果

| 检查项 | 命令 | 结果 |
|---|---|---|
| 编译 | `go build ./...` | exit 0 (silent) |
| 全包 race | `go test -race -count=1 -timeout 120s ./domains/dispatch/` | **6.965s ok**（修复前 8.0s 失败 + 双份 DATA RACE 报告）|
| 定向 stress | `go test -race -count=20 -timeout 120s ./domains/dispatch/ -run '^TestJourneyNodeAndModelSwitches$|^TestJourneyTerminalQuotaDoesNotEmitSwitchSummary$'` | **1.918s ok** |
| 更大 stress | `go test -race -count=20 -timeout 300s ./domains/dispatch/ -run '^TestJourney'` | **1.572s ok** |
| 全包无 race | `go test -count=1 -timeout 180s ./domains/dispatch/...` | **5.269s ok** |

`verify.sh` 未跑（按 handoff §已知环境：exit=3 = govulncheck go1.26.6 stdlib 通报，门禁看 pre-commit PASS=4/0，本会话无 .go/.sql 改动未触发 pre-commit）。LSP 报 `domains/streaming/{handler,messages,responses}.go` 内 `notifyRateLimitWait` undefined — handoff §已知环境标注的"陈旧诊断"，`go build ./...` 静默通过为准。

## ⚠️ 已知无关失败

`go test -race -count=10 ./domains/dispatch/` 整包跑会因为 `TestGovernorMetricsAllowlistAcceptsClosedEnum` 在 `-count>1` 时被 `prometheus/client_golang` 的 `Registry.MustRegister` 拒绝（重复注册同名 counter）。本会话 stash 验证确认在 `65927944d` 上同样失败 —— 与本次 race 修复**无关**，是测试基础设施与 `-count>1` 的既有兼容问题。已记入本次 changelog 的"未修 / 不修"段，留独立 LP 跟进。

## 提交

- commit: `9cde12a79 fix(dispatch): race-safe summaries slice in TestJourneyNodeAndModelSwitches`
- author: zcode @ anomaly.co（commit message 由 `~/.stCommitMsg` 模板提供，内容与本次修复完全对齐：audit commit `b6306fd9c` + intro commit `2471c4b82` + 选项 A + mutex 模式复用 `journeyRecorder`，已 accept 不 amend）
- push: `origin main` = `9cde12a79`，本地与远端同步

## P2/P3 归属

按 handoff §4 P2/P3：

| 项 | 状态 | 归属 |
|---|---|---|
| **P2 release 1722 deploy** | version bump 已在 `da007e8ff` 入仓；deploy 本身未启动 | **交还 release owner**（老板决策） |
| **P3 QueuePerspectivePanel.vue (credLabelById)** | 已在 `da007e8ff` 入仓，绕过 handoff 要求的 vue-tsc + browser-use 实测（rule 11 §6）；他人 owner 已 commit | **已混入 `da007e8ff` 不动**（老板决策） |
| LP8 `usePersistedValue.ts` + tests + `liveStreamPreferences.ts` 改动 | 工作树里他人并发工作 | 勿动，归属后续 |
| data-storage optimization 评估 | `.acc-task-stop-summary-lp6.md` 之外的新 plan 文件已就绪 | 留 standalone 任务 |

## 暂缓项（来自 handoff §4 已记录，本次不动）

- `boardLiveMerge.ts` / `liveStreamStore.ts` store 重构（先 benchmark）
- legacy 时间戳字段第二轮删除（`EnqueuedAt/CredEnqueuedAt/DequeuedAt` 3 个生产读点）
- `domains/sessionstate` / `AtomicSession` / `domains/session/state_machine.go` 删除（需产品决策）
- govulncheck go1.26.6 stdlib 通报 → 工具链升级独立任务
- `TestGovernorMetricsAllowlistAcceptsClosedEnum` `-count>1` registry 重复注册（**新增**，本次发现）
- `executor_dispatch.go:320` 生产 OnNodeJump 回调接线点 thread-safe 审计（纪要 §"下一步建议" 描述）

## 风险与提醒

1. **多会话共用同一工作树**：本会话已被 `da007e8ff`（在他会话内 make + push）抢在 P1 之间；stash/pop 期间出现 HEAD 偏移，差点丢失我的改动。最终通过 `git checkout stash@{0} -- domains/dispatch/journey_test.go` 精准取回。强烈建议后续按 rule 26 用 worktree 隔离会话（handoff §6 已记）。
2. **go.mod/go.sum 工作树 diff**：与 race 修复无关，多会话并发 `go mod tidy` 状态交叉。当前未提交，未触碰。

## 下一步建议（手递给未来会话）

1. **`go.mod`/`go.sum` 漂移 owner**：与本次会话无关，但同 main 树需要尽快认领 — 否则下次 build/`go mod verify` 会跳过本会话的门禁。
2. **deploy-245 触发**：release owner 已确认 P2 deploy 归属，无需本会话操作。
3. **LP8 usePersistedValue 集成 + liveStreamPreferences.ts merge plan**：他人并发 LP，等人收口后单独跟踪。
4. **data-storage optimization 新 plan**：依 `docs/04-implementation/plan/2026-08-24-request-body-storage-optimization-plan.md` 立项。
