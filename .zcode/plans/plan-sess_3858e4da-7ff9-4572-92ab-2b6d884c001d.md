## 修正后的审计结论（已重读代码）

前一轮方案的关键错误已纠正。真实的 hook 链式插件模式是：

- 生产网关跑 **V1 ChatHandler**（V4 pipeline 默认关闭）。
- **响应侧**：`ResponseInterceptor` 接口 + `InterceptorChain` 链式接入（`domains/hooks/response/{types,chain}.go`）。装配点 `cmd/gateway/goal_control.go:201`：`chain := response.NewInterceptorChain(goalHook, auditHook); chatHandler.SetResponseInterceptor(chain)`。
- **请求侧**：`CheckV1` 扁平接口（sessionaudit 模板，`handler.go:1098`）。
- `*sql.DB` 桥接已存在：`dbConn.Stdlib()`（main.go:1128,1585）。

**四个真实缺陷**：
1. `output_compliance` 只有 V4 hook，**无 V1 `ResponseInterceptor`** → 生产路径没跑。
2. `handler.go:2070-2082` 非流式路径**忽略 `ModifiedBody`**（脱敏 body 不写回）→ 必须修。
3. `checker.redactOutput`（checker.go:492）bug：`issue.Content` 已 mask，ReplaceAll 不命中。
4. `pii_stripped` 契约悬空（cache_update_hook 读但无生产写者）。

## 交付物

### 交付物 1：架构文档（先落地）
`docs/2026-07-09-session-tagging-redaction-architecture.md`：两身份面 / V1 hook 链式插件模式（ResponseInterceptor+Chain / CheckV1）/ 三标签存储审计 / 每会话打标统一层（session_tags 规范）/ 输出脱敏接线（OutputComplianceInterceptor 入链 + 修 ModifiedBody + 修 redactOutput + owner==caller 文本规则）/ 点亮 pii_stripped / 风险 / 文件索引。

### 交付物 2：实现（链式插件，不重复）

**阶段 A：输出脱敏接线（ResponseInterceptor）**
- A1 新建 `domains/hooks/outputcompliance/interceptor.go`：`OutputComplianceInterceptor` 实现 `response.ResponseInterceptor`，调 `Checker.Check` + owner 判定，返回 `ModifiedBody`。
- A2 `domains/streaming/handler.go:2070`：修非流式 `ModifiedBody` 应用。
- A3 `domains/outputcompliance/checker.go:492`：修 `redactOutput`（位置替换或委托 `approval.SensitiveDetector.Redact`）。
- A4 新建 `domains/outputcompliance/owner.go`：`OwnerAllowsSensitive` + redaction_mode。
- A5 新增设置 `output_compliance.redaction_mode`（off/always/owner_mismatch）。
- A6 `cmd/gateway/main.go`（goal_control 邻近）：`initOutputCompliance(dbConn.Stdlib(), chatHandler)` 构造 checker+interceptor，并入 InterceptorChain。
- A7 点亮 `pii_stripped`（经 telemetry/SessionCache）。

**阶段 B：每会话打标统一层**
- B1 `cache_update_hook.go` 投影 v6→session_tags；B2 `session_cache.go` loadFromDB 恢复 v6；B3 `tagger.go` 扩展词汇表+`TagFromSessionState`；B4 `session_panorama_handler.go` 统一 SessionTagReader。

**阶段 C+D：详情展示 + 契约文档化**
- C1 `session_compare.go` TurnView 加 tags；C2 SessionTurnsPanel chips；D 在 request_envelope.go Metadata 注释登记契约。

## 排除
不改 V4/main.go:1901 nil（死路径）；不新建脱敏引擎/标签存储/授权表；admin 不脱敏（看明文）；不改 L1-L3。

## 顺序
文档 → 阶段 A（验收）→ B → C+D。验证：go build+test、前端 type check、i18n parity。