# 05. 注册激活流程

> 客户端视角的注册激活全流程。分 4 个入口：试用、License Key、离线激活、查询状态。

## 一、激活流程总图

```
用户首次访问 http://server:8781
        │
        ▼
   网关检测 license.dat 是否存在
        │
        ├─ 不存在 ──► 重定向到 /setup（激活向导）
        │
        └─ 存在 ──► 正常首页
        
用户在 /setup 页面选择激活方式：
        │
        ├─ A: 免费试用 15 天 ──► 填邮箱 + 同意条款 ──► 提交
        │                                  │
        │                                  ▼
        │                        POST /api/system/license/trial
        │                                  │
        │                                  ▼
        │                  客户端 ──► 主控端 /api/v1/license/trial
        │                                  │
        │                                  ▼
        │                        主控端签发 trial license.dat
        │                                  │
        │                                  ▼
        │                        客户端写入 license.dat
        │                                  │
        │                                  ▼
        │                        重启网关 → 进入正常首页
        │
        ├─ B: License Key 激活 ──► 输入 KXGW-XXXX-XXXX-XXXX
        │                                  │
        │                                  ▼
        │                        POST /api/system/license/activate
        │                                  │
        │                                  ▼
        │                  客户端 ──► 主控端 /api/v1/license/activate
        │                                  │
        │                                  ▼
        │                        验证 + 创建 device + 签发 license.dat
        │                                  │
        │                                  ▼
        │                        客户端写入 + 重启
        │
        ├─ C: 离线激活 ──► 生成 activation.req ──► 拷贝到联网电脑
        │                                          │
        │                                          ▼
        │                          https://llm.kxpms.cn/offline-activation
        │                                          │
        │                                          ▼
        │                          下载 activation.resp（license.dat）
        │                                          │
        │                                          ▼
        │                          客户端导入 → 写入 → 重启
        │
        └─ D: 查询状态（已激活后）──► GET /api/system/license/status
                                          │
                                          ▼
                              返回 license 类型 / 到期 / 设备数
```

## 二、客户端 API 端点（M1-C13/C14/C15）

### 2.1 POST /api/system/license/trial

**用途**：用户开启 15 天免费试用。

**请求**：
```json
{
    "email": "ops@acme.com",
    "agree": true,
    "telemetry_opt_in": false,
    "fingerprint": {
        "machine_id": "abc123...",
        "cpu_info": "Intel Xeon Gold 6248",
        "host_id": "...",
        "primary_mac": "00:0c:29:...",
        "disk_serial": "ABC123XYZ",
        "bios_uuid": "...",
        "os": "linux",
        "arch": "amd64"
    },
    "instance_id": "a3b4c5d6-...",
    "hostname": "prod-app-01"
}
```

**处理流程**：
1. 客户端生成 machine fingerprint
2. 客户端 → 主控端：POST /api/v1/license/trial
3. 主控端校验邮箱、生成 trial license（15 天）
4. 主控端创建 license_devices 记录
5. 主控端签发 SignedLicense，返回 base64
6. 客户端写入 /var/lib/kx-gateway/license.dat
7. 客户端软重启（kill -HUP）

**响应**：
```json
{
    "success": true,
    "trial_expires_at": "2026-08-15T00:00:00Z",
    "license_type": "trial",
    "max_devices": 1,
    "features": ["basic-routing", "2-tenants", "audit-log"]
}
```

### 2.2 POST /api/system/license/activate

**用途**：使用 License Key 激活。

**请求**：
```json
{
    "license_key": "KXGW-XXXX-XXXX-XXXX",
    "fingerprint": {...},
    "instance_id": "...",
    "hostname": "..."
}
```

**处理流程**：
1. 客户端生成 machine fingerprint
2. 客户端 → 主控端：POST /api/v1/license/activate
3. 主控端校验 key 合法性、max_devices 限制
4. 创建或更新 license_devices
5. 签发 SignedLicense
6. 客户端写入 + 重启

**响应**：
```json
{
    "success": true,
    "license_type": "standard",
    "expires_at": "2027-07-12T00:00:00Z",
    "max_devices": 3,
    "active_devices": 1
}
```

**错误响应**：
```json
{
    "success": false,
    "error": "license_invalid" | "license_expired" | "license_revoked" | 
             "device_limit_exceeded" | "fingerprint_mismatch",
    "message": "..."
}
```

### 2.3 GET /api/system/license/status

**用途**：查询当前 license 状态。

**响应**：
```json
{
    "activated": true,
    "license_type": "standard",
    "customer": "ACME Corp",
    "expires_at": "2027-07-12T00:00:00Z",
    "days_remaining": 365,
    "max_devices": 3,
    "active_devices": 1,
    "instance_id": "a3b4c5d6-...",
    "needs_telemetry_consent": false
}
```

### 2.4 POST /api/system/license/offline/request

**用途**：生成离线激活请求文件。

**处理**：
1. 客户端生成 machine fingerprint
2. 构造 ActivationRequest（LicenseKey + HardwareHash + InstanceID + RequestID + Timestamp）
3. 客户端用本地工具签名（生成 activation.req）
4. 返回 activation.req（base64）

### 2.5 POST /api/system/license/offline/import

**用途**：导入离线激活响应。

**请求**：
```json
{
    "signed_license": "<base64 encoded SignedLicense>"
}
```

**处理**：
1. 客户端验证签名（RSA 公钥）
2. 验证指纹匹配
3. 写入 license.dat
4. 重启网关

## 三、主控端 API 端点（M1-T03/T04/T05/T06）

部署在 llm.kxpms.cn，仅内部访问。

### 3.1 POST /api/v1/license/activate

请求和响应同客户端 API（详见 2.2）。

### 3.2 POST /api/v1/license/trial

请求和响应同客户端 API（详见 2.1）。

### 3.3 GET /api/v1/license/crl

**用途**：撤销列表（防复用）。

**请求**：
```
GET /api/v1/license/crl?since=2026-01-01T00:00:00Z
```

**响应**：
```json
{
    "revoked_keys": [
        {"license_key": "KXGW-...", "revoked_at": "2026-07-01T00:00:00Z"}
    ],
    "server_time": "2026-07-12T10:00:00Z"
}
```

### 3.4 POST /api/v1/license/offline-approve

**用途**：主控端管理员审批离线激活请求。

**请求**：
```json
{
    "request_id": "uuid-...",
    "license_key": "KXGW-AIRGAP-XXXX-XXXX"
}
```

**响应**：
```json
{
    "signed_license": "<base64 encoded SignedLicense>",
    "request_id": "uuid-..."
}
```

## 四、激活向导 UI（M1-C17）

页面路径：`/setup`（首次访问重定向到此）

### 4.1 4 个入口

```
┌──────────────────────────────────────────────────────┐
│  欢迎使用 KX Gateway                                  │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌────────────────────────────────────────────┐     │
│  │  A: 免费试用 15 天                          │     │
│  │     邮箱 + 同意条款 → 立即开通              │     │
│  └────────────────────────────────────────────┘     │
│                                                      │
│  ┌────────────────────────────────────────────┐     │
│  │  B: License Key 激活                        │     │
│  │     输入 KXGW-XXXX-XXXX-XXXX → 激活         │     │
│  └────────────────────────────────────────────┘     │
│                                                      │
│  ┌────────────────────────────────────────────┐     │
│  │  C: 离线激活                                │     │
│  │     完全断网场景                              │     │
│  └────────────────────────────────────────────┘     │
│                                                      │
│  ┌────────────────────────────────────────────┐     │
│  │  D: 我已有激活码                            │     │
│  │     输入激活码导入                            │     │
│  └────────────────────────────────────────────┘     │
│                                                      │
│  [运行状态采集授权 opt-in/opt-out]                    │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 4.2 流程时序

```typescript
// web/src/views/setup/ActivationWizard.vue（待实现）

async function startTrial(email: string, agree: boolean, telemetryOptIn: boolean) {
    const fp = await collectFingerprint()  // 调用 /api/system/license/fingerprint
    
    const resp = await fetch('/api/system/license/trial', {
        method: 'POST',
        body: JSON.stringify({
            email, agree, telemetry_opt_in: telemetryOptIn,
            fingerprint: fp,
            instance_id: generateInstanceId(),
            hostname: location.hostname,
        }),
    })
    
    if (resp.ok) {
        // 软重启由后端触发
        showSuccess('试用已开通，3 秒后跳转...')
        setTimeout(() => location.href = '/', 3000)
    } else {
        showError(await resp.text())
    }
}
```

## 五、激活后的日常流程

### 5.1 启动时强制验证

```go
// licensing/enforce.go（待实现）

func EnforceAtStartup() error {
    // 1. 加载本地 license.dat
    lic, err := LoadLocalLicense()
    if err != nil {
        return err
    }
    
    // 2. RSA 验签
    if err := lic.VerifySignature(publicKey); err != nil {
        return err
    }
    
    // 3. 检查过期
    if time.Now().After(lic.ExpiresAt) {
        return ErrLicenseExpired
    }
    
    // 4. 检查指纹
    fp, _ := GenerateFingerprint()
    if fp.MatchScore(&lic.Fingerprint) < MatchThreshold {
        return ErrFingerprintMismatch
    }
    
    // 5. 时钟防回拨
    clockGuard := NewClockGuard()
    if err := clockGuard.ValidateLicense(); err != nil {
        return err
    }
    
    // 6. 反调试检测
    if antiDebug.DetectDebugger() {
        return ErrDebuggerDetected
    }
    
    // 7. 二进制完整性
    if !antitamper.VerifyIntegrity() {
        return ErrBinaryTampered
    }
    
    return nil
}
```

### 5.2 24h 心跳刷新

```go
// 后台 goroutine，每 24h 运行一次

func startLicenseHeartbeat(ctx context.Context) {
    ticker := time.NewTicker(24 * time.Hour)
    for {
        select {
        case <-ctx.Done():
            return
        case <-ticker.C:
            // 向主控端请求最新的签名 license
            resp, err := httpClient.Post(
                "https://llm.kxpms.cn/api/v1/license/refresh",
                request,
            )
            if err == nil {
                os.WriteFile("/var/lib/kx-gateway/license.dat", resp.Data, 0600)
            }
        }
    }
}
```

## 六、试用期转化（M3 → M4 触达）

### 6.1 触达时机

| 时机 | 触达方式 | 内容 |
|------|---------|------|
| 试用第 5 天 | 站内 banner | "试用还剩 10 天" |
| 试用第 10 天 | 邮件 + banner | "试用还剩 5 天" + 限时折扣 |
| 试用第 14 天 | 邮件 + banner | "明天到期" + 升级引导 |
| 试用第 15 天 | 强制 banner + 邮件 | "试用已到期" |

### 6.2 到期后的行为

**不强制停止**，降级到 Community 免费版：

```go
func onTrialExpired() {
    // 1. 标记 license 类型为 community
    lic.SubscriptionTier = "community"
    lic.Features = []string{"basic-routing", "2-tenants", "audit-log"}
    lic.MaxDevices = 1
    
    // 2. 强制 2 租户限制（用户必须删除多余租户）
    // 3. 显示"未授权商业使用"水印
    // 4. 用户可随时输入 license key 升级
}
```

## 七、关键决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 默认试用时长 | 15 天 | 用户指定 |
| 试用是否需要邮箱 | 是 | 留存用户 + 防滥用 |
| 离线激活请求有效期 | 7 天 | 安全 + 灵活 |
| 24h 心跳失败后 | 进入降级模式（不停止） | 用户体验优先 |
| 重启方式 | kill -HUP 软重启 | 不中断服务 |