# 12 小时内修订审计（2026-09-26 04:12）

- **审计对象**：origin/main 近 12h（2026-09-25 16:04 → 2026-09-26 03:33）共 29 个提交（含 merge/docs），其中代码修复 14 个、版本戳 2 个、审计/handoff 文档 8 个、merge 5 个。
- **审计基线**：origin/main = `db58c5ba8`（六仓同 tip 核验）。
- **审计方法**：逐修复提交读 diff（排除纯测试行数的干扰、测试另核）→ 对照随附审计/handoff 文档的根因与预期 → 按任务要求专项核验（流程闭环 / 数据来源与去处 / 并发锁控制 / 多线程资源竞争 / 内存句柄泄漏 / 异步处理异常 / 异常错误处理 / 请求数据转换）→ 实跑测试门禁复核。
- **结论先行**：**12h 内全部已提交修订均判定合理、与预期一致，未发现需要回滚或热修的缺陷**。发现 1 项文档明示"未实施"的预防建议（P2，本轮已落地修复）+ 2 项结构性暂缓项（有明确门禁与 Owner，非缺陷）。六仓同步已执行，处置记录见 §三。

---

## 一、逐项审计

### 1.1 probe 系列三连修（探测成本方案 §9，本窗口主体）

| 提交 | 内容 | 原因核验 | 方案核验 | 专项关注点 | 判定 |
|---|---|---|---|---|---|
| `19eb31536` P0-1+P0-3 | 自检系统密钥 key_tier='system' + 启动自愈 + 迁移748 + 自检429周期熔断 | 48h 实测 86%（35,440/41,056）自检 ping 被自家网关 gw_rpm_exceeded 弹回——INSERT 未设 tier 落列默认 'default'（12 RPM），失败矩阵自增强风暴，根因成立 | INSERT 显式 tier；`HealSelfCheckSystemKeyTier` 幂等 UPDATE（WHERE 全限定，二次执行 0 行）+ main.go 启动接线；迁移 748 双通道（installer + 启动自愈）互为兜底 | **并发锁**：熔断状态 `rateLimited/lastCycleRateLimited` 两字段注释宣称"仅 loop goroutine 触碰故无需锁"——实测 `Start()` 单 `go w.loop(ctx)`、`cycleOnce → runOne → doHTTP` 全程同 goroutine 串行，声明成立；**数据转换**：`isSelfcheckRateLimit` 三臂识别（429 状态 ∪ rate_limit 类 ∪ 明细标记）覆盖非 429 部署形态；**闭环**：`ON CONFLICT DO NOTHING` 不改存量 tier 的缺口由 Heal+迁移 748 闭合，`Heal` 限定 `owner_user IN (两 worker)` 不误伤 | ✅ 合理 |
| `1082ab0b8` P0-2 | 短梯长尾化 + request_failure 频控 + 队列代际 attempt 重置 + 分类器 defer 断链根修 | 60s 档永续滞留 45 对（实测主体源）+ 同分钟内 N 次业务失败触发 N 次探测 + 泵再入队后 attempt 从 1 重来梯位塌回 5s 档 + root_cause_total 指标 protocol/gateway 恒 0 | 长尾梯 5s/15s/30s/60s→5m/1h/2h/6h 封顶（开关默认 on）；频控谓词 SQL（错误证据行 ∧ 已排程/刚探过）fail-open（读行失败/缺行不丢触发，开关本身是 kill-switch）；attempt := max(task.Attempt, consecutive_failures+1) 与 legacy runOne 同算法；**defer 断链改命名返回值 `(r nodeProbeRoundResult)` 是教科书正解**——匿名返回值下 `return r` 先拷贝后跑 defer，分类结果永不到达调用方，实测指标恒 0 与代码缺陷互证 | **错误处理**：频控 ctx 2s 超时 + fail-open 取舍明确（触发不丢 vs 偶发过探，选前者，量级由 gap 兜底）；**数据来源**：谓词读 `node_probe_state` 与泵/提交路径同表同证据口径（错误证据行的定义与 pumpDueStatesSQL 一致）；健康停放行（INV-2）首臂为假永不命中，真失败仍即时再触发 | ✅ 合理 |
| `86f9e505f` ③ 裁决 | gateway 轮 transport 失败改判 gateway 根因 | 36h 实测 126/126 gateway 轮 transport 失败均为 `dial tcp 127.0.0.1:878x`（部署/重启窗回环不可达）——本实例的错被记到节点头上 | `classifyGatewayRoundRootCause`：仅 network_error/connection_error/dns_error 三码改判 gateway；timeout **有意维持 node**（回环超时可能等在上游腿，保守默认）；probeGateway defer + probeResultToRound 双接线，核验无重复分类（每 round 各自出口分类一次） | **数据转换**：分类纯函数、三码闭集，不動退避只动根因标注/指标；只影响 observability 不引入行为分叉 | ✅ 合理 |

**probe 系列补充判断（审计视角的接受性说明，非缺陷）**：P0-3 熔断触发同时覆盖"上游透传 429"（同凭据非同上游也可能 429），理论上存在过度熔断（暂时少采该凭据的 fallback 数据点）；但 fallback 探测本就走同一凭据密钥，且周期间记忆在首个干净周期即清零，对照 86% 自增强风暴的实测代价，取舍正确。

### 1.2 部署脚本双修（owner 决策①②，`eff8e6c22` + `282ebf42a`）

| 提交 | 内容 | 核验 |
|---|---|---|
| `eff8e6c22` 决策① | 探针默认 154/seamless 120→600s | 154 与 245 共享 252 PG，ensure 链 90-150s 失败模式在 245 压垮过两次（09-19/09-23 seq 2221），154 同构暴露；**host_rollback 有意保持 120s**（回滚候选已预热，拉 600s 只会拖长故障恢复）——边界处理精细。蓝绿契约测试断言同步换钉，实跑 exit 0 |
| `282ebf42a` 决策② | bump-version 机器级 floor 计数器 | 09-25 seq 2250 双 checkout 撞号实录（各读 2249 各产 2250 同名 release 互相覆盖）。方案核验：mkdir 原子锁（POSIX 通用，macOS 无 flock(1) 亦可用）+ 60s 崩溃接管（持锁段仅毫秒级 read/compute/write）+ 15s 有界等待超时 fail-open 退回旧行为不阻塞部署 + **floor 先于 version 文件落盘**（写 version 崩溃后该 seq 已被本机消费不会复用）+ `--seq` 显式值仍推进 floor。撞号回归测试 5/5 实跑绿（并发双 checkout {2250,2251} 无重复无跳号）。边界诚实：防护域=同机多 checkout，跨机由 upload_release 拒绝覆盖活跃 release 兜底 |

### 1.3 其余修复

| 提交 | 内容 | 核验与判定 |
|---|---|---|
| `da6b95627` cache-v2 H2 | full 模式 + 注 fileCache 时读写 L1.5（契约从"忽略"翻转为"共享"） | **并发锁**：L1/L1.5 读写与 Invalidate 共用分片锁（B4 不变量）保持；**闭环**：生产唯一调用方 `storage_mode_init.go:286` 仍传 Lite，当前 no-op，是 H2 装配点的启用准备；测试断言随契约翻转钉死。✅ |
| `2135bd2cf` reportrollup | 菜单入口补挂 + 供应商综合评分项 | 评分=纯函数现算不落快照（公式演进可重算历史，不违快照冻结语义）；除零/无数据行返回 0、p95=0 不做时效惩罚；xlsx 列对齐 nil-score 写空单元格。✅ |
| `f3f2c8a19` autoroute 测试地基 | stubIndex.Recommend 共享 fixture 底层数组被 tier 原地写 race 收口 | 生产代码零改动（`applyTierPolicyWithRoutes` 的原地写是唯一活动写点、属合法设计），修的是测试桩所有权契约 + `-race` 回归钉死存量并发测试的假阴性（无 RouteStore 接线时 tier 写不触发）。✅ |
| `565de1fe5` web | export-menu-config esbuild 直接依赖声明 + lockfile 死依赖清理 | 构建Closure完整性修复，4+/54-。✅ |
| `a3831e839` release-images | docker save 改 gzip 流式 | **错误处理**：脚本头部 `set -euo pipefail`（第17行核验）——管道中 docker save 失败仍会炸出非零退出，无静默截断 .tar.gz 风险；磁盘峰值=压缩件（不落中间裸 tar）。✅ |
| `ba95e7424` 上轮审计修复 | hot_zone_trimmer atomic 化 + request_mirror + classifier | retention/maxBytes 改 `atomic.Int64`（热重载 admin goroutine 写 vs TrimOnce 读的实锤竞争），且**一轮内快照一次**保证过期判定与配额口径自洽；手搓 stringsContains 删除换标准库。✅ |
| `db245c7c8` gofmt batch | 9 文件历轮格式遗留 | 纯格式，build 全绿背书。✅ |
| `0e77284f8`/`9fe6d7104` 版本戳 | build 2253 / 2264（版本序号取高防倒挂） | 03:14 并行会话从过期基线部署 2262 造成短暂回退、03:20 起 2263(86f9e505) 恢复、最终载体 2264；取高策略防"新提交配旧序号"倒挂。48h 验收窗口以 2264 时刻起算（automation-d7d001aa 已同步）。✅ |

### 1.4 文档提交（8 篇，审计链完整性）

`12a75bf00`（stash 清理核验三盲区补齐）、`2f76a882e`+`9fe6d7104`（P0-2 上线实测：频控生效 24 次/gateway 分类实证/410 对沉底长梯/runs 88/h——**实测结果与设计预期一致**）、`321027657`（autoroute race 排查）、`f662057ee`（252 真库 E2E 取证 + P1 新阻断 F1 + P2 设计稿）、`9da60135e`（252 SQL 日志九轮五项对账）、`d3012408d`（在制 autoroute v2 画像）、`db58c5ba8`（owner 决策两项执行轮）。文档与代码互相印证，无"文档说了代码没做"或反向缺口（唯一例外见 §二 F-1，文档自己已声明未实施）。

---

## 二、发现的问题与待修正清单

**F-1（P2，本轮已落地）** deploy-local admin 密码缺失无 build 前防护。
`docs/handoff/20260926-probe-p02-longtail-throttle-audit.md` §6.1 明示"预防建议（未实施）"：`.env.local` 重建丢 `LLM_GATEWAY_ADMIN_PASSWORD`（09-19，旧 SSOT bin/2239/env 已不存在）时，`sync_admin_password_from_env` 对空密码只 warn+skip、users 表留旧 hash，凭据解密冒烟等到 ~2 分钟构建后才 401（03:0x 实录）。**已修**（见 §四）。

**F-2（P1，移交——在制 autoroute v2 会话）** 主仓有未提交的 autoroute v2 重构（staged：scoring.go 改/scoring_new.go 删/scoring_v2.go 增/scoring_simplified.go 改/db-changelog.md 改）。
09-25 二轮审计记 P1"手搓 exp 恒 1（StrengthMatch 维度失效）+ 树不编译"；本轮 04:0x 复核：**在制树已编译通过（go build ./... RC=0）且 `go test ./autoroute/ -count=1` 全绿**，scoring_v2.go 中已无手搓 exp 痕迹——昨日 P1 看起来已在制解决，但**仍未提交**（03:46 仍有改动，会话活跃）。处置：不代提交（并行会话所有权），留待其自行收口；下轮审计首要复核项。

**F-3（P1，暂缓——门禁在制）** cursor 仓 P4 分支（endpoint selector + Ollama native executor，5 本地提交，从未推送）未整合进 main。
其 handoff 文档 §12.8 明确"功能分支未合并未推送；整合前应先解决门禁漂移（verify-migration-checksums 12 mismatch/2 stale/613 unregistered），明确并入策略"。cursor 工作树内有未提交的 R67 db-changelog 改写（03:13）正是该门禁治理在制。**处置：本轮只做 main 快进 + origin/main→feature 合并 + 编译/定向测试验证（全绿），feature→main 整合暂缓**，待 R67 门禁收口后由 Owner 裁定并入策略。同时建议：745/720/723/733 等"audit-rewrite 后 SHA 漂移"行需在 R67 收口时统一入档，verify-migration-checksums 的 mismatch 判定应区分"改写已应用（盘上为准）"与"真漂移"两类。

**F-4（P3，记录不动）** deploy-seamless（SSH 远端路径）存在与 F-1 同构的"远端 env 丢密码 → 冒烟才 401"理论窗口，但远端 env 由上传 bundle 管理、无本地事故实录，且 seamless 脚本被并行会话热用；仅记录，随 F-1 经验在下次 seamless 侧改动时一并评估。

**F-5（P3，记录）** go-5 仓有未提交在制改动（urlcheck cmd/upstreamurl fuzz/spec_probe 等，含 03:3x 活跃修改）与 go-4 的 bg/metrics.go 未提交修改——均属并行会话在制，本轮零触碰（六仓同步均为 ff/合并不涉工作树）。

---

## 三、六仓同步执行记录（任务第一段）

| 仓 | 同步前 | 动作 | 结果 |
|---|---|---|---|
| llm-gateway-go（主仓） | 0/0 干净 main | fetch 核验 | 已同步，无需合并 |
| llm-gateway-go-2 | 0/0 干净 main | fetch 核验 | 已同步 |
| llm-gateway-go-3 | 0/0 干净 main | fetch 核验 | 已同步 |
| llm-gateway-go-4 | 0/0（bg/metrics.go 并行在制） | fetch 核验，ff 不涉工作树 | 已同步，在制文件零触碰 |
| llm-gateway-go-5 | 0/0（多文件并行在制，03:3x 活跃） | fetch 核验 | 已同步，在制文件零触碰 |
| llm-gateway-go-cursor | 本地 main 落后 22；feature/p4 落后 9 且 5 本地提交 | ①`fetch origin main:main` 快进 78d91b94e→db58c5ba8；②feature 分支 merge origin/main（**零文件交集，无冲突**，进来的 9 提交不碰 db-changelog.md 故 R67 脏文件原样保留）；③`go build ./...`+vet+定向测试（executors 35.6s/endpointselect/settings/provider）全绿；④feature→main 整合暂缓（F-3） | main 已同步；feature 已保鲜；**未推送 feature、未整合**（有据暂缓） |

---

## 四、本轮落地修复（F-1）

**修法**：把检查提为 lib 函数（可单测），deploy() 在 SECRET_KEY 快速失败检查旁同位接线——两检查同属"build 前拒绝必败形态"惯例。

1. `scripts/deploy-local-lib.sh`：新增 `dl_admin_password_preflight()`（置于 `dl_write_env` 前）——`LLM_GATEWAY_ADMIN_USER` 非空 ∧ `LLM_GATEWAY_ADMIN_PASSWORD` 为空 → `_dl_die`（消息含事故背景与修法指引）；逃生口 `DL_ALLOW_EMPTY_ADMIN_PASSWORD=true`；两值同空维持旧行为不新增门槛。
2. `scripts/deploy-local.sh`：`deploy()` 在 SECRET_KEY 检查后调用之（set -e 语义下返回 1 即中止）。
3. `scripts/deploy-local-lib-adminpw_test.sh`（新增，可执行）：4 案——user-set+pw-empty 必拒且消息含逃生口提示 / 齐备放行 / 逃生口放行 / 双空放行。**实跑 PASS（4/4）**。

**回归**：`bash -n deploy-local.sh` 净；envload/project_env/preflight 既有 lib 测试全 PASS；blue-green 契约测试不涉本改动实跑仍 exit 0。

---

## 五、执行规划（有序）

1. **（本轮已完成）** F-1 落地 + 本审计文档入库推送；六仓 main 同步至含本修复的新 tip（见 §六核验）。
2. **（移交，预计下一会话）** F-2：autoroute v2 会话自行提交收口——提交前建议跑 `go test ./autoroute/ -race -count=1` 并把 09-25 二轮审计的 P1 结论在提交信息里闭环（exp 问题是否确已消除的定性）。
3. **（门禁驱动）** F-3：R67 db-changelog 治理收口 → verify-migration-checksums 绿 → Owner 裁定 P4 并入策略（建议 rebase-merge 保 provenance）→ 整合后跑完整 `./verify.sh --web`。
4. **（下轮审计复核项）** ①F-2 是否已提交；②2264 载体 48h 验收窗口（automation-d7d001aa）的实测结论；③F-4 是否随 seamless 改动评估。

## 六、验证记录（本审计实跑）

```
主仓（含在制 autoroute v2 树）：go build ./... RC=0
go test ./autoroute/ -count=1              ok 3.7s   ← F-2 在制状态复核
go test ./bg/... -count=1                  ok（bg 24.9s + 3 子包）
go test ./domains/session/v2/ ./domains/reportrollup/ ./settings/  全 ok
installer 模块 go test ./...               全 ok（748 parity 含）
bash tests/bump_version_collision_test.sh  5 passed
bash tests/deploy_blue_green_contract_test.sh  exit 0
bash scripts/deploy-local-lib-adminpw_test.sh   PASS（本轮新增）
bash scripts/deploy-local-lib-envload_test.sh / project_env / preflight  PASS
cursor 仓 feature：go build ./... RC=0；vet 净；executors/endpointselect/settings/provider 测试全 ok
迁移 748 六点同步核验：sql/migrations/startup/{748,748.down} + embeddata/startup/748 + runner.go:277 StartupFiles（embeddata 只嵌 UP 为既有约定，down 仅 canonical 存档——与存量 300/63 形态一致）
```
