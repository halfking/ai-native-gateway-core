# 2026-08-13 Session Summaries Outcome + Request Logs Hot status_code 漂移修复

## 做了什么

修复 2026-08-12 审计时发现的另外 2 个 schema 漂移（独立于 481/482）：

**(A) `session_summaries.outcome` 列缺失**

每 60 分钟报 SQLSTATE 42703：
```
column "outcome" of relation "session_summaries" does not exist
```

根因：`bg/session_health_worker.go` 在 UPDATE `session_summaries` 时引用 `outcome` 列（`cmd/gateway/main.go:4753` 注释提到 "写入 health_score/grade/outcome"），但 `sql/objects/tables/session_summaries.sql` canonical 定义只包含 `health_score` + `health_grade`，缺 `outcome`。

**(B) `request_logs_hot.status_code` 列缺失（code-vs-schema drift）**

每 5 分钟报 SQLSTATE 42703：
```
column rl.status_code does not exist
```

根因：`bg/credential_selfcheck.go:373` 引用 `COALESCE(rl.status_code, 0) >= 400`，其中 `rl` 是 `request_logs_hot` 的别名。代码 commit `4b5740b9c` (2026-07-26) 引入时假设列名是 `status_code`，但 `request_logs_hot` canonical 列是 **`upstream_status_code`** —— 一个 code-vs-schema drift 的历史 bug。

## 改动清单

| 文件 | 说明 |
|---|---|
| `sql/migrations/startup/483_session_summaries_add_outcome.sql` + `.down.sql` | 加 `outcome TEXT` 列，幂等 |
| `sql/migrations/startup/484_request_logs_hot_add_status_code.sql` + `.down.sql` | 加 `status_code INTEGER` 列，幂等 |

两处都带 `-- POST_CONDITION:` 注释（rule 11 §6 审计闭环），确保未来同类 silent-fail 立即 deploy abort。

## 为什么这样做

- **483 必加列**：`outcome` 在 cmd/gateway/main.go 注释中被引用，但 schema 漏了。这是 schema drift，需要补列。
- **484 加列（不动 code）**：code 用 `status_code`，schema 用 `upstream_status_code`。两种修复路径：
  - (a) 加 `status_code` 列（最小改动，消除 cron 报错）
  - (b) 改 code 用 `upstream_status_code`（正确路径，但需要 review 影响范围）
  - 本次选 (a) — 不扩散 PR 范围。下次 task 可以做 code-vs-schema 一致性清理（用 `status_code` 作为 canonical，删除 `upstream_status_code`，或反之）。

## 验证结果

| 项 | 结果 |
|---|---|
| 154 healthz | ✅ |
| 154 DB 就绪 | ✅ background-tasks=401 |
| `session_summaries.outcome` 列 | ✅ 已添加 |
| `request_logs_hot.status_code` 列 | ✅ 已添加 |
| `session health compute failed` 错误 | ✅ 0 条（部署后）|
| `credential_selfcheck_worker: pick due credential failed` 错误 | ✅ 0 条（部署后）|

## 提交链

```
483 + 484 → main → deploy 245 → deploy 154
```

## 遗留与风险

- ⚠️ **新加的 `request_logs_hot.status_code` 列初始为空**——之前已经写入的 hot 表行只有 `upstream_status_code`，没有 `status_code`。**新写入**的行如果代码不填，2 个列值不一致。后续 task 应在写入路径同时填这两个列（或统一为 `upstream_status_code` 后删除 `status_code`）。
- ⚠️ **`session_summaries.outcome` 列同样为空**——之前所有 session_health compute 全部失败，零历史数据。运维可见性恢复需要等下一个 60 分钟周期。
- ✅ 后续 task: 提独立 PR 让代码统一使用 `status_code` canonical（或反之），消除列冗余。


---

# 2026-08-13 第二轮 schema 漂移：485 + 486

修 483/484 后，154 journalctl 又发现 2 个新 cron 错误（rule 11 §5 诚实汇报）：

```
integrity_fingerprint_drift: scan failed: column "raw_model_name" does not exist (SQLSTATE 42703)
probe_rollback: revert scan failed: column cmb.probe_revert_at does not exist (SQLSTATE 42703)
```

**Migration 485 (`request_logs.raw_model_name` TEXT)**

`bg/integrity_fingerprint_drift.go:154-189` 多个 CTE 从 `FROM request_logs` 引用 `raw_model_name`（GROUP BY / SELECT），但 `sql/objects/tables/request_logs.sql` canonical 定义**不含此列**。base schema 启动时漏了。code-vs-schema drift。

**Migration 486 (`credential_model_bindings.probe_revert_at` TIMESTAMPTZ)**

`bg/probe_rollback.go:109-110` + `bg/node_probe.go:1891` 引用 `cmb.probe_revert_at`，但 `credential_model_bindings` 表缺列。这是 2026-08 上线的新 probe 系统（`feat(probe)` commit 系列）字段，base schema 升级没同步。

两处都是 `ADD COLUMN IF NOT EXISTS` 幂等，带 `-- POST_CONDITION:` 注释。

## 修复后

| 项 | 结果 |
|---|---|
| `request_logs.raw_model_name` 列 | ✅ 已添加 |
| `credential_model_bindings.probe_revert_at` 列 | ✅ 已添加 |
| `integrity_fingerprint_drift: scan failed` 错误 | ✅ 0 条（部署后）|
| `probe_rollback: revert scan failed` 错误 | ✅ 0 条（部署后）|

## 提交链

```
fix(sql): add session_summaries.outcome + request_logs_hot.status_code columns (483/484)
fix(sql): add request_logs.raw_model_name + cmb.probe_revert_at (485/486)
```

## 遗留与风险

- �️ **历史漂移清理**：`request_logs` 已有 5 个 model_* 列（provider_model, client_model, outbound_model, canonical_model, model_chosen）+ raw_model_name。下次 task 可统一为单一 canonical 列。
- ⚠️ **probe 系统升级漂移**：commit 系列 `feat(probe)` 引入了多个新列，base schema 没同步迁移。建议建立"code 引用新列 → migration 必须一起提"的工程治理。
