# LP3: web/src/api/vendor-credential-error.ts（TS API 客户端）

> Contract freeze (2026-08-29): keep the existing path/query and `{error:{code,detail}}` envelope. Do not introduce `message` or `trace_id`; callers may pass an `AbortSignal` for request cancellation.

## 元数据

- **逻辑点 ID**：LP3
- **预估代码行数**：~80 行（新增）
- **依赖**：LP1（Go handler URL 契约）
- **风险等级**：低（纯类型 + fetch 封装）

## 1. 输入（来自 LP1 契约）

- URL：`GET /api/vendors/credentials/${credentialId}/error-detail?hours=${hours}`
- Method：GET
- Auth：`admin()` middleware，HTTP 请求由 `api/_core.ts` 自动注入 cookie
- Query 参数：
  - `credentialId: number`（路径）
  - `hours?: '1' | '24' | '168'`（默认 '24'）

## 2. 输出（提供给 LP4）

```ts
export interface VendorCredentialMeta {
  id: number
  label: string
  provider_id: number
  health_status: string
  health_error: string | null
  health_latency_ms: number | null
  availability_state: string
  state_reason_code: string | null
  state_reason_detail: string | null
  state_updated_at: string | null
  quota_state: string
  lifecycle_status: string
  circuit_state: string
  consecutive_failures: number
  manual_disabled: boolean
  balance_usd: number | null
  balance_currency: string | null
}

export interface VendorErrorKindStat {
  error_kind: string
  count: number
  last_seen: string
  distinct_status_codes: number
}

export interface VendorRecentFailure {
  ts: string
  request_id: string
  raw_model_name: string
  attempt_index: number
  error_kind: string
  error_message: string | null
  upstream_status_code: number | null
  upstream_response_preview: string | null
  latency_ms: number | null
}

export interface VendorQualityScore {
  profile_date: string
  total_score: number
  availability_score: number
  stability_score: number
}

export interface VendorCredentialErrorDetail {
  credential_id: number
  credential_label: string
  credential: VendorCredentialMeta
  error_summary: VendorErrorKindStat[]
  recent_failures: VendorRecentFailure[]
  quality_scores_7d: VendorQualityScore[]
  hours: number
  since: string
}

export function getVendorCredentialErrorDetail(
  credentialId: number,
  hours: '1' | '24' | '168' = '24'
): Promise<VendorCredentialErrorDetail>
```

## 3. 验收标准

- [ ] AC1: `pnpm typecheck` exit 0
- [ ] AC2: 单元测试（vitest）覆盖 4 个分支：happy / 404 / 400 / 500
- [ ] AC3: 错误格式冻结为现有 `{error:{code,detail}}` envelope；不新增 `message` 或 `trace_id`，避免 breaking contract
- [ ] AC4: 复用项目既有 `fetch` 封装（`api/_core.ts`），不直接调 `fetch`
- [ ] AC5: 命名风格与 `web/src/api/providers.ts` 一致（camelCase 函数名 + PascalCase 类型名）

## 4. 代码落点

| 文件 | 函数 | 新增/修改 | 行数 |
|------|------|----------|------|
| `web/src/api/vendor-credential-error.ts` | 5 个 type + 1 个 function | 新增 | ~80 |
| `web/src/api/vendor-credential-error.test.ts`（可选）| 4 个 vitest case | 新增 | ~50 |
| **合计** | | | **~130** |

## 5. 与代码现状的对照

- 命名参考：`web/src/api/providers.ts:807`（`ProviderLogEntry` 接口）
- fetch 复用：`web/src/api/_core.ts`（项目统一 API client）
- 测试参考：`web/src/api/*.test.ts`（vitest 模式）

## 6. 副作用清单

- 仅新增文件，不修改任何已有代码
- 不引入新依赖
- 不影响既有 API 调用
