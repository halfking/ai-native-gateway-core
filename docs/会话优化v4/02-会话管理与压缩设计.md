# 02 · 会话管理与会话压缩（主题①②）

> 状态标签：CURRENT = 已实现接线；CURRENT-PARTIAL = 部分接线；TARGET = 设计态未接线。
> 代码基线：go-3 `f249c4f4`。来源：会话优化v2（31/59/60）+ 会话优化v3（ADR）+ 修订0811（SC 轨）。

---

## 1. 会话管理（主题①）

### 1.1 数据模型（CURRENT，public.* schema）

> **老板 08-15 修正**：所有表在 `public.*`（分区表除外）；migration 513（08-14）已 DROP 整库 gateway schema。会话存储按「**项目 + 任务 + 会话 + 轮次 + 附属会话 + 附属信息**」维度分解。

| 表 | 职责 | 分区 |
|---|---|---|
| `public.sessions` | 会话快照（元数据） | 按月分区 `sessions_%s` |
| `public.session_turns` | 轮次元数据（turn_no / request_id / 状态） | **hot + 分区表**（老板 08-15 确认：对齐 `request_logs_hot` 模式；当前为纯月分区 `session_turns_YYYY_MM`，hot 表待引入，见 10 号 P0-Z2） |
| `public.session_bodies` | 增量 body（delta 存储） | 按月分区（heap，支持冲突更新） |
| `public.session_turn_logs` | 诊断日志 | 按月分区（24h TTL 清理） |

**hot + 分区表模式（目标，对齐 request_logs）**：`request_logs_hot` 为写入口（`promote_request_logs_hot_to_partition()` 定期提升到 `request_logs_YYYY_MM`）；session_turns 将引入 `session_turns_hot` 写入口 + 定期 promote，双写期与旧模式并存。

**存储维度层级（现状 vs 目标）**：
- 6 维声明：项目(project) + 任务(task) + 会话(session) + 轮次(turn) + 附属会话(子请求/压缩派生 parent_request_id) + 附属信息（tags/注解/provider cache）。
- **现状**（以 `public.session_turns` 实列为准，2026-08-15 核实）：
  - ✅ **会话 + 轮次**：`session_id` + `turn_no` + `tenant_id` + `request_id` + `partition_date`。
  - ✅ **附属信息**（部分）：`session_bodies.request_attachments`/`response_attachments`（attachments 已存）；`session_turns.model`/`provider`/`credential_id`/`cost_usd`/`tokens`（provider cache + cost 已存）。
  - ✅ **任务**（间接）：`sessions.task_type`（会话级）；但 `session_turns` 无 `task_type` 列，跨轮次查询需 JOIN。
  - ⚠️ **附属会话**：`request_logs.parent_request_id`（压缩派生链）已存于旧表；`session_turns` **未持久化 parent_request_id**。
  - ❌ **项目**：`session_turns` **无 project_id / namespace 列**；Go `Session` struct 有 `Namespace` 字段，但未写入 V2 表。属设计目标，**未实施**。
- **目标**：6 维全部持久化到 `session_turns`（project_id / task_type / parent_request_id 列 + 索引），使跨轮次查询无需 JOIN sessions 表。详见 10 号 P0-Z4。
- **双写策略**：保留旧存储模式（`request_logs` / `request_logs_hot`）与 V2 并存双写，将来逐步移除（见 10 号计划 V1 下线项）。

### 1.2 写入路径（CURRENT，影子写）
- **DualWriter**（`domains/session/dual_writer.go`）：V1 主写 + V2 影子写；V2 失败仅 slog，不影响主请求。
- 开关：`sessions_v2.shadow_write`（默认 false，实际生效 key，60 号 NOTE-2 勘误修正）。
- **SubmitModeDetector 决策矩阵 P0-P5**：P0 header / P1.5 attachment_only（新增）/ P1 消息数回退 / P2 summary marker `[smm_v1:` / P3 orphaned tool_result / P4 LCS（≥70%=full，<30%=inferred_compressed）/ P5 首轮=full。
- **turn_no 幂等分配**：`pg_advisory_xact_lock(hash(tenant_id, session_id))` 串行 + `(tenant_id, request_id, partition_date)` 冲突回读既有 turn_no。
- 迁移：430/431/456/464/474/476/479/481/482（每个含 `.down.sql`）。

### 1.3 会话管理架构（CURRENT，ursm/v2）

> **老板 08-15 修正：会话管理采用 `ursm/v2` 架构**。`cmd/gateway/main.go:609-630`（T20）已接线 URSM v2 Manager，`URSM_V2_MODE` 默认 off，shadow/canary 才 SetReady。目录：`domains/ursm/v2/{manager,statesource,reducer,persist,store,shadow,cache,resource}`。

**会话语义管线（TARGET，v3 设计）**
> 7 条 ADR：V3-001 旁路异步 / 002 原始事实不可变 / 003 候选·事实分层 / 004 人工事实优先 / 005 租户·正文最小化 / 006 可降级可重算 / 007 稳定ID与聚类版本分离。

**未接线**。当前生产仍以「存储层 = 事实源」模型，语义层（候选/事实/聚类）为设计态。

### 1.4 待办
| 项 | 级别 | 说明 |
|---|---|---|
| Goal↔Handoff 四组件（ContextMonitor/GoalStateSerializer/HandoffTrigger/HandoffMessage） | P0 | 28 号设计，接口未冻结 |
| `public.sessions` 死表清理 | P2 | 无 Go 引用 |
| session_dim 随 V2 影子写自动维护 | ✅ 已修 | 修复层级树「未分类」（git 6e4fa32dc） |

---

## 2. 会话压缩（主题②）

### 2.1 压缩读路径（CURRENT）
- **V2 读路径已接通**：`tryLoadV2State → Builder.BuildLatestOutbound`（`domains/session/v2/session_writer_v2.go`）。
- 开关：`sessions_v2_compression_read` 默认 true；失败 slog.Warn + fallback V1。
- OutboundBuilder：`BuildFromDeltas(ctx, tenantID, sessionID, lastN, preserveCompression)` → `([]Message, *BuildMeta)`；`BuildMeta` 含 TotalTurns/TotalMessages/TokenEstimate/HasCompression/LastTurnNo。
- `filterCompressionMarkers` 按 CompactionMarkerPrefix 过滤，保留压缩 marker `smm_v1:` 语义。

### 2.2 压缩器与会话三层数据（CURRENT）

> **老板 08-15 修正：缓存 ≠ Redis/内存缓存，而是会话的三层数据形态**：
> ① **原始多轮会话**（未压缩事实源）→ ② **压缩后的多轮会话**（可为跨轮次合并）→ ③ **安全脱敏后的发送会话**（上游实际收到）。

- `domains/hooks/compression/session_compressor.go`（L196 Prepare / L770）。
- 三层数据衔接：`session_bodies` 增量（①事实源）→ `Builder.BuildLatestOutbound` 重建（②压缩后）→ `SessionSanitizeManager` 脱敏（③发送）。Redis 仅承载会话级脱敏映射表（TTL 30min），不是缓存层。
- 组件：estimator.go（token 估算）、breaker.go（压缩熔断）、smart_window.go（智能窗口）、recovery_coordinator.go（恢复协调）、diff.go（增量 diff）、rebuilder_openai/anthropic.go（按厂商重建）。
- **缺口**：AlignmentMap（三层位置对齐）未实现——压缩重建后消息位置对齐能力缺失，影响增量拼接正确性。

### 2.3 修订0811 SC 轨（会话压缩任务，部分待办）
- SC-1..SC-6：压缩链路强化任务（doc 19 六轨表）。M2 已交付 P2-C1（admin bodies 信封适配），压缩链路本体 SC 任务仍待排期。

### 2.4 压缩与脱敏（CURRENT-PARTIAL，08-07 专题）
- `SessionSanitizeManager` 会话级映射表持久化 Redis（TTL 30min）；Hook 顺序 = SanitizerInput(10) → 上游 → SanitizerOutput(50) → OutputCompliance(100)。
- P2 未实施：存储层脱敏（3-5d）、压缩时映射表快照入 `session_bodies.metadata`（2-3d）、映射表 AES-GCM 加密（1-2d）。

### 2.5 待办汇总
| 项 | 级别 | 说明 |
|---|---|---|
| AlignmentMap 三层位置对齐 | P1 | 压缩重建后对齐能力，当前缺失 |
| SC-1..SC-6 压缩任务排期 | P0/P1 | 修订0811 SC 轨 |
| 脱敏映射表快照入 session_bodies.metadata | P2 | 长会话支持 |
| V1 下线（V2-3）：V2 单写 + 归档 V1 | P0 | 前置 V2 稳定 ≥4 周 |
| 压缩性能/存储验收标准 | P1 | 43/44 TBD 项 |

---

## 3. 与其他主题的边界

| 相邻主题 | 分界 |
|---|---|
| 轮次多级缓存（主题③） | 压缩读路径=压缩主题；缓存层结构=主题③ |
| 会话分析（主题⑦） | 管理管「写+压缩」；分析管「读+展示」 |
| 供应商耗尽（修订0811 SR） | 压缩不涉；SR 管长时保活/持久恢复 |

---

**CHANGELOG**
- 2026-08-15 v1.3：审计修正——§1.1 区分 6 维「现状 vs 目标」并标注缺失列（project_id/namespace/parent_request_id/task_type）
- 2026-08-15 v1.2：老板确认 session_turns 走 hot+分区表模式（对齐 request_logs_hot）
- 2026-08-15 v1.1：老板修正——schema public.*（513 已 DROP gateway）、会话管理用 ursm/v2、三层数据缓存语义、会话维度分区表+双写保留
- 2026-08-15 v1.0 初版