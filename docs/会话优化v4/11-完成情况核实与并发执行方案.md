# 11 · 完成情况核实与并发执行方案（2026-08-16 核验）

> 对照 `docs/会话优化v4/00~05、10` 逐条核查当前代码（2026-08-16）。**结论：v4 文档基线 go-3 `f249c4f4` 已显著滞后于当前仓库**，M3 durable 与部分待办项实际已落地。本文件用于校正实施计划并拆分可并行任务。

---

## 1. 现状核实（文档声明 vs 代码事实）

| 文档项 | 文档声明 | 代码核实 | 证据 file:line | 裁决 |
|---|---|---|---|---|
| M3-1 EncryptWithAAD | 未实现（仅固定 AAD） | **已实现** | `secret/aad.go:98`（自定义 AAD + key_id 返回） | 文档过期 |
| M3-2 durable_llm_tasks 表+repo | 未开始 | **已实现** | `sql/migrations/startup/516_durable_llm_tasks.sql:22`；`durable/store.go:148` | 文档过期 |
| M3-3 PG 回源 + PendingStore CAS | 未开始 | **已实现** | `pending/pg_source.go:35`；`pending/durable_cas.go:59` Lua CAS；`cmd/gateway/main.go:2113` | 文档过期 |
| M3-4 CreateAndClaim/双 reaper/fencing | 未开始 | **已实现** | `durable/store.go:148`；`durable/store_terminal.go:247,340` | 文档过期 |
| M3-5 RecoveryWorker | 未开始 | **已实现** | `domains/streaming/durable_recovery_worker.go:103`；main.go:2117 | 文档过期 |
| M3-6 三协议装配 flag | flag 已定义未消费 | **已实现且被消费** | flag=`LLM_GATEWAY_REQUEST_SURVIVAL_DURABLE_ENABLED`（`config/config.go:109`，默认 false）；三协议持 `durableStore` gate；指标 `durable_metrics.go:27,65` | 文档过期，flag 名不符（文档写 `LLM_GATEWAY_DURABLE_*`） |
| M3-7 request_state_transitions survival_* 枚举 | 缺 | **已实现** | `516_*` test 含 `survival_waiting/retry/unknown`；表基线 511 | 文档过期 |
| durable capabilities 自评 | — | self-assessed `"partial"` | `cmd/gateway/capabilities.go:57` | **需对账**：代码已全但默认关闭、未跑 M4 全场景 |
| P0-Z1 ursm/v2 会话管理 | 已接线默认 off | **属实** | `main.go:616-635`；DefaultConfig ModeOff（`v2/config.go:77`）；生产仍走 v1 | 待 off→shadow→canary |
| P0-Z2 session_turns_hot | 无 hot 表 | **属实** | 全仓零命中 `session_turns_hot`；`request_logs_hot`+`promote_request_logs_hot_to_partition` 已实现可参照 | 待实施 |
| P0-Z3 AlignmentMap | 缺失 | **已实现**（文档错） | `domains/hooks/compression/alignment.go:25`；`session_compressor.go:160` | 文档过期；但 `SessionSanitizeManager` 类型未实现 |
| P0-Z4 session_turns 6 维列 | 缺 4 列 | **属实** | `01-schema.sql:15031` 实列无 `project_id/namespace/parent_request_id/task_type` | 待实施 |
| P0-A Admin V2 API 三件 | 待确认 | **未实现** | 全仓零命中 | 待产品确认后实施 |
| P0-B dispatch 模型自动变更 | 默认关未接 | **属实** | `settings/spec_dispatch.go:30` 默认 false | 待实施 |
| P0-C credential_client_quota | 未实施 | **未实现** | 零命中 | 需产品确认（阻塞） |
| P0-D Goal↔Handoff 四组件 | 未实现 | **未实现** | 仅 `HandoffTrigger` 模块名占位（`registry.go:95`） | 待契约冻结 |
| P0-E Goal retry HTTP 202 / `X-LLM-Gateway-Retry-Scheduled` | 17 号未实施提案 | **未实现，且不同于现有 pending 契约** | 17 号 §2.3/ADR-GOAL-001 明确未实施；`handler.go:3371` 是幂等 replay 的 202 + `X-Gw-Pending*` + `Retry-After` | 保留现有 `X-Gw-Pending*`；修正文档，不加冗余 header |
| P1-F Balanced.UseAudit | v4 行动项误写应 true，05 §2.1 勘误语义为 aggressive 专属 | **实现正确** | `cost_presets.go:113` UseAudit:false；`cost_presets_test.go:39` 断言 false | 保持 false，统一文档与注释 |
| P1-E TPM 真实 tokenizer | 返回 0 占位 | **非占位**（启发式 chars/3.5 + CJK） | `usage_estimator.go:35`；`ctx_compress.go:31` | 非 BPE 仍待接 |
| `public.sessions` 死表 | 待清理 | **仍活跃** | v2 聚合器/admin/turn_logs 多处引用 | 文档结论错，不可贸然清理 |
| OBS 整体开关 | 缺省未开 | **无独立总开关** | OBS 子项由 `dispatch_v2.enabled` 统管 | 文档假设需校正 |

**M3 裁决**：代码层面 **已全部实现**。剩余真实工作 = **M4 验证轨**（跑 S30-S35 全场景 + 注册 `LLM_GATEWAY_ADMIN_API_KEY_252` 部署 smoke + capabilities 自评 `partial`→对账校正）。

---

## 2. 校正后的真实待办（去阻塞后可并行）

### 立即可并行（无产品阻塞）
- **T1 M4 验证轨 + durable capabilities 对账**
- **T2 session_turns hot+分区表 + 6维补齐**（合并 Z2/Z4，同 touch session_turns 避免迁移冲撞）
- **T3 ursm/v2 off→shadow→canary 切换路径**
- **T4 dispatch 模型自动变更（P0-B）**
- **T5 Goal↔Handoff 四组件（P0-D，含契约冻结）**
- **T6 快速修复集合（P1-F balanced 审计口径勘误 + P0-E pending header 文档对齐）**
- **T7 v4 文档勘误（同步代码事实）**

### 需产品确认（阻塞，不在本轮并发）
- P0-A Admin V2 API 三件（46 号「等待确认」）
- P0-C credential_client_quota（租户配额语义）

### 长期/灰度依赖（后置）
- P0-F 旧存储双写移除（V2 稳定 ≥4 周）
- P1-E 真实 BPE tokenizer
- P1-A/B/C/OBS 收尾

---

## 3. 并发任务冲突矩阵

| 任务 | 主要触碰文件 | 与谁冲突 | 协调 |
|---|---|---|---|
| T1 验证轨 | `cmd/gateway/capabilities.go`、测试、env | T7（文档引用 capabilities 结论） | 先于 T7 |
| T2 session_turns | 新 migration、`dual_writer.go`、`session_aggregator.go`、`bg/partition_manager` | 无（独占 session_turns 写路径） | 内部 Z4→hot 顺序自洽 |
| T3 ursm/v2 | `domains/ursm/v2/*`、`main.go` 装配段 | 与 T1 同改 main.go 不同段 | 分段互不覆盖 |
| T4 dispatch 自动变更 | `domains/streaming/dispatch_gate.go`、`autoroute/*` | 无 | 独立 |
| T5 Goal↔Handoff | 新包 `domains/hooks/handoff/*`、`autoroute` | 无 | 独立 |
| T6 快速修复 | `cost_presets.go` 注释、v2/v4 口径文档与 handoff（不改 handler header） | 无 | 独立 |
| T7 文档勘误 | `docs/会话优化v4/*` | 只读代码 | 依赖 T1 结论 |

**可并发度**：T1/T2/T3/T4/T5/T6 七路互不阻塞；T7 等 T1 收口后并入。建议 T2 单独会话（迁移敏感）。

---

## 4. 每段验证基准（rule 11 §14）

- 写完 `.go`：`go build ./...`
- 写完 migration：`psql --dry-run -f <file>.sql` 或对等 schema lint
- 提交前：`go vet ./... && go test -race ./domains/streaming/... ./admin/... ./cmd/gateway/`
- T1：`go test -race ./domains/streaming/...`（含 `durable_scenario_test.go` S30-S35）
