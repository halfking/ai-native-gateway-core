# 01 · 现状基线与版本裁决

> 六套来源文档的逐套提取摘要 + 冲突速查表。供 v4 各主题文档引用。

---

## 1. 会话优化v2（08-06 ~ 08-13）

### 1.1 权威三件套
- **31-当前实现基线与修正决策**：ADR-V2-001（**历史错误记录**：原写 schema 为 `gateway.*`，老板 08-15 修正 + migration 513 证实全在 `public.*`）、ADR-GOAL-001（Goal retry 同步内部重试，HTTP 202 未实施）、ADR-TOKEN-001（holder=`{userKey}|{clientType}`，不用 IdentityHash）、ADR-HANDOFF-001（仅请求侧触发已实现）、Phase 前缀规范（V2-/Goal-/Handoff-）。
- **59-业务流程基准**：BP1 请求全链路（`dispatch_v2.enabled` 默认 ON + `KILL_DISPATCH_V2=1` kill-switch；首字节边界 `ChunkCount==0`）；BP2 Goal（P0/P1/P2 检测，balanced 不启用审计——勘误）；BP3 压缩与 V2 增量（DualWriter、SubmitMode P0-P5）；BP4 Token（Limiter 4 层内存 semaphore）；BP5 Dispatch（三层队列）；X-1~X-3 横切；9 个关键 migration。
- **60-核实报告**：PASS 49 / note 5 / fail 1（credential_client_quota 未实施）/ N/A 8 / defer 2。NOTE-2：DualWriter 实际检查 `sessions_v2.shadow_write`（非 `sessions_v2.enabled`）。

### 1.2 结论：已上生产
Session V2 影子写、Goal 全套、Handoff 请求侧、Token holder、Dispatch 三层队列（默认 ON）、V2 压缩读路径（`sessions_v2_compression_read` 默认 true）。

### 1.3 遗留待办（v4 接管）
P0：credential_client_quota、Goal↔Handoff 四组件、HTTP 202 异步重试、V1 下线、dispatch 模型自动变更。
P1：V2-P2.3~2.6 灰度、8 项 N/A runtime 验证、TPM 真实 tokenizer、DEF-004。balanced 审计项已裁决为无需代码修改：preset 保持 `UseAudit=false`，自动审计为 aggressive 专属。
P2：Admin V2 API 三件、`public.sessions` 死表清理、client_types 表、账户组共享队列。

---

## 2. 会话优化v3（08-14 ~ 08-15）

### 2.1 定位
会话语义管线为 **TARGET 态**（未接线）；**V3.3-OBS 可观测为 CURRENT 部分接线**（go-3 已有 OBS-BE1/3/5/6/7 + FE1/4/5 代码）。

### 2.2 7 条 ADR（设计契约，多数未接线）
V3-001 旁路异步、002 原始事实不可变、003 候选/事实分层、004 人工事实优先、005 租户与正文最小化、006 可降级可重算、007 稳定ID与聚类版本分离。

### 2.3 状态标签体系
CURRENT / CURRENT-PARTIAL / TARGET / NOT-WIRED / MIGRATION-GATE / HISTORICAL（v4 复用此标签）。

### 2.4 OBS 待办（未接线部分）
- OBS-BE2 / BE4 / BE8 队列/节点观测剩余项
- OBS-FE2 / FE3 前端观测面板剩余项
- OBS 未启用整体缺省（`sourceVersion` 已在 BE3 落地但整体未开）
- 30 号 handoff：下一阶段任务提示词（B1-B7 遗留清单）

---

## 3. 路由优化v3（URSM，08-12）

### 3.1 设计蓝本（v3.0）
P1-P6 原则、5 层流水线、状态层级 Provider→Credential→Model→Node→Binding、error_origin 三分类（client/gateway/upstream）、成本评分公式、L1/L2/L3 绑定、分级降级（circuit open 30s→half-open）、响应缓存回放（`X-Gw-Replay:true`，TTL 5min）、Hook 25+。

### 3.2 版本裁决（重要）
| 项 | 裁决 |
|---|---|
| 状态模型 | 代码已演进为 `ursm/v2` 三模式基座（CURRENT）；蓝本五维评分+P2C 为纸面 |
| 响应缓存 | `X-Gw-Replay:true` 已实现；TTL 5min 智能绑定部分纸面 |
| 智能绑定 | 纸面（待 M3 后接入） |
| Hook 插件化 | 纸面 |
| 分级降级 | circuit open 30s / half-open 已实现 |

> 详见 04 号文档 §4 URSM 裁决明细。

---

## 4. 修订0811（08-09 ~ 08-15，文档集 v1.6）

### 4.1 六轨任务
SR（供应商耗尽长时保活）/ SC（会话压缩）/ MM（多模态）/ ROUTE（路由）/ COST（评分口径）+ BND（绑定）。

### 4.2 里程碑进度（最新）
- M0/M1：✅ 完成（基线 main@96bc1425f）
- M2：✅ 完成（P2-C1 信封适配 + P2-S1 KeepaliveSender 删除；doc 23/24）
- **M3（durable）**：未开始，最小实施清单见 doc 24 §3
- **M4（验证）**：S30/S34/S35 有决策级覆盖；S31-S33 被 M3 阻塞；部署 smoke 被 `LLM_GATEWAY_ADMIN_API_KEY_252` 卡住

### 4.3 M3 最小实施顺序（doc 24 锁定）
EncryptWithAAD（无依赖）→ durable_llm_tasks migration+repo（依赖1）→ PG 回源读路径+PendingStore CAS（依赖1+2）→ CreateAndClaim/双 reaper/fencing → RecoveryWorker → 三协议装配（flag 已就绪，config.go:106-138）。

---

## 5. 自检优化（08-13）

### 5.1 已实现（✅ CURRENT）
- 统一持久队列 `credential_probe_queue`（`FOR UPDATE SKIP LOCKED` + lease_token 原子 claim + dedup 部分唯一索引）
- `ProbeService.Run` 单一执行拥有者；Round1 direct（proxy-aware）+ Round2 gateway（`X-LLM-Pin-Credential` 硬过滤）
- 常用模型分级自检：`probe.featured_models`（静态精选）∪ 用量 Top-N（`probe.featured_usage_top_n=20`）；深 ping 15min / 非常用 watchdog ×4 / 队列优先级 80 / 回退 ×0.5
- 状态回退：`probe_revert_at` 列 + `bg/probe_rollback.go` ticker + one-shot guard
- kill-switch：`LLM_GATEWAY_PROBE_QUEUE_ENABLED=false`

### 5.2 待办
- 过渡 C11：仪表板切到 `credential_probe_queue WHERE probe_command='node_probe'`（不阻塞）

---

## 6. 冲突速查表（v4 引用时以「以谁为准」列执行）

| # | 冲突点 | 说明 | 以谁为准 |
|---|---|---|---|
| 1 | schema 名 `public.*` vs `gateway.*` | 31（08-13）写 `gateway.*` 正式 schema；**老板 08-15 修正：全在 `public.*`**（分区表除外）；migration 513（08-14）已 DROP 整库 gateway schema、session_turns 等 4 表全部在 public | **代码 + migration 513（public.*）** |
| 2 | 压缩层是否已用 V2 | 43/44/45（08-06）称「只写不读」；59/60（08-13）证读路径已接通 | 59/60 |
| 3 | `sessions_v2_compression_read` 默认值 | 44 设计默认 false；59/60 实测默认 true | 59/60 |
| 4 | Flag key 名 | 59 §7.3 写 `sessions_v2.enabled`；代码实际 `sessions_v2.shadow_write` | 60（代码） |
| 5 | ConcSlot 并发限流是否生效 | 41 v1.0 误述；v1.1 勘误：`conc_slot_manager.go` 生产未接入，生效的是 `Limiter.AcquireAll` | 41 v1.1 / 31 |
| 6 | balanced 是否启用审计 | v2 26 号曾建议启用，但 v2 18 号原始成本分级、用户指南与实现均不启用 | 05 §2.1 勘误语义：balanced `UseAudit=false`；自动审计为 aggressive 专属 |
| 7 | URSM 状态模型 | 蓝本 v3.0 vs `ursm/v2` 三模式基座 | 代码（go-3 f249c4f4） |
| 8 | 会话语义管线（v3） | TARGET 态未接线 vs 当前以存储层（v2）为准 | 代码基线 |
| 9 | handoff 工作区 | v3 文档指向 go-3；本目录在 official-deploy | 合并视角（同 remote） |
| 10 | 会话管理架构 | **老板 08-15 修正：会话管理采用 `ursm/v2` 架构**（main.go:609 T20 已接线） | 代码 + 老板决策 |
| 11 | 缓存语义 | **老板 08-15 修正：缓存 = 会话三层数据**（原始多轮 → 压缩后多轮[可跨轮次] → 脱敏后发送），非 Redis/内存层 | 老板决策 |
| 12 | 会话存储维度与分区 | **老板 08-15 修正：项目+任务+会话+轮次+附属会话+附属信息分解；轮次用分区表（hot+分区表 session_turns_YYYY_MM）；保留旧模式（request_logs）双写逐步移除；session_turns 确认走 hot+分区表模式（对齐 request_logs_hot）；审计 08-15 补：session_turns 实列缺 project_id/namespace/parent_request_id/task_type（见 02 §1.1 现状 vs 目标 + 10 §P0-Z4）** | 老板决策 + 代码事实 |

---

## 7. 数据来源文件索引

| 目录 | 关键文件 |
|---|---|
| 会话优化v2 | 31 / 41 / 42 / 43 / 44 / 45 / 57 / 58 / 59 / 60 |
| 会话优化v3 | 01 / 12 / 23 / 24 / 25 / 28 / 30（+ 02/03/04 设计） |
| 路由优化v3 | 00 概览 / 01-09 / 11 / 13 / 17 |
| 修订0811 | 01 / 02 / 18 / 19 / 20 / 22 / 23 / 24 |
| 自检优化 | 00 / 01 / 02 |

---

**CHANGELOG**
- 2026-08-15 v1.3：审计修正——冲突 #12 补「session_turns 实列缺 project_id/namespace/parent_request_id/task_type」
- 2026-08-15 v1.2：老板确认 session_turns 走 hot+分区表（冲突 #12 补充对齐 request_logs_hot）
- 2026-08-15 v1.1：老板修正——冲突 #1 schema 裁决翻转为 public.*（migration 513 证实）；新增冲突 #10/#11/#12（ursm/v2 会话管理、缓存=三层数据、存储维度与分区）
- 2026-08-15 v1.0 初版（版本裁决）