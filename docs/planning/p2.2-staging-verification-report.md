# P2.2 路由优化 Staging 部署验证报告

**验证日期**: [YYYY-MM-DD]  
**验证人员**: [姓名]  
**环境**: Staging  
**Gateway 版本**: [commit hash]  
**部署指南**: `docs/deployment/p2.2-staging-deployment-guide.md`

---

## 1. 迁移脚本验证

### 1.1 数据库迁移状态

执行命令检查迁移表：
```bash
psql -h [staging-host] -U [user] -d [database] -c "\dt routing_*"
```

**预期输出**（应包含以下表）：
- `routing_feedback_log` (迁移 670)
- `routing_parameters` (迁移 671)
- `routing_user_affinity` (迁移 671)

**实际输出**：
```
[粘贴 \dt routing_* 的完整输出]
```

### 1.2 索引验证

```sql
SELECT tablename, indexname 
FROM pg_indexes 
WHERE tablename LIKE 'routing_%' 
ORDER BY tablename, indexname;
```

**实际输出**：
```
[粘贴 SQL 查询结果]
```

---

## 2. 环境变量配置

### 2.1 应用的配置

按 `docs/deployment/p2.2-staging-deployment-guide.md` §3 配置清单：

| 变量名 | 配置值 | 说明 |
|--------|--------|------|
| `ROUTING_OPT_ENABLED` | `true` | 启用路由优化 |
| `ROUTING_OPT_ADAPTIVE_LEARNING` | `true` | 启用自适应学习 |
| `ROUTING_OPT_AB_RATIO` | `0.10` | A/B 测试比例 10% |
| `ROUTING_OPT_CHECKPOINT_INTERVAL` | `5m` | checkpoint 间隔 |
| `ROUTING_OPT_REDIS_ENABLED` | `true` | 启用 Redis 缓存 |
| `ROUTING_OPT_BATCH_FEEDBACK` | `true` | 启用批量反馈写入 |
| ... | ... | [补充其他关键变量] |

**配置文件路径**: [例如 /etc/llm-gateway/env.staging]

---

## 3. 服务构建与启动

### 3.1 构建

```bash
# 构建命令
cd /path/to/llm-gateway-go-5
go build -o bin/gateway ./cmd/gateway

# 构建结果
[✅ 成功 / ❌ 失败 + 错误信息]
```

### 3.2 启动

```bash
# 启动命令
./bin/gateway [启动参数]

# 启动日志关键片段
[粘贴启动日志，重点：
- "Routing optimization enabled: true"
- "Adaptive learner checkpoint worker started"
- "Redis affinity cache initialized"
- "Batch feedback writer started"
]
```

### 3.3 健康检查

```bash
curl http://[staging-host]:8080/health
```

**响应**：
```json
[粘贴 health check 响应]
```

---

## 4. 功能验证

### 4.1 测试请求发送

使用测试脚本发送 100 条 AUTO 模型请求：

```bash
# 测试脚本
for i in {1..100}; do
  curl -X POST http://[staging-host]:8080/v1/chat/completions \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer [test-token]" \
    -d '{
      "model": "AUTO",
      "messages": [{"role": "user", "content": "测试消息 '$i'"}],
      "max_tokens": 10
    }'
  echo ""
done
```

**执行时间**: [HH:MM:SS - HH:MM:SS]  
**成功请求数**: [N/100]  
**失败请求数**: [M/100]  

失败请求错误摘要（如有）：
```
[错误类型及数量]
```

### 4.2 反馈日志写入验证

预期：100 条请求 × 10% A/B 比例 ≈ 10 条 feedback 记录

```sql
-- 查询测试窗口内的反馈日志
SELECT 
  COUNT(*) as total_feedback,
  COUNT(DISTINCT user_id) as unique_users,
  COUNT(DISTINCT task_type) as task_types,
  AVG(model_confidence) as avg_confidence
FROM routing_feedback_log
WHERE created_at >= '[测试开始时间]'
  AND created_at <= '[测试结束时间]';
```

**实际结果**：
```
 total_feedback | unique_users | task_types | avg_confidence 
----------------|--------------|------------|----------------
      [N]      |     [M]      |    [K]     |     [X.XX]
```

**分析**：
- 反馈日志数量是否在预期范围（8-12 条）？ [✅ 是 / ❌ 否]
- 如偏差较大，原因分析：[说明]

### 4.3 批量写入验证

检查批量写入是否正常工作（观察日志或 metrics）：

```bash
# 查看批量写入日志
grep "Batch feedback flush" /var/log/llm-gateway/gateway.log | tail -20
```

**日志输出**：
```
[粘贴批量刷盘日志，示例：
"Batch feedback flush: 5 records written in 12ms"
"Batch feedback flush: 8 records written in 15ms"
]
```

**批量统计**：
- 平均批次大小: [N 条]
- 平均刷盘耗时: [M ms]

---

## 5. 性能指标

### 5.1 路由耗时 (P99)

从 Prometheus 或日志中提取：

```promql
# PreClassify P99
histogram_quantile(0.99, 
  rate(routing_opt_preclassify_duration_ms_bucket[5m])
)

# RecommendModel P99
histogram_quantile(0.99, 
  rate(routing_opt_recommend_duration_ms_bucket[5m])
)
```

**实测结果**：

| 阶段 | P50 | P95 | P99 | 单位 |
|------|-----|-----|-----|------|
| PreClassify | [N] | [M] | [K] | ms |
| RecommendModel | [N] | [M] | [K] | ms |
| PostClassify | [N] | [M] | [K] | ms |
| **端到端路由** | [N] | [M] | [K] | ms |

**基线对比**（无路由优化时）：
- 端到端 P99: [X ms] → [Y ms] (变化: [±Z ms / ±W%])

### 5.2 Redis 缓存命中率

```promql
sum(rate(routing_opt_affinity_cache_hits[5m])) 
/ 
sum(rate(routing_opt_affinity_cache_requests[5m]))
```

**实测命中率**: [XX.X%]

**分析**：
- 首次启动预期命中率低，运行一段时间后应提升至 60%+
- 当前命中率是否符合预期？ [✅ 是 / ❌ 否]

### 5.3 数据库连接池

```sql
-- PostgreSQL 连接数
SELECT count(*) as active_connections
FROM pg_stat_activity
WHERE datname = '[database-name]'
  AND application_name LIKE '%gateway%';
```

**测试前连接数**: [N]  
**测试中连接数**: [M]  
**测试后连接数**: [K]  
**增量**: [M - N] 个连接

**连接池配置**：
- Max connections: [配置值]
- Idle connections: [配置值]
- 是否触发连接池上限？ [✅ 是 / ❌ 否]

---

## 6. Admin API 验证

### 6.1 整体统计

```bash
curl http://[staging-host]:8080/api/admin/routing-opt/stats
```

**响应**：
```json
{
  "overall_accuracy": [X.XX],
  "parameter_version": "[version-string]",
  "human_annotations_used": [N],
  "last_checkpoint_at": "[timestamp]"
}
```

### 6.2 按时间分组准确率

```bash
curl "http://[staging-host]:8080/api/admin/routing-opt/accuracy?hours=24"
```

**响应片段**：
```json
[
  {
    "hour": "[timestamp]",
    "task_type": "general_qa",
    "accuracy": [X.XX],
    "sample_count": [N]
  },
  ...
]
```

### 6.3 参数版本

```bash
curl http://[staging-host]:8080/api/admin/routing-opt/parameters
```

**响应**：
```json
{
  "version": "[version-id]",
  "created_at": "[timestamp]",
  "parameters": {
    "feature_weights": {...},
    "provider_scores": {...}
  }
}
```

---

## 7. 日志抽样检查

### 7.1 PreClassify 日志

```bash
grep "PreClassify" /var/log/llm-gateway/gateway.log | head -5
```

**示例日志**：
```
[timestamp] INFO PreClassify: user_id=[uid] task_type=general_qa affinity_found=true cached=true
```

### 7.2 RecommendModel 日志

```bash
grep "RecommendModel" /var/log/llm-gateway/gateway.log | head -5
```

**示例日志**：
```
[timestamp] INFO RecommendModel: task=general_qa selected=openai-gpt4 confidence=0.85 explore=false
```

### 7.3 错误日志

```bash
grep -i "error\|panic\|fatal" /var/log/llm-gateway/gateway.log | grep -i routing
```

**错误摘要**：
```
[如有错误，粘贴并分析；无错误则填 "无相关错误"]
```

---

## 8. 异常场景测试

### 8.1 Redis 不可用降级

**测试步骤**：
1. 临时关闭 Redis: `redis-cli shutdown`
2. 发送 10 条测试请求
3. 观察日志是否降级到直查 DB

**日志输出**：
```
[timestamp] WARN Redis unavailable, fallback to direct DB query
```

**结果**: [✅ 降级成功 / ❌ 服务异常]

### 8.2 数据库慢查询

（可选）使用 `pg_sleep` 模拟慢查询，验证超时处理。

**结果**: [描述或跳过]

---

## 9. 遗留问题与建议

### 9.1 发现的问题

1. **[问题描述]**
   - 严重性: [P0/P1/P2/P3]
   - 复现步骤: [说明]
   - 临时规避方案: [如有]

2. **[问题描述]**
   - ...

### 9.2 优化建议

1. **[建议内容]**
   - 预期收益: [说明]
   - 实施优先级: [高/中/低]

2. **[建议内容]**
   - ...

---

## 10. 验证结论

### 10.1 功能完整性

| 功能项 | 状态 | 备注 |
|--------|------|------|
| 迁移脚本应用 | [✅/❌] | |
| PreClassify 亲和力查询 | [✅/❌] | |
| RecommendModel 多目标推荐 | [✅/❌] | |
| PostClassify 置信度调整 | [✅/❌] | |
| 反馈日志异步写入 | [✅/❌] | |
| Redis 缓存 | [✅/❌] | |
| Prometheus 指标 | [✅/❌] | |
| Admin API | [✅/❌] | |
| 自适应学习 checkpoint | [✅/❌] | |

### 10.2 性能达标情况

| 指标 | 目标 | 实测 | 达标 |
|------|------|------|------|
| 路由 P99 延迟 | < 50ms | [N ms] | [✅/❌] |
| 缓存命中率 | > 50% | [X%] | [✅/❌] |
| 反馈写入成功率 | > 99% | [Y%] | [✅/❌] |

### 10.3 总体结论

**是否批准发布生产环境**: [✅ 批准 / ❌ 不批准 / ⚠️ 有条件批准]

**理由**：
[综合功能、性能、稳定性的评估结论]

**发布前必须解决的问题**（如有）：
1. [问题描述]
2. [问题描述]

---

## 附录

### A. 测试数据样本

**测试请求示例**：
```json
{
  "model": "AUTO",
  "messages": [
    {"role": "user", "content": "解释量子纠缠原理"}
  ],
  "max_tokens": 100
}
```

**反馈日志样本**（脱敏后）：
```json
{
  "request_id": "req_xxx",
  "user_id": "user_yyy",
  "task_type": "general_qa",
  "selected_provider": "openai",
  "model_confidence": 0.82,
  "latency_ms": 1250,
  "success": true
}
```

### B. 相关文档

- 设计文档: `docs/p2-ml-routing/p2.2-routing-optimization-plugin-design.md`
- 部署指南: `docs/deployment/p2.2-staging-deployment-guide.md`
- 迁移脚本: `pkg/db/migrations/000670_*.sql`, `000671_*.sql`

---

**报告生成时间**: [YYYY-MM-DD HH:MM:SS]  
**报告版本**: v1.0
