# 32 — OBS 审计修复与剩余验收 Handoff（2026-08-16）

> 状态：CURRENT。基线：`main@d0a0c0ac2`（OBS 审计修复已推送）；不得将下列真实上游项标为 PASS。

## 本轮已修复的 P1

1. **隐藏页恢复动作链缺失**：`web/src/composables/liveStreamStore.ts` 在页面隐藏时仍然不处理 SSE、不触发响应式写入；恢复可见且检测到遗漏事件时，改为关闭并重建 EventSource。既有连接初始化会回放 `initial_data`、`request_lifecycle` 与 `child_request`，不再只请求 `snapshot_refresh`。
2. **初始动作回放污染/遗漏**：`admin/live_stream_sse.go` 以最终发送的权威 snapshot 提取可见 request ID；`admin/live_stream_lifecycle.go` 从最多 500 条 Redis 候选中筛选这些 ID、租户可见性后按 `(ts, seq)` 升序发送最终最多 200 条。实时 fan-out 路径不受限制。
3. **父子索引恢复**：initial_data 重建 `children` 索引，重连后的 title/summary 子请求不再只能等待新帧才能显示。

## 唯一剩余验收项（需要测试环境真实上游）

### H1 — 成功流全动作链（#2，PARTIAL）

```text
在具备非生产测试上游凭据的环境，对一个真实流式请求抓取浏览器 SSE 帧及 UI 断言。必须证明：
arrive → route_resolved → model_enqueued → credential_selected → node_selected → upstream_request → first_byte → reply。
另强制制造一次节点失败/重试，证明 node_switch/retry（含 retry_seq）最终关联到 terminal reply。保存脱敏 SSE 样本、浏览器断言及请求 ID 到 doc 31；未完成前 #2 维持 PARTIAL。
```

### H2 — 强制启用回可用集（#3，BLOCKED）

```text
准备一个可控系统降级节点和候选缓存失效窗口。执行“强制启用”，持续观察节点状态/路由候选/实时流，验证 ≤30 秒回到可用集；同时验证手工禁用绝不被自动恢复。记录时间戳、节点 ID（脱敏）和 SSE/接口证据到 doc 31；未完成前 #3 维持 BLOCKED。
```

### H3 — 真实 title/summary 子请求可视化（#4，PARTIAL）

```text
用真实上游流量触发 title 或 summary 子请求，验证其 parent_request_id 出现在：
1) 首页实时请求流的父卡片 children/ActionTimeline；
2) 会话→轮次→子请求树。
覆盖一次页面隐藏后恢复，确认 EventSource 重连后 initial_data/replay 仍重建该 child 索引。保存脱敏 SSE 和浏览器断言到 doc 31；未完成前 #4 的这一子项保持 PARTIAL。
```

## 守则

- 新 SSE 字段仍必须 optional；正文、API key、系统 prompt 不得进流。
- 页面隐藏时必须零轮询、零 SSE 状态写入、零响应式渲染；恢复只通过权威 initial replay 或明确 snapshot 重新同步。
- 运行命令：后端 `go test ./internal/liveactions ./admin ./domains/dispatch ./domains/streaming/executors`；前端 `cd web && npx vitest run src/composables/liveStreamStore.actions.test.ts src/composables/liveStreamStore.test.ts`；构建 `cd web && npm run build`。
