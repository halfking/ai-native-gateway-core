# 30 — 下一阶段任务提示词（Handoff，2026-08-15）

> 状态：CURRENT（阶段交接提示词，可整段复制到新会话执行）
> 生成基线：commit `63230d42`（perf: 页面不可见零渲染/零轮询 + 路由热路径批量 MGET）。

## 0. 交接快照（新会话必读事实）

- 项目：`/Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3`（Go 模块 `github.com/kaixuan/llm-gateway-go`，前端 `web/` Vue3+TS）。
- 方案文档：`docs/会话优化v3/23~29`（23 总体方案 / 24 事件契约 / 25 节点状态与自检 / 26 前端设计 / 27 任务分解 / 28 子任务提示词 / 29 审计与修复记录）。
- 已完成：A1-A5 后端修复（紧急修复立即生效、URSM 整凭据清理、rate_limit 首败自检、node timeout 180s、resolve 负缓存）；B1（filterHealthyNodes 批量 MGET）；前端隐藏页零渲染/零轮询（probeStreamStore + SelfCheckPanel）。
- 待办主池：`27 号任务池`（OBS-BE1~7 / OBS-FE1~5 / OBS-DV1~2），细粒度提示词在 `28 号`；遗留缺陷 B2-B7 在 `29 号 §4`。
- 测试命令：后端 `go test ./<pkg>/...`；前端 `cd web && npx vitest run src/<dir>`。
- 硬约束：SSE 新字段全部 optional；正文/API key 不进 SSE；可观测写入旁路异步；颜色只用 `var(--kx-*)`；页面不可见不渲染不轮询（新代码必须延续）。

---

## P0 — 总控提示词（单会话推进整个 OBS 阶段）

```text
你在 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作（Go 后端 + web/ Vue3 前端）。
先按顺序读：docs/会话优化v3/23、24、25、26（方案与契约）→ 27（任务池与波次）→ 29 §4（遗留清单）。

目标：完成 V3.3-OBS 可观测性阶段的剩余任务（OBS-BE1~7、OBS-FE1~5、OBS-DV1~2，细粒度提示词在 docs/会话优化v3/28 号）。
执行方式：按 27 号波次推进——Wave1 后端并行项（BE3/4/5/6/7 可并行，BE1 先行）→ Wave2（BE2 依赖 BE1；FE 任务依赖对应 BE 契约，可先用 mock）→ Wave3 收口（DV1 契约冻结 + DV2 端到端验收）。
每完成一个任务：更新 docs/会话优化v3/08-执行记录.md 与 12 号基线，跑对应包测试，独立 commit（feat(obs): OBS-xxx <摘要>），分批推送 origin main（推送前 git pull --rebase，远端有并发提交）。
硬约束：SSE 新事件/新字段全部 optional 且旧客户端可忽略；正文、API key、系统 prompt 不得进 SSE；可观测写入旁路异步（有界队列+丢弃计数）；前端颜色只用 var(--kx-*)；页面不可见（document.hidden）时不渲染不轮询（参照 web/src/composables/probeStreamStore.ts 的 visibility 门控模式）。
完成定义：27 号任务池全部勾销 + DV2 验收报告 PASS + go build ./... 与 vitest 全绿。
```

## P1 — Wave1 后端并行批（BE3/BE4/BE5/BE6/BE7）

```text
你在 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作。
读 docs/会话优化v3/27（任务池）、28 号中 OBS-BE3/BE4/BE5/BE6/BE7 五个提示词，逐个执行（互相无文件冲突，可顺序完成、独立提交）：
- OBS-BE3 queue_snapshot 补 pipeline 层（depth/waiting p50p95/inFlight/degraded/sourceVersion，数据来自 domains/dispatch 真实指标，未启用时 omitempty 禁止零值冒充）
- OBS-BE4 node_update 投影字段补全（fp_disabled/disable_kind(manual|system)/system_recover_at/last_error_at）
- OBS-BE5 probe 三态查询 API + SSE 补 pending/completed（不改调度语义）
- OBS-BE6 会话轮次-子请求树 API（parent_request_id 关联，tenant 过滤，分页游标）
- OBS-BE7 state_transition 失败重放 + 定期清理（幂等 ON CONFLICT DO NOTHING）
每个任务：按 28 号提示词的验收标准写测试，go test + go vet 通过，独立 commit 并推送（先 pull --rebase）。
```

## P2 — Wave2 动作事件与 SSE（BE1→BE2）

```text
你在 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作。
读 docs/会话优化v3/24（动作事件词汇表 §2、SSE 扩展 §3、Redis 回放 §4、埋点锚点 §6）与 28 号 OBS-BE1/BE2 提示词。
1. OBS-BE1 动作事件发射器：ActionEvent 13 种动作、旁路异步（channel 满丢弃+计数）、Redis llmgw:live:actions 有界 5000（LPUSH+LTRIM）、按 24 号 §6 锚点埋点（arrive/route_resolved/model_enqueued/credential_selected/node_enqueued/upstream_request/first_byte/reply/node_switch/model_switch/retry），nil 安全。
2. OBS-BE2 SSE：envelope 新增 request_lifecycle（批量聚合 ≤500ms tick）与 child_request；initial_data 回放最近 200 条动作；契约快照测试保证旧 8 种事件不变。
注意：node_switch/retry 复用 NodeTracker（domains/streaming/executors/node_failover.go）现有状态，不要新建第二份状态；Emit 不得在请求热路径做同步 IO。
验收与提交方式见 28 号。完成后在 docs/会话优化v3/13 号契约追加新事件定义。
```

## P3 — Wave2 前端批（FE1~FE5）

```text
你在该仓库 web/ 前端工作（Vue3+TS）。读 docs/会话优化v3/26（前端设计）、24 §7、28 号 OBS-FE1~FE5 提示词。
顺序执行：FE1 liveStreamStore actions 索引（可先用 mock envelope 开发）→ FE2 按处理队列视图（QueuePerspectivePanel 升级 + NodeOpsRow + ActionTimeline）→ FE3 实时流卡片增强 → FE4 自检三态面板 → FE5 会话轮次下钻。
硬约束：颜色只用 var(--kx-*)；动画只用 transform/opacity；新数据右侧进入；OBS-BE3 的 pipeline 字段未上线时整行隐藏（禁止读零值冒充）；所有轮询与 SSE 状态写入必须带 document.hidden 门控（参照 probeStreamStore.ts / SelfCheckPanel.vue 既有模式）；列表超 100 条用截断或虚拟滚动保证 1000 卡片 ≥30FPS。
每个任务组件测试（vitest，现有 .test.ts 风格）通过后独立 commit：feat(obs): OBS-FEn <摘要>，分批推送。
```

## P4 — 性能专项（页面不可见治理推广 + 后端遗留）

```text
你在该仓库工作。读 docs/会话优化v3/29 §4（B 清单）与 web/src/composables/probeStreamStore.ts、liveStreamStore.ts 的 visibility 门控模式。
任务：
1. 全仓扫描 web/src 中 setInterval/EventSource 状态写入，凡无 document.hidden 门控的（如 BoardPanel/SessionStatsPanel/ops 视图轮询、api-system-monitor SSE 消费），统一加"隐藏跳过 + 恢复可见重同步"治理；重同步优先复用后端权威快照（initial_data/snapshot_refresh），避免本地补账。
2. 后端 OBS-BE1 落地后复核：动作事件发射在页面全隐藏时仍写 Redis——加采样/降频开关（如 hidden 期间只保留 state_change 与 terminal 动作），并在 29 号 §4 记录决策。
3. B2：bg/active_probe_worker.go LoadTarget 失败加 1 次短退避重试（5s），避免 DB 瞬断中断探测链。
4. B5：executor 主循环循环体内 defer 改为显式函数作用域或循环尾释放。
验收：vitest 全绿；go test ./bg/... ./domains/streaming/executors/ 通过；新增治理点列表写入 docs/会话优化v3/08-执行记录.md。
```

## P5 — Wave3 收口（DV1+DV2）

```text
你在该仓库工作。读 docs/会话优化v3/27 §3（风险与回退）、20 §9（指标）、28 号 OBS-DV1/DV2 提示词。
1. DV1：把本期新端点/新事件（request_lifecycle/child_request/queue_snapshot pipeline/node_update 新字段/probe 三态 API/session turns 树 API）冻结进 13 号契约；补 verify 脚本（模仿 verify-v3.3-docs.sh 的 rg 检查）防文档漂移。
2. DV2：新建 docs/会话优化v3/31-端到端验收报告.md 并逐项执行：真实流量下"到达→路由→选节点→切节点→重试(特别标)→回复"全链可见；强制启用后 ≤30s 回可用集（复测 29 号 §A1）；手工禁用不自动恢复、系统降级到点恢复；自检三态与右侧进入；会话→轮次→子请求下钻；SSE 延迟 ≤500ms、1000 卡片 ≥30FPS；页面隐藏期间零轮询（Network 面板核验）。
结果逐项 PASS/FAIL 记录，FAIL 项生成修复任务。全部 PASS 后在 00-README 索引标注系列 RELEASED。
```

## 附：遗留问题追踪（来自 29 号 §4）

B1 ✅ 已修（63230d42）｜B2 → P4｜B3 → P2（OBS-BE1 落地时复活或删除）｜B4 接受｜B5 → P4｜B6 → P1（OBS-BE3）｜B7 低优先。
