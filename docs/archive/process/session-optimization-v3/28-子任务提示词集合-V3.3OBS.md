# 28 — 子任务提示词集合（V3.3-OBS）

> 状态：CURRENT（2026-08-15）
> 用法：每个提示词独立投喂给一个并行执行代理。执行前必须先读 docs/会话优化v3/23~27 号文档对应章节。所有任务遵守：颜色只用 var(--kx-*)、可观测写入旁路异步、SSE 新字段全部 optional、正文/API key 不进 SSE。

---

## OBS-BE1 动作事件发射器

```text
你在 Go 项目 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作（模块 github.com/kaixuan/llm-gateway-go）。
先读 docs/会话优化v3/24-请求生命周期状态机与事件契约.md 的 §2 词汇表与 §6 埋点锚点，再读 23 号 §2 架构原则。

任务：实现请求生命周期动作事件发射器。
1. 新建包 admin 或 observability 下的 ActionEventEmitter（建议 internal/liveactions 或 admin/live_actions.go，与 live_stream_redis_store.go 同风格）：
   - 类型 ActionEvent{RequestID, Seq, Action, Ts, Model, CredentialID, ErrorKind, RetrySeq, Retry bool, Detail map[string]string}，Action 枚举严格按 24 号 §2 的 13 种。
   - 发射接口 Emit(ctx, ActionEvent)：内部异步（带缓冲 channel，满即丢 + 原子计数 live_actions_dropped_total 暴露给 metrics/prometheus 或现有 stats 口径）。
   - Redis 写入 llmgw:live:actions（LPUSH+LTRIM 有界 5000），复用 live_stream_redis_store 的 Redis client 注入方式；Redis 不可用时静默降级。
   - Seq 分配：per request_id 单调递增（进程内 sync.Map 计数即可，跨进程不要求严格全局序，前端按 (ts, seq) 排序）。
2. 在 24 号 §6 列出的锚点埋点：
   - handler.go 请求到达（arrive）、resolve 完成（route_resolved，含 auto 决策摘要）
   - domains/dispatch pipeline.Submit（model_enqueued）、dispatcher 凭据入队（node_enqueued）
   - executors/router.go PlanCandidates 输出（credential_selected）、executor 主循环每候选开始（upstream_request）、first_byte、reply（成功/失败含 error_kind）
   - NodeTracker 切换（node_switch，from/to/reason/retry/retry_seq）、模型切换点（model_switch）
   - 重试 retry_seq 递增时 retry:true 特别标。
3. 发射器通过现有依赖注入模式（如 executor params / handler 构造参数或全局 setter）接入，cmd/gateway/main.go 装配；nil 时所有 Emit 为 no-op（保证测试零依赖）。
4. 单元测试：不阻塞热路径（Emit 在 channel 满时立即返回）、丢弃计数递增、13 种动作各至少一条发射断言（可用 fake redis 或接口 stub）。
5. 运行 go test ./... 相关包与 go vet，保证编译通过。不要修改无关代码。提交信息：feat(obs): OBS-BE1 request lifecycle action event emitter。
```

---

## OBS-BE2 SSE envelope 扩展与回放

```text
你在 Go 项目 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作。
先读 docs/会话优化v3/24 号 §3/§4 与 13-V3.2实际API与SSE契约.md（现有 envelope 约定）。
前置：OBS-BE1 的 ActionEventEmitter 已合入（若无则按接口 mock，但类型名必须一致）。

任务：扩展 /api/admin/live-stream SSE。
1. envelope 新增 type: "request_lifecycle"（payload: 24 号 §2 动作事件）与 "child_request"（payload: 父 request_id + request 同构对象，来源 auto_title/summary/sensitive_word 建立关联时发射）。
2. initial_data 时回放 Redis llmgw:live:actions 最近 200 条（LRANGE 0 199），按 (ts,seq) 排序后逐条发送。
3. 动作事件以 ≤500ms 聚合 tick 批量推送（避免每动作一条 SSE 帧打爆带宽）；聚合帧 type 仍为 request_lifecycle，payload 为数组。
4. 兼容性：新字段全部 optional；写 SSE 契约快照测试（模仿现有 live stream SSE 测试），断言旧 8 种事件类型不变、未知类型对旧客户端无害（仅序列化层验证）。
5. 运行相关 go test 与 go vet。提交信息：feat(obs): OBS-BE2 SSE request_lifecycle/child_request + replay。
```

---

## OBS-BE3 queue_snapshot 补 pipeline 层

```text
你在 Go 项目 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作。
先读 docs/会话优化v3/13 号 §4（queue_snapshot 冻结结构）与 docs/会话优化v2/57 号 Tier 结构、domains/dispatch/pipeline.go。

任务：把 queue_snapshot 的 pipeline 层从 TARGET 变 CURRENT。
1. LiveQueueSnapshot 增加 pipeline: {depth, waitingMsP50, waitingMsP95, inFlight, degraded} 与顶层 sourceVersion（单调递增整数）。
2. 数据来自 domains/dispatch Pipeline 的真实指标（等待时长用环形窗口计算 p50/p95；in_flight 用在途计数；degraded= 任一调速器 disabled/溢出标记）。
3. dispatch 未启用时 pipeline 字段缺省（omitempty），enabled/wired 保持现有语义；禁止零值冒充（depth=0 且有数据时是真实 0）。
4. 契约测试：启用 dispatch_v2 后字段快照；未启用时字段缺失（不是零值）。
5. go test + go vet。提交信息：feat(obs): OBS-BE3 pipeline queue snapshot。
```

---

## OBS-BE4 node_update 增强

```text
你在 Go 项目 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作。
先读 docs/会话优化v3/25 号 §1 状态全景、admin/live_stream_sse.go 的 node_update 构造处。

任务：节点投影字段补全。
1. LiveNodeStatus 增加：fp_disabled bool、fp_disabled_until（credentialfpslot NodeState 投影，按节点任一绑定模型取最近一次禁用）、disable_kind: "manual"|"system"|""（manual_disabled→manual；availability/quota/circuit 非 ok→system）、system_recover_at（DB availability_recover_at/quota_recover_at/cooling_until 最早者）、last_error_at。
2. 投影频率与现有 node_update 相同，不新增状态存储（ADR-V3-103）。
3. 字段快照测试。
4. go test + go vet。提交信息：feat(obs): OBS-BE4 node_update projection fields。
```

---

## OBS-BE5 probe 三态 API 与 SSE

```text
你在 Go 项目 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作。
先读 docs/会话优化v3/25 号 §6（自检队列三态流转）、bg/probe_queue.go（PG 租约队列）、bg/probe_lifecycle_sink.go、admin/probe_stream_sse.go。

任务：
1. 新增 GET /api/admin/probe/tasks?status=pending|in_flight|completed&limit=（默认 50）：读 probe_queue 三态；completed 保留最近 N=200 条（表内或查询窗口）。
2. probe SSE 增补 pending/completed 事件（现stream 已有 in_flight 类事件则对齐命名）；事件含 origin(scheduled/error/manual)、attempt、next_retry_at（退避下一跳，供前端展示智能回退）。
3. 定期自检到期入队、active probe 只生成第一跳的语义不要改动——只做查询与推送，不改调度。
4. API 权限沿用现有 probe admin 路由；负向测试（401/403）。
5. go test + go vet。提交信息：feat(obs): OBS-BE5 probe queue tri-state API + SSE。
```

---

## OBS-BE6 会话轮次-子请求树 API

```text
你在 Go 项目 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作。
先读 docs/会话优化v3/26 号 §5、admin/session_online.go、domains/session/v2/turn_reader.go、request_logs_hot.parent_request_id。

任务：GET /api/admin/sessions/{id}/turns（分页）返回每轮 {turn_number, request_id, status, model, latency, child_requests: [{request_id, request_type(title|summary|sensitive_word), status, latency}]}。
1. 子请求经 parent_request_id 关联；request_type 读 request_logs.request_type（510 迁移列），缺失时回退 X-Gw-Source-Actor 头推断。
2. tenant 过滤与权限沿用现有 session admin 端点；分页游标用 (turn_number, request_id)。
3. 测试：正常、空会话 404、跨租户 403、归档轮次覆盖（session_turns 与 request_logs_hot 双源一致性按 12 号口径标注 TODO 不阻塞）。
4. go test + go vet。提交信息：feat(obs): OBS-BE6 session turns child-request tree API。
```

---

## OBS-BE7 state_transition 旁路补齐

```text
你在 Go 项目 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 工作。
先读 docs/会话优化v3/12 号 §3（state_transition_logger 现状与 TODO）、admin/state_transition_logger.go、request_state_transitions 表（511 迁移）。

任务：补齐状态变更旁路：失败重放（写失败进内存重试队列，指数退避 3 次）、定期清理（保留 7 天）、启动时回放中断批次。
1. 不阻塞请求路径（旁路异步，与现有 logger 同模式）。
2. 幂等：重放按 (request_id, seq) ON CONFLICT DO NOTHING。
3. 测试：失败注入 → 重放成功；清理任务删除过期行。
4. go test + go vet。提交信息：feat(obs): OBS-BE7 state transition replay + cleanup。
```

---

## OBS-FE1 liveStreamStore 扩展

```text
你在项目 /Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go-3 的 web/ 前端工作（Vue3 + TS）。
先读 docs/会话优化v3/24 号 §7、web/src/composables/liveStreamStore.ts。

任务：
1. 类型：ActionEvent（24 号 §2 字段）、LiveRequest 增加 stage/retrySeq/parentRequestId/requestType。
2. state 增加 actions: Map<string, ActionEvent[]>（每请求 cap 50、全局 cap 2000，LRU 淘汰）；handleEnvelope 处理 request_lifecycle（批量数组与单条两种帧）与 child_request（挂到 parent 的 children 索引）。
3. 导出 actionsRef 与 getRequestActions(request_id) computed 工具。
4. 单测模仿 liveStreamStore 现有测试风格（可新建 liveStreamStore.actions.test.ts）：乱序到达按 seq 排序、cap 淘汰、旧类型事件忽略。
5. npm test（或项目现有 test 命令）通过。提交信息：feat(obs): OBS-FE1 live stream actions store。
```

---

## OBS-FE2 按处理队列视图

```text
你在项目 web/ 前端工作。先读 docs/会话优化v3/26 号 §2（布局与交互）、QueuePerspectivePanel.vue、NodeStatusMatrix.vue、web/src/api/routing.ts（emergency-repair 调用）。

任务：实现「按处理队列」视图（实时请求流 tab 内二级切换）。
1. QueuePerspectivePanel 升级：顶部队列总览条（pipeline depth/waiting/inFlight + degraded 徽标，OBS-BE3 字段未上线时隐藏该行，禁止零值冒充）；左侧模型队列列表；中部请求动作流（小形态卡：stage 徽标 + 最新动作 + 左端客户端侧/右端当前节点，右侧进入动画只用 transform）。
2. 新组件 NodeOpsRow.vue：节点行（状态徽标组 circuit/availability/quota/manual、disable_kind 区分手工与系统、system_recover_at 显示预计自动恢复）+ 操作按钮：测试（POST /api/admin/providers/{id}/test-now，行内结果 toast）、强制启用、手工禁用（下拉含清除熔断/重置错误，调 PATCH /api/routing/emergency-repair）。
3. 新组件 ActionTimeline.vue：请求大形态（点击小卡展开），完整动作时间线，node_switch/retry 打特别标（描边+角标）。
4. 颜色只用 var(--kx-*)；三态 Skeleton/Empty/Error；操作后依赖 node_update 事件刷新。
5. 组件测试（现有 .test.ts 风格）+ 视觉走查。提交信息：feat(obs): OBS-FE2 queue perspective view。
```

---

## OBS-FE3 实时请求流卡片增强

```text
你在项目 web/ 前端工作。先读 docs/会话优化v3/26 号 §3、LiveRequestStreamV2.vue、LiveRequestBlock.vue。

任务：卡片增加 stage 徽标与 retrySeq 角标（retry≥1 时显示 ⭐r{n}）；右上角子请求计数徽标（children 数，类型缩写 T/S/SW），点击弹出子请求列表面板（数据来自 OBS-FE1 的 children 索引，缺失时调后端查询兜底——接口未上线前仅用 SSE 数据）。
组件测试覆盖徽标渲染与空态。提交信息：feat(obs): OBS-FE3 live card stage/retry/children badges。
```

---

## OBS-FE4 自检 tab 三态队列

```text
你在项目 web/ 前端工作。先读 docs/会话优化v3/25 号 §6.2、26 号 §4、web/src/composables/probeStreamStore.ts 与自检 tab 现有组件。

任务：自检 tab 重排为三段（待请求/正在请求/已完成）：
1. pending 小卡（目标节点、模型、origin、预计执行时间）；in_flight 卡实时耗时；completed 大卡可展开（结果/延迟/错误码/观察时间），失败卡显示退避下一跳时间。
2. 新数据从右侧进入（transform 动画），completed 保留最近 200 条（后端 cap）。
3. 数据：SSE + 新 tri-state API（OBS-BE5；未上线前 SSE-only 并显示降级提示）。
4. SSE 断线重连态展示。组件测试 + 视觉走查。提交信息：feat(obs): OBS-FE4 probe tri-state panel。
```

---

## OBS-FE5 会话轮次下钻

```text
你在项目 web/ 前端工作。先读 docs/会话优化v3/26 号 §5、SessionStatsPanel/SessionTurnsPanel/SessionTurnDrawer 现状。

任务：在线会话列表（会话键/用户/轮次数/最近活动/健康度）→ 会话详情轮次时间线 → 每轮内联子请求树（title/summary/sensitive_word 徽标 + 状态 + 延迟）。数据来自 OBS-BE6 API（未上线前用现有 timeline 数据并标注降级）。分页/空态/权限态沿用规范。组件测试。提交信息：feat(obs): OBS-FE5 session turns drilldown。
```

---

## OBS-DV1 契约冻结与漂移检查

```text
你在项目根工作。先读 docs/会话优化v3/13 号与 15 号（漂移检查脚本风格）。
任务：把 OBS-BE2/3/4/5/6 的新端点与新事件类型追加进 13 号契约（标注 CURRENT 与上线日期）；为 docs/会话优化v3 写 verify-v33obs-docs.sh：rg 检查文档引用的路径/端点与代码一致（模仿 verify-v3.3-docs.sh）。运行通过后提交：docs(obs): OBS-DV1 contract freeze。
```

---

## OBS-DV2 端到端验收清单

```text
你在项目根工作。先读 docs/会话优化v3/27 号 §3 与 20 号 §9 指标。
任务：编写 docs/会话优化v3/30-端到端验收报告.md 骨架与执行清单：
1. 真实流量下全链可见性：到达→路由→选节点→切节点→重试（特别标）→回复（含错误）在按处理队列视图逐项核验。
2. 节点操作：test-now 即时结果；强制启用后 ≤30s 回可用集（结合 29 号 §A 修复复测）；手工禁用不自动恢复、系统降级到点自动恢复。
3. 自检三态流转与右侧进入；会话→轮次→子请求下钻。
4. 性能：SSE 延迟 ≤500ms、1000 卡片 ≥30FPS。
用 curl/脚本 + 浏览器人工核验双轨，结果逐项记录 PASS/FAIL。提交：docs(obs): OBS-DV2 e2e checklist。
```
