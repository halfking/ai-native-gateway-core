# M2 四轨收尾审计报告（RT-2 / CO-5 / SR-13 / MM-2）

**审计范围**：M2 余项四轨（基线 d2da09ed8 → 合入点 0aa2348ce，含远端同期 eb9d47e86）
**审计提交**：RT-2 bb5264de5 ｜ CO-5 8773550f3..84deed488（5 commits）｜ SR-13 59c9a9b40..296fb7b9f（9 commits）｜ MM-2 67fef888d..815de24bc（3 commits）
**改动量**：约 30 文件，+2737/−308
**审计方法**：逐轨独立核验（不信执行者自述：diff 审查 + 测试逻辑真实性抽查 + `go test -race` 实跑）→ 逐轨 fix-review 差分复查 → 集成交叉点收口审计（本报告）
**审计日期**：2026-08-15

---

## 执行摘要

**总评**：A−（四轨全部达标，红线全绿，无 P0/P1；P2×5，其中 1 项为真实跨模块兼容性发现）
**P0**：0 ｜ **P1**：0 ｜ **P2**：5

**红线核验结论**：
- flag-off 字节/行为等价：四轨均以测试钉住（RT-2 OffIsByteIdentical 精确 `==`；MM-2 executor golden 逐字节；CO-5 回归钉住；SR-13 零事件钉住），全部实跑通过。
- 全部新开关默认关闭：`LLM_GATEWAY_ROUTING_W_COST/W_IQ`、`sessions_v2.request_bodies_full`（显式全量排障开关，摘要模式本身受 sessions_v2.enabled 门控）、`LLM_GATEWAY_ATTACHMENT_URL_FETCH_FALLBACK`。
- `go test -race`：受影响包全绿（metrics、streaming 全树、executors、attachments、session/v2、hooks 全树 20 包、settings、autoroute、cmd/gateway）；集成后 main 复跑亦绿。

---

## 1. 数据流溯源

- ✅ RT-2 `CostWeight/IQWeight`：来源=env 装配（沿用 W_* 惯例）→ 用途=calculateLoadScore 惩罚项 → 去处=P2C 评分（有量化 gap 测试证明消费）。
- ✅ SR-13 `CandidateOutcome.ProviderID`：写入点 foldCandidateOutcomes 唯一，读取点 survival metrics provider 标签；该结构无 JSON/DB 序列化出口，无外部契约影响。
- ✅ CO-5 摘要信封：`_gw_body_summary{mode,bytes,sha256,head,head_truncated}`，哨兵（""/null/{}）透传，NULL 语义与 ON CONFLICT COALESCE 未动。
- ✅ MM-2 回退：仅网关 base URL 前缀匹配 → relPath 遍历防护（含 `..` 拒绝）→ 自身存储取回（无外网请求）→ base64 data URI 内联。

## 2. 业务流程闭环

- ✅ SR-13：survival 九指标中 7 个接入协调器全部决策点（5 终态出口、retry/wait 分派、等待窗口、keepalive 写失败、ResumeBlocked）；每次 Run 恰好 +1 requests_total（测试钉住）。active_tasks/lease_conflicts 语义属 durable worker，M3 接入（正确裁决，非缺口）。
- ✅ MM-2：取回失败/超 provider inline 上限 best-effort 保 URL，不吞请求；按 relPath 去重单次取回；重复 URL（历史重放）全内联。
- ✅ CO-5：摘要化永不失败写入（marshal 失败回退原文）。

## 3. 状态机

- ✅ survival 状态转换：running↔retry_now/waiting_recovery + 终态 + 断连 cancelled，转换对测试覆盖。
- ✅ CO-5 flag 组合四态表驱动（sessions_v2 × request_bodies_full）。

## 4. 并发安全

- ✅ 四轨零新增 goroutine；CO-5 seam 用 atomic.Value（与 sessionv2mirror 同惯例）；promauto 计数器线程安全；`-race` 全绿。

## 5. 数据兼容性（发现 1 项真实交叉点）

- ❌ **P2-C1（CO-5 跨模块）**：admin 侧 `logs_summary.go`（会话日志预览）、`no_topic_session.go`（消息列表）、`session_title.go`、`compression_stats.go` 均 LEFT JOIN `request_logs_bodies_with_current_month` 读全文。sessions_v2 摘要模式下这些读取方会拿到信封——head 2048 字节保留、预览可用，但**标题生成与压缩统计会降级**。建议：读取方检测 `_gw_body_summary` 键并适配展示（徽标/截断标记），或这些 V1 视图在 sessions_v2 部署态明确降级语义。
- ✅ settings 新 key 默认 false + hot-reload + spec 清单钉住，旧配置无感知。
- ✅ 指标系列更名 `gateway_survival_active_durable_tasks`→`active_tasks`：原指标无生产者、仓内零引用（grep 核实），对齐 doc 18 §15.1。

---

## P2 清单汇总

| ID | 轨 | 内容 | 建议 |
|---|---|---|---|
| P2-C1 | CO-5 | admin bodies 读取方遇信封降级（见 §5） | 读取方检测信封键适配 |
| P2-M1 | MM-2 | `bytes.ReplaceAll` 全 body 子串替换，URL 恰现于文本也会被替换 | sha256 路径不可猜测，风险可忽略；后续改坐标级替换 |
| P2-M2 | MM-2 | 回退取回在候选循环内同步执行，启用时增出站延迟 | 已 flag 门控；量大时可加 LRU 缓存 |
| P2-R1 | RT-2 | flag-off 时 LOAD_SCORE_V2 采样日志多两个 0 值字段 | 良性，保留 |
| P2-S1 | SR-13 | `KeepaliveSender` 为死代码（生产零调用，帧格式会击穿严格客户端） | 清理轨直接删除（含其测试） |

## doc 20 行动项勾销

- **E-P2-3 ✅**：Anthropic→OpenAI 桥多模态改写落地（`RewriteAnthropicBody` 按 ClientProtocol 分派；测试 stub 与真实 `convertImageBlock` 映射逐分支核对一致，生产装配点 main.go:1050 核实注入真实转换器）。
- **E-P2-6 ✅**：executor 级 flag-off 字节回归落地（golden 钉住旧管线输出）。

## doc 21 §6 遗留项状态更新

- 遗留第一项（KeepaliveSender 并入串行写通道）：**已由远端协作者提交 600f6069e（doc 22 `serializedResponseWriter`）解决**，SR-13 轨的"仅评估"结论被自然取代，两者互补无冲突。
- 遗留第二项（SR-13 指标）：本审计四轨已完成，✅。
- 遗留第三项（部署态 e2e 归 M4）：维持。

## 正面亮点

- 四轨均以"公开接口 + 量化断言"钉住行为（RT-2 gap=weight×penalty、CO-5 pgxmock 实参 matcher、SR-13 计数器 delta、MM-2 golden 字节），非形状测试。
- MM-2 每候选从原始 body 派生改写体的 failover 隔离设计正确延续了 MM-1 模式。
- CO-5 的信封设计（sha256+长度+截断头）保留了审计可关联性，降采样不丢失可定位性。

## M3 前置门禁核对（durable）

1. Phase1 全绿 ✅（本审计覆盖）。
2. `EncryptWithAAD`（SR-16/SC-6 共用）⏳ 待实施。
3. PG 回源可用 ⏳ 待实施。
4. 新增建议：P2-C1 在 sessions_v2 生产放量前处理（否则 admin 视图降级不可见）。

---

**审计完成**：2026-08-15 ｜ 审计人：ZCode（M2 四轨主会话）
**下一审计**：M3 收口（SR-W3 durable + MM-4 E2E + CO-3 对账）
