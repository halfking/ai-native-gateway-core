# 当前实现差距与迁移

> v3 实施前必读：事实记录当前代码现状、迁移策略、与 v3 已有设计的对接方式。
> 配套 00~09 + 10 文档。
> 基于代码审计（2026-07-13）。

## 1. 代码现状（事实记录）

### 1.1 已上线路径（生产路径）

| 关注点 | 关键位置 | 关键事实 |
|--------|----------|----------|
| 认证 + 限流 | `auth` / `apihub` 包 | RPM/TPM/Key Concurrent 已就绪 |
| 路由决策 | `router` 包 | 硬过滤 + cost 选择 |
| 资源门禁（活跃） | `credentialfpslot.FPSlotManager` | Redis Lua acquire / release |
| 断路器 | `domains/credential/breaker.go` | 半开计数单字段；阈值 2（`autoRecoveryFailureThreshold`） |
| 健康检查器 | `credentialhealth.Checker` | 1h 滑动窗口，5 样本阈值，80% 失败率 |
| 失败日志 | `candidate_failure_logs` | Citus Columnar（append-only） |
| 状态写入 | 4 处 goroutine 各自写 | `credentials.availability_state` |
| 写降级 | `WriteOnError` + `StateObserver.UpdateOnFailure` + `auto-cool` | Kind 白名单见 §3.1 |
| Command entry | `cmd/gateway/main.go:2020` | 仅 `audit_trimmer` Start |
| FailureLogger | `cmd/gateway/main.go:882-885` | 注入但 DI 失败 nil-silently |

### 1.2 已写但未集成（死代码）

| 路径 | 状态 |
|------|------|
| `domains/ursm/recorder.go` (221 行) | 0 调用 |
| `domains/ursm/recorder_scripts.go` (114 行 Lua) | 0 注册 |
| `domains/ursm/keys.go` (56 行) | 0 调用 |
| `domains/ursm/manager.go` (165 行) | 0 注入 |
| `domains/ursm/state.go` | 0 调用 |
| `domains/ursm/cost_scorer.go` (439 行) | 占位 stub |
| `domains/ursm/fp_slot_manager.go` | 0 注入（main.go 用的是 `credentialfpslot`） |
| `domains/ursm/conc_slot_manager.go` | 0 注入 |
| `domains/ursm/validator.go` / `error_classifier.go` | 0 调用 |
| `sql/objects/tables/state_change_log.sql` | 无 migration |
| `bg/handoff_trimmer.go` | 未注册到 main |
| `bg/opslog_trimmer.go` | 未注册到 main（且因 Columnar 无效） |
| `domains/routing/candidate_failure_logger.go` | 与 `domains/streaming/executors/candidate_failure_logger.go` 重复，建议删除 |

### 1.3 今日临时修复（待部署，已在 HEAD 未提交）

| 位置 | 改动 |
|------|------|
| `candidate_failure_logger.go:124-131` | INSERT 失败改 `slog.Warn`（替换 `//nolint:errcheck`） |
| `executor.go:1610-1616` | FailureLogger nil 时 `slog.Warn` |
| `bg/audit_trimmer.go` | TTL 已接入（`main.go:2020-2024`） |
| `bg/handoff_trimmer.go` | TTL 已写但未 Start |
| `bg/opslog_trimmer.go` | TTL 已写但未 Start（且因 Columnar 无效） |
| `db/migrations/360_drop_redundant_handoff_logs_index.sql` | 索引清理 |

---

## 2. 关键代码引用（必须精确理解）

### 2.1 `shouldWriteCredentialState` 白名单

```go
// domains/streaming/executors/executor.go:3092-3102
func shouldWriteCredentialState(kind errorsx.ErrorKind) bool {
    switch kind {
    case errorsx.KindAuth, errorsx.KindAuthRevoked,
         errorsx.KindQuota, errorsx.KindQuotaPeriodic, errorsx.KindQuotaBalance, errorsx.KindQuotaPermanent,
         errorsx.KindConcurrent, errorsx.KindRateLimit,
         errorsx.KindStreamTimeout:
        return true
    default:
        return false
    }
}
```

**P0 修复**：白名单加 `KindModelNotFound, KindUpstreamDown`（见 v3 §05 §2.2 修订）。

### 2.2 FailureLogger 注入路径

```go
// cmd/gateway/main.go:882-885
if dbConn != nil {
    routingExec.FailureLogger = executors.NewCandidateFailureWriter(dbConn.Pool())
    slog.Info("candidate_failure_logger initialized")
}
```

`dbConn.Pool()` 可能为 nil → 仍构造非 nil writer → LogFailure 内部 `if w == nil || w.pool == nil ... return`（`candidate_failure_logger.go:95-97`）→ 完全静默。

### 2.3 候选失败写入路径（修复前/后）

修复前：
```go
// candidate_failure_logger.go:43（修复前）
_, err := w.pool.Exec(ctx, `INSERT INTO candidate_failure_logs ...`)
//nolint:errcheck  ← 静默吞错
```

修复后：
```go
// candidate_failure_logger.go:124-131
if err != nil {
    slog.Warn("candidate_failure_logger: insert failed",
        "error", err, "request_id", requestID, "credential_id", credentialID, ...)
}
```

### 2.4 auto-cool 门槛

```go
// bg/candidate_failure_monitor.go:89-95
coolMinSamples = 20 attempts / 5min
coolRatio     = 0.8
coolMinutes   = 2
```

实际生产流量远不达 240 req/h门槛。建议分层（详见 12 文档 §P1）。

---

## 3. 迁移策略

### 3.1 优先级总表（v3 模块 vs 现状 → 迁移路径）

| 优先级 | v3 模块 | 当前路径 | 迁移动作 | 工时 |
|--------|---------|----------|----------|------|
| **P0** | §05 ModelNotFound 白名单 | `executor.go:3092` | 1 行修改 + 部署 | 0.5 天 |
| **P0** | H.3 staleness monitor | 无 | 新建 `bg/candidate_failure_log_health.go` | 0.5-1 天 |
| **P0** | H.1 self-test | `main.go:882` | 启动 30s INSERT ... RETURNING 自检 | 0.5 天 |
| **P1** | §05 完整分级降级 | `shouldWriteCredentialState` + `Breaker` | 替换 `shouldWriteDBDegradation` + 加 halfOpenFailures | 5 天 |
| **P1** | H.5 门槛分桶 | `bg/candidate_failure_monitor.go:89-95` | hot/warm/cool 分桶 + p95 计算 | 1 天 |
| **P1** | §06 IdempotentCache 联动 PendingStore | `domains/streaming/idempotent.go` | 已有 pending/pending.go 联动 | 3 天 |
| **P2** | §07 资源门禁 v3 实现 | `credentialfpslot` 双套并存 | 切换到 `ursm.*` + AcquireUntil Pub/Sub | 6-8 天 |
| **P2** | §08 StateRecorder | 4 goroutine 各自写 | 接入 main.go 替换（消除 H.6 死代码） | 8-10 天 |
| **P2** | H.7 多写收敛 | 见 §D | 短期 OptimisticLock + StateRecorder 化 | 3-5 天 |
| **P3** | §03 三级绑定 | 弱 | 新建 `binding` 包 + CAS Lua | 5-7 天 |
| **P3** | §04 成本评分 | 占位 | `ursm/cost_scorer.go` 完整实现 + P2C | 3-4 天 |
| **P3** | §02 Hook 插件化 | `domains/hooks/` 基础 | 25+ Hook 注入 + 5 个内置插件 | 5-7 天 |

### 3.2 迁移原则

1. **最小侵入**：P0 修复不修改 v3 主体，仅补 patch
2. **特性开关**：所有 P1/P2 必须通过 `routing.ursm.<phase>.*` 热更新 flag，默认 off，逐步灰度
3. **可回滚**：每次迁移 commit 对应一个 kill-switch flag，可一键关闭
4. **可观测**：每个迁移步骤附带 Prometheus metrics + health check
5. **旧路径并行**：新路径启用后，旧路径 inline 保留 1 个 sprint，确认无回归再删除

### 3.3 旧路径退役时间表

| 路径 | 状态 | 退役点 |
|------|------|--------|
| `credentialfpslot.FPSlotManager` | P2 后 deprecated；P3 完全替换后删除 | P3 完结 + 1 sprint 灰度 |
| `executor.WriteOnError` | P1 后仅保留 fallback | P2 完结 |
| 多 goroutine 写 `availability_state` | P2 后仅 StateRecorder 写 | P2 完结 |
| `Writer` 中 `shouldWriteCredentialStateOnConfirmedFailure` | P1 替换为 `shouldWriteDBDegradation` | P1 完结 |
| `Breaker.consecutive` 单字段 | P1 加 `halfOpenAttempts/Failures` 双字段 | P1 完结 |
| `HealthChecker` 滑动窗口 | P1 增加 `consecutiveSuccessReset` | P1 完结 |
| `domains/ursm/` 完整包 | P0-P2 逐步激活 | P2 完结 |

### 3.4 关键不变量（迁移期间必须保持）

1. **客户端 cancel 不计为供应商错误**（v3 §01 S6）
2. **弱绑定会话按成本门控重评估**（v3 §01 S18）
3. **强绑定仅上游错误才切**（v3 §03 §5 切换矩阵）
4. **探测确认后方可恢复**（v3 §05 §3.3）
5. **`error_origin` 标签贯穿全链路**（client/gateway/upstream）
6. **失败日志 `//nolint:errcheck` 修复已合并**（INSERT 失败必须 slog）

---

## 4. 验证方案

### 4.1 单元测试清单（必须覆盖）

- `shouldWriteDBDegradation` 对每个 ErrorKind 的分支（≥12 个 kind）
- ModelNotFound 落入 IsImmediatePersist 集合
- UpstreamDown 落入 IsImmediatePersist 集合
- Breaker `halfOpenAttempts` / `halfOpenFailures` 计数正确
- `consecutiveSuccessReset` 清除窗口失败记录（3 次连续成功场景）
- staleness gauge 在 INSERT 成功后归零
- FailureLogger nil self-test 启动失败行为

### 4.2 集成测试场景（v3 §01 §10 + v3 10 文档 §3）

- S-SenseNova-NIM-model-not-found：模拟 glm-5.2 → 19/25 → model_not_found → cmb FALSE
- S-CandidateFailureLog-Pipeline-Down：停掉写入 → 30min 后告 webhook
- S-Monitor-Threshold-VS-Traffic-Mismatch：低流量凭据按 cool bucket 触发
- S9 + S24 + S25：5xx 闪烁 / 持续 / 恢复完整路径

### 4.3 回归测试

- 现有 19/25 不再被路由到（manual cool + P0 修复后）
- 探测确认后恢复 active
- breaker / checker / StateRecorder 三方协作正确
- 旧路径（feature flag off）行为不变

### 4.4 灰度方案

| 阶段 | 流量比例 | 时长 | 验证 |
|------|----------|------|------|
| Smoke | 0%（feature flag off） | 1 天 | 二进制稳定 |
| Canary | 10%（仅 71/154） | 3 天 | 业务指标不劣化 |
| Partial | 50% | 1 周 | 自检指标无异常 |
| Full | 100% | 持续 | 全量切换 |

---

## 5. 与其他文档的关系

- 详见 10 文档 §1（故障归因）、§2（8 项遗漏）
- 详见 12 文档（实施规划）
- 详见 13 文档（风险与回滚）
