# 执行记录

> 追加式 ledger。只记录已经发生且有证据的动作；方案内容和历史快照不在这里改写。

## 2026-08-15：V3.3-OBS DV2 浏览器补测（第二轮）

### 31号 §3 #1/#5/#6 补测全通过

- record_id: `v33obs-20260815-dv2browser`
- status: RELEASED
- 环境：headless Chromium 151（browser-use CLI，1728×1080）；本地 Docker 网关 `:8781`（`c7224329` 后 main 构建）+ 本机 PG `llm_gateway` + Redis db2 + 自签 license；#6 用网关直接服务 SPA（`LLM_GATEWAY_STATIC_DIR=/app/web/dist`，同构生产同源拓扑）。
- 验收内容与证据（详见 31号 §3.1，截图 `docs/会话优化v3/gui-screenshots/dv2-01..09`）：
  1. 按处理队列视图一屏三层（队列透视 95/89/5/5 + NodeOpsRow + 处理轨迹）；
  2. 大形态 18 卡 + ActionTimeline 弹层（到达/路由完成 candidates 0，含时间戳）；
  3. 暗色主题整页正常；
  4. NodeOpsRow 交互：test-now 路由命中（同步副本凭据 manual_disabled/keyring 缺失返回结构化 404，属数据态）；enable/disable 全链路（candidate_cache_cleared=1 + 幂等键 + DB 翻转）API 复核通过；
  5. FPS：1137 卡 + 持续 SSE 负载 10s rAF 72FPS；2404 卡连续滚动 55FPS；中位帧 8.3ms / p95 16.7ms（静态 4FPS 为 headless 无重绘伪影，空视图对照 118FPS 排除节流）；
  6. SSE 断线重连：硬停网关 2s 内 已连接→重连中，重启后 2s 内恢复并 initial_data 回放 3 条种子请求（完整时间序列记录在 31号 §3.1 T7）。
- 附带修复（测试环境准备中发现）：`config.LoadFromEnv` 未读取 `LLM_GATEWAY_LICENSE_PUBLIC_KEY/_PRIVATE_KEY/_AES_KEY`（struct env 标签仅为文档，yaml mergeFrom 也不覆盖 License* 字段）——补 env 读取（与 yaml 优先级语义一致：file values overridden by env）。
- rollback: revert commit 即可

### migration 515 的 252 PG up/down 演练

- record_id: `v33obs-20260815-mig515`
- status: RELEASED
- 环境：env-injector inject 252（loader llm-gateway-go@115.29.212.252），SSH tunnel 15432→172.16.2.210:5432，PG 17.10；演练前 pg_dump 备份表 schema（5417 bytes）+ 行数基线 0。
- 步骤与结果：预检（252 已应用过 515）→ UP 幂等重放（IF NOT EXISTS NOTICE ×2 提交）→ POST_CONDITION 三项（seq 列 / 唯一索引 btree(request_id,seq) / ON CONFLICT 重放计数=1，探针行清理）→ DOWN（索引+列干净移除）→ 再 UP（可重新前滚）→ 行数 0→0 数据零触碰 → 留置应用态（seq+索引在位）。
- 备注：252 的 request_state_transitions 无 tenant_id 列（511 建表变体），幂等探针按实际列集执行；普通表 DDL 未触发 Citus event trigger。
- 证据：31号 §3.2；门禁关闭（27号 §4）。
- rollback: down.sql 已验证可用

## 2026-08-15：V3.3-OBS 收口后审计修正

### OBS-BE2 审计修正：空转 Redis 开销 + 布防残余窗口

- record_id: `v33obs-20260815-be2audit`
- status: RELEASED
- 审计发现：
  1. 无 SSE 客户端时 pollLiveActions 仍每 250ms LRANGE ≤500 条（共享 Redis 常驻 ~400KB/s 扫描流量，仓库有 Redis slowlog 事故史）；
  2. `74f3a894` 的 actionArmed 布防在"客户端刚连接、initial_data 回放已读、光标尚未布防"的 ≤250ms 间隙仍可能漏投新事件。
- 修正：无客户端直接跳过轮询（at-least-once 语义保持：接入后首 poll ≤500 条追赶帧与回放重复，前端按 (request_id, seq) 折叠）；删除布防分支——cursor 为空即"全部按新事件投递"，彻底消除吞事件窗口。
- 测试：三个 poll 用例改为新语义（积压首投/at-least-once/无新增不推）；race + admin/liveactions 全测 + go vet + build 通过；本地 Docker 实流回归（空转 2s → 接入客户端 → arrive/route_resolved seq 1/2 均实时送达）。
- rollback: revert commit 即可

## 2026-08-15：V3.3-OBS 收口（OBS-BE2 / FE2 / DV1 / DV2 + BE2 修复）

### OBS-BE2 SSE envelope 扩展

- record_id: `v33obs-20260815-be2`
- status: RELEASED（commit `a4a4a821`）
- scope: admin/live_stream_lifecycle.go（新）、live_stream_lifecycle_test.go（新 21 用例）、live_stream_sse.go（envelope Action/ParentRequestID、cfg ActionPollInterval/ActionReplayLimit、Run actionTicker、broadcast 路径 rememberActionTenant+child 帧、HandleLiveStream 回放挂载、Stats 2 指标）、live_stream_redis_store.go（payload 保留 parent/type + normalizeLiveRequestType）
- action: request_lifecycle（250ms tick 消费 `llmgw:live:actions`，cursor 推进 + 单帧聚合 ≤500ms；detail 摊平顶层；state_change 节点维度排除）；initial_data 后 LRANGE 0 199 按 (ts,seq) 回放；child_request 对带 parent 的请求额外发帧；requestType 归一 24号 §3 冻结词表；LiveRequest 序列化追加 camelCase alias（零值不冒充）；租户隔离 = 有界 request_id→tenant 索引 + Redis 全局 detail 兜底，归属未知只发超管。
- review: 双轴（standards/spec）子代理评审；修复 state_change 通道违例、requestType 词表漂移、错误包装 3 项。
- tests: `go test ./admin ./internal/liveactions`、race 通过；`go vet`/`go build` 干净
- rollback: revert commit 即可
- next_gate: OBS-DV2 实流验收

### OBS-BE2 修复：空列表启动布防吞事件

- record_id: `v33obs-20260815-be2fix`
- status: RELEASED（commit `74f3a894`）
- action: DV2 本地实流发现 hub 启动时列表为空、首个 poll 延后导致首个真实事件（arrive）被 cursor 布防分支吞掉；引入显式 `actionArmed`，布防后列表为空则后续事件全部实时送达。回归测试 BootAgainstEmptyListDeliversFirstEvent。
- evidence: 修复后实测 arrive 110ms / route_resolved 31ms 实时送达（31号 §2）

### OBS-FE2 按处理队列视图

- record_id: `v33obs-20260815-fe2`
- status: RELEASED（commit `0f0e60e5`）
- scope: QueuePerspectivePanel.vue（pipeline 总览行 + 节点操作区 n/total 截断标注）、NodeOpsRow.vue（新：状态徽标组/最近延迟错误/test-now/强制启用/手工禁用下拉三动作+系统降级说明）、ActionTimeline.vue（新：seq 升序、detail chips、retry/switch 描边）、RequestTile.vue（大形态 ≈{n} 轨迹徽标 + Teleport 面板）、RequestProcessingTrail.vue（动作事件驱动）、liveStreamStore.ts（BE3/BE4 线上字段类型）、liveStreamDisplay.ts（actionEventLabel）
- tests: FE2 相关 35 用例；全量 vitest 54 文件 307 用例；vue-tsc 0 错误；vite build 通过
- rollback: revert commit 即可
- next_gate: OBS-DV2 浏览器可视化验收（BLOCKED 项见 31号 §3）

### OBS-DV1 契约冻结 + 漂移脚本

- record_id: `v33obs-20260815-dv1`
- status: RELEASED（commit `c3104e9b`）
- scope: 13号追加 V3.3 章节 §9–§12（envelope 契约/词表/回放/预算/租户隔离/BE4 投影/pipeline CURRENT/前端消费契约 + stage 缺口显式记录）；verify-v3.3-contract.sh（新，57 项三方断言）
- tests: 57/57 PASS；既有 verify-v3.3-docs.sh 17/17 保持 PASS

### OBS-DV2 端到端验收

- record_id: `v33obs-20260815-dv2`
- status: PARTIAL（协议级 PASS；浏览器项 BLOCKED）
- environment: 本地 Docker 网关（74f3a894）+ 本机 PG 同步副本 + 本机 Redis db2 + vite dev；本地自签测试 license；真实 /v1 流量（不存在模型，零上游计费）
- evidence: 见 `31-端到端验收报告-V3.3OBS-20260815.md`（PASS 10 项含 ≤500ms 实测、缺陷修复；BLOCKED 7 项含浏览器可视化/完整动作链/30s 强制启用/FPS）
- rollback: 本机痕迹清理说明见 31号 §1；容器与 vite 已停止
- next_gate: 桌面浏览器会话补测 31号 §3 BLOCKED 项；515 migration 252 up/down 演练

## 2026-07-28：建立 v3 方案基线

| 字段 | 内容 |
| --- | --- |
| record_id | `v3-doc-20260728-001` |
| status | `DOC-BASELINE` |
| scope | 会话语义、任务/项目/岗位归类、聚类、实体关系、记忆、人工闭环、评估 |
| action | 建立 `docs/会话优化v3/00–08` 文档集 |
| source evidence | `request_logs` schema、V2 session schema/baseline、session title/summary/cluster handlers、Memora pipeline 文档；OmniRoute 仅有父仓 `docs/omniroute-ref/` 参考文档，未发现其 memory 源码 |
| external research | Mem0、Graphiti、Microsoft GraphRAG、Neo4j LLM Graph Builder、Letta、BERTopic、Label Studio、Basic Memory、LangMem、Memori、Cognee、Claude Constellation；LongMemEval、LoCoMo、DialSummEval、GoodAI LTM |
| production code changed | 否 |
| SQL migration changed | 否 |
| wiring changed | 否 |
| tests/deploy | 未执行；仅写入后续门禁模板 |
| rollback | 删除本次新增文档即可；未触及生产数据和配置 |
| next gate | V3-P0：确认 canonical input、脱敏、事件/outbox、RLS fixture、feature flags 和 worker owner |

## 2026-08-15：V3.3-OBS Wave1 执行（OBS-BE1~7 / FE1~5 / DV1~2）

### OBS-FE1 liveStreamStore 动作事件索引

- record_id: `v33obs-20260815-fe1`
- status: RELEASED（本地）
- scope: web/src/composables/liveStreamStore.ts + liveStreamStore.actions.test.ts（新增）
- action: 按 24 号 §2/§3/§7 扩展 store——ActionEvent 类型（13 种动作 + per-action detail 字段，全部 optional）、LiveRequest 增 stage/retrySeq/retryReasonClass/parentRequestId/requestType；envelope 增 request_lifecycle（单条/批量数组双形状）与 child_request 帧；actions Map（每请求 cap 50、全局 2000 LRU 淘汰，seq 升序稳定插入、重复 seq 替换）+ children 索引（per-parent cap 50）；导出 actionsRef/childrenRef/getRequestActions/getRequestChildren；未知事件类型静默忽略。
- tests: `npx vitest run src/composables/liveStreamStore.actions.test.ts src/composables/liveStreamStore.test.ts` → 35/35 通过（新增 16 + 现有 19 无回归）
- rollback: revert 对应 commit 即可，纯前端 store 层。
- next_gate: OBS-FE3（卡片徽标消费 stage/retrySeq/children）与 OBS-BE2（后端事件源）。

### OBS-BE7 state_transition 旁路补齐

- record_id: `v33obs-20260815-be7`
- status: RELEASED（本地；515 迁移待 252 PG up/down 验证）
- scope: domains/dispatch/state_transition_logger.go（重写可靠性层）、state_transition_logger_test.go（新增 5 测试）、sql/migrations/startup/515_state_transitions_seq_unique.{sql,down.sql}
- action: 失败重放（内存重试队列，指数退避 1s/2s/4s 共 3 次，队列上限 10000 淘汰最老）；幂等 INSERT（seq 进程内单调计数，(request_id, seq) 唯一索引 + ON CONFLICT DO NOTHING，migration 515）；周期清理 goroutine（启动即清 + 每 1h，保留 7 天）；Stop() 幂等优雅退出并 flush+重放积压。启动回放：重试队列纯内存、无持久化积压，重启即清空，无需回放（文件头注明）。
- tests: `go test ./domains/dispatch/` ok（含新增 5 用例）；`go vet ./domains/dispatch/` 通过
- rollback: revert commit + scripts/sql-rollback.sh 515
- next_gate: 515 与 511 一起在 252 环境 up/down 验证

### OBS-BE6 会话轮次-子请求树 API

- record_id: `v33obs-20260815-be6`
- status: RELEASED（本地）
- scope: admin/session_turns_tree.go（新增）、admin/session_turns_tree_test.go（新增 8 测试）、admin/handler.go（仅路由注册）
- action: `GET /api/admin/sessions/{id}/turns?limit=&cursor=`——每轮 {turn_number, request_id, status, model, latency, child_requests[]}；主请求自 `request_logs_with_current_month`（parent_request_id 为空，turn_number 按 ts 派生），子请求按 parent_request_id 批量关联；request_type 优先 510 列（title_gen→title 等），回退 origin_actor（X-Gw-Source-Actor 已确认落库 request_logs_hot.origin_actor）。分页游标 base64+HMAC 绑定 session；权限沿用 session admin 模式（401/403 跨租户/404 空会话）；响应仅元数据（反射测试硬断言无 body/prompt）。
- tests: `go test ./admin/` ok（新增 8 用例）；`go vet ./admin/` 通过
- known_gaps: sensitive_check 尚无写入方（回退归 other）；session_turns 与 request_logs_hot 双源合并按 12 号口径标 TODO
- rollback: revert commit 即可
- next_gate: OBS-FE5 前端下钻消费

### OBS-BE5 probe 三态查询 API + SSE 补全

- record_id: `v33obs-20260815-be5`
- status: RELEASED（本地）
- scope: bg/probe_queue.go（ProbeTaskDetailSink 富化转换：origin/next_retry_at，失败重臂 pending 事件）、admin/probe_stream_sse.go（ProbeStreamTask 增 origin/next_retry_at_ms optional 字段 + PublishProbeTransition 适配 + Origin 缺省推导）、admin/probe_dashboard.go（GET /api/admin/probe/tasks?status=pending|in_flight|completed&limit=，completed 窗口 200，仅元数据无 body_preview）、admin/probe_tasks_tristate_test.go（新增 7 测试）、cmd/gateway/main.go（SetProbeTaskDetailSink 装配）
- action: 调度语义未动（定期到期入队、active probe 首跳不变）；SSE 事件命名对齐现有 submitted/started/completed/failed；detailSink 优先于 legacy probeSink 保证每转换恰好一次。
- tests: `go test ./admin/ -run 'TestQueryProbeTriState|TestHandleProbeTaskList|TestPublishProbeTransition|TestPublish_OriginDerived'` 7/7 PASS；`go test ./bg/` ok
- rollback: revert commit 即可
- next_gate: OBS-FE4 前端三态队列消费

### OBS-BE1 请求生命周期动作事件发射器

- record_id: `v33obs-20260815-be1`
- status: RELEASED（本地）
- scope: internal/liveactions/{liveactions.go,liveactions_test.go}（新包）；埋点：domains/streaming/handler.go（arrive/route_resolved/no_route/first_byte）、executors/executor.go（credential_selected/upstream_request/reply/node_switch×2/model_switch/no_route）、executors/executor_dispatch.go（v2 路径 credential_selected/upstream_request）、executors/node_failover.go（删除死代码 SendNodeJumpEvent/SendRetrySSE，新增 emitNodeSwitch）、dispatch/pipeline.go（model_enqueued/node_enqueued）、dispatch/dispatcher.go（model_switch/no_route）、dispatch/failover.go（node_switch retry:true/cred_switch）、dispatch/forwarder.go（node_selected）；装配 cmd/gateway/main.go + main_dispatch.go；测试 domains/dispatch/pipeline_liveactions_test.go
- action: ActionEvent{request_id, seq, action(13 种), ts, model, credential_id, error_kind, retry_seq, retry, detail}；Emit 非阻塞（缓冲 channel 满即丢 + live_actions_dropped_total，Prometheus 自定义 Collector）；Redis llmgw:live:actions LPUSH+LTRIM 5000（worker goroutine、detached ctx 2s 超时，失败计数 live_actions_redis_failures_total 静默降级）；per-request seq 进程内单调、终态回收；nil emitter 全点 no-op。死代码处置：删除 node_failover.go 的 SendNodeJumpEvent/SendRetrySSE（与 ADR-V3-101 单 SSE 出口冲突且零调用），语义经 emitNodeSwitch 复活（29 号 B3 关闭）。
- tests: go build ./... ok；go test -count=1 ./internal/liveactions/ ./domains/dispatch/ ./domains/streaming/ ./domains/streaming/executors/ ./admin/ ./cmd/gateway/ 全 ok（executors 一次组合跑偶发失败、复跑与单独跑均通过，同 29 号记录的 Redis 端口偶发）
- rollback: revert commit 即可（埋点全部 nil-safe no-op）
- next_gate: OBS-BE2（SSE request_lifecycle/child_request + initial_data 回放）

### OBS-FE5 会话轮次下钻

- record_id: `v33obs-20260815-fe5`
- status: RELEASED（本地）
- scope: web/src/api/sessionTurnsTree.ts（新，保留 HTTP 状态码的错误封装）、web/src/components/session/{OnlineSessionsPanel,SessionTurnsTimeline,SessionDrilldownPanel}.vue（新）+ 3 测试文件、DashboardViewV2.vue（stats tab 插入下钻面板）
- action: 在线会话列表 → 会话详情轮次时间线（游标分页）→ 每轮内联子请求树（T/S/SW/C 徽标+状态+彩延迟）。错误态区分 401/403/404/网络；null 延迟渲染「未知」不冒充 0；后端未提供的用户/轮次数/健康度列不渲染；本 tab 手动加载无轮询（无需 visibility 门控，组件头注明）。注意：turns 端点延迟字段为 `latency`（毫秒，null=未知），与 online/timeline 的 `latency_ms` 不同。
- tests: `npx vitest run src/components/session/` 14/14 通过；i18n parity 2 例失败为 HEAD 预存（dispatchWaterfall 等 key 仅 en-US/zh-CN 有），与本任务无关，收口阶段补齐
- rollback: revert commit 即可
- next_gate: OBS-DV2 浏览器验收

### OBS-FE4 自检 tab 三态队列

- record_id: `v33obs-20260815-fe4`
- status: RELEASED（本地）
- scope: web/src/components/probe/ProbeTriStateQueue.vue（新）+ .test.ts（9 测试）、web/src/views/SelfCheckPanel.vue（旧三泳道替换）、web/src/composables/probeStreamStore.ts（origin/next_retry_at_ms optional 字段、envelope 解析修正、connection 状态导出）、web/src/api-selfcheck.ts（fetchProbeTriStateTasks）
- action: 三段式（pending 小卡含 origin 徽标与预计执行倒计时 / in_flight 实时耗时 / completed 大卡可展开+失败卡退避下一跳）；TransitionGroup 右侧进入（只 transform/opacity）；completed cap 200 对齐后端；SSE 为主 + 15s tri-state API 对账（document.hidden 跳过）、API 全败 SSE-only 降级提示、断线 chip。
- tests: `npx vitest run src/components/probe/` 9/9 通过；相关既有测试 28 通过；vue-tsc 本任务文件零错误
- rollback: revert commit 即可
- next_gate: OBS-DV2 浏览器验收（SSE 断线重连实测）

### OBS-BE3 queue_snapshot 补 pipeline 层

- record_id: `v33obs-20260815-be3`
- status: RELEASED（本地）
- scope: domains/dispatch/queue_metrics_pipeline.go（新：PipelineQueueStats 聚合）、queue_metrics_collector.go（SnapshotView 增 sourceVersion/Pipeline）、admin/live_stream_sse.go（LiveQueueSnapshot 增 SourceVersion/Pipeline，均 omitempty）、cmd/gateway/main_v32_wiring.go（透传）、测试 queue_metrics_pipeline_test.go + live_stream_queue_snapshot_test.go
- 口径：depth=Σ Tier-1+Tier-2 等待；waitingMsP50/P95=最近 ≤200 样本 T0→T6 nearest-rank 分位数（无样本字段缺失）；inFlight=Σ dispatch_in_flight gauge；degraded=任一 governor disabled 或 tick 间 dispatch_overflow_total 增长（首 tick 只记基线）；sourceVersion=原子单调递增。未启用/未接线时 pipeline+sourceVersion 整体缺省，enabled/wired 语义不变，零值冒充防护有契约测试。
- tests: go test -count=1 ./admin/ ./domains/dispatch/ ok（新增 10 用例）；go vet 通过
- rollback: revert commit 即可
- next_gate: 13 号 §4 的 pipeline TARGET 注记改 CURRENT（OBS-DV1）

### OBS-FE3 实时请求卡增强

- record_id: `v33obs-20260815-fe3`
- status: RELEASED（本地）
- scope: web/src/components/RequestTile.vue（stage 徽标/⭐r{n} retry 角标/子请求计数徽标+Teleport 弹出面板）、web/src/types/swimlane.ts（RequestTile 增 stage/retrySeq/parentRequestId/requestType optional）、RequestTile.lifecycle.test.ts（新 7 测试）
- action: 实际渲染链路 LiveRequestStreamV2→SwimLane→SwimLaneTrack→RequestTile（LiveRequestBlock 仅 V1 用）；子请求面板只消费 FE1 的 children 索引（child_request SSE 推送），无后端查询；空态文案等待 child_request；字段缺省不渲染不冒充；颜色仅 var(--kx-*)，动画仅 transform/opacity（prefers-reduced-motion 降级）。
- tests: vitest 14/14（新 7+既有 7）；相关回归 44+22 通过；vue-tsc exit 0
- rollback: revert commit 即可
- next_gate: OBS-FE2（按处理队列视图复用 RequestTile 小形态）

### OBS-BE4 node_update 投影字段补全

- record_id: `v33obs-20260815-be4`
- status: RELEASED（本地）
- scope: admin/live_stream_sse.go（LiveNodeStatus 增 5 optional 字段）、cmd/gateway/main_livestream.go（provider 快照扩展 + nodeDisableKind/earliestTime/fpNodeProjection/decorateFPNodeState 纯函数）、cmd/gateway/main.go（fpslot Manager 注入 1 行）、测试 live_stream_sse_test.go（+2）+ main_livestream_provider_test.go（+3）
- 口径：fp_disabled/fp_disabled_until 来自 credentialfpslot NodeState（任一绑定模型禁用中；冷却到期残留不算，与 IsUsable 一致）；disable_kind=manual 优先（manual_disabled），否则 availability/quota/circuit 非 ok→system；system_recover_at=DB 三列最早非空；last_error_at=fpslot LastFailureAt 最大值。RTT 控制：1 条 SQL + 1 次 GetNodeStatesBatch MGET（复用 63230d42 批量接口），投影频率仍为既有 2s refresh，不新增状态存储（ADR-V3-103）。
- tests: go test -count=1 ./admin/ ./cmd/gateway/ ok（新增 5 用例含 JSON 契约）
- rollback: revert commit 即可
- next_gate: OBS-FE2 节点面板消费 disable_kind/system_recover_at

## 后续记录模板

```text
### YYYY-MM-DD：<阶段/变更>

- record_id:
- status: PLANNED | IN_PROGRESS | SHADOW | GATED | RELEASED | ROLLED_BACK
- scope:
- commit:
- migration:
- owner/wiring:
- feature_flags:
- tests:
- real_db_rls_evidence:
- metrics:
- rollout:
- rollback:
- known_gaps:
- next_gate:
```

## 2026-08-14：统一 v3.3 基线与双轨任务拆解

- record_id: `v3-doc-20260814-004`
- status: `DOC-BASELINE`
- scope: 统一 V3.0 Semantic Track 与 V3.2 Observability Track；补齐真实代码基线、实际 API/SSE 契约、逻辑点拆解、漂移和迁移门禁
- action: 新增 `12-20260814-当前实现基线与缺口.md`、`13-V3.2实际API与SSE契约.md`、`14-统一逻辑点与任务拆解.md`、`15-检查、漂移与迁移门禁.md`；更新 README、语义 DDL 状态声明
- evidence: `admin/` 路由与 handler、`cmd/gateway/main_v32_wiring.go`、`domains/dispatch/`、`domains/session/v2/`、`sql/migrations/startup/510*`、`511*`、前端 `.v32wip` 文件和 2026-08-13 既有审计结论
- corrected: 旧执行记录中“路由未注册/provider 未注入”属于 2026-08-13 历史状态；当前基线改为后端 CURRENT-PARTIAL，前端、审计、真实 PG/RLS 和浏览器验收仍未闭环
- production code changed: 否（仅文档）
- migration changed: 否（既有 510/511 只做事实核对）
- tests/deploy: 文档静态检查待执行；未执行部署和真实 PG/Redis 验证
- rollback: 仅回滚本次文档变更，不触及代码、数据库和配置
- next_gate: V32-LP1/L2，先冻结实际契约并完成 510/511 真实 migration/RLS 门禁

## 2026-08-14：HEAD 包含 schema 统一提案，但 migration 513 进入阻断复核

- record_id: `v3-impl-20260814-005`
- status: `MIGRATION-BLOCKED`
- commit: `f39321e55`
- scope: migration 513 试图将 V2 schema 统一到 `public.*`，清理重复 `gateway.*`，并给 `public.session_turns` 增加 V3.2 T0-T9 队列时间戳；同步调整 writer、reader、partition、admin turns 和双读验证路径
- evidence: `sql/migrations/startup/513_schema_unification_and_session_turns_dual_write.{sql,down.sql}`、`cmd/gateway/main.go`、`domains/dispatch/queued_request.go`、`domains/session/v2/`、`admin/session_*`
- production code changed: 已进入 HEAD；本次文档提交不修改业务代码
- migration: 513 up/down 已存在但未通过审计；up 的注释声称清理 `gateway.*`，实际 DROP `public.*`，且 DROP 后 ALTER `public.session_turns`，禁止部署
- known_gaps: down 脚本涉及重建 schema 和删除 T0-T9 列，无法替代数据恢复；511 状态表 tenant/RLS 也未闭环；V2 shadow/dual-write 运行时证据、分区实际状态、前端和浏览器验收仍待完成
- next_gate: 修正 513 + 511 tenant/RLS，完成备份、真实 PG up/down、恢复演练和 public.session_turns 对账

## 2026-08-13：建立 V3.2 可观测性 + 会话浏览基线

- record_id: `v3-doc-20260813-002`
- status: `IN_PROGRESS`
- scope: 统一实时请求流（总/模型/节点三流后端合流）、节点操作 API、主从请求展示、在线会话与多轮次浏览；**不含**语义/聚类/记忆
- action: 新增 `09-V3.2整合方案与代码现状对齐.md`、`10-任务分解与并行执行计划-V3.2.md`、`11-子任务提示词集合-V3.2.md`
- source evidence: 代码现状探索（live-stream Redis+SSE、trace 12 阶段、streamretry、provider_credential 状态、probe、auto_title parent_request_id、domains/session Redis、SessionStatsPanel）
- key decisions: ADR-V3-101 后端合流单 SSE；ADR-V3-102 复用 request_id/parent_request_id；ADR-V3-103 节点状态以 credentialhealth/circuit 为唯一真相；ADR-V3-104 可观测性写入旁路异步
- production code changed: 否（仅文档）
- SQL migration changed: 待 Task-DB-01（510/511）
- rollback: 删除 09/10/11 文档即可；未触及生产代码与数据
- next_gate: Task-DB-01 迁移 + BE-A2 节点操作 API（并行启动）

## 2026-08-13：V3.2 后端 partial — DB-01 + BE-A2/B1/B2 handler 就绪但未注册路由

- record_id: `v3-impl-20260813-003`
- status: `IN_PROGRESS`（后端 handler 路由、SSE envelope/provider 注入与前端实时状态已接入；仍待真实环境 INT-01 验证）
- scope: 统一实时请求流后端合流 + 节点操作 + 主从请求 + 状态变更历史 + 在线会话浏览 API
- migration（已写入工作区，未部署）: `sql/migrations/startup/510_request_type.{sql,down.sql}`、`511_state_transitions_table.{sql,down.sql}`、`test_510_511.sql`
- code 已写入工作区（未注册路由，外部不可达）:
  - `admin/node_operations.go`（test-now 同步探测 + enable 切换 + 候选缓存失效；**待注册** `POST /api/admin/providers/{id}/test-now` + `PATCH /api/admin/providers/{id}/enable`）
  - `admin/request_transitions.go`（**待注册** `GET /api/admin/requests/{id}/transitions`）
  - `admin/session_online.go`（**待注册** `GET /api/admin/sessions/online` + `GET /api/admin/sessions/{id}/timeline`）
  - `domains/dispatch/state_transition_logger.go`（旁路异步批量写 + 7 天清理；**当前为孤儿代码，无调用方**，按 rule 00 §3.1 应加 `KEEP:` 标记，待 INT 阶段 wire 到 route_resolve / streamretry）
- code 已入库（先前 commit 完成）:
  - `domains/hooks/observability/telemetry/client.go`（RequestType 字段 + inferRequestTypeV32 + 旁路 UPDATE，commit afbe26b3f）
- key decisions:
  - request_type 用旁路 UPDATE 而非重排 100 个硬编码 INSERT 参数（最小风险）
  - 节点状态以 credentials 表投影（ADR-V3-103），不维护第二份真相
  - queue/node provider 用函数注入避免 admin→dispatch 循环依赖
- tests: `go build ./...` exit=0
- known_gaps（2026-08-13，真实状态）:
  - **路由**：5 个 V3.2 admin 路由已注册；剩余状态变更/操作路由以现有 handler 注册情况为准
  - **SSE envelope**：`admin/live_stream_sse.go` 已包含 Queue/Nodes 字段、周期推送与 `initial_data` 注入
  - **Provider 注入**：`cmd/gateway/main.go` 已注入 queue snapshot 与缓存 node status provider
  - **前端实时状态**：`liveStreamStore.ts` 已消费 `queue_snapshot` / `node_update`；NodeStatusMatrix / QueuePerspectivePanel 已启用
  - **BE-B1 埋点**：route_resolve / streamretry 接入状态另行核对
  - **真实环境 browser-use 实测未做**（待 INT-01）
- next_gate: INT-01 集成验证 + BE-B1 埋点状态核对

## 2026-08-14：Migration 513 DROP TABLE 目标修正

- record_id: `v3-bugfix-20260814-001`
- status: `FIXED`（SQL 已修复，文档已更新，验证脚本已调整）
- scope: Migration 513 schema unification
- problem:
  - 注释说"DROP gateway schema 下 4 张重复 v2 表"
  - 实际 SQL 误写为 `DROP TABLE IF EXISTS public.session_turn_logs/session_bodies/session_turns/sessions`
  - 然后 `ALTER TABLE public.session_turns ADD COLUMN ...` → 会导致 42P01 (undefined_table) 错误
- root_cause: copy-paste 错误，public/gateway schema 名称未同步修改
- fix:
  - `sql/migrations/startup/513_schema_unification_and_session_turns_dual_write.sql` line 120-124: `DROP TABLE public.*` → `DROP TABLE gateway.*`
  - `docs/会话优化v3/MIGRATION-513-REHEARSAL.md`: 创建 DBA rehearsal 指南（备份 + up/down 验证 + 完整性检查 + 回滚预案）
  - `docs/会话优化v3/12-20260814-当前实现基线与缺口.md`: 状态从 `MIGRATION-BLOCKED` → `FIXED`
  - `docs/会话优化v3/15-检查、漂移与迁移门禁.md`: 添加 513 已修复说明
- verification:
  - ✅ 代码 grep 无 gateway.* 表引用
  - ✅ test_513.test.sql 期望检查 gateway.* 表被删除（与修复后 SQL 一致）
  - ✅ down 迁移无需修改（只 DROP 新加的列，不涉及 DROP 表）
  - ⚠️ 待真实 252 PG 环境 up/down 验证事务完整性
- commits:
  - `52aa49421`: fix(migration): correct Migration 513 to DROP gateway.* instead of public.*
- next_gate: 252 环境 PG 预演 + Migration 511 补充 tenant_id/RLS

## 2026-08-14：Migration 511 租户隔离改造

- record_id: `v3-fix-20260814-002`
- status: `FIXED`（SQL 已补全，Go 代码已更新，RLS 测试已创建）
- scope: Migration 511 request_state_transitions 租户隔离
- problem:
  - 表缺少 `tenant_id` 列，无法实现跨租户数据隔离
  - 无 RLS (Row-Level Security) 策略，违反 rule 19 租户隔离强制要求
  - `admin/request_transitions.go` 仅按 request_id 查询，跨租户可见
  - Go 代码 `StateTransition` 结构体和 `Log*` 方法签名缺少租户参数
- fix:
  - **SQL**: `sql/migrations/startup/511_state_transitions_table.sql`
    - 添加 `tenant_id TEXT NOT NULL` 列（位于 request_id 之后）
    - 启用 RLS: `ALTER TABLE request_state_transitions ENABLE ROW LEVEL SECURITY`
    - 策略 1: `state_transitions_tenant_isolation` (app.current_tenant 过滤)
    - 策略 2: `state_transitions_super_admin_bypass` (运维全局访问)
    - 租户索引: `idx_state_transitions_tenant_request` (tenant_id, request_id, created_at DESC)
    - POST_CONDITION 验证条件扩展（列存在、RLS 启用、策略创建）
  - **Go 代码**:
    - `domains/dispatch/state_transition_logger.go`: `StateTransition` 添加 `TenantID` 字段，INSERT 语句更新为 6 参数
    - `domains/dispatch/state_transition_logger_globals.go`: 所有 `Log*Global` 函数添加 `tenantID` 参数
    - `admin/request_transitions.go`: 查询前设置 `app.current_tenant` 会话变量（从 X-Tenant-ID header 提取）
  - **测试**: `sql/migrations/test/test_511_rls.test.sql`
    - Schema 验证: tenant_id 列、NOT NULL 约束、索引存在
    - RLS 验证: RLS 启用、2 个策略存在
    - 功能测试: 租户 A/B 数据隔离、super_admin 全局访问、bypass_rls 标志
    - 负测试: 跨租户查询返回 0 行（数据泄露防护）
- commits:
  - `8ed82e2b6`: fix(migration): add tenant isolation to Migration 511 request_state_transitions
- verification:
  - ✅ SQL 语法完整（tenant_id + RLS + indexes + POST_CONDITION）
  - ✅ Go 编译通过（所有 Log* 方法签名更新）
  - ✅ RLS 测试覆盖 12 个检查项（schema/功能/负测试）
  - ✅ Down 迁移无需修改（DROP TABLE CASCADE 自动清理 RLS）
  - ⚠️ 待 252 环境执行 `test_511_rls.test.sql` 验证隔离有效性
- known_gaps:
  - 当前所有 `LogRouteDecisionGlobal` 等调用处未传 tenantID（需后续埋点时从 request context 提取）
  - Migration 511/513 必须一起在 252 测试通过后才能部署 154
- next_gate: 252 环境 PG 验证（Migration 511 RLS + Migration 513 up/down）
