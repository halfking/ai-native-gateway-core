# 子任务提示词集合（V3.2）

> 创建时间：2026-08-13
> 前置文档：10-任务分解与并行执行计划-V3.2.md
> 使用说明：每个提示词可独立复制到新会话执行；按 10 号文档依赖图并行

> **V3.3 状态声明（2026-08-14）**：本文提示词仅作为历史输入，不得直接复制执行。实际代码路径、API/SSE 契约和任务依赖以 `12-20260814-当前实现基线与缺口.md`、`13-V3.2实际API与SSE契约.md`、`14-统一逻辑点与任务拆解.md` 为准。

---

## 使用指南

1. 提示词含上下文、目标、约束、验收标准
2. 执行顺序按依赖图（DB-01 先行，BE-A2 无依赖可同批）
3. 无依赖任务可同时启动多个会话并行
4. 每任务完成产出代码 + 测试 + 结构化总结（rule 11 §10）

---

## Task-DB-01：数据库迁移（阻塞，先跑）

```
你好！请完成 llm-gateway-go 的数据库迁移任务（会话优化 V3.2 / Task-DB-01）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go
- DB: PostgreSQL 17；迁移目录: sql/migrations/（先 ls 确认现有编号与命名规范，510/511 如被占用则顺延）
- 参考: docs/会话优化v3/09-V3.2整合方案与代码现状对齐.md
- 现状证据: request_logs 表定义在 sql/schema/01-schema.sql（约 L6185）；telemetry.RequestLogEntry 在 domains/hooks/observability/telemetry/client.go:122

【目标】
1. Migration 510: request_logs 增加 parent_request_id TEXT NULL、request_type TEXT NOT NULL DEFAULT 'main'
   （CHECK: main/title_gen/summary/sensitive_check/compression/other）；
   部分索引 idx_request_logs_parent ON request_logs(parent_request_id) WHERE parent_request_id IS NOT NULL
2. Migration 511: 新表 request_state_transitions(
   id BIGSERIAL PK, request_id TEXT NOT NULL,
   transition_type TEXT NOT NULL CHECK IN ('route','node_switch','retry','error','state'),
   from_state TEXT, to_state TEXT, metadata JSONB, created_at TIMESTAMPTZ DEFAULT NOW())；
   索引 (request_id, created_at DESC)；表注释

【约束】
- up/down 成对、幂等（IF NOT EXISTS / IF EXISTS）、详细注释
- 不破坏现有数据；遵守 rule 38（SQL 管理规范）与 rule 19（数据库设计规范）
- 每个 SQL 落盘后立即做语法验证（rule 11 §14）

【验收】
- [ ] 4 个迁移文件 + test_510_511.sql 验证脚本
- [ ] 空库与现有库均可执行；down 可完整回滚
- [ ] 索引与 CHECK 生效的验证 SQL 输出
完成后输出结构化任务总结（rule 11 §10）。
```

---

## Task-BE-A1：队列可观测性模块（依赖 DB-01）

```
你好！请实现 llm-gateway-go 的队列可观测性模块（V3.2 / Task-BE-A1）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go
- 现有实时流: admin/live_stream_redis_store.go（llmgw:live:main + dim:vendor/provider/model 队列）、
  admin/live_stream_sse.go（SSE Hub，消息类型 initial_data/request/health_update 等）、
  cmd/gateway/main_livestream.go（telemetry → LiveRequest 适配）
- 请求生命周期阶段: internal/trace/trace.go:64（12 阶段）
- 参考: docs/会话优化v3/01-需求分析与架构设计.md §2.2.2、05-深化需求分析 §2

【目标】
实现三层队列（总/模型/节点 credential）实时指标采集与推送：
1. domains/dispatch/queue_metrics_collector.go: QueueSnapshot（总队列 depth/waiting_ms_p50/p95；
   按 model 分组 depth/waiting_avg；按 credential_id 分组 depth/in_flight/concurrency）；
   sync.RWMutex；Snapshot()
2. domains/dispatch/queue_metrics_hook.go: Pipeline middleware，OnEnqueue/OnDequeue 更新指标
3. main 装配注入 LiveStreamSSEHub
4. SSE 新增 queue_snapshot 消息，2s 节流（复用现有 Hub fan-out，不新建 Hub）

【约束】
- 性能开销 < 5%（提供 benchmark）
- 采集不得阻塞 relay 主链路（ADR-V3-001/104 旁路）
- 每文件写完 go build ./...（rule 11 §14）；commit 前 go vet + 测试（rule 17）

【验收】
- [ ] 并发 100 请求统计准确、无竞态（go test -race）
- [ ] SSE queue_snapshot 稳定，断连无 goroutine 泄漏
- [ ] benchmark 开销 < 5%
完成后输出结构化任务总结。
```

---

## Task-BE-A2：节点操作 API（无依赖，可立即并行）

```
你好！请实现 llm-gateway-go 的节点操作 API（V3.2 / Task-BE-A2）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go
- 现状: admin/provider_credential.go:167 已暴露 circuit_state/availability_state/quota_state/health_status/manual_disabled；
  admin/probe_history.go:170/212 已有手动触发 probe；admin/provider_probe.go:58/139 有 doProbeRequest/doChatProbe；
  admin/handler.go:137 有 stateManager；候选缓存按 credential 失效: provider/client.go:352
- 参考: docs/会话优化v3/10-任务分解与并行执行计划-V3.2.md

【目标】
1. admin/node_operations.go:
   - POST /api/admin/providers/{id}/test-now：同步探测（复用 doChatProbe，超时 5s），
     返回 {provider_id, latency_ms, status, tested_at, error?}
   - PATCH /api/admin/providers/{id}/enable：{enabled: bool}，切换 manual_disabled，
     立即失效路由候选缓存，返回新状态
2. admin/handler.go 注册路由；super_admin 鉴权（参考现有 admin 权限中间件）
3. 操作审计日志（DB-01 完成后对接 request_state_transitions 风格记录，先日志 + TODO）

【约束】
- 禁用立即摘除路由；启用后重新入池
- test-now 5s 超时兜底，不得挂住
- 每文件 go build ./...；commit 前三件套

【验收】
- [ ] curl test-now 返回正确延迟与状态
- [ ] enable=false 后该节点不再被路由（diagnose_routing.sh 或等效验证）
- [ ] 非 super_admin 返回 403
- [ ] 单测覆盖 handler 主路径
完成后输出结构化任务总结。
```

---

## Task-BE-A3：请求链路追踪增强——主从请求（依赖 DB-01）

```
你好！请实现 llm-gateway-go 的请求链路追踪增强（V3.2 / Task-BE-A3）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go
- 现状: telemetry.RequestLogEntry 已有 ParentRequestID/Compression* 字段
  （domains/hooks/observability/telemetry/client.go:122-240）；
  标题生成通过 X-Gw-Parent-Request-Id 回写 parent_request_id（admin/auto_title_generator.go:24-32, 610）；
  live-stream 适配器在 cmd/gateway/main_livestream.go:51
- 参考: docs/会话优化v3/01-需求分析与架构设计.md §2.2.4

【目标】
1. RequestLogEntry 增加 RequestType（main/title_gen/summary/sensitive_check/compression/other），
   telemetry 写入时从 X-Gw-Source-Actor/路径推断填充
2. admin/live_stream_redis_store.go 增加 QueryChildRequests(ctx, parentID)（Redis 维度索引 + PG 兜底）
3. LiveRequest 增加 Children；SSE 推送主请求时异步附带子请求树（深度≤3，循环保护）
4. 非请求型扩展操作（敏感词 verdict）定义轻量 ChildEvent 挂父请求展示

【约束】
- 子请求查询 P95 < 50ms；循环保护深度≤3
- 每文件 go build ./...；go test -race

【验收】
- [ ] 标题生成请求正确挂到主请求下并在 SSE 出现
- [ ] 单测：主+子关联、深度保护、性能
完成后输出结构化任务总结。
```

---

## Task-BE-A4：节点请求流整合进 live-stream（依赖 DB-01，新增）

```
你好！请把"供应商节点请求流"整合进 llm-gateway-go 首页 live-stream（V3.2 / Task-BE-A4，新增）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go
- 现状: live-stream Redis 结构 admin/live_stream_redis_store.go:20-23（main + dim:vendor/provider/model + status）；
  节点状态字段 admin/provider_credential.go:167-194；probe 流独立 SSE（admin/probe_stream_sse.go），
  本任务不重做它，只把节点状态与节点维度请求聚合进主流
- 决策: 后端合流（ADR-V3-101）——前端保持单 SSE 连接 /api/admin/live-stream

【目标】
1. Redis 新增节点维度队列 llmgw:live:dim:node（按 credential_id 分桶在途/近期请求）
2. LiveStreamSnapshot/Delta 增加 nodes 段：每节点 circuit_state/availability_state/quota_state/
   health_status/in_flight/最近延迟/最近错误
3. 节点状态变更（熔断开合/配额耗尽/手动启停/probe 结果）实时推送为 node_update 消息
4. initial_data 快照包含全量节点状态矩阵

【约束】
- 复用现有 NotifyChange/Pub-Sub 通道，不新建 SSE Hub
- 节点状态以 credentialhealth/circuit 为唯一真相（ADR-V3-103），不做第二份状态
- 每文件 go build ./...；go test -race

【验收】
- [ ] SSE initial_data 含节点矩阵；状态变更 2s 内推送
- [ ] 节点维度队列与 main 队列对账一致
完成后输出结构化任务总结。
```

---

## Task-BE-B1：状态变更历史记录（依赖 DB-01 + BE-A1）

```
你好！请实现 llm-gateway-go 的请求状态变更历史（V3.2 / Task-BE-B1）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go
- 现状: trace 12 阶段 internal/trace/trace.go:64，写点 domains/streaming/handler.go；
  pre-stream 重试 internal/streamretry/wrapper.go:72-132，错误分类 internal/streamretry/retry.go:73-181；
  candidate 不可路由原因 provider/client.go:209-240
- 表: request_state_transitions（Task-DB-01 Migration 511）

【目标】
1. domains/dispatch/state_transition_logger.go: LogRouteDecision/LogNodeSwitch/LogRetry/LogError，
   异步批量写 request_state_transitions
2. 埋点: route_resolve/route_credential 记路由决策（含候选与 BlockReason）；
   streamretry 重试/切换记 from_node→to_node、原因、retry_seq；终态错误记录
3. 查询 API: GET /api/admin/requests/{id}/transitions（按 created_at 排序）
4. 清理任务: 删 7 天前数据（参考现有 cleanup 模式）

【约束】
- 写历史旁路异步不阻塞 relay（ADR-V3-001/104）；写失败只记日志
- 重试事件 metadata 含 retry_seq、reason_class（供前端特殊标）
- 每文件 go build ./...；go test -race

【验收】
- [ ] 构造 429 触发重试，transitions 返回完整 route→retry→node_switch→complete 链
- [ ] 清理任务单测
完成后输出结构化任务总结。
```

---

## Task-BE-B2：在线会话浏览 API（仅依赖 DB-01，新增）

```
你好！请实现 llm-gateway-go 的在线会话浏览 API（V3.2 / Task-BE-B2，新增）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go
- 现状: 在线会话在 Redis（domains/session/session.go Manager，session:<id> 哈希）；
  断线恢复缓存 session_last_requests（migrations/timeout-optimization/003）；
  V2 详情 GET /api/admin/sessions/detail（admin/session_detail_v2.go:96）；
  首页统计 admin/dashboard_session_stats.go:49
- 目标页面: 首页 stats tab（SessionStatsPanel.vue）增强

【目标】
1. GET /api/admin/sessions/online：在线会话列表（Redis SCAN session:* + session_last_requests 丰富：
   最后请求状态/模型/供应商/活跃时间/设备数），分页 + 租户过滤
2. GET /api/admin/sessions/{id}/timeline：多轮次时间线——每轮主请求 + 扩展请求子树
   （parent_request_id 关联），来源 V2 session_turns + request_logs，标注每轮状态与耗时
3. 租户隔离（ADR-V3-005）

【约束】
- Redis 用 SCAN 游标，禁 KEYS
- 在线列表 P95 < 200ms；timeline P95 < 500ms
- 每文件 go build ./...；单测覆盖

【验收】
- [ ] 在线会话列表与 Redis 实际一致
- [ ] 多轮会话 timeline 正确展示主从请求树
完成后输出结构化任务总结。
```

---

## Task-FE-A1：队列透视区组件（← BE-A1，可先 mock）

```
你好！请实现 llm-gateway-go 前端的队列透视区组件（V3.2 / Task-FE-A1）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go/web
- 首页: web/src/views/DashboardViewV2.vue（board/stream/stats/selfcheck 四 tab）；
  实时流组件 web/src/components/LiveRequestStreamV2.vue；SSE store web/src/composables/liveStreamStore.ts:109
- 后端新增 queue_snapshot SSE 消息（BE-A1）；参考 docs/会话优化v3/05-深化需求分析 §2.2
- 前端规范: 颜色 var(--kx-*) 禁硬编码 hex；三态（Skeleton/EmptyState/ErrorBanner）

【目标】
1. web/src/components/QueuePerspectivePanel.vue：三层队列透视（总/模型/节点），
   depth/waiting p50/p95/in_flight；拥堵高亮 + 根因提示
2. liveStreamStore.ts 处理 queue_snapshot
3. 嵌入 stream tab 顶部

【约束】
- kx-design token；动画只用 transform/opacity
- vue-tsc --noEmit 通过；组件 ≤ 300 行（超出拆子组件）

【验收】
- [ ] browser-use 实测：并发请求时队列深度实时变化、拥堵高亮正确
- [ ] 截图 ui-verify-queue-perspective-{场景}-{时间戳}.png
完成后输出结构化任务总结。
```

---

## Task-FE-A2：节点状态矩阵 + 点击测试/切换（← BE-A2 + BE-A4）

```
你好！请实现 llm-gateway-go 前端的节点状态矩阵组件（V3.2 / Task-FE-A2）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go/web
- 后端: POST /api/admin/providers/{id}/test-now、PATCH .../enable（BE-A2）；
  SSE initial_data/node_update 含节点矩阵（BE-A4）
- 前端规范: var(--kx-*) token、三态、element-plus 控件

【目标】
1. web/src/components/NodeStatusMatrix.vue：全节点矩阵——
   每节点 circuit/availability/quota/health 四态、in_flight、最近延迟、在途请求数
2. 点击节点 → 抽屉：详情 + "立即测试"（test-now 展示延迟/结果）+ 启用/禁用开关（enable，二次确认）
3. 节点状态变更 SSE 实时刷新；操作乐观更新 + 失败回滚

【约束】
- 操作有 loading/成功/失败反馈；禁用需 confirm
- vue-tsc 通过；组件 ≤ 300 行

【验收】
- [ ] browser-use 实测：点击测试返回延迟；禁用后矩阵状态即时变化
- [ ] 截图 ui-verify-node-matrix-*.png
完成后输出结构化任务总结。
```

---

## Task-FE-A3：增强泳道组件（← BE-A3 + BE-B1）

```
你好！请增强 llm-gateway-go 首页实时请求流泳道组件（V3.2 / Task-FE-A3）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go/web
- 主组件: web/src/components/LiveRequestStreamV2.vue（已支持 vendor/provider/model 分组、筛选、incident 抽屉）
- 后端: LiveRequest 增加 stage/state/children/retry_count（BE-A3）；
  GET /api/admin/requests/{id}/transitions（BE-B1）；trace API /api/admin/requests/{id}/trace 已存在

【目标】
1. 请求 tile 显示生命周期状态流转：到达→路由→选节点→(切换节点)→(重试🔄特殊标记)→回复/错误，
   当前阶段高亮、已完成打勾、失败红色
2. 重试请求显示 🔄 特殊标 + 次数 + 原因
3. 主请求 tile 可展开子请求树（标题生成/总结/敏感词检查/压缩），显示各扩展请求状态
4. 点击 tile → 抽屉展示 transitions 时间线 + trace 链接

【约束】
- 不破坏现有分组/筛选；增量更新不闪烁
- vue-tsc 通过；单组件 ≤ 300 行，超出拆子组件

【验收】
- [ ] browser-use 实测：构造含重试+标题生成的请求，泳道正确展示状态流转、🔄标记、子请求树
- [ ] 截图 ui-verify-stream-lifecycle-*.png
完成后输出结构化任务总结。
```

---

## Task-FE-B1：在线会话浏览面板（← BE-B2）

```
你好！请实现 llm-gateway-go 首页 stats tab 的在线会话浏览面板（V3.2 / Task-FE-B1）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go/web
- stats tab: web/src/components/SessionStatsPanel.vue（现有：总会话/活跃/健康度/成本趋势/Top 排行）
- 后端: GET /api/admin/sessions/online、GET /api/admin/sessions/{id}/timeline（BE-B2）；
  会话详情路由 /admin/sessions/:id 已存在（web/src/router.ts:135）

【目标】
1. SessionStatsPanel 新增"在线会话"区块：实时在线会话列表（ID/标题/最后请求状态/模型/供应商/
   活跃时间/设备数），30s 轮询或 SSE
2. 点击会话 → 多轮次时间线视图：每轮主请求状态/耗时 + 扩展请求子树（标题/总结/敏感词），
   可跳完整详情页
3. 与现有统计卡片共存，不破坏布局

【约束】
- var(--kx-*) token；三态；vue-tsc 通过

【验收】
- [ ] browser-use 实测：在线会话列表与实际一致；多轮时间线正确展示主从结构
- [ ] 截图 ui-verify-session-timeline-*.png
完成后输出结构化任务总结。
```

---

## Task-INT-01：集成验证与发布（依赖全部）

```
你好！请执行 llm-gateway-go 会话优化 V3.2 的集成验证（Task-INT-01）。

【上下文】
- 项目: /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go
- 全部前置: DB-01, BE-A1/A2/A3/A4, BE-B1/B2, FE-A1/A2/A3, FE-B1
- 红线: rule 11 §6（UI 必须 browser-use 实测）、rule 17（commit 前三件套）、rule 03（部署安全）

【目标】
1. 全量构建: go build ./... + go vet + go test ./... + web pnpm build + vue-tsc
2. 迁移演练: 空库/现有库 510/511 + down 回滚
3. 端到端 browser-use 实测（截图）:
   a. 正常请求全生命周期泳道可见
   b. 429/5xx 触发重试与节点切换，🔄标记与 transitions 正确
   c. 节点测试/禁用/启用全流程
   d. 主请求 + 标题生成子请求树
   e. 在线会话列表 + 多轮时间线
   f. 双主题 + 响应式
4. 性能: 队列开销 <5% 复核；SSE 长连 30min 无泄漏
5. 更新 docs/会话优化v3/08-执行记录.md ledger；rule 35/36 commit + CHANGELOG

【验收】
- [ ] 所有场景截图证据齐全
- [ ] 08-执行记录.md 追加 RELEASED 记录
完成后输出结构化任务总结。
```
