# 24-Hour Change Rollup: 2026-06-30 04:55 → 2026-07-01 03:33

**33 commits, 6 tracked contributors, ~15,000 lines changed across ~120 files**

---

## 1. Timeline by Theme

### Phase A — DB Sync Infrastructure (04:55–13:13, 7 commits)
| Commit | Description | Key Files |
|--------|-------------|-----------|
| `75842cfa` | sync 184→local: port 25022 + pg_dump 15.18 `--restrict` | `sync-db-from-184.sh` |
| `bd3d3e79` | extract `completion_tokens`/`cache_tokens` for non-streaming | `handler.go` |
| `37b900c0` | pg_basebackup-based 184→local sync (~3-5 min) | `sync-db-from-184-pgbase.sh` |
| `76771c5c` | 4-table partition & archive + columnar storage | 7 migration files, `partition_manager.go` |
| `0b0d80e8` | request_logs upstream diagnostics (migration 320) | `320_*.sql`, `request_log_pipeline.go` |
| `90a4be4a` | fix SSH hangs: 3 keys + 184 slow on unknowns | `sync-db-from-184*.sh` |
| `6ade07b6` | add `sync-db-from-71.sh` for host→docker replica | `sync-db-from-71.sh` |

### Phase B — Security Audit: Week 26 P0 Fixes (14:29–14:31, 6 commits)
| Commit | P0 | Description | Key Files |
|--------|----|-------------|-----------|
| `0db63cca` | P0-3/4/5 | auth middleware chain: cookie bypass + MustChangePassword + logout whitelist | `auth.go`, `auth_mw.go` |
| `15e72ffe` | P0-6/7/8 | nil-safe emitTelemetry + thread client_request_id | `executor.go`, `handler.go` |
| `20c902b6` | P0-9 | tenant isolation on `/api/agents/health` | `agents.go`, `service.go` |
| `be90f301` | P0-10 | expose `state_distribution` field in probe dashboard | `probe_dashboard.go` |
| `9d90d054` | P0-11 | advisory lock on probe view rebuild | `db.go` |
| `27e236cb` | — | weekly audit report (11 P0 findings) | `2026-06-30-weekly-audit-report.md` |

### Phase C — Routing Fix (17:47, 1 commit)
| Commit | Description | Key Files |
|--------|-------------|-----------|
| `ec4f8c52` | stop disguising DB errors as `no_candidate`; proper error classes | `routing_errors.go`, `messages.go` |

### Phase D — Security Sanitization + DB Isolation (20:42–23:19, 6 commits)
| Commit | Description | Key Files |
|--------|-------------|-----------|
| `728d081e` | 24h audit: fix `updateRequestLog` missing SET + sanitize errors + SSH paths | `client.go`, `routing_errors.go` |
| `cbbfe978` | remove hardcoded SSH passwords + fix DB_NAME | `diagnose-routing.sh`, `fix-71-missing-tables.sh` |
| `fe5b9176` | sanitize all 78 tracked files (IPs, passwords, keys → placeholders) | 78 files across entire repo |
| `bf880f77` | centralize state-manager interface + fix context/DB bugs | `credentialstate/ports.go`, `lifecycle.go` |
| `ffbc3e33` | isolate 71-production from 184-testing DBs | `sync-db-from-71.sh`, `.DISABLED` backup |
| `d88025c8` | DB environment separation + deployment security docs | 3 new docs (435+373+450 lines) |

### Phase E — Phase 2 Feature Delivery (Jul 1 00:39–03:33, 13 commits)
| Commit | Description | Key Files |
|--------|-------------|-----------|
| `a3d747a0` | wire StateManager into main.go + export `StateObserver` interface | `main.go`, `manager.go` |
| `1b196763` | implement `injectFollowUpRequest` — full follow-up injection | `response_interceptor_helpers.go` |
| `b585e388` | auto-control deployment report 2026-07-01 | `AUTO_CONTROL_DEPLOYMENT_20260701.md` |
| `8ffaaabf` | SOPS encrypted env storage + auto-deploy + runtime load | `.sops.yaml`, `sync-env.sh`, `load-env.sh` |
| `3342cfca` | Phase 2: popularity-aware probing | `popularity_tracker.go` |
| `7567a9de` | auto-control: AuditHook + i18n prompts (placeholder impl) | `audit_hook.go`, `prompts.go` |
| `ba7baff2` | Phase 2 deployment docs + scripts + DB prep SQL | `deploy_phase2_to_184.sh`, `phase2_db_setup.sql` |
| `7056f75d` | 184 deployment dirty changes | `deploy_phase2_to_184_fixed.sh` |
| `f4e0ed2a` | 184 test env deployment report | `phase2_184_deployment_report.md` |
| `00a38292` | SuperAdminMiddleware: cookie token support | `auth.go` |
| `0c577d7f` | health-tracker: filter client-cancel errors | `health_tracker.go` |
| `88772512` | credentialstate: user cancel ≠ credential error | `manager.go` |
| `b8ec9349` | update phase2 doc commit hash | `phase2_user_cancel_fix.md` |

---

## 2. Cross-Cutting Concerns

### Security
- 78 files sanitized of hardcoded IPs/passwords/keys (`fe5b9176`)
- Hardcoded SSH passwords removed from scripts (`cbbfe978`)
- 71-production DB sync disabled, `.DISABLED` backup created (`ffbc3e33`)
- SOPS-based encrypted env storage introduced (`8ffaaabf`)
- 5 P0 security findings fixed (auth bypass, tenant isolation, probe advisory lock, etc.)
- 3 security policy documents added (DB env separation, deployment checklist)

### Database
- Migrations 317-320 added (partition, archive, columnar, upstream diagnostics)
- All migrations have up/down symmetry, use `IF NOT EXISTS`, parameterized queries
- Oracle advisory lock on probe view rebuild (P0-11)
- No SQL injection vectors found in all reviewed code

### Testing
- Auth middleware: 143 lines new tests (`middleware/auth_mw_test.go`)
- Password enforcement: 109 lines new tests (`admin/auth_force_password_test.go`)
- Probe dashboard: 95 lines new tests (`admin/probe_dashboard_test.go`)
- Tenant isolation: 126 lines new tests (`admin/agents_test.go`)
- Telemetry: 66 lines new tests (`executor_async_success_test.go`)
- Partition manager: 98 lines new tests (`bg/partition_manager_test.go`)
- Diagnostics: 209 lines new tests (`request_log_diagnostics_test.go`)
- Credential state: 54 lines new tests (`manager_test.go`)

### Gaps Found & Addressed
- ❌ **Review found no actual hardcoded passwords or secrets remaining** after sanitization pass
- ⚠️ CredentialState manager core logic (smart probe backoff) lacks unit test coverage (suggestion for next iteration)

---

## 3. Architecture Changes

### CredentialState Centralization (`bf880f77` → `a3d747a0`)
```
Before: health_tracker.go managed state inline
After:  credentialstate/ports.go defines StateObserver interface
        → manager.go implements smart backoff (15min permanent / 30s→2m→5m transient)
        → health_tracker.go delegates to manager
        → bg/credential_probe_v2.go uses exported interface
```

### Environment Management (`8ffaaabf`)
- `.env.184.enc` / `.env.71.enc`: SOPS-encrypted per-environment config
- `deploy/sync-env.sh`: decrypt-and-deploy pipeline
- `scripts/lib/load-env.sh`: runtime auto-load

### Audit Hook (`7567a9de`)
- `domains/hooks/goal/audit_hook.go`: placeholder — structured audit logging hook for auto-control
- `domains/hooks/goal/prompts.go`: i18n prompt templates

---

## 4. Verification

- `go build ./...` — ✅ passes
- Tests for `credentialstate`, `admin`, `middleware` — ✅ all pass
- No new hardcoded secrets detected via regex sweep
