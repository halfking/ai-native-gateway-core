# V2 包测试健康度审计（独立技术债报告）

> **来源**: V2 压缩路径审计的副产物（2026-08-07）
> **性质**: 独立技术债，**不属于** V2 压缩路径范围
> **状态**: 已记录，待独立 task 处理

## 背景

在为 V2 压缩路径补真 DB 集成测试时，本地用完整 migration 链跑
`go test ./domains/session/v2` 发现多个预先存在的失败。这些失败
与 V2 压缩读/写路径（outbound_body）无关，是 v2 包整体的测试
健康度问题，单独立项处理更合适。

## 发现的失败（本地 430+431+456 链上复现）

### 1. schema 漂移：attachment_count（已规避）

- **现象**: `TestTurnWriter_ListTurns_WithAttachments` 报
  `column "attachment_count" of relation "session_turns" does not exist`
- **根因**: 该列由 migration **431** 引入，只建 430 时缺失
- **影响**: 仅在 migration 链不完整的 DB 上失败；生产已跑全链则正常
- **当前处理**: V2 压缩 CI job 收窄到 `-run '_RealDB|_RoundTrip'`
  规避，注释已写明

### 2. 幂等性 bug：TestTurnWriter_Idempotency（未规避）

- **现象**: 即便跑全链 430+431+456，仍失败：
  `expected turn_no=2, actual=1`
- **语义**: 同一 request_id 第二次插入应返回已有 turn_no（幂等），
  实际插入了新行 / 返回了错误值
- **性质**: **业务逻辑缺陷**，非 schema 问题
- **可能影响**: 重试场景下同一 request 被记录成两轮，污染会话轮次
- **优先级**: 高（数据正确性），但需 turn_writer 维护者确认

## 复现步骤

```bash
# 建库 + 跑全链 migration（430/431/456...）
createdb llm_gw_v2_test
for m in 430_sessions_v2_schema 431_session_turns_add_attachment_columns 456_session_v2_display_columns; do
  psql llm_gw_v2_test < sql/migrations/startup/$m.sql
done

# 跑全包
TEST_DATABASE_URL="postgres://.../llm_gw_v2_test" \
  go test ./domains/session/v2 -count=1
```

## 边界澄清

本次 V2 压缩路径审计（commit `96b6b2fe` → `715b3737`，共 9 个）
**只对以下范围负责**：

- `domains/hooks/compression/session_compressor.go`（V2 读路径接入）
- `domains/session/v2/outbound_builder.go`（增量拼接）
- `domains/session/v2/cache_v2*.go`（L2 Redis）
- `domains/session/v2/bodies_writer.go` 的 `outbound_body` 列读写闭环
- 关联的 spec / main.go 装配 / CI workflow

以下**不在**本轮范围，需独立处理：

- `turn_writer.go` 的幂等性逻辑
- v2 包全量 migration 链在 CI 的完整性
- 其他 v2 子模块（aggregator / turn_logs / rls owner filter 等）
  的测试健康度

## 建议的独立 task

1. **修复 `TestTurnWriter_Idempotency`**：确认是测试断言错还是
   AppendTurn 幂等逻辑缺陷。涉及数据正确性，优先。
2. **CI migration 完整性**：让 self-hosted runner 的 DB 跑齐
   430→最新 startup migration，使 `go test ./domains/session/v2`
   全包能在 CI 跑（届时可移除本轮的 `-run _RealDB|_RoundTrip`
   收窄）。
3. **v2 包测试隔离审计**：多个 DB 测试共享 `test_tenant` 清理
   约定，并发跑或顺序跑的隔离性需要一次梳理。

---

**审计日期**: 2026-08-07
**审计触发**: V2 压缩路径 `bodies_outbound_roundtrip_test` 引发的全包回归
**结论**: 上述失败与本轮交付的 V2 压缩读写闭环无关，已隔离记录
