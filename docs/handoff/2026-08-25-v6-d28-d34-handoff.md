# v6 D28-D34 工作交接文档 — 2026-08-25

## TL;DR
- 已落地：W2-8（LP1 body 列 drop）、W2-9（dae/sme promote specs）
- 待办：W2-11 / W2-12 / W2-13 / W1-9+10 / W3-13 / W3-14

## 已完成清单（这一会话）

### W2-8 — LP1 apply drop request_logs_hot 的 body 三列
- 状态：DONE
- 验证：`bash scripts/check-body-storage-schema.sh` → exit 0
- 证据：`sql/objects/tables/request_logs_hot.sql` 已无 request_body / response_body / outbound_body 三列
- Go 写入路径：commit 30eb4c34f `fix(telemetry): stop writing removed request body columns` 停写
- LP5 baseline 文档：`docs/migrations/2026-08-24-lp5-body-storage-schema-baseline.md`

### W2-9 — PartitionManager 注册 dae/sme
- 状态：DONE（SQL 迁移早在 commit `88c6fbf7b` 落地，但 `bg/partition_manager.go::promoteSpecs()` 的两条新 spec 漏写，后续 commit `2bb6f8f86` 补齐）
- 变更：
  - `sql/migrations/startup/579_dashboard_access_events_hot_promote.sql`（160 行）—— 定义 `promote_dashboard_access_events_hot_to_partition(interval, integer) RETURNS bigint`
  - `sql/migrations/startup/580_session_module_executions_hot_promote.sql`（160 行）—— 定义 `promote_session_module_executions_hot_to_partition(interval, integer) RETURNS bigint`
  - commit `2bb6f8f86` 在 `bg/partition_manager.go::promoteSpecs()` 补上两条 archiveSpec（`// Migration 579` dashboard / `// Migration 580` session_module_executions），并附 4 行注释（`// 2026-08-25: ... pg_cron archive_* scripts which drifted.`）
  - commit `2bb6f8f86` 同次跑 `go mod vendor`，把 bleve / RoaringBitmap / bbolt / snappy 等 vendor 目录补齐（之前 go.mod 与 vendor/modules.txt 不一致，`go build ./...` 报 `inconsistent vendoring`；vendor 同步后干净）
- 模式：
  - `pg_advisory_xact_lock(hashtextextended('llm-gateway:promote:<table>_hot:v1', 0))`
  - CTE 单语句 DELETE FROM hot USING batch + INSERT INTO parent SELECT … RETURNING 1
  - INSERT 前 `ensure_<table>_partition(v_month_value::date)`
  - `lifecycle.<table>_hot_retention_hours` 默认 8，hot-reloadable
- 提交链：
  - `88c6fbf7b feat(bg,sql): pair session_module_executions_hot + dashboard_access_events_hot with PartitionManager`（仅 SQL 与迁移条目，未在 promoteSpecs 注册）
  - `1049521b9 fix(migration): renumber session_module_executions promote 579 → 580`
  - `719860579 fix(sql): repair parallel promote migrations (579 renumber + COMMENT literal)`
  - `0c7e62b14 fix(sql): 579 references the real ensure fn name (ensure_dashboard_events_partition)`
  - `91d2e3b08 fix(migrations): resolve 579 duplicate + align LP5 audit script with 601/604`
  - **`2bb6f8f86 fix(LP1,LP9): register sme/dae in promoteSpecs + vendor sync`（本次落地的 Go 侧 wiring）**
- LP5 audit 仍 exit 0；`TestNumericUpMigrationVersionsAreUnique` PASS 0.00s
- 风险：`TestPromoteSpecsCoversAllDefaultPartitions` 当前无法在 `bg/` 包下编译（pre-existing HEAD 损坏：`bg/credential_probe_v2_test.go` 缺 package + imports + 函数头；`bg/credential_recovery.go` 含未解冲突标记 `<<<<<<<`；`bg/credential_probe_v2.go` 引用了 `BindingOnly` / `writeBindingUnavailable` / `uniqueStringSet` 等未实现字段/方法）。这些是另一作者的 WIP 损坏，跟 W2-9 无关，建议另开 PR 修复

## 待办清单

| ID | 任务 | 关键文件 | 备注 |
|---|---|---|---|
| ~~W2-10~~ | ~~titlestore 移出分区表 + Migration 562 columnar 重审~~ | — | **DONE 2026-08-26**：`session_titles` / `session_title_states` 均为普通 heap 表（DDL 无 `PARTITION BY`，`bg/partition_manager.go` 无注册条目），无迁移必要；562 的 columnar→heap 契约 + titlestore heap 防回归已写入 `sql/migrations/startup/migration_562_test.go`，3 个 case 全过 |
| W2-11 | `bg/partition_manager.go` 的 dae/sme promote-spec 注册 | `bg/partition_manager.go` | **DONE 2026-08-26**：commit `2bb6f8f86` 在 `promoteSpecs()` 补全两条新 spec（`// Migration 579` / `// Migration 580`） |
| W2-12 | import dry-run / audit / orphan cleanup | `scripts/audit-imports*`（如不存在则需创建） | 待评估 |
| W2-13 | 前端 specialty | `web/src/components/NodeDetailAvailabilityPanel.vue`（已被另一作者 WIP 修改） | **协调再动手**；先 `git status` 确认 WIP 是否落地，未 commit 前不动 |
| W1-9/10 | LP4 waterfall 优化 | `domains/session/queue/memory_optimization*` | 基线 41.9µs / 111KB / 214 allocs |
| W3-13 | nodestatecache 位宽重排 | `internal/cache/nodestate*` | 待评估 |
| W3-14 | 流式读取 watchdog | `internal/stream/watchdog*` | 待评估 |

## 不可触碰（WIP / 预 stage）
- `web/src/components/NodeDetailAvailabilityPanel.vue` — 另一作者进行中的 WIP，未提交
- `admin/bg/*` — 当前 main 上 `admin/bg/` 目录不存在；预 stage 文件若出现在工作区，禁止自动 commit（untracked 留待人工 review）

## 锁与约束
- 必须先 `git pull` 再开始新会话（避免覆盖此 commit `88c6fbf7b`）
- 必需 skills：`pg-release-schema-manager`, `pg-columnar-partition-audit`, `tdd`
- LP5 audit 必须在每次 schema 改动后跑：`bash scripts/check-body-storage-schema.sh`
- Promote 周期：默认 1h（hot-reloadable）；batch 上限：5000
- 当前 main 顶端 commit：`2bb6f8f86 fix(LP1,LP9): register sme/dae in promoteSpecs + vendor sync`
- 之前一关键 commit：`30eb4c34f fix(telemetry): stop writing removed request body columns`（W2-8 停写点）

## 风险提示
- `bg/partition_manager.go` 当前 main 顶端由 commit `2bb6f8f86` 补齐 promoteSpecs 的两条新 spec；若新会话需要新增 promote 函数，请参照 580/579 的 advisory lock + CTE DELETE+INSERT + ensure_partition 三件套，禁止复制 paste 时漏掉 `RETURNING 1` 或 advisory key。
- session_module_executions_hot 与 dashboard_access_events_hot 的父表分别是 columnar RANGE 分区（按 created_at 月）；INSERT 前必须确保目标月分区存在。
- `bg/` 包在 HEAD 处于损坏状态（`bg/credential_probe_v2_test.go` 缺 package/imports；`bg/credential_recovery.go` 含未解冲突标记；`bg/credential_probe_v2.go` 引用未实现字段 `pr.BindingOnly`、方法 `writeBindingUnavailable`/`restoreBindingOnProbeSuccess`/`loadBoundRawModels`、helper `uniqueStringSet`、常量 `provider`）。`TestPromoteSpecsCoversAllDefaultPartitions` 等本地 `bg/` 测试无法编译。新会话如果需要跑这些测试，必须先单独修复上述 3 个文件（不在 W2-11 scope 内）
- vendor 与 go.mod 在 `2bb6f8f86` 之前不一致（`go build ./...` 报 `inconsistent vendoring`）。如再次跑 `go mod tidy && go mod vendor`，请注意可能新增 vendor 文件或修改 `vendor/modules.txt`，commit 时一并提交
