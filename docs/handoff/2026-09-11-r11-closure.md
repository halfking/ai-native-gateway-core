---
round: R11
date: 2026-09-11
status: closure-complete
predecessor: R10b (docs/handoff/2026-09-10-r10b-planning.md)
successor: candidate R12 (待启动)
---

# R11 闭环 — 24h 审计（2026-09-11）与下一步规划

审计报告：`docs/audit/2026-09-11-24h-audit-round11.md`。本轮完成：
ff-merge 693 + vue-tsc 合并修复（4fd34bc00）、P1 解绑标记守卫 +
P2 journal 锁内尾读（ec54ea7a7）、R10b §2.1 doc-sync 残余清零 +
R11 报告落档。RawDataLogger 维持 R10b 方案 C（保留现状），R11 的
doc-sync-fix 子项就此完成。

## R12 建议切入顺序（按风险/收益）

1. **候选 7（P1 级行为）**：Limiter recoveryStep 无差别 RecoverStep(50)
   会把 admin 热更新的低并发上限 ~15 分钟内拉回 50。修法：
   shrink 基线 min(基线, 热更新值)。文件：Limiter/summarize 路径。
2. **候选 4 + 23 + 21 清理批次（破坏性，一次做）**：fallback 转换器收敛
   为薄包装 IR（ConvertAnthropicResponseToChat map[string]any 硬错、
   convertBlockMessage 首个 tool_result 即 return、system 声明 string、
   `_kxg_meta` 私有注入）；web 死视图 19/死组件 26（注意
   quality-api.ts:formatCalculatedAt 仍被 QualityTab 引用）；
   datetime/useFormat 双轨哨兵（'' vs '—'）。
3. **候选 18 尾巴**：/api/errors/trend（V371）UI 接入 +
   ErrorDetailTab 渲染 health_error/health_latency_ms。
4. **巡检类**（低频告警即可）：12 lite SQLite TTL worker、13 default
   缝隙计数巡检、14 promote per-table 保底份额、15 promote 函数体
   SET LOCAL TIME ZONE、16 注释漂移三处、8 慢性无界增长三处。

## 启动前 checklist（沿 R10b 惯例）

- `git pull --ff-only` 后核对 `git log --oneline -5`，注意并行会话
  新增迁移前必须 fetch 核对远端编号（693 已占用，下一编号从 694 起）。
- 子代理派发前先发一个探测 agent；provider 故障直接主代理串行
  （R11 实证：全 6 路同时失败，探测一句 go.mod 即可判明）。
- 提交纪律：逻辑单元完成即精确路径提交（并行会话会扫走未提交文件）。
- pre-commit 六门禁（go vet / SQL / 迁移编号 / down.sql / vue-tsc /
  token 合规），web 触碰后必跑 `cd web && npx vue-tsc --noEmit`。

## 可复制的 R12 启动提示词

> 以下提示词供新会话直接粘贴启动 R12。

```text
请继续 llm-gateway-go 的 R12 审计-修复轮。先读 docs/handoff/2026-09-11-r11-closure.md 与 docs/audit/2026-09-11-24h-audit-round11.md 恢复上下文，然后 git pull --ff-only 核对最新提交。本轮范围按优先级：(1) 修复 R9 候选7——Limiter recoveryStep 无差别 RecoverStep(全局默认50) 会把 admin 热更新的低并发上限约15分钟内拉回50，改为 shrink 基线 min(基线, 热更新值)，补回归测试；(2) 清理批次：fallback 转换器收敛为薄包装 IR（候选4，注意 ConvertAnthropicResponseToChat 的 map[string]any 硬错与 convertBlockMessage 首个 tool_result 即 return 丢并行工具结果）、web 死代码（候选23，保留 quality-api.ts:formatCalculatedAt）、datetime/useFormat 双轨哨兵统一（候选21）；(3) 候选18 尾巴：/api/errors/trend UI 接入与 ErrorDetailTab 补渲染 health_error/health_latency_ms。约束：新增迁移前先 fetch 核对远端编号（下一编号 694 起）；每逻辑单元精确路径提交；触碰 web 后跑 cd web && npx vue-tsc --noEmit；触碰 dispatch 后跑 go test ./domains/dispatch -race。完成后 go build ./... + 全量 go test，推送 main，并按 handoff 技能产出下一轮提示词。
```
