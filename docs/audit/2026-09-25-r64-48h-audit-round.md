# R64 48h 审计轮（2026-09-25，窗口死区补审 + auto 专项 P0/P1 批判式复审）

- 窗口：R63 收口（153ad093a）→ 97d8870aa，名义窗口仅 1 提交（taskprofile 回路 C）。
  **但本轮实锤了「审计窗口死区」**：R63 窗口终点（7489c8e92）之后、R63 收口提交之前
  落地了 5 个提交（69f5aa6a1 / e1f4be463 / 1c0c200be / 3b410b6ab / b21c499c4 / 
  80bb3675a——其中 80bb3675a autoroute v2 P0 为千行级大提交），全部逃逸 R63 审计。
  本轮按 48h 日志全额补审，主对象因此为 **auto 专项 P0（80bb3675a）+ P1（97d8870aa）
  首轮批判式审计**，外加三个逃逸小提交与 R63 遗留复核。
- 执行形态：6 组并行只读审计子代理（A1 taskprofile 回路 C / A2 autoroute P0 /
  A3 逃逸小提交 / A4 R63 遗留 §三 1-14 复核 / A5 installer 凭据三件+selector 回归 /
  A6 横切门禁+mockprobe 健康）→ 协调者汇总 → 5 组并行修复子代理（FixA-E，
  文件集互斥）→ 合并后全量验证 + 协调者 scratch PG17 单点真库契约。
- 入场：落后 origin/main 1 提交快进对齐（零合并）；codegraph 增量构建复现 malformed
  → rm -rf 全量重建（145275 nodes / 257504 edges，R61/R63 同款坑第三犯）。

## 一、声明实证主结论

- **taskprofile 回路 C（97d8870aa，P1 提交）**：核心声明全部实证成立——纯函数/DB
  分离、6 单测、闸门常量（≥30%/≥5/全局另需 ≥10）、阈值只降不升（候选
  {0.85..0.50}/0.05、地板 0.50、覆盖率 ≥80% 取最保守）、覆盖率基于原始置信度（单测
  钉桩）、回放只读只出计数、隐私红线（token 仅来自 domain_hint 特征列，SELECT 面无
  prompt/body 列）、admin 鉴权链（SuperAdminMiddleware）、双审计留痕、8 语言 10 键、
  apply 与 hydrate 同契约（llm_confidence 严格同口径）。两处打折：「pending 去重镜像
  bg 语义」实为更严谓词（方向安全）+ 并发不防重（本轮修）；CLI 回放互指注释单向。
  **i18n 10 键放错层级（filter: 而非 action:）致前端运行时缺键**——用户可见缺陷，
  本轮修（i18n-audit 实测 HEAD missing=10）。
- **autoroute v2 P0（80bb3675a，逃逸提交首轮审计）**：240 例 GATE PASS 本机逐字复现、
  零热路径改动属实、SQL 全参数化、隐私占位符落实、recent 策略旧行为逐字等价、
  8 语言在提交时点自洽、vue-tsc 纯 && 链。三处 P2：disagreement 采样排序键
  `classifier <> 'heuristic'` 语义失真（session_cache/v3_heuristic/jev/embedding 等
  非 LLM 行反被优先）；`--refresh-baseline` 在 set -e 下不可达（改基线恰是门禁 FAIL
  的场景）；baseline 门禁无语义校验（清零基线平凡通过）。「套件 60→240」口径失实
  （实为 100→240）与 v3 桶构成描述失实（en_business/trap_v2 在 v2 文件）已勘误。
- **逃逸小提交**：69f5aa6a1（hostedtask cancel 同步入队回调）声明全部成立（终态
  sticky/FOR UPDATE/幂等键/无持锁慢操作）；e1f4be463（origin 打标）核心修复成立
  （self_check 流量确曾误算 usage），**但 session-ping 的 Probe 字段是死代码**（声明
  证伪，恒 false，本轮修）；1c0c200be（secret-scan 6 处 BLOCK 修正）红线核查全部
  为真修复或真误报豁免，无凭据被标误报；3b410b6ab（kxpms.cn 放行）31 条均公开域名
  引用，类别键隔离不放过其他规则。
- **R63 修复回归（A5/A6）**：installer 凭据三件（F15-F19）全部在位健康且测试覆盖
  （验它别重做它——注：heartbeat 实际回退顺序为 ~/.kx-gateway → state/，refresh.token
  不在链上，实现自洽）；selector 权重语义 higher=higher priority 三方一致（代码/DDL/
  测试）；mockprobe F10-F14 无回退，装配面 config 三处同源零漂移，关闭态五不逐项有
  证据。R63 §三 14 项遗留：**13 项仍遗留、#14 状态漂移**（P0/P1 规划内推进，但
  tier_selector 无生产构造点的内核事实不变，被 P1 新代码注释自证）。
- **横切（A6）**：sequence 门禁含 745/800 且线测试钉住；五点同步完整；handoff 链
  引用文件全部存在（两处引用失准已修）；窗口文档抽查数字全部一致；menu-config
  有效且新鲜。

## 二、当轮修复（代码 17 项 + 文档 9 项，全部带回归测试或 diff 取证）

| # | 级别 | 发现（出处） | 修复 | 验证 |
|---|---|---|---|---|
| F1 | P2 | generate check-then-insert 竞态，双跑重复插 pending 提案（A1） | insertDraftsGuarded：单事务 pg_advisory_xact_lock('TUNP',1) + 锁后 EXISTS→INSERT；collect/backtest 保持锁外；真库并发契约测试 | TestInsertDraftsGuarded_ConcurrentDedup_RealDB ✓（scratch） |
| F2 | P2 | keyword_add 只能产出 technical/business 枚举词，无判别力（A1） | genericHints 纳入三枚举值（写入侧升级自由 token 后自动恢复）；单测三枚举 0 草稿 | taskprofile 套件绿 |
| F3 | P2 | AnalyzeOnce 无互斥，双管理员并发重跑+重复插入+HTTP 阻塞 5min（A1） | TryLock 非阻塞 + ErrAnalyzeInProgress 哨兵；handleAnalyze 回 409 | bg+admin 新测试 ✓ |
| F4 | P2 | 97d8870aa 把 10 个 i18n 键放 filter: 层级，视图引 autoTuning.action.* 运行时缺键（A2） | 8 语言 10 键迁 action: 块（文案逐字不动，纯位置移动）；i18n-audit missing→exit 1 门禁化 | i18n-audit missing=0 exit 0；vitest i18n 17/17 |
| F5 | P2 | disagreement 排序键 `<> 'heuristic'` 把 session_cache/jev/v3_heuristic/embedding 优先（A2） | 排序键改白名单 `classifier IN ('llm','llm_v2')`（llm_v2 为审计后新发现的 V2 后缀值，已核 decision_v2.go:385）；测试钉键 | sampling SQL 形状测试 ✓ |
| F6 | P2 | --refresh-baseline 在 set -e 下不可达（A2） | refresh 模式不再传 -gate-default，-write-baseline 并入同一次回归运行 | 脚本 stub 干跑三模式 |
| F7 | P2 | baseline 门禁无语义校验，清零基线平凡通过（A2） | validateBaseline：阈值非零+total_cases 一致+suite_files 一致，不符 exit 2 | 7 拒绝场景单测 ✓ |
| F8 | P2 | session-ping Probe 字段死代码恒 false（A3） | 抽 newCredentialSessionPingResponse 纯函数单点 Probe:true；wire 级测试 | admin 新测试 ✓ |
| F9 | P2 | global-auth-passed 哨兵可伪造 origin_stage 免计 usage（A3，pre-existing 被放大） | stage↔actor 配对表（self_check×3 worker / node_probe×2，全仓写入点枚举）；错配剥头走既有 strip 路径；其他 trustedOwner 语义未动 | middleware 6 用例 ✓ |
| F10 | P3 | 去重 LIKE 未转义 %_\（A1） | escapeLikePattern 纯函数 + ESCAPE '\' | 6 组单测 ✓ |
| F11 | P3 | apply 不强制只降 + ErrNoRows 500 + long_context_tokens 口径（A1） | thresholdOnlyLowerViolation（new≥current 409 fail-closed）；ErrNoRows 特判（同修 keyword/weight 两个 apply 同类缺陷）；tuning_store 对 ≤0 拒绝与 apply 同口径（种子 50000 正整数已核） | admin/tuning_store 测试 ✓ |
| F12 | P3 | 闸门分母混 classifier + 5000 行截断口径（A1） | volume 与 corrections 收集均加 classifier='heuristic'（与回放口径一致） | SQL 面审查+套件绿 |
| F13 | P3 | 负值舍入朝零致基线自斥 + stratified 突破 size≤200 + CLI 组合矛盾静默 + days/limit 无校验 + join 扇出重名（A2） | math.Round/Floor 语义化；外层 LIMIT size；validateFlags 表驱动 exit 2；dedupeByRequestID | auto-testbench 套件 + 240 例 GATE PASS 复跑 ✓ |
| F14 | P3 | mockprobe 客户端未打 origin 标（A3；路径结论：/mock/v1/* 不写 request_logs，打标为防御性） | X-LLM-Origin-Stage: self_check + Actor: mock-probe-client；不新增 stage 值避免与 DB CHECK 脱节 | client httptest 断言 ✓ |
| F15 | P3 | cancel 事件 actor 固定 "tenant"，recall 路径审计失真（A3） | cancelRowInTx 加 actor 参数：CancelTask=tenant / RecallTask=system | TestStoreAgainstPostgres 断言更新 ✓（scratch 实跑） |
| F16 | P3 | generateCorrectionProposals 响应类型失实 + TaskProfileView 写操作缺 super 门（A1/A2） | ProposalDraftSummary 如实类型；router requiresSuper + appNav/menu-config super 同步（export 脚本重生成） | vue-tsc 0 错误 ✓ |
| F17 | P3 | selector_test 注释自相矛盾（A5） | 重写为 Stage1 严格/Stage2 兜底分工描述 | 仅注释 |
| F18 | P3 | 745 头注释缺幂等声明（A6） | 补注记④（对齐 800 风格，措辞避开 migration_745_test 负向断言）；embeddata 副本逐字节同步 | 两文件 diff 为空 + TestMigration745 绿 |
| F19 | P3 | R63 handoff §三.6→§三.4 错引 + 失效行号 + 入口清单漏 §三.8/11/12/13 说明（A6） | 三处修正+补不带入说明 | 文档 |
| F20 | P3 | R63 §三.12 三处文档计数漂移（A4/A6） | hotzone 12→9、routingopt 30→29（两文件）、mock-probe 01/03/04 加废弃标注 | 文档 |
| F21 | P3 | P0 审计文档 v3 桶构成/60→240/失败数口径失实（A2） | 三处勘误（标 R64 勘误） | 文档 |

无 P1 发现。**A1-P3-6**（generate 中途失败 500 但已插提案不计入响应，重试自愈）、
**A2 i18n-audit --json --out 64KB 截断**、**bg keyword 去重 LIKE 同款未转义**（FixA
越界发现，同族预留）登记不处置。

## 三、R63 遗留复核（§三 1-14 → 判定）

| # | 项 | 判定 |
|---|---|---|
| 1 | mock-probe 生产不可达 | 仍遗留（cmd/gateway 零装配，装配点仍仅 gateway-v2；owner 未拍板） |
| 2 | PR4 删 python mock | 仍遗留（server*.py/Dockerfile*/conf/compose 全在，待 owner 确认） |
| 3 | selector 孤儿 P4 接线 | 仍遗留（dispatch 无 select 调用/executor_ollama 不存在/NativeEndpoints 未加） |
| 4 | ollama serialize 多模态 gate | 仍遗留（serialize_ollama.go:199-209 空 return 自认 gate 未落码） |
| 5 | V800 boot ensure | 仍遗留（头注释自证零 Go 消费者，db ensure 链无 800） |
| 6 | sanitizer 原子预占 | 仍遗留（read-modify-write + 三处 fail-open 原样） |
| 7 | nodestatecache 空壳 | 仍遗留（零生产 import；「13 文件」系登记口径差异，实 23 .go/9 非测试） |
| 8 | WeightedRouter 文档化 | 仍遗留（Deprecated 头出自 R28，非新动作） |
| 9 | installer S5 余项 | 仍遗留（五小项逐一复实在案） |
| 10 | R61 §六 1/2/3/4/5 | 仍遗留（逐项证据在案；R61 文件实为 2026-09-24-r61-24h-audit-round.md） |
| 11 | ollama golden NDJSON 边界 | 仍遗留（无 golden、无跨 read 分块用例、无 roundtrip） |
| 12 | 文档计数漂移三处 | **已消化（本轮 F20）** |
| 13 | /mock/* 无速率限制 | 仍遗留（接受风险维持） |
| 14 | auto 专项 | **状态漂移（变好）**：P0/P1 按规划落地；tier_selector 零生产构造点内核事实不变 |

## 四、验证（实跑）

- go build ./... ✓；go build -C installer ./... ✓；go vet ./... 全链 exit 0 ✓；
  gofmt 改动文件 clean（internal/mockprobe/runner{,_test}.go 系 HEAD 预存未格式化，
  R63 同款政策不动）✓
- 受影响包 go test -count=1 全绿：taskprofile / bg / middleware / internal/mockprobe /
  internal/endpointselect / cmd/auto-testbench / sql/migrations/startup /
  internal/upstreamurl / internal/ir / admin（66.3s）/ domains/hostedtask /
  domains/streaming（88.1s）/ autoroute ✓
- scripts/apply-db-revision-sequence_test.sh 契约 passed ✓；go -C installer
  ./internal/dbinit（五点 parity）✓；TestMigration745 绿 ✓
- **真库契约（一次性 docker PG17 scratch，端口 55432，用后即焚；子代理全程禁连库）**：
  - TestDDLSkipGuardsAgainstRealPostgres ✓（自建 oneshot 库）
  - TestEnsureSqlAuditPartialIndexes_RealDB ✓（744 重建+幂等快路径）
  - TestEnsureReportSnapshots_RealDB ✓（745 创建+幂等）
  - TestInsertDraftsGuarded_ConcurrentDedup_RealDB ✓（F1 新契约：并发恰 1 份 pending）
  - TestStoreAgainstPostgres ✓（hostedtask 711/742 全套，含 F15 actor 断言）
  - 745 psql 双跑幂等 ✓（raw_model_name + 四键 UNIQUE 复核）；800 双跑幂等 ✓
    （providers 夹具 backfill 2 行 is_primary/vendor_native 映射正确）
  - 夹具踩坑留档：schema_migrations 需 PK(version)（stamp ON CONFLICT 依赖），
    session_turns 需分区表+DEFAULT 分区（session_turns_hot 即 DEFAULT 分区）
- web：npm run typecheck 0 错误；i18n-audit missing=0 exit 0（含负向注入验证 exit 1）；
  vitest src/i18n 17/17（P0 轮归档的 keys_referenced 失败已不再出现）；npm run build
  全链绿 ✓；auto-testbench 240 例 GATE: PASS 复跑 ✓

## 五、引用

- 6 份审计子代理报告（A1-A6）+ 5 份修复子代理报告（FixA-E，本会话留档）
- R63 轮文档/handoff、AUTO_ROUTING_CLOSED_LOOP_V2_PLAN、P0/P1 落地审计文档（本轮勘误对象）
- 下一轮（R65）提示词：docs/handoff/2026-09-25-r64-48h-audit-handoff.md
