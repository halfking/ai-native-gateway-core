# R63 48h 审计轮（2026-09-24，新特性轨道批判式复审 + 标准清单横切复验）

- 窗口：2026-09-24 01:26（8ce9da13c，R61 覆盖终点）→ 7489c8e92，26 提交。R62 为部署轮，
  故本轮主对象 = **四个新特性轨道的首轮批判式审计**：r0924 supplier-protocol-optimization
  （8b94131d9）、mock-probe 通道（50065db1e）、迁移 745 对帐报表（2e52a7ee5）、installer
  自动激活（aa1160746/0f57fb0a5/3455a7a82），外加流式 P3 族与文档轨道。
- 执行形态：6 组并行只读审计子代理（S1 流式+hostedtask / S2 supplier-protocol /
  S3 mockprobe / S4 迁移+对帐 / S5 installer / S6 横切清单+文档清扫）→ 协调者汇总 →
  5 组并行修复子代理（文件集互斥）→ 合并后全量验证 + 协调者 scratch PG 单点真库契约。
- 入场：main 与 origin/main 一致（零合并）。codegraph 全量重建（144763 nodes /
  256595 edges；增量构建复现 malformed → rm -rf 重建解，与 R61 同款）。

## 一、声明实证主结论

- **r0924 supplier-protocol（8b94131d9）**：handoff「孤儿代码」三项声明（dispatch 未接线 /
  Candidate 无 NativeEndpoints / executor_ollama 未建）在 HEAD 全部仍成立，且与
  305089d63 addendum 的事实陈述一致、诚实。commit message「Closes F6」与同 message
  「P4 deferred」自相矛盾（已被 addendum 自纠）。F8 host-precise 修复本体无绕过
  （userinfo/IPv6/尾点均 fail-safe），仅 host.docker.internal 前缀洞（本轮修）。
- **mock-probe（50065db1e）**：核心声明成立——默认关闭、关闭态五不（无 goroutine/监听/
  DB 写/指标/可见）、mock 流量不进 pipeline/request_logs/计费、auth 旁路无放行扩大
  （路径穿越经 ServeMux cleanPath 回门链；token 常量时间比对）、migration 036 幂等。
  **但 v1 文档族（README/01/03/04）与实现是两套系统**：IsSelfProbe 字段、独立
  prometheus.Registry、__self_probe__ 租户 deny、9-mode、热重载全部未实现（v2 有意
  裁剪，文档未标废弃——本轮修）；且装配点= cmd/gateway-v2（注释自证「并行演示入口」），
  **生产主二进制 cmd/gateway 未装配，自探测通道现网不可达**（登记待拍板）。
- **迁移 745（2e52a7ee5）**：commit 标题「修正sql代码及对帐报表设计」失实——三文件全为
  纯新增，既没修正任何 SQL，设计与表结构互相矛盾（UNIQUE 无模型维度装不下 §3 聚合粒度、
  列族两边各说各话、hot+5 分区不存在）；**745 落在 migrations/ = 死文件**，boot ensure /
  sequence / installer / deploy 四通道无一可达，五点同步 0/5。本轮全额投递。
- **installer 激活（0f57fb0a5/aa1160746/3455a7a82）**：核心声明全部实证成立（register 即
  激活契约对齐、device_code 去凭据化、online.go 删除正确、真 ed25519、8 语言 README 同步
  无漏、取证文档无虚报、687 行测试是真测试）。但**凭据生命周期端到端断裂**：refresh_token
  解析后静默丢弃、activate --mode key 成功后丢 instance_token 成死循环、heartbeat 命令
  读取路径与 install 写入路径不匹配——7 天 JWT 到期实例必然 offline（本轮修）。
- **流式 15eb0f834**：finish_reason 穿 gate flush 机制链完整、usage 帧不误 gate、四协议
  面无第二处需同修，实证成立。8b94131d9 的 Unwrap 修复本体正确，但「8782 实测双终态」
  叙事在本树无活路径（打折扣，属中间构建态现象）。
- **横切清单**（S6 十项）：三层 provenance/SanitizedMessageRefs/AlignmentMap 已接 V2
  metadata ✅；超长压缩+4xx 保连重试生产可用 ✅；provider-window telemetry ✅；热区+分区
  8h 转移 ✅；supplier_errors 凭据质量闭环 ✅；代理出海四要素（订阅/探测/切换/HK 规避）✅；
  FreeDiscovery 全链 ✅。差距三项：sanitizer 跨进程 offset 原子预占缺失（fail-open 锁）、
  nodestatecache 单模块空壳（S7-4 遗留）、WeightedRouter 已废弃（权重 LB 实为 governor
  加权信号量+Stage D 软惩罚）。
- **凭据扫描**：窗口全量 docs/installer/README diff 按 sk-/Bearer/password 等模式扫描，
  命中逐条甄别均为占位符/测试令牌/字段名——**干净**。

## 二、当轮修复（代码 22 项 + 文档 5 项，全部带回归测试或 psql 实证）

| # | 级别 | 发现（出处组） | 修复 | 验证 |
|---|---|---|---|---|
| F1 | P1 | ParseOllamaStreamChunk 把累计文本写 Delta.Content（S2）——零消费者但 P4 接线即客户端文本重复 | 删 Delta 赋值只留 CumulativeContent，契约注释强制消费方 diff；更新三处测试断言 | ir 套件绿；grep 复核零消费者 |
| F2 | P2 | done+content 同行终止块 Type 覆盖、末段内容归宿未定义（S2） | 签名改 `([]*StreamChunk, error)`，done+content 拆 Delta+Done 两 chunk；补两测试 | 纯函数单测绿 |
| F3 | P2 | ParseOllamaResponse 错误路径返回普通 error，注释声称 *StreamError（S2） | 构造 `&StreamError{Type:"upstream_error"}` + Error() 方法，errors.As 可路由；补测试 | ir 套件绿 |
| F4 | P2 | OllamaChatURL 对 /v1 结尾 base 产出 /v1/api/chat（代码自认 404），测试固化错误行为（S2） | EpOllamaChat 分支剥离 versionTrailing；修正测试期望+补 /v9、幂等用例 | upstreamurl 套件绿 |
| F5 | P2 | selector 协议命名空间错位（ir "openai-chat"/"ollama-chat" vs catalog "openai-completions"/"ollama-native"）→ 恒 Stage 4，anthropic 同串侥幸掩盖（S2） | selector 入口加 irToCatalogProtocol 归一（对照 paramreg 先例）+ lockstep 契约测试（ir 五枚举表驱动） | endpointselect 套件绿 |
| F6 | P2 | selector 族内取 [0] 无排序违反自声明契约 + 权重方向反转（selector/DDL 声称 lower=高优先 matching provider——为假，provider 语义高=大流量份额）（S2） | 族内复用 (primary,weight,id) 排序；方向统一为 higher=higher priority，修正 selector 与 DDL 注释；测试反转断言 | endpointselect 套件绿 |
| F7 | P2 | V800/新表零下发通道（grep 全仓 Go 零引用、无 sequence/installer/ensure）——「P0 落地」实为死文件（S2/S4） | git mv → sql/migrations/startup/800（幂等声明+无主行边缘+零消费者三要素头注释）；sequence+门禁+installer embeddata+parity 全套登记（E1 同轮） | 门禁测试绿；scratch psql 双跑幂等+backfill 2 行正确 |
| F8 | P3 | F8 修复残留：host.docker.internal 前缀匹配洞（专治前缀不精确的修复里留了个前缀洞）（S2） | 改等值匹配；补 evil 子域/IPv6/反向 userinfo/大写/尾点 8 用例 | catalog 套件绿 |
| F9 | P3 | parse_ollama.go `_ = strconv.Itoa` 谬误注释（标准库无循环导入）（S2） | 删死代码与 import | 构建绿 |
| F10 | P2 | mockprobe runner/writeLoop 无 panic recover，子进程内 panic 即全进程崩溃（S3） | safeRound 包裹（recover+stack 日志+继续下轮）、writeLoop recover 退出；Stop 无界 wg.Wait 改有界；注入 panic 双测试 | mockprobe 套件 + -race 绿 |
| F11 | P3 | 探测目标未强制 loopback——LLM_GATEWAY_LISTEN 绑外网地址时自检流量走物理网络（S3） | BaseURLFromListen host 恒钳 127.0.0.1；补三 host 用例 | client 套件绿 |
| F12 | P3 | 启动竞态：runner 首轮先于 ListenAndServe，监听慢则首轮记脏失败（S3） | net.Listen 先建监听 → 起 runner → srv.Serve(ln)（确定性消除竞态，fail-fast 等价原路径） | gateway-v2 e2e 绿 |
| F13 | P3 | HistoryStore Insert/Close send-on-closed 窗口靠隐式契约；停机 10s 预算被在途探测与排空共享；pgxpool 永不 Close（S3） | done channel 替代 closed flag（天然安全+closeOnce 幂等）；停机序列 cancel 探测 ctx → 5s drain → 10s Shutdown 分账；probePool.Close 接线 | mockprobe 套件绿 |
| F14 | P3 | admin mock-% LIKE 前缀过滤爆炸半径：历史种子 mock-openai/anthropic 隐身、自建 mock-x 入库即隐身（S3） | 收敛精确集合 NOT IN ('mock-fast','mock-slow')（常量防漂移）；补可见性三断言 | admin 套件绿 |
| F15 | P2 | refresh_token 解析后静默丢弃——7 天 JWT 到期无续期实例必 offline（S5） | 注册成功路径写 state/refresh.token（0600 原子写，明文不进 activation.json）；补泄漏断言测试 | activation 套件绿 |
| F16 | P2 | activate --mode key 成功后丢 instance_token，heartbeat 报「请先 activate」死循环（S5） | WriteHomeInstanceToken 落 ~/.kx-gateway/instance.token（缺 token 报凭据不完整中止）；补三测试 | installer 套件绿 |
| F17 | P2 | heartbeat 命令只读 ~/.kx-gateway，与 install 写的 state/ 路径不匹配（S5） | cmd 层委托 enrollment.ReadInstanceToken 三路回退（消除两处读取逻辑漂移）；补回退链测试 | installer 套件绿 |
| F18 | P3 | writeInstanceToken 非原子写+固定 .tmp 名（并发 rename 互踩）（S5） | 三写点共用 writeTokenFileAtomic（CreateTemp+rename+0600）；并发写测试 | activation 套件绿 |
| F19 | P3 | instanceID/hardwareHash 错误吞噬——空 ID 致 DeriveDeviceCode("") 全机同码（S5） | 显式错误处理失败即中止；补 abort 测试 | activation 套件绿 |
| F20 | P3 | benign-EOF 分支可把零语义内容流钉成伪成功（与 empty-outcome 语义表冲突，usage 稀释构造型）（S1） | benign 分支追加 sawSemanticOutput 谓词（ChatChunkHasOutput；否决 gate 谓词因 usage 帧 commit 后恰好误判 true）；零语义→结构化错误回归 + MiniMax 形状仍绿 + toolsRequested=true 变体 | streaming 套件 88.8s 绿 |
| F21 | P3 | anomaly dedup 4096<history≤65536 区间每插入 O(n) 全表扫持全局锁（S1） | lastSweepAt 60s 时间闸（hardCap 清空路径刻意不受闸约束保内存硬上界）；时间闸三态测试 | ir 套件绿 |
| F22 | P3 | deliverOne 对 GetTask ErrNotFound（任务硬删）也 retryable，空转 8 次退避（S1） | errors.Is(ErrNotFound) → fail(false) 直达 DLQ，与 GetEvent 分支同构；三断言测试 | hostedtask 套件绿 |
| F23 | P1 | 迁移 745 死文件 + 五点同步 0/5 + 表结构装不下设计聚合粒度（S4） | 移入 sql/migrations/startup/745（+raw_model_name、UNIQUE 四键、删冗余索引、三要素头注释）+ sequence/门禁/installer embeddata+parity/Go ensure+stamp '745'/SSOT sql/objects/tables/report_snapshots.sql/纯单测 5 契约/真库契约测试 | 见 §四 |
| F24 | P2 | 对帐设计文档与表结构不闭环（scope 枚举/粒度/成本口径/schema_migrations.filename 幽灵列）（S4） | §2/§3 按实际表重写（四值 scope+raw_model_name 维度+分口径+hot 分区改演进计划）；§9 改 SELECT version | 文档自洽 |
| F25 | P3 | 744 down 用 DROP INDEX CONCURRENTLY 与 --single-transaction 消费不兼容无警示（S4）；R61 文档 F7 措辞出入；R62 handoff「下一可用=745」撞号 | down 头注释警示；R61 轮文档追加 R63 勘误注记；handoff 改 746（附 745/800 占用说明） | 文档 |

LICENSE_AUTHORITY_URL 透传（S5-P3）裁决**不透传**：.env 写入（第 5 步）先于激活（第 12 步）
无法门控；compose 无 state/→~/.kx-gateway 挂载且文件名不匹配（refresh.token vs
refresh_token），只写 env 会让容器 daemon 启动后永远读不到凭据、每周期报错，比现状
「干净禁用」更差。登记 handoff 待专项（挂载+命名对齐+激活后置写 env 协同改动）。

## 三、登记不处置（后续轮次入口）

1. **[P2] mock-probe 生产不可达**：装配在 cmd/gateway-v2（演示入口），生产 cmd/gateway
   零装配——README 已如实声明。接入生产入口是行为变更（新增端点+DB 写），待 owner 拍板。
2. **[P2] PR4 删 python mock 未执行**（scripts/mocks/llm-mock-upstream/*.py 等仍在）——
   05-implementation 偏差清单已补记，删除动作待 owner 确认。
3. **[P2] selector 孤儿（P4 接线）**：executor_dispatch 未调 endpointselect.Select、
   executor_ollama 未建、Candidate.NativeEndpoints 未加。本轮已把库函数级障碍全部清掉
   （F1-F6：累计文本契约/分块/错误类型/URL/命名空间/权重方向），P4 接线提示词见 handoff。
4. **[P2] ollama serialize 多模态静默降级空串**，§2.6.1 gate 未落码（P4 前置）。
5. **[P2] V800 boot ensure 未做**（文件头声明；与 P4 同做）。
6. **[P3] sanitizer 跨进程 offset 原子预占缺失**（S6-A1）：smart_sani_guard read-modify-write
   + fail-open 分布锁——用户清单项，大专项。
7. **[P3] nodestatecache 空壳**（S6-A6，S7-4 遗留延续）：13 文件零生产 import。
8. **[P3] WeightedRouter 已废弃**（S6-A10）：「权重负载均衡」实际=governor 加权信号量+
   Stage D 软惩罚；用户清单措辞与实现形状不符，文档化即可。
9. **[P3] S5 余项**：activation.json 明文 trial license_key（0600+launcher 白名单兜底）、
   state/ 0700 收紧对存量安装无效、legacy activation.json device_code=JWT 无清洗、
   st.ExpiresAt 语义混杂、prompt 包零测试。
10. **[P3] R61 §六 1/2/3/4/5 全部仍遗留**（读面归一 ~7 处、parse 面 requestID 4 处、
    hostedtask store 并发形状、UA SSOT、btrim/TrimSpace）；6/7/9 状态不变；10 已消化
    （R62 部署）；C1-C5 清理候选零引用复核通过，仍待 owner 确认。
11. **[P3] ollama golden 测试无 NDJSON 分块边界/roundtrip**（S2-P3）。
12. **[P3] 文档计数漂移**：hotzone 方案「12 项测试」实为 9、routingopt「30 文件」实为 29、
    mock-probe 01/03/04 v1 章节未标废弃（README 状态块已锚定）。
13. **[P3] 接受风险**：enabled 态 /mock/* 无速率限制 + 公开 token（设计文档已明示）；
    建议后续挂并发上限。
14. auto 专项（S6-A7）：tier_selector 无生产构造点、V3 分类器未进热路径——按
    AUTO_ROUTING_CLOSED_LOOP_V2_PLAN.md 自身 P0-P4 推进，不在审计轮代劳。

## 四、验证（实跑）

- go build ./... ✓；go -C installer build ./... ✓；改动文件 gofmt clean（5 个 HEAD 预先
  存在的未格式化文件不动，非本轮引入）✓；go vet 全链零输出 ✓
- 纯单测全绿：internal/ir、internal/upstreamurl、internal/endpointselect、provider/catalog、
  internal/mockprobe（含 -race）、internal/auth、config、cmd/gateway-v2、sql/...、db、
  domains/streaming（88.8s）、domains/hostedtask、admin（67.6s）、installer 独立模块 13 包、
  apply-db-revision-sequence 门禁脚本 ✓
- **真库契约（一次性 docker PG17 scratch，端口 55432，用后即焚；子代理全程禁连库，
  库级验证收敛协调者单点——纪律延续）**：
  - TestDDLSkipGuardsAgainstRealPostgres ✓（含契约 2b 第二遍 ensure 零 DDL）
  - TestEnsureSqlAuditPartialIndexes_RealDB ✓（744 重建路径+幂等快路径；夹具=分区化
    session_turns + default 分区 + session_aggregate_outbox）
  - TestEnsureReportSnapshots_RealDB ✓（745 新 ensure：缺席建表+stamp、二轮幂等）
  - 745 迁移 psql 实跑 ×2 幂等 ✓（结构验证：raw_model_name + 四键 UNIQUE）
  - 800 迁移 psql 实跑 ×2 幂等 ✓（providers 夹具 backfill 2 行、is_primary/vendor_native
    映射正确、ON CONFLICT 二跑零错误）

## 五、引用

- 6 份审计子代理报告（S1-S6）+ 5 份修复子代理报告（FixA-FixD2，本会话留档）
- R61/R62 轮文档、r0924 handoff、mock-probe 设计族、hotzone/auto-v2 方案（本轮勘误/对照对象）
- 下一轮（R64）提示词：docs/handoff/2026-09-24-r63-48h-audit-handoff.md
