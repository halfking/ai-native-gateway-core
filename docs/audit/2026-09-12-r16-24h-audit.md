# R16 — 24小时修正审计与修复（2026-09-12）

**审计对象**: 基线 `5367d6f84..84bae533d`（约 115 个非合并提交，317 文件）；本轮修复起点为 R15 收口 handoff（`2026-09-12-r15-baseline-ensure-699-behavior-closeout.md`）的遗留清单。
**审计模式**: codegraph 图谱（136k 节点）+ 主代理 + 7 个并行只读子代理（IR/流式、并发锁、SQL/分区/通道、供应商错误闭环、探测/代理/auto/免费池、会话轮次/摘要、Web/观测栈）；随后第二轮 4 子代理复核（IR 流式、分区基线、并发、文档一致性）。

---

## 一、执行摘要

24h 修正批次（694-700 迁移线、b2639182b IR 线、alias/fingerprint/自由池线）方向正确、无 P0。本轮审计新发现 **P1×2、P2×4、P3×9**，已修复 P1×2、P2×3 与大部分 P3，产出 7 个修复提交。两个 P1 均为"修复不可达/修复回退"类：

1. **b2639182b 的 unknown-only 归因不可达**：`executeAnthropicOnce` 在 `WriteNonStreamResponse` 之前用 `isEmptyAnthropicMessagesResponse` 判空，而该判空器的块循环无 default——`container_upload`（修复 commit 自己点名的案例）恰好不在白名单里，仍被误判 empty → failover + 供应商降级 + 拒计账，新守卫饿死。
2. **`go test -race ./internal/logging` 在 HEAD 上是红的**：R12 白盒测试改写 `l.baseDir` 触发 rotate 遗留的 fire-and-forget goroutine 无锁读共享字段。

## 二、本轮修复（7 提交）

| 级别 | 问题 | 根因 | 修复 | 提交 |
|---|---|---|---|---|
| P1 | unknown-only 归因被前置 empty 网关旁路 | 4 个判空器（executors/streaming × anthropic/chat/responses）块/项循环无 default 分支；chat 侧两副本对 content 数组形态判定相反 | 未知类型计为输出，归因权交还 IR/转换层（KindConversion, stage=gateway）；chat 判空器支持 content 数组（multimodal/refusal part）；container_upload-only executor 级回归测试锁门→守卫顺序 | 8f78d8c89 |
| P2 | 流式未知块：孤儿 `input_json_delta` 拼成空名 tool call；未知块零观测 | `content_block_start` 只对 tool_use 设 `currentToolCallID`，delta 无条件累积 | delta 累积加开放 tool_use 块守卫（孤儿丢弃+warn+quality flag）；unknown 块类型记录（去重 cap8 + `unknown_content_block` flag）；删除恒产生 `name:""` 的 ID 合成分支；EOF 空分类保持不变（有意语义，测试锁定） | 90e3bf18a |
| P2 | -race 红：cleanupOldFiles 无锁读 baseDir | rotate spawn 的 goroutine 读共享可变字段，无 WaitGroup 追踪 | rotate 持锁期值捕获 baseDir 作参数；Buffered 两处 2s flush 等待窗放宽到 5s（APFS fsync 停顿致 1/4 flaky） | 7d6f7ae94 |
| P2 | force-recover 无审计/无 actor；drawer 版与 applyForceEnable 恢复链不对齐；affected_credentials 恒 0；rows.Err 静默截断 | 06-23 minimax 事故"无 who/why"模式的 recover 侧翻版 | 写 model_offer_events（action=force_recover, reason_detail=actor+reason）；镜像 applyForceEnable 三件套（key cache/rotator/in-memory state）；diagnostics 版补 lifecycle 守卫；errors-trend 第三 UNION 分支喂 KPI；provider error stats rows.Err 升 500；serveUpdateModelOffer 部分失败回传 partial_errors | 59665a29e |
| P1 | 7 个分区函数存在于 Go 常驻调度面与开机 handoff 契约，但三份 baseline/全部安装通道均无定义 | 475/534/579/580 为带外迁移或原地演进，从未入 baseline；R15 同族（credit_ledger）只修了 2 个 | 权威库 pg_dump 体（非陈旧迁移文件——promote_sme/dash 已带 advisory lock+SKIP LOCKED，handoff ensure 为 NOOP 体）插入三份 baseline；含 R16 实测纠错：699 的 `ensure_supplier_errors_partition` 并非"在而未锁"而是三份全缺，一并补入 | 5a3915c5e |
| P3 | 关停顺序丢弃 close_drained 异常；limiter 热调容量单向粘滞 | reporter 先于 raw sink Close（enqueue 被 enabled 拦）；`hot < target` 只保护下调 | main.go 对调两段 Close；Close 内异常上报收敛 `remaining > 0`（stub 行不变）；hot 记录双向为恢复天花板；测试锁双向 | 089bb657b |
| P3 | Grafana 面板 provisioning 载入不了；deadcode-scan 假阳性扩到 24 文件；真死代码/孤儿 locale/axios | r12 面板是 API 包装格式；IMPORT_RE 漏两种 re-export 形态 | 面板改裸 dashboard 对象（契约测试绿）；正则补 `export … from`；删除 9 个确证死文件（逐一 import-spec 验证；i18n scanner/hardcodedCjk 为测试工具保留）+ navLinks 回归测试块；16 个孤儿 locale 文件+注册删除；axios 卸载；vue-tsc 零错误 | （web 单元提交） |

## 三、确认通过的关键面（抽查证据）

- **694-700 迁移线**（复核子代理确认）：三方 baseline ensure(15)/promote(18) 函数面实测收敛；五点接线齐；694/699 down replace-safe；700 guard 幂等；promote `pg_try_advisory_xact_lock` 蓝绿双实例竞争安全；hot 8h + 原子 CTE + default 缝隙批次成立。
- **供应商错误三闭环未被 24h 改动破坏**：写入点唯一、聚合三重防并发、`: thinking:` 注释帧路径（ADR-Disp-003）零触碰；监控读面切 current-month 修复 hot 失明。
- **会话轮次/摘要链路**：turn 双 advisory lock + ON CONFLICT 单调丰富、原始文本三层留存、digest 写/读/回填全绿；多 tag 存/滤/显通（缺写入 UI）。
- **文档一致性**（复核子代理）：7 大 P1/P2 修复的文档声称与代码/测试一致率 95%+。

## 四、遗留风险（未在本轮修复）

### P2 级
1. **OpenAI 响应侧未知 part 无载体**（IR 轴 P2-2）：`ParseOpenAIResponse` 的 `parseOpenAIResponseContentBlock` 对未知 part 只留空壳且不进 UnknownBlockTypes；`refusal` 字段无 IR 对应——空成功计账在 OpenAI 侧仍可复现。修法：Parse 补 refusal + UnknownBlockTypes + chat 执行器对称守卫。**建议下一轮首位**。
2. **全新安装的表形状缺口**（SQL 轴）：本轮补齐的是函数面；handoff 表 534 重构形状（分区父表+_hot+视图）、session_module_executions_hot/dashboard_access_events_hot 表体、475/534/579/580 迁移文件本体仍不在 installer 重放集（精选 478+ 清单）——全新安装仍会在启动契约 fail-closed。需单列一轮做"全新安装 e2e 重导验证"。
3. **#5 advisory-lock 迁移**（R15 §5 评估，编号从 **701** 起，700 已被占用）：14 个 ensure 函数 + `pg_advisory_xact_lock`，六面同步；顺带处理 3 个 date 签名函数的 `DEFAULT CURRENT_DATE/NULL` UTC 陷阱（现行路径由 Go 调用点显式传参兜住，本轮契约测试已把 4 个未钉扎函数列入显式 unpinned 清单，钉扎须随 ≥701 迁移而非改 baseline）。
4. **代理订阅池未接线**（探测轴）：proxy.Manager 出站热路径零调用，"海外模型走代理/避开香港/地域优先级"的配置均不作用于真实流量；现役通道是 `upstream/proxy_resolver.go` 单代理 URL。**需产品决策**：接线订阅池 or 明确单代理通道为准。

### P3 级
5. IR 打磨：混合响应未知块丢弃零打点；usage cache/reasoning token 流/非流、协议间不对称；`ParseOpenAIResponse` 只取 `Choices[0]`（n>1 丢弃上游已计费）。
6. rawFrameIndex 淘汰计数 Store 吞并发增量（有界振荡非泄漏，上界≈100k+并发突增）。
7. Web：statusBadge 仍 8 处独立实现；datetime 双轨入口未合并（哨兵已统一）。
8. 会话 tag 无写入 UI（后端 PUT 端点全通，仅能 curl）；两套并行 tag 体系建议产品收敛。
9. `sessions_v2_compression_read` "热更新回落 V1"注释失真（仅启动期生效）。
10. 判空器/IR 的块类型判定散落三层（executors/streaming/ir），建议下沉 `internal/ir` 共享谓词。
11. `UnknownBlockTypes` 只记类型名不存载荷——是否以 Extensions 保留 raw 块待产品决策（重放/排查 vs 存储成本）。
12. 本机环境行动项（R15 遗留 #1 未动）：llm-gateway-pg 应用 687（2026-10-01 死线，幂等）。

## 五、测试命令与结果

```bash
go build ./... && go vet ./...                       # 全仓零输出
go test -race ./internal/logging -count=1            # ok（修复前 FAIL）
go test ./domains/streaming/... ./domains/transformation/... ./internal/ir/... -count=1   # 全绿
go test ./admin/ ./sql/migrations/startup ./db ./bg ./domains/credential/ -count=1        # 全绿
cd installer && go test ./...                        # 全绿（含 TestStartupFilesAreAllEmbedded）
bash scripts/apply-db-revision-sequence_test.sh      # contract passed
cd web && npx vue-tsc --noEmit                       # 零错误
cd web && npx vitest run src/i18n/parity.test.ts src/config/appNav.test.ts   # 27 passed
go test ./deploy/grafana ./deploy/prometheus/rules -count=1                  # ok
# pre-commit 六项门禁逐提交全过
```

## 六、方法论备注

- 子代理比对结论须实测复核：本轮一处"699 函数在而未锁"的子代理结论被脚本实测推翻（三份全缺）——baseline 类审计结论一律以归一化比对脚本输出为准。
- 编号管理：startup 轨顶端 700，下一个安全编号 701（fetch 后需对共享 252 双账本复核）；deploy 轨顶端 V371。
