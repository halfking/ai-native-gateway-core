# 2026-09-10 24h 审计（第六轮 — R4 deferred 清零收口）

范围：R4 报告 `docs/audit/2026-09-09-24h-audit-round4.md` 全部 14 项 deferred
的最终收口。R5（`docs/audit/2026-09-09-24h-audit-round5.md`）已关 9/14，本轮
关掉剩余 5 项 P3（#1/#4/#5/#7/#13）+ 验证收口 #8，并完成两项独立清理
（logging_init.go 历史注释、web/src/views 视图层 datetime 残留）。
**R4 deferred 至此清零。**

本轮无新缺陷发现；属收口 + 守门钉线批次。会话开始时 fetch 确认远程无并行
新提交（基线 c56090dc0）。

## 修复时间线

| Commit | 内容 |
|---|---|
| `3fcfff226` | **P3 五项收口**：#7 region normalize、#1 explicit coverage flag、#4 probeKey 同源化、#5 swap 计数守门测试、#13 negative-cache 契约（7 files，+324 −43） |
| `fc3b17a2b` | logging_init.go 历史注释清理（initEnhancedIRTransport 引用） |
| `efd965c4b` | web views datetime 收敛：31 处 Date 型 toLocale* → utils/datetime（31 files，+96 −57） |

## R4 deferred 14 项最终状态

| # | 项 | 最终状态 | 收口位置 |
|---|---|---|---|
| 1 | aggregate cache partial 时 global selection 漏选 | **FIXED** | `3fcfff226` — `cacheEntry.covered` 显式覆盖标志；present-but-uncovered 强制 store fallback；global 路径保持 store 回源（per-entry coverage 无法证明聚合完整性，注释已改写为精确契约） |
| 2 | auto-swap 重选当前节点 | FIXED | `7086cc1fb`（R3 轮）`selectNodeExcluding(excludeNodeID)` |
| 3 | 并发 health-check 互相覆盖 | FIXED | `96e13c467`（R5）nodeProbeLock 全序列原子 |
| 4 | health-check 去重只用 ProxyURL | **FIXED** | R5 引入复合 probeKey；`3fcfff226` 将 probeKey 与 checker 探测目标统一到 `healthTargetURL`（trim + 默认 URL 同源），分组口径与探测口径不再可能分歧 |
| 5 | scheduled 探测不回灌 active swap 计数 | **VERIFIED + 钉线** | R5 重构已在 `healthCheckAllNodesInner` 步骤 5 调 `recordProbeResult`；本轮核对确认 `applyHealthCheckResult`（node 级 ConsecutiveFailures）与 `recordProbeResult`（selection 级 consecutiveFails）是两个独立计数器、各计一次，**无双计数**。新增 `TestGlobalHealthCheckFeedsActiveSwapCounter`（-race）钉住失败递增/成功归零双向 |
| 6 | 手动路径探测 PasswordDecryptFailed 节点 | FIXED | `96e13c467`（R5）`shouldSkipForHealthCheck` 四入口统一闸门 |
| 7 | 区域匹配大小写/空白敏感 | **FIXED** | `3fcfff226` — `normalizeRegion`（trim+upper）统一三处：`IsRegionBanned` 边界（既有）、LoadBalancer 地域亲和匹配（原为精确匹配，`" us "` 无法命中 `"US"`）、`RegionStatsReport` 分桶（原大小写变体分裂桶）。守门：`TestSelectNodeWithLocationNormalizesRegion`、`TestRegionStatsReportMergesCaseVariants` |
| 8 | policy 持久化绑 *PgStore | **VERIFIED（远程已修）** | `aea62be28`（R5 远程）已把 Load/UpsertSelectionPolicy 提升进 Store 接口；本轮核对全仓 policy 调用方（admin/proxy.go:968、manager.go:232）均走接口，无 type-assert 残留。manager 剩余两处 `*PgStore` 断言（:563/:619）是 #9 事务刷新的能力探测、带非 Pg fallback，属另一既定模式 |
| 9 | refresh commit 顺序 | FIXED | `b8d686267`（R5 远程）RefreshSubscriptionAndMetadata 单事务 |
| 10 | Start 持 lifecycleMu 跨 loadAllNodes | FIXED | `30293f256`（R5 远程） |
| 11 | swapLoop context.Background | FIXED | `96e13c467`（R5） |
| 12 | refresh goroutines 在 wg 外 | FIXED | R5（cacheRefreshes LoadOrStore + lifecycleMu 内 wg.Add） |
| 13 | empty subscriptions 不进 negative-cache | **FIXED** | `3fcfff226` — `installNegativeCacheEntries`：全量加载为"store 存在但无节点"的订阅安装 present-but-empty **covered** 条目；ReloadCache 孤儿清扫对现存订阅豁免（订阅删除后条目仍会被清）。守门：`TestFullCacheLoadInstallsNegativeCacheForEmptySubs`（含清扫豁免 + 订阅删除两条路径） |
| 14 | 缺覆盖测试 | **CLOSED** | R5 补 6+5 用例；本轮再补 #5 双向守门、#7 亲和/分桶、#1 covered 回源、#13 负缓存安装与清扫。R4 全部 manager 缺陷均有对应守门测试 |

## 独立清理

### logging_init.go 历史注释（`fc3b17a2b`）

- 删除提及已下线 `initEnhancedIRTransport` 的两段历史注释；保留仍有真实
  读取点的环境变量契约文档（`LLM_GATEWAY_ANOMALY_*` 在 main.go:1582、
  `LLM_GATEWAY_RAW_LOG_*` 在 initRawDataLogger）。
- **发现与后续候选**：`initRawDataLogger` / `initSemanticAnalyzer` 当前全仓
  无调用方（main 装配已改默认开启 async raw logger）。连同
  `logging.RawDataLogger` 的删除属破坏性清理（外部环境变量契约），按 R4
  "不扩删面"原则本轮只改注释，函数下线列为 breaking-change follow-up。

### web/src/views datetime 残留（`efd965c4b`）

- 交接预估 ~23 处，实际盘点 **31 处 / 31 文件**（首轮 grep head 截断 +
  tenant/、admin/ 子目录遗漏；完整清单以 `new Date(...)` 与裸 `d.toLocale*`
  两类模式全量扫描为准）。
- 全部迁移到 `utils/datetime` 的 `formatDateTime` / `formatTimeOnly`，options
  （hour12、dateStyle/timeStyle、zh-CN locale、date-only 组件裁剪）经
  `options` 透传，显示行为等价；ProvidersView / NodeHealthTimelineView /
  FreeDiscoveryView 保留原有 invalid-date 哨兵语义（守卫在前、合法 Date 进
  util）。
- **Number.toLocaleString（千分位）按设计排除**，未触碰。
- 至此 R3 #10 前端 datetime 收敛全部完成：组件层 11 文件（898f0b5d3）+
  视图层 31 文件（本轮）。

## 验证

- `go build ./...` PASS
- `go test ./proxy -count=1` PASS（0.85s）
- `go test ./proxy -count=1 -race` PASS（1.9s）
- `go vet ./proxy ./cmd/gateway` clean
- `go test ./sql/migrations/startup -run TestNumericUpMigrationVersionsAreUnique` PASS（push 前纪律）
- `web: npm run typecheck`（vue-tsc）PASS
- `web: vitest src/utils/datetime.test.ts` 16/16 PASS
- push 前 `git fetch` 复核：origin/main 无并行新提交

## 后续候选（非本轮范围）

1. `initRawDataLogger` / `initSemanticAnalyzer` / `logging.RawDataLogger`
   下线（breaking-change follow-up，涉及环境变量契约公告）。
2. `BatchUpdateNodes` 保留为公共 API（manager 已无调用方）；若确认无外部
   使用方可随上述破坏性批次一并退役。
3. `docs/architecture.md` 存储层描述与实际 Full 模式（SQLite+文件）不一致，
   文档修正 pending（见 memory lite-mode-architecture-doc-discrepancy）。
4. memory.Storage → lite.Storage 命名收敛（见 memory
   lite-mode-naming-convention，随破坏性批次处理）。

## 结论

R4 审计的 14 项 deferred 全部关闭（12 项代码修复 + #5 验证钉线 + #8 远程
修复经本轮核对确认），每项均有守门测试或验证记录。proxy manager 并发模型
（nodeProbeLock 唯一 per-node 写锁 + 四入口锁内全序列）经 R5 P0 修正与本轮
-race 全量验证，当前无已知竞态缺口。
