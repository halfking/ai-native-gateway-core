# R20 — 24 小时修正审计（2026-09-13）

审计窗口：`be3baaaad → a8c3a7aaa`（112 commits，310 文件 +20,540/−3,595）。
方法：codegraph 图谱 + 主代理 + 7 个只读子代理并行（IR/协议转换、流式与供应商错误、队列/并发/LB、
hot+分区与双存储、freediscovery/auto/代理、前端组件化、24h 文档汇总），汇总后统一修复。
本文 = 汇总结论 + 本轮修复清单 + 遗留项登记。

## 一、总体结论

窗口内变更质量高：三个 streaming 修复（FlushHoldback、benign-EOF、orphan input_json_delta）在各自
覆盖路径上正确且测试到位；698 时区钉扎全域收敛、695 事故催生的单语句 CTE promote 形态健壮；
17 对 hot+分区表中 8 小时不变量除两处已文档化的有意偏差（request_logs_bodies=24h、
model_probe_runs_hot=14d TTL）外全部成立；balance-floor guard 与既有 probe 的豁免协作闭环、无乒乓；
供应商错误处理链路（记录→failover 不中断→think 侧带→supplier_errors 独立表→凭据详情→健康降级）
是完整度最好的部分；freediscovery 三项 24h 修复（/tasks/{id} 路由、ErrTaskNotFound sentinel 404、
前端契约）三方一致。durable 队列（SKIP LOCKED + fencing + lease）、五层信号量、确定性加权轮转
的并发设计质量显著高于平均。未发现 P0。

主要风险集中在"修复的局部性"与"纪律的最后一公里"：streaming 修复只覆盖了部分桥接路径（Q3 桥
holdback 残留为 P1）；IR 未知块记录只落了 Anthropic 一个解析器；迁移自插/三处同步的缺口
（695/696/697/701）；前端 stacked 弹层 ESC 连环关闭（P1）与静态门禁未进 CI。

## 二、逐维度结论（7 路审计）

### 1. IR 与协议转换（internal/ir、domains/transformation）
- IR 字段全景核对通过：请求 IR（ir.InternalRequest）含多轮/路由/项目/用户/任务/轮次/tag/模型/
  总轮次/供应商/凭据载体；运营维度合理下沉 requestfact/requestjourney 层（有意分层）。
- 请求方向未知字段经 Extensions/RawContent 双通道无损保真；**修复呈现"只修 Anthropic"局部性**：
  OpenAI chat 内容块、Responses output items（无 default 分支，web_search_call/mcp_call 整体静默
  丢弃）、Gemini parts 均未记录 UnknownBlockTypes → 同构缺陷潜伏。**【已修，见 F11】**
- 窗口内 2 个 commit 正向修复了此前 P1（unknown-only 响应误判、orphan tool_call 合成），测试齐备。
- per-turn 摘要能力存在且已接线（sessiondigest Build/回填/admin 读取），但 Responses 协议轮次
  user_input 抽取失败（顶层键是 input，walk 未覆盖）。**【已修，见 F7】**
- 附件闭环基本成立（上传→hash 去重→DB 双写→出站改写→生命周期清理）；遗留：IRTransformer
  （MM-1a）零接线死代码、共享文件清理无引用计数反查（P2，登记遗留）。
- 归档 codec 丢失定时请求 Class/DueAt（P3，登记遗留）；ContentBlock 三份手工镜像无防漂移守卫（P3）。

### 2. 流式会话与供应商错误处理（domains/streaming、upstream）
- **[P1] Q3 桥（anthropic_bridge，生产接线）holdback 吞终止事件残留**：message_stop 成功路径把
  finish_reason/usage/[DONE] 渲染进 gate 后即 return，短流全部落在 holdback 窗口时这些帧被持有，
  仅当 survival 协调器恰巧 Finish 该 gate 才送达——桥不自包含。**【已修，见 F1】**
- **[P2] Q2 桥缺 benign-EOF 对等**：上游发完 finish_reason 未发 [DONE] 即关流被判
  eof_without_done 丢弃重试。**【已修，见 F2】**
- 空名 tool call 另有两条残余路径（Q2 桥允许空 fnName 建块；responses 桥终态信封未 sanitize）
  （P2，登记遗留——建议在 IR 解析层统一拒绝）。
- 四处 `_ = gate.FlushHoldback()` 吞错无日志。**【已修，见 F9】**
- 4xx 错误体单次 Read 可能截断后分类。**【已修，见 F8】**
- 正面确认：SSE 跨 chunk 拼接/黏帧/16MB 行上限、心跳保序、断连三层感知、ctx 取消强关 body、
  supplier_errors 单一 funnel 写入+脱敏+低基数维度、凭据详情错误聚合 API、credentialhealth 三件套
  完整、超时分层清晰、重试风暴防护（±20% 抖动）到位。
- upstream client 把 4xx/429 以 (resp, nil) 返回的双轨契约（P2，登记遗留——新调用方易漏检）；
  默认 client 未设 MaxConnsPerHost（P3）。

### 3. 队列/并发/负载均衡/资源安全（bg、ratelimit、domains/credential）
- 24h 三个并发修复（close-order、值捕获、balance-floor ownership）复核全部正确；Go 1.27 下
  loop-var 类已根除，无同类残留。
- **[P2] SetCredentialCapacity 写序竞态**：先 Store 容量后记 hot ceiling，与 recoveryStep 窗口竞态
  可致容量永久卡错值（管理员下调 50→5 可能被抬回 ~28 且不再回落）。**【已修，见 F3】**
- **[P2] 24h 新增 BalanceFloorGuard 顶层 goroutine 无 recover**（同批多个手写 worker 同病，一个
  panic 即进程级故障）。**【BalanceFloorGuard 已修，见 F4；其余 worker 迁移 BaseWorker 登记遗留】**
- **[P3] recoveryStep 持写锁逐条 slog**。**【已修，见 F14】**
- P3 登记：ratelimit per-key map 永不淘汰；router weightCounters sync.Map 无淘汰；
  weightStride 32 位平台理论溢出；nodestatecache int32 秒级时间戳 2038；Semaphore.Acquire
  time.After 轮询分配；BalanceFloorGuard.Stop 不中断在途 cycle；sweepPlanQuotas 串行 HTTP。
- 正面确认：durable 队列教科书级（SKIP LOCKED+fencing+lease）；五层信号量逆序释放防双释放；
  权重 LB 来源清晰（DB 手工权重优先，否则按有效并发派生 clamp [1,1000]）；go vet -copylocks 干净；
  日志无密钥泄漏。

### 4. hot+分区存储 / 双存储架构 / 迁移纪律（sql/、bg/partition_manager、storage/）
- 8 小时不变量逐表核对：17 对表中 16 张默认 8h ✓；两处有意偏差已文档化（request_logs_bodies=24h
  防 TOAST 超时、model_probe_runs_hot=14d TTL 无分区）。更新/删除只在 hot ✓（未发现旁路）。
  批量转移为单语句 CTE+SKIP LOCKED+advisory lock+周期预算 ✓。
- **[P2] promote_supplier_errors_hot_to_partition 未钉扎时区**（V371 轨），与已被 699 钉扎的 ensure
  形成不对称——UTC 会话下月初整批 23514 滞留 hot 的同型风险。**【登记遗留，随 ≥702 新编号迁移
  收口（契约测试已声明 next number ≥701 为刻意排队项）】**
- **[P2] 迁移 ledger 缺口**：695/696/697 无 schema_migrations 自插；701 缺通道+installer+embeddata
  三处同步。**【695 已修（无主缺口），见 F12；696/697/701 已由并行线 701 登记归属，不越界，登记
  维持】**
- 双存储：lite(sqlite+memory+files) 与 full(pg+redis+memory+files) 并非同接口同路径（full 生产路径
  由 main.go 直接装配，storage/factory 为桩）；**SQLite request_logs 无 TTL/清理，长跑无界增长**
  （P2，登记遗留）；PartitionManager/BalanceFloorGuard 等 PG 依赖在 lite 下整体缺席（预期形状）。
- balance-floor 闭环成立；Pass A/C `LIMIT 100` 无 ORDER BY 饿死风险。**【已修，见 F10】**
- baseline ensure/promote 三方契约测试到位；盲区：supplier_errors 与 session 三族 promote 不在
  契约内（P2，登记遗留，随 ≥702 一并纳入）。
- tool_usage_stats 保留谓词读会话 CURRENT_DATE 漂移 ≤8h（P3，登记遗留）。

### 5. freediscovery / auto 模型 / 代理 / 免费 token 池
- freediscovery 三项 24h 修复三方一致；SSRF 防护完备；租户隔离 RLS+服务端派生闭环。
- **[P1] proxy/ 订阅代理池不在数据面**：Manager.SelectBestNode/自动切换/健康检查全部只作用于库存
  视图，数据面 upstream.Client 只认 env 单代理——选优对真实 LLM 流量零作用（admin/proxy.go 头注释
  自证）。**【登记遗留——需 upstream.Client 支持动态 ProxyResolver，属特性开发】**
- **避开香港：部分实现**（banned_regions 硬过滤机制完备、HK 识别支持，但无默认值，需逐订阅手工
  配置 ["HK"]）。**地域优先级：部分实现**（有 LocationAffinity 三档+单次 preferredLocation，无有序
  优先级列表；Subscription.Priority 从不参与排序）。**【登记遗留——建议平台级 default_banned_regions
  默认 ["HK"] + ordered region_priority】**
- **[P2] freediscovery 定时扫描未实现**（scheduled/webhook 触发类型为死常量，全靠手动点扫）；
  catalog 条目无主动活性探测。**【登记遗留】**
- **[P2] auto 多协议不一致**：messages/responses 上 auto flag 关闭时字面量 "auto" 透传上游必 400，
  chat 则落 fallback。**【登记遗留——行为决策需 owner 确认】**
- **[P2] escapeTenantID 非法字符静默降级 default**（P2，登记遗留——应 400）。
- 正面确认：免费池聚合走标准候选池+评分（免费档亲和 1.0）；QuotaTracker 三类淘汰+bg worker；
  modelname variants 24h 修复真实 bug（map 迭代顺序致跨进程漂移）。

### 6. 前端组件化 / 可观测性 / 一致性（web/，141 文件 +4180/−2281）
- **[P1] stacked 弹层 ESC 一次关闭所有叠层**（AppModal/AppDrawer 各自 document keydown，
  stopPropagation 不隔离同节点监听）。**【已修，见 F15】**
- **[P2] el-* 未注册缺陷类无静态门禁**（step8 修了 24 文件但无脚本落库，随时回归）；扫描另发现
  **3 个 v-loading 无指令导入的真实运行时缺陷**（指令静默失效）。**【两处均已修，见 F17】**
- **[P2] responsive-audit 未进 CI**（47 处白名单外存量仅 WARN 放行）。**【已修，见 F17】**
- 组件采用率 3%~7%（100 视图），多处"同视图半迁移"（ModelsView/KeysView/AuditLogView）；
  未迁移清单（手写弹窗 18 文件、内联统计 8 文件、裸 table ~90 文件）已在审计代理报告中，按
  既定 P3 分批推进。**【登记维持】**
- 正面确认：断点单一事实源+useBreakpoint 单例、useScrollLock 引用计数、useFocusTrap 圈闭返还、
  8 locale 无缺失 key、菜单 9 分组 labelKey 全覆盖、step8 el-* 修复全量复扫干净、DashboardView
  非死代码。

### 7. 24h 文档承诺 vs 现状
- 上轮文档声明未完成的合并清单（前端 CredentialMonitor 拆分+7 弹窗、断点 47 处收敛、折叠屏
  polyfill、4 个 date 签名 ensure 调用侧时区推导、testcontainers 真库行为测试、ensure 建分区并发
  advisory lock、offline_apply/launcher 三缺口、BufferedRawSink>4MiB、分区生命周期/opslog_trimmer/
  user_intent 护栏、balance-floor web 控件与真实摘出观察、native Responses 透传终止事件观察、
  codex 0.45.0 锁定解除、freediscovery templates list :76 LOW 观察、R20 例行）——**全部维持登记，
  本轮不扩scope**；其中 R20 例行审计即本文。

## 三、本轮修复清单（13 项）

| # | 严重度 | 修复 | 位置 |
|---|---|---|---|
| F1 | P1 | Q3 桥 message_stop 成功路径写终态块前 FlushHoldback（桥自包含，不再依赖协调器 Finish）+ 第四协议 holdback 回归测试 | domains/streaming/anthropic_bridge.go、stream_holdback_regression_test.go |
| F2 | P2 | Q2 桥 benign-EOF 对等：finalFinishReason 已累计（错误类已在 midStreamHalt 提前返回）时 EOF 不再判 eof_without_done + 回归测试 | domains/streaming/anthropic_stream.go |
| F3 | P2 | SetCredentialCapacity 先记 credHotLimits 再 Store 容量，封死与 recoveryStep 的写序竞态 | domains/credential/limiter.go |
| F4 | P2 | BalanceFloorGuard 顶层 goroutine + 每 cycle 双层 panic guard | bg/balance_floor_guard.go |
| F5 | P2 | gofmt 污染清零（本轮触达文件全格式化；internal/ir 整包顺带收敛） | internal/ir/*、domains/streaming/responses_bridge.go 等 |
| F6 | P3 | responses_bridge 清洁 EOF 路径背靠背重复的 IsAnthropicStreamEmpty 死代码删除（5b249f31d 缩进重构复制残留） | domains/streaming/responses_bridge.go |
| F7 | P3 | sessiondigest walk 补 "input" 键 + 字符串 input 合成 user 消息——Responses 轮次 user_input 不再恒空 + 2 个单测 | domains/sessiondigest/digest.go |
| F8 | P3 | 4xx 错误体单次 Read 改 io.ReadFull（短读不再截断分类输入） | domains/streaming/executors/executor_chat.go |
| F9 | P3 | 四处 `_ = gate.FlushHoldback()` 吞错改 slog.Warn | responses_bridge.go ×3、anthropic_stream.go ×1 |
| F10 | P2 | balance-floor Pass A/Pass C `LIMIT 100` 补 `ORDER BY balance_last_checked_at ASC NULLS FIRST` 防饿死 | bg/balance_floor_guard.go |
| F11 | P2 | UnknownBlockTypes 记录下沉为共享 helper，补齐 OpenAI chat 内容块 / Responses output+message content / Gemini parts（inlineData=已知不支持、未知形状记 unrecognized、signature-only 豁免）+ 4 个单测 | internal/ir/response.go、response_protocols.go |
| F12 | P2 | 迁移 695 补 schema_migrations 自插（700 模式；696/697/701 三处同步缺口归属并行线 701，维持登记） | sql/migrations/startup/695_*.sql |
| F14 | P3 | recoveryStep 持锁期 slog 外移（RecoverStep 改返回迁移快照，锁内收集锁外打日志）+ credHotLimits 过时注释修正 | domains/credential/limiter.go |
| F15 | P1 | 弹层叠层栈 composable（useOverlayStack）：ESC 只关最顶层；AppModal/AppDrawer 接入 + 2 个单测 | web/src/composables/useOverlayStack.ts、ui/AppModal.vue、ui/AppDrawer.vue |
| F17 | P2 | el-* 静态门禁落库（web/scripts/element-import-audit.mjs，扫描另发现并修复 3 个 v-loading 无指令导入的真实缺陷）；responsive:check + element:check 双门禁进 verify.sh --web | web/scripts/、verify.sh、web/package.json、3 个图表组件 |

（F13 编号留空：promote_supplier_errors 时区钉扎按纪律归 ≥702 新编号迁移，不在本轮文本修。）

## 四、验证

- `go build ./...` ✓；`go vet`（streaming/credential/bg/ir/sessiondigest）✓
- 测试：domains/streaming/...（含新增 4 协议 holdback + benign-EOF 回归）、domains/credential、
  internal/ir（含新增 4 解析器未知块测试）、domains/sessiondigest（含新增 2 Responses input 测试）、
  sql/migrations/...（含 695 自插）、bg（TestBalanceFloor*）全部通过。
- web：vitest ui 组件 + overlay stack 全过；responsive:check 与 element:check 门禁 0 违规。
- 前端全量 typecheck/test/build 在提交前由 verify.sh --web 复核。

## 五、遗留项（按优先级，供下一轮领用）

1. **[P2] promote_supplier_errors 时区钉扎**（F13）：≥702 新编号迁移 + 纳入 promote 三方契约测试。
2. **[P2] 空名 tool call IR 层统一拒绝**（Q2 桥/responses 桥终态两条残余路径）。
3. **[P2] upstream client 4xx 双轨契约**：client 层统一构造 *Error。
4. **[P2] SQLite request_logs 无界增长**：补上限/按天清理。
5. **[P2] freediscovery 定时扫描 worker** + catalog 活性探测；escapeTenantID 改 400。
6. **[P2] auto 多协议 flag-off 行为**（字面量透传 vs fallback/422）——需 owner 决策。
7. **[P1 特性] proxy 数据面接入**：upstream.Client 动态 ProxyResolver ← proxy.Manager；
   平台级 default_banned_regions 默认 ["HK"]；ordered region_priority。
8. **[P2] 696/697/701 通道+installer+embeddata 三处同步**（归属并行线 701）。
9. **[P3 组]** IRTransformer 死代码处置；归档 codec 补 class/due_at；ContentBlock 防漂移守卫；
   ratelimit/weightCounters 淘汰；其余手写 worker 迁 BaseWorker；锁内日志余量；
   前端组件化 P3 批量（未迁移清单见 §6）。
