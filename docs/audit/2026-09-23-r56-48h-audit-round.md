# R56 48h 审计轮（2026-09-23）

- 窗口：`2de612429..80c74af01`（2026-09-22 09:42 → 09-23 00:27，24h 修正全量，~45 提交；Go 158 文件 +16k 行）
- 入口：R55 handoff §九 + tests/48h-audit/ 整合中心（f30ee3bc5）+ 设计差距总台账 §八挂账
- 执行：codegraph 全量重建（37148 函数 / 143610 节点 / 254239 边）→ 8 域子代理并行只读审计（D01/D02/D04+D11/D06+D07/D08+D09/D15+D16/D14/D17+R55 修复面）→ 主代理逐条亲读复核 → 修复 → 三门 → 本文档
- 开局：`git merge --ff-only origin/main`（4484585a6→80c74af01 六提交，本地无独有提交，零冲突；工作区版本漂移文件与来向改动无重叠）

## 一、发现与处置

### P1（本轮修复）

| # | 项 | 证据 | 修复 |
|---|---|---|---|
| P1-1 | **738 installer 五点同步缺口**：runner.go StartupFiles 已挂 738，但 main.go 无 go:embed/map 条目、对账 map 缺 738——新装 applySQL 读不到 738 直接 InitSchema 报错中止；`TestStartupFilesAreAllEmbedded` 在 HEAD 红（764d2514b 提交前没跑五点闸门） | main.go:515-519,672-673；stats_migrations_test.go:191-198；测试红实跑 | 补 embed 变量 + map 条目 + 对账 map 三处，测试转绿 |
| P1-2 | **736 倍率列未进 promote 函数**：698 的 promote_request_logs（列清单止于 system_fingerprint）/promote_usage_ledger（止于 error_kind）均缺倍率列——热窗（8h）一到的批量转移把 credits_rate_multiplier 落 NULL（读取 COALESCE 成 1.0）、rate_multiplier 落 DEFAULT 1.0，计费回放/估算对全部已提升行静默回 1x | 698:415-431,696-707；maas/credits_sql.go:20；telemetry/client.go:344-347 契约注释 | 新增迁移 **739**（程序化从 698 提取函数体，仅三处列清单各补一列，up/down diff 复核干净；本机 dev 库事务验证 + 幂等收敛）；installer 五点全同步 |
| P1-3 | **collapseRepeatedText 多段折叠丢消息**：`out=out[:i]` 在第二段命中时暴露零值 Message（静默丢段间消息+注入 Role:""）；且任何 run 之后的消息全部丢失（无尾部 append）；既有测试只覆盖单段全覆盖形态 | prompt_compress.go:271-321（改前） | 重写为区间拼接（prevEnd 追踪 + 尾部 append），新增双 run+gap+tail 回归测试 |

### P2（本轮修复）

| # | 项 | 证据 | 修复 |
|---|---|---|---|
| P2-1 | **§11.6/stream_timeout 终端帧未闩锁 MarkTerminalRendered**：committed-then-EOF 分支写 errChunk+[DONE] 后不闩锁，兄弟路径均闩锁——缓冲门 survival 下可能叠加第二终端 | stream.go:1157-1166,1230-1233；survival_coordinator.go:1116 消费 | 两分支 safeFlush 后补 `gate.MarkTerminalRendered()` |
| P2-2 | **B14 看门狗"强制释放"实际零释放**：走 releaseSlotScript 的 EXPIRE 保温（"DO NOT delete"），槽键仍被 availableCount 计为占用且 idle gate 重置——比不干预（自然 TTL 过期）更差； breach 计数先于释放判定（TOCTOU 误计）；AfterFunc timer 不可停（高 QPS 常驻 timer 堆积） | executor_dispatch.go:1094-1131；slot.go:517-543 | 新增 `Manager.ReleaseHard`（DEL 槽键硬释放，Lua 返回 0/1 整数避免 redis.Nil 误路由；pin 保留复用迁移语义）；breach 计数移入 performed==true 之后；Lease 挂 watchdogTimer，正常 release 先 Stop；miniredis 钉桩 ×2（键真删+非占有者不计 breach） |
| P2-3 | **SQL 估算倍率口径偏离 Go 计费**：SQL 把 mult 折进每档 `CEIL(rate×disc×mult)`，Go 是 `CEIL(Σ×mult/1e6)` 总额单次取整——非整有效费率时估算系统性偏高（10.4×1.5：40 vs 39） | credits_sql.go:15-41（改前）；credits.go:90 | mult 移出每档 CEIL、并入总额除法（每档保留既有 CEIL(base×disc) 粒度档）；双侧注释对齐实测语义 |
| P2-4 | **B8 findings 清理承诺未兑现 + 窗口自相矛盾**：737/代码注释都写"job 自身删 window 外旧 findings"，RunOnce 无任何 DELETE（每小时重复落同一发现，无界增长）；window=24h 但只扫 _hot 表（默认 8h 即排干） | ledger_reconciliation.go:30-31,52,132-142 | `pruneOldFindings`（DELETE run_at < now()-2×window）+ `effectiveWindow`（clamp 到 lifecycle.hot_retention_hours 设置值） |
| P2-5 | **管理员重置密码不吊销目标用户旧 token**：B4 只覆盖自改密；admin 强制重置（凭据泄露响应）后旧 JWT 剩余 TTL（默认 24h）内全权可用——恰是 B4 要堵的威胁面 | users.go:592-600 vs 698 | reset 路径复用 revokeUserTokensForPasswordChange（失败不阻断重置，同 B4 trade） |
| P2-6 | **B12 三个 settings 键未登记 spec**：goal.audit_max_rounds/audit_verify_enabled/audit_verify_model 被 audit_hook.go 读取但 settings 注册表无——管理 UI 不可见/改不了（与 B1 补登记同类病灶） | audit_hook.go:272,275,487 | GoalSpecs() 补三条（含 min/max 与默认值对齐代码 defaultAuditMaxRounds=3） |
| P2-7 | **error_probe submitter 无模式门**：默认新探测模式下 activeProbe.Start 被跳过，submitter 仍接线——向无消费者队列投递（每键 running 泄漏一条+队列满 WARN 刷屏）；四键实际仅 2/4 生效 | main.go:4142-4146 vs 4214-4223 | submitter 接线加 `!useNewProbeMode()` 门（新模式由新探测栈拥有故障触发探测） |
| P2-8 | **738 列数守卫只 WARNING 不 fail**：psql ON_ERROR_STOP 不拦 WARNING，regexp 未命中会静默交付残缺视图链（恰是 738 要修的事故形态）；735 有 fail-closed 先例 | 738.sql:165-177 | WARNING→EXCEPTION；embeddata 副本重同步 |
| P2-9 | **R55-D2 多行 PEM 静默截断**（修复面 #1）：行导向解析器对跨行引号值首行必然失配→静默截断+残留引号；下游 docker --env-file 本就无法投递多行值 | test-deploy-local-env.sh 测试 4 实跑失败 | **裁决选 (b)**：解析器检测"引号开但同行未闭"→stderr 报错+exit 2（函数级仍非致命，减去损坏键）；测试 4 改期"响亮拒绝"；约定单行+字面 `\n` 落函数头注释。**R55-D4 已由上游修复关闭**（测试 6 PASS 实证） |
| P2-10 | **ctxpool 未接线且带竞态面**：全仓零生产调用方（Handoff-B #4 挂账中），但 acquireWithTimeout 无锁写 wrapper、Err/Deadline 无锁读、Release check-then-act 可双 Put | ctxpool.go:123,144,195-197,298 | wrapper 读写入 mu、Release 改 CAS、包级 STATUS 注释钉死"未接线"事实 |

### P3（顺手清理）

- live_stream_redis_store.go `_ = idleThresholdSeconds` 哑赋值删除（变量本在 ：1636 使用）
- anthropic_bridge.go:438 陈旧注释改指 internal/emptyoutcome（原引用已删符号）
- prompt_compress 死代码移除：compressScratch pool + acquire/releaseScratch + CompressedStats（零引用）；StripEmptyJSONFields 配置注释补 no-op 钉子
- geoIPSourceStatus 补"未接线"标注（留待 /api diagnostics 或下轮删除）
- db-changelog 补 736/737/738/739 登记 + **B11 撞号登记：738 已被 view 链修复占用，B11 落地从 740 起**（wave3 台账未更新）

## 二、核实为健康（要点）

- **A4 429 剔除断路**：breaker.go KindRateLimit 早退+HALF_OPEN ReleaseProbe 防卡死；两策略表均删项；绑定级 3min 降权保留（writer.go）。
- **B2 老化+双确认**：2h demote→recovering 有恢复路径（recoveringSweeper），不触碰 404 三级冷却；闪断双确认 15s 预算+per-(cred,model) 去重+WithoutCancel。
- **B3 自动补录**：resolve miss 真 nil+autoseed 5min 防抖（r.mu+singleflight 双保险），ensureAttempts 有清理。
- **D2/D3/D4 收口**：emptyoutcome/vendor_channels/usage 单表 grep 无第二裸实现；GLM 上下文溢出 completed 误渲染修复三处在位；§11.6 端到端被 stream_eof_test 钉桩。
- **D1 三臂闭环**：快路径 DeviceSeed 门+窗口复核+miss 回 DB；本轮仅补写侧 default 第三臂（headless 客户端条目不再必死）。
- **F15 债务补记**：meter 注入一次/准入、4 个额外上游调用点各触发一次、tpm 同成本模型、测试钉桩。
- **B1 双侧同源**：半开区间/跨午夜/坏值兜底/时区回退逐条对应；单点取档单点盖章；无 DST 风险。
- **B8 sqlite 降级**：lite 模式 dbConn nil→reconciler 不启动；D1 快路径 nil 守卫+miss 回落。
- **D11 auto 全链路**：maybeResolveAuto→decider 组装→非聊天路径→auto 队列桶→模型切换门默认 false；灰度 RT-1/RT-3 维持 off 与报告一致。
- **D15/D16**：B4 三中间件全覆盖+5s TTL 缓存+写侧失效；A3 clamp 唯二入口无绕过；B12 状态机单调原子；落库↔前端字段对齐抽查通过；menu-config 无漏注册。
- **D14 实跑**：`go test -race` bg/credentialfpslot/dispatch/ctxpool/credentialhealth/executors 全绿零竞态；`go vet ./...` 干净。
- **R55-F1b 回归**（反思落实：多 bash 版本实跑）：project_env_test bash 5.3.9 + 3.2.57 双绿；duplicate-release 契约 25 PASS RC=0；test-deploy-local-env 8/8（此前 6/8）。

## 三、遗留登记（R57 入口，按优先级）

1. **B7 virtual_ip 假名 IP 死分支（P1 级遗留）**：virtual_ip 全部是 10.x 假名 IP（identity.go virtualIPPrefix=10），classifyVirtualIP 对 10/8 原样返回——B7 外网段表归类对唯一数据源不可达。需数据源级修复（落真实来源 dim 或 rollup 写 real ip）+ 修好前给 classifyVirtualIPPie 结果加 pseudo 标注。
2. **ProbeSync 双层信号量顺序**（全局槽先取+per-cred 槽后取→同凭据突发可占满全局槽饿死其它凭据热路径探测）+ **ProbeConfirm 绕过 per-cred ≤2 闸**（一次覆盖 N 模型派生 2N 直连）。
3. **D4 total_tokens 推断流式/非流式不对称**（usage.go:161-170 塌缩条件：流式仅 Prompt==nil 时推断，非流式双向）——计费相邻面，需独立小轮。
4. **Handoff-B 接线三批**（R55 §九 #4-6）：minheap_topk（孤儿模块实锤，O(N²) 旧路径仍在产）→ chunk_buffer → ctxpool（本轮已先加固竞态）/ring_error_counter/error_detector_ring/prompt_compress。
5. **§11.6 真机 154/245 故障注入验收**（s17 mock 钉桩已过）。
6. **promote 时间型饥饿 gauge 裁决 + lite journal 幽灵轮 view 过滤 + UA 三副本 SSOT + bg ~50 裸 go 盘点**（R55 治理面顺延）。
7. **B6/B11/B13**（Wave3 择机项；B11 用迁移 740）；IR SerializeAnthropic max_tokens=0 直发责任归属；tool_choice auto/none/any 裸字符串（Wave5 顺延，待 8782 实测）；GAP-3 残留单块空 content 字符串（同族）。
8. 观测面小项：IR usage 变体窄集低报 cache read（审计/capture 面）、降级错误信封三种帧形统一、B12 VERIFY 失败补验、per-cred 闸覆盖后台队列执行面、ringCounter uint16 溢出（休眠）、gauge 负值（2026-07 既有）。

## 四、测试与验证（实跑）

- `go build ./...` ✓ / `go vet ./...` ✓（仅 vendor cgo warning，非本仓）
- 触达包全绿：ir 0.4s / ctxpool 0.8s / credentialfpslot 6.3s / bg 19.5s / admin 66.3s / settings 1.5s / maas 1.1s / executors 35.2s / streaming 71.7s / installer 两包 ✓
- 竞态：`go test -race`（子代理实跑）bg/credentialfpslot/dispatch/ctxpool/credentialhealth/executors 全 ok
- 脚本：project_env_test 双 bash 版绿；deploy_local_contract_test 25 PASS RC=0；test-deploy-local-env 8/8
- 迁移 739：本机 dev 库 psql ON_ERROR_STOP 实跑函数创建成功（CREATE FUNCTION ×2）+ pg_proc 列数验证（3/3）+ schema_migrations 自登记；up/down diff 仅倍率列

## 五、部署冒烟

- 本轮未执行 deploy-local 冒烟（8782 当前 active=f7f8f66d 为 Wave1-4 覆盖面；本轮改动触及 installer/迁移/后台 job 语义，建议随下一次例行部署走 deploy-local 全流程并核对 active-version 字面量；739 会在启动迁移通道幂等收敛）。

## 六、下一轮提示词（R57）

```text
承接 R56，轮文档 docs/audit/2026-09-23-r56-48h-audit-round.md §三 遗留登记 1-8 项按序执行：
首项 = B7 virtual_ip 数据源级修复（identity.go 假名 IP → classifyVirtualIP 死分支），
次项 = ProbeSync 信号量顺序 + ProbeConfirm per-cred 闸，再次 = D4 total_tokens 流式推断对称化。
知识库入口：tests/48h-audit/README.md + docs/audit/playbook/conventions.md；窗口 = git log --since="48 hours ago"。
其余按 tests/48h-audit/00-PLAN.md v2 流程跑（子代理只读 + ≤5KB 输出 + 主代理亲读复核 + 三门 + 域报告回写）。
硬约束：子代理线索非事实；R4x handoff『已根修』断言必须多 bash 版本 subshell 实跑；新增迁移前 fetch 核对远端编号（下一可用 740）。
```
