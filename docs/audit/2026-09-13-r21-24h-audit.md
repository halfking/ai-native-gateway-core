# R21 — 24h 修正轮审计（2026-09-13）

- **审计基线**: `197dd2189..087fba395`（R15 收口之后 ~65 个非合并提交，Go 63 文件 +4154 / web 141 文件 +4193 / SQL 11 文件 +1932）
- **审计模式**: codegraph 图谱（136k 节点 / 240k 边，v3.7.0 native）+ 主代理 + 5 个并行只读子代理（streaming/IR 闭环、balance-floor guard 深审、FreeDiscovery/admin 复核、前端四件套门禁、SQL/分区/迁移完整性 + -race）；主代理汇总后系统级修复。
- **门禁终态**: `go build ./...` + `go vet ./...` 零输出；`go test -race`（streaming/ir/logging/bg）全绿；web 四件套 vue-tsc 0 错 / vitest 121 文件 887 用例 0 失败（darwin）/ i18n STRICT / responsive WARN 47 + 0 ERROR。

## 一、结论一句话

24h 内的修正（streaming 4 连修、balance-floor guard 701、FreeDiscovery 修复、前端 step1-8 全系列）主体真实闭环，但审计确认三处"修错了地方/语义分裂"级 P1：90e3bf18a 修复落在死桥、OpenAI→Responses 桥是六桥唯一无空流门的、reset-quota 会把 floor 摘出行打成永久绕过下限的毒态——均已在本轮修复。

## 二、本轮修复（主代理 4 提交 + 子代理 6 提交）

### P1（3 项，全部修复）

| # | 问题 | 根因 | 修复 | 提交 |
|---|---|---|---|---|
| P1-1 | 90e3bf18a 的 orphan input_json_delta 修复落在无任何生产调用方的 transformation 死桥，活 Q3 桥（anthropic_bridge.go）仍把 server_tool_use 等未知块的参数片合成 `{"function":{"name":""}}` tool_calls（客户端 SDK 硬错）且置 emittedContent 以成功收尾 | 死桥与活桥同名函数族，修错了位置 | 孤儿 delta 仅在有 open tool_use 块时缓冲，否则丢弃 + `orphan_input_json_delta_dropped` flag；未知 content_block_start 记录（bounded=8 + 去重 + `unknown_content_block` flag）；空判定不变 | 227d9cc64 |
| P1-2 | OpenAI→Responses 桥是六条桥路唯一没有空流门的：200 + role/usage-only + [DONE] 渲染合法但空的 response.completed 并返回成功，executor 不 failover（与 5b249f31d 修的 Q2 同病） | 该桥当时未纳入空流对称清单 | 两处成功返回点（[DONE] + clean-EOF）在 FlushHoldback 之前加空流门：以 fullText/reasoningText/toolStates 累积语义判定（chunkCount 会被脚手架掩盖），`openai_empty_response` + KindEmptyResponse + Resumable | 227d9cc64 |
| P1-3 | reset-quota 毒态：admin 端点只重置 quota_state 不清 state_reason_code，残留 `ok + balance_floor` 被 guard plan pull 的 Go 条件（`ReasonCode != "balance_floor"`）永久拒绝重摘——token/percent 下限失效，而货币 pass B 纯 SQL 仍摘，两路分裂；与 handoff 遗留 #3 声明直接矛盾 | admin 写入方未纳入 reason 所有权清点 | 双端根除：resetCredentialQuota 补清 reason 字段（对齐 resetAvailability 形态）+ plan pull 删 Go 条件（SQL WHERE `quota_state='ok'` 守卫已足够防覆盖自家已摘行） | 362f1fb42 + 152195f25 |

### P2（6 项，全部修复）

| # | 问题 | 修复 | 提交 |
|---|---|---|---|
| P2-1 | R16 首位遗留 P2-1 未修：ParseOpenAIResponse 对未知 part 留空壳不进 UnknownBlockTypes、refusal 文本丢弃（refusal-only 响应可被误计空成功） | refusal 进 Text（判空器两侧本就按 text-shaped 计数）；未知 part 记录 UnknownBlockTypes（bounded=8 + 去重，与 Anthropic 侧对称） | 36db8b222 |
| P2-2 | 活 Q3 桥 benign-EOF 无容错：minimax 式 finish_reason 后直接关流一律 eof_without_done 白白 failover；992-1024 收尾块因 messageStopReceived 恒 false 成死代码 | finishReason 已积累 + 无 pending tool + 有语义内容 → benign 完成；空流保护不变 | 227d9cc64 |
| P2-3 | candidate_failure_monitor.applyAutoCool 无 balance_floor 守卫，改写 floor 摘出行 reason → probe 豁免失效 → 乒乓复燃 | WHERE 补 `COALESCE(state_reason_code,'') <> 'balance_floor'` | 362f1fb42 |
| P2-4 | diagnostics 版 force-recover 缺 resetInMemoryNodeState（注释声称镜像 applyForceEnable 恢复链），circuit/fpslot 在恢复点击后仍过滤节点 ~5min | 查 provider_id 后补调 resetInMemoryNodeState(ctx, credID, pid, "", true) | 152195f25 |
| P2-5 | force-recover 家族 5 处 actor 恒为常量 "admin"（drawer 路径从不带 X-Admin-User，R16 who 维度在真实 UI 路径失效） | 统一 forceRecoverActor：JWT 上下文（fdActor 同形）→ legacy header → 常量 | 152195f25 |
| P2-6 | force-recover 审计 INSERT 失败静默丢失（nolint:errcheck） | 捕获 error + slog.Warn（保持 best-effort） | 4a2462b7b |

### P3（低风险快修 + 登记）

- **guard sweep 公平性/守卫对齐**（aa75c1966）：currency pass A/C 补 `ORDER BY balance_last_checked_at ASC NULLS FIRST`（>100 条 stale 时不再饥饿）；pass B 补 provider enabled EXISTS 过滤（与 pass C 对齐）；plan pull UPDATE 补 `status='active' AND lifecycle_status='active'`（消 TOCTOU）。
- **701 账本 stamp**（8f462f2c1）：ensureCredentialBalanceFloor 末尾补 `INSERT INTO schema_migrations ('701') ON CONFLICT DO NOTHING`（对齐 693 先例），防未来接入通道时复刻 7bd3bfe6d 双账本漂移。
- **advisory-lock 编号登记 701→702**（7e90544b9）：R16 预留的 701 已被 balance-floor 占用，6 处文档/测试注释改为 ≥702。
- **注释修正**：guard 头注释触发器名 trg_notify_auto_route_refresh → trg_notify_auto_route_creds。
- **web 门禁脚本**（54b60fbb6）：i18n 0-missing 打印 `STRICT PASS (0 missing)` 留痕；responsive-audit 扫描 `@container` 条件（999px 负向验证可抓）。
- **文档标注**（70daa2fa8）：前端 vitest 失败基线 40 仅适用 Linux（jsdom），darwin 实测 0 失败，防误判。

## 三、专项核验（用户检查单覆盖）

- **供应商错误三闭环**：写入点唯一、聚合三重防并发、`: thinking:` 注释帧（ADR-Disp-003）零触碰，24h 改动未破坏（R16 结论维持）。
- **hot+分区**：9 表 hot 8h + 小时 promote 全绿；701 走"3×01-schema baseline + boot ensure"既定交付形态（handoff 明示），全新安装有列、升级轨 ensure 自愈、gateway migrate 兜底。
- **迁移五点同步**：698/699/700 全绿有测试钉桩；701 刻意不走通道（baseline-only），已补账本 stamp 补齐可追溯性。
- **并发/内存**：`go test -race`（streaming/ir/logging/bg）全绿；guard 与 probe 行集不相交 + 写全幂等，无乒乓；advisory lock 全仓无裸 `pg_advisory_lock`。
- **前端**：step1-8 零新增白名单外断点；el-* 24 文件注册修复真实有效（全树 0 未解析）；menu-config 49 路径全量可达；ui/ 复用率持续上升（PageHeader 4→7、DataTable 3→4）。
- **/maintain/ops/autoupdate 菜单重复**：核实为有意设计（运维中心 super-only 入口 vs 接入指南全员入口，eb72d9533 有意迁入 + 测试保护），不处置。

## 四、遗留风险（未在本轮修复）

### P2 级
1. **recordBalanceCheck 伪造货币新鲜度**：webhook/force-probe 链路不经余额查询却敲 `balance_last_checked_at`，guard pass A（15min 新鲜度）最长 15min 内不刷新陈旧 balance_usd。修法需 guard 自持新鲜度或该函数停写时间戳——涉及行为语义，建议随 701 部署验证轮一并做。
2. **全新安装表形状缺口**（R16 P2-2 维持）：534 重构形状 / _hot 表体 / 475/534/579/580 迁移文件本体仍不在 installer 重放集；需单列一轮"全新安装 e2e 重导验证"。
3. **integrity-breach 双终止事件**：committed 时 finishAttempt 写 response.completed 后 renderSurvivalTerminal 再追加 response.failed（cab1a95fb 引入）。修法：interrupted outcome 走 finishInterrupted 或跳过 finishAttempt。
4. **死桥裁决**：transformation 包 StreamAnthropicSSEToOpenAI/StreamAnthropicPassthrough 无生产调用方（仅测试），90e3bf18a 修错的直接温床——建议退役或删除，防再次修错位置。
5. **Q2 FlushHoldback 位于空流判定之前**（anthropic_stream.go 874-900）：当前无害（空流无语义帧→holdback 未开启→flush no-op），但属非局部不变量，建议交换顺序或注释钉扎。

### P3 级
6. 566 credentials_governor_revision 函数族仅 1/3 基线；572/661 update_session_summary 基线体三方不一致（channel 链收敛到 661 无行为风险，基线应对齐）。
7. model_offer_events / request_stats_minute 无 TTL 清理（写入频率低，登记容量基线即可）。
8. partial_errors 前端不消费（后端已回传）；fdStatusFor 子串回退可漂移 500→400。
9. balance-floor guard 部署级验证（handoff 遗留 #6）：本地 8782 跑 701 列生效 + 真实 zhipu percent 下限摘出/恢复观察 + deepseek 货币 pass A/B/C。
10. 本机环境行动项（R15 遗留 #1）：llm-gateway-pg 应用 687（2026-10-01 死线，幂等）。
11. 回归测试缺口：benign-EOF+holdback 组合（245 journal 实际根因形状）、Q2 empty+holdback、活 Q3 holdback 场景。

## 五、测试命令与结果

```bash
go build ./... && go vet ./...                        # 零输出
go test ./domains/streaming/... -count=1              # ok（含 3 个新回归测试）
go test ./internal/ir/ ./bg/ ./admin/ -count=1        # ok（含 1 个新 IR 测试 + guard 公平性契约测试）
go test -race ./domains/streaming/ ./internal/ir/ -count=1  # ok
cd web && npx vue-tsc --noEmit                        # 0 错误
cd web && npx vitest run                              # 121 文件 / 887 用例 0 失败（darwin）
cd web && node scripts/i18n-audit.mjs --strict --missing-only   # STRICT PASS (0 missing)
cd web && node scripts/responsive-audit.mjs --allow-legacy      # WARN 47（存量基线）/ 0 ERROR
```
