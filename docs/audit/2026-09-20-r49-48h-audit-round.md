# R49 48h 审计轮（2026-09-20）

> 审计窗口：95d170645..92f18cf22（R47 留档之后 10 提交，57 文件 +4496/-275）。
> 方法：主代理定界 + 5 子代理并行独立复核（R48 autoroute / 730+727 迁移 / GUC 修复 / 探针 v3 / 排重+deploy），
> 基线实测（go build/test/契约脚本）+ 全部结论以代码证据（文件:行）为准。
> 输入文档：docs/handoff/2026-09-19-session-role-llm-routing-handoff.md、
> docs/probe/2026-09-20-probe-volume-optimization.md、docs/audit/2026-09-20-252-pg-sql-log-audit.md（§六 顺延项继承）。

## §〇、总体结论

本窗口三条主线（R48 会话角色路由 / 探针量优化 v3 / 252 SQL 日志审计轮）的**机械实现质量高于近期均值**：
730 迁移幂等矩阵完整、flag-off 字节等价成立、探针 INV-1/2/4/5 与文档一致且默认 queue 模式闭环、
GUC set_config 根修干净无泄漏。但**交付面再次出现多点同步遗漏**（本窗口 2 个契约门禁基线即红，
是 726 轮"五点同步三犯"的同型第 4/5 犯），且 R48 的"全部完成"表述与实况有差距——
特性当前只在「首轮 + chat completions + 无 DB 覆盖」窄路径真正工作（§一 P1-2/3/4）。

**本轮收口：8×P1 全部处置（7 修复 + 1 结构性豁免），P2 修复 6 项，P3 修复 4 项，其余登记台账。**
自审轮（§六）追加修复 P0×1（S1，F4 视图读面列契约崩坏）+ P2×2。回归：全仓 go build 过；
autoroute/bg/settings/sessionaudit/admin/streaming 全绿；installer 契约与 deploy 契约从红转绿；
新增钉桩 6 个 + 更新 4 处；SQL 变更全部过真库验证（§六 S5）。

## §一、发现与处置

| # | 级别 | 发现（证据摘录见子代理报告） | 处置 |
|---|------|------------------------------|------|
| F1 | **P1** | **迁移 727 漏做 installer 五点同步全部五点**，契约测试 `TestCanonicalStartupMigrationsAtOrAbove704AreRegistered` 基线即红（2aff71420 交付 727 时只跑了 shell 契约没跑 go test）。且**不能机械补齐**：727 全文 `CREATE INDEX CONCURRENTLY`（\gexec），installer applySQL 走 `psql --single-transaction`（dbinit/runner.go:253），照抄 730 五点会把全新安装炸在 727 | **结构性豁免**：契约测试加 `psqlConcurrencyRequired` 豁免名单（带理由，t.Logf 留痕）；727 独占 revision-sequence 通道（psql -f 无事务包裹）。全新安装暂缺三索引（纯性能件），登记 R50 可选做非并发变体 |
| F2 | **P1** | **deploy 契约测试断言漂移**：tests/deploy_blue_green_contract_test.sh:64 钉 `PROBE_TIMEOUT_SECS:-180`，e5879c497 已把默认改 120s，HEAD 必红（改默认值漏改断言，同型第 5 犯） | 修复：断言更新 120s + 补钉 `PROBE_RETRY_TIMEOUT_SECS:-60` 第二窗口有界；契约 30/30 PASS |
| F3 | **P1** | **审批超时清扫首扫永久不收敛风险**：MarkTimeout 单事务全量 UPDATE 无 LIMIT × worker 仅 10s sweep 预算（比文档评估的 30s 更紧）；R41 起 F1-GUC 缺陷使清扫 100% 失败，积压=数周全部过期 pending；首扫跑不完→整体回滚→零进度→60s 后面对同一全集死循环；sweep UPDATE 还会被并发 decide() 的 FOR UPDATE 行锁拖死 | 修复：`FOR UPDATE SKIP LOCKED + LIMIT 500` 分批循环，每批独立事务提交（增量推进）；pgxmock 钉桩单批+多批两用例 |
| F4 | **P1** | **探针 INV-3 名义达标**：`'probe'` quality_flags 只有 direct 轮合成行携带（active_probe_emitter.go:408 全仓唯一写入点），探测的 **gateway 轮**走正常管线（origin_stage='node_probe'、无 flag），被 DailyProbeAudit/featuredCycle/ModelTier/TodaySuccessProbe 四处 usage 扫描当真实使用 → "排除探测流量"未闭环，自我延续只是被 INV-1/INV-5 掩盖 | 修复分两读面（§六 自纠后终态）：物理表三处（featuredCycle/ModelTier/TodaySuccessProbe，均查 request_logs_hot）用双臂谓词 `flags + origin_stage='business'`（真库实证 hot 侧 11.6k 行 node_probe 落值）；**canonical 视图读面（DailyProbeAudit）必须用冻结 113 列契约内标记** `flags + task_type<>'probe_triggered' + origin_actor NOT IN (probe actors)`——origin_stage 不在视图契约，物理谓词对视图必 42703（详见 §六） |
| F5 | **P1** | **R48：admin 给 main 配的 DB 行不可达**：调用点门禁 `roleRoutingOn = flag && roleRoutedRoles[role]`（仅 worker/planner/orchestrator）把 SelectLLM 内部"DB 行对任意角色生效"语义短路，main/unknown 的 DB 行是死数据（handoff §0 声称不成立） | 修复（V1+V2）：门禁改为 `len(rolePrefs)>0`——flag 开启即询 SelectLLM（DB 行任意角色生效；内置表仍仅子代理角色）；钉桩 TestDecide_RoleRouting_MainDBRowReachable |
| F6 | **P1** | **R48：同会话同角色下 TaskKind 跨轮变化不失效 intent 缓存**——首轮"总结一下"缓存轻池后，第二轮"深入分析"复用缓存，kind 路由在 TTL/50hit 内静默失效；工具型会话首轮缓存 TaskAgent 后被 DetectSessionDrift 白名单钉死。245 e2e 三断言全用唯一 session-id（文档复跑要点③）恰好绕开此路径 | 修复（V1+V2）：缓存命中链补 `normalizeTaskKind(cached.Kind) != normalizeTaskKind(ClassifyTaskKind(sigs))` 失效臂（纯函数微秒级）；钉桩 TestDecide_RoleRouting_KindChangeInvalidatesCache |
| F7 | **P1** | **R48：/v1/messages 与 /v1/responses 协议面不解析角色头**——sigs.AgentRole 恒空，Anthropic/Responses 客户端的角色路由静默失效且审计字段写 unknown 误导排障；跨协议共用 session_id 时 role 缓存互相打失效 | 修复：两个 non-chat 提取点补 `ResolveAgentRoleFromHeaders`（与 chat 路径同款两行） |
| F8 | **P1** | **cleanup 脚本 FK CASCADE 三表盲区**：model_capability_profiles / model_capability_profile_audit / model_substitution_overrides 对 models_canonical 是 `ON DELETE CASCADE`（342 迁移），20 表备份清单与 2.3 重定向均未覆盖 → §7 DELETE loser 静默级联删能力画像/替换策略且无备份 | 加固：三表入备份清单 + 2.3 补重定向（含 substitution 双列）+ 新增 V8 悬挂校验（非 0 禁跑 §7）。**实测 252 与本地库均无 342 表（迁移从未应用）→ 级联删未实际发生**，风险为潜在结构性 |
| F9 | P2 | R48：V1 tier 豁免漏 pins（V2 为 append(pins, rolePrefs...)）→ V1 下 admin pin 不在 tier 配置内被硬过滤、PromotePins 空转 | 修复：V1 对齐 V2 |
| F10 | P2 | R48：V2 role_route 判定基准是 tier 前 winner → tier 滤除自然头名、role 偏好经豁免原地升头名时功劳误记 role_route，V1/V2 审计标签分叉 | 修复：基准改 tier 后 winner（postTierWinner，与 V1 preRoleWinner 同款）；三模型场景钉桩 TestDecideV2_RoleRoute_CreditStaysWithTier |
| F11 | P2 | **timeout_action 旋钮未接线**：WithTimeoutAction 无生产调用点，settings_kv `session_audit.timeout_action` 到不了 MarkTimeout（文档"252 当前=reject"实为未接线巧合；改 auto_approve 会静默无效） | 修复：main.go 接线（auto_approve→Approve + Warn 留痕；deny/escalate/未知值/未就绪一律 reject fail-safe） |
| F12 | P2 | task_kind 词表窄：planning 无 "plan" 裸词、solution enWords=nil，重量池两通道物化命名漏报落 unknown→轻池，违背文件头"重任务误判轻池代价更高"优先级 | 修复：planning 补 "plan"+中文"计划"裸词；solution 补 "solution"（词边界不误伤；运维语境 ops 通道先行命中） |
| F13 | P2 | **provider_vendor.enrollCredentialModels 盲插源未堵**：92f18cf22 只修了 provider/client.go，凭据探测成功路径仍会回种 "opus-5" 类截断行（cleanup 清完即回种） | 修复：内联同款 junk-seed 守卫（fail-closed，与 client.go 同谓词+交叉引用）；TODO(R50) 三处归一器收敛 modelname 包 |
| F14 | P2 | R48：role 命中后 TierFailoverModels 不含选中模型 → pre-first-byte 恢复链跳过 role winner 的其它凭据绑定，违反 "starts with the selected model" 契约 | 登记台账 R50（修复需与 handler.go 重试序列联调，单行改动面不确定） |
| F15 | P2 | 730 在分区父表 sessions 上两条**非 CONCURRENT** 索引（SHARE 锁写停顿），与本窗口 727 自定的三段式标准冲突；且 sessions 三列当前无 Go 写入方（两条部分索引是空索引预支成本） | 登记台账 R50（与 sessions 落库路径同轮做：写入方 + 索引改造） |
| F16 | P2 | 730 down 的 tier 还原无列守卫（up 有 DO 块双列守卫）→ 252 形态库 down 必炸 42703 | 登记台账（down 未执行过；修补须开 731 或专项——730 已应用，就地修订违反 revision-sequence 纪律） |
| F17 | P2 | role_task_llm_mapping 二进制 1 分钟轮询依赖但 ApplyMigrations 主通道零 ensure → 缺表环境每分钟 42P01 Warn + DB 覆盖静默失效 | 登记台账 R50（对齐 724 ensureTaskTypeCorrections 先例） |
| F18 | P2 | 探针 INV-5 错误证据可被探测自身失败滚动刷新（candidate_failure_logs 无 probe 归因）；停放行"检出延迟=下一笔真实请求"的权衡文档未明示；admin 泳道不再显示停放行 | 登记台账 R50（归因注入 + 文档权衡条目 + parked-since 徽标三选一或全做） |
| F19 | P2 | admin/models.go 归一化查重 SELECT-then-INSERT 并发窗口无 DB 兜底 + 不折叠连续分隔符（与 NormalizeRouteKey/V1 校验三方口径不一） | 登记台账 R50（归一化表达式唯一索引 + modelname 包收敛，与 F13 TODO 合并专项） |
| F20 | P2 | 探针 v3 测试全为 SQL 字符串钉桩：INV-2 ON CONFLICT 行为、INV-4 门数据语义、credential_probe_v2 新臂、model_tier 排除零运行时验证 | 登记台账 R50（pgxmock/容器行为测试补齐） |
| F21 | P2 | deploy-local --cache-from 引用正在创建的同版本 tag（对新版本 no-op），6f5a2405f 提交信息的加速宣称主要来自 Go 缓存 bind（该部分正确） | 登记台账（口径修正或固定 cache tag，低优先） |
| F22 | P3 | 杂项 8 条：refresher 无 panic recover（存量惯例）；IncrementHit→Invalidate Redis 复活竞态（自愈）；routing_source 注释前提不实/flag-on 透出面窄；730 pg_get_constraintdef 文本匹配（列名实为 nulls_not_distinct）；727 ATTACH 守卫不查 indisvalid；727/730 零契约测试（726 有 C1-C4 模板）；"25 用例"实为 24；freequotareset 注释 rot（SET LOCAL 已改 set_config）；GUC 三种写法并存未收敛；readOnly 形参被无视；dedup 脚本 UNIQUE 冲突预检缺/':variant' 归并与 V2 矛盾；bump-version python3 失败回落 0；readyz 最坏链 300s 口径 | 登记 §三 台账（按性价比排序，多为注释/守卫/一致性小改） |

## §二、修复清单（本轮提交）

| 文件 | 变更 |
|------|------|
| installer/cmd/llm-gw-installer/stats_migrations_test.go | `psqlConcurrencyRequired` 豁免名单（F1，727 通道豁免+理由留痕） |
| tests/deploy_blue_green_contract_test.sh | 120s 断言 + retry 60s 补钉（F2） |
| domains/sessionaudit/approval_manager.go | MarkTimeout → FOR UPDATE SKIP LOCKED + LIMIT 500 分批循环（F3） |
| domains/sessionaudit/approval_manager_test.go | 单批断言更新 + TestMarkTimeout_MultiBatch（F3 钉桩） |
| bg/probe_policy.go + bg/{daily_probe_audit,model_probe,model_tier,today_success_probe}.go + bg/probe_policy_test.go | INV-3 双读面谓词（物理表双臂 origin_stage / 冻结视图三臂 origin_actor，见 §六 S1）；读面守卫钉桩（F4 + S1） |
| autoroute/decision.go + decision_v2.go | 门禁=SelectLLM 返回值（F5）；kind 变化失效缓存（F6）；V1 tier 豁免并 pins（F9）；V2 role_route 基准改 tier 后（F10） |
| autoroute/decision_role_wiring_test.go | 三个 R49 回归钉桩（F5/F6/F10） |
| domains/streaming/auto_route_nonchat.go + auto_route_e2e_test.go | messages/responses 两协议面角色头解析（F7）+ 双协议面接线钉桩（S3） |
| autoroute/task_kind.go | planning/solution 词表补裸词（F12） |
| cmd/gateway/main.go | timeout_action 接线（F11；spec 注册在案 TypeEnum deny/escalate/auto_approve，映射已核对） |
| admin/provider_vendor.go | enrollCredentialModels junk-seed 守卫（F13，fail-closed） |
| sql/fixes/2026-09-20-canonical-dedup-cleanup.sql | CASCADE 三表备份+重定向 + role_task_llm_mapping 重定向 + V5 补 gpt-5.6-sol + V8（to_regclass 守卫版，S2）（F8） |

## §三、遗留台账（顺延 R50；继承 252 SQL 审计轮 §六 1-5 与 R45 §六纪律）

**代码侧（按优先级）**：
1. F14 role winner 进 failover 恢复链；F15 sessions 三列写入方+730 索引三段式化（同轮）；F17 role_task_llm_mapping 主通道 ensure。
2. F19+F13-TODO 归一化收敛专项：modelname 包单一实现 + 表达式唯一索引 + admin/client/enroll 三调用点统一。
3. F18 探针：candidate_failure_logs origin 归因 + 停放行观测面（parked-since）+ 文档权衡条目。
4. F20 探针行为级测试；F16 730 down 守卫（开 731 或专项）；F21 cache-from 口径。
5. F22 P3 杂项按性价比清扫（注释 rot/indisvalid 守卫/727+730 契约测试按 726 模板/镜像测试钉 730 种子↔builtinRoleLLMPreference）。

**运维侧**：F1 修复上线观察（approval_queue 状态迁移量；分批后 sweep 不再一次性清算，存量积压按 500/分钟消化）；AUTO_ROLE_ROUTING_ENABLED 灰度扩大前先补 non-chat e2e + 多轮会话 e2e（本轮 F6/F7 修复的实盘验证）。

## §四、测试

- `go build ./...` 全仓过（macOS 本机）。
- `go test ./autoroute/... ./bg/... ./settings/... ./domains/sessionaudit/... ./admin/... ./domains/streaming/...` 全绿。
- installer 模块 `go test ./...` 全绿（F1 契约红→绿）；`bash tests/deploy_blue_green_contract_test.sh` 30/30 PASS（F2 红→绿）。
- 新增钉桩 6 个 + 更新既有钉桩 4 处：TestMarkTimeout_MultiBatch（新）、TestDecide_RoleRouting_MainDBRowReachable（新）、TestDecide_RoleRouting_KindChangeInvalidatesCache（新）、TestDecideV2_RoleRoute_CreditStaysWithTier（新）、TestProbeUsageViewScanUsesFrozenContractPredicate（新，读面守卫）、TestAutoRouteE2E_NonChat_AgentRoleHeaderParsed（新，双子测）；INV-3 双谓词形状钉桩、featuredCycle 源钉桩、TestMarkTimeout、Sprintf 调用点（更新）。
- **真库验证（§六 自审补课）**：① MarkTimeout CTE（SKIP LOCKED+LIMIT）真库 UPDATE 2/回滚零残留；② 视图三臂谓词真库跑通（35758 行、无 42703）；③ hot 双臂谓词实证（origin_stage 落值 11.6k node_probe 行）；④ 加固后 cleanup 脚本全量零持久化演练（外层事务 ROLLBACK，V8=0，无 ERROR）；⑤ 342 迁移三表列名逐一核对。
- 明确未覆盖：F4 修复的真实库行为（需 252 日志观察 probe 占比回落）；F14 未修故无测试。

## §五、纪律注记（本轮新证据）

1. **契约门禁必须在交付同轮实跑**：本窗口两个 P1 都是"改了 A 忘了钉 A 的测试"（727 五点、180→120），
   且提交信息各自声称"门禁收口/follow-up"——commit message 声明不等于验证，`go test ./...` 须含 installer 子模块。
2. **"机械五点同步"对新迁移不再安全**：CONCURRENTLY 类迁移与 installer single-transaction 通道互斥，
   五点同步前先判迁移的 SQL 事务兼容性（豁免名单机制已建立）。
3. **e2e 断言设计避免自证**：R48 三断言用唯一 session-id 绕开了缓存路径，恰好漏过 F6——
   关键行为断言须覆盖"跨轮/跨协议同会话"形态。

## §六、自审轮（2026-09-20，交付 a7baa1f5a 的批判式复审）

**根因（自省）**：上一轮把继承纪律⑦"PG 语义类修复必须真库测试（pgxmock 测不出）"写进了台账，
自己交付的 SQL 却全部只过了 pgxmock/字符串钉桩——审计者的修复同样需要被审计。自审抓到并修复：

| # | 级别 | 发现 | 根因 | 处置 |
|---|------|------|------|------|
| S1 | **P0** | **F4 修复在视图读面必炸**：`probeTrafficExclusionPredicate` 引用 `rl.origin_stage`，而 DailyProbeAudit 查的是 canonical 视图 `request_logs_with_current_month`——该视图是 113 列冻结契约（710 会话族拼装体），origin_stage 既不在 projectionExprsV2 也不在 canonicalColumnOrderV2，预存 frozen v1 链亦无此列；真库实测本地与 252 视图均 0 列。上一版提交上线即 DailyProbeAudit 每周期 42703（R44 providers.name 42703 同款，恰是本轮审计文档批评过的缺陷类别） | 修复时未验证读面所引列在目标对象上的存在性——字符串钉桩测试只锁文本形状，锁不住列契约 | 视图读面改用 113 契约内三臂（flags/task_type/origin_actor，与镜像子系统探针判定同口径，actor 全集真库枚举）；物理表三处保留双臂；新增读面守卫测试钉死"视图源必须用视图谓词" |
| S2 | P2 | cleanup 脚本 V8 校验无条件引用三张 CASCADE 表——252/本地均无 342 表，操作员任何环境跑校验段即 42P01 | 写守卫段时未对齐被守卫对象的部署实况（§一 F8 自己查明的"两库均无 342 表"） | V8 改 DO 块逐表 to_regclass 守卫 |
| S3 | P2 | F7 修复只有接线无测试（"声明完成"） | 测试可行性未调查就归档 | 复用 auto_route_e2e_test harness 补双协议面接线钉桩（修复前必红形态） |
| S4 | P3 | 审计文档"回归钉桩 8 个"计数失实（实为 4 新增+3 更新） | 求全心理 | 本节更正为 6 新增+4 更新（含自审轮 2 个） |
| S5 | — | 以下经真库验证**确认无问题**（此前只是"应该没问题"）：MarkTimeout CTE 语义（UPDATE 2/回滚零残留）；timeout_action spec 注册在案（TypeEnum deny/escalate/auto_approve，接线映射正确）；mv_consistency 的 origin_stage 引用走 routing_analytics_source（实表有列，非视图，无恙）；342 三表列名与重定向 SQL 逐一相符 | — | 留证于本节 |

**方法论沉淀**：凡 SQL 变更，交付前必须过真库三件套——列存在性（对视图查 information_schema，不是对基表）、
语义演练（外层 BEGIN+ROLLBACK 零持久化）、幂等重放。字符串钉桩测试只是格式锁，不是存在性锁。

**S1 复演实证（S1b，同日）**：并行会话的 R50 轮（d4d54520c 一系）在扩铺排除谓词时把物理双臂谓词
铺到了 shared_pick.go（1 处）与 passive_probe_listener.go（3 处）的 **canonical 视图查询**上——与 S1
完全同款（视图无 origin_stage 列，必 42703）。本自审轮在 rebase 冲突核查时抓到并修复：四处切换
冻结视图谓词；R50 调用面闭环测试的不变量从"都用物理谓词"更正为"按读面分治"（物理表站点钉物理
谓词、视图站点钉视图谓词，7 文件 9+5 处全量钉桩）；shared_pick 查询形状真库复跑通过。
**结论：视图列契约陷阱是系统性的**——两个独立会话先后踩中同一坑，佐证"读面对象列存在性验证"
必须成为交付门禁而非个人经验；读面守卫测试（TestProbeUsageViewScanUsesFrozenContractPredicate
+ 更正后的 TestProbeExclusionPredicateCallSitesR50）已把该门禁机制化。
