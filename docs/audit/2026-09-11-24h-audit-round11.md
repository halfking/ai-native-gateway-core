# 2026-09-11 24h 审计（第十一轮 — R11）

范围：基线 `e3738133af..4fd34bc00`（2026-09-10 05:08 → 2026-09-11 05:07，
40+ 提交，104 文件 +6600/-1310）。含 R9 五项后续修复（候选
6/10/11/17/19）的落地本身、zstd 存储批次、供应商模型匹配链四连发
（78caf3fac→fe6407647 + 迁移 693）、候选 18 后端半、sessionv2mirror
异步化、auto_index_refresher ON CONFLICT。

方法：codegraph 索引重建（135562 节点 / 238010 边）+ 主代理串行深审。
原计划 6 子代理并行，因子代理模型 provider 全故障（`No available
provider` 探测确认）降级为主代理按 A–E 五路逐区串行，全部结论基于实读
代码。前置：ff-merge 吸收 fe6407647 时 vue-tsc 门禁红（fe6407647 自带
的类型缺陷），合并修复 `4fd34bc00`（canonical_name null→undefined，
语义：null=未绑定，消费方 `|| standardized_name` 兜底不变）。

## 结论速览

发现 **P1×1、P2×1、P3×4**，本轮修复并提交 2 个 commit（P1/P2/P3-doc）。
全量 `go build` PASS、`go vet` clean、`go test ./bg ./domains/dispatch`
PASS、dispatch `-race` PASS、vue-tsc PASS（4fd34bc00 时已绿）。

| 级别 | 发现 | 处置 |
|---|---|---|
| **P1** | bg/routing_health_checks 绕过 693 解绑标记（F1） | ✅ ec54ea7a7 |
| **P2** | dispatch DimensionIndex.Complete 锁外读 journal 尾（F2） | ✅ ec54ea7a7 |
| P3 | docs/design 两份 lite-mode 文档残留 storage/memory 旧路径（F3） | ✅ 本轮 docs 提交 |
| P3 | deploy 与 installer 两份 01-schema.sql 差 ~1KB 行尾空白（语义等价） | 留档：下次 baseline 再生时统一 |
| P3 | attachments nosniff 为路由局部，提交信息"全局"言过其实（用户内容出口已覆盖，风险闭环） | 留档不改 |
| P3 | admin clear_canonical 失败仅 slog.Warn 后仍 200（fail-open）；forwarder panic recover 后 depth gauge 永久 +1 | 留档：观测类，低频 |

## F1（P1）：routing_health_checks 复活管理员解绑

迁移 693 把"解绑持久化"收口在 UpsertCredentialModelSQL 单点，但
`bg/routing_health_checks.go` 有两条独立 UPDATE 直写
provider_models.canonical_id：`autoFixCanonicalID`（exact-match 自动修复）
与 `canonical_id_null` 检查的 fixSQL。管理员解绑后行恰好处于
`canonical_id IS NULL AND raw_model_name = canonical_name` 的命中形态，
下一轮 RunChecks 即重新标 critical 并静默复活绑定——693 在此路径失效。
修复：两处 SQL 均补 `AND pm.canonical_cleared_at IS NULL`；新增
`bg/routing_health_checks_sql_test.go` 形状回归（检查注册表 + 内联
auto-fix 源码双断言，无 PG CI 可挡）。

其余 693 生命周期逐点核实无漏：admin PATCH clear/relink 双分支（含
binding-locator join 修复）、InsertManualCredentialModel ON CONFLICT
抬标记、suggestions 抑制（cleared→suggested_id=0 + canonical_cleared
透出）、provider_models.canonical_id 写入方全仓枚举（仅 modelcatalog 两
SQL + admin + bg 四处，均已覆盖）、installer 5 点同步完整、
`model_offers.id IS credential_model_bindings.id` 论断与视图定义逐列
核对成立。

## F2（P2）：journal 锁外尾读

R8 P0 修复给 recordDecision/JournalSnapshot 加了 journalMu，但
`dimension_index.go` Complete 的终态回填守卫仍裸读
`len(qr.AttemptJournal)` + 尾索引——与并发 recordDecision 的
append/环形平移构成数据竞争（Go 内存模型意义 UB；现实后果是终态误判→
冗余回填，现被 recordDecision 终态守卫兜住）。修复：新增
`QueuedRequest.journalTailIsTerminal()` 锁内尾读；顺带修
`planner.decisionHistoryOf` 的"裸 len + 再取快照"双读不一致（改为单次
快照）。全仓锁外访问点枚举后归零。

## 各路核实摘要（已验证无问题清单）

**匹配链质量（A 路）**：modelname/match.go 打分核心护栏齐备——
digit-guard 拒代际误配、containmentCap=0.84 压在 AutoLinkThreshold=0.85
之下防 "-thinking" 变体折叠、fuzzCap 防模糊通道越位、双 demote 规则解
"垃圾基行 vs 拼接匹配"排序冲突、同分 tie-break 确定序；Q3 续写切片
`validated[len(start):]` 安全性成立（valid 快路径原样返回、repair 路径
为纯后缀追加，前缀逐字节不变）；maintainMatchedCanonical 单条 guarded
UPDATE 与 ON CONFLICT 三分支逐字对齐（family tag 回填/拆分归一/
sticky-modality），WHERE 只收养需变更行，健康行零写放大；
EnsureCanonicalAndAliases 仅 admin 单模型手工路径调用，每次整目录加载
可接受。candidate_failure 三读出端（list/by-credential/vendor-errors）
upstream_response_preview 脱敏一致，写入端 500 字符截断在前。

**zstd 存储链（B 路）**：文件侧 magic 即后缀、读路径配置优先+全编码
兜底、Delete/List/ModTime 双格式处理齐备、路径遍历守卫不变；Redis
artifact 逐条 encoding 标记分发解码，蓝绿共存窗口安全；默认 gzip
（ApplyLiteDefaults）保证回滚可读，zstd 为显式 opt-in；crypto.go 的
gzipBytes 由"失败静默回退明文字节却标 gzip"改为显式 error（消除了
读侧 gunzip 必炸的隐性炸弹）；bodies_trimmer 按会话目录整删对编码无感。
promote 函数体核实为原子 CTE + 显式列 + DEFAULT 8h，两份 baseline 内容
语义一致（仅行尾空白漂移）。

**dispatch/队列（C 路）**：take-once 二次准入修复枚举
park/onScheduledDue/complete 全路径无新泄漏；journalMu 后全仓
AttemptJournal 访问点归零（本轮 F2 补完最后两处）；forwarder
dispatchOne recover 的 gov nil 语义正确（acquire 失败不双释放）；
bandit LiveLoad（候选6）、tool_usage_stats 双模式（候选11）、
midstream thinking 帧（候选17）落地与 R9 报告一致。auto 模型链
（model="auto"→分类→6维评分→autoroute 索引→改写+X-Gw-Is-Auto）24h
唯一改动 auto_index_refresher 的 ON CONFLICT 修复论证成立（DISTINCT ON
尾部保证单语句无重复冲突键，21000 不可复发）。

**IR/流式/镜像（D 路）**：IR buildOpenAIResponseContent 去除 content
数组内无 input 的 tool_use 冗余块（R8 P2）后无新破坏；Q2 thinking
块序/闭合与 spec 测试在位；sessionv2mirror 异步化有界（信号量 8 槽 +
有界 backlog FIFO 逐出 + 失败计数 + Grafana 告警面），releaseBodies
竞态经 deep-parsed 快照隔离，20 槽预算内不阻塞 telemetry worker。

**bg/安全/文档（E 路）**：probe/HC 脱敏 choke 双侧在位（写入
SanitizeErrorText + 读出兜历史行）；auto_index ON CONFLICT 修复；
canary BOOT_RETRY 75s < systemd 90s 语义不变。文档侧 R10b §2.1 验收：
lite-mode*.md 残留 storage/memory 引用清零（checklist 以勘误注记保留
计划原貌标题）；R10b RawDataLogger 维持方案 C（保留现状）复核无误；
R9 报告候选 18 的进度标注与实际一致（后端脱敏已做，/api/errors/trend
UI 仍未接入）。

## 遗留与后续（转 R12 候选）

1. R9 未竟批次不变：候选 4（fallback 转换器收敛）、7（Limiter
   recoveryStep 拉平热更新上限）、8/9（慢性增长/race flaky 观测）、
   12–16（lite TTL、default 缝隙巡检、promote 预算份额、TimeZone、
   注释漂移）、20–23（孤儿路由/datetime 双轨/alert 残留/死代码清理）。
2. 本轮新增 P3：baseline 空白统一、clear_canonical fail-open 改显式
   失败响应（需前端联动）、forwarder depth gauge panic 漂移。
3. 流程性：本轮子代理通道故障期间采用主代理串行审计，后续轮次启动前
   先用一个探测 agent 验证 provider 可用性，故障时直接串行不再重试。

## 验证

- `go build ./...` PASS；`go vet ./bg ./domains/dispatch` clean
- `go test ./bg ./domains/dispatch -count=1` PASS；
  `domains/dispatch -race` PASS
- 新增 `TestCanonicalIDNullCheckQueryExcludesAdminUnbound`、
  `TestAutoFixCanonicalIDExcludesAdminUnbound` PASS
- web：vue-tsc PASS（4fd34bc00）；本轮无 web 代码改动
