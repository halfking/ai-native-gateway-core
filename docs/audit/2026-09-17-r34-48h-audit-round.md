# R34 — 48h 横向审计轮（2026-09-17）

- 窗口：1cacc62cb^（09-15 00:05）→ 643735a28（09-17 01:18），101 个非合并提交、212 文件（+23618/-1417）——即 R30 收口后未被全量横向扫过的完整 48h
- 方法：codegraph 全量图谱重建（138k 节点/243k 边）+ 主代理 + 6 路子代理并行审计（incident 复核 / R31-32 修复复核 / IR+流式 / 存储分区迁移链 / 并发+auto / UI+遗留），每条 P0/P1 发现经主代理亲读复核后才动手
- 与并行 R33 轮（b9d9a8ba5，同 workspace 同时段）对账：R33 已修 F1 outcome 回填（采 e8ddff41d）、routeincident lockActive 补 pending（P0，真库红绿）、715 双通道登记、ops 改密脚本加固；本轮不重复，R33 §五遗留为本轮起点之一

## 一、本批修复（19 项，P1×5 / P2×9 / P3×5，提交 01bd55ed7 + efc8b5268）

### P1

| # | 发现 | 处置 |
|---|------|------|
| P1-1 | breaker 指数退避溢出：`InitialCooling·2^(cycle-1)` 在 cycle≥30 后 int64 环绕/float 饱和为负，负冷却绕过 `>MaxCooling` 钳位 → coolingExpires 落在过去，熔断停止保护（免费档 15s 起步约 2.1h 持续故障可达；RateLimit 族更快）。01350f089 把同一未钳公式复制进升级分支 | 新增 `exponentialCooling()`（float 域计算、转换前钳位），两处调用点替换 |
| P1-2 | breaker 跨错误族 coolingCycle 污染：仅"新族 policy 为 Exponential"才重置，upstream_down(cycle3)→timeout 后升级首步即 2³× | 换族无条件重置 |
| P1-3 | licensing 席位冲突 409 死分支（47e54f90b 自带缺陷）：DeviceManager 席位满返回 `(Success:false,NeedDeactivate:true,nil err)`，`errors.Is(err,ErrDeviceLimitExceeded)` 恒 false → 实回 500 | activate-quick 改判 `resp.NeedDeactivate/ErrorCode` |
| P1-4 | licensing handleActivate 本地分支：不查 `resp.Success`（席位满也返回 200"已激活"+ 启动 center agent）且无本机指纹校验 —— R30 L-1 只堵了 activate-quick，同组无认证 /activate 仍可被同网者拿 instance_id 占掉唯一席位 | 补 Success/NeedDeactivate→409 契约 + FREE- 前缀 license 的指纹校验（fail toward 403） |
| P1-5 | Gemini 方向四连（同根：测试全用网关自产 fixture）：① `thought` 按 string 建模，真实 wire 是 bool 标记 → 真实流量 json.Unmarshal 整请求 400/响应解析失败（三处解析点全中）；② 响应侧 tool_use id 合成 `<idx>_<name>` 与请求侧 `<name>_<idx>` 互不兼容 → 工具循环第二轮 functionResponse.name 错误（Gemini 400）；③ 响应序列化器漏 refusal（692205664 修三漏一，Gemini 客户端 refusal-only 仍空壳）；④ 出向 thought 以 string 形态发出（真实客户端不识别） | 三处解析改 RawMessage+`geminiThoughtMarker` 双形态解码；响应侧 id 统一 `<name>_<idx>` + `toolUseNameFromID` 兼容旧形态（在途会话）；refusal case 补齐；三处序列化改 `{"text":…,"thought":true}`；回归测试×5（gemini_r34_regression_test.go） |

### P2

| # | 发现 | 处置 |
|---|------|------|
| P2-1 | responses 桥终态闩锁缺口：writeFinalEvents 无返回值、finish 两路径无条件 MarkTerminalRendered —— 写失败时吞掉 coordinator 兜底终态（18933bc6c 只修了 chat/anthropic tail） | writeFinalEvents 返回 error，仅写成功闩锁 |
| P2-2 | openai 内容数组 tool_use 双丢：parse 只进 Content，openai-chat/responses 序列化器只从 ToolCalls 发射（Qwen/DashScope 形态全丢且无 loss 上报） | parse 双面登记，序列化器按 ID 去重 |
| P2-3 | credentialstate.UpdateFromProbe 持久化不对称（bba08b922 只修了成败两路）：探测恢复只写内存+Redis，Redis 过期后 DB available=false 复活凭据 | probe 结果补 batchWriter.Add（nil 防护）；**零读者问题见遗留#1** |
| P2-4 | affinity EMA 读错误吞为 prevEMA=0：UpdateEMA 0.85 权重落零 → 学习史静默清零且无条件覆盖写 | ErrNoRows 之外 skip 该桶 |
| P2-5 | scan_scheduler sweep 预算截断计入 consecutive_scan_failures → 慢而健康的尾部模板 3 次即被误自动禁用 | ctx.Err()/DeadlineExceeded/Canceled 不计失败 |
| P2-6 | session_mirror_outbox requeue/markDead 无 `status='claimed'` 守卫：租约过期孤儿回收后，慢 worker 终态写可把 dead 复活成 pending（dead-letter 契约失真） | 两处 UPDATE 补守卫 |
| P2-7 | installer 迁移通道再次断裂（R30 遗留#8 扩大）：704/705/709/710 漂移在 installer 外，714/715 落地时又未同步；714 无任何 Go ensure 镜像，installer 是全新安装唯一交付通道 | 6 迁移五点同步回填（embeddata 副本/go:embed/embeddedSQLFiles/StartupFiles/parity map）；新增 `TestCanonicalStartupMigrationsAtOrAbove704AreRegistered` 守卫（≥704 canonical 必须注册——**本守卫首战即中**：合并 origin 带入的 716 也漏登记，见 efc8b5268） |
| P2-8 | distlock Acquire leader 路径 SetNX 裸用 sweep ctx（follower 有 3s 上限），Redis 挂起吃 sweep 预算 | 补 3s 独立超时 |
| P2-9 | model_availability_backfill 预算截断全静默（2cead66dd 去噪副作用） | 截断时单条 WARN（written/deferred） |

### P3

- routeincident recover 动作靶态白名单收敛：剔除 CHECK 不认的 `closed`（executor 今天不写列，属休眠 23514）
- admin route-incidents 列表默认隐藏 pending（低于阈值的记账行不是仪表盘实体）；`visible=0` 回全量
- SetDistLock 文档改 "MUST be called before Start()"（原 "safe after Start" 承诺了不存在的同步边）
- web `/admin/usage` 补 `requiresSuper` meta（相邻 admin 路由全有，唯它裸奔）
- web routeIncident 类型补 `pending`；FreeDiscoveryView 补 auto_disabled_at/consecutive_scan_failures/last_scan_failure_at 三字段 + 禁用悬停提示（兑现 99e77a587 的 UI 承诺，R30 遗留#3 的具体缺口）

## 二、核实为健康（子代理亲读+测试实跑，未动手）

- R31 三修/R32 两修/8373a6775 keyring 锁/S3 波1 读端两态等价：复核属实（与 R33 结论一致）
- incident 修复 6f3d03073 本体：ensure 接线位置正确（先于 HTTP/observer）、stamp 一致、集成测试矩阵扎实；bba08b922 的 lockActive 漏改由 R33 修复（本轮复核其修法正确、集成测试在位）
- toolexecution hot-only 写链、sessionv2mirror 双执行幂等封闭（v2.Write ON CONFLICT + aggregate 认领）、supplier_errors TTL90d 双侧一致、scan_scheduler RLS 旁路全覆盖（无裸写 provider_templates 残留）
- 流式：attempt_outcome 并发保护、survival 换供应商防内容串 attempt、anthropic_stream body 关闭、O5 fallback——全健康
- IR：Anthropic thinking+signature 全链路保真、A-#17/18 载体统一、generateToolCallID 修复正确、paramreg 上报前提成立
- ops 改密脚本无密钥泄漏（密码走未跟踪文件）
- hostedtask 状态机白名单/CAS、acc_client HashRequest 确定性、breakers claimProbe 持锁

## 三、测试与验证

```
go build ./... && go vet（九个触碰包）                       # clean
go test credential/licensing/credentialstate/routeincident/sessionv2mirror/admin/ir/streaming/bg -count=1   # 全绿
go test bg streaming -race                                   # 全绿
installer 模块独立构建 + TestCanonicalStartupMigrationsAtOrAbove704AreRegistered + parity 全绿
bash scripts/apply-db-revision-sequence_test.sh              # passed（716 补登记后）
web: vue-tsc --noEmit 无错；i18n 三棘轮（parity/keys_referenced/hardcoded_cjk）12 用例绿
Gemini 回归 gemini_r34_regression_test.go ×5 + 旧 thought 钉桩按新 wire 契约改写
```

## 四、遗留登记（本轮新登记，均经亲读确认）

1. **P2 | credential_state_log 零读者**（承 R33 §五）：本轮已补探测路径持久化对称（P2-3），但重启恢复链 GetState 仍走 mem→Redis→node_probe_state，不读 credential_state_log——"重启丢冷却"症状实质未解。需决策：GetState L3 兜底读该表（latest-per-key）或改写权威表；附带 recover_at/last_error 的 COALESCE 永不清空语义需一并定
2. **P1（大工程）| 01-schema.sql 列型漂移致全新安装整体不可用**：hot 侧 12 列（protocol_conversion/response_body/ir_extensions/sanitizer_mutations/dlp_violations/customer_id/tenant_id/agent_name/agent_type/api_key_fingerprint/task_id/content_safety_score）与分区母表类型族不一致，ensureRequestLogsCurrentMonthView 的 UNION 报 42804，启动链中止（6f3d03073 集成测试显式 SKIP；252 生产 hot 侧同漂移）。修复路径（已评估）：迁移 ALTER request_logs_hot 12 列（hot 仅 8h 数据，秒级）+ 重生成三份 baseline 快照 + 现有守卫测试从"数列数"升级为"校列型"；customer_id 可保留既有摘除特判。**建议下一轮首位**
3. **P2 | hosted_task_reconciler tick 无整体预算**：ACC 黑洞时 50×30s 串行最坏 ~25min，ClaimExpiredTasks/DeliverDueCallbacks 被饿死——建议每 pass 套 30-60s 预算或 dispatch 有界并发
4. **P2 | call_history_aggregator**：insertBatch 非事务（失败重试落新桶双计）+ 双实例无 distlock（settle/affinity/metrics 已接，它没接）——每 entry 每分钟双计
5. **P2 | acc_client SSE 无读空闲超时**：半开连接 ReadString 永久阻塞，50 个 stream handle 可被僵尸占满
6. **P3 | worker panic guard 在 run 外层**：settle/affinity/metrics 一次 sweep panic 后本进程内永久停摆（对比 balance_floor/scan_scheduler 的 per-cycle safeCycle）——recover 移进 sweep
7. **P3 | keyring 无锁相邻缝隙**（承 R33）：discovery/discovery.go:69/85、bg/credential_probe_v2.go:126/476
8. **P3 | O4 超时链解析不一致**：classifier strconv.Atoi vs caller Sscanf %f，且 caller 的 client Timeout 无 ≤30s 钳位（当前唯一调用路径经 classifier 故钳位有效）
9. **P3 | reducer forgetSeen 复活键可被容量驱逐误删新映射**（低概率；EffectRecordCircuitFailure 多计 1）
10. **P3 | licensing fail-open 与信息暴露**：指纹不可用时跳过校验（叠加 /status 无认证返回 hardware_hash）——建议指纹失败对免费分支 503、hash 脱敏
11. **P3 | IR 残余**：Gemini thoughtSignature 全链路丢弃（Gemini-3 工具调用需要）；多条 system/developer 跨协议（Anthropic 400/Responses 静默丢/Gemini 400）；Responses annotations 丢；mapFinishReasonToGemini 缺 refusal→SAFETY；v2ContentTextValue 不识别 video/thinking/tool/refusal（占位缺失非丢轮）；anomaly reporterDed 无 TTL
12. **P3 | UI**：FreeDiscoveryView 手写 table/confirm 未迁移 ui/ 套件（R30 #3 主体）；routing-opt 4 端点零前端；AnnotationView 动态 i18n key 无兜底 + scoped badge 重复；7 条全孤立路由（/routing-decisions、/admin/output-compliance、/admin/usage、/correlations、/quality-correlations、/routing/overrides(+audit)）零菜单零页内链接
13. **P3 | R30 #4/#6 仍开放**：currency pass A 串行头部阻塞（balance_floor_guard.go:885）；Governor MaxQueueWaitMS=0 默认（dispatch/config.go:115）

## 五、协作纪要（双会话同 workspace 实录）

- 本轮 6 路子代理运行期间，并行会话完成并推送 R33（b9d9a8ba5）；其对账、合并（fce00f609）、716 补登记（efc8b5268）均按"发现同点位先行入 main 时主动撤并/对账而非叠加"处理
- handler.go 唯一交叠点经 hunk 定位（对方 1014/5180/5206，本轮 6292+）确认不相交后合并；合并后 build+相关测试复跑绿
- 新增守卫（canonical≥704 ⊆ installer）落地后首跑即抓到并行会话 716 漏登记——门禁价值实证

## 六、下一轮提示词（建议）

> 以 docs/audit/2026-09-17-r34-48h-audit-round.md §四 遗留清单为起点：**首位 01-schema 列型漂移**（遗留#2，迁移 ALTER request_logs_hot 12 列 + dump-schema.sh 重生成三份 baseline + 守卫测试升列型校验；动 baseline 前先读 sql/scripts/dump-schema.sh 与 252 生产对账）；其次 credential_state_log 零读者决策（#1，GetState L3 兜底 vs 改写权威表，注意 recover_at/last_error 清空语义）；hosted_task_reconciler 预算与 call_history 双计（#3/#4）可同批小修。新增迁移前 git fetch 核对远端编号（当前已至 716，714/715/716 均 sequence+installer 双通道）。触碰 installer 前先跑 TestCanonicalStartupMigrationsAtOrAbove704AreRegistered 与 apply-db-revision-sequence_test.sh 双门禁。
