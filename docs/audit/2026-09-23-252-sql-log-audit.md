# 252 PG SQL 日志审计轮（2026-09-23）

> 轮次性质：去编号专项轮（252 PG SQL 日志审计系列第五轮，接 09-20 审计轮 / 09-21 复核轮 / 09-21 部署验证轮 / 09-22 第四轮）。
> 审计目标：对 252 docker 内 pg-252-pg17 实例 SQL 日志全量窗口做慢 SQL（>1s）与错误 SQL 审计，逐类根因分析并修正。
> 上游输入：docs/audit/2026-09-22-252-sql-log-audit.md §九（下一轮建议四项）。

## §〇、取证与起点

1. **起点重算**：开工时 HEAD = 29d1b5d31（main，与 origin/main 同步）；本轮 4 个代码修复位于其上。
2. **取证快照（先快照后分析纪律）**：06:03 快照 pg-252-pg17 容器 ctr.log **23.1MB / 237,870 行**，窗口 **04:49:28–06:03:35（74 分钟）**，写入 ~5.2KB/s——基线 590B/s 的 9 倍（高错误密度再次压缩取证窗口，连续三轮实证"取证先快照"）。
3. **窗口内总量**：慢查询（>1s）1,879 条（88 形状）+ ERROR 1,822 + FATAL 1,143 + cancel 风暴 1,491（上轮 205 的 7.3 倍）。
4. **客户端清单（纪律⑭：跨机归因先核 IP）**：172.16.2.210=252 本机（llmgo-252-dev 20 连接 + acc-go + memora）、172.16.2.209=154、172.16.2.241=245（ss -tnp → pid → cmdline 三段实锤；10.88.0.1/10.89.13.x=podman 网桥）。

## §一、错误 SQL 分类与根因

| # | 错误（74min 窗口量） | 根因 | 发送方 | 处置 |
|---|---|---|---|---|
| F1 | `column "ts" does not exist` ×24 + `column "created_at" does not exist` ×16（初版解析误并为 2 组，**按 STATEMENT 表名重分桶后实为 10 表×每 promote 周期必炸**） | **R48 §五#3 hotTableTSColumn 映射 10/20 张表与真库不符**：session_turns/session_bodies/auto_route_selections/candidate_failure_logs 写成 created_at（实为 ts）；request_wal/credit_ledger/tool_usage_stats/handoff_logs 走默认 ts（实为 created_at）；credential_model_index_hot 实为 bucket；supplier_errors_hot 实为 occurred_at。**TestHotTableTSColumn 把错误期望钉死为全绿**（"R48 侦察确认"与真库矛盾），gauge 对这 10 表自 R48 起从未工作过 | 网关 partition_manager（三台皆然） | **本轮代码修复 FIX-1**：switch 逐表对照 252 真库 information_schema 重写 + 白名单补 bucket/occurred_at + 测试期望重写 + **真库回归钉**（hot_ts_column_realdb_test.go：promoteSpecs 全 label 的 MIN(col) 查询真库实跑，TEST_DATABASE_URL 门控） |
| F2 | `duplicate key violates uq_models_canonical_active_folded_name` ×18（05:06:08–05:06:40 集中 32s） | **迁移 735（09-21 上线）的表达式唯一索引 vs `ON CONFLICT (canonical_name)` 覆盖缺口**：raw 名不同但折叠名相同的 INSERT 接不住（doubao-1.5-ui-tars 撞既有 active doubao-1-5-ui-tars、glm-4-5-air、deepseek-v3-2、glm-4-7 同型，STATEMENT 逐条实锤 `source='discovery'`），discovery 模型同步该批次全失败 | 网关 discovery worker（单 pid 671255） | **本轮代码修复 FIX-2**：upsert 前按 735 同款折叠链预解析（modelname.FoldedActiveLookupSQL，单一来源合成）到既有 active 行，让 upsert 走 ON CONFLICT DO UPDATE 合并；upsertModel/EnsureCanonicalAndAliases 两点共用 adoptFoldedExisting；查询失败 fail-open 不阻塞发现流水线。真库三段验证（①折叠命中 ②裸插必 23505 ③reroute 后 ON CONFLICT 无错） |
| F3 | `could not map dynamic shared memory segment` ×123 + `could not attach to dynamic shared area` ×94 + parallel worker 被终止 ×307 | **容器 /dev/shm 仅 podman 默认 64MB**（dynamic_shared_memory_type=posix，DSM 段全落 shm；宿主 15G 内存、容器无 memory limit——纯配置问题）。94/94 的 attach 失败受害者=探针会话 `MAX(turn_no)` 查询（其对旧月分区走 tenant 前缀扫 + Parallel Append，见 §二） | pg-252-pg17 实例级 | **runbook 根修**：/opt/scripts/pg17-start.sh 加 `--shm-size 1g`（备份 .bak-20260923；脚本改动 no-op，**容器重建属受控窗口动作不代执行**；在线 remount 因缺 CAP_SYS_ADMIN 不可行已实测） |
| F4 | `canceling statement due to statement timeout` ×36 | **不是**上轮预设的"REFRESH 未钉"——逐条提取附带语句归因：REFRESH analytics_7d ×5（见 F5）+ bindings 候选查询 ×7 + 抽屉族 ×7 + `analyze_llm_gateway_table_stats` ×4 + 小时级 rollup 族 ×12 + promote ×1 | 混合 | analyze ×4 = **本轮代码修复 FIX-3**（事务内 SET LOCAL 10min 与 Go 预算对齐，analyze 全实例逐表实测 >30s，30s rolconfig 击杀使 5min cooldown 形同虚设）；其余 30s 临界族维持 D2 登记（本轮新增击杀证据） |
| F5 | REFRESH analytics_7d 每 10min tick 精确击杀（05:33/05:43/05:53/06:03 = tick 起点+180s） | **跨实例协调裂缝**：Redis 选举与 advisory lock 兜底互不可见——252-dev（无 Redis）走 advisory，154/245 的 Redis leader 裸奔 REFRESH（useAdvisoryLock=false），同视图双发叠跑，252-dev 饿死在 180s pin 上。watcher 红手抓捕：252-dev 刷新 98s 仍在跑（pin 生效）、154 同语句 71-86s 在跑——**上轮"REFRESH 击杀全部来自过期二进制"的结论被修正：二进制都是新的，死因是协调分裂** | 252-dev + Redis leader 叠加 | **本轮代码修复 FIX-4**：advisory lock 升级为全局互斥后端（Redis leader 不再绕过，useAdvisoryLock 恒 true），任一后端获胜另一实例 try-lock 失败即跳过 |
| F6 | pocket `inconsistent types $4` ×15 | R46 已登记跨库问题（opencode-pocket 所有） | 10.88.0.1 | 维持登记 |
| F7 | `password authentication failed for user "smm"` ×16 | 外部客户端错密（pg_hba scram） | 非 llm_gateway 面 | 维持观察（跨库不越界） |
| F8 | `canceling autovacuum task` ×5（request_logs_2026_09 analyze 被冲突锁打断） | 顶层小时批与 autovacuum 撞锁 | 实例级 | 登记（量小） |

## §二、慢 SQL（>1s，1,879 条）

- **cancel 风暴 1,491（上轮 7.3 倍）逐条归因**：Top 受害=探针会话族——MAX(turn_no) ×607、session_turns advisory lock（sys:probe:credNN）×306、session_turns_hot INSERT ×55、bodies upsert ×22……探针请求在 per-session 串行锁上排队，顶部小时批（05:00/05:40/06:00 三次 ×4 击杀簇）拉长尾延迟 → 探针客户端 ctx 短熔断级联 cancel。**定性维持 R46：应用侧行为非 PG 故障**，但本轮实证其规模随负载尖峰放大 7 倍且与 DSM 失败（F3）同源放大——登记 §四 D2'。
- **30s 临界族新增击杀证据**（D2 族从"慢"升级为"开始死"）：bindings 候选（med 22.4s max 28.5s，击杀 ×7）、小时级 rollup（usage_facts dedup / fingerprint / session_key stats 各 ×4，整点簇发）、agg_src hot-only 版 max 29.96s 贴线。
- promote_request_logs max 58.8s 贴 60s SET LOCAL 预算（窗口 1 次击杀=预算语义正确生效，批 5000 行在当前负载下偏大——登记调优项）。
- REFRESH analytics_7d：完成 10 次 med 45.3s max 175s（pss max 178.9s）——180s pin 双刃：救活长刷新但也让 F5 的叠跑饿死可见。summary_7d med 3.7s 正常。
- 探针 MAX(turn_no) 本身 med 1.27s：2026_09 分区走复合索引、**旧分区臂 planner 误选 effective_session（tenant 前缀扫）**（复合索引各分区都在且 valid，EXPLAIN 实锤选错索引）——单条不致命，但探针高频 × 并行 Append 孵化 = DSM 压力主源。优化空间登记 D2'。

## §三、pss 增量复核（上轮 §九.1 顺延项）

- provider_error_agg_src 旧 unified 形状仅剩 **6 次调用**（均值 0.43s）——上轮"252-dev 重部署后此族绝迹"确认 ✅；hot-only 主形状 2,850 次均值 5.7s 但 max 29.96s（仍 30s 临界，D2）。
- 上轮"analytics_7d 击杀归零确认"**不成立**：击杀仍在（每 tick），但归因修正为 F5 协调裂缝而非过期二进制——修复面从"部署收敛"改为"代码协调统一"（FIX-4）。
- promote_request_logs 击杀 1 次/74min（60s SET LOCAL 预算语义下正常回收，非活锁）。

## §三A、部署与修复迭代（当日三台全量收敛）

A5 原定"代码推送、部署留窗口"，但部署 245/154 时暴露 D1 残余的完整形态——boot 链在共享库负载下确定性失败，不修则部署本身无法完成。当日迭代 9 提交（5f160c37a→9cbd60e4），烧点由 boot 日志/PG 日志逐个点名：

| 提交 | 内容 | 触发证据 |
|---|---|---|
| 5f160c37a | FIX-1~4 + 审计报告 | 本轮审计 |
| 7b5627e6e | FIX-5 request_logs ensure 目录短路（31+11 列/13 索引） | 245 07:23/07:26 boot 两连败，PG 日志 07:27:20 [705137] ALTER request_logs 57014 逐字实锤 |
| 5e1ae4a3d | FIX-6 columnsAllPresent helper + 701/704 守卫 | 245 seq 2195：烧点后移到 balance-floor 8 连 ALTER credentials |
| 1bc6d7e12 | FIX-7 auto_route_selections_hot 全量守卫（44 列+4 索引+3 约束+视图；**真库验证抓到视图臂逻辑写反已修正**） | 154 seq 2197 boot CREATE TABLE auto_route 两次 57014 |
| ef2b43156 | FIX-8 ApplyMigrations 迁移期 statement_timeout 钉（BeforeAcquire SET 5min + pool.Reset 零泄漏） | 烧点逐个后移不可持续；真库验证钉住/还原语义（迁移期 5min/Reset 回默认） |
| 79eee4582 | FIX-9 538 probe constraints 守卫（无条件 DROP+ADD CHECK 每 boot 全表校验） | 245 seq 2201 boot 日志点名 |
| 9cbd60e40 | 单元模板 boot 预算 75s→600s + TimeoutStartSec 700s | 守卫+钉后链自然耗时仍 1.5-3min（每步大查询锁排队 6-16s），75s 结构性不足 |
| 0a5ad6b20 | unavailable_recover_at + tuning_signals views 守卫 | 245 seq 2207 boot 两处 27s 等待 |

**迁移 736（R55/R56 并行轨道 maas rate_multiplier）预应用**：245 首次部署在切换前迁移步骤被 736 的 ALTER TABLE usage_ledger/request_logs 30s 击杀 → superuser 预应用（幂等）+ 照 db-changelog.sh 契约记账（schema_migrations + llm_gateway_migration_checksums，advisory lock 契约同式）。

**最终状态（09:00 验证）**：154 生产 **2211-9cbd60e4 ready:true**（切换 63s）；245 **2209-9cbd60e4 ready:true**；252-dev **2211**（ready:false=D4/D8 已知形态）。部署后窗口（09:00+，35min）：42703 列错误 **0**（前 10 表×每周期）、folded 冲突 **0**、REFRESH 击杀 **0**（完成 7 次=10min 节拍健康）。残余 23 条 statement timeout 全属 D2' 登记族。245 seq 2193-2208 共 6 次失败迭代的教学链：deployer 60s 探针 < boot 时长 → PROBE_TIMEOUT_SECS=600（部署器自带机制）；失败全部由蓝绿安全网正确拦截，旧实例零中断。

## §四、登记（本轮不修）

- **D2'（P1）30s 临界慢查询族专项**：bindings 候选 med 22s（击杀 ×7/74min）、小时 rollup 整点簇发击杀、agg_src 29.96s 贴线、抽屉族 ×7 击杀——上轮 D2"30s 内完成"的前提在当前负载下已破，建议下轮主轴（先 bindings 候选 + rollup 错峰）。
- **D2''（P2）探针 MAX(turn_no) 旧分区臂选错索引**（effective_session tenant 扫 vs 复合索引）：EXPLAIN 证据在案；候选修法=旧分区 extended statistics 或 view 臂显式索引提示，需专项验证。
- **D6（P2→已建 runbook）容器 /dev/shm 64MB**：pg17-start.sh 已加 --shm-size 1g，**下次受控重建生效**（重建动作=维护窗口，连带评审 postgresql.auto.conf 持久化遗留项）。
- **D7（P3）promote 批 5000 行在当前负载下 58.8s 贴 60s 预算**：观测，必要时批 size 热调（settings_kv lifecycle.promote_batch_size 已支持热更）。
- **D8（P3）252-dev healthz ready 恒 false**（上轮 D4 顺延，未动）。
- D3（245 collector/quality-service 老二进制）、smm 错密、pocket $4、autovacuum 打断 ×5：维持。

## §五、改动清单（全部 main 代码，无 schema 变更）

1. `bg/metrics.go`：hotTableTSColumn 重写（10 表修正 + bucket/occurred_at 特列），R48 事故注记。
2. `bg/partition_manager.go`：白名单补 bucket/occurred_at；analyzePartitionStats 事务内 `SET LOCAL statement_timeout='10min'`（FIX-3）。
3. `bg/partition_manager_test.go`：TestHotTableTSColumn 期望值重写（真库核定）+ 注入守卫扩 whitelist。
4. `bg/hot_ts_column_realdb_test.go`（新增）：promoteSpecs 全 label gauge 查询真库回归（TEST_DATABASE_URL 门控）。
5. `modelname/canonical_dedup.go`：新增 FoldedActiveLookupSQL（735 折叠链单一来源合成）。
6. `discovery/canonical_folded_resolve.go`（新增）+ `discovery/discovery.go` 两点接线：adoptFoldedExisting 预解析（FIX-2）。
7. `discovery/canonical_maintain_test.go`：seed-path 期望补预解析查询；`discovery/canonical_folded_resolve_test.go`（新增）：三分支钉。
8. `bg/materialized_view_refresher.go`：advisory lock 全局互斥（FIX-4，删 useAdvisoryLock=false）。
9. **252 生产面实改**：/opt/scripts/pg17-start.sh 加 `--shm-size 1g`（+备份；重建生效）。

## §六、测试

```
go build ./... 全绿；go vet ./bg/ ./discovery/ ./modelname/ 全绿
go test ./bg/ -count=1                    # 19.6s 全绿
go test ./admin/ -count=1                 # 66.6s 全绿
go test ./discovery/ ./modelname/ ./db/ -count=1  # 全绿（含 adoptFoldedExisting 三分支钉）
本机真库：TestHotTableOldestRowAge_RealDB PASS（TEST_DATABASE_URL → 127.0.0.1:5432/llm_gateway，
          promoteSpecs 20 label 的 MIN(col) 查询驱动层实跑）
本机真库：FIX-2 三段验证 PASS（折叠命中 / 裸插 23505 / reroute 后 ON CONFLICT，事务回滚无残留）
252 真库：10 表列名逐表 information_schema 核定；EXPLAIN 探针 MAX 查询（llm_gateway 角色）取证；
          pg17-start.sh bash -n + 语义复核
```

明确未覆盖：① 30s 临界族优化未做（D2'）；② 探针查询索引误选未修（D2''）；③ 容器未重建（D6 受控窗口）；④ 245/154/252-dev 网关二进制未随本轮修复重部署（见 §八 A5 处置）。

## §七、自审计（同日批判式复核）

| # | 初版问题 | 复核处置 |
|---|---|---|
| A1 | **初版错误分桶把 10 表 42703 误并为 2 组**（同消息不同表共用 normalized key），差点得出"只有 2 张表错"的结论、漏掉 8 张 | 按附加 STATEMENT 的 FROM 表名重分桶后 10 组各 4 条浮出；教训：错误分组键必须含语句形状，不能只看错误消息 |
| A2 | **曾断言"REFRESH 击杀=252-dev 旧二进制"**（沿用上轮结论）——但 252-dev 二进制 `go version -m` vcs.revision=6827fce8 含 pin 修复，理论被戳穿 | watcher 逐秒采样拿到 252-dev 刷新 98s 仍存活（pin 生效）+ 击杀时间轴=tick+180s → 死因改判 180s 钉超时；再挖出协调后端分裂（F5）。教训：**上轮结论不能当本轮公理**，归因必须当场重做 |
| A3 | **statement timeout 击杀默认按 30s rolconfig 归因**——实际本轮 5 次 REFRESH 击杀在 180s pin 上，30s 与 180s 击杀在日志里不可区分 | 唯一样本不可只靠日志文本；用 watcher elapsed 存活边界 + tick 时间轴交叉定位。后续轮：击杀归因先问"哪层 timeout" |
| A4 | pg17-start.sh 注释曾插进行续行块内（`--shm-size 1g \ # comment`）——bash -n 通过但 `#` 会变 podman 参数 | 已移到 podman run 之外并逐行复核；教训：`bash -n` 只查语法不查续行语义，shell 改动必须肉眼复核续行块 |
| A5 | **本轮 4 个修复是否随轮部署三台网关**：FIX-1/3/4 属 bg worker 行为、FIX-2 属 discovery 行为，均为非请求路径缺陷，生产当前无数据损坏/请求失败级影响（F1 只伤 gauge、F2 只伤模型同步批、F4/F5 只浪费刷新） | 决策：**代码随 main 推送，部署留给下一个常规部署窗口**（避免清晨连做三台蓝绿；252-dev 等低风险实例亦不例外——与"部署=维护窗口动作"既定纪律一致）。若要立即止血 F5，可先 252-dev 单台部署观察 |
| A6 | watcher 初版 pkill -f 自匹配杀掉自己 ssh 会话（exit 255 ×3） | 换包装脚本 + 精确 pattern；无证据损失（重试窗口覆盖） |
| A7 | A7'（上轮顺延：`//` 错误 06:48 自停之谜）未专项查透 | 本轮日志证实 `//` 错误 0 条且现役二进制均含修复——自停之谜已无现实后果，关闭 A7' |

## §八、下一轮提示词（建议）

> 以本文 §四 D2'/D2''/D6/D7 为起点，审计入口 docs/audit/playbook/orchestrator-prompt.md。优先级：
> 1) D2'：bindings 候选查询（med 22s）与小时 rollup 错峰专项——本轮击杀证据即基线；
> 2) D6：受控窗口重建 pg-252-pg17（--shm-size 1g 已就位，连带 auto.conf 持久化评审）；
> 3) D2''：探针 MAX(turn_no) 旧分区臂索引误选（EXPLAIN 证据 §二）；
> 4) 四台网关（252-dev/245/154+本机）随下一部署窗口带上本轮修复（FIX-1~4），部署后验证：42703 归零、canonical 23505 归零、REFRESH 击杀时间轴消失（advisory 互斥生效）。
> 纪律沿用：⑪ 真库经驱动执行；⑫ CONCURRENTLY superuser+indisvalid；⑬ 空集不落行≠故障；⑭ 跨机归因先核本机 IP；⑮ 部署失败先查启动链；⑯ "重跑自愈"先验证烧点可推进；⑰ 上轮结论不当本轮公理（A2）；⑱ 击杀归因先分层 timeout（A3）；⑲ 错误分桶键含语句形状（A1）。

## §九、handoff 更新

- 记忆库：llm-gateway-go-audit-cycle-progress 追加本轮；pg-252-sql-log-audit-facts 增补（10 表列名映射事故、735 折叠冲突缺口与预解析、/dev/shm 64MB 与 pg17-start.sh、REFRESH 协调后端分裂、30s/180s 击杀分层）。
- 原始物证：252:/tmp/pg252-snapshot-0923.log（23.1MB）+ /tmp/refresh_watch.log；本地 /tmp/pg252-audit-0923/（parse.py/bucket.py/kills.py/victims.py + analysis.txt/bucket.txt/kills.txt/victims.txt/cancels.txt）。

## §十、烧点家族补遗（同日 245 部署工单跟进，70e47c4f + 本轮批判式复审）

### 10.1 漏网烧点：ensureRoutingRecentSuccessRate（已修，70e47c4f）

09-23 15:02 245 seq 2221 (49ca6332) 部署失败：候选 ensure 链 30s 走到 governor revision (15:02:36) 后沉默 156s → 15:05:12 `schema migrations failed transiently (context deadline exceeded)`（applyMigrationsOnce 3min 预算烧穿，db.go:218）；attempt 2 重跑同链被探针双窗口 (180s+60s) 击杀。烧点 = `ensureRoutingRecentSuccessRate` 单体 Exec 首句对 request_logs_hot（最热大表）no-op `ALTER ... ADD COLUMN IF NOT EXISTS` 仍取 ACCESS EXCLUSIVE 锁——**紧随其后的 ensureUnavailableRecoverAtSchema 已有同款守卫（§十注释自证 seq 2207 实测 27.6s），唯独前一步漏配**。修复后实测该步 0.7s（原 156s+）；seq 2215 全绿上线（245 本机/443/公网 healthz+version 身份全绿，17:52 切换 67s）。

### 10.2 本轮批判式复审发现的预存缺陷（test-only，已修）

| # | 缺陷 | 修复 |
|---|---|---|
| B1 | `tests/deploy_readiness_contract_test.sh` 断言 `TimeoutStartSec=90s`，与 9cbd60e40 已提交的 700s 模板脱节——**F2"改默认值漏改断言"同型第 6 犯**，契约测试长期红 | 断言改 `^TimeoutStartSec=700s$`，语义改为"systemd 预算覆盖 600s 探针窗口" |
| B2 | `tests/deploy_local_contract_test.sh` redis harness：`DOCKER_PING_NAME='x' redis_discover` 临时赋值**未 export**，传不进 harness 内 `bash -c` 子进程，假 docker exec 永远不命中——初始版本(052246236)即坏，契约形同虚设 | 4 处改 `export DOCKER_PING_NAME=...;`（手工复现定位：lib 逻辑本身正确） |
| B3 | deploy-seamless 探针失败诊断（70e47c4f 引入）标记 grep 只看末 60 行 journal——09-23 第二次部署实证迁移重试告警恰好落在窗口外，分类降级 | grep 源扩到 `-n 400`，展示仍取尾 60 行 |

### 10.3 探针默认 180→600（deploy-245.sh，决策记录）

seq 2214 部署实证：**即使烧点已修**，晚间 252 PG 争用下整条 ensure 链仍拖 3-4 分钟（work_type 区 ~74s、tuning_signals→route_incident 区 ~2min，均无守卫无进度日志），180s+60s 双窗口仍不够，600s 手动覆盖即成（总 144s）。600s 与单元 TimeoutStartSec=700s 对齐；代价=真坏候选的回滚上限 4min→11min。**deploy-154.sh 仍 120s 未动**——154 是生产，探针窗口加倍意味着坏候选多占生产回滚时间 7 分钟，该决策留给 owner（下一轮提示词已列）。

### 10.4 复审声明（实事求是）

- 烧点归因是**强 circumstantial + 修复后行为回证**（UPDATE/CREATE OR REPLACE 在修复版中保留且实测毫秒级 → 排除），未做 252 PG 语句级日志归因。
- applyMigrationsOnce 3min 预算 vs 争用时 3-4min 链长的矛盾未完全解释（17:44 boot 链运行超推算 deadline 仍在推进——怀疑 attempt 2 重启点落在 60 行窗口外，未逐行核实）。**3min 预算是否上调到 6min 未动**——需 PG 侧证据支撑，盲调有 statement_timeout 5min pin 拉长风险。
- work_type/Maas 区烧点未修（无守卫无日志，修需 PG 语句级取证），登记为下一批候选。
