# R67 24h 审计轮（2026-09-26）

## 头部元信息

- **窗口**：2026-09-25 05:00 → 2026-09-26 05:00（54 非 merge 提交；含 HEAD 对账时按 55 计）
- **入口基线**：origin/main `db58c5ba8` → 本轮 fetch fast-forward 至 `17316e487`（12h 审计轮 F-1 修复），零冲突（并行会话的 `docs/audit/2026-09-26-r66-48h-audit-round.md` 未跟踪文件与新入文件不重名，已单独入库为 `e03764f2a`）
- **方法**：codegraph 全量重建（37878 函数 / 146383 节点 / 259720 边）→ 6 域子代理并行只读审计（协议IR / 探针 / 存储 / 队列路由auto / 安全可观测UI / 提交对账文档）→ 主代理交叉验证 → 系统级修复
- **继承上下文**：R66 48h 轮（85 提交 message↔diff 全绿）+ 12h 修订审计二轮（29 提交深读）+ 12h 审计 F-1~F-5 台账
- **下一可用迁移号**：750（749 本轮已用；801 仍空闲）

## §一 全绿复核（子代理实证，非采信前档）

| 域 | 判定 | 关键证据 |
|---|---|---|
| ollama 契约反转（985d23ae9） | ✅ 结构自洽，无 P0 | ollama 在流式响应方向**无下游序列化器**（stream.go 四槽位为 openai/anthropic/responses/gemini），SerializeOllama 是请求方向不涉流式契约；parse 增量 + `CumulativeContent`（json:"-"）零消费者；round-trip 心算三帧 "Hel"/"lo"/" world" 拼接正确；`TestParseOllamaStreamChunk_MultiFrameIncremental` 双面覆盖 |
| 探针 P0 三连修（19eb31536/1082ab0b8/86f9e505f） | ✅ 全部属实 | 迁移 748 六点同步（installer module TestStartupFilesAreAllEmbedded 实跑过）；429 熔断单 goroutine 声明核验成立（Start→loop→cycleOnce→runOne→doHTTP 全程串行，全仓无第二触碰方）；频控 fail-open 方向正确（宁过探不丢触发）；defer 断链命名返回值修复无绕过；Heal 幂等且不误伤（owner_user 双 worker 专名）；attempt int 域 + 6h 封顶无溢出 |
| 供应商错误处理闭环（standing） | ✅ 记录面闭环 | CandidateFailureWriter→supplier_errors_hot→provider_error_details/supplier_error_stats（credential_id 粒度）→凭据详情/趋势 API；解密失败非静默（recordDecryptFailure+实例级熔断 5/15min） |
| **think/注释模式软降级（standing）** | ⚠️ 不存在，属设计现状 | 全量搜索无"错误包装成 thinking/注释以 200 不中断"实现；现役降级=failover 换候选+pinned rescue，**绝不伪装错误为模型输出**。是否引入该软降级=产品决策，登记 §四 待 owner 拍板 |
| hot 8h + 分区红线 | ✅（PG 侧） | DefaultRetentionWindow=8h（partition_manager.go:45）；promote 批 5000/预算 5min/100 批/advisory lock 防 154/245 并发；分区父表零行级 DELETE；ba95e7424 atomic.Int64+单次快照无 TOCTOU |
| 双存储架构 | ✅ 装配差异清晰 | full(pg+redis+memory+files) vs lite(sqlite+memory+files) 装配表核实（storage_mode_init.go）；lite 无 pg-only 误用（main.go 门控点抽查） |
| F-1 admin 密码 preflight（17316e487） | ✅ | 函数置于 dl_write_env 前、set -e 中止路径成立、逃生口拼写一致、检查值==落盘值、4 案测试实跑 PASS |
| 55 提交 message↔diff 对账 | ✅ 0 硬缺失 | 10 个初始 flag 逐条裁决为改名/现状描述/文档引用豁免；遗留 2 弱项：`docs/audit/2026-09-25-llm-gw-24h.md` 悬空引用（dad3377d1/3957c1423，跨仓引用未带仓库限定）、02da86168 locale 去重系变基叙述不可自证 |
| auto 全链路 | ✅ 无断链 | main.go:5020-5302 装配链完整；model=auto：handler.go:2862→maybeResolveAuto→DecideWithFeatureFlags→失败 502 透明→tenant denylist 二次复查→dispatch ModelRecommendFunc；TierSelector 零接线属实（standing） |
| WeightedRouter / sticky | ✅ | WeightedRouter 生产零持有维持（唯一构造方 tests/local）；sticky ZSET ursm:v2:stickyload 活跃；queue 两层（admitTotal+totalQueue / per-credential Governor+limiter 三级+RPM 滑窗+冷却）分层协同无重叠 |
| f746a4f52 cgo 隔离 | ✅ 双构建全绿 | cgo 与 CGO_ENABLED=0 两种 go build ./... 均 exit 0 |
| 版本戳链（282ebf42a floor） | ✅ 5/5 | bump_version_collision_test.sh 实跑 5 案过；floor 先于 version 落盘、15s fail-open；746→747 撞号重编链（61e2f6c85 出生撞号、a0e4d9c59 重编）入档 |

## §二 本轮修复（P1×1 + P2×1 + P3×4）

### F1（P1）usage_facts occurred_at 前导索引 —— 迁移 749（67ce8dc16）

- **实锤**：usage_facts 是 RANGE(occurred_at) 分区父表但仅 DEFAULT 单分区，537 的 4 个二级索引全部非 occurred_at 前导；每日 rollup **五**查询（rollup.go 227/312/344/412/475）+ stats 对账（reconciliation.go 195-302）全是纯 occurred_at 范围条件（R66 只计 5 处，本轮实为 **≥8 处/日**），全表顺序扫线性退化至被共享 PG 30s statement_timeout 成批击杀。
- **修法**：744 同款三段式（分区父表 42809 不支持 CONCURRENTLY）：逐分区 CONCURRENTLY → ONLY 壳 → ATTACH；未来按日分区自动继承。三臂投递齐备（canonical SQL + sequence 通道登记 + Go ensure `ensureUsageFactsOccurredAtIndex`：10min 预算/INVALID 先 DROP 再建/749 盖章）；有意不经 installer（单事务容不下 CONCURRENTLY）。
- **验证四通道**：contract test（绑定真查询形状）+ scratch PG17 真库 ensure 回归（重建+幂等+盖章）+ psql 实跑 canonical SQL + EXPLAIN 实证 Bitmap Index Scan 走新索引。

### F2（P2）cache-v2 H2 契约翻转不完整（96a6629c2）

da6b95627 翻转了 Set/读链，但 Get 的 **L3 冷启动回填**仍残留 `effectiveMode()==Lite` 门，与翻转注释矛盾——full+注 fileCache 时 L3 恢复不回填 L1.5，H2 装配点启用即踩。改为 `l1_5 != nil` 即回填（B4 分片锁不变）；补 full 模式 L3 回填集成钉桩。生产唯一调用方仍传 Lite，无行为变化。

### F3（P3·崩溃级）scope_key 解码 panic 守卫（96a6629c2）

`splitInternalPersonScopeKey` 守卫只校验到 tenant 尾：`"2:ab"`（前缀恰好耗尽）过守卫后 `k[i+1+n+1:]` 越界 panic；`"-1:xyz"`（负数长度）`k[i+1:i+1+n]` 低>高 panic。补 `i+1+n+1 > len(k)` 与 `n < 0` 双臂 + 7 案钉桩（含 tenant 含冒号消歧回归）。可达面=读库中历史/外来 scope_key。

### F4（P3）scoring_new.go 手写泰勒 exp 换 math.Exp（96a6629c2）

被丢弃的 autoroute v2 在制 P1（手搓 exp 恒 1）的同族残留，±5 截断精度损失在生产评分路径；衰减输入恒非正，保底分数由调用处下限钳制承担。

### F5（P3）陈旧注释×3（96a6629c2 + 749 提交内）

parse_ollama.go 非流式 thinking 残留 cumulative 措辞；minheap_topk.go "WeightedRouter 唯一热消费方"失真；report_rollup_worker.go "单日分区扫描"假设（随 749 落地改写为索引依赖）。

### 台账定性修正：F-2（12h 审计移交项）

在制 autoroute v2（scoring_v2.go 等）**已被丢弃**——未提交、未 stash（`git log --all` + 55 个 stash 全扫零命中），非"在制解决"。d3012408d 所记 P1 随代码消失；"手搓 exp 恒 1"不再存在，但其同族问题（F4）在树上并由本轮修复。

## §三 测试与验证（本轮实跑）

```
go build ./...                                    RC=0
CGO_ENABLED=0 go build ./...                      RC=0
go vet（6 个受影响包）                             净
go test ./domains/reportrollup/...                 ok
go test ./domains/session/v2/...                   ok（含新增 L3 回填钉桩）
go test ./autoroute/                               ok
go test ./internal/ir/...                          ok
go test ./sql/migrations/startup/                  ok（含 749 契约）
go test ./db/                                      ok
go test ./bg/...                                   ok（bg 24.9s）
installer module go test ./...                     ok（748 parity 含）
TEST_DATABASE_URL=scratch PG17: TestEnsureUsageFactsOccurredAtIndex_RealDB  PASS
psql 实跑 749 canonical SQL                        PASS（父/子索引 indisvalid=t）
EXPLAIN rollup 形状查询                             Bitmap Index Scan 走新索引
bash scripts/apply-db-revision-sequence_test.sh     contract passed
bash scripts/deploy-local-lib-adminpw_test.sh       PASS（复核 F-1）
tests/bump_version_collision_test.sh                5 passed（复核）
```

## §四 登记不处置（后续轮次入口）

| 级别 | 项 | 去向 |
|---|---|---|
| P2 | usage_facts 按日分区函数 + ensureSpecs/promoteSpecs 接线（749 只解决索引，分区裁剪仍未建）；TTL 需产品裁决（canonical 真相源） | R68 |
| P2 | HotZoneTrimmer 未接线（wiring-pending TODO，零生产调用方）；data/hotzone 7h/1GB 死配置；与 H2/H3 接线同步评估 | 特性分支待 owner |
| P2 | per-tenant 并发闸（domains/tenant/quota.go Acquire）零生产接线——租户级总闸缺位，产品拍板"补接线 or 删代码" | owner 拍板 |
| P2 | Gemini 流式 stop_reason 透传有损（RECITATION→SAFETY、MALFORMED_FUNCTION_CALL→STOP；parse 未填 StopReason 原生槽）——anthropic A#2 同族，修法现成 | R68 |
| P2 | 供应商错误 think/注释模式软降级不存在——standing 期望与"绝不伪装错误为模型输出"设计现状冲突，需 owner 裁决 | owner 拍板 |
| P3 | 自检 429 熔断无 metric（只有日志）；/metrics 挂 admin token 常规 scrape 不可见 | 观测专项 |
| P3 | scan-secrets 白名单整行短路且 `'sed.*postgres(ql)?'` 未锚定；INTERNAL_DOMAIN 先于 CRED_ASSIGN 吞掉 kxpms.cn 凭据赋值 WARN | 收紧时一并评估 |
| P3 | 429 周期间记忆 worker 级非凭据级（可 map[credentialID]bool 化）；`docs/audit/2026-09-25-llm-gw-24h.md` 悬空引用 | 观察项 |
| P3 | CumulativeContent 永久死字段（ProtocolOllamaChat 接线时统一裁决接或删）；classifier_v3.go 零调用 Deprecated | 清理候选 |
| 记录 | 全新装 718（installer 单事务 × CONCURRENTLY）理论必炸——全新安装本就登记为坏的（installer-fresh-install-baseline），不新增处置 | 既有台账 |

## §五 执行记录

1. `e03764f2a` R66 48h 轮文档入库（并行会话产物保全）
2. `67ce8dc16` F1 迁移 749（7 文件）
3. `96a6629c2` F2-F5 批（7 文件）
4. `01a9b140e` 合并 origin/main（见 §七①：092ab1b61 部分提交事故修复）
5. `c57e856e9` 合并 origin/main（cursor 会话 P4 feature 分支合入，见 §七②）
6. `bc68bd1b0` scan-secrets baseline 补 53 条（见 §七③）
7. 本文档；推送 origin（codeup）+ github 镜像，三方一致于 `bc68bd1b0`

## §七 后记（文档提交后窗口内发生的反转与处置）

### ① F-2 二次反转：被丢弃的在制 v2 于 05:26 复活，且以"部分提交"形态击穿 main 编译

本文档 §二"台账定性修正"写入时 F-2 确为丢弃态；05:26 并行会话 `092ab1b61`
将其重新落地——但该提交**message↔diff 不符**（本仓登记过的虚构模式复发）：
message 声称改 4 文件（scoring.go 补 scoreVersionRecency / scoring_simplified.go
删桩），实际 diff 只删 scoring_new.go + 增 scoring_v2.go，导致 origin/main 上
autoroute 包**编译不过**（scoring.go:191/192 引用悬空 + v2 与 simplified 大面积
重声明，重复实现已逐一 diff 核验为逐字节相同）。本轮合并 `01a9b140e` 按其
message 声明的终态收敛：v2 接管 4/5 维与池分层族，simplified 回 2 维兼容 +
共享工具，scoreVersionRecency/scoreStrengthMatch 自 96a6629c2 恢复（math.Exp
随迁）。**遗留**：v2 的 ScoreWithChannelQuality 仍将 VersionRecency/StrengthMatch
恒 0——scoring.go 8 维加权中两维权重实际收 0 分，随 v2 演进收口。

### ② F-3 解除：cursor 会话 P4 feature 分支已合入 main

12h 审计登记的暂缓项（endpoint selector + Ollama native executor）由其会话
自行整合（`422bb5fc4`），随带 9 个迁移的 audit-rewrite SHA 治理
（verify.sh 0 mismatch）与 FF_OLLAMA feature flags（settings/p4_feature_flags.go）。
合并零冲突；executors 测试套件 33.6s 全绿。R66/R67 优先入口"ollama P4 接线"
自此落地，R68 转为验证接线效果与 mock-probe 拍板。

### ③ github 镜像同步解阻塞

pre-push 严格扫描以 53 条 WARN 阻断 github 推送（R66 登记的 ~1109 已被
白名单+tracked-only 收敛到 53）。逐条人工核验均为占位符误报（测试桩
test-secret / 文档 any-string / i18n 文案 / 配置展示策略串），按钩子官方
指引补入 `scripts/scan-secrets.baseline`（`bc68bd1b0`），复扫 exit 0。
**file:line 基线随行漂移需维护**——R66 §四的"baseline 策略拍板"仍开放。

## §六 下一轮（R68）提示词建议

> 你是 llm-gateway-go 仓库（/Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go）的审计主代理，执行 R68 24h 审计轮。
> 1. git fetch 对齐 origin/main（本轮 HEAD 见 git log）；窗口 `git log --since="24 hours ago"`；继承 message↔diff 机械对账门禁。
> 2. 读 docs/audit/2026-09-26-r67-24h-audit-round.md §四登记项 + docs/audit/2026-09-26-r66-48h-audit-round.md §二优先入口。
> 3. 优先入口：①usage_facts 按日分区 + ensureSpecs 接线（749 索引已就位，分区裁剪待建；TTL 需 owner 裁决）；②Gemini stop_reason 透传修复（anthropic A#2 同款三步法）；③R66 遗留：mock-probe 生产接入拍板、ollama P4 selector 接线包（迁移 750 起先 fetch 核对）、auto TierSelector 接线；④owner 拍板项跟催：per-tenant 并发闸去留、供应商错误 think 模式软降级。
> 4. 关键上下文：ollama content 契约已定增量且无流式序列化器（P4 接线时序列化面从零建）；cache-v2 L1.5 契约已全链翻转（Get 读+Set 写+L3 回填均 l1_5 非空即生效）；探针长尾梯/频控/熔断已上线（默认 on）；提交信息对账是审计对象不是元数据豁免区；下一迁移号 750（startup）/801，登记前 fetch 核对。
