# 2026-09-07 24小时修正审计报告

- 审计对象：`c0ea82328..f3f086b82`（2026-09-06 14:09 ~ 2026-09-07 04:17 +0800 合入 main 的全部提交，约 80 个）
- 审计模式：主代理 + 并行子代理分轴（前端/文档轴、IR/优化器/ML 轴完成；其余轴因子代理路由故障转为主代理定向核验）
- 基线参考：`docs/audit/2026-09-05-24h-comprehensive-audit.md`（不重复报告其已闭环项，只复核其遗留清单）

## 一、执行摘要

| 级别 | 本轮发现 | 已修 | 转交后续 |
|------|---------|------|---------|
| P0 | 1（迁移撞号，仓库契约测试红） | 1 | 0 |
| P1 | 9 | 3 | 6 |
| P2 | 10 | 3 | 7 |

上一轮（2026-09-05）审计遗留的 P1/P2 清单中，本轮核验仍未闭环的项在 §四 单列，避免"报告绿了、实际没修"。

## 二、已修复（本轮，全部含验证）

| # | 级别 | 问题 | 修复 | 验证 |
|---|------|------|------|------|
| 1 | **P0** | `671_routing_opt_active_fix.sql`（e436cb83）与已应用+验证的 `671_local_provider_catalog.sql`（154 build_seq 1996）撞号，`TestNumericUpMigrationVersionsAreUnique` 在 main 上**红**——迁移器按数字版本去重时 671 之一会被跳过，路由优化单活索引修复静默丢失 | 本轮独立发现并改号 675；推送前变基发现远端已并行修复为 **676**（154 build_seq 2015 applied+verified，且 675 已被 `675_qwen38_family_vendor` 占用——本地 675 若强推将二次撞号）。采纳远端 676，本地撤销改号；契约测试恢复绿 | `go test ./sql/migrations/startup/` 全绿（rebase 后复验通过） |
| 2 | **P1** | `validateLocalBaseURL` 按 `"10."`/`"192.168."` 字符串前缀放行，`http://10.evil.com:8080`、`http://192.168.attacker.tld` 等伪私网主机名通过校验（SSRF 面）；且未排除云 metadata 网段 | 改为 `net.ParseIP` + 私网 CIDR 精确匹配（127/8、RFC1918、::1、fc00::/7）；域名一律拒绝（保留 localhost / host.docker.internal）；**刻意不含链路本地段**（169.254.169.254 metadata SSRF 目标） | 测试新增 5 个攻击用例（伪私网域名×3、metadata 域名/IP、0.0.0.0）与 IPv6 回环/ULA、127.8.8.8 正例；`go test ./admin/` 全绿 |
| 3 | **P1** | 热力图 `exclude_self_test` 注释与 FEATURE-REQ §4.2 均承诺"缺省 true"，实现 `queryBool` 缺省 false——裸 API 调用方把自检流量计入服务质量统计 | 缺省 true，仅当查询串显式携带该参数时才按值解析（前端显式传值行为不变） | `go test ./admin/` 全绿（handler 级参数解析随包测试覆盖） |
| 4 | **P1** | 路由记录 `kind=state_change` 时 probe 分支仍输出 `'probe' AS kind` 且 change 恒 NULL——recovered/broke 共识翻转被标成普通探测，前端按 kind 过滤/展示丢失状态变化语义 | `kind=state_change` 时 probe 分支输出 `'state_change' AS kind` + `COALESCE(mpr.state_change,'') AS change`；`kind=all` 语义不变 | SQL 形状测试双向钉死（state_change 与 all 两种 kind 的分支标注断言）；`go test ./admin/` 全绿 |
| 5 | P2 | 热力图手动切换粒度不触发重新查询：网格按新粒度重排旧数据，meta 与数据错位 | `watch(granularity)` 触发 `loadHeatmap()` | vue-tsc 干净 |
| 6 | P2 | 自动刷新间隔切换不重启定时器（新间隔要到下次手动开关才生效） | `watch(refreshInterval)` 就地重启 timer | vue-tsc 干净 |
| 7 | P2 | 菜单漂移：`appNav.ts` 的「系统自检」（/dashboard?tab=selfcheck, super）在导出脚本内嵌副本与 `menu-config.json` 中缺失 | 导出脚本副本补齐 + 重新导出（净增该条目及其 tenantScope/pluginScope 标注） | `node scripts/export-menu-config.mjs` 重放一致 |

回归记录：`go build ./...` 通过；`go vet ./admin/` 干净；`go test ./admin/ ./sql/migrations/startup/ -count=1` 全绿；`vue-tsc --noEmit` 零错误。

## 三、需求 → 证据对照（任务书 14 项）

| 需求 | 结论 | 证据/缺口 |
|------|------|----------|
| 流程/数据/反馈闭环 | 🟡 | 请求生命周期与错误聚合闭环延续 09-05 审计结论；**反馈闭环失真**见 §四 F-1 |
| IR 定义清晰度（轮次/项目/任务/tag/凭据等） | 🟡 | 延续 09-05 结论：协议字段显式，业务元数据靠 dispatch/会话层承载；本轮无回归 |
| 多协议转换（chat/message/response、流式/非流式） | ✅ | 本轮 24h 未触及转换层；09-05 修复（Anthropic file_id、Gemini 罚项）仍在 |
| IR 创建/赋值/序列化/存储分层 | ✅ | L1→L1.5→L2→L3 回填/失效链路延续；lite sink 已接线（09-05 #14） |
| 单请求/多轮双向解析 + 每轮人读摘要 | ✅ | digest 管道延续；双轨摘要统一仍为 P3 |
| 多层队列/限流/权重负载均衡 | 🟡 | 本轮未改动 dispatch；shutdown 排空修复延续；**V2 权重排序无 optimizer** 见 §四 F-1 |
| 并发/泄漏/溢出/网络安全场景 | 🟡 | 本轮修掉 local provider SSRF（#2）；`go test -race ./autoroute ./routingopt` 绿；真实故障注入仍未做 |
| 可观测性/展示统一/菜单/交互 | 🟡 | 菜单漂移修复（#7）、交互两处修复（#5/#6）；热图/路由记录仅 duration_ms，无持续指标（§四 F-7） |
| hot+分区（8h、更新删除只在 hot） | ✅ | 本轮未触及；09-05 D 轴遗留（656 ensure、SQL 测试未接线）仍在 §四 |
| 供应商错误记录 + think 非中断 + 凭据详情呈现 | ✅ | candidate_failure_logs→provider_error_details→ErrorDetailTab 链路延续；本轮无回归 |
| 全量/本地双存储 | 🟡 | lite sink 已接线（09-05 #14）；StateStore 仍未接线（§四 F-9） |
| auto 模型全量实现 | 🔴 | **V2 生产路径无 optimizer 接入、反馈非真实结果、子开关未生效**（§四 F-1/F-2/F-3）；ML 离线产物无 Go 推理适配（§四 F-5） |
| 冗余流程标注/清理准备 | 🟡 | 本轮在修复点补充了约束性注释；系统性清理仍需专项（handoff） |
| 24h 方案文档阅读与更新 | ✅ | 阅读 FEATURE-REQ-credential-heatmap-routing-log、p2.4-training-pipeline、dual-storage ADR、09-05/09-06 审计；本报告即更新 |

## 四、已核验未修（按优先级，均已定位 file:line，转 handoff 工作包）

### P1

1. **F-1 optimizer 未接入 V2 生产路径**：默认 `UseChannelQualityRouting=true` 时 `DecideWithFeatureFlags` 直走 `DecideV2`（autoroute/feature_flags.go:318-326），optimizer hooks 与反馈只在 V1 `Decide`（decision.go:417-503,565-567；decision_v2.go:119-249 无 hooks）。e436cb83 "接入生产路径"对默认流量不成立。方案：optimizer 注入统一决策管线（V1/V2 共用 PreClassify/重排/反馈缝），补 V2+optimizer 集成测试。
2. **F-2 反馈非真实结果**：`recordFeedbackAsync` 固定 `ActualProvider=ChosenModel`、`IsSuccess=true`（optimizer_bridge.go:131-146），RequestID 由 apiKey+纳秒拼装而非 HTTP X-Request-Id（:149-153；domains/streaming/auto_route.go:356-363 已有真 ID）；integrator confidence 恒 0.8（routingopt/integrator.go:65-82）。方案：在执行器结算处（成功/失败/延迟/成本已知点）落真实反馈，V1/V2 共用幂等事件。
3. **F-3 optimizer 子开关未生效**：`EnableClassificationEnhancement/ModelRecommendation/FeedbackIntegration/AdaptiveLearning/MaxPluginLatencyMs` 仅 init 日志展示（cmd/gateway/routing_optimizer_init.go:24-41），RealOptimizer 无条件全跑。方案：effective flags 贯穿调用链 + 每 flag 行为测试。
4. **F-4 路由记录契约字段缺失**：FEATURE-REQ §5 要求 probe `http_status`、routing `sticky`/`outbound_model`、统一 `detail`，SQL/Go 结构/TS 类型/详情模板均无（admin/credential_routing_log.go:23-41,299-346；web/src/api/credential-monitor.ts:396-413）。
5. **F-5 probe-health 手动触发丢失**：整合规格 §7 明确保留，ProbeHealthPanel 无触发按钮（web/src/views/probe/ProbeHealthPanel.vue:105-315），API 仍在（web/src/api-selfcheck.ts:175-192）。
6. **F-6 凭据启停按钮缺失**：§4.7 要求 `setManualDisabled`，热图抽屉只有 online/offline/promote/demote 四钮且不按当前状态条件渲染（CredentialHeatmapView.vue:368-395,673-685）。

### P1/P2

7. **F-7 热图/路由记录可观测性不足**：仅单次 duration_ms，无调用量/失败率/慢查询指标；heatmap cache_hit 恒 false（admin/credential_monitor_heatmap.go:48-51,164-171）。方案：接入既有 monitor 打点体系。
8. **F-8 ML 闭环断裂**：ONNX 导出仅 Python 侧（ml-training/src/export_model.py:4-6），无 Go 加载/推理/热更新/回滚；`annotation_require_known_label` 默认 False 存在标签空间污染（data_loader.py:188-205）；**当前环境 pytest 因缺 pyarrow 不可复跑**（conftest.py:28），"27/27 pass" 不可作为本机证据——需 CI 装 requirements 并加依赖预检。
9. **F-9 lite StateStore 未接线**（延续 09-05）：进程内 KV 无生产消费者。

### P2

10. F-10 `SeedProvidersFromCatalog` 只建 provider 不补 local 凭据（admin/providers.go:1318-1352），seed 出的本地供应商不可路由——启动期应调 `ensureLocalCredential`。
11. F-11 admin refresh-models 传 `rawModelsJSON=nil`（discovery/local_context.go:61-64），vLLM/llama.cpp 的 /v1/models 上下文字段被跳过。
12. F-12 RoutingLogView 刷新间隔硬编码 30s 无控件（web/src/views/RoutingLogView.vue:159-173）。
13. F-13 文档漂移：部署指南仍三 Tab/`is_self_test`/`date_trunc`（docs/deployment/credential-monitor-heatmap-deployment-guide.md:133-219,430-475）；设计文档组件拆分/localStorage key 与实现不符。
14. F-14 延续 09-05 遗留未修清单：request_logs 错误路径未脱敏（E-#2）、error_message 参与聚合键（E-#4）、656 无 gateway 侧 ensure（D-#3）、auto_route SQL 测试未接自动化（D-#4）、signature_delta 只解析不序列化（A-#5）、max_completion_tokens 折叠（A-#6）、附件版本注册表缺失（F-#5）、双轨摘要统一（F-#6）。

## 五、覆盖度声明（不夸大）

- 子代理 IR/存储分区/队列并发/全局验证四轴因上游路由服务故障（EPIPE/routing executor unavailable）未产出独立报告；相应维度由主代理按 09-05 分轴报告 + 本轮定向核验（git show/grep/测试）覆盖，未重跑全量 `go test ./...`。
- 本轮未在真实 PG/Redis 环境执行迁移与故障注入；675 迁移的"applied+verified"状态待下次部署后回填 db-changelog。
- 工作区另有 6 个 toolchain 升级配套文件（Dockerfile/go.mod）为上一提交（18687a7b0）遗漏的未提交改动，本轮未纳入、未回滚，见 handoff。

## 六、方法论

git log --since=24h 锁定范围 → 并行子代理分轴（可用的两轴完成）→ 主代理对全部子代理发现逐条 file:line 核验（确认/纠正/否决）→ 只修有测试可钉死的问题 → 统一回归 → 本报告。子代理原始发现与主代理核验差异示例：迁移撞号（子代理发现，主代理以契约测试证实为红）；probe kind 标注（子代理报"SQL 无 state_change 输出"，核验后确认仅 kind=state_change 查询路径失真，范围收窄后修复）。
