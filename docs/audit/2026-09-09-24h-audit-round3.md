# 2026-09-09 24h 审计（第三轮）

范围：`e89eccfcb..HEAD`（第二轮报告之后的后续轨道 16 commits：Track A 学习信号/A#2 流式IR/B#3 request_flow/B#5 requestfact/B#4 digest/C#7 迁移688 + 部署记录），并以重建的 codegraph 全库图谱（134,210 节点 / 235,443 边）+ 5 个并行子代理（24h delta 专项 / IR 协议传输 / 队列并发安全 / 存储分区双模式 / 错误闭环·auto·UI·SSRF）按 13 项审计要求核对。同时推进第二轮未修清单剩余 5 项（#6/#7/#8/#10/#11）。

## 修复（本轮 7 commits，已合入 main）

| Commit | 内容 |
|---|---|
| `16b2291e8` | **P1-部署** deploy-local 同版本重部署保护 active 发布：`ensure_release_available` 改为 active bundle `mv` 到 `.prev-<epoch>` 后缀（0c465a815 的 `rm -rf` 会删掉运行中实例的 web/env，build 失败则 current 悬空且回滚守卫静默跳过） |
| `905aeb954` | **P1-auto反馈** pending 注册表三修：`container/list` O(1) 删除（旧 []string 全局锁内 O(n)，8192 满载成 Decide 串行点、TTL 批量过期 O(n²)）；TTL/驱逐/同ID重占位不再写 IsSuccess=true 占位行（限流/崩溃/超长流请求被系统性标成 routing 成功=假阳性训练标签，三子代理独立确认）；janitor recover 移入每 tick（原一次 panic 永久停摆 TTL 清扫）+ Debug 统计日志 |
| `3f8ba33d1` | **P1-凭据健康** OnSuccess/OnError 每请求派生无界 goroutine（上游故障风暴 × 10s PG 链 = 数千 goroutine 冲连接池）改 32 槽有界信号量 drop-on-full + recover；dispatch defer 的 OnError 按 `dispatchFailureStage` 哨兵过滤网关侧准入拒绝（circuit_open/keys_exhausted/并发限流——checker skip list 不含这三类，负载尖峰下健康凭据被自产失败推向 80% 硬降级=熔断自我强化，恰是 28280b597 要消除的噪声换了来源） |
| `424d58be3` | **P1-计费** 流式 cache token 全链闭环（生产活跃路径）：ObserveChunk 补拷贝 CacheRead/CacheWrite 指针（bridge writeChunk→ObserveChunk 链全覆盖）；anthropic_bridge 累计器补 cache 字段并在两个终端合成 usage 块透出；OpenAI 兼容上游三站点提取 `prompt_tokens_details.cached_tokens`；native Responses 提取 `input_tokens_details.cached_tokens`。此前 request_logs 缓存列恒 NULL |
| `5fbbe7ac1` | **P1-SSRF(#10)** providercap.EgressBlocked 预检接入 quotafetcher balance + probe_v2 chat/balance 四出口：无条件拒绝非 http/https、link-local(169.254/16+fe80::/10)、CGNAT(100.64/10，阿里云元数据 100.100.100.200 所在段)；RFC1918/loopback 默认放行（自建 vLLM/one-api），`PROVIDERCAP_EGRESS_ALLOW_PRIVATE=false` 整体禁私网。DNS 解析到禁段需 safehttpclient dial-hook，列后续 |
| `c4bcf2073` | **P2-批次** digest 摘要窗口按轮裁剪（capByTurn：digest 源开启后 maxMessages=10 语义从轮变侧，窗口静默减半为 5 轮）；affinity Redis 写补 200ms 超时（头阻塞类）；BaseWorker 脚手架 recover（20+ worker 原「一次 panic=进程退出」）；outbox 退避 shift 钳制 ≤6（maxAttempts≥33 时 Duration 溢出→紧重试风暴）；gemini-generate dispatch 显式 KindUnsupportedFeature（原误入 executeOpenAI 产生 4xx 污染凭据错误统计） |
| `52067a55c` | **P2-前端(#7)** ErrorDetailTab 消费后端已算的 retryable_count/stage_counts（错误分布表新增「可重试 N/M」列 + 阶段分布徽标，复用 errorStageBadgeClass 共享词表）；8 语言包补键，顺带把 ja/de/fr/es/ar 该行遗留中文占位补为对应语言翻译 |

另有结构标注：删除两个不参与构建的死文件（client_adapter_v2.go.disabled 231 行、unified_transport.go.disabled 389 行）；response_format_adapter.go 的 AdaptiveResponseConverter/ConvertStreamChunk 标注 Deprecated（无调用点 + 同类缺陷 + deriveResponsesMessageID 双实现边界不一致）。

## 关键纠偏（对第二轮报告结论）

1. **A#2 流式 IR 修复是 test-only 路径**：TransportFactory.Pick/IRTransport.ConvertStream 无生产调用点，`TRANSPORT_LAYER_IR_ENABLED`（docker-compose.yml 设 true）无消费者。生产流式由 main.go 接线的 executors/bridges 承担（TransportIRConverter 默认开、不经工厂）。且 SerializeAnthropic 输出形状 SDK 不可消费（message_start 在 content 后、无 content_block_start/stop、缺 signature_delta 透出）。已回写第二轮报告并标注代码。**决策项**：真接线 vs 整体下线，移交 handoff。
2. **B#5 requestfact 状态应为"契约就绪/待接线"**：Inject/Project 无生产调用方，数据未流动；codec hash 覆盖全量字段，旧二进制 Decode 新数据会 ErrIntegrityMismatch（additive 兼容仅单方向）。已回写第二轮报告。
3. **Track A 的 TTL 占位语义是数据质量缺陷而非设计保留**：三子代理独立确认 IsSuccess=true 兜底写把限流请求变成假阳性；本轮已改为不写仅计数（905aeb954）。

## 审计结论摘要（按 13 项要求，未修项）

1. **流程/数据/反馈闭环**：主干通（classify→dispatch 汇聚→supplier_errors/candidate_failure→凭据详情）；auto 学习信号闭环全链核实贯通（决策 pending→三协议终态回填→routing_feedback_log→settle→export→重训）。**缺**：RoutingOutcomeStats 五计数器无导出点（orphan 激增不可见）；EmitRateLimited 不上报（现 TTL 不写假阳性，但限流请求的决策彻底无信号，可考虑显式 not_run 标记）。
2. **IR 定义**：四协议超集 + requestfact（Timeline/Attachments/Integrity/Metadata）完整；metadata 防泄漏有回归锁定。
3. **传输完整性**：活跃桥（anthropic_stream/anthropic_bridge/responses_bridge/native_responses）核无 6ebd5f30c 同类双前缀；Responses 上游对非 Responses 客户端显式 501 无静默串协议。**缺**：活跃 OpenAI→Anthropic 桥 message_start.usage.input_tokens 恒 0（Claude Code 按此管上下文，系统性低估）；非流式 IR 的 stop_sequence 往返仍有损（InternalResponse 无承载）。
4. **IR 生命周期**：流式增量填充与落库一致性核实；session 持久化往返有 recoverIRRaw 无损 envelope。
5. **多轮双向解析/逐轮摘要**：digest 链核实（写侧 compact→backfill job→读侧单视图）；本轮修窗口减半后覆盖面恢复 10 轮。
6. **队列/并发/安全**：pending 注册表锁纪律主体成立（本轮补 O(1) 删除）；队列分层/关停排水/attempt recover/exactly-once 核实通过。**缺**：~60 处 fire-and-forget goroutine 无 recover（本轮修 BaseWorker+health_tracker 两类高频面，其余按频率分批）；outbox/janitor 类退化路径已修。
7. **网络/密钥**：EPIPE 方向分桶、billing 免罚、凭据轮换在途结算、DecryptCache 有界、quota 401/403 主动失效均核实；probe 退避索引封顶链无溢出。
8. **可观测/UI**：request_flow 三事件接线核实（低基数、无敏感字段）；**缺**：anthropic/native-responses 协议的 empty_response failover 无埋点（与 chat 不对等）；前端不消费 X-Gateway-Failover-Notice 头。UI 统一性：时间格式化 ≥10 处、反馈条 ≥8 处、徽标 9+ 文件未收敛（ErrorDetailTab 本轮接入共享词表是模板）；菜单「数据运维」组 13 项混杂、「模型与路由」标注双入口。
9. **分区存储**：688 十函数逐字仅差 DEFAULT、Go 调度值一一对齐、installer 5 点同步核实；16 表 promote 单语句 CTE+SKIP LOCKED+FNV-1a advisory 锁、父表写路径仅 2 处合法例外。**P1 缺口**：opslog_trimmer 对 candidate_failure_logs 的行级 DELETE 打在 columnar 分区必败（562 已明确 columnar append-only），7d TTL 双路径失效（行删失败被吞 + drop 侧取各表 TTL 的 max 且 floor 30d）；ensure_candidate_failure_logs_partition 存在空体 stub 地雷。**需独立批次**（迁移+夹具库验证）。
10. **双模式**：lite 装配完整、四 store 桥接核实。**#8 维持**：降级面未文档化、启动日志无清单（PG-only：session_summaries/计费链/学习信号/凭据健康/settle/ASM outbox/多数 admin 端点；Redis 其余消费者各自静默禁用）。MemoryStateStore 无容量上限（未接线，潜伏）。
11. **auto 全量**：决策/执行/转移/记录/导出/重排全链贯通，置信度贯通核实。
12. **附件/媒体**：**缺**：文件侧 LRU/TTL 清理与 DB 元数据双向 dangling（无对账 job；columnar 不可 UPDATE，建议 append-only 审计行 + 定期 diff）。
13. **结构清理(#11)**：本轮删 2 死文件 + 2 处 Deprecated 标注。余：handler.go(8982 行) 身份块平移 identity.go、scoring_new 并入 scoring.go、bg 探测域 20+ 文件目录内分组、cache_reused 列落库（additive 低风险）、PreClassify 重复块抽 helper（中风险）。

## 验证

- `go build ./...` PASS；`go vet` 干净
- `go test -count=1`：autoroute（含 -race 新语义用例）/executors/streaming(72s)/hooks/audit/sessionsummary/dispatch/outbox/bg/quotafetcher/providercap 全绿
- 前端 vue-tsc PASS（pre-commit 6/6）

## 未修项（移交 handoff，按优先级）

1. **P1 candidate_failure_logs columnar DELETE 失效**（第 9 条）：迁移把分区转 heap（仿 562）或 trimmer 弃行删 + dropOldStatePartitions 按表 TTL；顺修 ensure 函数空体 stub。需夹具库验证。
2. **P1 IR 灰度路径接线/下线决策**（纠偏 1）+ SerializeAnthropic 补 message_start 前置/content_block 生命周期/signature_delta 透出。
3. **P1 message_start.input_tokens=0**（活跃 OpenAI→Anthropic 桥）：需流首 token 估算或文档化降级面。
4. **P2 session_summaries 无界增长（#6）**：推荐 TTL 方案（lifecycle.session_summaries_ttl_days + 分批 DELETE + (archived_at,last_request_at) 部分索引 + Archiver 升级为每日 bg 调度）；分区方案收益低不推荐。
5. **P2 lite 降级面文档化（#8）**：启动日志逐项 Warn + docs lite 降级矩阵。
6. **P2 fire-and-forget goroutine recover 分批**（~60 处，优先 handler:5942 anomaly 异步记录、credentialstate 4 处）。
7. **P2 RoutingOutcomeStats 导出**（prometheus counter 或 /admin 只读端点）。
8. **P2 requestfact builder 接线 + codec hash 版本化决策**（纠偏 2）。
9. **P2 附件/元数据对账 job**（append-only 审计行 + 台账 diff）。
10. **P2 UI 统一批次**：utils/datetime.ts 收敛 10 处时间格式化、useActionMessage 收敛 8 处反馈条、badge tone CSS 收敛 style.css、ProbeHealthPanel 手动触发改 per-credential API、菜单「数据运维」拆组、标注双入口合并 tab、前端消费 X-Gateway-Failover-Notice。
11. **P2 quotafetcher/probe 域整体换 safehttpclient**（DNS rebinding 完整防护，本轮预检之上）。
12. **P3 6xx 迁移 down.sql 惯约**：640/657/660 起 27 个迁移系统性缺 down，惯例已名存实亡——在 db-changelog 明文记录「660+ 不再提供 down」或补齐关键迁移（688 为 CREATE OR REPLACE 幂等，回滚=re-run 旧定义，风险低）。
