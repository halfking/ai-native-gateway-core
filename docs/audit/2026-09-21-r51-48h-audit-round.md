# R51 48h 审计轮 —— 2026-09-21

- 基线：`119981c02`（audit/154-delivery-disk-20260921，与 origin/main 同步）
- 窗口：48h 内 40+ 提交，主线为 R50 收口、CN canonical fold、count_tokens 端点、会话存储解耦 v3（733/734）、reqprobe+paramledger、taskprofile、响应侧扩展过滤（/v1/messages usage 全 0 根修）。
- 方式：主代理汇总 + 7 个只读子代理并行审计（transformation/modelname 域、存储 v3 写入链、reqprobe/paramledger/taskprofile、R50 遗留、provenance+压缩、队列/限流/错误链、分区/双存储/auto/代理），证据均到 file:line。
- 审计通过的域（不再展开）：三层 provenance 链（identity/occurrence 映射、V2 metadata 生产化、压缩三层 AlignmentMap、CutMarker pre_sanitize_offset_range）；4xx 上下文超长五级恢复梯子（压缩在流开始前、免预算重试、仍超长才透传）；队列分层与 Governor 限流解耦、权重全 0 除零防护；供应商错误链（candidate_failure_logs → ProviderErrorAggregator → provider_error_details → 凭据详情）；failover 透明（thinking SSE 注释）；733/734 入 promoteSpecs 且 8h 通道统一；auto 模型全链路（含 count_tokens 不解析 model 的启发式路径）；Lite 对等（session_turn_details SQLite 表+视图）；schema_migrations 730/733 双通道注册闭合（通道分治 by design，见 F30）。

## 一、发现清单

### P1（本轮必修）

| # | 状态 | 发现 | 证据 | 修复方向 |
|---|------|------|------|---------|
| R51-F1 | 新增(48h) | paramledger 非流式回显还原在 Content-Length 定长**之后**改写响应体，长度变化时响应被截断/拒写——grok x-high 非流式闭环主场景必现；e2e 用 httptest.Recorder 不校验长度未暴露 | domains/streaming/executors/executor_chat.go:1560-1566 | 先 restore 再按改写后长度 copyHeaders；补真实 http.Server 校验测试 |
| R51-F2 | 存量 | HK 规避可被节点命名绕过：parser 只配整词 HONGKONG，"Hong Kong xx" 分词为 HONG/KONG 不命中 → location=""→ IsRegionBanned("") 放行；且默认 HK 禁用仅在 CreateSubscription 注入，存量订阅不回填 | proxy/parser.go:549-597、proxy/types.go:216-220、admin/proxy.go:385-390 | parser 补 phrase 匹配（HONG KONG 相邻双词）；全局默认禁用区 overlay（settings 可配，默认 HK），存量订阅天然覆盖 |
| R51-F3 | 新增(48h) | S4 停写门控只接 Lite：`storage.request_logs_write_enabled=false` 仅 lite_telemetry_sink 生效，Full 链路 telemetry 写 request_logs 无门控，开关在 Full 部署为静默 no-op | cmd/gateway/lite_telemetry_sink.go:128（全仓唯一接线点） | Full 写路径接同一开关（settings/spec_storage.go:118 语义为准），补双模式测试 |
| R51-F4 | 存量(R50顺延) | 归一化唯一索引未落：`uq_models_canonical_active_folded_name` 被 6 组 fold 重复对阻塞，迁移不存在；cleanup 脚本已备可重入但缺「执行→验证→建索引」闭环 | modelname/canonical_dedup.go:48、sql/fixes/2026-09-20-canonical-dedup-cleanup.sql | 735 迁移 = dedup（merge_map 与脚本同步）+ 建唯一索引；本地真库先演练 |
| R51-F5 | 存量(154/245 coverage) | ursm_node_snapshot_min（2083 万行）脚本无 SAVEPOINT、无并发索引，UPDATE 超时单点回滚 | sql/fixes/ 下 ursm 相关脚本 | 脚本加固：SAVEPOINT 包裹 + CREATE INDEX CONCURRENTLY 拆段 |
| R51-F32 | 存量(约2026-07引入) | **执行期新发现**：admin/telemetry.go persistRequestLog 的 request_logs_hot INSERT 列清单 37 列、VALUES 仅 34 表达式（stream_interrupted/stream_chunks_sent/upstream_finish_reason 三列缺失）——`INSERT has more target columns than expressions`，/api/telemetry ingest 路径每次执行必失败约两个月；client.go 侧 2026-08-25 事故后已有同型守卫，admin 侧无守卫所以漏网 | admin/telemetry.go:355-386 | 补 $34-$36；新增 admin 侧占位符对齐守卫测试（镜像 telemetry 包 8-25 守卫） |

### P2（本轮应修）

| # | 状态 | 发现 | 证据 |
|---|------|------|------|
| R51-F6 | 新增(48h) | Fix B 覆盖不全：3 处 model_aliases upsert 无条件 `status='active'`，disabled/deprecated alias 会被 discovery tick 复活（alias_sync.go:265-269 已有守卫，三处漏） | discovery/discovery.go:818,976,1006 |
| R51-F7 | 存量(R50遗留) | admin pin 翻盘后 TierFailoverModels 头部错位（F14 同型）：梯子在 pin 前计算，PromotePins 后仅置 routingSource 不重建头部 | autoroute/decision_v2.go:296-341、role_llm_router.go:218 |
| R51-F8 | 新增(48h) | Lite journal 链 details 写点破坏重试幂等：UNIQUE(tenant_id,request_id) 不在 ON CONFLICT 目标内，重试撞唯一索引→永久失败；且写点在 markJournaled 前，失败重试重复轮 | cmd/gateway/lite_telemetry_sink.go:194-214、storage/sqlite/schema.go:78 |
| R51-F9 | 新增(48h) | installer StartupFiles 中 733/734 重复注册（两轮各自追加），全新安装各跑两遍 | installer/internal/dbinit/runner.go:204-215 |
| R51-F10 | 新增(48h) | 非流式 usage 兜底过窄：仅 in==0&&out==0 才补 input；usage 键整体缺失、或仅 input 缺失（output>0）时客户端 input 仍 0 | domains/streaming/messages.go:1341-1355 |
| R51-F11 | 新增(48h) | node_probe 自愈退避不随健康运行重置，累计几次即永久 5min 间隔；重启无计数指标 | bg/node_probe.go:667-681 |
| R51-F12 | 新增(48h) | paramledger 回显还原为全 buffer 字节级 ReplaceAll，模型输出文本恰含同形 JSON 串时被误改写 | internal/paramledger/ledger.go:188-209 |
| R51-F13 | 存量 | bg panic 防护收口名不副实：credential_selfcheck/active_probe_worker/apihub_watcher/feedback_analyzer/credential_autoheal/concurrency_auto_scaleup 仍裸 go；BaseWorker recover 后永久退出不重启 | bg/credential_selfcheck.go:157、bg/active_probe_worker.go:200、bg/base_worker.go:98-110 |
| R51-F14 | 新增(48h) | container 入响应标准字段集但 IR 响应侧不解析 container → 响应字段静默丢弃（code-execution beta 场景） | domains/transformation/fields.go:93、internal/ir/serialize_anthropic.go:192 |
| R51-F15 | 新增(48h) | reqprobe 两次免费重试（参数剔除+模式回退）在凭据 attempt 内自旋不重新过 Governor Acquire，一次准入最多 3 次上游调用，限流计量失真（与既有恢复机制同型） | domains/streaming/executors/executor_chat.go:462,514 |

### P3（择优小修 / 登记）

| # | 状态 | 发现 | 处置 |
|---|------|------|------|
| R51-F16 | 新增 | taskprofile CSV 错误列号错位（报 column 3/4 实为 6/7）；列4/列8 超限静默吞 | 本轮小修 |
| R51-F17 | 存量(F21) | deploy-local.sh cache-from 引用正在创建的同版本 tag，对新版本 no-op | 本轮一行修 |
| R51-F18 | 存量 | hotTableTSColumn 白名单含三张不存在表（harness_hot/session_audit_logs_hot/probe_feedback_hot），gauge 恒失败 | 本轮删条目 |
| R51-F19 | 存量 | promote 周期预算全表共享计数，大表积压饿死后段表 | 本轮：每表保底 1 批 |
| R51-F20 | 新增 | 733 promote 的 DELETE..RETURNING→INSERT..DO NOTHING 与回填并发可静默丢行 | 本轮：DO UPDATE |
| R51-F21 | 新增 | reqprobe 学习规则缓存无 singleflight，60s 到期瞬间并发打 Redis 全表读 | 本轮小修 |
| R51-F22 | 存量 | settings StoreDB.Get 用无超时 context.Background()，DB 挂起时 sweep 无限阻塞 | 本轮小修 |
| R51-F23 | 新增 | isStandardResponseField 精确匹配无归一化，异常大小写厂商字段漏过滤 | 本轮小修 |
| R51-F24 | 登记顺延 | sanitize offset 降级窗口撞号（R35 已知残余）；stateMu 全局串行；锁 TTL 5s | 下轮专项 |
| R51-F25 | 登记顺延 | F18 探针归因 origin 列 + parked-since 徽标 | 下轮 |
| R51-F26 | 登记顺延 | count_tokens 无审计/参数账本/metrics/独立频控 | 与 F25 同批 |
| R51-F27 | 登记顺延 | paramledger Lite/无 redis 时 mirror=nil，多实例回显还原跨实例不可见 | 下轮 |
| R51-F28 | 存量 | firstHopLotteryWeights sticky/recency 快照未接（R48 遗留，非热点） | 维持登记 |
| R51-F29 | 设计确认 | mid-stream error chunk 透传（字节已发出不可重放）；stripCNSuffix 大小写敏感（设计接受） | 关闭 |
| R51-F30 | 设计确认 | schema_migrations 730/733 无内联注册行：通道分治 by design（apply-db-revision-sequence.sh + installer 双注册在位，apply-missing-migrations.sh 可自愈），避免 ensure 通道被整文件跳过 | 补文档说明，不改代码 |
| R51-F31 | 存量 | StorageDecoupling v3 未完事项（S3 视图切换、S4 前提三条、缺源 4 列接线） | 维持 v3 方案跟踪 |

## 二、本轮执行（交付记录）

1. **P1×6 全修**：
   - F1（paramledger 时序）：restore+redact 提前到定长/WriteHeader 之前；5 处调用点核查（chat/anthropic 本就正确，仅 responses 路径错）；`TestParamLedgerNativeResponsesRestoreContentLengthConsistent`（真实 httptest.Server，修复前复跑 FAIL 证实可捕获）。
   - F2（HK 绕过）：locationRules 增 Phrases 字段（HK="HONG KONG"，相邻词组匹配兼容 Hong.Kong/hong_kong/多空格）；`proxy.default_banned_regions`（settings 默认 "HK"，HotReload）在选择路径 overlay 并集；IsRegionBanned 改 variadic。测试 3 组全过。
   - F3（S4 Full 门控）：telemetry client insert/update 六个包裹点 + admin ingest 门控；usage_ledger/会话家族/outbox 不受影响；4 个门控测试。
   - F4（735 迁移）：守卫（active 折叠重复对存在则报错指路 cleanup 脚本——CASCADE 引用族裁决不进启动迁移静默选 winner）+ 表达式唯一索引（与 DedupCanonicalNameSQL run-collapse 臂逐字一致，R50 handoff 明文规格）。五点同步（embeddata 副本/main.go embed+map/runner.go StartupFiles/revision-sequence 脚本/stats_migrations_test 双向对账）+ `migration_735_test.go`（C1 解析 Go 源合成期望表达式/C2 守卫/C3 幂等与 down 手术性/C4 embeddata 字节一致）。**真库演练**：本地 llm_gateway 库 6 组重复对 → cleanup 脚本 V1-V9 全绿（949→942）→ 735 应用成功 → 负例（脏数据 GUARD_FIRED / 索引 23505 拦截）双证 → 幂等重放 no-op → schema_migrations 注册。
   - F5（ursm 脚本加固）：cleanup 脚本 §0.5 并发索引（CONCURRENTLY + indisvalid 守卫）、§2.3 EXCEPTION 子事务包裹失败降级、头部执行前提/恢复说明、V9 事后留痕；顺带修 `::regclass` 强转在缺表库崩溃的隐患。本机 PG17 模拟表三路径演练全过。
   - F32（telemetry INSERT）：$34-$36 补齐 + `TestPersistRequestLogPlaceholderAlignment` 守卫（解析 telemetry.go 本身，表达式数=列数、占位符语义对齐、无未引用参数）。
2. **P2 修 8 件**：F6 alias 守卫三处对齐 alias_sync（`WHERE status<>'disabled'`）；F7 admin pin 翻盘 withRoleFailoverHead 置顶（照 R50 同构，修复前实测 FAIL 后 PASS）；F8 Lite details 幂等键改 (tenant_id,request_id) DO UPDATE；F9 installer 733/734 重复注册去重 + TestStartupFilesHaveNoDuplicates；F10 usage 兜底放宽（input==0 即补、usage 键缺失构造、非零永不覆盖）三用例；F11 node_probe 退避重置（连续 3 tick 健康回退 + 修 8min 超顶 bug + promauto counter）；F12 paramledger 结构感知还原（限定 reasoning 对象内首次出现，对象外零改动）；F13 六个裸 go worker 照 runRecovered 模式收口 + panic 测试（BaseWorker 自愈重构登记下轮）；F14 IR 响应侧 container 透传（Extensions 通道往返）。
3. **P3 修 7 件**：F16 CSV 列号真值+超限整行拒绝；F17 cache-from 固定缓存 tag + 成功后回打；F19 promote 每表保底 1 批；F20 733 promote DO UPDATE（733 未在任何环境应用，可安全编辑，installer 内嵌副本同步）；F21 学习规则缓存 singleflight；F22 StoreDB.Get 5s 超时；F23 isStandardResponseField 归一化；F18 hotTableTSColumn 删三张幽灵表。
4. **登记顺延**：F15（reqprobe 免费重试限流计量，机制既有扩展面）、F24（sanitize offset 降级窗口/stateMu/TTL）、F25（F18 归因列）、F26（count_tokens 观测面）、F27（paramledger Lite mirror）、F28（lottery 快照）、bg 剩余约 50 处裸 go（清单见 worker panic 报告）、727/730 契约测试、BaseWorker 自愈重构。
5. **验证**：`go build ./...` 全绿；全量 `go test ./...` + installer 两 module 契约测试；deploy-local 冒烟（见 §四）。

## 四、部署与发布状态

- 本地（llm_gateway 库）：对账已执行（949→942，V1-V9 绿）、735 已应用并注册 schema_migrations。
- 252 共享库：dedup 已于 154/245 coverage 轮清零（926 行）→ 735 下次部署窗口应用即可，无需再对账。
- 154/245：binary 落后（缺 match-first 守卫）仍待 ops 升级；升级后 735 随 installer 通道落地。
- R51-F30 裁决留档：schema_migrations 730/733 无内联注册行是通道分治 by design（apply-db-revision-sequence.sh + installer 双注册在位；apply-missing-migrations.sh 幂等自愈），不改代码。

## 三、遗留与下轮（R52）入口

- R51-F15/F24/F25/F26/F27 + BaseWorker 自愈重构 + 727/730 契约测试（726 模板）。
- 运维裁决（252）：approval pending / probe 占比查询；70/71/72 × glm-4-flash auth 失败绑定停用；154/245 binary 追平 ≥92f18cf22 后执行 unmapped-cn-rename.sql。
- StorageDecoupling v3：S3 视图切换与 S4 前提观测（mirror outbox 清零、dual_read 7 日零漂移）。
