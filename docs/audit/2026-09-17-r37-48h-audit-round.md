# R37 — 48h 审计轮（2026-09-17）

- 窗口：`876302d5e..67f78247c`（R36 轮之后 4 个未审计提交：59eb7fa06 prompt_too_large 413 三修、be24c65c9/e0f94a799 deploy-local cutover 修复、67f78247c no-nodes survival E2E + per-retry think reason enrichment）+ 遗留驱动检查（R35-R1/R2/R7/R8/R9）
- 轮号说明：任务书原指定 R36，但派发前 fetch 发现 **R36 已被并行会话完成**（ad68c91ac，含轮文档），按硬性纪律改编号 **R37**，遗留台账以 R36 §四 + R35 §四合并为准
- 方法：playbook 路线。codegraph 全量重建未成（`codegraph index` 子命令不存在，降级 grep+git diff 追溯）+ 主代理 + 5 路只读子代理并行（D02 流式窗口面 / D14 X-Gw-* 拓扑核实 / D16 部署脚本 / D17 卫生定点核实 / D11 灰度口径事实枚举），每条 P1/P2 线索经主代理亲读复核后才处置；D14/D17 首派遇限流，后台重派成功

## 一、本批修复（P0×1 / P1×2 / P2×6 / P3 批，均为本会话提交）

### P0

| # | 发现 | 证据 | 处置 |
|---|---|---|---|
| P0-1 | **717 迁移在全新安装中止（R36 修复引入的回归）**：R36 把三份 baseline 的 request_logs_hot 手工对齐成母表列型，而 717 的 USING 表达式对列直接施加 text 算子——对已对齐列型即 42883 → 启动链中止。e0f94a799 提交信息已预警。本轮主代理独立复核成立并先以 `::text` 化 + 真库双态集成测试修复；**真库测试随即逮出第二颗雷**（R36 的 jsonb 预筛正则字符类 `[\[\{"0-9tfn-]` 放行 `not-json` 词首 → `::jsonb` 22P02 同样中止漂移库迁移） | sql/migrations/startup/717…sql（R36 原版）:34-64；deploy/sql/schemas/baseline/01-schema.sql:13417+ | **合并终态（与并行会话同日真库重写版对账后采纳远端）**：远端 R36 补位轮已用真库实测推翻单条 ALTER 方案——整批语句中任何被视图（经 pg_rewrite 传递）引用的列连类型相同的 no-op 都会 0A000 拒绝（680 视图链先于 717 存在）；其重写版=逐列子事务 + information_schema 列型守卫（对齐安装静默跳过）+ 逐列 feature_not_supported 豁免 + customer_id 子句整体撤销（574 已 owns text→bigint）。本轮保留并改写两份钉桩适配该版：TestMigration717ShapePins（结构不变量：禁复活 customer_id 子句/禁单条整批 ALTER/逐列 0A000 处理器≥7/列型守卫/收尾报告）+ TestMigration717HotColumnAlignmentBothColumnStates（漂移态=live facts 列型七列转换+保值；对齐态=no-op 保值；testcontainers 真库）。**登记遗留**：远端版 jsonb 预筛仍用字符类正则，22P02 残渣容忍声明不成立（本会话 fixture 实证）；因文件已上真库不再改动，若未来安装报 22P02 按"显式 true/false/null 交替"修补 |

### P1

| # | 发现 | 证据 | 处置 |
|---|---|---|---|
| P1-1 | **deploy-local Layer-2 防线无效**：be24c65c9 写 `LLM_GATEWAY_DB_BOOT_RETRY_SECONDS` 裸数字 `90`，网关 `time.ParseDuration("90")` 报 missing unit → 静默回退 20s 默认，incident 2114 的 cutover 兜底第三层实际不存在 | scripts/deploy-local-lib.sh:389 + cmd/gateway/main_helpers.go:93-105（ParseDuration→fallback） | 默认值改 `90s`；钉桩 scripts/deploy-local-lib-bootretry_test.sh（驱动真实 dl_write_env 断言产出的 env 值 + 禁止回归裸数字） |
| P1-2 | **deploy-252-gateway.sh `set +e` 永不恢复**：SSOT 注入段 `set +e`/`set +u` 后整脚本裸奔——构建失败/scp 失败/远端 systemd 安装失败/healthz 12 次全败任一发生都继续跑到 `VERIFY_RESULT=pass`，部署验证门形同虚设 | scripts/deploy-252-gateway.sh:42-44 | SSOT 校验后恢复 `set -euo pipefail`；同批密钥面三修：TEST_KEY 不再内插远端命令行（改 `--header @-` stdin）、env 写出前置 `umask 077`（DATABASE_URL/JWT_SECRET 落盘窗口）、scp -p、SECRET_KEY 与 CREDENTIAL_ENCRYPTION_KEY 拆两把独立钥、CORS * 注释钉死 dev-only 风险接受 |

### P2

| # | 发现 | 证据 | 处置 |
|---|---|---|---|
| P2-1 | **R35-R2 落地：auto 灰度口径混入网关合成轮**。goal audit 影子轮（use_autoroute_for_audit 默认 true + fallback_audit_model 默认 auto）与内部回环（auto-title/summary generator 的 X-Gw-Is-Auto 回环）都是真实 auto 决策但非用户流量：matched/stashed 抬升、title/summary 轮无 stash 每次生成多计 1 个 orphan、settle cohort p95/p75 基线被污染、tuning_signals 被影子轮写入 | autoroute/outcome_feedback.go:228-252；optimizer_bridge.go:188-199；handler.go:6181/6196；request_log_pipeline.go:1131；bg/auto_route_settle_worker.go:294-303/394-402；admin/work_types.go 四查询；settings/goal_specs.go:232-243 | **口径裁决（本轮决策）：灰度观测面与训练信号面剔除全部网关合成轮**——`autoroute.IsSyntheticActor`＝内部回环三 actor（auto-title-generator/auto-summary-generator/session-summary）+ `goal-` 前缀。实施：①决策时 recordFeedbackAsync 单 choke 点设门（stash 与 legacy 占位双路径同盖，防"只剔终端把 stash 变 expired"）；②RoutingOutcome 增 OriginActor 字段，ReportRoutingOutcome 在任何计数器之前丢弃合成终态；③settle baselines + mr LATERAL SQL 过滤（共享谓词 SQLExcludeSyntheticActors）；④work_types 四查询过滤；⑤tuning 发射门过滤；⑥mode_hook.go:793 task_type_hint 死参数删除（R35-R2 附带，接线与删除二选一裁决为删）。钉桩：TestIsSyntheticActor / TestSQLExcludeSyntheticActors / TestRecordFeedback_SyntheticRoundNeverStashes / TestReportRoutingOutcome_SyntheticOutcomeIgnored |
| P2-2 | **R35-R1 落地：X-Gw-* 信任头客户端可注入**。前置拓扑核实成立：auto-title/summary 回环默认恒 `loopback.GatewayBase()`＝127.0.0.1:自身端口直连本进程（逃生门 env LLM_GATEWAY_ENDPOINT 仓内部署面均未设置）；goal 影子轮纯进程内直调不经中间件 | admin/auto_title_generator.go:1051-1057；admin/auto_summary_generator.go:705-708；internal/loopback/loopback.go:35-45；middleware/requestid_mw.go:42-63；D14 子代理 §〇 | `internal/loopback` 新增 per-boot 32 字节随机 token（`X-Gw-Loopback-Token`，crypto/rand 失败即 panic fail-closed）+ `StripUntrustedCorrelationHeaders`（constant-time 比较；合法回环保三头、token 头恒删防泄漏；返回实剥清单供日志）；RequestIDMiddleware 剥离开关（Warn 带实剥头清单）；两个回环生产者带 token；executor.go:2115 直读点自动继承剥后语义零改动。钉桩：TestTokenStableAndUnforgeable / TestStripUntrustedCorrelationHeaders / TestRequestIDMiddleware_StripsUntrustedGatewayCorrelationHeaders / TestRequestIDMiddleware_KeepsLoopbackTokenedCorrelationHeaders。跨实例退化＝可观测回归（parent/origin 落 NULL+Warn），无安全回归 |
| P2-3 | dl_wait_pg_isready 三处失效：①"could not parse" 护栏死代码（sed -n 无匹配退出码 0，`! $(...)` 永不为真）——坏 DSN 带空组件硬探 90s/次（cutover 最多烧 6 分钟）；②probe_host 重映射死逻辑（仅 DL_DOCKER=1 赋值但只被 =0 分支读）；③:645 日志把函数名当变量恒显 `none` | scripts/deploy-local-lib.sh:632-639/640-641/645 | 护栏改用户/主机/端口/库四组件非空校验（密码允许空）；删死重映射（psql/TCP 按 DSN 原样 host 探测）；docker exec 分支要求容器名非空（DL_DB_MODE=external 不再误选容器）；容器名一次解析、日志如实描述"容器内视角"；/dev/tcp fallback 注释声明 TCP≠SQL 的局限 |
| P2-4 | **存量测试永久泄漏写信号量**：TestOutcomeBackfill_RespectsWriteBound 的占坑 goroutine `close(release)` 后只做 `<-release`、从不 `<-feedbackWriteSlots`——32 个槽位永久占用，其后**全包所有反馈写被静默 shed**（实测 dropped 恒 +4，autoroute 包内任何依赖 dispatch 反馈到达的新测试都会超时假失败） | autoroute/outcome_feedback_test.go:270-284 | filler 释放槽位；该测试此后才是首个受害者顺序问题的实证记录在案 |
| P2-5 | intentStore typed-nil 通道（D02#2）：main.go 具体指针注入接口成 typed-nil，SessionIntentCache.SetRedisStore 无归一化，redisFallback 将在 nil receiver 上调 Get；当前被 fpSlotRedis!=nil 同分支门挡住不可达 | cmd/gateway/main.go:1232-1234/886-888；autoroute/session_intent_cache.go:129-134 | 同款反射归一化（59eb7fa06 executors/sticky.go 同类），防两处来源分叉后复活 252 同款 crash |
| P2-6 | routing/sticky.go 死代码孪生无 typed-nil 归一化（D02#1）：生产零调用但与被修的 executors 版逐字同构，复制粘贴复活即带回 252 同款 crash | domains/routing/sticky.go:76-80 | 同步归一化 + Dormant 副本警示注释指向 59eb7fa06 |

### P3

- py 场景脚本四修：readyz 子串误判（`b"ready" in not_ready` 恒匹配 → 精确 `"status":"ready"`）；str 分支补 `原因=` 提取（钉住本轮 reason enrichment 的 mock 层可见性，sanity 实测通过）；docstring 撒谎段重写（场景 C 并不存在、凭证禁用回退无实现）；no-op `--skip-budget` 删除
- survival E2E 卫生：noNodesExecutor.attempts 死字段删除（断言实走 exec.calls）+ 注释纠偏；用例 a 读窗口 floor 5s（retryInterval*15 在慢 CI+-race 下 1.2s 偏紧）
- **streamRead 数据竞争修复**（-race 实抓，窗口内 67f78247c 新增测试自身缺陷）：reader goroutine 与 String() 无锁共享 bytes.Buffer → 互斥锁保护
- R35-R7 清账：TestDefaultDispatchFollowUpAppliesAuthHeader 改名 TestInjectFollowUpSeamPropagatesAuthHeader + 注释如实（seam≠生产 dispatcher）；TestDefaultDispatchFollowUpHitsLiveServer 恒真测试改写为 TestBuildFollowUpRequestHitsLiveServer（真走 buildFollowUpRequest 上线）+ 新增 TestFollowUpDepthHeaderTracksRealDepth
- R35-R8 清账：X-Gw-Follow-Up-Depth 写真实深度（原硬编码 "1"，真深度链 handler.go:5189→helpers.go:206 是 1..15）；X-Gw-Follow-Up-Attempt 死头删除；followUpAuthCandidates/isFollowUpAuthFailure/authKindLabel 三助手整批删除（initial release 起零调用， Attempt 语义无交集已核）
- deploy-local-lib 卫生批（D17#13）：CORS 双写默认值不一致删首处；dl_prepare_layout no-op 死语句删除
- settings/goal_specs.go use_autoroute_for_audit 文案纠偏（code_audit→L1 分类器）+ 口径说明
- 窗口内触碰文件 gofmt 清零（存量 272 文件欠账单独登记，勿与功能修复混提交）

## 二、复核裁决（子代理线索 → 主代理亲读结论）

- D16#1（裸 90）→ **P1 成立**（亲读 main_helpers.go ParseDuration 实证）；D14#1（set +e）→ **P1 成立**（亲读全脚本）
- e0f94a799 提交信息自登记的 717 cast bug → **升级 P0**：主代理比对 baseline 实型后确认是"全新安装必挂"，且真库集成测试再逮出 22P02 第二雷
- D11 事实枚举全单采信（逐点亲读发射门/settle/work_types/配置默认）；口径裁决见 P2-1；2c/2d（selection 反连接+affinity）与 4c（649 视图+MV，涉新迁移 718）登记遗留
- D14 拓扑结论全单采信（关键点亲读：loopback.go 端口解析、两 generator 写头点、defaultDispatchFollowUp 直调、RequestIDMiddleware 位置）；goalrun_action_scheduler 亲读确认为死脚手架无 HTTP 路径，无需 token
- D02#3（E2E 读窗口）成立已修；D02#4（attempt_limit_exceeded 先于 retry_limit_exceeded、用例 c 依赖 stub 不耗预算）备案不改
- D17 R35-R7/R8 定性全单采信并实施；#11 liveactions 未用 emitter 顺手项从简跳过
- 复核中推翻/收窄：无（本轮五路线索全部成立或降为备案）

## 三、核实为健康的关键面

- 59eb7fa06 413 三修本体：typed-nil 归一化全覆盖三读点、liveactions 守卫恢复、413 链路端到端、双钉在位（D02 实跑验证）
- 67f78247c E2E 三用例走真实 ChatHandler+SurvivalCoordinator 路径、断连即时生效、预算上界确定性（NightMaxRetries 同值）；think reason enrichment 消费面安全（无生产解析依赖旧串）
- deploy-local cutover 回滚路径结构完整；LLM_GATEWAY_DB_BOOT_RETRY_SECONDS 作用域正确（不泄漏 245）
- goal 影子轮 actor 链路完整可对账；IsInternalAutoEntry 单一判定源三处共享；X-Gw-Is-Auto 刻意缺席镜像契约有注释+测试锚
- internal/trace/loopback 默认解析、admin postAdminLLMChat 不携三头、deploy-252 密钥 shred trap
- 窗口 4 提交中 be24c65c9/e0f94a799 的 Layer-3（重试 2→3+stagger）与可见性修复本体成立（缺陷在其声称的 Layer-2）

## 四、遗留登记

**新登记（均经亲读确认）**：
1. **P2 | settle selection/affinity 反连接**（D11 2c/2d）：auto_route_selections_hot 无 origin_actor 列，影子轮 selection 行仍会 settle 出 reward 并经 affinity 回灌；需 request_id 反连接 request_logs_hot 剔除 + 防未 settle 行永久堵塞批扫描（要配 settled 标记语义）。goal 当前默认关、154 未启用 goal，暂无实际污染
2. **P2 | admin audit KPI 视图扩投影**（D11 4c）：routing_analytics_source 不投影 origin_actor，MV 层过滤需迁移 718（扩视图+重建 MV，走 revision 通道 + installer 五点同步）
3. **P2 | 717 部署前置（承 R36 #2，范围缩小）**：全新安装双态已由本轮集成测试覆盖；252 生产执行 717 前仍须真库复核 information_schema（漂移态）；随后 dump-schema.sh 重导三 baseline
4. **P3 | TestDecideV2_RecommendModelHook_ReordersWinner 候选 flake**：SetGlobalFeatureFlagsForTest 全局旗标变更下 6 跑 2 挂（重载机器）；基线单跑未复现，待下轮定点
5. **P3 | gofmt 存量 272 文件**（R35-R9 扩容，D17 附录全清单）：按包批量机械提交，单独成 commit 勿混功能
6. **P3 批**：admin postAdminLLMChat 自调 URL 从 r.Host 构造（经 LB 绕公网，与 loopback 统一）；LLM_GATEWAY_ENDPOINT 跨实例时共享 token 的部署面方案；py 场景 D 断连仅 Go 层守护（py 补服务端冻结断言或明示）；DEFAULT_API_KEY 硬编码与 --interval 隐式耦合；liveactions 测试未用 emitter；deploy-252/seamless/local 三份构建回退提取共享 deploy-lib；cleanupSessionFollowUps 死接缝；D02#4 终态 reason 顺序备案（换真耗预算 stub 时用例 c 同步）
7. **P3 | 717 jsonb 预筛字符类正则 22P02 残留**（远端重写版继承 R36 正则）：漂移态 text 列若存在 n/t/f 词首的非 JSON 残渣（"not-json"）仍会在 ::jsonb 解析炸 22P02（本会话 fixture 实证其"容忍任意非 JSON 残渣"声明不成立）；文件已上真库不再改动，未来安装若报 22P02 按显式 `true|false|null` 交替修补（本会话 R37 提交内有现成形态）

**承继遗留状态**：R35-R1/R2 **本轮闭环**；R35-R7/R8 **本轮闭环**；R35-R9 部分闭环（窗口 1 文件，272 存量登记）；R35-R3/R4（goal 放量前决策）、R5（死脚手架裁决）、R6、R10 开放；R36 #3（credential_recovery 守卫+reviver 门控）、#4（716 全文等价守卫+down 补体）、#5（A-3 余面旧表直读）、#6（P3 批）开放；R34 #5/#7-#13 开放

## 五、测试与验证（真实实测，合并 origin/main 后终态复跑）

```
go build ./...                                                        # clean
go vet ./sql/migrations/startup/ ./internal/loopback/ ./middleware/ ./admin/ ./autoroute/ \
       ./bg/ ./domains/streaming/ ./domains/hooks/goal/ ./settings/ ./domains/routing/ ./cmd/gateway/  # clean
go vet -tags integration ./sql/migrations/startup/                    # clean
go test ./internal/loopback/ ./middleware/ ./autoroute/ ./settings/ ./domains/hooks/goal/ \
        ./sql/migrations/startup/ -count=1                            # 全绿
go test ./admin/ -count=1                                             # ok 66.1s
go test ./bg/ -count=1                                                # ok 7.3s
go test ./domains/streaming/ -count=1                                 # ok 71.3s
go test ./bg/ ./domains/streaming/ ./autoroute/ ./internal/loopback/ -race -count=1  # 全绿（-race 抓出并修复 streamRead 数据竞争）
go test ./autoroute/ -count=1 ×9 连跑 + -race                         # 全绿（信号量泄漏修复后）
installer: go build ./... + TestCanonicalStartupMigrationsAtOrAbove704AreRegistered PASS
go test -tags integration ./sql/migrations/startup/ -run TestMigration717HotColumnAlignment  # PASS（真库漂移/对齐双态，适配远端重写版）
bash scripts/deploy-local-lib-bootretry_test.sh                       # PASS（合并后复跑）
python3 ast.parse scenario_no_nodes_survival.py                       # PASS（远端重写版，"ready" 带引号检查正确）
具名钉桩（红绿）：
  TestMigration717ShapePins                               PASS（禁复活 customer_id 子句/单条整批 ALTER/0A000 处理器≥7）
  TestMigration717HotColumnAlignmentBothColumnStates      PASS
  deploy-local-lib-bootretry_test.sh                      PASS（env 值=90s + 裸数字回归即红）
  TestTokenStableAndUnforgeable / TestStripUntrustedCorrelationHeaders                    PASS
  TestRequestIDMiddleware_StripsUntrustedGatewayCorrelationHeaders / …KeepsLoopbackTokened PASS
  TestIsSyntheticActor / TestSQLExcludeSyntheticActors                                    PASS
  TestRecordFeedback_SyntheticRoundNeverStashes / TestReportRoutingOutcome_SyntheticOutcomeIgnored PASS
  TestInjectFollowUpSeamPropagatesAuthHeader / TestBuildFollowUpRequestHitsLiveServer /
  TestFollowUpDepthHeaderTracksRealDepth                  PASS
  TestSurvivalNoNodes* ×3（-race）                         PASS
gofmt -l（本轮触碰文件）                                                # 清零
bash -n deploy-252-gateway.sh / deploy-local-lib.sh / deploy-local.sh  # 全过
```

**合并对账（origin/main 8125a06ad..13039a498，R36 补位轮三件套 + 245 报告入主）**：冲突 6 文件——717 迁移+installer 副本+py 场景脚本**采纳远端**（真库验证版；我的 ::text 中间形态被其文档列为 Rev2 弃用形态，我的两钉改写为钉远端结构不变量）；deploy-local-lib 手工并集（我的 90s/死护栏修/死重映射删/容器误选修 + 远端的 DL_PG_PREFLIGHT_REQUIRED 失败关闭门/die 回退；双方独立发现同一死护栏）；survival E2E 并集（我的 streamRead 互斥锁竞争修复 + 远端 attempts 死字段删除）；playbook CHANGELOG 双行并记。R35-R1/R2/R7/R8 与 deploy-252 修复为远端未触及的净增。

## 六、下一轮提示词（建议）

> 以 docs/audit/2026-09-17-r37-48h-audit-round.md §四 遗留为起点：首位 R36 #3（credential_recovery 守卫读冻结旧表 + BrokenProbeReviver 无门控）与 #4（716 双通道全文等价守卫 + down 补体）可同批小修；其次 R37 #1（settle selection 反连接，需配 settled 标记语义防批堵塞）与 #2（迁移 718：routing_analytics_source 扩 origin_actor 投影 + MV 重建——动迁移前 git fetch 核对远端编号（当前已至 717），触碰 installer 前先跑 TestCanonicalStartupMigrationsAtOrAbove704AreRegistered 与 apply-db-revision-sequence_test.sh 双门禁）。252 真库执行 717 前置复核仍开放（R37 #3）。TestDecideV2_RecommendModelHook_ReordersWinner flake 定点（R37 #4）。gofmt 272 存量按包机械批单独 commit。审计入口：docs/audit/playbook/orchestrator-prompt.md（下一轮 R38）。**教训回注**：R36 的 717 修复未在"对齐后 baseline"形态下执行过——迁移类修复必须双态（漂移存量/全新安装）真库验证，静态注册门禁不覆盖 USING 表达式的列型算子合法性。
