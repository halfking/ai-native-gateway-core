# R61 24h 审计轮（2026-09-24，R60 增量批判式复审 + 用户清单横切复验）

- 窗口：2026-09-23 05:01 → 2026-09-24 05:30（HEAD=8ce9da13c）。R59（8 域全量）与 R60
  （遗留收口）已覆盖至 9f7b0ea3f / 8ce9da13c，故本轮定位 = **R60 自身改动的批判式复审**
  （a3e2232e3 全部新代码）+ 用户标准清单在 HEAD 的横切复验 + 24h 文档一致性清扫。
- 执行形态：6 组并行子代理（S1 DDL 守卫 / S2 迁移743+协议归一 / S3 回调+镜像+异常报告 /
  S4 proxy+脚本卫生 / S5 横切清单复验 / S6 文档清扫），协调者汇总 + 当轮修复 + scratch 库实证。
- 入场：main ff-only 合并 ef87317c5→8ce9da13c（远端 2 提交，零冲突，push no-op）。
  codegraph 全量重建（143970 nodes / 255112 edges，增量构建再撞 malformed → rm -rf 重建解）。

## 一、R60 声明实证主结论

R60 的机械性声明**逐项实证成立**：20 别名 SQL↔Go 逐条一致、743 幂等 + fail-closed、
五点同步、doMessagesProbe 头与真实上游逐字对齐、proxy env 名逐字一致、守卫覆盖面
完整（无裸执行点残留）、synthetic 过滤判据构造不出真实用户误伤路径、dedup TTL 实现
与声明一致。DDL 守卫真库契约与 743 矩阵本轮已由协调者以一次性 scratch PG17 容器
**独立复验**（见 §四）。

声明打折扣项：
- **S2-P2-2**：S3-F3「理由留档于轮文档与 commit」——R60 轮文档全文无理由段落（留档落空）。
  本轮 §五 补档。裁决方向本身站得住：RecommendedProtocolForModel 全仓零生产调用
  （仅定义+测试），接线只会引入新行为变更（改 createProvider 语义），不接是保守正确解。
- **S1-P3-1**：计数口径失实——R59 说 26 POLICY+7 TRIGGER、R60 说 26+15；实测（全仓 grep
  排除 sql/ 与 installer/）= **31 条 POLICY 语句 / 23 个守卫调用点 / 17 个 TRIGGER 重建点**
  （+1 处 CREATE-once trigger db.go:1926 非重建点）。31/17 全部有守卫，覆盖面无遗漏，
  但数字口径必须以本条为准，避免后续轮按册清点误判遗漏。
- **S3-P3-4**：「advisory lock 噪声根除」表述过强——只除去 probe-kind 类（R51 实测 99.6%），
  self_check/system_health/manual 类无会话头流量仍合成日聚合会话走同一把
  pg_advisory_xact_lock，结构性根因未变（设计内在约束，登记观察）。

## 二、当轮修复（代码 8 项，全部带回归测试或真库实证）

| # | 级别 | 发现（出处组） | 修复 | 验证 |
|---|---|---|---|---|
| F1 | P2 | users 表 `ENABLE ROW LEVEL SECURITY` 仍每 boot 无条件执行——ALTER TABLE 即使 no-op 也排 ACCESS EXCLUSIVE，「ENABLE/FORCE 一并纳入」在 31 点位中此一处不成立（S1-P2-2） | ENABLE RLS 移入 rlsPoliciesCurrent miss 分支（该守卫已含 relrowsecurity 位检查，命中=RLS+policy 双到位） | scratch 真库契约绿（第二遍 ensure 零 DDL 含此点） |
| F2 | P2 | 契约测试对「守卫真的走了跳过路径」证据不足：契约2 快照比对在守卫全失效时依然 PASS（DROP+CREATE 同定义还原相同目录态），跳过路径仅 8 组采样间接背书（S1-P2-1） | 新增契约 2b：pgx TraceQueryStart 记录第二遍 ensure 全部语句，断言零 `CREATE/DROP POLICY`、零 `CREATE/DROP TRIGGER`；ensures 改方法表达式使 traced pass 复用同一清单 | 实跑绿；敏感性验证：临时令 triggersCurrent 返回 false → 契约红（列出 8 条重放语句）→ 恢复绿 |
| F3 | P2 | bg 探针族读面归一漏网：credential_probe_v2.go:795/1885、active_probe_executor.go:271/505、balance_floor_guard.go:980 仍裸 Resolve 原始 protocol——hourly 自动探针是 availability_state 的裁决者，legacy 别名行走错探针形态正是 vapeur 事故类（S2-P2-1/S5-F1） | 全部接 NormalizeProviderProtocol（前四处走 probeDescriptorFor 单点；balance 处内联归一、保留 catalogCode 传参）；admin/routing.go free-pool 测试探针同步三路分发（anthropic 用 x-api-key+messages 形状、responses 用 input 形状，openai 路径行为不变） | build+vet+admin/bg 全套件绿 |
| F4 | P2 | deliverOne 无差别吞掉 GetEvent 错误——瞬时 DB 故障把 recalled 回调降级成无包 legacy 形状、标记已投递、永不重试，§3.3 包投递契约静默破坏（S3-P2-1） | errors.Is(err, ErrNotFound) 才降级 ev=nil；其他错误 fail(retryable)。deliverOne 参数提取窄接口（callbackDeliveryStore/Deliverer，*Store/*Deliverer 天然满足）使分支可单测 | 新增 TestDeliverOneTransientEventErrorIsRetryable（ErrNotFound 照常投递 / 瞬时错误 retryable 且不触达 deliverer）双断言绿 |
| F5 | P3 | credential_keys_touch_updated_at() 函数体 CREATE OR REPLACE 被移入 trigger 守卫 else——trigger 不漂移则函数体漂移不自愈（R60 前每 boot 执行，行为面收窄）（S1-P3-3） | 函数体移出 else 无条件执行（CREATE OR REPLACE FUNCTION 不取表级锁，无烧点代价） | scratch 真库契约绿 |
| F6 | P3 | triggerCurrent 缺 nil 接收者自检（同文件四函数唯一没有的，潜伏）；noiseCastTypes 含 bpchar/character——char(n)→text 有尾随空格语义非机械 no-op，理论上可把语义漂移判等（S1-P3-2/P3-5） | 补 nil 检查；bpchar/character 移出 noise 集（现网 policy 只涉 TEXT/VARCHAR，行为不变、口径收紧） | 纯函数单测 10/10 绿 |
| F7 | P3 | 守卫 miss 静默——PG 升级改 ruleutils 渲染时全部守卫退化为每 boot 重放（烧点复发）无任何信号（S1-P3-4） | rlsFlagsCurrent/policiesCurrent/triggerCurrent 三叶子函数 miss 记 slog.Warn（表名+原因，含 absent/drift/parse 分类）；复合函数不重复记。首 boot 全量 miss 属预期，措辞含 "replaying" 自解释 | scratch 真库实跑：契约3 注入漂移时 Warn 精确报出 `definition drift <对象名>` |
| F8 | P3 | requestID plumb 只接了 anthropic 面——serialize_openai 15 处 / gemini 5 处 / responses 11 处 report* 仍打 "unknown"；executor 侧 stamp 无直接回归测试（S3-P3-3/S4-P3-2） | 新增 stampIRRequestID 助手（ir_request_stamp.go），executor_chat 三处 SerializeOpenAI（主/兜底/断路器）+ executor_anthropic 统一 stamp；钉桩测试三态（nil 载体/空 requestID/正常写入+保留既有 Metadata） | 新增 TestStampIRRequestID 绿；gemini/responses 请求侧序列化器现无生产调用点，parse 面 4 处登记 §六 |

proxy env 下界（S4-P3-1）：`envDurationOrDefault` 过小正值钳 30s 下界（同步循环体下
1ns 级间隔=对全部上游背靠背探活风暴）+ warn；新增钳制用例，proxy 全绿。
anomaly dedup 硬上界（S3-P3-2/S5-F2）：新增 anomalyDedupHardCap=65536，TTL 清扫后
仍超硬上界（异常风暴）整表清空，换确定内存上界；新增风暴测试（64 唯一 key、
threshold=4/hardCap=8 → map 压回 ≤9）。

## 三、文档清扫（R60/R59 窗口留档失实处，勘误注记就地标注）

| 发现（出处组） | 处置 |
|---|---|
| S6-F1 R60「R61 入口见 handoff（docs/handoff…）」落盘未兑现——docs/handoff 最新只到 r58 | 本轮 handoff 已落盘（docs/handoff/2026-09-24-r61-24h-audit-handoff.md），同型缺口闭合 |
| S6-F2/F10 T2 报告声称 `scenarios-T2-2026-09-23.log` ✅ 已落盘——从未提交且磁盘不存在；§9 自称已合并 memstats 但该节无数据 | T2 报告两处改 ❌ 并加 R61 勘误注记 |
| S6-F3 252 SQL 审计 §三A「当日迭代 9 提交」实为 11，表漏登 9ad0a7ce1/71eac22d8/4e7175f4d 三修 | 计数改 11 + 三行补登（标 R61 补登） |
| S6-F4 全面测试 README 归档路径指向不存在目录（docs/全面测试/48h-audit/reports/ 等），与 run-all.sh 恒写 tests/48h-audit 直接矛盾；「轮级 history/（待建）」已过时 | 三处路径改真 + 勘误注记 + 待建注记删除 |
| S6-F5/F8 T1 报告 scenario.go ✅（实为 .go.txt）、run-all.log ✅（未落盘）、§3.1 指针失配 | 三处加勘误注记 |
| S6-F7/S2-P3-1 revision-sequence 括注 725…742 滞后一版 | 改 725…743（脚本 SSOT 本已登记 743） |
| S6-F6 R59 §3.3 第 9 项 S8-F6（UA 副本 SSOT 化）在 R60 台账凭空消失 | 重新登记于 §六 |

## 四、验证（实跑）

- go build ./... ✓；go vet（db/ir/hostedtask/admin/bg/proxy/streaming 全链）零输出 ✓
- 纯单测全绿：db（含守卫 10/10）/ internal/ir（含 dedup TTL+硬上界）/ hostedtask（含新
  deliverOne 双断言）/ proxy（含 30s 钳制用例）/ streaming+executors（含 stamp 钉桩）/
  admin（67s）/ bg
- **真库契约（一次性 docker PG17 scratch，端口 55432，用后即焚）**：
  - TestDDLSkipGuardsAgainstRealPostgres ✓（含新契约 2b 零 DDL 断言）；
    敏感性验证回退红/恢复绿 ✓（本轮纪律执行）
  - 迁移 743 矩阵独立复验：最小夹具 14 行脏值 → up 归一正确（含空格脏值、未知值
    azure-custom 保留）→ up×2 幂等 ✓ → down 回污符合文件头自述 → re-up 复愈 ✓
- 子代理纪律：本轮全部子代理禁连库（吸取 R60 B 组 TEST_PG_URL 误指共享 dev 库教训），
  库级验证收敛到协调者 scratch 容器单点执行。

## 五、S3-F3（家族推荐不接线）裁决理由补档

R60 裁决「三候选点均不接线」但未留理由，本轮补档：①RecommendedProtocolForModel
全仓零生产调用（仅 provider/catalog/protocol_normalize.go 定义 + 测试），接线=引入
无先例的行为变更；②唯一候选 createProvider 已有 RecommendedProtocolForBaseURL
回退（admin/providers.go:830，8a0bd5c95 引入），再叠家族推荐会让「同一 baseURL 不同
模型家族」的 provider 创建结果不稳定；③读面归一+743 清洗已把误判面封到可忽略，
再加一层推荐不解决新问题。结论：维持不接线，表保留作 catalog 知识库。

## 六、登记不处置（后续轮次入口）

1. **读面归一剩余点位（P2→P3，R61 已收口可用性裁决面）**：admin 展示/诊断面仍有
   ~7 处裸比较（provider_diagnose.go:160,453 恒 chat 探针、provider_vendor.go:102,262,530、
   providers.go:223,288、node_operations.go:219、provider_credential_balance.go:79、
   providerprofile/adapters.go:73、cmd/probe-cred/main.go:105）——availability_state 裁决面
   本轮已全接，剩余仅影响诊断展示口径；建议随 743 存量清洗自然消化，或与
   provider_diagnose 协议分支一并做（依赖 doMessagesProbe 已就绪）。
2. **parse 面 requestID plumb（P3）**：parse_openai/anthropic/gemini/responses 4 处 +
   extensions_restore.go:107 仍打 unknown；requestID 在 handler 侧可得，需签名下传，
   与 serialize 面不同（无现成载体）。
3. **hostedtask store 并发形状（P3，既有非回归）**：RecordProgress/RecordDispatchDegraded/
   RecordCallbackOutcome 追加事件无行锁（并发同算 MAX(seq)+1 靠 PK 兜底，事件丢失靠
   重投自愈）；ClaimDueCallbacks 认领事务从不 Commit（单 deliverer goroutine 假设兜底）。
   多 worker 化前必须修。
4. **S8-F6（P3，重新登记）**：UA 关键词匹配真重复 2 份（client_fingerprint/executor）、
   客户端类型封闭集枚举 3 份（credentialquota/sessionmeta 已同步，第三份待确认）——SSOT 化专项。
5. **btrim vs TrimSpace 漂移（P3）**：743 SQL 用 btrim（ASCII 空格），Go 用
   strings.TrimSpace（Unicode 空白）——带 \t\n 的存量值 SQL 不命中且验证同式自洽盲区。
   写边界已封，仅登记。
6. **743.down 运维注记（P3）**：down 把全表 canonical 行反向污染为代表性脏值（含升级前
   本就干净的行），文件头已自述仅测试/回滚演练用——生产回滚前必须人工快照。
7. **doMessagesProbe warning 无消费面（P3 观察）**：探针失败只置 health_status warning
   不触 availability_state（设计如此），持续失败无告警只能人看 UI。
8. **S7-4（P3）节点状态单模块架构专项**（R59 遗留延续）。
9. **清理候选 C1-C5**（domains/session/preprocess、adapter/unified、internal/fsstore、
   domains/nodestatecache、WeightedRouter 集群）——import 级零引用已核实，待 owner 确认。
10. **例行部署**：R59+R60+R61 尚未部署至 154/245（R60 §四①）；今日 20:52 automation
    复核产物核对（R60 §四②）。

## 七、下一轮（R62）提示词

见 docs/handoff/2026-09-24-r61-24h-audit-handoff.md。

## 八、引用

- 6 份子代理分组报告（S1-S6，本会话最终输出存档）
- R59/R60 轮文档、252 SQL 审计 §三A、T1/T2 全面测试报告（本轮勘误对象）
- 修复 commit：见 git log（本轮收口提交）
