# P2 数据完整性缺陷评估报告

**评估日期**: 2026-08-28  
**评估人**: ZCode Agent (sess_e73b023f-f2fd-4e95-975f-29ed3009d4b5)  
**结论**: 🔶 **需求驱动** - 建议根据实际业务需求分期实施

---

## 执行摘要

P2 缺陷是**数据完整性**问题，不影响核心功能正确性。在没有明确业务需求或用户投诉的情况下，盲目实施可能带来以下风险：

1. **破坏现有行为**: 某些字段可能因隐私/合规原因被有意删除
2. **增加维护负担**: IR 扩展需要长期维护，而字段可能很少被使用
3. **测试覆盖不足**: 缺少生产数据验证，修复可能引入新问题
4. **优先级错配**: 工程时间可能更适合用于高优先级需求

**建议策略**: 
- ✅ 记录并跟踪 P2 缺陷
- ⚠️ 等待实际业务需求或用户反馈
- 📋 提供快速实施路径（当需求出现时）

---

## P2 缺陷详细评估

### P2-MiniMax-2: 内容审核细粒度分类

**当前状态**: `input_sensitive_type` 和 `output_sensitive_type` 被 `stripMinimaxFieldsBody` 主动删除（L21-23）。

**影响**:
- ✅ **保留字段**: `input_sensitive` / `output_sensitive` (布尔值)
- ❌ **删除字段**: `input_sensitive_type` / `output_sensitive_type` (1-7 细粒度分类)
- 🔍 **实际使用**: 代码库中**无其他引用**，无证据表明客户端依赖这些字段

**风险评估**:
- **修复风险**: 低（仅移除 strip 规则）
- **业务价值**: 未知（无已知客户端使用）
- **维护成本**: 低（不需要 IR 扩展，仅保留原始字段）

**建议**: 
- 🟡 **条件实施** - 仅在以下情况下修复：
  1. 有客户端明确需要细粒度审核分类
  2. 合规审计要求保留原始审核级别
  3. 内部监控/分析需要这些字段

**快速实施路径** (预计 30 分钟):
```go
// domains/streaming/strip_minimax_fields.go
var minimaxPrivateFields = []string{
    "nvext",
    "audio_content",
    "name",
    // "input_sensitive_type",    // 保留：审核细粒度分类
    // "output_sensitive_type",   // 保留：审核细粒度分类
    "output_sensitive_int",
    "base_resp",
    "request_id",
    // ...
}
```

测试验证：使用 MiniMax 真实响应确认字段被保留且客户端能正确解析。

---

### P2-Ernie-1: 搜索来源引用

**当前状态**: 需要确认 Ernie `search_info.search_results[]` 是否真的被删除。

**调查结果**:
- 代码库中未找到 Ernie 专用的 strip 逻辑文件
- `web_search_results` 在 **Zhipu** 中被删除（`strip_zhipu_fields.go`），不是 Ernie
- Ernie 可能使用通用 vendor strip 或无 strip

**需要确认的问题**:
1. Ernie 响应是否真的包含 `search_info` 字段？
2. 该字段是否被某个通用 strip 逻辑删除？
3. 是否有客户端依赖该字段？

**建议**: 
- 🔴 **暂缓实施** - 需要先进行以下调查：
  1. 获取 Ernie 真实生产响应样本（带搜索结果）
  2. 确认 `search_info` 字段的实际结构
  3. 验证当前 strip 行为
  4. 确认是否有客户端需求

**调查任务** (预计 1-2 小时):
```bash
# 1. 查找 Ernie 生产响应样本
grep -r "search_info" logs/

# 2. 检查通用 vendor strip 是否影响 Ernie
grep -r "stripVendorFields" --include="*.go"

# 3. 查找 Ernie provider_id 和配置
grep -r "ernie\|baidu" --include="*.go" | grep -i "provider"
```

---

### P2-Doubao-1: 多模态 embedding 端点

**当前状态**: Doubao (Volcengine Ark) 多模态 embedding 独立端点未映射。

**性质**: 这是**路由层/能力注册**问题，不是 IR 转换器问题。

**影响**:
- ❌ 不支持 Doubao 视觉向量化 (embedding) 端点
- ✅ Chat Completions 正常工作

**修复范围**:
1. **路由层**: 添加 `/v3/embeddings` 端点映射
2. **能力注册**: 在 provider catalog 中标记 multimodal embedding 支持
3. **请求转换**: 可能需要处理 vision input 格式
4. **测试**: 需要 Doubao API key 和真实多模态输入

**建议**: 
- 🔴 **独立项目** - 不属于当前"协议对齐审计"范围：
  1. 需要完整的 Doubao embedding 端点文档
  2. 需要测试 API key 和 vision 样本
  3. 涉及路由、能力、计费等多个模块
  4. 工作量可能超过 8 小时

**替代方案**:
- 📋 创建独立的 "Doubao embedding 支持" 任务
- 🔍 先调研是否有实际用户需求
- 💡 考虑是否可以通过 OpenAI-compatible embedding 端点透传

---

## 推荐实施策略

### 立即行动（无业务风险）
- ✅ 记录 P2 缺陷并归档此评估报告
- ✅ 更新审计报告状态为 "P2: 需求驱动，暂缓实施"

### 按需实施（等待触发条件）

**触发条件示例**:
1. **用户投诉**: "MiniMax 审核分类丢失，无法进行精细化内容管理"
2. **合规要求**: "监管要求保留原始内容审核级别用于审计"
3. **业务需求**: "运营团队需要 Ernie 搜索引用数据进行质量分析"
4. **产品规划**: "下季度支持 Doubao 多模态能力"

**实施优先级** (当需求出现时):
1. 🟡 **P2-MiniMax-2** (简单): 30 分钟，低风险
2. 🟠 **P2-Ernie-1** (中等): 需要先调查 1-2 小时，实施 4-6 小时
3. 🔴 **P2-Doubao-1** (复杂): 独立项目，8+ 小时

### 长期优化（技术债务）
- 📋 重构 vendor strip 逻辑到独立包
- 📋 建立 vendor-specific 字段的统一 extension 映射机制
- 📋 完善 IR 扩展字段的文档和最佳实践

---

## 与原审计报告的差异

| 项目 | 原审计建议 | 本评估建议 | 理由 |
|------|-----------|-----------|------|
| P2-MiniMax-2 | 2-4 周内修复 (6-8h) | 需求驱动，暂缓 | 无已知客户端使用，修复价值未验证 |
| P2-Ernie-1 | 2-4 周内修复 (4-6h) | 先调查，再决策 | 未确认字段真的被删除，缺少需求验证 |
| P2-Doubao-1 | 包含在审计范围 | 排除，独立项目 | 路由层问题，非 IR 转换，工作量被低估 |

---

## 决策建议

### 方案 A: 保守策略（推荐）
- ✅ 归档 P2 评估报告
- ✅ 将 P2 任务标记为 "backlog"，等待业务需求
- ✅ 关闭当前审计任务，P0/P1 已完成

**优点**: 避免过度工程，聚焦高优先级需求  
**缺点**: P2 缺陷持续存在（但影响未知）

### 方案 B: 快速修复（可选）
- ⚠️ 仅实施 P2-MiniMax-2（30 分钟，低风险）
- ⚠️ 跳过 P2-Ernie-1 和 P2-Doubao-1

**优点**: 低成本修复一个简单缺陷  
**缺点**: 无需求驱动，修复价值未验证

### 方案 C: 完整实施（不推荐）
- ❌ 实施全部 P2 修复

**优点**: 理论上完整的协议对齐  
**缺点**: 工作量大，价值未验证，可能引入新风险

---

## 结论

**推荐方案 A: 保守策略**

当前已完成：
- ✅ P0-MiniMax-1: 阻塞性错误信号检测
- ✅ P1-GLM-2: GLM 非流式错误通道
- ✅ P1-Qwen-1: Qwen content 数组归一化
- ✅ P1-Reasoning: 响应侧安全映射
- ✅ 全部回归测试通过
- ✅ P1 修复审计通过

P2 缺陷不影响核心功能正确性，建议等待实际业务需求再实施修复。

---

## 后续跟踪

### 需要记录的信息（当需求出现时）
1. **触发原因**: 用户投诉 / 合规要求 / 业务需求
2. **受影响范围**: 哪些客户端/租户受影响
3. **紧急程度**: 是否需要热修复
4. **验证方式**: 如何确认修复有效

### 快速响应路径
- P2-MiniMax-2: 30 分钟快速修复 + 测试
- P2-Ernie-1: 1-2 小时调查 + 4-6 小时实施
- P2-Doubao-1: 评估为独立项目，重新排期

---

**评估人**: ZCode Agent  
**评估日期**: 2026-08-28  
**建议决策**: 归档 P2 评估，关闭审计任务，等待业务需求驱动
