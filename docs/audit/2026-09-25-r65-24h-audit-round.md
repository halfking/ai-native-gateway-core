# R65 24h 审计轮（2026-09-25）

## 头部元信息

- **窗口**：2026-09-24 05:00 → 2026-09-25 05:00（35 提交，含 5 个 merge）；HEAD = 9635b9b17（reportrollup 对账报表落地）。
- **轮次**：R65（用户指令"24 小时内的修正审计"；同时覆盖 R64 窗口终点后的死区——9635b9b17 落于 R64 收口提交 891542ff1 之后，为 R64 逃逸提交，本轮全额补审）。
- **合并前置**：本轮开工时本地分支落后 origin/main 一提交（9635b9b17），快进合并无冲突；审计与修复在该基线上进行。
- **子代理**：6 轨道并行只读（T1 reportrollup / T2 IR·协议·mock-probe / T3 installer·hostedtask·secret-scan / T4 auto·taskprofile·testbench / T5 横向不变量·安全·卫生 / T6 R64 遗留复核·文档一致性），原文留档 `docs/audit/playbook/runs/R65-2026-09-25/`。
- **修复执行**：主代理亲读复核全部关键发现后，主代理修 reportrollup/admin/db/scripts/注释面，2 个文件集互斥的修复子代理分别修 taskprofile 面与 web i18n 面。
- **下一可用迁移：747**（745/746/800 已占用）。

## §一 发现与处置

### 已修复（当轮 commit，P0→P1→P2→P3）

| 级别 | 发现 | 证据 | 修复 |
|---|---|---|---|
| **P1** | reportrollup internal_person 跨租户 UNIQUE 冲突→静默覆盖：GROUP BY (tenant,person) 但 scope_key=person，四键 UNIQUE 不含 tenant_id，跨租户同人（含双缺 unknown）日桶互相 DO UPDATE 覆盖 | rollup.go queryInternalPersonDay + 745 UNIQUE | scope_key 编码租户 `internalPersonScopeKey(tenant,person)=tenant+"\x00"+person`；读面 splitInternalPersonScopeKey 还原展示名（历史裸键原样回退）；真实库尚无 745/746 数据无迁移负担。钉桩：realdb E2E 改造为跨租户同人双桶断言 + 纯单测 TestInternalPersonScopeKey_CrossTenantDistinct |
| **P2** | taskprofile 回路C（修正/volume/回放）读冷父表 auto_route_selections，hot heap promotion 滞后期间（≥8h，异常 7 天）修正与流量对闸门不可见；annotation/testbench 均已用 `_all`，同数据三消费者两口径 | analyzer.go 4 处 + tuning-backtest/main.go | 5 处全部切 `auto_route_selections_all`，文件头口径注释声明与 annotation/testbench 同源 |
| **P2** | 9635b9b17 新页面 35 键 8 语言全缺，`npm run i18n:check` exit 1（R64 刚收紧的门禁被当轮提交打破） | ReconciliationReport.vue + 8 locale | 新增 `<lang>/reports.ts`（32 键）×8 + common 3 键×8（280 条文案，术语对齐各语言既有报表键）；i18n-audit 恢复 0 missing exit 0 |
| **P2** | internal 视角 + tenant_id 过滤时按天序列恒空（days 只在无过滤时填充，与 totals/tenants 不对称；SQL WHERE 已收窄无双计） | report.go ScopeInternalTenant case | 放宽为 `filter.Model == ""` 即填充；钉桩 TestBuildRangeReport_InternalTenantFilterKeepsDays |
| **P2** | 746 未登记 sequence 通道（dbinit+boot ensure 双臂齐但 sequence 缺席，违反五点同步；744/745 均三臂先例） | apply-db-revision-sequence.sh files 数组止于 745/800 | 补登 746（ALTER TYPE text::text 与 ADD COLUMN IF NOT EXISTS 均可重入，零风险）+ 自测钉桩列表加 746 |
| **P3** | report-rollup 三端点在无 DB 模式 nil pool panic 而非约定 503 | admin/report_rollup.go | 分发器入口 `h.db == nil` → 503（对齐 AuditTrimmer 先例；worker 面 T5 证实被 main.go dbConn 门控跳过无需修） |
| **P3** | export 缺 degraded 映射（summary 200 degraded vs export 500） | admin/report_rollup.go | export 复用同款 REPORT_SNAPSHOTS_NOT_MIGRATED 降级 |
| **P3** | /run 500 响应透传 err.Error()（可含 SQL/约束细节） | admin/report_rollup.go | 细节只进日志，响应改固定文案 |
| **P3** | daily_total 折叠行死写（与 queryTotalDay 同四键必被覆盖，注释失实，RowsWritten 虚高 1/轮） | rollup.go foldBuckets | 删除死写与 foldBuckets 函数，注释改为"daily_total 只此一份全流量口径" |
| **P3** | daily_by_model 的 '' 与 NULL 模型名撞键（投影 COALESCE 但 GROUP BY 保留 NULL 组） | rollup.go providerModelDaySQL | f CTE 内 `COALESCE(raw_model_name,'')`（internalModelDaySQL 原本已同款） |
| **P3** | xlsx 无 1,048,576 行上限守卫，超限产出 Excel 判损坏的文件 | xlsx.go | buildWorkbookBytes 显式校验超限报错（含收窄区间提示） |
| **P3** | qualityWidths 只数 Models 的 breakdown，表头 kinds 是三源并集→列宽错位；`_ = withProvider` 死参数 | workbook.go | 抽取 qualityKindTotals 共用数据源；删死参数 |
| **P3** | ensure 漏 746 三条 COMMENT ON COLUMN（仅走 boot ensure 的库 col_description 与迁移直跑库不一致） | db/db.go ensureReportSnapshots | 链后独立幂等 COMMENT 块（新建/升级/健康库三态全覆盖） |
| **P3** | keyword_add/weight_adjust apply 无 key 白名单 + keyword JSON 解析失败 fail-open 覆写参数行 | admin/auto_route_tuning.go | allowedKeywordKeys/allowedWeightKeys 白名单（keywords.reasoning/code/creative、weights.smart/speed_first/cost_first）+ 解析失败 fail-closed；3 个纯函数钉桩测试 |
| **P3** | BacktestKeywordHint 缺 classifier='heuristic' 过滤，回放证据混入 llm/jev 行虚高 | taskprofile/analyzer.go | 补同款过滤 |
| **P3** | rejectProposal 对不存在/非 pending 回 200（与 approve 404/409 不对称） | admin/auto_route_tuning.go | 改单事务 FOR UPDATE：404 admin_proposal_not_found / 409 admin_proposal_not_pending（真库分支待契约，admin 无 sqlmock 基建） |
| **P3** | deploy/prometheus .env.example 缺 1c0c200be 参数化出的 6 个 PG 变量，按模板 up 必连库失败 | deploy/prometheus/.env.example | 补 6 变量（取值对齐 compose 默认，注明本地栈改 llm-gateway-pg） |
| **P3** | report worker 隐式继承 !IsCredRecoveryDisabled() 门控未披露（CRED_RECOVERY_DISABLED=true 连带停对账日报） | cmd/gateway/main.go | 装配点注释披露 + 解耦路径提示（挪出条件块留待后续） |
| **P3** | ollama 流式 content 累计 vs 增量契约自相矛盾（CONTRACT 注释 vs 仓内 wire 文档，活体未裁决） | parse_ollama_stream.go vs docs/vendor-formats/ollama.md | CONTRACT 注释钉"存疑+P4 接线前必清"门（零生产调用方不可达；不盲改语义） |
| **P3** | 745 头注与 SSOT 口径漂移（scope 四值/canonical 粒度/person 裸键均为交付时点草稿） | sql/migrations/startup/745 + sql/objects/tables | 745 头注加 R65 勘误⑤（终态以 SSOT 为准）；SSOT 补 internal_person 编码租户口径；installer 745 副本 cp 同步 cmp 一致 |

### 登记不处置（遗留/裁决，按优先级）

| 级别 | 项 | 去向 |
|---|---|---|
| P2 | mock-probe 生产接入拍板（第三轮+1）：现装配 cmd/gateway-v2，生产零装配已证实；拍板接入时按 R63 装配四件 + settings 热开关评估 | R66 入口#1 |
| P2 | ollama content 契约活体裁决（抓 NDJSON 定 cumulative/incremental，同步 CONTRACT 注释与 wire 文档二取一改）——P4 selector 接线硬前置 | R66 入口#2（与 P4 接线绑定） |
| P3 | P4 selector 接线 + V800 boot ensure（下一可用迁移 747；形态对照 745 的 ensureReportSnapshots） | R66 入口 |
| P3 | auto P2/P3：TierSelector 生产接线、V3 影子（≥7 天）；闸门 classifier 口径是否扩 `heuristic_v2` 待查各环境 V2 flag 现值后拍板（R64 F5 采样面已白名单化 `llm_v2`，闸门面未同步） | auto v2 计划自身节奏 |
| P3 | threshold 只降不升且无 API 回滚通道（apply 允许精确回退 or manual 端点 or UI 明示） | auto 专项 |
| P3 | bg/feedback_analyzer weight_adjust 提案零去重 + `"new":20` 硬编码不读当前权重 | auto 专项 |
| P3 | task-profile 写端点（apply-tier-config/reload/corrections import）后端 super parity（R64 F16 有意前端兜底残留） | R66 候选 |
| P3 | testbench vs 包内回归 known_failure 语义分叉（当前 0 行，添加首行即打爆 baseline=1.0） | testbench 专项 |
| P3 | hostedtask 回调在途回写 vs 终态再入队竞态可丢召回回调（前置存在；RecordCallbackOutcome 回写无 event_id 守卫；ClaimDueCallbacks 从不 Commit 单 deliverer 假设）——修法=回写加 `AND event_id=$2` | callbacks 闭环后续轮 |
| P3 | usage_facts 无 occurred_at 前导索引且不进 partition_manager ensureSpecs（每日 rollup 全表扫线性退化；新消费方每业多扫一遍） | 迁移评审（需实跑） |
| P3 | report_snapshots 无 retention/分区（有意演进，量级 ~2k-2.2 万行/日；阈值：行/日>10 万 或 表>1-2GB 或 persons 过万时启动分区/TTL 演进） | 观察项 |
| P3 | reportrollup 多币种混算（SUM 跨币种 + MAX(currency)；单币种部署无症状）——设计文档补勘误或按币种分组 | 报表演进 |
| P3 | worker 与 /run 并发同日聚合可 40P01（ON CONFLICT 保证最终一致，可重跑；如需加固 pg_advisory_xact_lock(date)） | 可接受，登记 |
| P3 | DetectProtocol ollama 独占键单键即判 0.85 且先于 Responses（现实影响=遥测口径噪声） | IR 专项 |
| P3 | mockprobe 停机在途探测记假失败（classifyTransportError 不分 Canceled）；Probe 死字段零消费方 | mock-probe 拍板时一并 |
| P3 | secret-scan"解锁 github 推送"表述失真（strict 复跑余 1161 WARN 仍 block；0 BLOCK 属实）；1c0c200be #2-#6"扫描器不可见"规避形态记录为先例风险（内容全占位符无真凭据） | 下次同步 github 前拍板 baseline 策略 |
| P3 | SerializeOllama 多模态/tool_calls 静默丢（P4 前置 schema 闸）；F8 host-precise 收窄的子域识别面 | P4 接线包 |
| 登记 | R64 handoff §一 9 项逐项判定：#1/#2/#3/#5(三子项)/#6/#7/#8 全部仍遗留；#4 规划内推进；行号勘误 feedback_analyzer.go :252→:260；R61 §六.2 的 extensions_restore.go:107 锚点失效剔除 | 见 runs/R65 T6 报告 |

## §二 核实为健康的关键面（本轮六轨汇总，防下轮重复怀疑）

1. **对账报表全链**（T1/T5）：六 scope 聚合键与四键 UNIQUE 收敛（internal_person 破口已修）；SQL 全参数化无注入面；时区全钉 UTC；ON CONFLICT 幂等重跑不翻倍；jsonb `::text::jsonb` 绑定规避 22P02；panic recover+30min 超时+hour 双重钳制+HotReload；xlsx 转义/PartName/中文 sheet 全对；迁移 745/746 主仓与 installer 副本逐字节一致、ensure 双路径与 SSOT 终态一致；三端点 superAdmin + 区间钳 366 天 + 前端契约逐字段对齐。
2. **Mock 通道隔离**（T2）：mock 全生命周期不触 providers/api_keys/健康度/熔断/usage_facts/sticky；关闭态"五不"逐条证实 + e2e 钉桩；生产 cmd/gateway 零装配。
3. **origin 打标链路**（T2）：self-check 打标→origin_stage→traffic_class→internal 三 scope `AND traffic_class='business'` 端到端成立；daily_* 含探针是显式设计双口径（供应商对帐须全量）。
4. **hostedtask cancel 回调**（T3）：恰好一次入队成立（行锁 check-then-act + 终态 sticky + 同事务原子）；自动激活凭据面（0600 原子写/白名单/不进日志）健康；refresh.token 休眠判定仍成立。
5. **auto 闭环**（T4）：gate 口径 heuristic 统一 + keyword 三枚举休眠；advisory lock 并发面；阈值经在库路径不可调成非法值；testbench baseline 门禁 + 240 例 GATE PASS 复现；前端 8 语言键集/路由/门控全齐。
6. **横向不变量**（T5/T6）：迁移编号 74x/80x 对账全绿无撞号；lite 模式下新 worker/mockprobe 均安全降级；窗口内新增 HTTP client 超时/句柄面干净；i18n/testbench 两门禁机制在位；hotzone 方案文档与代码互洽（勿误判为漂移）。

## §三 测试与验证（实跑输出）

- `go build ./...`（主模块）exit 0；`go build ./...`（installer 模块）exit 0。
- `go vet ./domains/reportrollup/... ./admin/... ./db/... ./internal/ir/... ./taskprofile/... ./cmd/tuning-backtest/...` exit 0。
- `go test ./domains/reportrollup/... ./admin/ ./db/ ./internal/ir/... ./taskprofile/... -count=1` 全 ok（admin 66.6s）。
- `bash scripts/apply-db-revision-sequence_test.sh` → "contract passed"（含新钉 746）。
- `node scripts/i18n-audit.mjs` → "i18n audit PASS (0 missing keys)" exit 0。
- `npm run typecheck`（vue-tsc --noEmit）clean。
- installer `go test ./cmd/llm-gw-installer/ -run 'TestStartupFiles|TestStats|TestCanonical'` ok（含 745 副本 cmp 一致）。
- 未复跑：realdb E2E / ensure 真库测试（TEST_DATABASE_URL 门控，禁连库纪律）；已按新键格式更新断言并新增跨租户同人双桶钉桩，待下次 scratch 库轮实跑。

## §四 下一轮（R66）提示词建议

> 你是 llm-gateway-go 仓库（/Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go）的审计主代理，执行 R66 48h 审计轮。
> 1. git fetch 对齐 origin/main；窗口 `git log --since="48 hours ago"`；同时检查本轮收口提交之后的逃逸区段（R64 死区教训）。codegraph malformed 则 rm -rf .codegraph 全量重建。
> 2. 读 docs/audit/2026-09-25-r65-24h-audit-round.md §一登记项 + docs/audit/playbook/runs/R65-2026-09-25/ 六轨报告。
> 3. 优先入口：①mock-probe 生产接入拍板（第三轮挂账）；②ollama content 契约活体抓包裁决 + P4 selector 接线（serialize 多模态闸 + V800 ensure，下一迁移 747）；③usage_facts occurred_at 索引/分区评估（迁移须 scratch 实跑）；④R61 §六 1/2/3/4/5 与大专项候选三项复核；⑤auto P2/P3（TierSelector 接线、V3 影子观察）。
> 4. 关键上下文：下一可用迁移 747；reportrollup internal_person scope_key 已编码租户（tenant\x00person，读面 split 还原）——新读者必须走 splitInternalPersonScopeKey；i18n/testbench/sequence 门禁均已收紧，746 已补登 sequence；本仓 secret-scan strict 仍有 ~1161 WARN（github 同步通道待拍板 baseline 策略）。
