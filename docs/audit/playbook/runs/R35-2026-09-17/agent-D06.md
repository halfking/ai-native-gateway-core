# D06 双重存储架构 子代理报告（窗口：b9d9a8ba5..HEAD，核心提交 72d921819）

改动面：domains/streaming/handler.go、domains/streaming/response_interceptor_helpers.go(+test)（另含 goal/hooks/settings/cmd 文档性联动）。以下按 conventions.md §4 输出；所有行号为当前 HEAD 工作树。

## 一、发现（候选，待主代理复核）

| # | 级别候选 | 发现 | 证据 file:line | 建议处置 |
|---|---|---|---|---|
| 1 | P1 | **F1 修复（e8ddff41d，在窗口 BASE 内）使成功态 auto-title/auto-summary 回环行开始被镜像排除，但其 request_logs 行仍 claim is_final_success=TRUE → GLOBAL_G2 将恒 >0，S2→S4 停写 gate 7 天零漂移观察期被打破，且现有回填工具无法闭合该缺口。** 触发路径：auto-title 回环带 `X-Gw-Is-Auto:true`（admin/auto_title_generator.go:1057）→ F1 修复后成功终态 entry 现在携带 IsAutoRequest（domains/streaming/handler.go:5644-5657 propagateIsAutoRequestToEntry，修复前恒 nil → isInternalAutoEntry 短路为 false → 被镜像，这正是台账 Round1-4 中 gs_ 会话 v1≈v2 且 GLOBAL_G2=0 的原因）→ 镜像排除（internal/sessionv2mirror/hook.go:92-94 + 923-925 origin_actor 命中列表）→ 但 claim 无 is_auto 谓词（domains/hooks/observability/telemetry/client.go:2318-2323、2474-2488），gt_/gs_ 会话的成功行照常置 is_final_success=TRUE → GLOBAL_G2 口径（scripts/audit/storage_observation_round.sh:109-118，SQL 无任何 internal-auto 豁免）把这些行计为"终态缺 turns"。闭环验证：回填脚本不分青红皂白灌入（scripts/audit/mirror_outbox_backfill.sql:125-155 无 auto 排除），reaper 用同一 isInternalAutoEntry 门删行不写 turns（internal/sessionv2mirror/replay.go:293-297 "skipped: internal loopback"）→ 该类 G2 缺口永久存在（每日手动回填也治不好）。注意：此问题**非窗口提交 72d921819 引入**（它正确地没给影子轮打 X-Gw-Is-Auto），而是窗口 BASE 前 1 小时的 e8ddff41d 翻转了成功态 auto 行的镜像行为；最后一次 GLOBAL_G2=0 观测（09-16 09:03，build 5e3032d63/2122）是 F1 之前的二进制。 | handler.go:5657；hook.go:92-94,923-925；client.go:2318-2323,2474-2488；storage_observation_round.sh:109-118；replay.go:293-297；mirror_outbox_backfill.sql:135 | 主代理亲读复核后在 ledger 登记新例外类（如 E6"内部回环终态行"）或三选一：claim UPDATE 增加 is_auto_request 豁免 / GLOBAL_G2 SQL 排除 origin_actor 内部名单 / 镜像恢复纳入内部回环（回到 F1 前行为）。**09-17 每日轮将是首个 post-F1 观测点，优先跑一轮实测定量。** |
| 2 | P2 | **goal 影子轮会竞争业务会话的 is_final_success 单例标记**。影子轮回环走完整业务管道（response_interceptor_helpers.go:190-222 httptest+ServeHTTP），成功后与父轮同会话（buildFollowUpRequest 设 X-Gw-Session-Id=业务会话，helpers.go:231-232）→ shouldClaimFinalSuccess 命中（client.go:2318-2323）→ 影子轮可能抢走该会话唯一 is_final_success（migration 532 语义"本会话唯一最终成功"），admin/session_online.go 的终态标注会把网关合成的"请继续"轮标为会话最终成功而非用户真实末轮。触发路径：goal.enabled + auto_continue 触发 + 影子轮 100ms 延迟内父轮终态尚未落库（或父轮失败、影子轮成功）。窗口提交引入（72d921819 首次让影子轮进业务会话 turns）。 | response_interceptor_helpers.go:231-232；client.go:2474-2488；session_online.go:204,274 | 评估 claim 谓词是否应排除 origin_actor LIKE 'goal-%'（或在 session_online 读侧按 origin_actor 归位终态）；至少在方案文档 §3 登记该语义变化 |
| 3 | P3 | lite 会话 journal 无 isInternalAutoEntry 对等排除（lite_telemetry_sink.go:260-271 liteEntryTerminal 只判终态）——full 模式排除内部回环不出现在 session_turns，lite 模式会记轮。属既有已文档化分歧（"lite 不参与双写对账"，lite_telemetry_sink.go:17-21），窗口内未变化、goal 影子轮在 lite 不触发（见健康面#2），仅登记口径差。 | lite_telemetry_sink.go:17-21,260-271 | 保持登记，无需动作 |

## 二、核实为健康的面

- **commit 核心声称（影子轮不打 X-Gw-Is-Auto 保 G2）——成立**。buildFollowUpRequest 只设 X-Gw-Source-Actor（goal-continue/goal-model-switch/goal-audit/兜底 goal-followup，helpers.go:236,253-264）与 X-Gw-Parent-Request-Id（helpers.go:239），不设 X-Gw-Is-Auto；若误设，影子轮 entry（RequestType 非 title_gen/summary、origin_actor 不在排除名单、TaskType 为 nil）会落到 hook.go:927 兜底分支被排除 → request_logs 有行+turns 无行 → 破坏 G2。当前实现两侧对称：request_logs_hot 落行（origin_actor/parent_request_id 经 handler.go:1726-1731 → request_log_pipeline.go:472-482 持久化）+ session_turns 落行（IsAutoRequest=nil → isInternalAutoEntry 首卫 false，hook.go:911-914；origin_actor 进 turns，s1a_fields.go:112 + session/v2/turn_writer.go:342）。goal-% 影子轮失败态也对称（isTerminalFailure 放行后同样不被排除）。测试钉桩在位（response_interceptor_helpers_test.go:360-370 断言 X-Gw-Is-Auto 缺席）。
- **lite 模式无"影子轮只进一种存储"的分叉**。lite（storageRt≠nil）下 bootDatabaseURL 置空（main.go:469-474）→ openDBWithBootRetry 返回 nil（main_helpers.go:115-118）→ Stdlib() nil 安全返回 nil（db/db.go:2193-2196）→ initGoalControl 以 db==nil 短路"disabled (no DB)"（goal_control.go:127-131；goal store 本就是 PG 专属 goal.NewPGStore，goal_control.go:144）。即 goal 影子轮整个特性在 lite 一致地不存在，回环机制本身（httptest+ServeHTTP 进程内）模式中立；即使触发，lite telemetry 走 sink 落 SQLite request_logs+journal（client.go:1014-1017，Enabled() 含 sink，client.go:520-522），两侧同库对称。窗口改动无 redis/pg 专属新依赖（D06 §3-1/2/4 不变量保持）。
- **request_logs_hot 8h/7d promote 分区链对影子行无差异**。promote 纯时间谓词 `ts < now()-p_retention`，无 origin_actor/is_auto 过滤，且显式携带 origin_stage/origin_actor/parent_request_id/is_auto_request/is_final_success 列（installer/cmd/llm-gw-installer/embeddata/startup/602_request_logs_promote_atomic.sql:82-92,103-113,128-138）。影子行（origin_actor='goal-*'）与普通行同批转移；GLOBAL_G2 双表 UNION 也两侧核对。session_turns 侧（hot∪父表）同理。
- **G2 口径本体**：GLOBAL_G2=近 24h v1（hot∪父表）is_final_success=TRUE 且 request_id 无对应 session_turns(hot∪父表) 行计数（storage_observation_round.sh:109-118）——与镜像 hook/回填/reaper 的 gate 语义除"内部 auto 行"外一致（除发现#1 外平衡）。

## 三、未覆盖项与原因

- **发现#1 的运行时定量**（post-F1 build 上每 24h 实际产生多少条 gt_/gs_ 成功行、GLOBAL_G2 具体数值）——需真机 PG + 已配置 LLMGatewayAutoLLMEndpoint 的部署跑一轮 storage_observation_round.sh，静态读码无法给出量级。
- **影子轮与父轮 claim 竞态的实际可触发性**（发现#2）——需并发真机观测；静态判断父轮终态同步落库 + 影子轮 100ms sleep + 上游秒级延迟使父轮几乎必胜，列为低概率。
- telemetry OutboxEnqueueMirrorFailure 等 pg 侧失败路径在 lite 的行为——本轮窗口改动不触及，未展开。
