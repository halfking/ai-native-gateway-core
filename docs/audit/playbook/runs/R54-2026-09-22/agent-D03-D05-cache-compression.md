# D03（三层缓存 provenance）+ D05（超长自动压缩与 overflow 保连重试）子代理报告
（窗口：`af34fc8b8d055359472ab0d3b983317aec973f9b..c48651e5815b7b81f9666ae51d30327f57f1fe3a`）

> 只读审计；主代理复核结论待 R54 主轮文档定稿。
> 本域基线见 conventions.md §4 与 D03/D05 域文档 §3 检查清单。
> 窗口内主要新代码：a1b17b224（Handoff-B 七项优化模块落库：ctxpool/chunk_buffer/prompt_compress，未接线 cmd/gateway）+ 4f88f1861（Wave1-A5 影子轮不进拼装链）+ 36f179598（R53 聚焦修复轮：R51-F15 免费重试 Governor 补记 + R51-F13 BaseWorker 自愈重构）。

## 一、发现（候选，待主代理复核）

| # | 级别候选 | 发现 | 证据 file:line | 建议处置 |
|---|---|---|---|---|
| 1 | P2 | **prompt_compress.go 的 IR 层变换破坏 original→sanitized→compressed provenance identity**：`CompressMessages` 在 `collapseRepeatedText` 第三轮把 ≥3 条相同 text 块坍缩为 `"[repeated N times]"` 单条（internal/ir/prompt_compress.go:271-324），整段折叠破坏了「compressed 层回溯到哪几条 original 消息」的 identity 映射；既不写回映射到 AlignmentMap，也不落 occurrence 索引。域文档 §3 检查清单第 1 条「任一 compressed 内容都能沿 identity/occurrence 回溯到 original」对 IR 层同样适用，本轮首条 IR 层压缩接入就引入了这条静默断链 | internal/ir/prompt_compress.go:271-324（collapseRepeatedText）；同样问题：dedupConsecutiveToolResults（:146-193）丢弃中间 tool 结果但无 identity map | 至少在压缩对象上挂一个 `[]OriginOccurrence`（message_index_in, count, kind=dedup/collapse），或在压缩数据流旁路附 V2 metadata；或显式登记为 IR 压缩 ≠ 三层缓存层（语义独立）。本轮首条 IR 层压缩是「落库未接线」（Handoff-B §11.7，tests/stress/REPORT.md:95），可登记下轮 |
| 2 | P2 | **prompt_compress.go 的 compressScratch sync.Pool 持有 *[]Message 但 release 前未清零元素**：acquireScratch/releaseScratch 复用同一个 `[]Message`（internal/ir/prompt_compress.go:359-376）`Put` 前只截到 0 长度，元素（每个 Message 含 ContentBlocks 子切片）仍在 backing array 上。下一个 acquirer 若 `*scratch = append(*scratch, m)` 会读到 stale Message 值。但更严重的：注释声称「hot path 用这个池」，但全仓无任何调用方（grep acquireScratch/releaseScratch 0 hits），池是死代码，未来接入时若配合 `append` 而不是 `make+copy` 路径会立刻读到 stale | internal/ir/prompt_compress.go:359-376（compressScratch 池）；零调用方 | 池接入前补：release 时 `for i := range *s { (*s)[i] = Message{} }` 显式清零；或干脆删除（无调用方）。登记为下轮接线门禁项 |
| 3 | P1 | **R53-F1 ExtraCallRecorder 未覆盖 Redis 分布式 governor**：domains/dispatch/redis_backend.go 三种 redis-backed governor（redisRateGovernor / redisEnforceGovernor / redisShadowGovernor）均未实现 RecordExtraCall；R53 审计报告已登记 R53-D2「该 backend 为 env 显式 opt-in，当前部署均在 in-process governor」。但 forwarder.go:417 `extraCallMeterFor` 走 `gov.(ExtraCallRecorder)` 类型断言，Redis 部署下 `qr.OnExtraUpstreamCall` 恒为 nil → reqprobe 参数剔除/模式回退/executor_chat ctxLen 恢复/executor_anthropic ctxLen 恢复四触发点（executor_chat.go:1045,1075,1175；executor_anthropic.go:887）的 `meterExtraUpstreamCall(params)` 全 nil-safe 跳过 → **R51-F15 的限流计量目标在 Redis 部署下整体失效**，回到 R53 修复前的失真状态 | domains/dispatch/redis_backend.go:429-493,497-712（无 RecordExtraCall）；forwarder.go:417（断言点）；executor_chat.go:1045,1075,1175；executor_anthropic.go:887 | 在 redisRateGovernor 上增加 RecordExtraCall（按 R53 D2 备注，需新计费 Lua）→ 至少给 governance 告警面：ExtraCallRecorder miss 时单独打 metric `dispatch_extra_call_capability_missing_total{governor=redis_*}`，让运维能侦测到。R55 修复前登记 |
| 4 | P3（观察） | **ctxpool/PooledCtx 的「parent cancellation 不传播」被声明为 intentional 但有红线注释无落地压测**：ctxpool.go:38-43 明确说明「There is NO propagation between the parent and child for cancellation. The parent cancelling does NOT close the child's done channel. This is intentional」；acquireWithTimeout 用 `context.AfterFunc(wrapper, pc.Cancel)` 兜底（:294）。但 Handoff-B 七项（a1b17b224）整批**未接到 cmd/gateway**（tests/stress/REPORT.md:95、docs/handoff/2026-09-22-stress-audit-handoff.md:66），目前没有任何 dispatcher 调用方消费池；本轮只落库钉测试。**潜在风险（未触发）**：未来接线时若有人把 PooledCtx 直接传给 `provider/client.go`（已经搜了全仓：无 ctxpool 引用）作 long-lived HTTP 调用 ctx，父 ctx 超时不会传播→上游请求会泄漏到自然返回 | internal/ctxpool/ctxpool.go:38-43, 191-214, 246-310；cmd/gateway 内零引用；tests/stress/handoff_b_s8s13_test.go 唯一外部使用 | 接线前补：(1) `PooledCtx.Value` 调用前确认父 ctx 仍存活（`parent.Err()` 检查）；或 (2) 给 ctxpool 加 `AcquireLinked(parent)` 变体（持 parent context.AfterFunc 显式转发取消），把无传播 ctx 限定在显式非 cancelable 场景 |
| 5 | P3（观察） | **chunkBuffer 的 `closed atomic.Bool` 与 pool reuse 组合下双重关闭路径无并发 race 防护**：AcquireChunkBufferWith 把 `closed.Store(false)`（chunk_buffer.go:125），但 Release 不重置 `closed`，Put 回 pool 后下一个 acquirer 才覆盖。若某个 acquirer 在 Release 之前没 Close（注释说「Caller is expected to call ReleaseChunkBuffer explicitly」），下次 acquire 拿到同一个 chunkBuffer 时 closed=true → 首次 Write 直接返回 ErrClosed | domains/streaming/executors/chunk_buffer.go:107-142 | 加固：在 ReleaseChunkBuffer 内 `closed.Store(false)` 与 buf 清零一起做，acquire 不必再 Store；保持单一真值。**当前未触发**：chunkBuffer 同样未接入 cmd/gateway（tests/stress/REPORT.md:95），仅为合成 benchmark |
| 6 | P3 | **Wave1-A5 影子轮不进拼装链：dedup/digest 两个 source 修了，但 internal/sessionv2mirror 的 goal-% 排除仍是各 caller 各自拼接，存在漂移风险**：本次 message_source_v2.go/message_source_digest.go 加 `NOT LIKE 'goal-%'`（:90、:100），shadow_turn_exclusion_test.go 钉桩只检这两个 SQL 常量。但同仓仍有多处 goal-% 过滤拼写不一致：autoroute/shadow_actors.go:63 用 `COALESCE(" + col + ", '') NOT LIKE 'goal-%'`，admin/work_types.go:316 是 `wtSyntheticExclude contains '%' (LIKE 'goal-%')`；admin/session_title.go:318、admin/auto_route.go:722、admin/sliding_window_realdb_test.go 等各自拼接。test 只锁了 sessionsummary 两源，**未来若有人新加 message source 而忘写 goal-% 谓词，shadow_turn_exclusion_test 不会红** | domains/sessionsummary/message_source_v2.go:86-91、message_source_digest.go:97-101、shadow_turn_exclusion_test.go:16-25（仅检本包内两 SQL 常量）；对比：autoroute/shadow_actors.go:63（独立拼写） | 收敛方案：把 goal-% 谓词抽成 `autoroute.SQLExcludeSyntheticActors(col)`（已在用）作为 SSOT；shadow_turn_exclusion_test 改检该函数返回串而非裸字符串；新 source 接入时强制引用同一函数。下轮登记 |
| 7 | P3 | **shadow_turn_exclusion_test.go 仅做字符串包含断言，未做真库或 pgxmock 行为验证**：测试只 grep `v2SessionBodiesBaseQuery` 和 `sessionTurnDigestQuery` 两个包级常量是否含「NOT LIKE 'goal-%'」。若有人把谓词改到 CTE 子查询或 view definition 里（不在这两段字符串中），或改用 `!= ANY(ARRAY['goal-continue', 'goal-model-switch', 'goal-audit'])`（等价但拼写不同），test 即红但语义仍正确——**反向**问题：若有人拼错为 `NOT LIKE 'goal_'`，strings.Contains 仍通过→test 不抓 | domains/sessionsummary/shadow_turn_exclusion_test.go:16-25 | 加 pgxmock 真测：构造含 goal-% 与不含 goal-% 的两行 session_turns 灌 mock pool，断言 GetSessionMessages 返回的 SessionMessage 数仅含非 goal-% 行（message_source_digest_test.go:174-217 已经有 pgxmock 模式，可同款扩展）；或至少把断言改成 token-level（用 sqlparser 解析 WHERE 子句、再断言谓词结构） |
| 8 | P3 | **message_source_digest.go 的 expandDigestTurn 在 digest envelope `version != SchemaVersion \|\| Algorithm != AlgorithmVersion` 时统一返回 nil（message_source_digest.go:144-152），导致 schema 漂移时整段会话沉默掉**：sessiondigest.Unmarshal（digest.go:93-106）拒绝未来 version，调用方 expandDigestTurn 收到 nil envelope → 「err or nil envelope 都是 nil 切走」。当前测试覆盖（message_source_digest_test.go:92-106）已钉桩这条行为。但语义上：schema 升级上线期间（先发布读侧、再回填存量），新写路径已经写 v2 envelope，老读侧忽略 v2 → **整段历史会话在摘要输入上消失**（被 silence fallback），不是仅缺该 turn。建议加一条 metric：`sessionsummary_unknown_digest_version_total` 让运维可观测漂移期 | domains/sessiondigest/digest.go:93-106；domains/sessionsummary/message_source_digest.go:144-152 | 加 metric；或在 envelope nil 时返回单条 `SessionMessage{Role:"system", Content:"[digest schema version unknown: <ver>]"}` 让摘要 LLM 至少看到漂移提示（治本要靠回填 job 升级覆盖） |
| 9 | P3 | **base_worker.go supervise 自愈的退避位移溢出后取 workerRestartMaxBackoff 但 backoff=0 路径未禁**：`delay := workerRestartBackoff << (n - 1)`（base_worker.go:113），`if delay <= 0 \|\| delay > workerRestartMaxBackoff { delay = workerRestartMaxBackoff }`（:114-116）。n=1 时 1s<<0=1s（正确）；n=63+ 时左移溢出为负数，触发 `<=0` 分支取封顶值。逻辑正确但**缺乏 n 上限断言**：连续 panic 100 次后仍然无限自愈（每次仍跑 runOnce 再 panic），没有「达到 N 次后改 fail-loud」的门 | bg/base_worker.go:101-131（supervise + runOnce） | 加 `if n >= workerRestartFailLoudAfter { slog.Error("bg worker keeps panicking", ...) ; metric 增量；return }` 让运维能从告警看到 worker 真正烂了。当前无限退避封顶是「高频 panic 慢速轰炸」状态，监控弱 |
| 10 | P3（观察） | **domains/streaming/handler.go:5135 新增 GoalModeHeader 字段承载 X-Gw-Goal-Mode 但读侧是否消费未在本窗口确认**：handler.go 改动仅 1 行（`GoalModeHeader: strings.TrimSpace(r.Header.Get("X-Gw-Goal-Mode"))`），但 telemetry client.go（窗口 diff 855+/811- 行）是否消费 `entry.GoalModeHeader`、是否进入 session_turns/turn_digest 列、是否进入 `governance.compression_reason` 等观测面，未在本窗口内闭合 | domains/streaming/handler.go:5135（新增）；其余消费方待全仓 grep | 下轮 D10 观测面专项验证：本轮仅记入观察 |
| 11 | P2（窗口内 R53-F1 闭环 + 已知债） | **executor_anthropic.go ctxLen 恢复路径补记 meter 但未覆盖 §11.6 EOF-without-DONE 这类新的结构化错误流**：a1b17b224 同时落地 §11.6（committed 截断走结构化错误 KindUpstreamDown）与 executor_anthropic.go:887 的 ctxLen 恢复 meter。两条改动**未做交叉测试**：当上游既返回 4xx context-length 又中途 EOF（理论上 Anthropic 在 400 之前不会断流，但 4xx 前已发部分 SSE 时）→ executor_anthropic 的 retry 分支进入 ctxLen 恢复路径但响应侧已被 §11.6 接管，meter 与 outcome 写入可能错位 | domains/streaming/executors/executor_anthropic.go:873-902（ctxLen 恢复分支）；a1b17b224 §11.6 落地 | R54 主轮如动 executor_anthropic 加一条交叉测试：mock 同时返回 4xx context-length 与 EOF 无 [DONE]，断言 meter 不重记 + outcome.Kind 为 context_length_exceeded 而非 upstream_down |

## 二、核实为健康的面

- **三层缓存 provenance（original/sanitized/compressed）D03 §3 检查清单 1-7 项**：窗口内 `domains/hooks/compression/`、`security/sanitize/`、`internal/sessionv2mirror/` 主链路无代码改动（diff 仅 hook.go:374 加 applyStorageS1BFields 一行调用 + s1a_fields.go:39-42 三列赋值 + s1b_fields.go 全新文件）；session_turns mirror 与 cache_v2 字段契约、白名单（window_source / sanitize_refs_truncated / alignment_map_truncated）维持 R36 修复后状态。SanitizedMessageRefs 与 AlignmentMap 写读两侧在窗口内未触碰，e2e_alignment_test / session_cache_concurrency_test 健康面基线继续成立。
- **R53-F1 ExtraCallRecorder 主线闭合**：forwarder.go:417 `extraCallMeterFor(gov, qr)` + executor_chat.go:1045,1075,1175 + executor_anthropic.go:887 四触发点补记 + governor_extra_call_test.go 4 测钉桩（债务式扣减、双 governor 钳制、concurrency/noop 无能力、meter 工厂发现）— 在 in-process governor 部署下 R51-F15 限流计量失真已闭环（仅 Redis 部署仍缺口，见发现 #3）。
- **R53-F2 BaseWorker supervise 自愈重构**：bg/base_worker.go 监督循环 `runOnce` + 指数退避（1s→60s 封顶，移位溢出同样取封顶） + done 一次性 close + `NotifyStopped` 转 no-op 兼容既有 20+ runFn `defer` 调用点；`Restarts()` 访问器 + `llm_gateway_bg_worker_restarts_total{worker}`（worker 名为编译期字面量闭集 ~25 值，低基数）；3 测钉桩两次 panic + 健康长跑 / 正常 return 不重启 / 退避睡眠中 Stop 一秒内返回。
- **Wave1-A5 影子轮不进拼装链（核心需求）**：message_source_v2.go 与 message_source_digest.go 两个 SQL 常量同步加 `AND COALESCE(t.origin_actor, '') NOT LIKE 'goal-%'`（v2:86-91、digest:97-101）；shadow_turn_exclusion_test.go:16-25 双源钉桩；message_source_digest_test.go:174-217 pgxmock 钉 SQL 契约已存在；session_turns 行本身保留供 admin/turn_digest 与对账（设计 §5.12「打标不排除、过滤权在查询侧」方案 18 §3 落实）。shadow_turn_exclusion_test 钉桩单调性成立（仅当两 SQL 常量丢失排除子句才 FAIL）。
- **D05 overflow 保连重试 §3 检查清单 1-6**：errorsx.ClassifyError（errorsx/classify.go:837,1028,1370）+ KindContextLength 单一族 → errorsx.IsContextLength 包装（classify.go:1423）；executor_chat.go:1149-1201 + executor_anthropic.go:873-902 双路径触发 handleContextLengthRecovery；context_summarize.go:991-1330 四层（smart window / mechanical trim / memora L1 / llm summary）覆盖；策略选择器（LLM_GATEWAY_COMPRESSION_MODE 五模，settings/spec_compression.go:19）热生效；可观测 `dispatch_extra_upstream_calls_total{mode}` + `compression_reason` + `compression_meta.trim_phase`。client 不感知压缩、连接保持（流式 OnNodeJump comment `: thinking:` 提示，executor_chat.go:1043-1045 周边）、kind 化信封出口（contextLengthExhaustedError）— D05 §3.2/3.4 闭环。
- **message_source_digest 计算正确性**：expandDigestTurn（message_source_digest.go:144-172）按 user→assistant 顺序展开 digest envelope；空侧跳过（test:71-86）、不可用 digest 跳过（test:92-106）、rune 截断保持 UTF-8 合法（test:139-172，rune-based cut 防 2026-09-08 b1f9da6ca 字节切坏 CJK）；gatedPerTurnDigestSource 旗标 off → 字节等价转 V2 source（test:256-282）；旗标 hot-reload 每调用重读。message_source_digest 整体是只读层（D03 §3 检查清单第 3 条 V2 metadata 接入）— 行为正确。
- **prompt_compress 单元/集成测试健康**：TestCompressMessages_* 6 测覆盖 fold / dedup / collapse / 零配置 / 协议字段保留；TestCompressMessages_LongAgentLoop 30 轮集成断言压缩比 ≤80%；FoldTextWhitespace 8 case；ItoaRepeat 5 case；3 基准（BenchmarkCompressMessages_LongAgentLoop/NoChange + BenchmarkFoldTextWhitespace）。Handoff-B §11.7 未接线 cmd/gateway（tests/stress/REPORT.md:95）即「合成 benchmark ≠ 网关吞吐」事实已确认。
- **chunk_buffer 单元测试健康**：TestChunkBuffer_* 11 测覆盖 8 KiB 高水位、idle flush（10ms 阈值）、0 = idle 禁用、chunkSize 边界、单写超阈值直冲、Close 后 ErrClosed、Pool 复用 scratch、并发 50 goroutine race（待实跑 -race）、Writer 错误传播、Stats 计数、Flush 幂等；3 基准。AcquireChunkBufferWith 内 `if cap(b.buf) < chunkSize { b.buf = make(...) }` 容量伸缩正确。
- **ctxpool 单元测试健康**：ctxpool_test.go 9 测覆盖基本 context surface / Cancel 传播 / WithTimeout 超时 / Parent Value 透传 / Pool 复用 / Race（待实跑）/ Release 计时 / Wrapped 取消 / 基准；`PooledCtx.Done/Err/Deadline/Value` 全 context 接口子集对齐；Cancel 幂等（先 close done，下次空 no-op）。
- **Wave1-A5 影子轮落库侧一致**：handler.go:5135 GoalModeHeader（X-Gw-Goal-Mode）+ response_interceptor_helpers.go:250 followUpSourceActor → X-Gw-Source-Actor + session_turns/origin_actor 落库链路完整；handlers 已区分 handoff→goal-followup 与 auto-title-generator 两条命名空间（见 R35-R1 后续注释核实，domains/streaming/response_interceptor_helpers.go:273-285 followUpSourceActor 表只覆盖 goal_continue/goal_model_switch/audit 前缀）。

## 三、未覆盖项与原因

- **D03 §3 第 2 条「sanitizer 跨进程 offset 原子预占」本轮无变更**：security/sanitize/smart_sani_guard.go / cross_tenant_race_test.go / tenant_scope_test.go / sanitize_info_bridge_test.go 均无 diff（grep 已确认）；R36 #2（P2 锁租约 5s 无续期）维持登记下轮真机耗时分布验证。
- **D03 §3 第 4 条「provider-window source telemetry」本轮无 telemetry 改动面**：session_turns.digest 仍是同形状落库；compression_reason / compression_strategy / compression_meta 在 request_log_pipeline.go 写路径未触碰；e2e_alignment_test / session_cache_concurrency_test 健康面基线继续成立。
- **prompt_compress.go / chunk_buffer.go / ctxpool.go 是否能破坏 SSE 边界 / sanitizer offset 原子预占**：均未接入 cmd/gateway 主路径（tests/stress/REPORT.md:95 「未接线优化不得算完成」五项明确点名；docs/handoff/2026-09-22-stress-audit-handoff.md:66,107,129）；**真机压测未做**，本子代理仅做静态读，无法就「流式分片聚合是否破坏 SSE 边界」「context 复用是否破坏 sanitizer 跨进程 offset」给出运行时结论 — 待 s8/s13 网关吞吐接线后由 §11.7 Handoff-B 端到端测试覆盖（已登记 Handoff-B 全员接线为下轮入口）。
- **executor_anthropic.go vs §11.6 交叉行为（发现 #11）**：本窗口内 §11.6 与 R51-F15 meter 同时落地，但未做交叉 mock；行为错位风险需 R54 主轮在 executor_anthropic 区域加单测覆盖。
- **shadow_turn_exclusion_test 覆盖范围**：仅检本包内两个 SQL 常量字符串（发现 #6、#7），未做真库行为验证；新 source 接入或谓词改写到 CTE/view 时无兜底；不下钻做全仓 goal-% 谓词 SSOT 收敛（已登记下轮 D11/D03 协作项）。
- **shadow 注入面（D14 已登记）**：R35-R1「X-Gw-* 信任头客户端可注入」三头伪造面（X-Gw-Source-Actor:goal-% / X-Gw-Is-Auto:true / X-Gw-Parent-Request-Id）仍开放，本轮 Wave1-A5 反而引入新 X-Gw-Goal-Mode 头（handler.go:5135）— 缺同等 token 方案保护。仍属 R35-R1 已登记 P2，不重复立条。
- **Redis 分布式 governor 的 ExtraCallRecorder 实装（R53-D2）**：redis_backend.go:429-712 三种 governor 类型均未实现 RecordExtraCall；部署侧 opt-in（LLM_GATEWAY_DISPATCH_GOVERNOR_BACKEND=redis_*），当前部署均在 in-process governor；纳入 R55 / R56 入口。
- **154/245 真机验收 / 真库回填**：未跑真库回填（无凭据），message_source_digest 的 `digest NULL → 跳该 turn` 行为在真实 backfill 期间会丢弃多少 turn 需待真实环境量化。
- **go test -race 实跑**：只读未跑（域文档 §3.7 建议并发包加 -race）；ctxpool_test TestPoolRace（:243+）/ chunk_buffer_test TestChunkBuffer_ConcurrentAcquireRelease（:245-266）/ base_worker_test 自愈 race 需主代理复核时实跑 — bg/ -race 与 dispatch -race 已由 R53 主轮实跑全绿（仅本批新模块未覆盖）。

## 四、与 D04 域交叠（跨域补注，给主代理）

R53-F1 ExtraCallRecorder 与 R53-F2 BaseWorker 自愈在 D04/D16 域已分别落地（docs/audit/playbook/domains/D04-queue-concurrency.md:57；docs/audit/2026-09-22-r53-24h-audit-round.md §一 R53-F1/F2）。本子代理仅就「与压缩路径交互（ctxLen 恢复重试触发压缩后是否仍正确 meter）」做交叉核验：executor_chat.go:1149-1201 + executor_anthropic.go:873-902 的 ctxLen 恢复分支在返回 retryableError 之前调用 meterExtraUpstreamCall(params)，参数传递沿用 forwardForDispatch 装配的 `qr.OnExtraUpstreamCall`（executor_dispatch.go:211 透传），governor 实例捕获于准入时刻（forwarder.go:417 注释「捕获的是本次准入的 governor 实例」），ApplyPolicy 换装不影响在途请求 — **与压缩路径的交互正确**。详见发现 #3（Redis 部署缺口）与 #11（§11.6 交叉未测）。
