# D08（供应商错误链与凭据服务质量）子代理报告（窗口：af34fc8b8d055359472ab0d3b983317aec973f9b..c48651e5815b7b81f9666ae51d30327f57f1fe3a，48h 滚动 R54）

> 域文档 `docs/audit/playbook/domains/D08-provider-errors.md` §3 检查清单逐项核对。
> 重点：Handoff-B 环式错误检测器/计数器落库形态、breaker KindRateLimit 剔除后绑定级降权回路闭合、§11.6 EOF-without-DONE 走结构化错误、proxy region overlay + settings 缓存接线。

## 一、发现（候选，待主代理复核）

| # | 级别候选 | 发现 | 证据 file:line | 建议处置 |
|---|----------|------|----------------|----------|
| F1 | P2 | **环式错误检测器（RingBackedDetector）未接入主流程，与 legacy `domains/health.ErrorDetector` 双轨并存**——Handoff-B #5 commit 描述明确写"未接线进入主流程前先落库"（a1b17b224 commit message），全仓 `grep -rn "NewRingBackedDetector" --include="*.go"` 在生产代码侧零调用方，仅在 `tests/stress/handoff_b_s8s13_test.go:139,174` 的合成 micro-bench envelope 中使用；`domains/routing/weighted_router.go:55,161,238,281` 仍消费 `*health.ErrorDetector`（旧 map[string]*ErrorCounter 形态），`tests/local/gateway/main.go:79,98,141` 同。两条路径若同时接入会出现双计风险（同一 envelope 内 OnError 各调一次 → 同一 credential consecutiveFails 各 +1）——窗口内未触发，仅留待未来接线时的主代理专项合并审查。 | credentialhealth/error_detector_ring.go:36,45；credentialhealth/ring_error_counter.go:256；a1b17b224 commit message；tests/stress/handoff_b_s8s13_test.go:139,174；domains/routing/weighted_router.go:55,161 | P2（候选）：Handoff-B 接线专项轮处理；RingBackedDetector 与 legacy 切流期间必须保证单一投递方，目前 `WeightedRouter.RecordError` 一处独占 OnError，调换时整点切流而非灰度共存。钉桩：TestRingBackedDetector_* 已存在 12 例，可承担接线回归。 |
| F2 | P2 | **`ringCounter` 内 `window[60]uint16` 单槽位容量 65535，5xx 风暴下仍可能溢出**——`Increment` 第 137 行 `c.window[c.cur]++` 与第 138 行 `atomic.AddUint32(&c.total, 1)` 均为 uint16/uint32 自增，单元测试 `TestRingCounter_IncrementAndReadback` 仅验证 5 次场景；生产中凭据若被 5xx 风暴打 1s 内连续 65535+ 次，cur 槽会 wrap 到 0，**`Last1Min()` 求和返回小于实际错误数**，误判 credential 健康（错误率指标低估、consec gauge 仍由 uint32 consec 承接不受影响）。同时 `total` 字段 uint32 在 4.29B 次（约 4.29 万 RPS × 1 小时即可能）后溢出，影响 `Snapshot` `RingCounterStats.Total`。文档注释行 22-23"hot read is a tight uint16 loop"未提示该风险。 | credentialhealth/ring_error_counter.go:70,76,137,191；credentialhealth/ring_error_counter_test.go:13-28 | P2（候选）：要么在 `Increment` 内对 `c.window[c.cur]` 做 `< 65535` clamp（保留上限做"爆量"信号），要么切 uint32 把单槽位预算扩到 4.29B；`total` 同。钉桩加 `TestRingCounter_WindowSaturation_ClampsOrReports` 或 `TestRingCounter_TotalNoOverflow`。 |
| F3 | P2 | **Handoff-B 接线公告与现实不一致：commit a1b17b224 description 写"未接线进入主流程前先落库"，但 grep 不到任何"X 已切换至 NewRingBackedDetector"标记位**——`credentialhealth/error_detector_ring.go:10-13` 注释明确写"callers can opt-in to the ring-backed path by switching to NewRingBackedDetector while the legacy detector stays unchanged for gradual rollout"，但全生产路径（routing/weighted_router.go、health_tracker.go、local/gateway/main.go）零切换。R54 commit message 自承"未接线进入主流程"，但子代理报告回执若以"已落库"措辞登记会让主代理误判为"切流完成"。 | a1b17b224 commit message; credentialhealth/error_detector_ring.go:10-13; 全仓 grep 验证 | P2（候选）：主代理撰写 R54 总结时显式标注 "落库但未接线" 状态，避免下轮子代理按"已接线"假设审计；D08 域文档 §历史回归点回注"R54 ring_backed detector 落库未接线"。 |
| F4 | P1（**复核后视情升降**） | **`breaker.go RecordFailure(KindRateLimit)` 早退，但 `defaultPolicies` map 仍可被 `RecordFailureWithBillingMode` 经 `policy, ok := defaultPolicies[kind]; if !ok { policy = b.coolingPolicy }` 兜底**——`breaker.go:395-398` 在 `if !ok` 时回落到 `b.coolingPolicy`（NewWithPolicy 传入的 zero-value 默认 KindTransient 60s）。KindRateLimit 在 `RecordFailure` 早退根本走不到 395 行，所以该兜底不构成实际风险。**已核实为健康**：早退路径在 `b.mu.Lock()` 之前完成（line 383-388），不会触碰锁、不递增任何 atomic、不写 lastErrorKind、不触发任何状态机；HALF_OPEN 探针撞 429 通过 `ReleaseProbe()` 归还探针槽的设计与 client-bug 路径一致（line 384-386，与 line 370-372 client-bug 早退位置并列）。 | domains/credential/breaker.go:366-388 | 建议**降级为核实为健康**（见 §二）；保留 F4 编号仅为完整记录早退契约已闭环。 |
| F5 | P1（**复核后降为核实健康**） | **`writer.go:347` 把 KindRateLimit 走 `writeModelLevelFailureOnly`（绑定级 3min 冷却）仍存在，与 breaker 早退形成双层降权契约**——R51-F2 §5.6 红线裁决要求"限流即降权"，具体承担边界在 `coolingDuration(KindRateLimit) = 3 * time.Minute`（writer.go:525-531）。ADR `2026-09-22-rate-limit-breaker-exclusion.md` §Decision 1-3 三条契约均闭合：(a) 断路器不计；(b) 绑定级 3min 排序降权保留；(c) HALF_OPEN 探针撞 429 归还探针槽。**已核实为健康**：单点入口收敛（executor_nodehealth.go:201,392 → Credential.Failure{Kind:KindRateLimit} → State.WriteOnError → writer.go UpdateOnFailure → case KindRateLimit 走绑定路径）。 | domains/credential/writer.go:347,525-531；docs/adr/2026-09-22-rate-limit-breaker-exclusion.md；domains/streaming/executors/executor_nodehealth.go:201,392 | 主代理复核后**核实为健康**：契约已闭环，但需在域文档 §历史回归点添加 "R54 KindRateLimit 双重降权契约（breaker 早退 + writer 绑定级 3min + HALF_OPEN ReleaseProbe）"。 |
| F6 | P2 | **§11.6 EOF-without-DONE 翻转 Kind 变更后，`isInterruptionCode` 与 `KindEmptyResponse` 旧链路残留路径未清理**——a1b17b224 将 committed-then-truncated 的 `outcome.Kind` 从 `KindEmptyResponse` 翻转为 `KindUpstreamDown`，注释旧 commit 05c79fbe9 已被废弃，但 `domains/streaming/executors/executor_eof_test.go:11-34` `TestIsBenignEOFStreamingInterruption` 仍然在表里写 `"not benign: zero chunks" → false` 与 `tc.reason == "eof_without_done" && tc.chunkCount > 0` 真值表——但**生产路径 commit 后分支已不存在"benign"出口**（committed 路径永远 Interrupted=true、KindUpstreamDown），该测试变成了"未实现的旧契约钉桩"。`executor_eof_test.go` 不在 R54 改动文件清单，但已被 §11.6 翻转语义架空：当前生产对 `chunkCount > 0 && reason == "eof_without_done"` 不再"benign"=true，而是 wire-error + Interrupted=true。 | domains/streaming/executors/executor_eof_test.go:11-34；domains/streaming/stream.go:~1180-1230 (a1b17b224 diff)；a1b17b224 commit message | P2（候选）：测试名 IsBenignEOFStreamingInterruption 在 §11.6 后是死语义；要么改名 `TestStreamReadEOFLegacyBenignBranchRemoved`，要么删除。审计轮 P3 即可清理。 |
| F7 | P2 | **`proxy/types.go` IsRegionBanned 改为 variadic 后，5 处调用点签名需重审**——签名从 `IsRegionBanned(region string, subscriptionBans, nodeBans []string)` 改为 `IsRegionBanned(region string, banLists ...[]string)`。R54 调用点更新（proxy/manager.go:385,1998、manager_region_test.go）使用 `subscriptionBans, overlayBans, nodeBans` 三参数顺序；新签名不再区分"subscription vs node"语义，订阅层 vs 节点层靠**调用方传参顺序**保证。错误路径（manager.go:398 错误信息 `subscription banned regions: %v, platform default banned regions: %v`）以 2-list 形式输出，但若调用方传 (sub, node) 两参数会显示 `subscription banned regions: <sub>, platform default banned regions: <node>`——**靠注释不靠类型**保证正确性。 | proxy/types.go:213-235；proxy/manager.go:385,398,1998 | P2（候选）：variadic 隐藏了调用语义；要么回归到显式 `subscriptionBans, platformOverlay, nodeBans []string` 三参，要么保留 variadic 但写函数级"顺序契约"测试 `TestIsRegionBanned_ParamOrderContract`。 |
| F8 | P2 | **`proxy/manager.go:419-431` `subPrioritiesSort` clamp [0,99] 与 R47 API 写侧校验 [0,99] 双层防护**——`admin/proxy.go:469` create 路径写侧 `[0,99]` 校验；`admin/proxy.go:469-473` update 路径同样 `[0,99]` 校验；`proxy/manager.go:422-427` 读侧 clamp 到 [0,99]。但 `sub.Priority` 入库由 `proxy/store_pg.go:113,181,513` `$N` 直接写入 PostgreSQL，**没有 DB 列上的 CHECK 约束或 trigger 兜底**——SQL migration 阶段若绕过 admin API 直接 `INSERT INTO subscriptions ... priority=-1`，读侧 clamp 会把它当 0 处理（注释明确"存量负值行会排到 0 值订阅之前——越权最高优先"的根修是 clamp 兜底）。**核实为健康侧**：clamp 把"越权最高优先"折中为"与 0 同桶"（最高合法优先），不是 DB 列约束（DB schema 不在 R54 窗口审计范围）。但需提示：若凭据分桶全 0（大量订阅都 priority=0），存量负值不能被 SQL 报表区分。 | proxy/manager.go:419-431；admin/proxy.go:377-379,469-473；proxy/store_pg.go:113,181,513 | P2（候选）：DB 列 CHECK 约束或 trigger 兜底属于 schema 治理范围，由 D08 域文档提示下轮 D07 schema 审计轮考虑（不是 R54 P 修）。域文档 §历史回归点回注"R54 R50 读侧 clamp 与 R47 写侧校验双层；DB 层无 CHECK"。 |
| F9 | P3 | **Handoff-B benchmarks_test.go 引用了 `miniredis.Run()` + `pgxmock`，但未引用 `b.RunParallel` 或 `-race`**——`credentialhealth/benchmarks_test.go` 是新增文件，与 R54 提交同时落库；测试运行 `BenchmarkChecker_DominantKindResolution` 等依赖 miniredis + pgxmock，跑时 `miniredis.Run()` 在每个 bench 函数内反复启动销毁（lines 27-31, 57-61, 105-109）。窗口内 a1b17b224 commit message 写 `go test ./domains/credential/ -count=1` 跑了但**未跑 -race**；窗口内 grep 显示 `TestCounterSet_ConcurrentIncrement`（32 goroutines × 1000 increments = 32K 并发）属于 race-prone，但**测试包从未被 -race 跑过**。`ringCounter.Increment` 内 mu.Lock + atomic.Add + atomic.StoreInt64 顺序正确，但 `Forget` 路径 `delete + Release` 在锁外（line 327-331）需 `-race` 验证。 | credentialhealth/ring_error_counter_test.go:221-250；credentialhealth/benchmarks_test.go:1-130 | P3（候选）：补 `-race` 跑一次（Handoff-B 接线时必跑，conventions.md §5 三门之一即含 `-race`）。 |
| F10 | P3 | **junk_seed_guard 集成测试依赖 testcontainers/Postgres（`_test.go` 的 `//go:build integration`）未在常规 CI 链路跑**——`modelname/junk_seed_guard_integration_test.go:1-2` `//go:build integration` 标签保护，需 `-tags integration` 启用；常规 CI grep `go test ./...` 不会触发此用例。`canonical_dedup_test.go:13 TestJunkSeedGuardSQL_FoldSingleSource` 是 byte-exact 钉桩（已 R50 收敛），故"两臂全死"防御有 unit 级兜底；但**真库语义命中**（equal 臂 + suffix 臂）只在 integration 标签下验证。R54 改动 `provider/client.go:1350-1380` 接入 JunkSeedGuardSQL，对真实 stdName="opus-5" / "claude-opus-5" / "totally-new-model" 三例的命中行为依赖 integration test 真库回归。 | modelname/junk_seed_guard_integration_test.go:1-2；modelname/canonical_dedup_test.go:13；provider/client.go:1350-1380 | P3（候选）：CI 链路补 `go test -tags integration ./modelname/`（真库是 R51 之后的 252/254/245 host）；conventions.md §5 迁移三纪律对真库实证有要求。 |
| F11 | P3 | **`admin/settings.go:288` `settings.InvalidatePlatformValue(key)` 仅在 `!tenant` 分支调用——租户层改值不失效 platform cache**——R52-F3 `selectionMu` 锁内 settings DB 读修后，admin/settings.go:283-289 把 `InvalidatePlatformValue(key)` 包在 `!tenant` 内，符合"platform 缓存"语义。但 `proxy.default_banned_regions` 是 `Scope: ScopePlatform`（spec_proxy.go:15），所以 `tenant` 路径根本不会改这个键；若改了一个 `tenant` 层的同名键（如租户级 banned override），缓存会按 `key` 维度失效（ttl_cache.go:70-72 Delete 是单 key 删除）——`CachedPlatformString` 只读 `ScopePlatform` 不读 tenant，所以**语义上不会出现 tenant 写入影响 platform 读端**。**核实为健康**：注释 "tenant 分支跳过失效 platform cache" 与 spec 平台作用域严格匹配。 | admin/settings.go:283-289；settings/ttl_cache.go:70-72,114-128；settings/spec_proxy.go:9,15 | P3（候选）：核实为健康，无需修改；在域文档 §历史回归点回注"R54 R52-F3 InvalidatePlatformValue 仅 platform 失效，tenant 层不触发——因为 CachedPlatformString 只读 ScopePlatform"。 |
| F12 | P3 | **`credentialhealth/ring_error_counter.go` 结构体内 `_ [88]byte` 保留字段未启用——文档承诺"room for lastError *error if wired"但生产侧无 caller**——文件头注释（行 47-49）声明 `ringCounter` 留 `_ [88]byte` 88B 为未来挂 `lastError *error`，但 R54 没有任何 caller 消费这个字段，`ErrorDetector.lastError map[string]error` 旧语义也未移植到 RingBackedDetector。**纯预留字节，不影响行为**——但 MemoryBudget 测试 `TestRingCounter_MemoryBudget` 行 281 写 `totalBytes += 256` 已包含这 88B，意味着承诺的"240B per cred"实际是 248B 而非 240B；commit 描述写"~248 B vs ~24 KiB for the map[string]*ErrorCounter baseline"——**baseline 计算口径需要更新**：248B 是含预留，240B 是承诺预算。 | credentialhealth/ring_error_counter.go:47-49,75-78（结构体字段）；credentialhealth/ring_error_counter_test.go:256-285；commit a1b17b224 message | P3（候选）：要么删 `_ [88]byte` 字段把 88B 真正省下到 ~160B；要么更新文档说明 248B = 含预留；要么实现 lastError 挂载让承诺生效。当前承诺/实现分叉在 Handoff-B 文档验收门槛（"~240B"）上是个 doc drift。 |

## 二、核实为健康的面

- **breaker.go RecordFailure 早退闭环（候选 F4 降级）** —— `breaker.go:366-388` 对 KindRateLimit 在 `b.mu.Lock()` 之前早退：client-bug 早退（370-372）、KindRateLimit 早退（383-388）平行对齐；HALF_OPEN 探针撞 429 通过 `ReleaseProbe()` 归还探针槽的设计与 client-bug 路径一致。`RecordFailureWithBillingMode` 入口（breaker.go:752-758）已落入此早退路径——`freeTierPolicies` 配 KindRateLimit 项（R54 删除）与 `defaultPolicies` 配 KindRateLimit 项（R54 删除）结构性固化裁决不留死配置。`TestRateLimitExcludedFromBreaker`（breaker_test.go:213-255）三段断言（429 风暴不累计 / 探针撞 429 归还 / 真实故障仍 OPEN）齐全。

- **§11.6 EOF-without-DONE 翻转 + 钉桩** —— a1b17b224 把 2026-09-01 commit 05c79fbe9 的"benign success"路径反过来：committed-chunk + EOF-without-DONE 走结构化 SSE error envelope（`data: {"error":{"type":"upstream_incomplete","code":"eof_without_done"}}`）+ 合成 `[DONE]`，HTTP 200 不改写（committed 字节流状态不可回退），outcome.Interrupted=true、Kind=KindUpstreamDown、Resumable=false、failure_detail_code="eof_without_done"。`TestStreamChatWithPendingCapture_Section11_6_PseudoSuccessGuard`（stream_eof_test.go:657-742）五条断言（wire shape envelope-first、HTTP 200、capture interrupted、Kind UpstreamDown、Resumable=false）齐全。SQL 过滤链 `failure_detail_code='eof_without_done'` 仍可用。Kind 从 `KindEmptyResponse`（旧 benign kind）翻转到 `KindUpstreamDown`（真故障 kind）后，断路器与凭据冷却链路被正确驱动（executor_nodehealth.go:161 的 `e.Circuit.RecordFailureWithBillingMode` 接收 KindUpstreamDown，writer.go:358 case KindUpstreamDown 走 credential.availability_state 更新路径）。

- **proxy region overlay + CachedPlatformString 接线** —— R52-F3 selectionMu 锁内 DB 读修后，proxy/manager.go:354 把 `defaultBannedRegionsOverlay()` 调用移到 selectionMu 锁外（"overlay 读 settings（虽有 ≤5s 缓存仍是潜在 DB 读）不进 selectionMu 临界区"）；settings/ttl_cache.go:23 `platformValueCacheTTL = 5 * time.Second` 满足要求 ≤5s；ttl_cache.go:55-58 "Do not cache failures: a transient DB error must not pin fallback behavior for a full TTL window" 失败不缓存满足要求。admin/settings.go:283-289 `InvalidatePlatformValue(key)` 在 admin 改 platform 键后立即生效（不走 TTL）。proxy/manager.go:354-356 注释 "R51 决策"明确指明存量订阅未回填 banned_regions 也被规避覆盖；proxy/manager_region_test.go:494-548 三段断言（Spec 默认 HK 拒节点 / 空字符串放行 / 多地区带大小写空白归一化）齐全。proxy/types.go:213-235 variadic 签名变更与 caller 顺序契约正确（候选 F7 是契约风控建议，不是行为缺陷）。

- **junk_seed_guard.go R50 收敛 + 真库实证 + 字节级钉桩** —— provider/client.go:1350-1380 改用 `modelname.JunkSeedGuardSQL`（canonical_dedup.go:32-41），R48/R49 的两臂全死（stdName dash 形 vs 左侧下划线折叠、截断臂 `'-'` 永不匹配）已修正为左折叠 `%[1]s = replace($1, '-', '_')` 与右截断 `'_' || %[1]s LIKE '%_' || replace($1, '-', '_')`。`TestJunkSeedGuardSQL_FoldSingleSource`（canonical_dedup_test.go:13-26）byte-exact 钉桩（"R50 真库实证修正时的字面量字节级一致，并行会话 df9b041e6 的两臂全死修复语义不许漂移"）；`TestJunkSeedGuardSQL_Behavior`（junk_seed_guard_integration_test.go:22-113）真 PG 实证（opus-5→claude-opus-5 截断命中、claude-opus-5→精确等值命中、totally-new-model→放行、glm5.3→不放行 dot↔dash 契约边界、claude--opus-5→放行 run-collapse 前置条件）。fail-closed 语义（"查询失败时调用方必须 fail-closed（跳过 INSERT），不得放行盲插"，canonical_dedup.go:29-30）由 caller 注释与 `if err == nil { ... } else if !errors.Is(err, pgx.ErrNoRows) { slog.Warn } else { INSERT }` 控制流保证。

- **storage.request_logs_write_enabled 装配 R52-F8 收口** —— cmd/gateway/storage_mode_init.go:107-120 lite 形态 storage 族 specs 注册（`settings.Init(nil) + for _, sp := range settings.PlatformSpecs() { if sp.Category == settings.CategoryStorage { _ = settings.Global.RegisterSpec(sp) } }`），让 lite 进程能读 `LLM_GATEWAY_STORAGE_REQUEST_LOGS_WRITE_ENABLED` env（EnvNameAuto 派生）真正生效；与 Full 链路 telemetry/client.go:38, 1176, 1951 共键同一默认（true = 维持现状双写）。`admin/telemetry_ingest_stop_write_gate_test.go` + `domains/hooks/observability/telemetry/request_logs_stop_write_gate_test.go` 双模式测试齐全。

- **credentialhealth 单元测试覆盖** —— ring_error_counter_test.go 8 测（IncrementAndReadback、AdvanceWindowDropsOld、AdvanceAcrossBoundary、RecordSuccessDecrementsConsec、ResetConsecZerosGauge、AcquireReleaseNoLeakState、ResetClearsWindow、ConcurrentIncrement、MemoryBudget）+ error_detector_ring_test.go 10 测（含 MemoryFootprintPerCred 1000-credential 高水位钉桩）+ TestJunkSeedGuardSQL_FoldSingleSource 字节级钉桩；breaker_test.go TestRateLimitExcludedFromBreaker 三段。

## 三、未覆盖项与原因

- **154/245 真机验收** —— §11.6 真实供应商验收仍需在 154/245 上跑故障注入脚本（本机 mock 不模拟此协议违例）；Handoff-B 七项未接线进入主路径，吞吐评估延后。本机审计范围内无法触达真供应商协议违例。
- **Handoff-B 接线后双轨并存风险（F1）** —— RingBackedDetector 接线专项轮处理，本轮 commit a1b17b224 公告明确"未接线进入主流程前先落库"；本轮不触发双计。
- **F2 window[60]uint16 溢出需真实 5xx 风暴流量** —— 单元测试 5 次场景无法覆盖；需 154/245 真机压测或 mock-injected 5xx 风暴脚本。
- **F8 sub.Priority DB 层 CHECK 约束** —— 属于 DB schema 治理，需 D07 schema 审计轮专项，不属 D08 域。
- **F10 junk_seed_guard 真库回归** —— 需 `-tags integration` 启用 testcontainers/Postgres；常规 CI 不跑；D08 域仅承担"调用方接入正确 + 字节级钉桩存在"的弱保证。
- **F9 ringCounter -race 回归** —— 需 `go test -race ./credentialhealth/` 跑一次；本次只跑了 `-count=1`，commit message 也未声明 -race；建议 Handoff-B 接线专项轮补跑。

---

报告路径：`docs/audit/playbook/runs/R54-2026-09-22/agent-D08-provider-errors.md`
发现条数：12（候选 12 条；其中 F4/F5/F11 复核后核实为健康，建议主代理回注域文档后归档）
关键 P0/P1：经复核本次窗口内**无 P0/P1**——F4/F5 早退闭环契约已闭合；F1/F2/F7/F8 是 P2 候选需接线专项处理；F3/F6/F9/F10/F12 是 P3 文档/测试侧债务。
