# D15+D16 合并域子代理报告（窗口：`643735a28^..HEAD`；重点增量：`b75c91900..HEAD`）

> 范围：a8d3a5bbe（taskprofile CSV/apply/工作台闭环）、R42 ⟳ 修复面（ProvidersView/CredsTab）、i18n 六语言补齐复核、admin 端点注册、admin/data_lifecycle_storage.go lock_timeout、attachments UpsertSummary/DeleteOlderThan、sessionforensics tenantID。所有发现均为**线索**，按 conventions §3 须主代理亲读复核后方可登记。

## 一、发现（候选，待主代理复核）

| # | 级别候选 | 发现 | 证据 file:line | 触发路径 | 建议处置 |
|---|---|---|---|---|---|
| 1 | **P1** | **FeedbackRecorder 生产链断裂：`RecordFeedback` 在生产中零调用**，Prometheus classification feedback 计数器（v3_metrics `feedbackTotal`）恒零。带 recorder 的 store（routing_optimizer_init.go:73-77 构造）只被 adapter 调 `Stats()`（只读），永远走不到 `Record()`/`ImportCorrectionsCSV()`；真正接写路径的 admin handlers store（`taskprofile.NewHandlers(h.db)`）从未 `SetRecorder`。a8d3a5bbe 提交语"给从未接线的 Prometheus 计数器第一个生产者"未兑现；e2e Phase 5 通过仅因测试把 recorder 挂到了 handlers 用的同一 store（测试接线≠生产接线），且 e2e 注释自己写明"production wiring: admin handlers carry none" | cmd/gateway/routing_optimizer_init.go:73-78,131-143；admin/handler.go:1359；taskprofile/corrections.go:122-126；taskprofile/csv.go:257-262；autoroute/classification_feedback.go:104；autoroute/v3_metrics.go:123；taskprofile/e2e_loop_integration_test.go:214-217 | 运维在 UI 提交修正或 curl 导入 CSV → 落库成功 → `s.recorder==nil` → 计数器不增 → 修正反馈在 Prometheus 永久不可见（D16 #4 反馈闭环断裂 + D15 #1 新增必有观测） | `NewHandlers` 构造后 `SetRecorder`（与 optimizer 共享同一聚合器实例），并钉一条生产接线守卫测试 |
| 2 | **P1** | **工作台→taskprofile 词汇表不匹配 + 静默吞错**：AnnotationView 提交的 `human_task_type` 取自 L1 八类种子（chat/reasoning/code/**agent/long_context/vision/function_call**/creative + 运营自定义），其中 agent/long_context/vision/function_call（8 类中 4 类）及未知自定义 key **不在 taskprofile 15 类型注册表**内 → 后端 400 → 前端 `.catch(() => undefined)` 静默丢弃。对这半壁分类法，"工作台前端闭环"是空转的，且违反 D15 R42 回注刚立的"每条失败路径都能在 UI 上被用户看见" | web/src/api-work-types.ts:156-165；web/src/views/AnnotationView.vue:190-199；taskprofile/handler.go:124-127；taskprofile/registry.go:33-53；web/src/components/AnnotationForm.vue:136-139（自由值 option） | 标注员把 auto=chat 的请求改判为 agent → POST /corrections 400（unknown human_task_type）→ promise 被吞 → 主标注成功、修正未落库，UI 无任何提示 | 注册表补齐 L1 类型或在提交前按 `getTaskProfile().task_types` 过滤；失败至少 toast/行内提示 |
| 3 | P2 | **apply-tier-config 写入悬空（写了没人读）**：`task_type_tier_config` 全仓唯一运行时读者是 `autoroute.TierSelector`，而 `NewTierSelector` 在生产代码零构造（grep 全仓仅 tier_selector.go 自身与 taskprofile 注释）——apply 成功后运行时无任何行为变化。设计文档 §5.2 明说 TierSelector 消费是"将来可挂点"，但 csv.go:285、handler.go:261、taskProfile.ts:97 注释把它表述为现役"运行时数据源"——注释漂移会误导运维以为落盘即生效 | 全仓 grep `NewTierSelector`（仅 autoroute/tier_selector.go:78）；autoroute/tier_selector.go:38-51,222；docs/planning/TASKPROFILE_MODULE_DESIGN.md §5.2（111-112 行） | 运维 POST apply-tier-config 200 `{applied:[...]}` → 表更新 → 无任何组件读该表 → 路由行为不变（空壳闭环） | 要么接通 TierSelector 读面，要么把注释/文档改为"预置位落盘，当前无运行时读者" |
| 4 | P2 | **ErrTierConfigMissing 指向一条 PostgreSQL 上无法执行的迁移，apply 存在永久 503 死胡同**：`202609_02` 用表级 `UNIQUE(task_type, COALESCE(tenant_id,''))` ——SQLite 风格 DDL，PG 表级 UNIQUE 只接受列名不接受表达式，run 即语法错误（**此结论未真库实跑，见 §三**）；而 ensure 链（724）只建 `task_type_corrections`，installer startup 集也无 task_type_tier_config → installer/ensure 形态部署的库里该表不存在 → 非空 apply 永远 503，且 503 文案指向的"补救迁移"本身就跑不成。e2e 测试自行用 `CREATE UNIQUE INDEX ... (task_type, COALESCE(tenant_id,''))` 表达式索引绕开了原迁移 DDL，侧面印证作者知道原文件不可直接执行 | sql/migrations/202609_02_create_task_type_tier_config.sql:21；db/db.go:419-424,1575-1614；taskprofile/handler.go:276-278；taskprofile/e2e_loop_integration_test.go:157-171 | 全新装/存量 ensure 链库上，修正率达阈值后运维 POST apply → 503 "migration 202609_02 not applied" → 按提示去跑该迁移 → 迁移本身语法失败 → 无路可走 | 补 ensure（或新迁移，真库实跑后定稿）用表达式唯一索引建 task_type_tier_config；252 真库先查该表是否存在 |
| 5 | P2 | **变更型 taskprofile 端点零审计留痕/零结构化日志**：apply-tier-config（改路由行为配置）、import、reload、POST corrections 四个变更端点既不写 `routing_audit_log` 也不打 slog（handler.go 无 log/slog import）。仓库既有惯例是 `writeAuditLog`（admin 包 18 处：model_policy CRUD、credential 生命周期等均留痕）。apply 尤其：改 tier 分层却无 who/when 记录，仅 description 列写死 "taskprofile suggestion (source=...)"，无操作者身份 | taskprofile/handler.go:49-57,263-284,289-296；admin/users.go:52-58；admin/model_policies.go:311 | 运维误把全部类型 apply 成 tier-a → 排查时无任何审计记录可查谁在何时改的 | 至少给 apply-tier-config 接 audit hook（需在 taskprofile.Handlers 上加可选回调，避免 taskprofile→admin 反向依赖） |
| 6 | P3 | **CredsTab.vue 手抄了 ProvidersView 的 refreshBalance 且已漂移**：缺 R42 的 `400 → balanceUnsupported` 映射（厂商无余额 API 时显示原始报错而非友好文案），同一窗口内两份实现已经不一致（错误本身可见——balanceRefreshError 在模板有渲染，非静默失败，故 P3） | web/src/views/provider-detail/CredsTab.vue:876-906（catch 在 899-902）；web/src/views/ProvidersView.vue:526-547 | CredsTab 抽屉对无余额 API 的凭据点 ⟳ → 400 → 显示通用错误文案，与 /providers 抽屉行为不一致 | 抽 composable 共用，钉一条两视图行为一致测试 |
| 7 | P3 | **taskProfile.ts 四个导出无任何视图消费**：`getTaskProfile/getTaskTypeCorrectionStats/applyTierConfig/reloadTaskProfile` 全 web/src 无引用；CSV 导出/导入/apply/reload 无 UI 入口（纯 curl 运维面）。菜单面无恶化（无新路由、无孤儿页；menu-config.json 仅 exported_at 时间戳变化，合规） | web/src/api/taskProfile.ts:84-107；全 web/src grep 无消费点 | — | 登记债（下轮补管理页）或删除未用客户端 |
| 8 | P3 | **attachments `DeleteOlderThan` 分块化修复打在无调用者的死代码上**：全仓唯一 DELETE request_attachments 的函数无任何生产调用者（仅 nil-repo 单测），即该表实际没有在跑的清理路径——D16 三问之"清理"当前无答案（窗口内 commit 把它修好了，但修的是没人调的函数） | domains/attachments/repository.go:281-308；全仓 grep 调用者仅 repository_test.go:103 | request_attachments 随 mirror 钩子只增不删 → 无界增长（登记债，非本窗口引入） | 确认该表 TTL 策略归属（谁该调它）后接线或删函数留档 |
| 9 | P3 | **CSV 导出公式注入面**：request_id/annotator 等用户可控字段未做 `= + - @` 前缀转义，Excel/WPS 打开导出文件可执行公式（admin-only 导出，低危） | taskprofile/csv.go:71-81 | 标注员把 annotator 填 `=cmd|...` → 导出 → 表格软件执行 | 导出侧对危险前缀加 `'` 转义 |
| 10 | P3 | **导入端点无请求体字节上限**：只挡 `ContentLength==0`；行数上限 10000 ✓，但单条超长记录会被 csv.Reader 全量缓冲（admin 鉴权内 DoS 面） | taskprofile/handler.go:241-245；taskprofile/csv.go:111-162 | 鉴权用户 POST 一条 2GB 无换行行 → 内存膨胀 | `http.MaxBytesReader` 包 body |

## 二、核实为健康的面

- **admin 权限**：taskprofile 七个端点全部经 `admin := h.admin`（admin/handler.go:890,1359）注册，与 P2.1 标注端点同层；pool nil → 503 语义统一（taskprofile/handler.go:298-304）。
- **corrections 数据来源 SSOT**：`Record` 从 `auto_route_selections_all`（与 P2.1 标注同一视图，db.go:6345-6346 有建面）取 auto 值，两个标注流不可能对 classifier 原始输出产生分歧；`request_id` 不存在 → 400 `ErrUnknownRequest`、重复 → 409（corrections.go:87-98,113-118；e2e Phase 2 护栏全覆盖）。
- **corrections→blend 读链路闭环**：routingopt `ConfidenceAdjuster` 30d 窗口经 `CorrectionSource` 读 DB，`BlendCorrectionsIntoAccuracy` human×2 加权、失败 fail-open 降级（routingopt/confidence.go:108-126,131-150）——这条反馈回路是通的。
- **CSV roundtrip 本身**：9 列 header 严格校验；bool/float('g')/RFC3339 时间戳导出→导入互通；`agrees` 与标签一致性冻结（防篡改）；`ON CONFLICT (request_id) DO NOTHING` 幂等重放；导入插入不做 auto_route_selections 存在性校验属设计声明（离线回灌，e2e Phase 8 注释明示），与单条 POST 的不对称是有意的。全链路参数化 SQL，无注入面。
- **reason 枚举三处对齐**：前端 `VALID_REASONS`（api/annotations.ts:224-231）= taskprofile `correctionReasons`（reasons.go:10-17）= P2.1 标注词汇，6 值完全一致。
- **724 ensure 链**：`ensureTaskTypeCorrections`（db.go:1581-1614）幂等建表+索引+账本 stamp，"二进制启动即生效"自愈模式在位；724 五点同步已收口（ac39850e0）。
- **UpsertSummary tenantID 修复正确**：`tenantID` 参数化、空→"default"兜底、ON CONFLICT 只覆盖 title/summary/topics/intent 不碰触发器维护的聚合字段；唯一生产调用 service.go:181 已同步传 `args.TenantID`（domains/sessionforensics/export.go:431-469）。
- **data_lifecycle lock_timeout 修复正确**：`SET LOCAL` 在事务块外被 PG 静默忽略（仅 WARNING）——改会话级 `SET` + defer `RESET lock_timeout`（2s 超时、先于 Release 执行），VACUUM/REINDEX 不能包事务的约束有注释说明（admin/data_lifecycle_storage.go:740-760）。
- **i18n 8 语言 parity 全绿**：providers.ts 八个 locale（zh-CN/en-US/zh-TW/ja-JP/de-DE/fr-FR/es-ES/ar-SA）各 315 个 leaf key，逐一程序化比对零缺失零多余；721 的 5 个 balance key（balanceSourceApi/balanceSourceManual/balanceRefreshTooltip/balanceRefreshFailed/balanceUnsupported）八语言全落。
- **R42 ⟳ 修复本体生效**：ProvidersView catch 已写已渲染的 `c.balance_error`，400 映射 balanceUnsupported（ProvidersView.vue:540-547）；AnnotationForm 控件复用（无重复实现），AnnotationView 新代码零新增 UI 字符串（无 parity 风险）。
- **registry V2 legacy 类型补齐**（chat/code/creative/reasoning/planning，registry.go:44-52）覆盖了 252 生产分类器实际输出，修正校验对齐现实。

## 三、未覆盖项与原因

- **`UNIQUE(expr)` PG 语法结论未真库实跑**——只读审计不能起 scratch DB/容器；建议主代理用一次性 psql 会话 30 秒验证（conventions 迁移纪律 #1 同款要求），再定 finding #4 的级别。
- **252/245/154 真库是否已存在 task_type_tier_config**——需真机 `\dt`；若某库手工建过表，finding #4 的"永久 503"面收窄为"该库可 apply 但无人读"（并入 finding #3）。
- **TierSelector 接入后的 5min TTL 缓存多实例失效语义**——当前无生产消费者，故"apply 后 ≤5min 生效"仅是纸面结论；接入时应重新审计跨实例失效（本域已留档）。
- **web vitest run（parity 门禁）未执行**——审计只读纪律不跑构建/测试；parity 用 node 程序化比对替代，结论等价。
- **hostedtask / attachmentmirror mirror_outbox / requestarchive 链路**——b75c91900..HEAD 未触碰（仅 attachments/repository.go DeleteOlderThan），按窗口纪律不追溯。
- **AnnotationView 暗色模式全页审查**——窗口内该文件仅 +12 行逻辑调用、无样式改动，未逐条核既有样式；如需全页三态/暗色审查属存量债扫描，建议 D15 独立轮处理。

**主代理复核结论（R43）**：#1/#2 实锤（与 D11 汇聚）→ 均已修（SetRecorder 接线+守卫测试；registry +6 类+警告条+8语言 i18n key）；#3 实锤 → 注释已纠偏为"当前无运行时读者"（接线是产品决策，登记遗留）；#4 亲核真库实锤且更深（V370 为真实所有者、min_confidence 列亦缺）→ ensure 已按 V370 形态落地+真库验证；#5 成立 → 本轮以 P2 登记为遗留（审计 hook 需回调设计，避免反向依赖），优先接 apply-tier-config；#6/#7/#8 登记；#9 已修（csvSafeFormula）；#10 已修（MaxBytesReader 32MB）。
