# D07 hot + columnar 分区存储不变量 子代理报告（窗口：643735a28^..HEAD，48h；重点未经审计增量：b75c91900..HEAD）

按 conventions.md §3 证据纪律，以下均为线索，须主代理亲读复核后方可登记。

## 一、发现（候选，待主代理复核）

| # | 级别候选 | 发现 | 证据 file:line | 触发路径 | 建议处置 |
|---|---|---|---|---|---|
| 1 | **P0**（CI 必挂，R42 后新增增量 58384b0d8 引入） | **迁移 726 五点同步只落了 2/5**：canonical 文件与 embeddata 文件副本存在（两份字节一致，已 diff 核实），但 ① installer/internal/dbinit/runner.go StartupFiles 无 726（列表止于 725）；② installer main.go 无 726 的 go:embed var 与 embeddedSQLFiles 条目（map 止于 725）；③ scripts/apply-db-revision-sequence.sh 序列通道无 726（files 数组止于 725）；④ stats_migrations_test.go parity map 无 726。`TestStartupFilesAreAllEmbedded`（embeddata ReadDir ⊆ StartupFiles，.down 豁免）与 `TestCanonicalStartupMigrationsAtOrAbove704AreRegistered`（canonical ≥704 ⊆ StartupFiles）两个契约测试在 HEAD 上**静态可判必红**（embeddata 与 canonical 目录均含 726 up 文件） | installer/cmd/llm-gw-installer/embeddata/startup/726_restore_credential_model_index_hot_unique.sql 存在但 runner.go:165-185、main.go:613-618、scripts/apply-db-revision-sequence.sh:467-470 均止于 725；测试体 stats_migrations_test.go:308-346（ReadDir⊆StartupFiles）、:342-389（≥704 登记） | 58384b0d8 提交信息自述仅跑了 `go test ./sql/migrations/startup/`——该包不含上述 installer 测试，作者未察觉红灯；任何跑全量 `go test ./...` 的 CI/验证门即挂 | 照 720/721/724/725 的 R40 收口模式补齐三点：runner.go StartupFiles 追加 726、main.go go:embed var + embeddedSQLFiles 条目、sequence 通道登记（含注释）；补 parity map 条目；跑 `go test ./installer/...` 验证 |
| 2 | **P1**（全新安装功能性破坏，#1 的独立后果面） | **installer 全新装在 718 应用后 credential_model_index_hot 冲突元组上零唯一索引**：baseline 01-schema 只建 347 名 `credential_model_index_hot_unique_key`；installer 链不含 347/354（StartupFiles 起点为 478），保留名 `idx_credential_model_index_hot_unique` 的唯一创建者 startup/354 不在链内；718 在链内 drop 掉 347 名；Go 侧无任何 ensure 复活该索引（db/*.grep 零命中创建语句）。726 本可经 IF NOT EXISTS 无缝修复新装，但因 #1 未进 StartupFiles | sql/schema/01-schema.sql:22797-22800（baseline 仅 347 名）；718_drop_redundant_indexes_and_add_ttl_indexes.sql:45-46（drop 347 名/三号）；sql/migrations/startup/354_credential_model_index_hot_independence.sql:57（保留名唯一创建者）；installer/internal/dbinit/runner.go:33（起点 478，无 347/354）；bg/auto_index_refresher.go:191（ON CONFLICT 依赖该索引） | 全新装：InitSchema（baseline→StartupFiles 含 718）→ 718 drop 后 hot 表无唯一索引 → 网关启动后 auto route listener 每个 5-min ticker 的 rollup `ON CONFLICT (bucket, credential_id, raw_model)` 必炸 42P10 → 与 154 生产事故同构，且在所有新装环境结构性复现 | 随 #1 把 726 接入 StartupFiles 后，新装链自动痊愈（726 的 ctid 去重在全新装上为空操作）；建议补一条"新装后 hot 唯一索引存在性"的 fresh-install 集成断言防复发 |
| 3 | **P1**（存量库标准升级通道缺修复登记，#1 的第二后果面） | **apply-db-revision-sequence.sh 未登记 726 → 154/245 类"718 应用后唯一索引归零"的存量库无法经标准通道修复**；该序列通道同样不含 347/354（数组起点为 677/655 一带），故序列通道是这些库获得 726 的唯一正规途径 | scripts/apply-db-revision-sequence.sh:112-470（files 数组无 726；亦无 347/354 startup 版）；sql/migrations/startup/726_*.sql:46-50 头注自述 154 只做过**只读**核实（pg_indexes 清单 + 0 行确认），未记录 up 实跑 | 运维在 154/245 重放 revision-sequence → 725 即终、726 永不执行 → rollup 42P10 持续；若带外手工 psql 726 则绕过了登记通道的幂等/marker 记账 | 726 登记进 files 数组（注释说明 154 事故背景）；同时核对该迁移是否已在某真库实跑（迁移三纪律 #1：726 头注只有只读核实记录，**未见任何实跑证据**，静态契约测试不覆盖 DDL 真跑） |
| 4 | **P2**（确认仍开放，非本轮新增；9769a18c4 已升级待办） | **claim 置位 is_final_success 结构性漏镜像未在窗口内修复**：直接 `UPDATE request_logs_hot SET is_final_success = TRUE` 的 SQL 侧置位不触发 hooks → mirror 登记缺失，重放器天然无效；09-18 首扫 FAIL（GLOBAL_G2=1），连续归零计数清零重起，earliest 顺延 09-25；待办为"S4 停写评估前必须修复 claim 路径或登记例外类 E6"。窗口 diff 对 client.go 该路径零改动 | domains/hooks/observability/telemetry/client.go:2486（claim UPDATE，窗口未触碰；`git diff b75c91900..HEAD` 对 is_final_success 零命中）；docs/03-design/04-data-design/storage-observation-ledger.md:166-178（09-18 FAIL 判定与待办升级）、:135/:149/:163（前史） | 后台 final-success claim/usage 修正每见终态即直接 SQL UPDATE request_logs_hot → 写侧合规（**写只在 hot 不变量未破**），但 hook 管道看不到 → G2 度量恒有 ~1 行/天缺口 → "每日回填兜底"成为单点依赖 | 维持账本定性；S4 评估前落 ① claim 路径补发 mirror 触发 或 ② 正式登记 E6；勿让"回填 cron 常态化"替代修复 |
| 5 | **P3**（窗口内被触碰的既有读端点，pre-existing 缺陷） | **lookupTurnNumber 读 request_logs 母表未联合 hot**：request_logs_hot 是独立表（非分区/继承成员），8h 窗口内会话行只存在于 hot，`SELECT COUNT(*) FROM request_logs WHERE gw_session_id=…` 看不到它们（含同事务刚 INSERT 进 hot 的当前行，行内注释"inclusion"断言不成立）→ 活跃会话 count≈0，turn_no 塌缩为 1。窗口改动仅追加 `ts >= NOW()-30d` 修剪（恢复分区裁剪），未引入也未修复 hot-miss | domains/hooks/observability/telemetry/client.go:3415-3455（COUNT 母表 + 30d 新增）；:1146（同文件 request_logs 热写路径 INSERT INTO request_logs_hot）；sql/schema/01-schema.sql:13571（request_logs_hot 独立 CREATE TABLE）；注释自认"session/v2 aggregator owns the authoritative turn_no"（client.go:3427-3431） | 终态请求 → outbox 构建 RequestCompleted V3 事件（client.go:1767 buildRequestCompletedEvent）→ turn_no 取母表计数 → 8h 内全部返回 1（或陈旧值）→ 事件内 turn_no 字段失真（权威 turn_no 另有归属，故降为 P3） | 若该字段仍有消费方：改计数为 hot∪mother 联合（`COUNT(hot)+COUNT(mother)` 去重或 UNION ALL）；若无消费方：P3 清账注释纠偏"including the current insert"错误断言 |

## 二、核实为健康的面

- **726 迁移本体质量（除登记缺位外）**——up 为 ctid 守卫去重 + `CREATE UNIQUE INDEX IF NOT EXISTS`（幂等，新装/已建库 no-op），显式 BEGIN/COMMIT 事务包裹；两条应用通道均为裸 `psql -f`（apply-db-revision-sequence.sh:11,22 无 `--single-transaction`；installer runner.go:242 docker exec psql -f），内嵌 BEGIN/COMMIT 无嵌套冲突。非 CONCURRENTLY 建索引会在建期阻塞 hot 表写，但 hot 表 8h 窗口体量小（154 实测 0 行），风险可忽略。down 为精确语义反转且头注警示 42P10 重开。migration_726_test.go C1-C4 契约（索引元组与 bg/auto_index_refresher.go ON CONFLICT 元组解析绑定、去重先于建索引、禁 DROP 保留名、embeddata 字节一致）设计到位——但 C4 只钉了 embeddata 副本，钉不住 runner/main/sequence 三点（这正是 #1 漏网原因）。
- **3a9dca46d schema identity 修复未动分区拓扑**——01-schema.sql 全 commit 仅 +1 行：api_key_auto_profile 的 PRIMARY KEY（sql/schema/01-schema.sql:5279），普通表；request_logs 六表族 baseline 零触碰，R36 列型守卫不涉及。
- **api_key_auto_profile 是普通表，未误入分区族**——独立 CREATE TABLE（01-schema.sql:5273-5281），无 _hot 对、无 promote 拓扑登记（bg/partition_manager.go 零命中）；写入仅 admin/auto_route.go:421、autoroute/decision.go:897 两处 upsert（ON CONFLICT (api_key_id)）；"更新删除只在 hot"约束对其不适用，合规。ensure 修复链有双测试钉桩（db/db_api_key_auto_profile_test.go:11-88，含 SET LOCAL 误用反制断言）。
- **bg/partition_manager.go**——窗口唯一改动是 runWithBypass（RLS 旁路事务包装）套在 provider_error_details TTL 清理上（bg/partition_manager.go:1317-1330,1456-1484）：普通表、非分区族；修的是 Phase2 降权后清理静默 0 行，无分区不变量影响。
- **bg/vacuum_worker.go**——移除对分区父表 request_logs_bodies 的 VACUUM FULL（父表无存储、纯耗 VacuumFullMutex 窗口），保留仅对 request_logs_bodies_hot 的普通 VACUUM (ANALYZE)（bg/vacuum_worker.go:159-170）——与"分区空间回收归 drop 清单"的正确分工一致；8h 保留/promote 调度零触碰。
- **admin/data_lifecycle_storage.go**——`SET LOCAL lock_timeout`（事务块外被 PG 静默忽略）改会话级 `SET` + defer `RESET`（admin/data_lifecycle_storage.go:743-757）：VACUUM/REINDEX 不能入事务块，修法正确；维护 ops 不触及分区拓扑。
- **窗口写路径唯一性（§3.1）**——对 b75c91900..HEAD 全部 `+INSERT/UPDATE/DELETE` 扫描：落点仅 providers、credentials、session_summaries、task_type_corrections、task_type_tier_config、api_key_auto_profile、provider_error_details（旁路事务）、request_attachments（ctid 批删改造，普通表 01-schema:12938）及各 _hot 表，**无任何直写分区母表的新增写入点**；claim UPDATE 落 request_logs_hot 合规。
- **724/725 五点同步完整**——canonical + embeddata + runner.go:181,184 + main.go:463-470,617-618 + sequence 脚本：463-470 + parity map（stats_migrations_test.go:166-170）五点齐；724 为独立普通新表（写方 taskprofile/corrections.go、csv.go），725 纯 policy（DROP IF EXISTS + CREATE 幂等，GUC 标准形），均无分区拓扑影响。
- **时区钉扎（§3.5）**——窗口内无新增 promote/ensure 类 SQL 函数（726 只建索引、724 只建表、725 只建 policy），无新钉扎面。
- **R42 D07 发现 #1/#2 的修复在链上可见**——516/520 已入 StartupFiles（runner.go:33-40 注释与条目）、723 to_regclass 守卫由 9c99f3c40 落地（本轮未逐行重验，R42 主代理已核）。

## 三、未覆盖项与原因

- **`git fetch` 核对 726 编号远端冲突**——fetch 会改写本地 refs，违反只读纪律；仅确认本地无 727+ 与之撞号。
- **两个 installer 契约测试的实跑红/绿**——`go test` 需写构建缓存，违反只读纪律；红为静态推导（embeddata ReadDir 与 canonical 目录内容 vs StartupFiles 字面清单，逻辑简单直白），建议主代理以 `go test ./installer/cmd/llm-gw-installer/ -run 'TestStartupFilesAreAllEmbedded|TestCanonicalStartupMigrationsAtOrAbove704' -count=1` 复核坐实。
- **726 是否已在任一真库实跑**——迁移头注只记录 154 只读核实（pg_indexes/行数），commit 验证段只列静态测试；无法从仓库判定带外手工执行与否，直接影响 #1/#3 的处置顺序（若已带外实跑，登记时补记 evidence；若未实跑，按迁移三纪律 #1 先实跑再定稿）。
- **fleet 内还有多少存量库处于"唯一索引归零"态**——需逐真库查 pg_indexes（154 已证实，245 未查）。
- **GLOBAL_G2 09-19 每日轮结果**——按惯例属主代理轮文档固定动作，本报告时点尚未产生。
- **lookupTurnNumber turn_no 的下游消费方全量清单**——未逐一追踪 RequestCompleted V3 事件 turn_no 字段的所有读取者（影响 #5 的 P3 定级是否可再降），超出本轮窗口预算。

**主代理复核结论（R43）**：#1 亲跑复现双红 → 已补三点+parity map，双测试转绿；#2/#3 为 #1 的两个后果面，随登记收口（sequence 通道已补；本地真库核实 idx_credential_model_index_hot_unique 已在、形态与 726 一致——带外已实跑，证据记入轮文档）；#4 维持账本定性登记；#5 与 D03#1 合并登记。
