# D01+D02（IR 生命周期 + 协议适配）子代理报告（窗口：51d6147c7..de7efd453）

> 核心审查对象：commit 7cdf11eb4「R45 thinking intent TargetProvider 前送完整修复——IR context 携带方案」。改动面（本域子集）：internal/ir/source_reasoning_context.go(+test)、domains/streaming/handler_gemini.go、domains/streaming/executors/executor_chat.go、inline_validation.go、executor_source_reasoning_test.go。窗口内其余 commit（2c5d1f804..7ada7d87b）均为 telemetry/analysis/autoroute/livestream/docs 域，不涉本域。

## 一、发现（候选，待主代理复核）

| # | 级别候选 | 发现 | 证据 file:line | 建议处置 |
|---|---|---|---|---|
| 1 | P2 | **R45 恢复点只盖 OpenAI 形上游，anthropic-messages 上游的 Q3 转换路径无恢复——同类 P5 缺陷残余面**。Gemini 入向（thinkingConfig>0）路由到 `cand.Protocol=="anthropic-messages"` 候选时（executor_dispatch.go:813→executeAnthropic），prepareAnthropicRequestBody 的 Q3 分支用 `ParseOpenAI(sourceBody)` 重解析 step-6 合成 body（intent 已在 handler 丢失）再 `SerializeAnthropic`，全程无 `RestoreSourceReasoning`；serialize_anthropic.go:95-134 明明具备 Thinking/Reasoning→anthropic thinking 的渲染能力却拿不到输入。触发路径：Gemini 客户端 POST /v1beta/models/{m}:generateContent 带 thinkingConfig → 模型解析到 anthropic 形候选（claude relay 族，本仓常见）→ intent 静默丢（仅 step-6 先发一条 loss 异常，不阻断）。注：恢复该分支时需同时恢复 SourceProtocol（SerializeAnthropic 的 Reasoning→thinking 转换不受 SourceProtocol 排除键约束，但保持两半程语义一致）。 | domains/streaming/executors/executor_anthropic.go:478-480（needsConversion 判定）、:509（ParseOpenAI(sourceBody)）、:557（SerializeAnthropic）；对照 executor_chat.go:1834 的 OpenAI 侧恢复点 | 在 Q3 分支 ParseOpenAI 后、SerializeAnthropic 前补 `irReq = ir.RestoreSourceReasoning(irReq, params.R.Context())`；补 executor_anthropic 层钉桩（gemini 载体 → anthropic 上游渲染 thinking 对象） |
| 2 | P2 | **thinkingBudget=0（Gemini「关闭推理」语义）经恢复链反转为开启推理**。parse_gemini 把任意 thinkingBudget（含 0=disable、-1=dynamic）一律映射 `Reasoning{Type:"enabled"}`；openAIReasoningIntent 因 `Type=="enabled"` 判 ModeEnabled（budget==0 也过 `budget>0‖Type=="enabled"` 门）→ renderMiniMax 输出 `{"type":"adaptive"}` / renderGenericThinking 输出 `{"type":"enabled"}`。R45 之前该 intent 从不渲染（丢弃），本修复使其首次到达上游线——显式关推理的 Gemini 请求到 minimax/deepseek/glm/ark/qwen 变成开推理，且渲染成功无 loss 上报。触发路径：`generationConfig.thinkingConfig.thinkingBudget:0` → handler step-4.5 attach → executor 恢复 → dialect 渲染 ON。parse 侧映射在窗口外（R43 前），属"窗口外旧映射 × 窗口内新激活"交叉缺陷。 | internal/ir/parse_gemini.go:557-561（0/-1 均映射 enabled）；internal/ir/serialize_openai.go:311-325（`budget > 0 || r.Type == "enabled"`）；internal/reasonnorm/norm.go:444-451（renderMiniMax：非 disabled 一律 adaptive）、:427-442 | parse_gemini 对 `*ThinkingBudget==0` 映射 Type:"disabled"（-1 可保守保持 enabled≈adaptive）；或 intent 层 budget==0 视为 disabled；补 budget=0 钉桩 |
| 3 | P3 | **step-6 序列化点恒发 loss 异常，R45 修复后成为已修复流量的持续假阳性**。handler step-6 路由前 SerializeOpenAI（TargetProvider 空→openai_chat 方言→不渲染）必触发 `reasoning.budget_tokens` loss 上报；executor 侧恢复并渲染成功并不抵消这条（两处在不同 IR scope，anomaly 去互不感知）。运营视角：每笔成功携带 thinkingConfig 的 Gemini 请求都报一次丢失，淹没真丢失（含发现#1）。 | domains/streaming/handler_gemini.go:317（step-6 SerializeOpenAI）；internal/ir/serialize_openai.go:1055-1066（loss 分支，src=gemini≠openai 即报）；对照 executor_chat.go:1669（executor 独立 scope） | 恢复渲染成功时对该请求的 step-6 loss 做标记/降噪，或把 loss 判定挪到"最终出向体不含 thinking 且 intent 存在" |
| 4 | P3 | **PARAMREG_ENABLED=false 应急回退模式下，恢复后的 SourceProtocol 使 Extensions 停止还原**。restoreExtensions 的旧门禁回退路径以 `SourceProtocol != targetProtocol` 拒绝还原；RestoreSourceReasoning 把重解析 IR 的 SourceProtocol 从 openai-chat（==目标）盖写为 gemini-generate（≠目标）→ 合成 body 未知顶层字段的还原从"同协议还原"变"跳过"。触发面窄：需显式设 PARAMREG_ENABLED=false（紧急回退杠杆）且 body 带未知字段。 | internal/ir/extensions_restore.go:52-56（回退门禁）；internal/ir/source_reasoning_context.go:113-116（协议盖写） | 回退门禁对"目标=OpenAIChat 且 SourceProtocol 为重解析 stamp 被恢复改写"场景豁免，或接受并登记 |
| 5 | P3 | **"body 优先"守卫对扩展形态的 thinking 不成立（未来发送方陷阱）**。ParseOpenAI 把顶层 `thinking` 收进 Extensions（knownFields 无此键）；RestoreSourceReasoning 的 body-first 守卫只看 ir.Thinking/ir.Reasoning。若未来某发送方 attach carrier 的同时其 body 自带 dialect thinking 对象，applyThinkingToOpenAIChat（先跑）渲染 carrier 意图后，restoreExtensions（后跑、不覆盖已有键）不再还原 body 原生值 → carrier 反超 body。当前生产不可达（gemini step-6 body 必无 thinking），纯登记。 | internal/ir/parse_openai.go:66-88（thinking→Extensions）；internal/ir/serialize_openai.go:163 vs :192（渲染先于扩展还原的顺序）；internal/ir/source_reasoning_context.go:108-110 | 弱化注释断言，或在守卫中加 Extensions["thinking"]/["enable_thinking"]/["thinking_budget"] 检查 |
| 6 | P3 | **新增测试两处钉桩弱点**：(a) `TestFinalizeOpenAIUpstreamBody_SourceReasoning_LegacyWithIR/unknown catalog` 用例名承诺 "drops with loss report"，实际只断言 thinking 键不存在，未验证 loss 上报发生；(b) `...SurvivesDerivedContext` 用例名与实际钉住语义相反——它钉的是"内层空载体遮蔽外层真值=视为无携带"，且用例内注释自相矛盾。 | domains/streaming/executors/executor_source_reasoning_test.go:110-123、:193-212（含 :205-207 注释） | (a) 断言 anomaly reporter 收到 loss；(b) 改名并清理注释 |

## 二、核实为健康的面

- **context 携带链全程保值（问题 1）**：attach（handler_gemini.go:310）→ newGeminiSyntheticRequest（:206）→ ChatHandler 全部 ctx 自 r.Context() 派生（handler.go:2162 起）→ buildExecParams R: r（handler.go:4042）→ survival FreezeOwner=WithValue（survival_wiring.go:67-71）→ executor SetTenantID 再派生 → finalize 三处 params.R.Context()。streamretry wrapper 本身保值（wrapper.go:363-365/:384），**但 Gemini 入向实际不经过 streamretry wrapper**——GeminiHandler 直接调 chatHandler.ServeHTTP，真正承载重试的 goal-retry 循环 / executor dispatch failover / survival coordinator 三条路径均已核对保值。commit message 的 streamretry 论述技术上正确但不在本链路上（表述小瑕疵）。
- **三分支恢复点齐全且幂等（问题 4）**：legacy_with_ir（executor_chat.go:1828-1834）、断路器兜底（:1877-1879）、e.IR==nil inline（:1936→inline_validation.go:46-49）；context-length recovery 重入 finalize（:1046）时重复恢复无害。anthropic_to_openai_ir 分支免恢复理由成立：ClientProtocol==anthropic-messages ⇒ ParseAnthropic 从原始 body 原生恢复 ir.Thinking；gemini 载体不可能进入该分支。该分支非断路器 parse 失败/serialize 失败路径会静默丢 intent，但 ParseOpenAI 解析自己产出的合成 body 实际不可失败，风险可忽略。
- **openAIReasoningIntent 排除逻辑与恢复交互正确（问题 3）**：gemini budget 形真过门；effort 形不被误伤；Anthropic ir.Thinking 优先级最高且该路径无载体无恢复。SourceProtocol 盖写只打三态，非 OpenAI 身份保留；resolveTargetDialect 不读 SourceProtocol，协议盖写不影响方言决策。
- **无携带零回归**：原生 OpenAI 入向 RestoreSourceReasoning 纯 no-op；OpenAI 直连客户端带顶层 dialect thinking（走 Extensions）路径先后序不变；applyInlineValidation 签名变更全仓仅一处调用方。
- **测试真实性（问题 5）**：executor 层测试经 irAdapterForTest 走真 ir.SerializeOpenAI；geminiSyntheticBody 用 handler 同款管线真产生 step-6 形态；TestSourceReasoning_Step6SyntheticBodyLacksThinking 先钉住"缺陷前置事实"。非"测试自己证自己"。
- **指针别名无腐化风险**：restore 后 executor IR 与 handler irReq 指针共享，但 ValidateAndFixRequest 全在恢复前执行，渲染器只读，无写入点。
- **D02 矩阵其余格（问题 6）**：anthropic 源×openai 族=原生渲染（2026-09-18 修复）；gemini×gemini 上游 dispatch 显式跳过（executor_dispatch.go:815-826）；gemini×responses-native 上游显式 501（executor_chat.go:375-395）——均无静默丢 intent 面。压缩尾变换只裁 messages 保顶层键。
- **R45 登记条目与代码一致**：volcengine-coding/volcano-normal/volcano-tokenplan/azure-openai/google-gemini 确未登记 catalogToDialect → DialectUnknown。维持"登记不修+逐上游真机验证"处置。
- **三门验证**：build/vet/test 三改动包全绿。

## 三、未覆盖项与原因

- **真机端到端未复现**：commit 自述"MiniMax pre 129→post 160 恰 +31B"无法在本环境验证，采信自述并经代码路径逐环核对（renderMiniMax adaptive 与 +31B 字节形态吻合）。
- **发现#1（anthropic 上游缺口）若修复需真机**：Gemini thinkingConfig → Anthropic thinking 对象的接受度需逐上游验证。
- **reasonnorm 对 budget=-1（Gemini dynamic）的钳制**：未跑边界用例（-1 不会上 wire，dynamic→adaptive 语义等价性未深究）。
- **includeThoughts 静默忽略**：parse_gemini 解析该字段但从不映射，暂无影响面；未展开为发现。
- **streamretry wrapper 直连 OpenAI 入向行为**：与本修复无携带交互，仅作链路论述核对。
