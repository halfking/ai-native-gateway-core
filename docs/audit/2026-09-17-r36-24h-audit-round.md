# R36 — 24h 审计轮（2026-09-17）

- 窗口：`643735a28..876302d5e`（2026-09-17 01:18 → 04:56）——R33/R34/R35/R35-gap 四个并行审计轮落地后的**未经独立复核的修复提交段**（45054170c 九项修复、313d1ebc8 探测健康统一、4552a2021 契约锁、462fc9a6f/01bd55ed7 抽检）+ R34/R35 两个"下一轮提示词"指向的遗留修复
- 方法：playbook 路线。codegraph 全量重建（增量 malformed→rm -rf 重建）+ 主代理 + 6 路只读子代理并行（D01+D02 / D03+D05 / D04+D08 / D07+D06 / D09+D12 / D14+D15+D16+D17），每条 P1/P2 线索经主代理亲读复核后才处置
- 会话前置：拉取 origin/main 快进合并（2f3151a27→876302d5e，12 提交，无冲突；本地仅版本自动漂移取上游 build_seq 2118）

## 一、本批修复（P1×2 / P2×7 / P3×4 + 存档/注释批，提交见 git log）

### P1

| # | 发现 | 证据 | 处置 |
|---|---|---|---|
| P1-1 | **scan_scheduler 预算豁免把真失败一并豁免**：R34 豁免分支含 `errors.Is(err, context.DeadlineExceeded)`，而 Go ≥1.23 的 http.Client.Timeout 满足该判定（本机 go1.27 实测钉死）；扫描器 30s Client.Timeout 命中豁免 → 挂起供应商 `consecutive_scan_failures` 永不加 1，3 次自动禁用**永久不可达**，且每挂起模板每轮烧 30s 饿死 sweep 尾部 | bg/scan_scheduler.go:370-381 + provider_scanner.go:51 | 豁免收紧为仅 `ctx.Err() != nil`（父 sweep 上下文），抽 `scanCutBySweepBudget` 助手；钉桩 TestScanBudgetExempt_ClientTimeoutCountsAsFailure（实证 errors.Is 命中 + 父 ctx 存活时不豁免）+ TestScanBudgetExempt_SweepCtxCanceled |
| P1-2 | **01-schema 列型漂移致全新安装整体不可用**（R34 遗留#2 修复）：动态 hot∩parent 交集 UNION（request_logs_view_schema.go:109 / 迁移 680:84）在 10 列型差（6 硬不兼容+4 varchar↔text）下 42804 → 启动链中止；602 promote 投影 customer_id（hot text→母表 bigint）每批 42804 空转 | sql/schema/01-schema.sql 母表 :7376+ vs hot :13570+（子代理取证，主代理逐列复核；**实测 10 列**，R34 "12 列"多报 tenant_id/response_body） | 迁移 **717_request_logs_hot_column_alignment**（ALTER 10 列对齐母表，JSON cast 带正则预筛兼容 PG15，down 尽力还原+文档化）+ 三份 baseline hot 块手工对齐（dump-schema 需真库，列入部署前置）+ 新守卫 TestBaselineRequestLogsHotColumnTypesMatchMother（129 共享列逐列型等价×3 baseline）+ TestBaselineRequestLogsAlignedColumnsPinned（10 列目标型钉桩）+ installer 五点同步（embeddata 副本/go:embed/embeddedSQLFiles/StartupFiles/parity map）+ sequence 通道登记 |

### P2

| # | 发现 | 证据 | 处置 |
|---|---|---|---|
| P2-1 | **探测"立即测试"失败路径结果黑洞**（子代理报 P1，复核降级）：单模型 trigger 走 legacy TriggerManual，成功路径有 MarkNodeProbeHealthy 同步（model_probe.go:1352/1464），**失败路径**零痕迹——recordRun 写 model_probe_runs_hot、node_probe_runs 无行、node_probe_state 不更新，运营者付费调用后 UI 无任何反馈 | admin/probe_history.go:229 + bg/model_probe.go:1247 | trigger 端点在 probeSubmitter 已接线时改走 `NodeProbeWorker.Submit`（同 routing_reset trigger_probe 模式，返回 queued:true+via），完整继承新系统失败记账+退避；legacy fallback 保留；trigger-all 保留同步实时结果（其 runs 不入 node_probe_runs 登记遗留） |
| P2-2 | 附件下载端点无归属校验（R35-gap 遗留#4 归属半边）：内容寻址布局 URL 无 request_id 无租户，tenant_admin 枚举/泄漏获得 relPath 即跨租户读文件 | domains/attachments/handler.go:77（TODO）+ admin/attachments_routes.go | tenant_admin 下载前 fail-closed 归属校验（新布局 request_attachments→request_logs 租户联查；历史 req_<id>/ 走 request_id 快路径；拒绝 404 防存在性泄漏；DB 错误 500 不放行）；super/admin_key 与列表端点同口径不收敛 |
| P2-3 | **FREE- 指纹校验时序倒置**（/activate）：Activate 先占座（无回滚）再 403 → 异机占掉唯一席位，真机撞 409（R30 L-1 占座 DoS 复活形态）；指纹不可用静默跳过 | licensing/bootstrap_api.go:205 vs :237-247 | 校验前移到 Activate 之前 + fail-open 路径补 Warn；钉桩 TestHandleActivate_FreeLicenseForeignMachineRejectedBeforeSeatTaken（403 + 席位未被占） |
| P2-4 | diagnostics 两端点只重置旧探测系统：凭据恢复/路由阻断修复清 model_probe_state 但不碰 node_probe_state，v_routable 的 node_probe_failed 门未清，"修复成功"后路由仍剔除 | admin/diagnostics_credential.go:259、diagnostics_routing.go:300（对照 routing.go:5497 双清样板） | 两端点补 node_probe_state 双清（复用 force_enable 同款 UPDATE，按凭据/按 provider 两个作用域） |
| P2-5 | 可用性缓存回填以冻结旧表为源：新模式下 model_probe_state 停更，backfill 周期把冻结状态刷进 llmgw:avail | bg/model_availability_backfill.go:173 + admin/probe_dashboard.go:2249 | 两处查询源切 `v_node_probe_state_compat`（列名兼容投影） |
| P2-6 | **镜像白名单丢 provenance 三键**：P0-1 写端的 window_source/alignment_map_truncated/sanitize_refs_truncated 被 safeCompressionMeta switch 静默丢弃，commit message"白名单已知这些键"不成立 | internal/sessionv2mirror/hook.go:400-439 | 白名单补三键（window_source 经 safeWindowSource 有界过滤：≤16 键×boundedCompressionWord×boundedNumber；两旗标 bool）+ V2 读侧 CompressionMeta 结构补三字段；钉桩随白名单键生效路径复跑 sessionv2mirror 全绿 |
| P2-7 | durable_contract.go UNUSED 头标注事实错误（"zero production callers"实为已接线 dormant 特性）——照标注清理会误删活特性；action_bridge.go 措辞同病 | durable_contract.go:1-2 vs handler.go:3116/5137、main.go:2809-2820 | durable_contract 标注改为 DORMANT+已接线+不可整体删除（登记 roadmap 决策）；action_bridge 改"构造链生产未接线" |

### P3

- **survival 层 KindContextLength 压缩重试**（R35-gap 遗留#2）：centralActionForTaskWithHistory 不动（durable worker 复用聚合但无 body 钩子，决策层放行会空转烧 retry ceiling——TestDurableRecoveryWorkerTerminalDecisionFails 红灯实证后裁决）；改判收进 SurvivalCoordinator.Run（`survivalCtxLenCompressRetryDue`：终态+未提交+context-length+一次性预算）+ retry 分支 body 机械压缩（`survivalCompressBodyForRetry`，CompressMessagesIfNeededBody 窗口 len/4，responses 形态天然短路、压缩失败即终态不重撞）+ 钉桩 ×3
- call_history_aggregator 双实例互写桶聚合（R34 遗留#4）：接 `SetDistLock`+acquireSweepDistLock 单写者选举（实例本地 watermark+replace ON CONFLICT 使双实例互摋，不止浪费）；insertBatch 撒谎注释改写为真实的"replace+单写者=收敛"契约；main.go 接线
- hosted_task_reconciler tick 无整体预算（R34 遗留#3）：dispatch/project 两 pass 各套 45s 独立预算（hostedTaskPassBudget），ACC 黑洞不再饿死 reaper/callback pass
- settle/affinity/metrics 三 worker panic guard 在 run 外层（R34 遗留#6）：一次 sweep panic 永久停摆；加 `safeSweep` per-cycle recover，循环存活
- model-toggle 手动上线清 `last_err_code/last_err_detail`（展示面残留 manual_offline/旧错误码）
- responses 流式 refusal 终态对齐（`openaiFinishReasonIsError` 补 "refusal"，与非流式 incomplete/content_filter 一致； refusal-only 流式响应不再渲染成 completed 成功）+ 钉桩
- sanitizer offsets 锁降级日志 Debug→Warn + rand 失败补日志（跨进程互斥失效可观测）
- mergeCompressionMetaV3 "Producer-side facts win" 注释与先到先得行为相反——纠偏
- 6 文件头标注 §五→§四 指针修正

## 二、复核裁决（子代理线索 → 主代理亲读结论）

- **D09#1"探测结果黑洞"→ 降级 P2（仅失败路径）**：亲读 model_probe.go:1352-1360/1464-1480 证实成功路径 MarkNodeProbeHealthy 同步新系统；子代理"UI 永远无变化"不成立
- **D03#4 提交文档口径**：window_source 等三键"白名单已就绪"不成立（switch 无 default）——以补白名单修复结案
- **F2 决策层放行 → 一次红灯驳回**：TestDurableRecoveryWorkerTerminalDecisionFails 失败暴露 durable worker 复用聚合、无 body 钩子；改判收进 coordinator 本地，durable 钉桩回归绿
- **01-schema "12 列"→ 10 列**：守卫测试以 129 共享列逐列比对证实 tenant_id/response_body 两列无漂移，R34 清单多报 2 列
- D01#2（chat tool_calls 数组无 dedup）、D01#1（toolUseNameFromID 双向歧义）：成立但可达性依赖混合形态上游/在途旧对话，登记不动
- D09#5（HK 存量订阅不回填）、D09#3（reviver 无门控）、A-1（716 down 半回滚）、A-2（716 双通道无全文守卫）、A-3 余下直读旧表面、D09#6/#7/#8/#9、D04#4/#5/#6/#7/#10、D14#2/#3（i18n 硬编码）：登记（见 §四）

## 三、核实为健康的关键面（6 域子代理+主代理抽读）

- R35 补面九项修复本体：P0-1 provenance 写端主链路单一写者读写对齐、P0-2 锁骨架（粒度/唯一写者/无泄漏）、P1-1 Resumable 口径、P2-1 正则无误伤、P2-3 strict 钉桩、P2-4 三字段有界校验——除白名单三键与可观测性外全部成立
- 313d1ebc8 视图统一：716 四不变量、双通道逐段一致、列序钉桩、installer 五点同步全绿、bans 回放不回放主动清空、双代理冲突不存在
- 4552a2021 契约锁与生产函数逐字段一致；R34 Gemini 四连修复横向不变量（thought 三解析点/出向三态/tool id 统一/refusal 四序列化器）
- exponentialCooling/EMA/affinity distlock/attempt_gate_context 标注/流式 handler 并发面/D08 两去向不变量
- ops 改密脚本三闸未回退无密钥落盘；web 类型与后端 JSON 对齐；goal 影子轮 R35 修复在位
- RFC auto-multidim（阶段 0）：确为纯方案零代码，无方案-代码漂移

## 四、遗留登记

**新登记（均经亲读确认）**：
1. **P2 | 代理订阅子系统与数据面脱钩**（承 R35-gap #1，架构级）：RequiresProxy/egress_profile/proxy_subscription_id 零读取方——需产品级设计把 GetProxyTransport 接入 upstream Transport 池；同批 #2 survival context_length 已由本轮闭环，#4 附件归属已闭环（MM-1 签名 URL 半边仍开放）
2. **P2 | 01-schema 修复的部署前置**：717 迁移在 252 生产执行前必须真库复核 `information_schema.columns`（baseline 头注释 08-04 dump、mtime 09-14 不一致）；随后 dump-schema.sh 重导三 baseline 替换手工对齐体
3. **P2 | credential_recovery 守卫读冻结旧表 + BrokenProbeReviver 无门控改旧表**（D09#3）：被新系统证实坏死的模型可能随凭据恢复入池
4. **P2 | 716 双通道无全文等价守卫**（D07 A-2）+ **716 down 半回滚**（A-1）：collapseWS 全文比对测试 + down 补旧体（或文档化不支持）
5. **P2 | A-3 余面**：credentialstate/cache.go:165、credentialhealth/checker.go:642、provider/client.go 三处、routing.go:5542/5557 仍直读旧表，逐点核验 useNewProbeMode 门控
6. **P3 批**：HK 存量订阅回填（产品决策）；回滚开关单向门文档化；compat 投影 total_attempts 语义漂移声明；node_probe_runs skipped 缺映射；ensure compat 存在性守卫；trigger-all runs 不入 node_probe_runs；breaker exponentialCooling 极值钉桩；buildOutboundProvenance 钉桩；backfill deferred 口径；materialized_view_refresher distlock ctx；scan_scheduler leader 注明；toolUseNameFromID 歧义；chat tool_calls 数组 dedup；geminiMediaBlock 注释；HeatmapView/FreeDiscoveryView i18n 硬编码 ×2

**承继遗留状态**：R34 遗留 #2（本轮 P1-2 闭环）/ #3 #4 #6（本轮 P3 批闭环）；R34 #1 credential_state_log 结案维持（R35 决策）；R34 #5 acc_client SSE 空闲超时、#7 #8 #9 #10 #11 #12 #13 开放；R35-R1（X-Gw-* 信任头，前置拓扑核实）、R35-R2（auto 灰度口径决策）、R35-R3~R10 开放；R35-gap #3（节点地域优先级）、#5 预压缩覆盖、#6 死指标、#7 代理故障归因、#8 其余开放

## 五、测试与验证（真实实测）

```
go build ./...                                                        # clean
go vet ./internal/sessionv2mirror/ ./domains/session/v2/ ./security/sanitize/ \
       ./domains/streaming/ ./bg/ ./admin/ ./licensing/ ./sql/migrations/startup/ ./cmd/gateway/   # clean
go test ./internal/sessionv2mirror/ ./domains/session/v2/ ./security/sanitize/ ./licensing/ \
        ./sql/migrations/startup/ ./cmd/gateway/ -count=1             # 全绿
go test ./bg/ -count=1                                                # ok 7.2s
go test ./admin/ -count=1                                             # ok 66.2s
go test ./domains/streaming/ -count=1                                 # ok 70.0s
go test ./bg/ ./domains/streaming/ ./internal/sessionv2mirror/ -race -count=1   # 全绿
installer 模块 go build ./... + go test ./... 全绿
bash scripts/apply-db-revision-sequence_test.sh                       # passed（717 登记）
具名钉桩（红绿）：
  TestScanBudgetExempt_ClientTimeoutCountsAsFailure   PASS（实证 Client.Timeout 满足 errors.Is(DeadlineExceeded)）
  TestScanBudgetExempt_SweepCtxCanceled               PASS
  TestHandleActivate_FreeLicenseForeignMachineRejectedBeforeSeatTaken PASS
  TestOpenaiFinishReasonIsError_IncludesRefusal       PASS
  TestSurvivalCtxLenCompressRetryDue / TestSurvivalAttemptHasKind / TestSurvivalCompressBodyForRetry PASS
  TestDurableRecoveryWorkerTerminalDecisionFails      红（决策层放行）→ 改判收进 coordinator 后 绿（语义归属修正的实证）
  TestBaselineRequestLogsHotColumnTypesMatchMother    PASS（129 共享列 ×3 baseline 零型差）
  TestBaselineRequestLogsAlignedColumnsPinned         PASS（10 列目标型）
  TestCanonicalStartupMigrationsAtOrAbove704AreRegistered PASS（717 同步后）
gofmt -l（本轮触碰 28 文件）                                           # 清零
```

## 六、下一轮提示词（建议）

> 以 docs/audit/2026-09-17-r36-24h-audit-round.md §四 遗留为起点：首位 **01-schema 的部署前置**（遗留#2：252 真库 information_schema 复核 → 跑 717 → dump-schema.sh 重导三 baseline 替换手工体，动 baseline 前先读 sql/scripts/dump-schema.sh 与 apply-db-revision-sequence.sh 双通道）；其次 credential_recovery 守卫+reviver 门控（#3）与 716 全文等价守卫+down 补体（#4）可同批小修。R35-R1（X-Gw-* 信任头 token 方案）动代码前必须先证实 auto-title/summary 回环拓扑是否恒 127.0.0.1 直连。新增迁移前 git fetch 核对远端编号（当前已至 717）；触碰 installer 前先跑 TestCanonicalStartupMigrationsAtOrAbove704AreRegistered 与 apply-db-revision-sequence_test.sh 双门禁。冗余清理批从 R35-gap §四 22 条低风险删除候选起步（注意本轮已把 durable_contract/action_bridge 两文件标注改写为不可整体删除）。审计入口：docs/audit/playbook/orchestrator-prompt.md（下一轮 R37）。
