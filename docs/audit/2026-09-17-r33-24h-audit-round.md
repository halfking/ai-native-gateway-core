# R33 — 24h 审计轮（2026-09-17）

- 窗口：R30 收口 7756d9670 → origin/main HEAD 643735a28（重算后的 start_sha，禁止复用的 2cead66dd 仅为窗口中段一修复提交）
- 方法：窗口内 10 个功能提交按面拆 5 路子代理并行场景审计（每条发现经主代理亲读复核后才动手），叠加 R32 154 灰度实测新发现 F1 修复与 R30 遗留 7 项逐条处置
- 审计输入：R31 三修（aea284108/ceddf5438/01350f089）、R32 两修（40e198c60）+245 日志治理（2cead66dd）、S3 波1 读端（77ee456ab，实由 2b8eb0023 先落 main）、incident 双修（bba08b922/6f3d03073）、balance-floor 三轮自审（8373a6775）、ops 改密脚本（643735a28）、R32 实测报告（9f5694620）

## 一、结论与根因

**本批修正 4 项（P0×1 / P1×1 / P2×2），全部红绿验证；窗口存量提交整体健康（10 项复核 8 项属实无新缺陷，2 项各带实质发现）。**

1. **F1（P0·阻断放量，已修——以并行线 e8ddff41d 实现为最终落点）**：成功路径 outcome 回填缺失 → 优化器反馈流≈纯失败样本。根因：`emitTelemetry` 成功终态 entry 的 struct 字面量从未应用 auto 字段（初始行/失败行都调，唯独成功行漏），IsAutoRequest 在内存 entry 上恒空，ReportRoutingOutcome 回填门恒 false。154 实测 120 stash 仅 22 失败 matched、98 成功全 TTL expired、orphan=0 与该机理精确吻合。**处置**：本会话与并行会话同日各自独立实现修复（本会话 applyAutoRouteFields 全字段方案；并行会话 e8ddff41d `propagateIsAutoRequestToEntry` 单标记方案，先推入 main 并附复验 2f8407dcb）。为避免双修，本会话主动撤并自身 handler.go 改动，采用上游实现；本会话的行为级钉桩（pgxmock 驱动真 emitTelemetry + autoroute orphan 计数）保留为回归钉，在上游实现上实测绿。持久层本就 COALESCE 保留列值，无需迁移。
2. **R33-P1（已修）routeincident pending 毒化**：715 把部分唯一索引扩到含 `pending`、bba08b922 落 pending 行、6f3d03073 使 pending 写入"成功"，三者叠加后 `lockActive` 仍只查 `active/recovering` → 同路由第二次失败走 DecideState(nil)→insertNew→**23505 重试耗尽**，阈值永不累计、事故永不可见、路由键永久毒化（此后每个失败 5 连重试+warn）。**修**：lockActive 状态过滤补 `'pending'`（一次性一行；715 索引保证每路由恰一行，FOR UPDATE 语义不变）。scratch 库实测：无修复红（23505 精确复现）、有修复绿（pending1→pending2→active3 可见）。
3. **R33-P2a（已修）715 通道契约违规**：sql/migrations/startup/715_*.sql 在三条合法投递路径零登记，仓库自带门禁 `apply-db-revision-sequence_test.sh` 自 6f3d03073 起长红（亲证 exit 1）。**修**：按 701 双通道定式登记（sequence files 数组 + ensure allowlist/必须保留清单），门禁绿。序列管存量库预升级，ensure 管全新安装/启动自愈。
4. **R33-P2b（已修）ops 改密脚本可复现性缺陷**（643735a28，四台已跑过属人工消化，缺陷在重跑/新机）：P1×3——reload 失败 `|| true` 吞掉报假成功（Debian 单元名 ssh 非 sshd）；只改主文件不查 `sshd_config.d` Include first-match-wins（禁令可被 drop-in 静默压过，sshd -t 发现不了）；无密钥预检即禁外网密码（"自检"仅回显当前会话，对纯密码机器=外网锁死）。P2×2——验证函数装饰性（NumberOfPasswordPrompts=0 恒"拒"）、密码生成提示进 shell history。**修**：远端 `sshd -T -C addr=203.0.113.9` 生效配置闸（权威判定，一并覆盖 Include 压制）+ authorized_keys 非空前置闸 + reload 失败即回滚（四路单元名兜底）+ verify_key_login 真实退出码（PIPESTATUS）计入 FAILED_HOSTS 非零退出 + mktemp/umask 077 + openssl rand 生成提示。`bash -n` 过；**未对真实服务器执行**（变更只保护下次运行）。

**核实为健康（子代理场景审计+测试实跑，未动手）**：R31 三修全部属实（distlock key 独立/TTL>超时/无 Redis 退化/EMA 折叠消除/退避曲线锁内递增封顶，钉桩 13 个真钉）；R32 两修属实（端点 admin JWT 链、SQL 全参数化、distlock key 无冲突、租户面无新增暴露）；8373a6775 属实（keyring 触点全仓 3 处全受锁，-race 绿）；S3 波1 off 态与改造前逐字节等价有钉，native 读 113 列同形投影、admin 鉴权/租户过滤两态一致。

## 二、改动（本批 8 文件）

| 文件 | 内容 |
|---|---|
| domains/streaming/handler.go | 本会话 F1 修复已撤并（采用 e8ddff41d 上游实现）；工作区该文件另含并行会话在途功能（Goal 影子指令 parentRequestID 穿线），未提交、未经审计 |
| domains/streaming/success_auto_outcome_test.go | 新增：pgxmock 驱动真 emitTelemetry，orphan 计数钉两道门（auto 成功 +1 / 非 auto +0） |
| domains/routeincident/store.go | lockActive 补 'pending' + Transition/lockActive 契约注释更正 |
| domains/routeincident/store_pending_lifecycle_integration_test.go | 新增：`-tags integration` + ROUTEINCIDENT_IT_DSN 惯例，真库重放毒化场景×2（GUARD_IT_DSN 同款一次性 scratch 库，用毕已 DROP） |
| scripts/apply-db-revision-sequence.sh | files 数组登记 715（含 701 双通道定式注释） |
| scripts/apply-db-revision-sequence_test.sh | ensure_allowlist + 必须保留清单补 715 |
| deploy/ssh-mgmt/update-root-and-lockdown.sh | 生效配置闸/密钥前置闸/reload 硬失败/退出码聚合/mktemp/umask/openssl rand |
| bg/auto_route_workers_test.go | 补文件尾换行（gofmt -l 清零，R31 审计 P3 顺带） |

## 三、测试与验证（真实实测）

```
go build ./...                                                     # clean
go vet ./domains/streaming/ && go test ./domains/streaming/ -count=1   # ok 69.8s（含 F1 两钉）
go test ./domains/routeincident/ -count=1                          # ok 8.5s
ROUTEINCIDENT_IT_DSN=… go test -tags integration ./domains/routeincident/ -run TestITPendingLifecycle -v
  # 无修复：FAIL SQLSTATE 23505（第二失败，毒化精确复现）
  # 有修复：2/2 PASS（pending 累计→active 可见；pending+success→recovered→新行）
bash scripts/apply-db-revision-sequence_test.sh                    # 登记前 exit 1 → 登记后 passed
bash -n deploy/ssh-mgmt/update-root-and-lockdown.sh                # ok（脚本不具本地可测性，未对服务器执行）
F1 红绿（本会话实现）：git stash handler.go → AutoSuccessReportsOutcome FAIL → pop → PASS；撤并后行为钉桩在上游 e8ddff41d 实现上复跑 PASS（ green 证据与实现解耦，断言 autoroute orphan 计数而非实现细节）
```

子代理侧实跑：`./bg`（131s 全量+race 关键面）、`./admin`、`./db`（710 契约）、`./credential`、`./credentialhealth`、`./internal/ir`、`./internal/paramreg`、`./domains/transformation` 均绿。

## 四、风险

1. **本批改动均未部署**：F1 修复需随下个二进制上 154 才能解除放量阻断；routeincident lockActive 修复上线前，避免触发 routeincident observer（或接受 pending 毒化继续累积）。154 10% 灰度重启以 F1 部署为前提（9f5694620 结论）。
2. **ops 脚本修复未经真机演练**：生效配置闸逻辑经静态推演+bash -n，下次对第五台/重跑时先 `bash -n` 外建议先对单台跑一遍观察 FAILED_HOSTS 语义。
3. **双通道 715**：sequence 先于新二进制应用时，旧二进制不写 pending、CHECK/索引向后兼容，安全；ensure 每启动无条件重建约束/索引（AccessExclusive 毫秒级，与 389 trigger 同级，注释已自认）。
4. **工作区卫生（本轮实际发生）**：与并行会话同 workspace 竞态——其 F1 修复（e8ddff41d）先入 main 后，本会话首个本地提交被发现误夹带其在途功能半成品（handler.go parentRequestID 穿线，单独 checkout 不编译），已 reset 撤销并手术式还原 handler.go=上游修复+其在途功能，重做净提交。教训：**共享 workspace 双会话并行时，git add 必须 `git diff --staged` 逐块核对；发现同点位修复先行入 main 时主动撤并而非叠加**。其未跟踪文档（会话优化v4/18）与其余在途文件均未纳入本批。
5. 本机 PG 于 02:47-02:48（+08）发生一次 crash-recovery（自行恢复，与 693 时代模式同）；本地网关容器若出现 DB-less 窗口丢行，按既有幂等回填兜底观察。

## 五、遗留登记（R30 遗留 7 项逐条处置 + 本轮新登记）

**R30 遗留处置**（修/再登记）：#1/#5/#10 已修（R31）；#2 未知路由入口 UI（再登记，产品决策）、#3 FreeDiscoveryView UI 套件+健康字段展示（再登记）、#4 currency pass A 串行头部阻塞（再登记，P3 候选并发池）、#6 Governor MaxQueueWaitMS=0 默认（再登记，行为变更需决策）、#7 归档 codec wire 缺 class/due_at 等四小项（再登记）、#8 installer 709/710 副本同步（再登记；注意 715 已按纪律登记 sequence+ensure，installer StartupFiles 若触碰需五点同步）、#9 licensing 免费分支零测试+无吊销（再登记，产品决策项）。

**本轮新登记**：

- **P3 | emitTuningSignal 成功路径仍死（F1 残留半修）**：e8ddff41d 的 propagateIsAutoRequestToEntry 只透传 IsAutoRequest 单标记，而 emitTuningSignal 门为 `IsAutoRequest && TaskType != nil`——成功终态 entry 的 TaskType 仍恒 nil，v2.1 隐式调优信号在成功路径依旧不发（outcome 回填不受影响：routingOutcomeFromEntry 只需 RequestID/Success/Latency/Cost）。修法即本会话撤并的方案：成功行改用 applyAutoRouteFields（或 helper 补 TaskType 透传）。

- **P2 | credential_state_log 零读者**：bba08b922 之 Bug1 修复杂化写进只写不读的日志表（全仓唯一写者 batch_writer，零读者），重启恢复链 GetState 走内存→Redis→node_probe_state/model_probe_state，仪表盘读 credentials.*_recover_at——"重启后冷却丢失"症状未被缓解。需决策：恢复链补读该表或改写权威表。
- P3 批（随轮登记，不阻断）：① routing-opt 组合过滤恒空（GROUPING SETS 无 (task,provider) 组合行）与"30 天窗口"实际被 LIMIT 2000 截断（建议文档标注/分页）；② 2cead66dd 三处测试缺口（settle COALESCE/callhist skip 分支/backfill 早退均无回归钉）+ ir_converter restoreExtensionsWithParamreg 对 ActionDrop 静默不报 loss（与 internal/ir 版本注释自称一致实则不一致）；③ S3 波1 运维文档弱化 ON 态不可见行（E4 3.71%/天 shadow-write 丢失+E6 claim 漏镜像持续存在）+ native 分支无 42P01 守卫 + 提交信息 ts 索引声明不实 + top-models/session-summary 未随开关切换；④ breaker 跨错误族 coolingCycle 遗留（换族不重置，修复后才可观测，有界自愈）+ SetDistLock 文档"Start 后可调"过度承诺（实为数据竞态）；⑤ keyring 无锁模式相邻缝隙：discovery/discovery.go:69/85 与 bg/credential_probe_v2.go:126/476（下轮候选）；⑥ 集成/单元两态路由入口灰藏页需页内注明（同 R30 #2）。

## 六、下一轮提示词（建议）

> 以 docs/audit/2026-09-17-r33-24h-audit-round.md §五 遗留清单为起点：优先 P2 credential_state_log 零读者决策（恢复链补读 or 改写权威表），其次 ④ breaker 跨族 cycle 重置（一行级）与 ② ir_converter Drop 上报对齐；F1+lockActive 修复随下个二进制上 154 后按 9f5694620 口径重启 10% 灰度并复测 llmgw_autoroute_outcome_total matched/expired 比。新增迁移前 git fetch 核对远端编号（当前已至 715；715=sequence+ensure 双通道）。触碰 installer 前先读 llm-gateway-installer-migration-3way-sync 五点同步纪律。
