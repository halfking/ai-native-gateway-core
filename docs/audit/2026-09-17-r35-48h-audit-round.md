# R35 — 48h 审计轮（2026-09-17）

- 窗口：R33 定稿 b9d9a8ba5 → 2f3151a27（滚动 48h 的增量段；48h 全窗 1cacc62cb^→643735a28 已由 R30-R33 与并行 R34 覆盖，e8ddff41d 经 R33 撤并+复验 2f8407dcb）
- 方法：playbook 首轮实践。窗口内唯一功能提交 = 72d921819（goal 影子轮打标 + tool_calls advisory + 配置缺失启动告警，R33 期间并行会话在途、未经审计）；叠加 R33 遗留清单处置。8 路只读域子代理（D01/D02/D06/D08/D11/D14/D16/D17）并行审计，全部线索经主代理亲读复核后才动手。
- **与并行 R34 轮（063e268e2，同 workspace 同时段）对账**：并行 R34 窗口为完整 48h（1cacc62cb^→643735a28），19 项修复与本轮增量窗口（72d921819 特性审计+R33 遗留）几乎正交；**唯一同点位重叠 = F1 残留修复**（见 §一 R35-P1a 撤并记录）。R34 已修的 UpdateFromProbe 持久化（其 P2-3）与 SetDistLock 契约注释（其 P3）覆盖本轮 D08#3 登记与 keyring 相邻项的一半，遗留清单已相应对齐。本轮编号因 R34 被并行轮先行定稿而顺延为 R35（教训重演：**落档前必须 fetch 重查 R 编号**）。

## 一、结论与处置总表

**本批净修正 7 项（P1×1 / P2×2 / P3×4），全部静态+测试验证；另 1 项 P1 主动撤并采用并行 R34 实现；登记 10 项（P2×2 / P3×8）。核心事件：F1 修复（e8ddff41d，放量门槛构建）被证实带镜像剔除回归——本轮与并行 R34 各自独立发现并修复，本轮撤并采用上游实现后补钉桩；154 放量门槛构建应更新为 ≥并行 R34 的 01bd55ed7 且包含本轮 claim 修复。**

| # | 级别 | 发现 | 根因 | 处置 |
|---|---|---|---|---|
| R35-P1a | P1 | **e8ddff41d 半修：成功终态 entry 只透传 IsAutoRequest 单标记**。(a) emitTuningSignal 门为 `IsAutoRequest && TaskType != nil` 且还读 AutoDecision/AutoConfidence——tuning_signals 生产恒 0 行（全路径死，不止 R33 登记的"成功路径死"）；(b) `isInternalAutoEntry` 兜底分支 `TaskType==""→internal` 把**业务 auto 成功轮**从 session_turns 镜像剔除（request_logs 有行/turns 无行），破坏 GLOBAL_G2 观察期与 hook.go:88-91 既有契约。 | F1 修复用了单标记 helper 而非全字段应用 | **撤并采用并行 R34 实现**（01bd55ed7 扩展 propagateIsAutoRequestToEntry：never-overwrite 补 TaskType/OutboundModel/AutoDecision/AutoConfidence）。本轮原方案（换 applyAutoRouteFields 全字段函数）在并行轮推 main 后主动撤销（R33 §四.4 先例）；上游实现经复核等价覆盖 (a)(b)（业务 auto 轮 TaskType 非空→镜像保留+tuning 门通过）。**本轮补钉桩**：TestPropagateIsAutoRequestToEntry_FullAutoFields（合并后落档，红=旧 helper 无 TaskType/绿=上游扩展版），锁 never-overwrite 语义与业务 auto 不落入 IsInternalAutoEntry |
| R35-P1b | P1 | **F1 后内部回环行 claim is_final_success → GLOBAL_G2 恒>0**。auto-title/auto-summary 回环带 `X-Gw-Is-Auto:true` 且 `X-Gw-Session-Id: gt_/gs_<原会话>`（非空→非 synthetic）→ F1 后成功终态带 IsAutoRequest → 被 isInternalAutoEntry（actor 名单）排除出 session_turns（语义正确）；但 `shouldClaimFinalSuccess` 无内部豁免，gt_/gs_ 行照常置 is_final_success=TRUE → 观察脚本计为"终态缺 turns"，reaper 同门排除、回填无豁免，缺口永久存在。09-16 最后一次 G2=0 观测是 F1 前二进制——**首个 post-F1 每日观察必破窗**。并行 R34 未覆盖此面。 | claim 谓词与镜像排除门分属两包、无共享判定 | **已修（本轮）**：`isInternalAutoEntry` 提升为 `telemetry.IsInternalAutoEntry`（单一事实源，sessionv2mirror 三处调用点换用）；`shouldClaimFinalSuccess` 补 `!IsInternalAutoEntry(entry)`。业务 auto 轮（TaskType 非空，含并行 R34 扩展后）仍正常 claim。钉桩：TestShouldClaimFinalSuccess_ExcludesInternalLoopbacks（gt_/gs_/taskless 不 claim + business auto claim + claim 与镜像排除一致性断言） |
| R35-P2a | P2 | **followUpSourceActor 映射缺口**：(a) audit_hook 实际产出 `Action:"audit_auto_fix"`（生产路径）落入 default 分支得 `goal-followup`——审计族 actor 粒度错误；(b) default 分支"未知 action 一律 goal-followup"使未来非 goal 动作（如响应链 handoff）静默归入 goal 影子轮预算对账（方案18 §8 口径污染）。 | 精确匹配 + 泛化兜底 | **已修**：audit 族改 `HasPrefix("audit")`（与 mode_hook 跳过判定同语义）；default 返回 `""`（不打头，origin_actor 保持 NULL=改动前行为），`if actor != ""` 守卫由恒真变有效。钉桩：TestFollowUpSourceActorMapping 补 audit_auto_fix/handoff/空串用例 + 未映射动作不产生头断言 |
| R35-P2b | P2 | **Path 1.5 advisory 抢占 Path 2 模型轮换**。switchModel 生成与 finish_reason 无关（预算耗尽/重复检测均可在 tool_calls 轮成立）→ advisory 先 return，AtomicModelSwitch 不执行、OutcomeDegraded 不上报；全会话 tool_calls 收尾时轮换通道永久饿死、switch_budget_exhausted 型 giveUp 永不触发（无限 advisory 帧）。 | Path 1.5 插在 Path 2 之前且无让位条件 | **已修**：命中条件补 `decision.switchModel == ""`（轮换优先，恢复 72d921819 之前 Path 2 无条件生效的行为）；顺修 tryBuildAdvisoryToolSignal 注释中"legacy 自调用绝不对 tool_calls 触发"的过宽断言。钉桩：TestInterceptNonStream_ToolCalls_ModelSwitch_PrecedesAdvisory（advisory 让位+AtomicModelSwitch 真执行）+ TestInterceptNonStream_ToolCalls_NoAdvisorySignal_HandoffMode（mode=handoff 抑制，D02/D17 测试缺口一并补） |
| R35-P3a | P3 | resolveSessionTitleTaskID 会把 goal 影子轮（success 且非 auto、无 task 头）选为"最近成功行"→ 标题刷新 scope 静默退化 'auto' | 查询侧未按方案18 §3.3 过滤 goal-% | **已修**：admin/session_title.go 查询补 `AND COALESCE(origin_actor,'') NOT LIKE 'goal-%'` |
| R35-P3b | P3 | 启动告警不覆盖 audit-only 部署（goal.enabled=false + audit_enabled=true 时 audit LLM step 静默降级无告警） | 告警条件只查 goalCfg.Enabled | **已修**：条件改 `(goalCfg.Enabled || goalCfg.UseAudit)`；注释登记已知缺口（运行时租户热开 goal 无 boot 告警、endpoint 有 key 无不告警——后者可能是有意支持免鉴权内部端点） |
| R35-P3c | P3 | injectFollowUpRequest 对 seam 已 rune-safe 截断的结果再做字节级 `[:256]` 截断（可截半个 rune/切掉 "..."） | 72d921819 拆分时外层旧截断未删 | **已修**：外层改用 bodySnippetPrefix（stub 路径也有界） |
| R35-P3d | P3 | goal_control.go:483-486 buildGoalLLMCaller 注释残留挂在 buildSanitizeRestoreInterceptor 头上（2026-07-06 复制粘贴）；keyring SetKeyring 无锁写缺契约钉 | 历史遗留 / R33 遗留⑤ | **已修**：删除四行残留注释；discovery.go/credential_probe_v2.go 两处 SetKeyring 补"必须 Start 前调用"契约注释（并行 R34 已改 SetDistLock 文档为同款契约）；GOAL_CLIENT_SIGNALS.md §5.1 补混合模式行（D02#1） |

## 二、复核裁决（子代理线索 → 主代理亲读结论）

- **D01#2 handoff 误标（候选 P2）→ 不成立（生产）**：goal_control.go:352-356 注释与代码证实 handoff 只经 SetHandoffHook 挂请求侧、刻意不入响应链，生产不会经 handler.go:5185/5211 派发 InjectFollowUp。examples 接线路径存在但非生产。降级并入 R35-P2a 的 default→"" 防御性修复。
- **D06#1 → 成立，定级 P1（R35-P1b）**：亲证 auto_title_generator.go:1041 `X-Gw-Session-Id: gt_<sessionID>`（非空）+ shouldClaimFinalSuccess 仅查 Success/GwSessionID + G2 SQL 无内部豁免。修复取 D06 给的三选一中的 claim 豁免方案（最小、不动观察口径、不动镜像语义）。
- **D11#1/#2 → 成立，定级 P1（R35-P1a）**：亲证单标记 helper（handler.go:6304）+ isInternalAutoEntry 兜底（hook.go:930）+ emitTuningSignal 三字段读取（handler.go:8616-8650）。修法经并行 R34 先行落 main，本轮撤并；上游扩展版经亲读验证覆盖全部三字段+OutboundModel。
- **D14#1/#2 头信任缝隙 → 成立但本轮登记不修**：X-Gw-Source-Actor/X-Gw-Parent-Request-Id/X-Gw-Is-Auto 客户端可注入（handler 入口无剥离/校验）。修复需 per-boot token 或入口剥离层，但 auto-title/auto-summary 回环经真实 http.Client（可能跨实例经 LB 自调），剥离层会切断其关联头——**部署拓扑依赖（回环是否恒 127.0.0.1 直连）未经证实前动代码有生产风险**。登记 R35-R1（P2）附方案。
- **D16#4 llmCallerConfigured 只查 endpoint → 部分采纳**：key/model 不查可能是有意（免鉴权内部端点）；只修 audit-only 覆盖（P3b）。
- **D01#1 audit 影子轮 model="auto" 后门（decider nil 时被镜像排除）→ 边缘成立，随 P1a 缓解**：decider 接线（154 形态）时 TaskType 由 SetAutoDecision 填充→不被兜底排除。登记 R35-R6。
- **D02#4 gemini/responses finish_reason 潜伏漏判 → 登记**（当前两 parser 无生产调用方；域文档回注为接入核对项）。
- **其余 P3 批（D16#3 死头硬编码、D17#4/#5 测试命名、D17#9 步骤编号、D17#10 gofmt 41 文件）→ 登记**，不顺手扩面。

## 三、核实为健康的关键面（8 域子代理+主代理抽读，未动手）

- **影子轮落库链完整可 JOIN**（D01）：buildFollowUpRequest 三头 → 回环不经外部中间件 → 入口 1726-1731 读入 logCtx → 初始/终态/成功 upsert 三点均 applyParentCorrelationFields → request_logs_hot + session_turns 双侧列齐。
- **requestID 传参正确性**（D01）：两调用点均在 serveWithExecutor 内取 `logCtx.RequestID`，与 interceptReq.RequestID 同源，无空串/复用错值路径。
- **advisory 帧契约与文档逐字段一致**（D02）：13 键全对齐、hint 正确缺席、mode 门与正式帧同集、ClientSignalAllowed 同源双闸；不触发 dispatch/不消耗预算（handler else-if 结构+测试三断言）。
- **finish_reason="tool_calls" 在两条活跃上游链路（openai-chat/anthropic）精确产出**（D02）；anthropic tool_use→tool_calls 映射无别名泄漏。
- **advisory 无持久层注入面**（D14）：payload 字段均非客户端直接可控；SSE 帧 allowlist+json.Valid 双校验。
- **goal 影子轮不打 X-Gw-Is-Auto 的 G2 保护成立**（D06）：具体模型影子轮 IsAutoRequest 恒 false，两侧对称落行；lite 模式 goal 整体不启用（db==nil 短路），无存储分叉。
- **影子轮再判定链有界**（D16）：深度 15/每会话 50/预算/轮换四重上限；audit 影子轮无二次审计环；goal_run_actions 无生产写入方、无悬挂 pending。
- **F1 核心（成功终态 outcome 回填）本身健康**（D11）：ReportRoutingOutcome 门在修复后确实发射；DB UPDATE COALESCE 不抹首行列。
- **credential_state_log 恢复链三道覆盖**（D08）：瞬时失败每次 dispatch 即写 node_probe_state + permanent 落 credentials 表 + 外部 Redis 5min TTL；并行 R34 P2-3 再补探测恢复持久化——R33"症状未缓解"的窄缝已闭合。

## 四、遗留登记

**R33 遗留处置状态**：
- ~~P3 emitTuningSignal 成功路径死~~ → 升级为 P1 **已修**（并行 R34 01bd55ed7 扩展实现，本轮撤并采用+补钉桩；表述修正：全路径死+镜像剔除回归）
- ~~P2 credential_state_log 零读者~~ → **结案（决策：保持 ops 快照表）**：D08 评估否决方案 B（与两个在役状态机争单写者权）；方案 A 仅覆盖 deprecated 包窄缝（且并行 R34 P2-3 补探测持久化后窄缝进一步收窄），不值得延长 legacy 路径——保留写路径喂 monitor-245 M2a/M2b，随 v3.0（2026-Q4）移除包一并清退。修正 R33 措辞："零应用读者"（ops 脚本在用）。D08#2（RecoverAt 全链无过期语义）/D08#4（updated_at 可回退）仍为 legacy 债。
- ~~⑤ keyring 无锁缝隙~~ → **结案（注释钉契约）**：D14 证实当前 wiring 全部 Start 前一次性 Set、无活竞态；本轮已在两处 SetKeyring 钉契约注释。热轮换前须改 atomic.Pointer。
- ①②③④⑥ 及 R30 遗留 #2/#3/#4/#6/#7/#8/#9 → 继续开放（本轮无触碰；④ breaker 跨族 cycle 重置已被并行 R34 P1-2 修复）。

**本轮新登记**：
- **R35-R1（P2）X-Gw-* 信任头客户端可注入**（D14#1/#2）：伪造 X-Gw-Source-Actor:goal-% 污染影子轮对账、伪造 auto-title-generator 干扰前端分类、伪造 X-Gw-Is-Auto:true 自我剔除出 session_turns 镜像+触发 orphan 计数。**修复方案（下轮候选，需先证实部署拓扑）**：per-boot 随机 token 方案——internal 新包持有 sync.Once 随机 token，goal 回环（进程内直调）与 auto-title/summary 回环（http.Client）均带 `X-Gw-Loopback-Token`，handler 入口对无有效 token 的请求剥离 X-Gw-Source-Actor/X-Gw-Parent-Request-Id/X-Gw-Is-Auto 三头；**前置核实**：auto-title/summary 自调 URL 是否恒 127.0.0.1 直连本进程（若经 LB 跨实例则 token 不匹配，需共享配置 token 或 LB 层剥离）。改动面：新包 + 3 个回环生产者 + handler 入口 + 钉桩。
- **R35-R2（P2·决策）auto 灰度口径混入网关合成流量**（D11#4）：goal audit 影子轮默认 model="auto"（use_autoroute_for_audit 默认 true，fallback_audit_model 默认 auto）→ 真实 auto 决策进 matched/accuracy/settle/reward 环。goal 当前默认关，但**154 灰度指标口径需在 goal 启用前决策**：验证报告是否按 `origin_actor NOT LIKE 'goal-%'` 剔除合成轮。连带：mode_hook.go:783 `task_type_hint:"code_audit"` 全仓无消费方（要么接线要么删）。
- **R35-R3（P3）goal 影子轮竞争 is_final_success**（D06#2）：父轮失败/未 claim 时影子轮可抢走会话终态标记。低概率。claim 谓词补 origin_actor 过滤即可修，但 goal 影子轮是真续跑轮（镜像保留），是否该 claim 属产品语义，待 goal 放量前决策。
- **R35-R4（P3）work_types/top_models/settle 统计含 goal 影子轮**（D01#4b/D11#5）：goal 启用后三个统计面无 goal-% 过滤；与 R35-R2 一并定口径后一行修（settle LATERAL：`AND COALESCE(r2.origin_actor,'') NOT LIKE 'goal-%'`）。
- **R35-R5（P3）goalrun durable scheduler 死脚手架双轨**（D16#5）：scheduler 无生产接线、goal_run_actions 无写入方，生产续跑仍是 fire-and-forget `go injectFollowUpRequest`（进程崩溃即丢）。要么接线要么删脚手架。
- **R35-R6（P3）decider-nil 部署形态下 audit 影子轮被镜像排除**（D01#1 残余）：非放量形态；若需支持，isInternalAutoEntry 的 TaskType 兜底需排除 goal-% actor（与 R35-P1b 同函数，一行），语义随 goal 放量决策。
- **R35-R7（P3）测试债**（D17#4/#5）：TestDefaultDispatchFollowUpAppliesAuthHeader 命名误导（stub 未走生产 dispatcher）；TestDefaultDispatchFollowUpHitsLiveServer 恒真性质。
- **R35-R8（P3）X-Gw-Follow-Up-Depth 头硬编码 "1"**（D16#3）：全仓零读者、纯元数据漂移；Attempt 恒 1 与 followUpAuthCandidates 死重试机制一并清理。
- **R35-R9（P3）gofmt 存量 41 文件**（D17#10）：窗口内零回归；按包批量单独提交。
- **R35-R10（P3）新协议接入时的 finish_reason=tool_calls IR 不变量**（D02#4）：域文档 D02 §4 已回注。

## 五、改动清单（本批）

| 文件 | 内容 |
|---|---|
| domains/hooks/observability/telemetry/internal_loopback.go | 新增：IsInternalAutoEntry 导出（单一事实源，双门一致性构造保证） |
| domains/hooks/observability/telemetry/client.go | P1b：shouldClaimFinalSuccess 补 !IsInternalAutoEntry |
| domains/hooks/observability/telemetry/final_success_claim_test.go | TestShouldClaimFinalSuccess_ExcludesInternalLoopbacks |
| internal/sessionv2mirror/{hook,replay,synthetic_session}.go | isInternalAutoEntry 三调用点换 telemetry.IsInternalAutoEntry，本地副本删除 |
| domains/streaming/response_interceptor_helpers.go | P2a 映射重写+注释更正；P3c 外层截断改 bodySnippetPrefix；注释补 model=auto 入口说明 |
| domains/streaming/response_interceptor_helpers_test.go | 映射测试补 audit_auto_fix/handoff/空串+未映射动作无头断言 |
| domains/hooks/goal/mode_hook.go | P2b：Path 1.5 补 switchModel=="" 让位+注释两处更正 |
| domains/hooks/goal/goal_test.go | 新增 HandoffMode 抑制 + ModelSwitch_PrecedesAdvisory 两钉桩 |
| domains/streaming/handler_test.go | TestPropagateIsAutoRequestToEntry_FullAutoFields（撤并对账后落档：钉上游扩展 helper 的全字段+never-overwrite 契约） |
| admin/session_title.go | P3a：title task 查询排除 goal-% 影子轮 |
| cmd/gateway/goal_control.go | P3b：告警条件补 UseAudit；P3d：残留注释删除 |
| discovery/discovery.go / bg/credential_probe_v2.go | SetKeyring "Start 前调用" 契约注释 |
| docs/GOAL_CLIENT_SIGNALS.md | §5.1 补混合模式行 |
| docs/audit/playbook/runs/R35-2026-09-17/ | 8 份子代理原文 + README |
| docs/audit/playbook/domains/{D02,D06,D11,D14,D16}.md | §4 历史回归点回注 |
| docs/audit/playbook/CHANGELOG.md | 记一行 |

（撤并未入批：domains/streaming/handler.go 的 applyAutoRouteFields 替换方案——采用并行 R34 01bd55ed7 的扩展 propagateIsAutoRequestToEntry 实现，行为等价，R33 §四.4 撤并先例。）

## 六、测试与验证（真实实测）

```
go build ./...                                                                    # clean
go vet ./domains/streaming/ ./domains/hooks/observability/telemetry/ ./internal/sessionv2mirror/ \
       ./domains/hooks/goal/ ./admin/ ./cmd/gateway/ ./discovery/ ./bg/            # clean
go test ./domains/hooks/goal/ -count=1                                            # ok 1.574s
go test ./domains/hooks/observability/telemetry/ -count=1                         # ok 1.100s
go test ./internal/sessionv2mirror/ -count=1                                      # ok 1.143s
go test ./domains/streaming/ -count=1                                             # ok 70.391s
go test ./admin/ -count=1                                                         # ok 67.944s
go test ./cmd/gateway/ -count=1                                                   # ok 1.391s
go test ./domains/hooks/observability/telemetry/ ./internal/sessionv2mirror/ ./domains/hooks/goal/ -count=1 -race
                                                                                  # 全 ok
具名钉桩（合并 origin/main、采用上游 F1 残留修复后）：
  TestPropagateIsAutoRequestToEntry_FullAutoFields      PASS（上游扩展 helper 全字段+never-overwrite）
  TestEmitTelemetry_AutoSuccessReportsOutcome（R33 行为钉） PASS
  TestFollowUpSourceActorMapping / TestBuildFollowUpRequestCorrelationHeaders PASS
  TestShouldClaimFinalSuccess_ExcludesInternalLoopbacks PASS
  TestInterceptNonStream_ToolCalls_{AdvisorySignal_WhenEnabled,NoAdvisorySignal_WhenDisabled,
    NoAdvisorySignal_WithoutCapability,NoAdvisorySignal_SubAgentsPending,
    NoAdvisorySignal_AfterGiveUp,NoAdvisorySignal_HandoffMode,ModelSwitch_PrecedesAdvisory} 全 PASS
gofmt -l（本批触碰文件）                                                             # 清零
```

红绿说明：P2a/P2b 钉桩断言的是契约而非实现细节（audit_auto_fix→goal-audit 与旧 goal-followup 不符 / advisory 让位分支为新增），修复与测试同批落地；P1a 采用上游实现后补钉桩于合并后树（撤并对账），P1b 钉桩独立红绿（断言内部回环不 claim，与门函数同批）。

## 七、风险与部署提示

1. **154 放量门槛构建必须更新**：P2.2 验证报告 v1.2 门槛 ≥e8ddff41d 带镜像剔除回归（业务 auto 成功轮 session_turns 丢失 + tuning 信号零生产 + gt_/gs_ G2 破窗）。**放量前部署 ≥并行 R34（01bd55ed7）+ 本轮提交**（缺本轮则 gt_/gs_ 行仍破 G2），并以部署后首个每日 storage observation（GLOBAL_G2=0）复核。
2. **claim 豁免的语义变化**：gt_/gs_ 合成分支会话将不再有 is_final_success 标记（此前有）；admin session_online 对这些会话的终态标注消失——分属合成命名空间，可接受；若下游有依赖需回看 admin/session_online.go:418 读数。
3. 本批全部为代码级修复，**未部署**；goal 特性生产默认关闭，P2a/P2b/P3a 影响面在 goal 启用前为零。
4. 混合 workspace 双审计轮竞态（本轮实际发生）：并行 R34 与本轮同窗同 workspace，落档前未再 fetch 导致轮文档重命名返工；**教训：子代理回收后、写轮文档前，必须 fetch 重查 R 编号与同名文件**。

## 八、下一轮提示词（建议）

> 以 docs/audit/2026-09-17-r35-48h-audit-round.md §四 遗留清单为起点（并与并行 R34 的遗留清单合并对账）：优先 R35-R1（X-Gw-* 信任头剥离——先证实 auto-title/summary 回环拓扑是否恒 127.0.0.1 直连，再按 token 方案实施）与 R35-R2（auto 灰度口径是否剔除 goal-% 合成轮——154 复测 llmgw_autoroute_outcome_total 前必须定口径）；其次 R35-R7/R9 测试与 gofmt 批量清账。154 部署 ≥R34+R35 提交后按 9f5694620 口径重启 10% 灰度并复测 matched/expired 比，同时跑每日 storage observation 验证 G2 归零。新增迁移前 git fetch 核对远端编号（当前已至 716）。触碰 installer 前先读五点同步纪律。审计入口：docs/audit/playbook/orchestrator-prompt.md（下一轮 R36）。
