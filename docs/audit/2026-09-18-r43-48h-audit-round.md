# R43 48h 审计轮 —— 726五点同步三犯收口 + taskprofile闭环四断点修复 + IR thinking方言Gemini补漏 + V370双所有者裁决

- 日期：2026-09-18
- 轮次性质：48h 滚动窗口全量审计轮（playbook 全流程：主代理 + 13 只读域子代理两波并行 → 亲读复核 → 修复 → 留档 → 推送）
- 基线：main @ 468a1ce82（开局 ff 推送对齐 origin/main；build/vet 开局全绿；codegraph 3.7.0 全量图谱）
- 窗口：2026-09-16 → 2026-09-18 23:06（643735a28^..HEAD，98 提交）；**重点未经审计增量 = b75c91900..HEAD**（R42 基线之后 17 提交：MiniMax thinking P5 补课/executor 接线、taskprofile CSV+工作台闭环、迁移 726、PG 扩容二轮、审计 SQL 路径、deploy 脚本族）
- 执行方式：13 个只读子代理（第一波 D01+D02/D03/D04/D06/D07/D08/D09+D10/D14，第二波 D05/D11+D13/D12/D15+D16/D17），主代理逐条亲读复核后修复
- 收尾 commit：见 git log（本轮单 commit）

## §一、发现与处置

| 级别 | 发现 | 处置 | 证据 |
|------|------|------|------|
| **P0** | **迁移 726 五点同步只落 2/5**（D06/D07/D14 三子代理独立命中，三犯：720/721 同款）。58384b0d8 只落 canonical+embeddata 文件副本；runner.go StartupFiles、main.go go:embed+embeddedSQLFiles、apply-db-revision-sequence.sh 通道、parity map 四点全缺。**亲跑复现**：`TestStartupFilesAreAllEmbedded`+`TestCanonicalStartupMigrationsAtOrAbove704AreRegistered` 双红。后果链：全新装/存量库标准通道都拿不到 credential_model_index_hot 唯一索引 → auto route rollup 42P10（154 事故原样复发） | 四处登记补齐（runner.go 726 条目、main.go embed var+map 条目、sequence 通道含事故背景注释、parity map 条目）；双测试转绿；本地真库核实 `idx_credential_model_index_hot_unique` 已带外应用且形态与 726 一致（真跑证据，补迁移纪律#1） | `installer/internal/dbinit/runner.go`、`installer/cmd/llm-gw-installer/main.go`、`stats_migrations_test.go`、`scripts/apply-db-revision-sequence.sh` |
| **P1** | **sessionforensics UpsertSummary 连接泄漏**（D14 F2）：`e.store.Query(...)` 迭代器被 `_` 丢弃，pgx Rows 在 Close 前占住池连接不归还——auto 摘要每次落 summary 泄漏 1 连接，池上限 32 迟早耗尽（池饥饿，网关级）。函数头注释"用 Exec 接口"与实现（Query）自相矛盾 | 改为排干+Close+Err 传播；双钉桩（`TestUpsertSummaryClosesQueryIterator` 断言 Close 被调、`TestUpsertSummaryPropagatesIteratorErr` 断言错误不吞） | `domains/sessionforensics/export.go` + `export_upsert_close_test.go` |
| **P1** | **FeedbackRecorder 生产链断裂**（D11#1+D15#1 汇聚）：recorder 挂在 optimizer 侧 store（只读 CorrectionSource，永不写），admin 主写路径 store recorder=nil → Prometheus 分类反馈计数恒零，a8d3a5bbe "给计数器第一个生产者"落空。e2e 测试注释自认"production wiring: admin handlers carry none" | taskprofile.Handlers 增 SetRecorder；admin/handler.go 装配时挂 `autoroute.NewClassificationFeedbackAggregator()`；optimizer 侧死接线移除（注释如实化）；双向源码守卫钉桩（`TestAdminWiring_AttachesFeedbackRecorder` / `TestOptimizerSideStore_StaysReadOnly`） | `taskprofile/handler.go`、`admin/handler.go`、`cmd/gateway/routing_optimizer_init.go`、`taskprofile/wiring_guard_test.go` |
| **P1** | **工作台→taskprofile 词汇表断链 + 静默吞错**（D11#2+D15#2 汇聚）：registry 缺 autoroute V2 枚举 6 类（agent/long_context/vision/function_call/code_audit/intent_classification），其中 4 类恰是标注工作台 L1 种子的半壁 → 提交 400；前端 `.catch(() => undefined)` 静默丢弃，修正空转且不可见 | registry 补 6 类默认档案（tier-b/0.70/[tier-a,tier-c]，镜像 autoroute unknown-type fallback 语义，RegistryVersion 升 v3.1）；pin 测试与 e2e 计数同步 15→21；AnnotationView 失败不再吞——`correctionWarning` 警告条（409 幂等豁免）+ 8 语言 i18n key（parity 纪律） | `taskprofile/registry.go(+test)`、`e2e_loop_integration_test.go`、`web/src/views/AnnotationView.vue`、`web/src/locales/*/annotation.ts` |
| **P1** | **Gemini 入向 thinkingConfig 推理意图静默丢弃**（D01 F-1）：P5 补课（7bb1708d5）对 Gemini 不闭环——budget 形 Reasoning 在 handler step-6 序列化时（路由前 TargetProvider 必空）方言回退 openai_chat 不渲染，executor 重序列化时 intent 已不在 body 中；且 loss 上报无 Reasoning 分支（违背 P5"停止静默丢失"目标） | 本轮补丢失上报：`reportSerializeOpenAILosses` 增 `reasoning.budget_tokens` 分支（budget 形+非 openai 源+未渲染时上报）；双向钉桩（空目标报丢失、minimax 目标渲染不报）。**完整修复（TargetProvider 前送 executor）涉及跨层 IR 携带设计，登记遗留** | `internal/ir/serialize_openai.go`、`serialize_openai_thinking_p5_test.go` |
| **P2** | **task_type_tier_config 双所有者分叉 + apply 双头死**（D11#3+D15#4 汇聚，真库实锤后比报告更深一层）：①repo 内两份 DDL——`sql/migrations/202609_02`（TEXT 型 tenant_id+表级表达式 UNIQUE，PG 语法错误，从未可执行）vs deploy 通道 `V370`（BIGINT+COALESCE(tenant_id,0)，154/245/252/本地实表来源）；②ApplySuggestions 的 `ON CONFLICT (task_type, COALESCE(tenant_id,''))` 在真表上 42P10（索引推断失败）、缺表库上 503 且指向跑不成的迁移——两头死；③真表连 `min_confidence` 列都没有，taskprofile 写面与 TierSelector 读面的共同契约落空；④e2e 测试自建 TEXT 形态 schema，缺陷测不出 | 裁决 V370 为表唯一所有者：202609_02 降级为文档桩；新增 `db.ensureTaskTypeTierConfig`（V370 形态幂等自愈+补 min_confidence 列+CHECK+触发器+十类种子，与 ensureTaskTypeCorrections 同模式）接线启动链；ApplySuggestions ON CONFLICT 改 `COALESCE(tenant_id, 0)`；503 文案指向 ensure；e2e schema 对齐 V370。**真库验证**：本地 llm_gateway 实跑 ensure 全块（种子 no-op、ON CONFLICT 推断成功、ADD COLUMN 生效） | `db/db.go`、`taskprofile/csv.go`、`taskprofile/handler.go`、`e2e_loop_integration_test.go`、`sql/migrations/202609_02_*`（桩化） |
| **P2** | **PostClassify 未套 hookContext 超时**（D04#1）：Options.HookTimeout 文档承诺约束三 hook，唯独 PostClassify 没套；本窗口新增的 CorrectionStats 使 TTL 刷新持 c.mu 跑两条 DB 聚合，DB 慢时全部并发 Decide 在锁上排队无上限 | 补 `o.opts.hookContext(ctx)`（对齐 PreClassify/RecommendModel）；源码钉桩 `TestPostClassify_WrappedInHookContext` | `routingopt/real_optimizer.go`、`postclassify_timeout_pin_test.go` |
| **P2** | **feature_stats 同 worker 日窗口径分叉**（D09#1）：computeDedupRate 已是 UTC 半开窗，computeSingleFeatureDistribution 仍 `DATE(ts)`（会话时区）——两表 stat_date 底层窗口错位至多 8h，且不可 sargable | :141 对齐 UTC 半开窗（与 3a9dca46d 同构改法） | `bg/feature_stats_worker.go` |
| **P2** | **installer embeddata 01-schema.sql 缺 api_key_auto_profile 主键**（D06#2）：3a9dca46d 修了 canonical 两处，embeddata 副本漏同步——R40"中间形态漂移"在 installer 通道重现 | embeddata/01-schema.sql 补 PK 行；钉桩测试 `TestApiKeyAutoProfileSchemaHasIdentity` 纳入第三路径 | `installer/cmd/llm-gw-installer/embeddata/01-schema.sql`、`db/db_api_key_auto_profile_test.go` |
| **P2** | **refresh-balance 失败 bump checked_at 无限顺延 manual 保护窗**（D08#2，R42 遗留#3）：manual 行厂商端点持续故障时，每次 ⟳ 点击把 24h 保护窗续期，自动探测被永久挂起——bg 两路径 R42 已按"错误不动 checked_at"收口，唯独 admin 侧漏 | 失败 UPDATE 去掉 `balance_last_checked_at=NOW()`（与 bg balanceProbeFailStamp 语义统一）；源码钉桩 `TestRefreshBalanceFailureDoesNotBumpCheckedAt` | `admin/provider_credential_balance.go(+test)` |
| **P3 批** | ①R42 bridge bad_request_error 分支无钉桩 → 用例表补 bad_request_error/invalid_request_error 两行；②outbox 毒丸注释漂移（"max_attempts 后 DLQ"实为一步 markDLQ）×2 处修正；③resolve.go 2026-07-14 注释与 H1 lower() 修复相悖 → 诚实化（含函数索引指向）；④translate.go 覆盖范围注描述的"未来工作"45 分钟后已成现实 → 改写为 P5 已接管+衔接关系；⑤ensureApiKeyAutoProfileIdentity 的 RESET 用同一 ctx（取消时漏 RESET 污染池连接）→ 改 `context.WithoutCancel`；⑥CSV 导出公式注入 → csvSafeFormula `'` 前缀中和；⑦导入端点无字节上限 → http.MaxBytesReader 32MB；⑧TestDurableFamilyPrerequisitesRegistered 未钉 520 顺序 → 补 base520 断言；⑨taskprofile handler 头注端点清单 4→7 条回填；⑩registry.go 引用不存在的测试文件名 → 更正；⑪两新测试文件 gofmt 债 → 清零；⑫724 头注残留"721_"文件名 → 更正+embeddata 锁步+ensure 补 COMMENT ON COLUMN agrees；⑬main.go STORAGE_MAX 注释失实（该 env 两模式皆死）→ 如实化+脚本噪音登记；⑭initTaskProfile "Independent of ROUTING_OPT_ENABLED" 过誉 → 注明 cred-recovery 门控嵌套事实；⑮settings CachedPlatformBool 族零消费方 → RESERVED 注解；⑯GetLatestStats "用于测试和监控"半句失实 → test-only 如实化；⑰**vue-tsc 门禁红（web）**：a8d3a5bbe 交付漏跑 vue-tsc，taskProfile.ts:79 响应泛型强转 TS2352 在 main 存在至今 → 按后端 `{success,correction}` 信封修正泛型（EXIT 0），vitest 925 绿不覆盖类型门的姊妹教训与 R42 i18n 同型 | 全部落地 | 逐文件见 diff |
| **P3 登记（只登记不修）** | affinity 合成轮过滤 hot-only JOIN 盲窗（D04#2+D09#2 汇聚）；claim is_final_success 漏镜像（D07#4，9769a18c4 已立案 E6）；lookupTurnNumber hot-miss/30d（D07#5+D03#1）；R36 压缩修剪 len/4 过激+CJK 低估（D05#1/#2）；⟳ 无能力门控/FetchBalanceUSD 丢状态码/balance 文档漂移（D08#3/#4/#5，R42 遗留继承）；auto_index_refresher DELETE+INSERT 非原子（D08#6）；apply-tier-config 零审计留痕（D15#5）；ApplySuggestions 复活 enabled=FALSE 行（D11#4）；CredsTab 手抄 refreshBalance 漂移/taskProfile.ts 零消费/DeleteOlderThan 无调用方/write-only 统计表（D15#6/#7/#8、D09#3）；.db-audit 双拷贝 SSOT/deploy timeout GNU 依赖/sync-admin 魔法数三写（D17#1/#11/#12）；overlay 多副本一致性（D11#7）；Gemini thinking 方言不对称×2（D01 F-3/F-4） | 见 §四遗留登记 | — |

**复核后丢弃/降级的子代理线索**：D12#1（订阅 Priority 不参与选择）——亲读确认属"由调用方传 subscriptionID 承担"的既有分工，以域文档销账注记收口而非缺陷；D06#1 的"CI 必挂则 P0"推测——本仓无强制 CI 跑 installer 模块，但全新装断链后果链成立，维持 P0；D04#2 与 D09#2 的保留期数字（8h vs 7d）——亲读澄清为两套机制（341 p_retention=7d 管 request_logs 族 promote，partition_manager 8h 常量是 *_default 兜底），盲窗结论不变（7~14d 段）。

## §二、核实为健康的关键面（本轮留档，下轮不再重复怀疑）

- **R42 修复本体全部经第三双眼复核成立**：URSM v2 三路失败写门控对称（D09 逐点核对 6 调用点成对）；721 TOCTOU 谓词五处消费计数锁死（D08 grep+测试对账）；manual 戳 IS DISTINCT FROM 条件化；⟳ 静默失败修复；bad_request_error 流内分支方向正确；installer 516/520 R42 修复的 StartupFiles 顺序与幂等（D06 diff 实测）。
- **P5/TargetProvider 出向接线覆盖面**（D01）：chat 出向 4 分支+anthropic 出向全部 stamp；wiring 测试用真实 ir 序列化非 mock；流式/非流式同源（同一 SerializeOpenAI）；重试路径每候选重跑 finalize 方言正确。handler_gemini 是唯一豁免点（本轮 P1 补丢失上报）。
- **outbox dispatcher 并发骨架**（D04/D14）：SKIP LOCKED 锁覆盖投递窗、退避钳位防溢出、毒丸直接 DLQ、at-least-once+ASM 幂等、gauge 节流正确。
- **session/v2 三层 provenance 链**（D03）：session_writer_v2 的 GUC 追加不触序列化内容；outbox 载荷结构性不含 AlignmentMap（reaper 清理不可能伤 provenance）；provider-window source telemetry 写/镜像/读三层对齐；compression/secretmask 48h 零改动。
- **725/durable RLS 面**（D14）：725 幂等+.down 完整+与 720 词汇兼容；durable/rls.go GUC 事务级配对规范；RLS Phase 2 适配批（modelpolicy/partition_manager/reaper/candidate_failure_logger）全部 BeginTx→set_config(true)→Commit 无裸 SET。
- **代理主链路零改动**（D12）：HK 排除/地域 ban/订阅刷新保留节点 ban 全在位；TargetProvider 接线不触出站 transport。
- **D13 免费池窗口零改动**（D11）：freediscovery/freeresource/discovery 无回归面。
- **压缩主链路完整**（D05）：one-shot guard+修剪不减即终端无死循环；保连接+流内 think 提示链路接通；413 三协议信封一致。
- **center runtime_alerts→runtime_alert_events 切换**（D09）：8 处 SQL 全部指向真实存在表——窗口内隐性 P1 级救火（原查询查不存在的表必 42P01），修正正确且无双计面。
- **i18n parity 8 语言全绿**（D15）：315 leaf key × 8 locale 程序化比对零缺失（本轮新增 correctionWriteFailed 后复验仍需全绿，见 §五）。

## §三、本轮事故与教训

**202609_02 vs V370 双所有者陷阱（本轮最重要教训）。** 子代理报告按字面采信"202609_02 是表的 DDL"几乎导致按 TEXT 形态写修复迁移；真库 `\d` 一查才发现真实表出自 deploy 通道 V370（BIGINT+0 哨兵），且列集都不同（无 min_confidence）。教训入册：①同一表在 repo 出现两份 DDL 时，**先查真库 information_schema 定所有者，再动手**——"哪份 DDL 是真的"永远以实表为准（迁移三纪律 #3 的变体）；②deploy/sql/migrations（V 系列）与 sql/migrations（startup/dated）是两条独立交付通道，审计改动面时必须把 deploy/ 目录纳入 grep 范围；③"e2e 测试自建 schema"与生产形态不同形时，测试测的是想象中的表（本例 e2e 自建 TEXT 形态恰好掩盖了 ON CONFLICT 42P10）。

**五点同步三犯的根因补充**：726 作者只跑了 `go test ./sql/migrations/startup/`（含 migration_726_test.go C1-C4 契约），该包不覆盖 installer 通道与 sequence 通道——**门禁的覆盖面声明比门禁本身更重要**，本轮在轮文档把"迁移五点"扩为"六点"候选（+deploy V 系列通道 grep），待下轮评估是否落 checker。

## §四、测试与验证（实跑输出）

```
开局 build/vet                    # OK（后台门禁 exec_c273975c exit 0）
installer 双契约（修前）          # TestStartupFilesAreAllEmbedded +
                                  # TestCanonicalStartupMigrationsAtOrAbove704AreRegistered 双红实锤
installer 双包（修后）            # ok 5.7s / ok 0.9s（含 726 parity、520 顺序新钉）
go test ./sql/migrations/startup/ # ok（TestMigration726 C1-C4）
真库验证（llm_gateway@127.0.0.1:5432，PG 17.10）：
  idx_credential_model_index_hot_unique   # 已在，形态 (bucket,credential_id,raw_model) 与 726 一致
  ensureTaskTypeTierConfig SQL 全块实跑    # 幂等通过；种子 INSERT 0 0（no-op）；
                                           # ON CONFLICT (task_type,COALESCE(tenant_id,0)) DO UPDATE 推断成功
                                           # ADD COLUMN min_confidence 生效
domains/sessionforensics          # ok（含新增双钉桩）
taskprofile                       # ok（wiring 守卫 ×2 + registry pin 21 类）
routingopt                        # ok（PostClassify hookContext 钉桩）
db                                # ok（identity ensure + schema identity 三路径）
admin                             # ok（refresh-balance 失败不 bump 钉桩）
internal/ir                       # ok（P5 双向钉桩 + 全包）
internal/paramreg/internal/outbox # ok / ok 171.6s
domains/streaming                 # ok（bridge 用例表新钉两行）
bg                                # ok
resolve/settings/cmd/gateway      # ok
最终门禁                          # build/vet/test 三门全绿（触碰包全覆盖：ir/paramreg/
                                  # sessionforensics/taskprofile/routingopt/settings/resolve/
                                  # db/admin 66s/streaming 72s/bg/outbox/cmd/gateway + installer 模块）
web 门禁                          # vitest run 128 files / 925 tests 全绿（parity 8 语言含新增
                                  # correctionWriteFailed）；vue-tsc 首跑红（taskProfile.ts TS2352，
                                  # 窗口内 a8d3a5bbe 引入、非本轮改动）→ 修正后 EXIT 0
```

## §五、遗留登记

1. **P1 | Gemini thinking intent 的完整修复**（D01 F-1 残余）：丢失上报已补，但 intent 仍在 gemini step-6 被丢——完整修复需把 TargetProvider 前送（handler 经 context 携带 IR 到 executor，或 step-6 延迟序列化），涉及跨层数据流设计，立项专项；先决：MiniMax 对 adaptive+budget 的真机接受度实测（D01 F-4 同项）。
2. **P2 | affinity 合成轮过滤盲窗**（D04#2/D09#2）：JOIN 仅 hot，7~14d 段纵深防御失效（主防线 reward IS NOT NULL 有效）。修复三选一：JOIN 改 hot∪父表 UNION（扫 14d 分区，性能权衡）、settle 写入时冗余 origin_actor 进 selections（需 727 迁移）、auto_route_selections_all 视图加投影。附 341 p_retention 与窗口覆盖关系注释钉扎。
3. **P2 | apply-tier-config 审计留痕**（D15#5）：apply/import/reload/corrections 四变更端点无 audit hook——需在 taskprofile.Handlers 加可选回调避免反向依赖，apply 优先。
4. **P2 | claim is_final_success 漏镜像**（D07#4，继承 9769a18c4 E6 待办）：S4 停写评估前必须修复 claim 路径或正式登记 E6；09-19 每日轮照跑。
5. **P3 批**：①R35 遗留两条转客户端/运维域（7MB 阈值/告警重分类）+413 响应体可选级；②R36 压缩修剪 len/4+CJK 口径（行为债，修复需 E2E）；③⟳ 能力门控/FetchBalanceUSD 状态码/CredsTab 同款 UI 三合一（余额面 UI 轮）；④auto_index_refresher DELETE+INSERT 包事务；⑤taskprofile 变更端点响应标注 ApplySuggestions 复活 enabled=FALSE 行为；⑥CredsTab/ProvidersView refreshBalance 抽 composable；⑦taskProfile.ts 管理页（4 导出零消费）；⑧attachments DeleteOlderThan 接线或标注无调用方；⑨write-only 统计三表挂只读端点或登记；⑩.db-audit 双拷贝归位 SSOT；⑪deploy-local-lib timeout GNU 依赖降级分支；⑫sync-admin RETRY_BUDGET 常量化；⑬TASKPROFILE_OVERLAY 多副本 reload 进 runbook；⑭Gemini 方言不对称×2（最小对象统一，待真机证实）；⑮lookupTurnNumber hot-miss 与 turn_no 消费方清点；⑯STORAGE_MAX 死配置从 deploy-252-gateway.sh/start-full.sh 移除。
6. **P2 | 设计文档回填**（D11 未单列项）：TASKPROFILE_MODULE_DESIGN.md §三"4 个 admin 端点"实为 7 个、§五.2 TierSelector 消费仍未接线（本轮注释已如实化）——§七交付清单补注。
7. **R42 §五/R40 §五 顺延**：RLS Phase 2 前置（73 表 FORCE 清单+pending/pg_source.go 旁路+PG<15 下推钉桩）、settle session 分量合成轮污染、census 守卫扩面、720/722/723 db-changelog 随 deploy 落、719 复活扫描待 deploy-local、252 运维窗口——均未变，继承。

## §六、下一轮提示词（建议）

> 以本文 §五 + R42 §五 + R40 §五 为起点，审计入口 docs/audit/playbook/orchestrator-prompt.md（下一轮 R44）。优先级：
> 1) **Gemini thinking intent 完整修复专项**（§五#1）：TargetProvider 前送方案设计（IR context 携带 vs 延迟序列化）+ MiniMax adaptive+budget 真机实测 + gemini 入向 E2E；
> 2) **affinity 盲窗三选一裁决**（§五#2）：性能权衡后落地 + 真库"promote 后旧行"集成用例；
> 3) **taskprofile 审计 hook**（§五#3，apply 优先）+ 设计文档 §七 回填；
> 4) **E6 裁决**（§五#4）：修复 claim mirror 或登记例外，勿再顺延。
> 纪律：①表 DDL 分叉先查真库定所有者（§三教训）；②迁移门禁覆盖面声明（installer/sequence/deploy V 系列通道）在交付清单里逐点打勾；③新增 i18n key 8 语言全落 + vitest run（vue-tsc 不覆盖 parity）。
