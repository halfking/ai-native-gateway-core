# 252 PG SQL 日志审计轮（2026-09-22）

> 轮次性质：去编号专项轮（252 PG SQL 日志审计系列第四轮，接 09-20 审计轮 / 09-21 复核轮 / 09-21 部署验证轮）。
> 审计目标：对 252 docker 内 pg-252-pg17 实例 SQL 日志全量窗口做慢 SQL（>1s）与错误 SQL 审计，逐类根因分析并修正。
> 上游输入：docs/audit/2026-09-21-252-deploy-verification.md §八A2（pss 增量顺延）+ §九（下一轮建议）。

## §〇、取证与起点

1. **起点重算**：本轮开工时工作区 HEAD = f1efc5b45（waterfall merge）；审计完成后 rebase 到 origin/main = 89a4a9f88（R51/R52 并行轨道已入 112 文件），修复提交 6827fce80 位于其上。
2. **取证快照（先快照后分析，日志易失纪律）**：pg-252-pg17 容器 ctr.log（logging_collector=off，LogConfig max-size=100MB 轮转即丢）在 06:04 快照 10.2MB → 本地解析。**窗口仅 21 分钟（05:43:27–06:04:05）**：写入速率 ~8KB/s，是 09-21 复核轮基线 590B/s 的 **13 倍**——高错误率自身在压缩取证窗口（100MB 轮转 ≈ 3.5h），本轮再证"取证先快照"。
3. **窗口内总量**：慢查询（>1s）479 条 + ERROR 594 + FATAL 121。错误密度 ≈ 28 条/分钟，远超正常基线 → 逐类归因见 §一。

## §一、错误 SQL 分类与根因（全部归因到发送方二进制）

| # | 错误（21min 窗口量） | 根因 | 发送方 | 处置 |
|---|---|---|---|---|
| F1 | `syntax error at or near "//"` × 330 | models_canonical upsert SQL 内嵌 **Go 风格 `//` 注释**（29b4ea64b 09-21 14:28 引入，862bb8087 09-21 23:49 已修——**main 含修复、生产二进制不含**） | 154 生产 ddb6a09b-2164 + 245 ddb6a09b-2167（两处均建于 29b4ea64b 之后、862bb8087 之前） | **随本轮部署收敛**（见 §三）；discovery canonical upsert 自 09-21 15:30 起 100% 失败 ~15h（模型同步停摆 + 每分钟 ~950 条错误日志） |
| F2 | `syntax error at or near "current_role"` × 21（`SET LOCAL app.current_role = 'super_admin'`） | **252 本机 llmgo-252-dev.service 过期二进制**（09-21 09:14 构建，缺 R46 set_config 根修） | 252 主机 /opt/llm-gateway-go/bin/gateway（pg_stat_activity 红手抓捕 + systemd unit 确认） | **已重新部署**（§三 step1）；旧 SQL 30s 采样 0 命中 |
| F3 | `inconsistent types deduced for parameter $4` × 4（scheduled_tasks UPDATE） | R46 已登记的 **pocket 库跨库问题**（opencode-pocket 所有），不越界修 | 10.88.0.1（252 容器） | 维持登记，无新增事实 |
| F4 | `syntax error at or near "("` × 1 | R50 双臂排除谓词 `probeTrafficExclusionPredicate` 拼接时**缺 `AND`**（`rl.success = TRUE` 后直接跟 `(NOT COALESCE...)`）；同型谓词在 model_probe.go 两处均带 AND，仅 credential_selfcheck.go:487 遗漏 | 任意含 R50 提交的二进制（main 亦有） | **本轮代码修复**（6827fce80），本机真库语义执行验证 |

**发送方归因方法**（本轮沉淀）：podman ctr.log 无 %d/%u 前缀 → ① 语句形状归一化分组；② `pg_stat_activity` 2s 间隔红手抓捕 `client_addr`（45 样本）；③ 172.16.2.210 = **252 主机自身 eth0 IP**（曾误判为未知主机，ss -tnp + ip addr 双证）——**跨机归因前先核对本机 IP 清单**；④ 二进制版本靠 ancestry（`git merge-base --is-ancestor`）而非 tag。

## §二、慢 SQL（>1s）与 30s 击杀链

### §二.一 被角色级 statement_timeout=30s 击杀的语句（17 条/21min ≈ 1.2k/h 浪费）

| 语句族（窗口量） | 归属 | 判定 |
|---|---|---|
| `REFRESH MATERIALIZED VIEW CONCURRENTLY routing_analytics_7d` × 4 | **llmgo-252-dev 过期二进制**（无 F2 钉连接修复；154 生产同语句 28.6s 完成于钉连接 = 上轮 F2 修复**生产生效实证**） | 部署收敛（§三） |
| `CREATE TEMP TABLE provider_error_agg_src ... FROM candidate_failure_logs_unified` × 2 | 同上（语句 FROM 子句实锤旧 SQL——252-dev 不含 18e1e1188 hot-only 根修） | 部署收敛（§三） |
| `promote_request_logs_hot_to_partition('08:00:00','5000')` × 1（usage_ledger 同族 26.5s 贴线完成） | **main 代码缺陷**：Go 侧批次预算 60s vs PG 侧角色 30s 击杀，整批回滚、下 tick 重试同一批 = 活锁形态 | **本轮代码修复**（6827fce80）：promote 事务内 `SET LOCAL statement_timeout='60s'`，与 Go 预算对齐，事务结束自动还原 |
| session_summaries backfill chunk × 2、integrity_fingerprint × 1、session digest tokens × 2、usage_facts+dedup × 2、session_key stats × 1 | 启动/周期分析查询族，30s 临界 | 登记 §五（可续跑设计在位，击杀是浪费非数据缺口；但烧启动预算 → §四 D1） |

### §二.二 慢查询 Top（完成但慢，≥5s 峰值）

| 语句 | 峰值 | 判定 |
|---|---|---|
| REFRESH routing_audit_summary_7d | 28.6s **完成** | >30s 能完成 = 跑在上轮 F2 钉连接（180s）上，修复生效实证；watch item |
| promote_usage_ledger / request_wal | 26.5s / 12.6s | 已由 SET LOCAL 修复覆盖 |
| selfcheck 探活候选查询（INV-4/5 形态） | 20.0s（且 ×2 被击杀） | EXISTS 双子查询成本，30s 临界；登记 |
| DELETE credential_model_index_hot（refresh Half1） | 16.4s | 每行 2 次关联 credential_model_peak_1m 子查询；登记 |
| used/unhealthy/ranked（probe policy） | 15.5s | 登记 |
| 自路由候选构建（c+mo+p 大 COALESCE 列） | 15.1s，29 次/21min | 路由热路径、med ~1.6s；专项优化另立，登记 |
| provider_error_aggregator pass two（date_bin） | 10.4s | pass one 已 hot-only（09-21 轮），pass two 整桶精确性设计如此；登记 |

### §二.三 cancel 风暴

`canceling statement due to user request` × 205（≈10/min）：受害语句为请求路径查询（session turn MAX、routing policy、advisory lock、request_logs_hot UPDATE）+ 客户端断连弃查（pgx ctx cancel），与 R46 定性一致（应用侧行为非 PG 故障）。维持观察，不修。

## §三、部署（本轮"修正"的落地面）

按 runbook 契约顺序 **252-dev → 245 → 154**（先 dev/pre-prod 后生产）：

1. **252-dev（llmgo-252-dev.service）**：deploy-252-gateway.sh 实改 06:45。重启后启动迁移链受 30s 击杀烧预算（attempt 2 过）；旧 SQL（F2/F3 族 + 无钉 REFRESH + 旧 agg_src）**30s 采样 0 命中**。healthz `ready:false` 为该实例既有形态（full PG + 不配 Redis → redis pinger nil → ready 恒 false，非本轮回归）。
2. **245 pre-prod（llmgo-245-canary 蓝绿）**：首轮 `--seq 2168` 探针失败、`PROBE_TIMEOUT_SECS=600` 重跑**同样失败**——候选启动链被 session_summaries 回填 chunk 确定性烧穿预算（boot1=60s/75s、boot2=35s+30s，两次 attempt 各烧一个 30s 击杀，全部对账到该 chunk）→ `postgres disabled` DB-less → readyz 503；蓝绿安全网正确拦下，旧实例持续服务。**根修 beda1cd89**（回填段 5s 快失败，§六#3）后 `--seq 2169` 重跑：**145s 总耗时 / 68s 切换收敛，version=2170-beda1cd8，ready:true**（active 槽 8782）。
3. **154 生产（llm-gateway-go-canary 蓝绿）**：`--seq 2171` 一次通过：**243s 总耗时 / 切换 158s，version=2171-beda1cd8，ready:true**（active 槽 8781）。
4. **切换后日志验证（07:19–07:26 窗口）**：`current_role` 自 06:45（252-dev 重启）起 **0 条**；`//` 语法错误 **0 条**且三台网关现均含 862bb8087 修复（结构性不可能复发）；pocket $4（F3）维持 ~5 条/30min（跨库登记项）。注：`//` 在 06:48（154/245 尚未部署时）即停——discovery 同步触发条件未在本轮查透（A7），但不影响结论：现役全部二进制均含修复。

## §四、登记（本轮不修）

- **D1（P1→已修 + 残余）启动 DB setup 链 30s 烧预算**：确定性烧点（session_summaries 回填 chunk 30s 击杀×每 attempt）已由 beda1cd89 根修（5s 快失败）；**残余**：ensure 链内 DDL 偶发锁等待仍可烧 30s（概率型，boot1 的 26s 烧点），`ApplyMigrations maxAttempts=2` + boot 预算可吸收单次，连续多次仍可致 DB-less——后续可做 per-step 短 deadline 或 setup 期 pinned timeout（禁用 RuntimeParams 全池方案——会泄漏到 serving pool）。
- **D2（P2）慢查询 Top 登记族**（§二.二表）：selfcheck 20s / index-refresh 16.4s / 候选构建 15s / pass two 10.4s——均 30s 内完成或临界，专项优化另立轮。
- **D3（P2）245 上 collector-service / quality-service 为 2026-07-19 老二进制**：cmd/collector-service 已不在本仓；quality-service 在仓但跨两月盲升级风险大。日志窗口内无错误输出，暂不动，登记。
- **D4（P3）252-dev healthz ready 恒 false**（full PG + 无 Redis 形态）+ deploy-252-gateway.sh 就绪验证与该形态矛盾（首验 curl 时序也偏早），下一轮顺手修。
- **D5（P3）252-dev 二进制版本串滞后**（工作区 VERSION 被 rebase 带回远端值，/healthz 显示 03b43979-2167 而代码为 6827fce80）：dev 实例身份以构建日志为准；prod 部署走 seamless 自动 bump 不受影响。

## §五、pss 增量复核（上轮顺延项收口）

- **729 抽屉索引**：`session_turns_2026_09_credential_ts` idx_scan 18,439（09-21 13:00）→ **191,854**（09-22 06:40）——+173k/17h ≈ 170 次/分钟持续命中，抽屉查询修复的量级与持续性双确认。
- **provider_error_agg_src**（上轮 5.67s/2706 次/17 击杀）：pass one hot-only 已随生产部署生效；窗口内该语句的击杀全部来自 252-dev 旧二进制（FROM unified 实锤），252-dev 重部署后此族绝迹——下轮 pss 增量应见均值显著回落。
- REFRESH timeout：summary_7d 28.6s 完成（钉连接 180s 生效）；analytics_7d 击杀全部归 252-dev 旧码，重部署后归零。

## §六、改动清单

1. `bg/credential_selfcheck.go`：selfcheckRecentFallback 谓词前补 `AND`（F4）。
2. `bg/partition_manager.go`：promote 批次事务内 `SET LOCAL statement_timeout = '60s'`（30s rolconfig 与 Go 60s 批次预算对齐，防活锁）。
3. `db/db.go`：session_summaries 回填段钉连接 `SET statement_timeout = '5s'` 快失败（确定性部署 blocker 根修；索引构建段 10min 不变）。
4. **252 生产面实改**：llmgo-252-dev 重新部署（06:45）；245 蓝绿 **2170-beda1cd8**（active 8782，ready:true）；154 生产蓝绿 **2171-beda1cd8**（active 8781，ready:true）。
5. 提交：6827fce80 + beda1cd89（rebase 于 origin/main 89a4a9f88 之上，均已推送）。

## §七、测试

```
go build ./... / go vet ./bg/ ./admin/          # 全绿
go test ./bg/ -count=1                          # 7.5s 全绿（rebase 后复跑 7.6s；beda1cd89 后 7.3s）
go test ./admin/ -count=1                       # 66.5s / 66.3s 全绿
go test -race ./bg/ -count=1                    # 9.2s 全绿
go test ./db/ -count=1                          # beda1cd89 后全绿
本机真库：F4 修复后 SQL 形状（AND 版谓词）psql 语义执行 PASS（0 行=空集正确语义，无语法错误）
252 真库：252-dev 重部署后旧 SQL 30s 采样 0 命中；729 idx_scan 191,854 增量确认；
          切换后日志窗口（07:19-07:26）current_role=0、//=0
245 蓝绿：2170-beda1cd8 ready:true（145s 总/68s 切换，含 2168/2169 两次失败迭代的教学证据）
154 蓝绿：2171-beda1cd8 ready:true（243s 总/158s 切换，一次通过 = beda1cd89 启动修复的生产实证）
```

明确未覆盖：① D1 启动链加固未实现（本轮登记）；② 慢查询 Top 族优化未动（登记）；③ 245 collector/quality-service 未升级（登记）；④ cancel 风暴属应用侧行为，无本轮动作。

## §八、自审计（同日批判式复核）

| # | 初版问题 | 复核处置 |
|---|---|---|
| A1 | **曾把 172.16.2.210 当"未知外部主机"** 差点登记成"无法归因的过期网关" | `ip addr` 核对后确认 = 252 主机自身 eth0；教训：跨机归因第一步先列本机 IP 清单（含 overlay/podman 网桥 10.89.13.0/24 = 252 容器侧） |
| A2 | **245 首轮部署失败后曾疑"我的代码改动引发"** | 逐层证据排除：失败点在启动迁移链 30s 烧预算（与 6827fce80 改动无交集；252-dev 同症状先于本轮改动存在）；蓝绿安全网拦截属正确行为 |
| A3 | **日志速率 13 倍暴涨初版未解释** | 归因闭环：F1 每分钟 ~950 条 4KB 语法错误 + 30s 击杀族 = 8KB/s；也解释窗口缩至 21 分钟——先快照后分析的纪律再次生效 |
| A4 | **"REFRESH 修复失效"误判风险**（analytics_7d 仍在被杀） | 红手抓捕证明击杀全部来自 252-dev 旧码（无钉连接）；154 生产同语句 28.6s 完成于钉连接 = 上轮修复生效的正反双重证据 |
| A5 | **245 重跑依赖"frontier 可续跑"推理未实证** | 600s 重跑**同样失败**——推理被证伪：被杀 chunk 水位不前进、每 boot 烧同样 30s = 稳定活锁非暂时收敛；升级为根修（beda1cd89）后一次通过。"重跑可能自愈"类判断必须先验证烧点是否可推进 |
| A6 | version.json/VERSION 工作区脏文件曾与 rebase 冲突 | stash→rebase→drop（远端已含等价变更 2167，本地 2161 为过时残留）；D5 登记版本串滞后事实 |
| A7 | **`//` 错误在 06:48（154/245 部署前）即自行停止**，与"16/min 持续失败"的模型不符 | 未查透（discovery 同步的触发/退避机制），报告中如实标注；结论不受影响：现役全部二进制均含修复，`//` 结构性不可能复发。留 A7' 下轮可查：discovery tick 的失败退避策略 |

## §九、下一轮提示词（建议）

> 以本文 §四 D1 残余-D5 + §二.二慢查询表为起点，审计入口 docs/audit/playbook/orchestrator-prompt.md。优先级：
> 1) pss 增量复核：provider_error_agg_src 均值回落 + analytics_7d 击杀归零 + promote 击杀归零确认（本轮部署收敛的滞后证据）；A7'：discovery tick 失败退避机制查透（`//` 为何 06:48 自停）；
> 2) D1 残余：ensure 链 DDL 偶发锁等待烧点（per-step 短 deadline 或 setup 期 pinned timeout；禁 RuntimeParams 全池方案）；
> 3) §二.二慢查询专项（建议先 selfcheck 20s 与 index-refresh 16.4s 两个 30s 临界族）；
> 4) D3/D4/D5 小项清偿。
> 纪律沿用：⑪ PG 修复完成态=真库经驱动执行；⑫ CONCURRENTLY 必须 superuser + indisvalid 复查；⑬ 空集不落行≠故障；⑭ 跨机归因先核本机 IP（A1）；⑮ 部署失败先查启动链 30s 烧预算（A2/D1）；⑯ "重跑自愈"判断先验证烧点可推进（A5）。

## §十、handoff 更新

- 记忆库：llm-gateway-go-audit-cycle-progress 追加本轮；pg-252-sql-log-audit-facts 增补（252-dev 过期二进制三症状、172.16.2.210=252 eth0、promote SET LOCAL、729 idx_scan 增量、boot 链 30s 烧预算）。
- 原始物证：252:/tmp/pg252-snapshot-060409.log（10.2MB）；本地 /tmp/pg252-audit-0922/（parse.py + analysis.txt）。
