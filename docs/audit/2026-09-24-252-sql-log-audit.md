# 252 PG SQL 日志审计轮（2026-09-24）

> 轮次性质：去编号专项轮（252 PG SQL 日志审计系列第六轮，接 09-20 / 09-21×2 / 09-22 / 09-23）。
> 审计目标：pg-252-pg17 全量 SQL 日志窗口慢 SQL（>1s）与错误 SQL 审计、逐类根因分析并修正。
> 上游输入：docs/audit/2026-09-23-252-sql-log-audit.md §八（下一轮建议四项）。

## §〇、取证与起点

1. **起点重算**：开工时 HEAD = ef87317c5（main，与 origin/main 同步），工作区仅有 mock-probe 设计稿未跟踪文件（他任务产物，未动）。
2. **取证快照（先快照后分析纪律）**：06:03 快照 pg-252-pg17 容器日志 **15.6MB / 286,990 行**，窗口 **05:17:11–06:02:17 CST（45 分钟）**，写入 ~5.8KB/s——基线 590B/s 的 10 倍，连续四轮实证"高错误密度压缩取证窗口"。
3. **窗口内总量**：PG entries 21,056；ERROR 4,492 + FATAL 2,581；慢查询（>1s）**5,426 条（99 形状）**；user-cancel 风暴 **3,953**（上轮 1,491/74min 的 2.6 倍密度）；statement timeout 击杀 39。
4. **客户端清单（纪律⑭）**：沿用上轮 ss -tnp 三段实锤——172.16.2.210=252 本机（llmgo-252-dev + acc-go + memora）、172.16.2.209=154、172.16.2.241=245；**三台网关共用 252 这一个 PG**（本轮 744 只需落 252 即全覆盖）。

## §一、上轮修复对账（FIX-1~4 部署后首个全天窗口）

上轮 §八.4 要求部署后验证三项，结果：

| 项 | 上轮基线 | 本轮窗口 | 结论 |
|---|---|---|---|
| 42703 hotTableTSColumn 列错误 | 每 promote 周期 10 表 ×4 条 | **0** | ✅ FIX-1 生效，未复发 |
| 23505 folded canonical 冲突 | 32s 集中 18 条 | **0** | ✅ FIX-2 生效，未复发 |
| REFRESH 双发叠跑 | 每 tick 两实例叠跑、252-dev 饿死 | 每 tick 单跑者、无叠跑 | ✅ FIX-4 生效 |
| analyze 被 30s rolconfig 击杀 | ×4/74min | **0 击杀**（SET LOCAL 10min 生效） | ✅ FIX-3 生效（但见 §二 analyze 观测） |

**REFRESH 击杀复发甄别（自审计 A3）**：窗口内 REFRESH routing_analytics_7d 被击杀 1 次（05:22:14）。时间轴重建：tick 节拍 `:X9:14`（05:19:14/05:29:14/…），击杀时刻 = tick 起点 + **180s 整** = pin 语义正确回收，非协调分裂复发；4 次完成 med 136s max 158s 均在预算内。但 med 136s 已贴近 180s 预算（登记 D9）。

## §二、慢 SQL（>1s，5,426 条 / 99 形状）要点

- **analyze_llm_gateway_table_stats ×2：med 503s max 572s**——FIX-3 的 10min 预算内完成（无击杀），但单次 8.5-9.5min、45min 内 2 次 ≈ 连续运转。函数体扫全部 `*_hot` + 11 族 × `p_recent_months=2` 个月分区；月-1 分区在 promote 后理论上冻结，每 13min 重 ANALYZE 疑似纯浪费（登记 D7'，TTL 删除语义未查透前不盲改）。
- **session_aggregate_outbox DELETE ×255/45min（3 副本 × 30s tick）：med 4.2s max 25.1s、击杀 ×3**——done 行 TTL 清理对 645MB 表全表扫（6 个现有索引无一覆盖 status='done'+completed_at），每 10s 烧 ~64MB/s 读 I/O，是整库负载的主要背景噪音。**本轮迁移 744 FIX-6 根修**。
- **digest 回填 SELECT 击杀 ×6**——`WHERE digest IS NULL ORDER BY ts,id LIMIT 100` 无任何索引；且 `session_turns_with_current_month` 视图**名字误导：实为 hot 反连接臂 + 全分区父表**（非"当前月"），backlog=0 时仍要扫 68 万行证明空。3 副本 × 30min backoff ≈ 每 10min 一次重型空扫。**本轮 744 FIX-7 根修**。
- **漂移核查（mv_consistency）med 21.4s max 29.4s、击杀 ×14**（mv_data effective_task_type ×8 + audit_summary ×6，每次 REFRESH 后必跑）——30s rolconfig 临界。**本轮代码 FIX-8 根修**。
- **bindings 候选 med 20.8s max 24.9s、击杀 ×7**——上轮 D2' 主犯原样保留，本轮未修（登记 D2' 续）。
- **MAX(turn_no) ×549 slow（med 1.26s）+ advisory xact lock ×2,616（med 1.46s max 9.7s）**——探针族锁排队证据链。cancel 风暴受害者归因：MAX ×1,994、advisory lock 族 ~1,110、policy 模型替代品查询 ×176（完成态全部 <1s，纯请求断开连带 cancel，collateral 非独立慢查询）、turns INSERT ×65、bodies ×71。
- **D2'' 演化**：上轮"旧分区臂 effective_session 误选"在本轮 EXPLAIN 中已非主要成本（2026_07/08/10/default 分区空表、臂 cost 2.35 平凡）；真实成本 = **hot 反连接臂（1374 行 × 5 分区逐行 anti-join）+ 每次 Gather 孵化 2 个并行 worker**。

## §三、错误 SQL 分类（9 类）

| # | 错误（45min 量） | 根因 | 处置 |
|---|---|---|---|
| E1 | DSM map ×338 + attach ×148（受害者=探针 MAX 查询） | D6 /dev/shm 64MB（pg17-start.sh 已就位 `--shm-size 1g`，待受控重建）；**供给侧放大器 = 每次写轮次的并行 worker 孵化**（FIX-5 根修需求侧） | 重建仍登记 D6 |
| E2 | statement timeout ×39 | 漂移核查 ×14（FIX-8）、digest 回填 ×6（FIX-7）、outbox ×3（FIX-6）、bindings 候选 ×7（D2' 续）、usage_facts/fingerprint/session_key 各 ×2（整点簇发，D2' 续）、REFRESH ×1（pin 正常）、stats 覆盖诊断 ×2 | 本轮收口 23/39 |
| E3 | pocket scheduled_tasks `$4` inconsistent types ×9 | R46 登记的跨库问题（opencode-pocket 所有） | 维持登记 |
| E4 | `unavailable_recover_at` text 类型 ×1 | **外部客户端**（语句形状=内联字面量 `'auto_model_not_found'` + 残留占位符在 char 198 被推断 text = JDBC setString 特征；Go 侧 writeModelNotFoundTiered/writeModelLevelFailureOnly 全分支参数类型核对均为 time.Time） | 登记观察（自审计 A2：差点误修） |
| E5 | smm 库引号损坏 SQL ×3（`1270701active1270701` 等数字串替换引号） | 外部 AI 代理生成的烂 SQL（pg_stat_activity/pg_stat_database 诊断查询） | 登记（跨库不越界） |
| E6 | `pg_matviews.last_analyze` 不存在 ×1 | 同上 AI 代理幻觉列 | 登记 |
| E7 | FATAL connection to client lost ×1,917 | 探针短 ctx 客户端断开（R46 定性维持：应用侧行为），规模随 cancel 风暴同步放大 | FIX-5 落地后观察 |
| E8 | parallel worker terminated ×654 | DSM 失败链的殉葬（同 E1） | 同 D6/FIX-5 |
| E9 | smm password authentication ×10 | 外部错密（上轮 F7 同型） | 维持 |

## §四、本轮修复（改动清单）

| 修复 | 内容 | 证据链 |
|---|---|---|
| **FIX-5** | `domains/session/v2/turn_writer.go`：sessionAdvisoryLockSQL 合并 `set_config('max_parallel_workers_per_gather','0',true)`——按 (tenant,session) 持锁的事务（写轮次/聚合/镜像）整体退出并行，SET LOCAL 语义 commit 自动还原 | 252 真库 EXPLAIN ANALYZE：并行 242-284ms vs 关并行 **90ms（2.7×）**；并行 worker 孵化 = DSM 段分配/释放的窗口内最大来源（E1/E8 供给侧）。真库单语句语义验证（SHOW=0、事务级） |
| **FIX-6** | 迁移 **744** A 段：`idx_session_aggregate_outbox_done_completed_at (completed_at) WHERE status='done'` | 252 EXPLAIN：645MB 全扫（cost 10 万+、4.2s 实测）→ 索引边界扫 cost 1190 |
| **FIX-7** | 迁移 **744** B 段：session_turns 家族 `(ts, id) WHERE digest IS NULL` 部分索引（hot 独立 + 逐分区 CONCURRENTLY + ONLY 壳 + ATTACH 三段式，未来分区 PARTITION OF 自动继承） | 252 EXPLAIN：Parallel Append cost 4404 → **Merge Append cost 65.78（67×降）**，每臂走新索引、排序消失、零 worker |
| **FIX-8** | `bg/materialized_view_refresher.go`：checkConsistency 改 pinned conn + 会话级 180s 预算（与刷新同款） | 击杀 ×14/45min 实锤；对 180s 仍超时才是真异常，走 query_failed 指标 |
| Go ensure | `db.ensureSqlAuditPartialIndexes`：744 的 boot 幂等镜像（indisvalid 检查 + INVALID 残留 DROP 重建 + 动态分区枚举 + ATTACH 守卫 + 744 账本 stamp），接入 ApplyMigrations 链（ensureSessionSummariesArchivalSchema 之后） | 727/728/729 只有手工 psql 通道无 boot 收敛，本轮补齐（上轮 FIX-5..9 同型） |
| 契约 | `sql/migrations/startup/743_*.sql + .down.sql`（729 三段式同款）+ `migration_743_test.go` 契约测试（C1-C4）+ installer `psqlConcurrencyRequired` 排除表补 744 | installer --single-transaction 与 CONCURRENTLY 不兼容（727 C3 先例） |

**生产面实改**：744 已于当日 06:30-06:40 在 **252 真库应用**（psql -f、postgres superuser、`CREATE INDEX ×7 + DO ×2` 全绿）+ **本机真库应用**；8 索引 indisvalid/indisready 全 t（纪律⑫复查完成）。三台网关共用 252 PG，单点落地即全覆盖。

## §五、测试

```
go build ./... 全绿；go vet ./db/ ./bg/ ./domains/session/v2/ ./installer/... ./sql/... 全绿
go test ./db/ ./domains/session/v2/ ./bg/ -count=1                    # 全绿
go test ./installer/cmd/llm-gw-installer/ -run "TestCanonical|Stats"  # 全绿（排除表契约）
go test ./sql/migrations/startup/ -run "TestMigration743|727|Canonical" # 全绿
本机真库：TestEnsureSqlAuditPartialIndexes_RealDB PASS（DROP→ensure 重建 indisvalid=t→二跑幂等 1.96s）
252 真库：744 应用后 8 索引 indisvalid 全 t；outbox trim 与 digest 回填 EXPLAIN 双 A/B 实证
```

明确未覆盖：① bindings 候选 med 21s 未修（D2' 续）；② analyze 范围/节奏未调（D7'）；③ 容器未重建（D6 受控窗口）；④ FIX-5/8 属网关代码，需随下一部署窗口上三台（744 是 DB 层已生效）。

## §六、自审计（同日批判式复核）

| # | 初版问题 | 复核处置 |
|---|---|---|
| A1 | **首选方案"MAX 改写 ORDER BY DESC LIMIT 1"被真库 EXPLAIN 否决**——视图 hot 反连接臂阻止 Limit 下推，计划原样（Parallel Append cost 4404），改写白做 | 改为并行度控制并 EXPLAIN ANALYZE 实测（90ms vs 242-284ms）后才动手。教训：**查询改写必须先在真库 EXPLAIN，不能靠教科书模式** |
| A2 | **曾把 E4 判为 writer.go bug 并准备修复**——日志语句带 `'auto_model_not_found' ,`（逗号前空格）与 Go 静态 SQL 不符；逐分支核对参数类型全为 time.Time；内联字面量+残留占位符 = 简单协议不会留下的 $n = 外部 JDBC 客户端特征 | 推翻初判，登记观察。教训：错误语句的**格式指纹**（占位符残留、空格风格）先于代码归因 |
| A3 | REFRESH 击杀 1 次初看疑似 FIX-4 协调分裂复发 | 时间轴重建（tick `:X9:14` 节拍 + 击杀=tick+180s 整 + 每 tick 单跑者）→ pin 正常回收。上轮结论经证据加固 |
| A4 | analyze 572s/call 未调——月-1 分区"冻结"取决于 TTL 删除语义（未查透），盲改有统计过期风险 | 按"盲调不修"纪律登记 D7' |
| A5 | 视图名 `session_turns_with_current_month` 与实际语义（hot+全分区）不符——上轮 D2'' 结论基于"旧分区臂误选"，本轮证实空分区臂平凡、真实成本在 hot 反连接臂+并行孵化 | D2'' 重新定性；FIX-5 消除并行孵化后 MAX 实测 90ms，D2'' 关闭（如有复发再开） |
| A6 | **迁移号与并行轨道撞号**：开工时 HEAD=ef87317c5 遂用 744-1=743；提交时远程并入了并行轨道的 743_normalize_provider_protocol——文本合并零冲突但 `TestNumericUpMigrationVersionsAreUnique` 契约已破，且本机已盖错误 743 账本戳（会让真 743 被跳过） | 全套重编号 **743→744**（文件/契约测试/ensure stamp/排除表/报告）+ 删除本机错误戳（252 账本干净：.sql 为 729 式无戳、ensure 未上 252）。教训：**并行轨道期间新迁移号必须 push 前 pull 重查，文本零冲突 ≠ 号位无冲突** |

## §七、登记（本轮不修）

- **D2'（P1 续）bindings 候选 med 21s（击杀 ×7/45min）+ 小时 rollup 整点簇发 ×6**——当前最大 30s 临界族，建议下轮主轴。
- **D6（P2）容器 /dev/shm 64MB**——pg17-start.sh 就位；FIX-5 落地后 DSM 需求侧大减，受控重建优先级可降但建议仍做（连带 auto.conf 持久化评审）。
- **D7'（P2 新）analyze_llm_gateway_table_stats 572s/call 连续运转**——月-1 分区冻结语义查透后再调（跳过冻结分区 or 拉长 cooldown）。
- **D9（P2 新）REFRESH analytics_7d med 136s 贴 180s 预算**——FIX-6/7 落地后整库负载下降可能自然回落，观测两个 tick 周期再议。
- **E4/E5/E6（P3）外部客户端**：JDBC text 时间戳 ×1、smm AI 代理烂 SQL ×4——移交对应系统 owner。
- D8（P3）252-dev healthz ready 恒 false（顺延）。

## §八、下一轮提示词（建议）

> 以本文 §七 D2'/D6/D7'/D9 为起点，审计入口 docs/audit/playbook/orchestrator-prompt.md。优先级：
> 1) D2'：bindings 候选查询（`SELECT cmb.credential_id, pm.raw_model_name, ...` med 21s 击杀 ×7/45min）+ 小时 rollup 错峰——本轮击杀时间轴即基线；
> 2) 部署窗口带 FIX-5/8 上三台网关后验证：cancel 风暴量级（基线 3,953/45min）、DSM 错误量（基线 486/45min）、漂移核查击杀（基线 14/45min，预期归零）；
> 3) D7'：analyze 范围调优（先查透 TTL 删除对月-1 分区统计的影响）；
> 4) D6：受控窗口重建 pg-252-pg17（若 cancel/DSN 观测显示 FIX-5 已足够则降级为评审项）。
> 纪律沿用第五轮 ⑪-⑲ 全部 + 本轮新增：⑳ 查询改写先真库 EXPLAIN（A1）；㉑ 错误语句格式指纹先于代码归因（A2）；㉒ 并行轨道期 push 前重查迁移号（A6）。

## §九、handoff 更新

- 记忆库：llm-gateway-go-audit-cycle-progress 追加本轮；pg-252-sql-log-audit-facts 增补（视图名误导、outbox 645MB 无 done 索引、并行孵化=DSM 主源 3×实测、744 三段式与 installer 排除表、E4 JDBC 指纹）。
- 原始物证：252:/tmp/pg252-snapshot-0924.log（15.6MB）；本地 /tmp/pg252-audit-0924/（parse.py/kills.py/victims.py + analysis.txt/kills.txt/victims.txt）。
