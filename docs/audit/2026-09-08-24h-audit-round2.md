# 2026-09-08 24h 审计（第二轮）

范围：`d85534f4..3da1024e4`（09-07 09:07 起 24 小时窗口，75 commits / 176 文件），并以 codegraph 全库图谱（13.4 万节点）+ 5 个并行子代理（IR 协议 / 存储分区 / 队列并发安全 / 供应商错误闭环与可观测 / 24h 变更专项与 auto 模型）按 12 项审计要求核对。基线：`go build ./...`、`go vet`、installer 双向对账在修复前已有一项红（见 P1-1）。

## 修复（本轮 6 commits，已合入 main）

| Commit | 内容 |
|---|---|
| `20d9bcd59` | **P0-分区** 687 迁移修复 473 遗留的 9 表 08:00 分区边界污染（26 个坏分区，10-01 起 ensure 42P17 连锁）；installer 补 685/687 embeddata + 修复 runner.go 漏登 682-687 导致的对账红 |
| `244e3deff` | **P0-IR/会话** Responses 非流式保留 tool_calls 伴随文本；messages/responses 会话身份对齐 chat（deriveGatewaySessionID + EnsureV2WithID）；宽松 tool_call 解析提取真实工具名；注册 goroutine 补 recover |
| `28280b597` | **P1-凭据健康闭环** HealthTracker.OnError 重新接线到 dispatch 失败汇聚点（f7eb0eb1b 裁撤旧循环后 80% 硬降级/分 kind 梯度/自动调优全停摆、滑动窗恒 100%）；附接线契约测试 |
| `9d7df18b2` | **P1-安全** notification 三渠道接 safehttpclient（F-6 只接了 webhook）+ Allowlist 配置；feedback/注册两处后台 goroutine recover；webhook 退避溢出封顶；node_probe 日志不再输出密文前缀 |
| `b1f9da6ca` | **P1/P2-杂项** task_default_routing 平台默认行 NULL 语义（'default' 字面量匹配不到任何行）；摘要 rune 截断；CreateV2 守卫；parquet 双 WriteStop once 守卫（非幂等，双 footer 会损坏文件）；db-sync 远端循环补 EXCLUDE_DB_REGEX；restore 路径注入 lock/statement timeout |
| `67debef8c` | **结构标注** classifier_v3 死代码 Deprecated；导出的 InitializeRequestIdentity 标注防复活随机 gw_ 缺陷；过期 //nolint:unused 删除 |

687 已在临时库用三类夹具（有 default+缝隙行 / 无 default / 干净分区）验证：边界全部修复、行数守恒、幂等。夹具库已清理。

## 审计结论摘要（按 12 项要求）

1. **流程/数据/反馈闭环**：错误记录主干（classify 28 类 → dispatch 汇聚 → candidate_failure_logs_hot → supplier_errors_hot(V371) → 凭据详情 unified 读端）是通的；断点在降级反馈（OnError 失联，已修）与 auto 学习信号（见未修项 #1）。
2. **IR 定义**：internal/ir 四协议矩阵 + requestfact（Timeline 瀑布/Attachments/Integrity）总体完整；缺 project/多 tag/user 维度/总轮次字段（未修项 #4）。
3. **传输完整性**：Responses 丢文本已修；流式 IR 灰度路径 Anthropic 序列化丢 output_tokens/stop_reason（未修项 #2，flag 门控默认关）。
4. **多轮双向解析/逐轮摘要**：逐轮原文有（bodies hot），逐轮"去格式供人查看"摘要层不存在，assistant 回复不进摘要输入（未修项 #5）。
5. **队列/并发/安全**：队列分层与锁纪律良好（race 抽查通过）；泄漏面抽查无实体。SSRF 覆盖面已补三渠道，数据面出口（provider 探测/quota fetcher）按设计不拦。
6. **分区存储**：16 表 hot+promote、更新删除仅 hot、8h 保留成立；473 污染是唯一系统性缺口（已修）；session_summaries 无分区仅软标（未修项 #6）。
7. **双模式**：lite（sqlite+memory+files）装配完整、无可触发 panic；降级面未文档化（未修项 #8）。
8. **auto 全量**：决策/执行/转移/记录/导出/重排代码链路接通；auto_route_selections 0 行三根因已由 24h 内修复钉住；断点在反馈=合成成功（未修项 #1）。
9. **可观测/UI**：request_flow 结构化日志仅 3 个埋点（未修项 #3）；前端三类控件（时间/反馈条/徽标）未复用共享实现（未修项 #7）。

## 未修项（按优先级，移交 handoff）

1. ~~**P1 auto 学习信号断链**~~ **✅ 已修（后续轨道 Track A，commit d123e5518）**：决策时点不再写 IsSuccess=true 占位行——有真实 request id 时进入 pending 注册表，由三协议终态（emitTelemetry/EmitFailure）经 `autoroute.ReportRoutingOutcome` 回填真实 success/latency/cost 后写出；10min TTL + 8192 容量兜底，未上报条目按旧占位语义补写。置信度硬编码 0.8 已改为贯通 cls.Confidence（RoutingFeedback 新增 Confidence 字段，integrator 对 ≤0 回退 0.8）。副产品：人工标注回写按 request_id 从"永不命中"变为实际可用。
2. **P1 流式 IR Anthropic 序列化**：Usage 帧互斥丢 CompletionTokens、Delta 帧不输出 stop_reason（internal/ir/stream.go:851-953）。TRANSPORT_LAYER_IR_ENABLED 门控，默认关。
3. **P1 request_flow 覆盖面**：dispatch V2 的 node_switch/preflight 拒绝/流中断无 request_flow 行；仅 slog 不落库。
4. **P1 requestfact 元数据缺口**：project/tags[]/user_id/turn_total 无承载字段（internal/ir/types.go:511、internal/requestfact/types.go:128）。
5. **P1 逐轮人读摘要**：MessageSource 每轮只取最后一条（恒 user），assistant 终文不参与；无 per-turn digest 层。
6. **P1 session_summaries 无界增长**：archival 只打标不删（domains/sessionarchive/archiver.go），无分区。
7. **P2 前端统一性**：时间格式化 ×4、反馈条 ×3、徽标未复用共享实现；ErrorDetailTab 未展示后端已算的 retryable_count/stage_counts；ProbeHealthPanel 手动触发用 ILIKE models[0] 占位。
8. **P2 lite 模式降级面**：无 Redis 时会话功能静默消失，启动日志未列出降级清单（storage_mode_init.go:97）。
9. ~~**P2 promote SQL 默认 7d vs Go 8h**~~ **✅ 已修（后续轨道 Track C#7，commit d21134104，startup 迁移 688）**：9 个 promote 函数 DEFAULT 对齐 Go 调度值（8 个 → '8 hours'；request_logs_bodies → '24 hours'，与 Go 专属默认一致）。更正：审计原定位 341:210 已非线上最新，实际以 602/659/628/640 的当前定义为准逐字复制仅改 DEFAULT；已存在且正确的 8 个函数（supplier_errors/handoff 等）不动，model_probe_runs 纯运维遗留不动。夹具库验证：DEFAULT 生效 + 空表调用返回 0 + 幂等，installer 5 点同步 + TestStartupFilesAreAllEmbedded PASS。
10. **P2 数据面 SSRF 评估**：quotafetcher/provider_probe 把 API key 发往 admin 配置 URL，可考虑域名校验。
11. **结构清理清单**：classifier_v3（已标注）、V1/V2 PreClassify/PostClassify 重复块抽 helper、handler.go(8956 行) 身份块拆 identity.go、scoring 三文件合并、bg 探测域 20+ 文件收敛、client_adapter_v2.go.disabled 移除、PreClassify 传副本严格化、缓存复用 selection 加 cache_reused 列。

## 验证

- `go build ./...` PASS；installer 独立模块 `go test ./...` PASS（修复前 TestStartupFilesAreAllEmbedded 红）
- `go test`：streaming(72s)/executors/ir/bg/notification/routingopt/exporter/session/sessionsummary 全绿
- `go test -race`：bg 探针、routingopt feedback/affinity 抽查通过
- 前端本轮无改动；vue-tsc 于审计中由子代理跑过 PASS
- 后续轨道合并验证（6ec23a521）：`go build ./...` PASS；autoroute/routingopt/streaming `go test` 全绿（含新增 -race 用例）；installer `TestStartupFilesAreAllEmbedded`/`TestStatsStartupMigrations*` `-count=1` PASS；688 夹具库三点验证（DEFAULT 生效/空表调用 0 行/幂等）PASS
