# R30 — 24h 修正审计轮（2026-09-16）

- 窗口：2026-09-15 ~04:00 → 2026-09-16 ~05:00（部署血统 3a68ff1f 之后的全部增量 + 系统横向维度）
- 方法：codegraph 全量图谱重建 + 主代理 + 6 路子代理并行审计（每条发现经主代理亲读复核后才动手修正），覆盖任务书 12 个维度
- 审计输入：balance-floor 九项修复（ae41328c4/74240e999/f915808dc）、p2.2 反馈聚合（2e8bfdce1）、w5 ml-ci（efc0d2c30）、licensing 本地免费激活（c0f71b440）、免费容量优化（8f00bde9c）、freediscovery 健康反馈（99e77a587）、hostedtask P0、R29 定稿文档

## 一、九项修复核验结论（全部属实）

A-C2 Start 重入守卫 / A-C1 keyring RWMutex / B-E1 逃生门 2h / D-L1 Stop join 10s /
E-B1 有界并发池（semaphore，LLM_GATEWAY_FLOOR_PLAN_CONCURRENCY 默认 10）/ F-L1 周期汇总日志 /
F-L2 Warn 15min 限流 / G-O1 控制面重试 1+2 仅网络错/5xx（F-1 请求只构造一次）/ F-2 空 sweep 静默
—— 逐项与 docs/handoff/2026-09-13-balance-floor-guard-handoff.md §2026-09-16 对账一致。
`go test ./bg/ -race` 与集成 harness（GUARD_IT_DSN）均绿。

## 二、发现与处置（本批已修正）

### P0

| # | 发现 | 处置 |
|---|------|------|
| P0-1 | ml-training CI integration-check 必挂：workflow 传 `--output`，make_go_fixture.py 只定义 `--out`（b3f8a1a65 起潜伏，efc0d2c30 融合时未修） | workflow 改 `--out`；同修 M-2（`TestManifestLoad`/`TestRoutingMLFlags` 全仓零命中=幽灵测试名，改 `TestLoadMLManifestFromFixture|TestMLSelectorEndToEnd` 真实测试 + `go vet`）；M-3（CI Go 1.26.6 < go.mod 1.27.1 对齐）；config.yaml `annotation_require_known_label` 重复键去重 |

### P1

| # | 发现 | 处置 |
|---|------|------|
| P1-1 | freediscovery 健康反馈绕过 RLS：scan_scheduler.recordScanSuccess/Failure 裸连接池写 provider_templates，非 default 租户被 RLS 过滤成 0 行，"连续 3 次自动禁用"对非 default 租户永不生效 | 新增 runPrivileged（与 cycle() 同款 super_admin+bypass_rls LOCAL GUC 事务），两条 UPDATE 包入 |
| P1-2 | IR refusal 块 parse 侧保留（R21）、三个响应序列化器全部丢弃 → refusal-only 响应坍缩为空壳成功 | buildOpenAIResponseContent/buildAnthropicResponseContent 补 refusal→text 分支；buildResponsesResponseOutput 把 refusal 并入 output_text 流 |
| P1-3 | sessionsummary V2 消息源 `Content string` 解不了多模态块数组 → 该轮从摘要输入整体消失 | sessionMessageV2.Content 改 json.RawMessage + v2ContentText 块数组→文本/占位符抽取（对齐 sessiondigest）；system_prompt_prefix 同步；回归测试钉桩 |
| P1-4 | session_tools 直写分区母表（INSERT+UPDATE），session_tools_hot 与 promote 是空转死设备；行一旦 promote 进 columnar 月分区 UPDATE 直接报错 | 写链切 session_tools_hot；Get/Update/List×4/ListToolNames 全部 hot∪母表联合读（NOT EXISTS 兜 promote ON CONFLICT 残留双行）；GetStats/ListStats（tool_usage_stats_hot 同病）同款修复。partitionDate 保持 UTC 日与会话族 calendarDate 一致（子代理"上海日历"说法经核实不成立） |
| P1-5 | 473 类时区缺陷漏网 6 函数：4 个 ensure（session_module_executions/dashboard_access_events/cache_metrics/handoff_logs）+ 579/580 promote 月份分组未钉扎；686 已实际复发（42P17） | 迁移 714（CREATE OR REPLACE + SET LOCAL 钉扎 + DECLARE 初始化器移入函数体 + 账本 upsert），已登记 revision-sequence 通道；db-changelog 补条目 |

### P2

| # | 发现 | 处置 |
|---|------|------|
| P2-1 | balance-floor 逃生门与货币摘出组合配置永久乒乓：pass B 按新鲜货币证据摘出 → escape hatch 以"plan 证据陈旧"释放 → 货币下限被绕过 | escape hatch WHERE 增补：货币证据新鲜且仍击穿时不释放 |
| P2-2 | errorsx `function` 名词在无 status 门的包裹路径（ClassifyError）会把客户端 function/tool 错误判成 model_not_found → survival 硬终 + 绑定 5min 冷却 | 新增 modelNotFoundWrappedRe（剔除 function 名词）仅用于包裹路径；体路径（有 400/404/422 门）保留全名词；回归测试钉桩 |
| P2-3 | 聚合器 DELETE+重插在双实例下 NULL 维度 global 行无唯一约束 → 同桶重复行 SUM 双倍计数 | 事务首行 pg_advisory_xact_lock 串行化 |
| P2-4 | supplier_errors 历史月分区无任何 drop 清单（hot 有界、历史无限增长） | stateTableTTLSpecs + settings spec 补 `lifecycle.supplier_errors_ttl_days`（默认 90，689 helper 逐分区 DROP，无迁移） |
| P2-5 | licensing 免费激活无认证端点可被任意 hardware_hash 占掉唯一设备席位（首启 DoS）+ 席位冲突返回 500 | 免费 hash 必须与本机 GenerateFingerprint().Hash() 一致（指纹不可用退回原行为并 Warn）；ErrDeviceLimitExceeded → 409 + need_deactivate；补 Store/Activator nil 防护（L-4）；gofmt 收敛（L-5） |
| P2-6 | template_manager.Update 全字段回写丢更新：GET 快照与 UPDATE 之间调度器写入被旧快照覆盖，自动禁用可被静默复活 | 动态 SET：仅写请求涉及字段；健康三列仅在显式 enable=true 时清零（与 README 对齐） |
| P2-7 | balance-floor 汇总 `probed=len(planCands)` 在 ctx 到点时虚报（未探测也计入） | planSweepStats 增 probed/skipped 原子计数，runPlanProbes Acquire 失败 continue 计 skipped |

### P3（顺带清账）

- restorePulledCurrency 死代码（balURL/egress 无请求发生）移除 + 注释如实化（pass A 持有刷新权）
- ClassifyResponseBody 补 isGenericWebBody 守卫（与 WithBody 的 V20 对齐）+ 误导性注释更正（KindUpstreamDown 本身可重试，收益在归因）
- 709 down 头注释 706 错字更正（711→709 重编复制残留）
- freediscovery README 与代码矛盾更正：手动启用=健康三列清零（以代码为准）
- ml-canary-rollback-runbook 补 ROUTING_ML_RELOAD_SECONDS（默认 0=禁用热更新）并修正"无热更新"绝对表述
- routing_optimization_metrics 聚合表当前写-only（GetAggregatedMetrics 零调用方）——保留聚合器（成本可忽略），已在本档标注为已知债；后续给 admin 挂只读端点或暂停聚合器二选一
  **→ R32 已修（处置=挂只读端点）**：新增 `GET /api/admin/routing-opt/metrics?hours=N&task_type=&provider=`（聚合表首个生产读者，hours clamp 1..720 与 30 天保留期一致，LIMIT 2000 防护，全参数化）；同批聚合器接 R31 distlock leader 选举（follower 免扫 30min 窗口，正确性仍由 advisory lock 兜底），钉桩测试 admin×5 + bg×4

## 三、核实为健康的关键面（无需动手）

- **流式**：anthropic 7 参死桥已退役；六桥空流门矩阵齐备（responses_bridge 补齐并有钉桩测试）；arg-first hold 有界（1MB）；integrity breach→incomplete 终态四协议钉桩在位；survival 首帧前静默切换不中断、错误文本不泄漏（terminal 只发 kind 化信封）
- **供应商错误闭环**：supplier_errors_hot 8h promote（V371）→ 凭据详情 error-detail API + web ErrorDetailTab（error_summary/recent_failures/quality_scores_7d，双道脱敏）；breaker 单进程锁内迁移无竞态，跨实例靠 DB 幂等收敛
- **IR 序列化**：JSONB 全走 `$N::text::jsonb`（22P02 免疫）；Extensions+paramreg 未知字段跨协议保真；requestfact T0-T9 调度瀑布/session_turns 诊断组齐备
- **队列/LB**：五层信号量 + dispatch Governor（并发/RPM/TPM），队列满即拒绝+Retry-After 非阻塞；权重 0/负数边界全部钳位；候选空→降级模式→全 cooling 放行三级兜底
- **auto 决策链闭环证实**：分类→推荐→P2.2 re-rank→affinity→settle→feedback_log→AdaptiveLearner(+2pp checkpoint)→re-rank 回灌；affinity 经 settle worker 回灌；双回路均有消费方（与"反馈只落表"的担忧相反）
- **双存储模式**：full/lite 开关明确（lite 六 env 收口禁 redis）；lite 缓存 L1+L1.5 文件级；分区管理器 lite 下不启动
- **outbox**：mirror_outbox 失败登记+重放器（SKIP LOCKED/lease/指数退避/dead）+ kill switch 接线完整

## 四、遗留（未在本批动手，已登记）

> R31 收口（2026-09-16 会话）：#1、#5、#10 已修，见各项标注；其余 7 项仍开放。

1. **P2 settle/affinity worker 无 leader 选举**：双实例重复扫（幂等安全，计数虚高+JOIN 开销）；建议套用主网关 Redis token-bucket 选举（main.go:3584 模式）——**已修 aea284108**：共享 acquireSweepDistLock 门控（settle/affinity 各自 key、TTL>sweep 超时），无 Redis 行为不变；钉桩×8
2. **P2 未知路由入口**：/routing-decisions、/admin/approvals、/admin/output-compliance、/admin/usage、/correlations、/quality-correlations、/routing/overrides 无菜单无页内链接（灰藏页需注明）
3. **P3 FreeDiscoveryView 手写 table/window.confirm**，未复用 ui/ 套件（DataTable/AppModal/confirmDialog）；freediscovery 页未展示 auto_disabled_at 等新健康字段
4. **P3 currency pass A 串行头部阻塞**（挂死端点饿死 sweep；60s 预算 ~6 行）——建议入 runPlanProbes 同款并发池或失败退避戳
5. **P3 免费档熔断画像未覆盖 legacy 协议路径与探针路径**（recordProtocolCircuitFailure/CloseProbe billing-blind）；reducer Degraded 阈值 3 与熔断 free=6 分叉；BillingMode 空串静默回退——**已修 ceddf5438**：recordProtocolCircuitFailure 改 RecordFailureWithBillingMode（chat×3+anthropic×1）；CloseProbe/ProbeCheck 系零调用死接缝，删除防复活；reducer Degraded 边界对齐 circuitFailureThreshold(billingMode)；空串回退 paid 经查活路径不可达（两条候选 SQL 均 COALESCE 'per_token'），钉扎为文档契约
6. **P3 Governor 并发等待默认无服务端时限**（MaxQueueWaitMS=0，热键可调，上限 60s）——低并发凭据长尾来源
7. **P2-5 归档 codec**：requestfact wire 缺 class/due_at；Responses reasoning.summary 数组形式静默丢弃；遗留消息级 function_call 无解析无上报；extractSystemPrompt 丢首条 system 非首块
8. **installer 709/710 副本未同步**（Go ensure 链功能兜底，canonical 三路守卫经由 Go-ensure 通过）；下次 installer 触碰时五点同步
9. **licensing 免费分支零处理层测试**；免费 License 无吊销通道/到期机制（L-2/L-3，产品决策项）
10. **escalated to exponential cooling 日志不实**（升级分支不递增 coolingCycle，free 恒 15s 平铺）——行为或文案二选一修正——**已修（行为）01350f089**：升级分支递增 cycle 并按升级策略指数计算（paid 30s→…→30min 封顶 / free 15s→…→5min 封顶），cycle≥5 持续故障告警恢复生效；RecordSuccess 重置故单次毛刺不爬坡；两个旧平铺行为测试重写

## 五、测试与验证

```
go build ./...                                        # clean
go vet ./bg/ ./errorsx/ ./licensing/ ./domains/freediscovery/ ./domains/sessionsummary/ ./domains/toolexecution/ ./settings/ ./internal/ir/
go test ./bg/ -count=1                                # ok（含 -race 前序多轮）
go test ./errorsx/ ./domains/sessionsummary/ ./internal/ir/ ./domains/toolexecution/ ./domains/freediscovery/ ./settings/ ./licensing/ -count=1   # ok
bash scripts/apply-db-revision-sequence_test.sh       # 通道契约 passed（714 登记）
```

## 下一轮提示词（建议）

> 以 docs/audit/2026-09-16-r30-24h-audit-round.md §四 遗留清单为起点，优先 settle/affinity leader 选举与免费档熔断画像两 P2；动 installer 前先读 llm-gateway-installer-migration-3way-sync 五点同步纪律；新增迁移前 git fetch 核对远端编号（当前已至 714）。
