# 2026-09-10 24h 审计（第七轮 — R6 后续候选四项执行）

范围：R6 报告 `docs/audit/2026-09-10-24h-audit-round6.md`「后续候选」四项的
执行收口：① 死代码下线（initRawDataLogger/initSemanticAnalyzer）、
② BatchUpdateNodes 退役、③ architecture.md 存储描述修正、
④ storage/memory → storage/lite 命名收敛。

本轮无新缺陷发现；属清理 + 文档钉线批次。会话开始时 pull --rebase 吸收
远程并行合并（aa9beb6c7/d12e28acd，部署脚本向，与本轮四项无交集）。

## 执行时间线

| Commit | 内容 |
|---|---|
| `872d3ca54` | **② BatchUpdateNodes 退役**：删 PgStore 方法 + 测试桩 batchStore（2 files，+10 −70） |
| `ff4617734` | **① 死代码下线**：logging_init.go 整文件删除（79 行） |
| `469a69f71` | **④ 命名收敛**：storage/memory → storage/lite（package lite，MemoryStateStore 名保留） |
| `55a3ae057` | **③ 文档修正**：architecture.md 双模式存储描述（+12） |

## 各项执行细节与关键核实

### ① initRawDataLogger / initSemanticAnalyzer 下线（`ff4617734`）

- 两函数全仓无调用方，整文件删除。**R6 前提部分修正**：
  `logging.RawDataLogger`（同步版）**结构性存活**——它是
  AsyncRawDataLogger 的内部落盘引擎（NewRawDataLogger/writeEntries/
  CurrentLocation/Close 均被 async 路径与包内 envelope 测试消费），
  不存在"整体下线"选项，本轮不删。
- **环境变量契约零变化**（逐个核实读取点）：`LLM_GATEWAY_RAW_LOG_*` 三项
  由 main.go 诊断装配块直读（default-on）；`LLM_GATEWAY_SEMANTIC_ANALYSIS_ENABLED`
  由 main.go 语义分析器装配点直读（opt-in "true"，与死函数 default-on 语义
  不同，以 main 为准）；`LLM_GATEWAY_ANOMALY_*` 一直在 main.go 直读。
  死函数只是陈旧的重复装配，删除后无任何行为或契约变化，无需公告。

### ② BatchUpdateNodes 退役（`872d3ca54`）

- manager 全局落库路径经 5486952e8 + R5 重构后在 per-node 锁内逐节点走
  UpdateNode；BatchUpdateNodes 全仓零调用方且不在 Store 接口
  （proxy/types.go Store interface 无此方法）。
- 删 PgStore.BatchUpdateNodes；删测试桩 batchStore；
  `TestBatchPathRacesWithSingleNodeProbe` 直接用 sharedNodeStore，
  继续钉住全局路径 per-node 串行化守门（P0 回归保护不减弱）。

### ③ architecture.md 存储描述（`55a3ae057`）——**R6 前提证伪**

- R6 报告（转引自 memory lite-mode-architecture-doc-discrepancy）称
  "Full 模式实际是 SQLite+文件，文档写 PostgreSQL 属漂移"。**经当前代码
  核实该说法不成立**：`StorageModeFull` 路径（storage/factory/factory.go
  PG pool + Redis client、db/db.go、proxy/store_pg.go）就是 PostgreSQL +
  Redis；即便回溯到 2026-09-09 版本，factory.go 引用行号处也是 PG 初始化
  代码。memory 快照（lite-mode-storage-schemes 称 "Full=SQLite+file
  bodies"）已失真，本轮一并修正 memory。
- **真实缺口**：architecture.md 完全未提 Lite 双模式存储。补充：
  - Storage 节改为双模式结构——默认 full（原 PG/Redis 内容不变）+ 可选
    lite（`LLM_GATEWAY_STORAGE_MODE=lite`：SQLite 三 store + 文件 bodies +
    进程内 KV，main.go 显式跳过 PG/Redis 初始化），指路
    docs/storage/README.md 与 lite-mode quick-ref；
  - 注明 proxy.Store（订阅/节点/策略）为 PG 专属、不在双模式切换内；
  - Deployment Modes 表新增 Lite 单二进制行。

### ④ storage/memory → storage/lite（`469a69f71`）

- R6 所记 "memory.Storage → lite.Storage" 中的类型名从未存在（实际类型为
  `MemoryStateStore`）；按命名收敛的本意执行：目录 storage/memory →
  storage/lite、package memory → package lite。类型名 MemoryStateStore
  保留（与 SQLiteSessionStore / FileBodiesStore 的"实现形态+接口"命名平行）。
- 模块内引用面仅 storage/factory 三文件（import alias memorystore →
  litestore）+ 根包 factory.go / stubs.go 注释 + cmd/gateway 注释；
  errClosed 消息前缀 memory: → lite:。
- docs/storage/README.md（活文档）路径同步；ADR-0019 以追加 Amendment
  方式记录（不重写历史正文）。docs/design/lite-mode-*.md 交付期历史文档
  中的旧路径按历史记录保留，不改写。
- 无外部使用方（module 为服务二进制，storage/memory 原引用仅在
  storage/factory 内）。

## 验证

- `go build ./...` PASS
- `go vet ./proxy ./cmd/gateway ./storage/...` clean
- `go test ./proxy -count=1` PASS（0.95s）；`-race` PASS（1.9s）
- `go test ./storage/... ./cmd/gateway -count=1` 全 PASS
  （含 lite 一致性套件、factory、file、lite、sqlite、cmd/gateway）
- `go test ./sql/migrations/startup -run TestNumericUpMigrationVersionsAreUnique` PASS
  （push 前纪律）
- push 前 `git fetch` 复核：origin/main 无并行新提交（d12e28acd 基线）
- web 未触碰，无需 typecheck

## 后续候选（非本轮范围）

1. docs/design/lite-mode-*.md 交付期历史文档中的 `storage/memory` 旧路径
   （历史记录，低优先级；如需统一建议随文档大版本清理）。
2. `logging.RawDataLogger` 同步版如未来要真下线，需先给 AsyncRawDataLogger
   换独立落盘引擎，属重写级改动，无计划。
3. memory 快照已修正（lite-mode-storage-schemes 重写、
   lite-mode-architecture-doc-discrepancy 删除、lite-mode-naming-convention
   标记完成）；本轮教训——**引用 memory 中的行号级断言前必须对当前代码
   复核**，R6 报告 ③ 的前提即因未复核而失真。

## 结论

R6「后续候选」四项全部执行完毕（两项代码清理 + 一项改名 + 一项文档），
其中两项（①③）在执行时依据当前代码修正了 R6 报告的前提偏差并以实际
核实结果落地。无新缺陷；全量验证通过，已推送 origin/main
（d12e28acd..55a3ae057）。
