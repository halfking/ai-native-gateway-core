# R12 自审计（2026-09-11，推送后复核）

对象：main ff41075ff（R12 六提交：80f422b91 / d272fd664 / 1ff4bf578 /
4e31ebeb1 / 3aa8252c5 / ff41075ff）。方法：逐提交对照需求复核 + 远端
实测补验（`-race` 补跑、死代码扫描复跑、契约比对）。

## 一、任务总结

R11 移交的 R12 首批五组任务全部交付并推送：候选 7（Limiter 热更新
上限守卫）、候选 4（fallback 转换器收敛 IR 薄包装）、候选 23（web 死
代码清理）、候选 21（datetime 哨兵统一）、候选 18 尾巴（errors/trend
UI + ErrorDetailTab 健康字段）。相关文档：
`docs/handoff/2026-09-11-r11-closure.md`（任务来源）、
`docs/audit/2026-09-11-24h-audit-round11.md`（发现定级）、
`docs/handoff/2026-09-11-r12-closure.md`（交付与 R13 规划）、
`docs/prompts/task3-error-display-optimization.md`（候选 18 原始需求）。

## 二、特性需求 → 实现 → 验证

| # | 需求 | 实现 | 验证证据 |
|---|---|---|---|
| 1 | admin 热更新并发上限不被恢复循环 ~15 分钟拉回全局默认 | credHotLimits 记录热更新值；recoveryStep 目标 = min(credentialLimit, 热值)；shrink 后仍恢复至热上限 | TestRecoveryStepRespectsHotUpdatedCapacity；-race 全绿（20.1s） |
| 2 | IR 关闭时 fallback 不再整包硬错/丢数据/泄内部字段 | 双份 ConvertAnthropicResponseToChat 收敛为 IR 薄包装；tool_use.input RawMessage 透传；_kxg_meta 移除；convertBlockMessage 逐条展开 tool_result + 尾随文本；system 双形态 | transformation/anthropic + streaming + integration 四包测试绿（含 -race）；scalar-input/_kxg-meta/并行 tool_result/块数组 system 四个回归测试 |
| 3 | 删除不可达 web 视图/组件（保留 formatCalculatedAt） | main.ts 可达性扫描（级联安全 + 字符串交叉验证）删 77 文件；scan 工具入库 | vue-tsc、vitest 基线对比零新增失败、vite build PASS；复扫 DEAD VIEWS/COMPONENTS = 0 |
| 4 | datetime 空值哨兵全站统一 | useFormat 四函数 '' → '—'（主流哨兵） | useFormat.test.ts 重钉后绿 |
| 5 | 错误趋势可视化 + 错误明细补健康字段 | errors-trend.ts 客户端（hours 1/24/168 与 parseVendorErrorHours 契约一致）+ ErrorTrendPanel tab + ErrorDetailTab 补 health_latency_ms/health_error；i18n ×8 locale | vue-tsc/i18n parity/CJK 棘轮/vite build 全绿 |

## 三、审计发现与修正（本提交）

1. **[-race 误判，已补验]** 闭环文档原记 "-race 未跑成（平台/工具链
   差异）"——实际 darwin/arm64 支持 -race。补跑：credential（20.1s）、
   transformation/anthropic、internal/ir、streaming 全套（72.4s）全部
   PASS，候选 7 并发正确性获得实测背书。文档表述已更正。
2. **[deadcode-scan 误报，已修]** `*.d.ts`（ambient 声明，由 tsconfig
   消费、从不被 import）被列入 DEAD OTHER，会误导 R13 清理执行者。
   已在脚本中单独归类为 AMBIENT 并附人工甄别提醒（DEAD OTHER 含
   i18n/scanner.ts 等测试工具，import 可达性看不到测试消费方）。
3. **[确认非问题]** CredentialMonitorWithTabs 新 tab 标签沿用同文件
   既有硬编码中文先例（列表视图/热力图…），CJK 棘轮因候选 23 大幅
   下降仍通过；R13 可统一 tab 标签 i18n 化（记入遗留，不阻塞）。

## 四、遗留（转 R13）

- web package-lock 漂移（npm ci 不可用）——R13 首项
- DEAD OTHER 36 模块人工甄别删除
- 巡检类 R9 候选 12/13/14/15/16/8、候选 20 孤儿路由六条
- tab 标签 i18n 统一（本次审计新增，低优先级）
