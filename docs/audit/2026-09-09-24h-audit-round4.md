# 2026-09-09 24h 审计（第四轮）

范围：`b9b84c3b1..HEAD`（R3 报告之后落地的批次：proxy 691 接线、deploy-local
contracts、useActionMessage + datetime 工具、RoutingOutcomeStats Prometheus
导出、EmitRateLimited 决策行、OpenAI→Anthropic message_start 流首估算、
IR 传输层整体下线 ADR）。直接代码核对 + 子代理并行（5 路）审计 +
目标测试套件全跑，最终发现并修复 1 个 P0 wiring gap（R3 同型问题复现）+
报告合成阶段另有 1 个 P2 manager 缺陷（#2）被并行 audit 修掉。

## P0 修复（已落 main，5e6c9ac3c）

| Commit | 内容 |
|---|---|
| `5e6c9ac3c` | **P0-接线** 691_proxy_region_policy.sql 接线五点补齐：b9b84c3b1 在 `installer/internal/dbinit/runner.go` StartupFiles 注册了 691，但漏掉 `installer/cmd/llm-gw-installer/embeddata/startup/` 的物理拷贝、`main.go` 的 `//go:embed` 变量、`embeddedSQLFiles` map 条目；`TestStartupFilesAreAllEmbedded` 红了，**与 R2 报告的 682-687 wiring gap 同型问题**。修复后 reconcile 测试 + 全 installer 模块测试 PASS |

## P2 同步修复（并行 audit 修掉，7086cc1fb）

| Commit | 内容 |
|---|---|
| `7086cc1fb` | **P2-Proxy fail-safe** prevent stale cache and ineffective failover：`selectNodeExcluding(excludeNodeID)` 让 ForceSwap 不再重选当前失败节点（关掉 R3 deferred #2）；selection policy 缓存语义收紧；`manager_region_test.go` 新增覆盖测试 |

修复要点（与 R2 教训同源）：

- embeddata/startup/691_proxy_region_policy.{sql,down.sql} 物理拷贝
- main.go 加 `//go:embed embeddata/startup/691_proxy_region_policy.sql` + `proxyRegionPolicyMigration691 []byte`
- main.go `embeddedSQLFiles` map 加 `"startup/691_proxy_region_policy.sql": proxyRegionPolicyMigration691` 条目
- 顺手把 `RecordConversionPathForRequest` 的过时注释（"Called by the TransportFactory.Pick instrumentation point"）改为 ADR 引用 + 现状说明（工厂已下线、零生产写入端、`applyRoutingMetadata` 仍在读端）

## 审计结论摘要

### 1. Proxy 691 接线（`b9b84c3b1`）

**Wired items 核验**：

| 项 | 结果 | 位置 | 备注 |
|---|---|---|---|
| 691 物理存在 + StartupFiles 注册 + canonical schema 镜像 | ✓（修复后） | `sql/migrations/startup/691_proxy_region_policy.sql` + `installer/internal/dbinit/runner.go` + `db/db.go` |
| strategy enum 与 Go 常量一致（`weighted_rr`/`least_conn`） | ✓ | `proxy/store_pg.go:44` |
| CHECK 约束 + 正向阈值校验 | ✓ | `proxy/store_pg.go` UpsertSelectionPolicy/GetSelectionPolicy |
| 6 个新 admin handler HTTP method 405 检查 | ✓ | `admin/proxy.go` + `admin/proxy_test.go:TestProxyNewRoutesMethodEnforcement` PASS |
| `/api/proxy/policy` 统一 dispatcher | ✓ | `admin/handler.go` |
| `handleProxyStatus` 空 regions + error 字段 | ✓ | `admin/proxy.go` |

**R3 deferred 14 项 manager 缺陷复审**：

| # | 项 | 状态 | 位置 | 严重度 |
|---|---|---|---|---|
| 1 | aggregate cache partial 时 global selection 漏选 | **PARTIAL**（present-but-empty 走 store fallback；缺显式 coverage flag） | `proxy/manager.go:303-307` | P2 |
| 2 | auto-swap 可能重选当前节点 | **FIXED (7086cc1fb)** | `proxy/manager.go:selectNodeExcluding` | — |
| 3 | 并发 health-check 互相覆盖 | **PRESENT**（无 per-node keyed mutex；走 DB 写仍存在竞态） | `proxy/manager.go:1480-1500` | P2 |
| 4 | health-check 去重只用 ProxyURL | **PRESENT**（同 ProxyURL + 不同 HealthCheckURL 会被合并） | `proxy/manager.go:1152-1185` | P3 |
| 5 | 调度全局 health-check 不更新 active swap 计数器 | **PRESENT**（仅 recordActiveSelection/update node.Health，未走 recordProbeResult） | `proxy/manager.go:1110-1140` | P3 |
| 6 | 手动 health-check 探测 PasswordDecryptFailed 节点 | **PRESENT**（线路 853 跳过滤但未统一覆盖手动路径） | `proxy/manager.go:853` | P2 |
| 7 | 区域匹配大小写/空白敏感 | **PRESENT**（PgStore normalize 但 manager 自身不 normalize） | `proxy/manager.go` 多处 | P3 |
| 8 | policy 持久化绑 *PgStore | **PRESENT**（Store interface 未扩展） | `proxy/store_pg.go:44` | P3 |
| 9 | refresh commit nodes 在订阅 metadata update 前 | **PRESENT**（非事务） | `proxy/manager.go:1536-1546` | P2 |
| 10 | Start 持 lifecycleMu 跨同步 loadAllNodes | **PRESENT**（Stop 可挂起） | `proxy/manager.go:115-122` | P2 |
| 11 | swapLoop 用 context.Background | **PRESENT**（Stop 需等最多 15s/active probe） | `proxy/manager.go:1034` | P2 |
| 12 | refresh goroutines 在 wg 外 | **PRESENT**（cacheRefreshes LoadOrStore 但无 wg.Add） | `proxy/manager.go:1536-1546` | P2 |
| 13 | empty subscriptions 不进 negative-cache | **PARTIAL**（line 303-307 注释已表达意图，命中逻辑 OK；但 ListSubscriptions 全量安装负缓存的契约仍未实现） | `proxy/manager.go:303` | P3 |
| 14 | 缺覆盖测试 | **PARTIAL**（manager_region_test.go 新增但覆盖不全） | `proxy/manager_region_test.go` | P3 |

**Verdict**：proxy 模块作为可发布版可上线（核心 wiring 修复+ i18n 完整+ admin 405 保护+ store 校验），14 项 manager 缺陷经本轮 + 并行 audit 已修 1 项（#2），剩余 13 项中 6 项 P2 真实存在。建议下一批次做：
- 并发 health-check 写竞态（#3）
- Start 持锁跨 loadAllNodes + swapLoop context.Background + refresh wg 三件套（#10/#11/#12）合并为一个"Manager shutdown 安全"批次
- transactional refresh（#9）+ manual health-check skip PasswordDecryptFailed（#6）+ aggregate cache coverage flag（#1）

### 2. Deploy-local contracts（`27f1148e6`、`15a7d8c27`、`6c6f5d870`）

**测试套件 PASS**：`tests/deploy_local_contract_test.sh` 24/24 PASS（含 27f1148e6 新增 4 组 + 历史 20 组）

| 契约 | 状态 |
|---|---|
| 蓝绿切换 vs controlled restart 分支顺序 | ✓ locked |
| `dl_wait_port_free` 守门 | ✓ locked |
| `dl_verify_release` stderr 诊断 | ✓ locked |
| `dl_ensure_release_available` 4 分支（active/intact/empty self-heal/fail-closed） | ✓ locked |
| remote wrappers 无 sshpass、key-only 干跑 | ✓ |
| `--cleanup-downloads` 选用 + legacy tree 归档 | ✓ |
| active 端口默认 8782 + INSTALL_ROOT guard | ✓ |
| 同版本重部署保护 active bundle（`16b2291e8`） | ✓ |

### 3. 前端工具收敛（`cef1081d5`、`9143f11a4`、`52067a55c`）

| 项 | 状态 | 备注 |
|---|---|---|
| `useActionMessage` composable | ✓ | 41 个 `.vue` 引用；无 n-message/n-alert/n-notification 残留 |
| `utils/datetime.ts` 工具 | ⚠ partial | 22 个 `.vue` 引用，但仍有 ~10 处 `new Date().toLocaleString()`（TenantModelPolicyPanel、FeeCostCell、NodeDetailOtherModelsPanel、NodeDetailAvailabilityPanel、SessionSummaryDrawer、SessionTurnListItem、SessionStatsPanel） |
| `ErrorDetailTab` 消费 retryable_count + stage_counts | ✓ | `52067a55c` + 8 语言包补键 |
| `vue-tsc --noEmit` | ✓ PASS | |
| `vitest run` | ✓ 826/826 PASS | 6.71s |

**剩余**：`utils/datetime.ts` 收敛度约 70%，剩余 ad-hoc toLocaleString 主要在 session 模块与 NodeDetail 类组件，下一批次统一。

### 4. 可观测性（`c4f50ec32`、`36c4126c1`、`ADR-0003`）

| 项 | 状态 | 备注 |
|---|---|---|
| `autoroute/outcome_metrics.go` 5 计数器 Prometheus 导出 | ✓ | `routingMetricPrefix + "outcome_total"`，单 label `result` ∈ {stashed,matched,orphan,expired,dropped}；init() 注册；`prometheus.MustRegister` via `sync.Once` |
| `domains/streaming/request_log_pipeline.go:EmitRateLimited` | ✓ | 写入 `request_status=rate_limited`（非 failure）+ `emitFailedDecisionLog` 决策行；handler.go:2024 captureAndEmitRateLimited 三处调用（key_throttled、rate_limit_exceeded） |
| `rate_limited_decision_log_test.go` | ✓ PASS | 两条用例：基本发出 + disabled client skip |
| `ADR-0003` requestfact 生产接线 + hash 版本化决策 | ✓ concrete | 明确"契约就绪/生产待接线" + V1 hash 全量 fail-closed + 未来 V2 codec 路线；不修 wire 常量 |
| 副作用：handler.go:2875 备用 `captureAndEmitRateLimited("rate_limit_exceeded", ...)` | ✓ | |

### 5. 流式 OpenAI→Anthropic input_tokens（`495f5bf0d`）

| 项 | 状态 | 备注 |
|---|---|---|
| `domains/streaming/executors/input_token_estimate.go` 算法 | ✓ | chars/3.5 + image/document 固定 1600 + blob (>=4096B 无空白) len/5 + system/tools/messages 遍历；非 Anthropic 形状回退整体估算；空 body → 0 |
| `executor_chat.go:1177` 调用 `estimateAnthropicInputTokens(params.BodyBytes)` | ✓ | |
| `OpenAIToAnthropicSSEFunc` 接受 `inputTokensEstimate int` 参数 | ✓ | `domains/streaming/executors/executor.go:458` |
| `anthropic_stream.go:314` `message_start.usage.input_tokens = inputTokensEstimate` | ✓ | 注释明确"SSE 不可回写，流尾仅日志比对" |
| `input_token_estimate_test.go` | ✓ PASS | |

### 6. IR 传输层下线（`d206ca771`、`db64c6bdf`、`7a04b1c0c`、`78089454d`、`2de8bce6e`）

**Dead code 删除**：

| 文件 | 状态 |
|---|---|
| `domains/transformation/factory.go` | ✓ gone |
| `domains/transformation/layer.go` | ✓ gone |
| `domains/transformation/ir_transport.go` | ✓ gone |
| `domains/transformation/legacy_transport.go` | ✓ gone |
| `domains/transformation/detector.go` | ✓ gone |
| `domains/transformation/metrics.go` | ✓ gone |
| `domains/transformation/fixture.go` | ✓ gone |
| `domains/transformation/testdata/ir_golden/` | ✓ gone |
| `domains/streaming/response_format_adapter.go` | ✓ gone |
| `docker-compose.yml` `TRANSPORT_LAYER_IR_ENABLED` | ✓ gone |
| `domain/README.md` 死文档 | ✓ gone |
| 11 个对应测试文件 + `tests/integration/ir_default_switch_test.go` | ✓ gone |

**Active code preserved**：

| 文件 | 状态 |
|---|---|
| `internal/ir` 全包（parser/serializer） | ✓ 保留（executors/dispatch/hooks/audit 仍消费） |
| `domains/transformation/ir_converter.go`（TransportIRConverter） | ✓ 保留（生产请求转换主路径） |
| `domains/transformation/circuit_breaker.go` | ✓ 保留（按 provider 熔断在用） |
| `domains/transformation/extension.go` | ✓ 保留（IRExtensionExtractor 在用） |
| `domains/transformation/anthropic` 子包 | ✓ 保留（活跃桥在用） |
| `(ir.StreamChunk).SerializeAnthropic` + golden | ✓ 保留为库（仅单测消费，零运行时成本） |

**已知遗留**（按 ADR 已接受）：`SerializeAnthropic` 缺 message_start 前置缓冲/content_block 生命周期/signature_delta 透出 + `InternalResponse` 缺原生 StopReason/StopSequence 字段——仅影响未来接线路径保真度，对现行活跃桥（手写 anthropic_bridge/responses_bridge）零影响。

**遗留 stale comment 修复**（本轮）：`domains/streaming/executors/routing_source_map.go:RecordConversionPathForRequest` 的"Called by the TransportFactory.Pick instrumentation point"改为 ADR 引用 + 现状说明（5e6c9ac3c）。

### 7. 其他

- `db/migrations/397_proxy_region_policy.{sql,down.sql}` 已删除（被 691 替代）；`db/db.go` `ensureProxyManagementCanonicalSchema` 是真正的运行时 schema 来源
- `cmd/gateway/logging_init.go` 注释仍提及已删除的 `initEnhancedIRTransport` 路径——历史注释，不影响运行（init() 已注释化）；本轮不动以免扩删面
- `docs/adr/2026-09-09-ir-transport-layer-retirement.md` + `docs/adr/ADR-0003-requestfact-production-wiring-and-hash-versioning.md` 已交叉引用 Track F/G

## 验证（修复后）

- `go build ./...` PASS
- `go test ./proxy ./admin -count=1` PASS（admin 66s）
- `go test ./autoroute -count=1` PASS（含 outcome_metrics_test）
- `go test ./domains/streaming/... -count=1` PASS（streaming 71s + executors 23s）
- `go test ./installer/... -count=1` PASS（`TestStartupFilesAreAllEmbedded` + `TestStatsStartupMigrations*`）
- `bash tests/deploy_local_contract_test.sh` PASS 24/24
- `pnpm vue-tsc --noEmit` PASS
- `pnpm vitest run` PASS 826/826

## 未修项（移交下一批次）

按优先级（与 R3 deferred 14 项的子集对齐 + 几条新发现）：

1. **P1 Manager 并发安全批次**：#3 health-check 写竞态 + #10 Start 持锁 + #11 swapLoop context + #12 refresh wg——4 项打包为一个"Manager shutdown safety"批次
2. **P2 frontend datetime 全收敛**：utils/datetime.ts 替换 ~10 处 ad-hoc `new Date().toLocaleString()`（Session/NodeDetail 类组件）
3. **P2 proxy #9 transactional refresh**：subscription metadata + nodes 同步落库；或半失败时 invalidate cache
4. **P2 proxy #6 manual health-check skip PasswordDecryptFailed**：统一 helper 避免漏路径
5. **P3 proxy #4 HealthCheckURL 复合键 + #5 recordProbeResult in scheduled + #7 region normalize + #8 Store interface**
6. **P3 proxy #1 explicit coverage flag**：替代现有"partially warmed cannot prove empty"启发式
7. **P3 cmd/gateway/logging_init.go 历史注释清理**：`initEnhancedIRTransport` 已下线 1 年+，注释误导新人

## 与 R3 报告的衔接

- R3 P0 #1（IR transport 决策批次）→ 本轮验证下线完成 ✓
- R3 P0 #2（message_start.input_tokens）→ 本轮修复并 wiring 完整 ✓
- R3 P0 #3（RoutingOutcomeStats 导出）→ 本轮 Prometheus 导出实现 + init 注册 + 测试 ✓
- R3 P0 #4（EmitRateLimited 决策行）→ 本轮实现 + 测试 ✓
- R3 P0 #5（requestfact ADR）→ ADR-0003 落盘 ✓
- R3 deferred 14 项 manager 缺陷 → 本轮 7 项 P2 仍 present，建议下批统一打
- R3 #10 前端 UI → 本轮 useActionMessage 收敛 100%、datetime 收敛 70%、ErrorDetailTab 完成

## 风险与回滚

- P0 修复（5e6c9ac3c）：纯 wiring 补全，无业务逻辑变更；reconcile 测试已守门；回滚 = `git revert 5e6c9ac3c`
- Stale comment 清理（5e6c9ac3c）：仅注释文案，不影响任何调用路径；不可观测的回滚点

## 教训

- **R3 教训"wiring gap 同型问题复现"**：R2 已经出过 682-687 wiring gap（reconcile 测试抓住），本次 691 wiring gap 是同源问题（runner.go 改了，embeddata 没改）。建议在 PR 模板里加 checklist "新增 startup 迁移 → 5 点同步"作为提交门槛
- **IR 传输层"测试代码修复假覆盖"风险**：R3 报告已点名 SerializeAnthropic 修复"实际零生产覆盖"。本次 ADR 明确下线决策，避免未来再次踩坑——任何"修复"前先 grep 生产调用点
- **审计报告 commit 与实现 commit 不同步风险**：本轮发现 R3 报告已落 main 之后又出现新 wiring gap，说明 report 自身的核对未对接到 reconcile 测试全跑——后续 audit report 应在写完后跑一遍 reconcile + go test 才算"完成"
