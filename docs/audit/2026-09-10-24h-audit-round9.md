# 2026-09-10 24h 审计（第九轮 — 主代理+6 子代理并行审计）

范围：① R7 报告（d35997688）之后的 8 个提交逐个深审（本轮唯一未经审计的
新变更面）；② 用户 checklist 全维度六路并行扫审（IR 与协议适配 / 队列并
发与负载均衡 / 分区与双模式存储 / 供应商错误处理链 / Web UI 一致性 /
post-R7 diff）。方法：codegraph 索引 + 主代理汇总 + 6 个只读子代理并行，
全部发现经主代理逐项核实后修复或转为后续项。

## 结论速览

发现 **P0×2、P1×4、P2×10+、P3×15+**。本轮修复并提交 **10 个 commit**
（P0 全部、P1 全部、P2 六项）；其余 P2/P3 转入「后续候选」并附执行要点。
全量 `go build ./...` PASS、触碰包 `go vet` clean、全量 `go test ./...`
PASS（0 失败）、`domains/dispatch -race` 连续 3 次 PASS。

## 本轮修复（按 commit，rebase 后以 git log 为准）

| 级别 | 内容 |
|---|---|
| **P0** | dispatch：定时请求二次准入重置 take-once 令牌（a97e7afe2）。`totalQueueDone` 全仓只写不复位，parkScheduledRequest 首次准入即消费，onScheduledDue 二次 tryEnqueue 后 drainer 的 releaseTotal CAS 必失败 → queued 永久 +1；累积约 1000 个定时请求（X-Gw-Due-At 生产可达）后 Tier-0 等待室打满，**全网关 total_queue_full**。溢出路径回拨令牌防 complete() 误减未占用槽位。回归测试 TestScheduledRequestReAdmissionDoesNotLeakTotalSlot 无修复时红。 |
| **P0** | dispatch：`recordDecision` 加 journalMu 串行化 + completed 后拒绝非终态追加（f553079e8）。dispatch()/tryModelChangeOutcome/scheduleCapacityRetry 在 I/O 后写 journal，与 Submit ctx 过期路径的 complete() 并发 append 同一 AttemptJournal slice（无锁）——丢条目/头撕裂；JournalSnapshot 同锁拷贝。保住「终态后无追加」不变量。 |
| **P1** | deploy：`scripts/deploy-lib` 软链重指（fcabd678d）。b071bb120 写成 `../../../`（解析到不存在的 official-deploy/deploy-lib），154/245/deploy.sh/local-host-blue-green.sh 四入口与五个契约测试在干净 checkout 全断；按 _shared-lib.sh 头注重指 `../../../../ai-native-tools/deploy-lib`，bg 契约测试加软链可解析前置断言（防悬空时 `! grep` 空转假通过）。 |
| **P1** | streaming：Q2 桥（OpenAI→Anthropic 客户端）`delta.reasoning_content` 整段丢弃 → thinking 块输出（e924f7ac4）。DeepSeek R1/GLM-Z1/QwQ 经默认主路径思维链全丢；与非流式转换器语义对齐（无 signature），块序严格递增，tool 块打开前与流尾 flush 前显式闭合 thinking 块。 |
| **P1** | sessionsummary：buildRollingPrompt 补 `MaskSecrets`（全量路径 buildSummaryPrompt:352 有、此生产路径漏）+ `content[:300]` 字节截断改 truncateRunes（CJK 非法 UTF-8，同文件 229 行同款 bug 漏网）（384e33285）。 |
| **P1** | bg,admin：HC/主动探测错误体脱敏（d7a6d30cb）。bg 包此前零 errorsx 引用——401/403 错误体常回显被探测凭据本身，truncateBody/truncateProbeBody 原样直写 credentials.health_error / model_probe_runs.error_message 并直出 admin UI；写入侧两个 choke 统一走 SanitizeErrorText（顺带修字节截断 CJK），读出侧 probe_history error_message 与凭据 meta health_error 再脱敏兜历史行。 |
| **P1** | dispatch：credForwarder loop per-item panic recover（d073dec1e，C-#14 模式）。acquire 成功（Redis 租约占位）到 go attempt 之间 panic 会使该凭据队列悬挂至 Stop、租约等 TTL；panic 时释放租约 + complete(dispatch_panic) 后循环继续。 |
| **P2** | db：ensure 链补 user_intent varchar(50)→200 条件收敛（fddb80a0e）。692 只修 SQL 文件，纯 ensure 环境（6b7410dd3 自证数字迁移到不了）持续 22001 写入失败；v_session_flow 同义 DROP/CREATE（定义与 692 逐字一致）。 |
| **P2** | attachments：SVG 强制 attachment + 全局 nosniff（af96ff990）。上传 MIME 客户端可控（parseDataURI 无白名单），image/ 前缀判断对 SVG 失效 = 网关域存储型 XSS。 |
| **P2** | streaming,ir：Q3 桥 input_json_delta 续写 + IR 响应 tool_use 冗余块（1a7779a51）。Q3：stop 时 start+delta 整体过 ValidateStreamingToolArgs——有效则追加续片段（OpenAI 客户端按片段拼接），无效（relay junk delta）则丢弃尾部打 tool_args_continuation_discarded 旗标，不再破坏已流出片段（注：content_block_start.input 经 json.RawMessage 提取必为完整 JSON 值，故拼接失效即 junk，语义绝不回归）。IR：buildOpenAIResponseContent 不再向 content 数组发射缺 input 的 tool_use 块（OpenAI 规范形态是 tool_calls，双份表示会被严格 SDK 拒绝）。 |
| **P2** | executors：candidate_failure_logs context 逐字符串脱敏（721b90f6c）。事实源表不再弱于投影表 supplier_errors_hot——context 含 err_msg/upstream_raw_error/preflight_reason 自由文本，原样入库使 v_candidate_failure_logs_diagnosis 等直接消费者泄露面更大。 |

## post-R7 八提交审计结论（子代理 1）

- **e3738133a（692）**：两副本逐字节一致、视图重建与 V354 二进制等价、
  幂等、BEGIN/COMMIT 包裹、无其他依赖方。遗留「ensure 链缺口」已由
  fddb80a0e 修复。
- **6b7410dd3（ensure 689/690/471）**：镜像逐字忠实、只补缺失对象、行数
  守恒 fail-closed、简单协议单事务。遗留：多实例并发 boot 竞态（P2，见
  后续候选 1）、镜像缺 drift 护栏测试（P3，候选 2）、471 回填每 boot
  重跑（P3，候选 3）。
- **6beeec03f（autoroute NULL）**：指针化 + 默认 0 + pressure 守卫完整；
  其余可空列早已指针化。
- **b071bb120（deploy-lib）**：resolve_build_image 与旧内联三层回退语义
  等价或更强；问题仅在软链本体（已修）。
- **b366b51cd / ee932ef1a（测试）**：均为真行为钉（fresh 翻转+计数、除零
  守卫、no-backup 错误串、bgDataPlaneOnly 逐字对应现行门控）。
- **08c0b7e6e/4d3142c9b**：三处版本一致；08c0b7e6e 提交信息与实际 diff
  （menu-config exported_at）不符，记录备查无需返工。

## 各路扫审摘要与系统层判断

### IR 与协议适配（子代理 2）

现状图谱：IR 主结构在 internal/ir（types.go 超集请求/response.go/stream.go），
传输层已按 ADR 下线；现行链路 = 请求方向 executor 先走 IR（TransportIRConverter，
默认开、按 provider 熔断、熔断回落手写桥），响应流式方向为手写活跃桥
（Q2/Q3/Q4/responses 编排），非流式默认 IR 序列化。会话/轮次：sessiondigest
（260-rune 去格式、排除 tool arguments、双版本）+ sessionsummary（user_intent/
tags）+ requestjourney（流程跟踪）+ dispatch/waterfall（调度瀑布）。

已修：Q2 reasoning_content（P1）、Q3 input_json_delta（P2）、IR tool_use
冗余块（P2）。**后续候选**：
4. fallback 转换器簇硬错（IR 关闭才受影响）：`ConvertAnthropicResponseToChat`
   的 tool_use.input 声明 map[string]any（标量 input 整包 unmarshal 失败，
   IR 版已用 json.RawMessage）；`messages.go convertBlockMessage` 首个
   tool_result 即 return（并行工具调用的其余 tool_result + 尾随文本全丢）；
   system 声明 string（块数组 system 整包失败）；`_kxg_meta` 私有字段注入。
   系统层建议：fallback 收敛为薄包装 IR 而非维护第二套语义。
5. IR 杂项：未知 content 类型静默丢弃（响应方向无 RawContent 兜底）、
   SerializeResponsesResponse usage 丢 cache/reasoning 细分、
   ParseOpenAIResponse 只取 choices[0]、Q2 `data:`（无空格）帧整帧丢弃。

### 队列/并发/负载均衡（子代理 3）

调度链路：Tier-0 totalQueue(1000) → Tier-1 每模型 lane(300) → Tier-2 每凭据
lane → Governor（并发/RPM/TPM）→ attempt（FpSlots→熔断→外层 Limiter→
PeakCollector）。P2C LiveLoad 读写对称、等分加权掷签、准入 take-once、
五级 residue drain 齐备、requestjourney outbox SKIP LOCKED+fencing。

已修：totalQueue 槽位泄漏（P0）、journal 竞态（P0）、forwarder recover（P1）。
**后续候选**：
6. ~~banditOrder 的 pressure 因子读 Limiter 信号量（恒 1.0，251e751df 同根
   bug）；Bandit 当前未接线（main.go:1459 注释），接线前必须先修——改用
   calculateConcurrencyScore 同一 LiveLoad 信号源。~~
   ✅ 已修 2026-09-10（beee856ac）：banditOrder 签名穿 ctx，pressure 改经
   calculateConcurrencyScore（LiveLoad 优先 + Limiter 回退），保留
   capacity=0 不衰减语义；回归测试 TestBanditOrder_PressureFactorReadsLiveLoad
   修复前红/修复后绿 + legacy 路径护栏。Bandit 接线前置条件已满足。
7. Limiter recoveryStep 无差别 RecoverStep(全局默认 50)，会把 admin 热更新
   的低并发上限 ~15 分钟内拉回 50（summarize 路径 AcquireAll 仍硬性执行该
   信号量）；修法：shrink 基线 min(基线, 热更新值)。
8. 慢性无界增长三处（量级小）：concurrency_peak_collector counters 按
   (cred,model) 只增、router weightCounters 以候选集身份串为键只增、
   registry pending 僵尸（终态 CAS 输掉后 MarkRetryScheduled 复活而
   onRetryDue 因 completed 短路）；另 policy_publisher 初始 LISTEN 失败
   永久禁用热推送直至重启。
9. dispatch -race 曾出现一次非确定性失败后连续 3 次 PASS——疑似既有
   flaky，未定位到具体用例，值得挂 keep-on-error 复跑观察。

### 分区与双模式存储（子代理 4）

16 张 hot 表 + bg/partition_manager 三 worker（promote 1h advisory-lock /
ensure+archive 24h / cleanup 1h）；hot-only 写边界经 telemetry/trace/
selection/bodies 全链核实；双模式分派 ~65 处 dbConn 守卫抽查无漏；
lite sink「有 meta 必有 body」、文件存储路径遍历防护与原子写齐备。

**后续候选**：
10. ~~baseline/objects 仍留 602 修复前的危险 promote 函数体（DELETE 先行 +
    INSERT 失败吞异常 = 当年实丢 52.9 万行的根因代码），对象级恢复/对比
    回灌会复活缺陷；DEFAULT 仍是 7 days。同步为 602/628/640/659/688
    线上版或文件头标注 stale。~~
    ✅ 已修 2026-09-10（31a0b8cee）：9 个 hot_to_partition 对象文件 +
    deploy/installer 两份 baseline 同步 688 线上体（原子 CTE + 显式列 +
    DEFAULT 8h/24h）；default_batch 未动（与线上一致）；objects 计数 112
    保持不变。session_turns 无对象文件/baseline 条目，无危险体不涉及。
11. ~~tool_usage_stats 读侧漏 8h 热数据（GetUsageStats/GetTopTools 直查父表，
    348 建的联合视图 tool_usage_stats_with_current_month Go 侧零调用）、
    fallback 写直打父表与 hot 双行分裂。~~
    ✅ 已修 2026-09-10（516be72d0）：读改查联合视图且按天 GROUP BY 聚合
    （免疫双行分裂），删 hot 失败直写父表的 fallback（缺表即报错）；注意
    该文件三个导出方法当前全仓零调用（休眠 API），为接线前防御性修复。
12. lite 模式 SQLite 元数据表无 TTL 消费方：retention.request_logs_days
    声明即终态，长期运行 lite 实例无界增长；补按天删除 worker。
13. `*_default` 分区缝隙行无自动重路由任务（promote_default_batch 仅存于
    baseline，336 已 .skip；partition_manager 只调度 hot 搬运）——473 类
    边界错位再现时只能人工修复；补低频计数巡检+告警。
14. promote 批次预算跨表共享按固定顺序消费，尾部表（auto_route/
    supplier_errors）饥饿风险；改 per-table 保底份额或轮转起点。
15. ensure 分区边界依赖会话 TimeZone（标准部署 Asia/Shanghai 自洽，非 +08
    会话执行会造 8h 缝隙 = 473 成因模式）；函数体首加 SET LOCAL TIME ZONE。
16. 注释漂移：telemetry client "0-7 天"（实为 8h）、partition_manager 称
    candidate_failure_logs ensure 函数空 body（392/689 已有完整逻辑）、
    sqlite turns_store 可空列 Scan 进非指针 int。

### 供应商错误处理链（子代理 5）

错误链路闭环成立：upstream.Error 归类 → CandidateFailureWriter 双写
（candidate_failure_logs_hot 脱敏 body + supplier_errors_hot 低基数维度）
→ bg 聚合 provider_error_details/supplier_error_stats → 调度
HealthTracker 梯度降级 → 客户端首字节前 SSE `: thinking:` 注释帧（不进
会话、opencode Zod 安全）/ 非流式 X-Gateway-Failover-Notice → 凭据详情
error-detail（表→API→视图闭环 + request_id 跳转）。

已修：HC/探测错误体脱敏（P1）、context 逐串脱敏（P2）。**后续候选**：
17. 首字节后流中失败无客户端提示通道（ADR-Disp-003 限定 pre-first-byte，
    属设计约束下的体验缺口）：ChunkCountersSnapshot>0 且 terminal 中断时
    补发 `: thinking:` 注释帧（不进会话），提供热开关。
    ✅ 已修 2026-09-10（d848e7453）：forwardForDispatch bytesSent 终态点
    触发 OnMidStreamFailure（客户端取消跳过），三协议入口桥接
    preStream.writeThinking；热开关 LLM_GATEWAY_MIDSTREAM_FAILURE_NOTICE
    （默认开）；测试 TestMaybeNotifyMidStreamFailure。
18. `/api/errors/trend`（V371 趋势读端）无任何前端消费者；凭据维度候选
    失败端点读出脱敏与 list 端不一致；ErrorDetailTab 未渲染 API 已返回的
    health_error/health_latency_ms。

### Web UI 一致性（子代理 6）

已收口项未复发（31 视图 + 11 组件 datetime、useActionMessage、
useConfirmDialog 42 文件）；菜单↔路由无死链、路由守卫完备。**后续候选**：
19. [P1] menu-config.json 导出脚本内嵌副本与 appNav.ts 漂移（多 plugin 会话
    项、缺 /admin/turns 等 4 项）；export-nav-config.ts 重写版未接线且
    schema 不同、误运行会破坏 tenantScope 消费方——择一接线或删。
20. [P1] 6 个孤儿路由（页面注册但无菜单无入口）：/routing-decisions、
    /quality-correlations、/admin/output-compliance、/admin/usage、
    /routing/overrides(/audit)。
21. [P2] datetime 残留 15 文件（composables/utils/types 层，不在已收口
    42 文件内）；useFormat 与 utils/datetime 双轨哨兵不一（'' vs '—'）。
22. [P2] 原生 alert() 残留 60 处/14 文件（CredsTab 17 处最密）、原生
    confirm/prompt 绕过 useConfirmDialog（prompt 三处活代码）；空态
    EmptyState 仅 4 视图用、手写 112 处；加载态三形态并存。
23. 清理候选：死视图 19、死组件 26（含 SessionTurnsPanel 链、analytics 9 个
    EnhancedDashboard 陪葬件）、debug-filters.js、export-nav-config.ts。
    删除时注意 quality-api.ts:formatCalculatedAt 仍被 QualityTab 活引用。

## 验证

- `go build ./...` PASS；触碰包 `go vet` clean
- 全量 `go test ./... -count=1` PASS（0 失败）
- `go test ./domains/dispatch -race -count=1` ×3 PASS
- `bash tests/deploy_blue_green_contract_test.sh` PASS（含新软链断言）
- web 侧本轮无代码改动（全部转后续候选）
- push 前 rebase 吸收远端 zstd 批次（ef0739f4a/f645cfb7d/aa67ff1b7），
  与本轮修复零交集，rebase 后关键包复测 PASS

## 后续候选优先级建议

> **2026-09-10 执行进度**：第 1、2 项全部完成 + 第 3 项的候选 17 已完成
> （6=beee856ac、10=31a0b8cee、11=516be72d0、17=d848e7453，均已推 main，
> 各项测试全绿）。剩余待办从第 3 项的候选 18/19 继续。

1. ~~**接线前必修**：banditOrder LiveLoad（候选 6）~~ ✅ 完成（beee856ac）。
2. ~~**数据安全**：baseline/objects 危险 promote 函数体（候选 10）；
   tool_usage_stats 读侧（候选 11）~~ ✅ 完成（31a0b8cee / 516be72d0）。
3. **体验闭环**：~~流中失败 `: thinking:` 提示（候选 17）~~ ✅ 完成
   （d848e7453）；errors/trend UI 接入（候选 18）；menu-config 漂移
   （候选 19）。
4. **清理批次**（破坏性，一次做）：fallback 转换器收敛（候选 4）+ web 死
   代码（候选 23）+ datetime/useFormat 双轨（候选 21）。
