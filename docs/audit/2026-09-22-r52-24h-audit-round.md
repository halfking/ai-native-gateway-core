# R52 24h 审计轮 —— 2026-09-22

- 基线：`119981c02`（R51 审计轮基线之后）→ HEAD `5dc4e9f3b`，本轮修复后 HEAD 见 §四。
- 窗口：24h 内 15 个非 merge 提交，两条主线：① R51 修复波（655ca58b2..993a41a1b，F1-F23 收口 + 735 迁移）；② 窗口内两个新 feature 提交 ce85e767a（国内编程客户端识别链路 zcode/MiniMax Code/DeepSeek Code）+ 5dc4e9f3b（名义 reasoning_content，实际 MiniMax/vLLM 参数提升）。
- 方式：主代理汇总 + 7 个只读子代理并行审计（D01 IR / D02 协议适配与客户端识别 / D16 R51 修复波复核 / D14 安全恒查 / D07+D06 分区与双存储 / D04 队列并发 / D17 代码卫生），证据均到 file:line，主代理逐条亲读复核后才定级修复。
- 会话前置：git fetch 后发现本地分支 0 领先 / 20 落后（R51 修复波已由并行会话推入 main）——快进合并无冲突；工作区 4 个脏文件（VERSION/version.json/menu-config.json/web version.json）为版本钩子漂移，弃置后 ff。

## ⚠️ 专记：提交信息失真（5dc4e9f3b）

5dc4e9f3b 提交信息声称"新增 reasoning_content 支持（ReasoningContent 字段/StreamChunk.ReasoningDelta/TestParseOpenAIReasoningContent 6 子测试）"，实际 diff 为 MiniMax/vLLM 参数提升（RepetitionPenalty/MaskSensitiveInfo/BotSetting 升 KindIRHandled）+ ProtocolOllamaChat + CumulativeContent + 4 个不同名测试。`ReasoningDelta`、`TestParseOpenAIReasoningContent` 全仓不存在；reasoning_content 能力为窗口前既有。**后续按提交信息 grep 此窗口做审计或 bisect 会被误导**；提交尾部 "2026-09-21 audit" 字样易误认为审计轮产物。纪律回注：push 前自查 commit message 与 diff 对应。

## 一、发现与处置

### P1（本轮必修，均已修）

| # | 状态 | 发现 | 证据 | 修复 |
|---|---|---|---|---|
| R52-F1 | 新增(24h) | **MiniMax 专有字段出向泄漏**：mask_sensitive_info/bot_setting 从 KindDialectOnly 升 KindIRHandled 后，方言守卫三处失效——① serialize_openai 无条件发射（注释声称的 extensions_restore 守卫只遍历 req.Extensions，而这些 key 已被 parse 移出 Extensions，永不生效）；② KnownFieldsForDialect 对所有方言放行（rejects 只查 RejectedBy），strict 白名单模式失守；③ 非 openai 入向 Extensions 路径 Decide 由 Drop 变 Restore。触发：客户端发 bot_setting → 路由非 MiniMax 上游 → 严格上游（additionalProperties:false）400/参数泄漏 | internal/ir/serialize_openai.go:157-176、internal/ir/extensions_restore.go:47、internal/paramreg/decide.go:118-134,209-211 | 三处恢复方言守卫：serialize 发射按 `IRFieldAllowedForDialect(dst)` 门控（新导出助手，DialectUnknown fail-open 与原 knownBy 语义一致）；Decide/KnownFieldsForDialect 对"IRHandled 且声明非空 Dialects"恢复 knownBy 判定；repetition_penalty 摘除 Dialects（KindPortable 时代即全目标还原，列表当时仅 advisory——保留会撕裂序列化/Extensions 两口径）；注释勘误。钉桩：TestSerializeOpenAI_MiniMaxPrivateFields_DialectGated（5 目标正反例）+ TestIRHandledWithDialects_WhitelistAndDecide + repetition_penalty 无条件保持用例 |
| R52-F2 | 新增(24h) | **admin ingest NOT NULL 列绑裸指针 → 23502 连累计费行回滚**：37 列 INSERT 中 NOT NULL 列 stream_chunks_sent（migration 320）绑裸 `$35`（JSON 省略该字段即 nil → 23502 → return → defer tx.Rollback 把同事务已写的 usage_ledger 计费行一并回滚）；可空的 stream_chunk_count 反而套了 COALESCE——错位。F32 让该 INSERT 自 2026-07 以来首次真正执行，雷首次变活。telemetry 包 streamChunksSentArg 早已收口并点名 HTTP path，admin 侧漏同步 | admin/telemetry.go:370-385、404-408；对照 client.go:2734-2747 | slot36 改 `COALESCE($35, 0)`；对齐守卫测试补两条不变量：位置对齐（slot k ↔ 占位符 $k）+ NOT NULL 列必须 COALESCE——负例复跑 FAIL 证实可捕获（R51 同款纪律） |

### P2（本轮应修，8 件全修）

| # | 状态 | 发现 | 证据 | 修复 |
|---|---|---|---|---|
| R52-F3 | R51-F2 修复引入 | defaultBannedRegionsOverlay 无缓存 settings 读且在 selectionMu 临界区内——每次出口选择持锁做 PG 查询，DB 抖动时全部选择串行阻塞 ≤5s/次 | proxy/manager.go:354-365 | 双管齐下：改 CachedPlatformString（≤5s TTL，失败不缓存）+ 调用移出临界区；并接通 ttl_cache.InvalidatePlatformValue 生产接线（此前零调用方）——settingsPut 成功后失效缓存，admin 改键立即生效；适配既有热更新钉桩测试（直改 store 后显式失效） |
| R52-F4 | 新增(24h) | 735 之后 admin updateModel re-enable 空壳成功：把 disabled 行改回 active 撞 uq_models_canonical_active_folded_name → 23505 被 //nolint:errcheck 裸吞，HTTP 200 但行未启用 | admin/models.go:614-616 | updateModel 全分支错误浮出：23505→409（指名折叠冲突索引）、其余→500+日志；createModel 23505 分支注释预言成真 |
| R52-F5 | R51-F13 收口副作用 | active_probe panic 后 running[key] 永久残留：dedup 使该 (credential, model) 探测对静默丢弃直至重启——"崩溃"变"单键探测永锁"（六 worker 中唯一非幂等者） | bg/active_probe_worker.go:259-266、138-144 | processOneRecovered recover 分支删 running[key]；钉桩 TestActiveProbeWorker_PanicReleasesRunningDedupKey |
| R52-F6 | 跨窗口存量（R30 同构） | SerializeGeminiResponse 不读 ir.ReasoningContent：OpenAI 系上游（DeepSeek-R1/GLM）的 message.reasoning_content 只进 IR 字段不产 thinking 块 → Gemini 客户端非流式静默丢推理；流式却发射（不对称） | internal/ir/response.go:1187-1235 对照 :443-444、:556-557 | 补 ReasoningContent→`{"text":…,"thought":true}` 前插分支；守卫：Content 中已有 thinking 块（anthropic 上游）不双发。钉桩 ×2（发射 + 不双发） |
| R52-F7 | 新增(24h) | 客户端识别链两缺陷：① 时序倒置——body-marker 在 EnsureCaptured 内跑，先于 SetKey 的 header 链，AgentName 恒空使 body 结论无条件压过 UA/X-Agent-Name（UA=zcode/2.0 + model=deepseek-* 被记成 deepseek-code）；② model 前缀/通用工具名单信号误归类（SDK 客户端调 deepseek-chat 被记成 deepseek-code；read_file 等是通用编码工具名）；③ 系统提示词裸词 deepseek-coder/deepseek-v3 coding 复发 09-03 教训 | request_log_pipeline.go:581-585、handler.go:1960<2102、body_client_marker.go:74-78、request_metadata.go:75-82 | ① body-marker 移入 refreshMeta（fillAttemptMeta 之后，仅覆盖弱名）+ 结果 memo（识别不出也置位，消除重复全 body 扫描）；② deepseek-code 判定收紧为 metadata.deepseek_session_id 独立强信号 ∨ model×工具集共现，zcode_* 前缀维持独立；③ 裸词锚定自述形态。测试同步重写（正例+单信号负例）；conversion-audit.md 意图基线同步勘误 |
| R52-F8 | 新增(装配) | S4 停写门控在 lite 形态结构性失效：specs 只在 dbConn.Enabled() 分支注册，lite 进程 Global.Spec(key)==nil → GetPlatformBool 恒回落 true——门控开关无法表达（方案文档明写 lite"同门控"；R51 双模式测试手工注册 registry 掩盖了装配缺口） | cmd/gateway/main.go:1339-1347、storage_mode_init.go、settings/helpers.go:83-101 | initStorageMode 内注册 storage 族 specs（settings.Init(nil) 改为支持无 DB backend 仅接 env，env→default 链生效）；只收窄 storage 族不把 Full 侧其余语义带进 lite |
| R52-F9 | R51-F3 覆盖缺口 | CorrectEstimatedUsage 的 UPDATE request_logs_hot 游离在停写门控外（F3 声称"六个包裹点"实为 4 个 if logsWrite 块）——停写期仍可改写存量行 | telemetry/client.go:1836-1869 | 函数入口补 requestLogsWriteEnabled() 门，停写期跳过（宽表冻结语义） |
| R52-F10 | 存量 | X-Agent-Name 客户端可控原样透传未截断：>255 字节 → 22001 整行 INSERT 失败；任意值撑大 agent 维度基数 | telemetry/request_metadata.go:340,359-361 | normalizeAgentName：TrimSpace + 剥控制字符 + 截断 255 rune；刻意不过 clienttype.Normalize 白名单（自定义 agent 名是合法用法，白名单会塌缩成 unknown）——基数治理登记后续专项 |

### P3（本轮小修 8 件 / 登记顺延）

- **已修**：R52-F12 ProtocolOllamaChat/CumulativeContent 零引用 → RESERVED(ollama-chat 接入) 标注；R52-F13 测试死代码三处清理（maskSensitiveInfoPtr/bytesContains→bytes.Contains/var _ = json.Marshal）；R52-F14 bot_setting 畸形输入回退 Extensions（原始字节经 Decide 方言守卫透传，恢复 KindDialectOnly 时代保真）；R52-F15 RegionStatsReport 并入 overlay（与选择路径口径对齐）；R52-F16 default_banned_regions DangerLevel Warning→Dangerous（安全开关权限档对齐）；R52-F17 worker panic 测试补 apihub/autoheal 两例（4/6→6/6，apihub 用例需非 nil hub 方能触达 panic 路径）；R52-F19 ExtractAgentType + sessionmeta agentRole 补 minimax-code/deepseek-code；R52-F20 文档回改（deepseek-code.md 符号名、conversion-audit.md 符号/UA 方案/收紧语义/识别链顺序、body_client_marker 链序注释、promoteSpecs 位次注释、parse_openai 包头注释）。
- **登记顺延**：UA 三副本口径 SSOT 收敛（zcode 既有裸词跨窗口不动）；弱 UA 名单双副本提取；X-Agent-Name 维度基数治理；unknown-location 放行桶运营可见性（D14-#3b）；Phrases 归一化 NFKC/零宽剥离（订阅源受信边界）；F19 promote 时间型饥饿观察（两 gauge）+ promote 循环钉桩测试；F18 残留 session_last_requests 死映射；lite journal 重试重复轮"幽灵轮"（F8 已知残留）+ 换冲突目标后的 PK 硬失败边；R51-F15 reqprobe 免费重试限流计量；BaseWorker 自愈重构；R51-F24/F25/F26/F27；bg 剩余约 50 处裸 go；D17 大文件/长函数清理候选清单（见 runs/agent-D17）。

## 二、核实为健康的关键面

- **R51 修复波主体质量高**（D16 逐提交复核）：F1 时序根修全路径闭合且修复前可 FAIL 的真实 http.Server 测试在位；F2 词组匹配正确无流量绕过口；F32 列值逐一对齐；F6 三处守卫无第四处；F7 与 R50 F14 同构；735 迁移与 dedup SQL 字节级一致、五点同步齐；F11/F19/F20/F18 行为正确。
- reasoning 流式桥全通过（空流门把 reasoning 计语义输出；四协议流式序列化器对齐）；R30 refusal 修复仍完好；paramreg 提升无响应侧回显；JSONB 不变量无新增写点。
- 客户端识别数据闭环存在（agent_name → request_logs_hot + request_context_attrs → admin 过滤链全通）；Prometheus 标签基数白名单有钉。
- ingest 三端点 h.admin 包裹、SQL 全参数化；S4 门控 Full 侧四门+事务级单次读；usage_ledger 在门外为设计意图。
- 735 与分区四不变量无冲突（普通表/UTC 分区键）；733 promote DO UPDATE 冲突目标精确对应不可达残边。
- F21 singleflight 主体正确；F7 pin 置顶不会错误迁移 sticky；窗口无新增绕过 Governor 的出口点。
- docs sweep 无漏项（735/733/count_tokens 均已入册）；窗口 diff 零新增 TODO 标记。

## 三、未覆盖项与原因

- 严格上游对未知顶层参数的 400 行为、生产 catalog 路由面：需真机/真库，按 registry 机制与 spec 注释定性。
- DeepSeek Code 多轮历史 per-message reasoning_content 回传：客户端文档线索，D02 后续。
- web Vue 组件运行时行为（993a41a1b）、ursm 脚本三路径演练、252 共享库 735 实应用：npm/真库/部署动作。
- credential_selfcheck/active_probe 双实例重复探测（进程内 dedup 无分布式锁）：窗口未改动，备注存疑。

## 四、测试与验证

- 三门：`go build ./...` 全绿；`go vet` 12 个改动包全净；`go test -count=1` 全部改动包（ir/paramreg/admin/proxy/telemetry/settings/trace/observability-telemetry/sessionmeta/streaming/bg/cmd-gateway）全绿，其中 bg+cmd/gateway 加 `-race` 全绿。
- 每条 P1/P2 配钉桩回归测试；F2 守卫做了负例自证（还原裸绑定 → 测试 FAIL → 还原修复 → PASS）。
- admin settingsPut 失效接线 + proxy 热更新钉桩适配（直改 store 场景显式 InvalidatePlatformValue）。
- 子代理原文：docs/audit/playbook/runs/R52-2026-09-22/（7 份，各含主代理复核结论）。

## 五、部署与运维状态

- 本轮无新迁移；735 维持 R51 状态（本地已应用注册；252 下次部署窗口应用；154/245 待 binary 追平）。
- R52-F8 落地后：lite 形态的 storage.request_logs_write_enabled 可经 `LLM_GATEWAY_STORAGE_REQUEST_LOGS_WRITE_ENABLED` env（EnvNameAuto 派生）真实关停。
- 运维裁决维持 R51 §四：252 approval pending / probe 占比查询；70/71/72 × glm-4-flash 绑定停用；154/245 binary 追平后 unmapped-cn-rename.sql。

## 六、下一轮（R53）入口

- 修复面优先：R51-F15（reqprobe 免费重试计入 Governor/retry budget）、BaseWorker 自愈重构（recover 后退避重启 + restarts 指标）、727/730 契约测试（726 模板）。
- 观测面：F18 探针归因 origin 列 + parked-since 徽标；count_tokens 观测补齐（审计/参数账本/独立频控）；paramledger Lite mirror=nil 跨实例回显还原。
- 安全面：Phrases NFKC/零宽剥离 + unknown-location 计数；X-Agent-Name 维度基数治理（rollup dim_key 归一）。
- 治理面：UA 三副本 SSOT 收敛；promote 时间型饥饿 gauge 观察 + 钉桩测试；lite journal"幽灵轮"view 侧过滤；D17 清理候选清单（超 800 行文件/超 150 行函数）分批瘦身。
- StorageDecoupling v3：S3 视图切换与 S4 前提观测（mirror outbox 清零、dual_read 7 日零漂移）。
