# 252 PG SQL 日志审计轮·第七轮（2026-09-25）

以第六轮（docs/audit/2026-09-24-252-sql-log-audit.md）§八提示词为起点：①D2' bindings/候选族主轴；②FIX-5/8 部署后验证；③D7'/D9 观测。纪律沿用 ⑪-㉒。

## §〇、取证与起点

- 窗口：2026-09-25 05:48:04 → 06:43:05 CST（55 分钟，1,552 条 PG 日志行；慢日志 529 条/错误 534/FATAL 44）。
- 物证：`252:/tmp/pg252-snapshot-0925-final.log`（77.5MB，终版）+ `/tmp/pg252-audit-0925/`（parse.py/deep.py/explain_after_vacuum.txt）。上轮 0924 快照仍在 `/tmp/pg252-snapshot-0924.log`。
- **日志窗口只有 55 分钟的原因**：ctr.log 于 05:48 被轮转截断（logrotate `podman-pg-252`：daily/rotate3/maxsize100M/copytruncate；归档 .gz 未留存）。运行容器（07-29 建）LogConfig=`json-file map[]` 无上限，早于 pg17-start.sh 的 100m×3 修复——无轮转兜底时会重演 49GB 撑爆磁盘（D6 重建顺带解决）。
- **污染声明（重要）**：窗口内检测到并行 hotzone-dual-mode 会话对 252 生产库跑真库测试（日志 1,792 行含 hot_zone/request_mirror 形状；来源 172.16.2.241；05:48-06:07 的大 payload 测试 INSERT 泛洪 ~30MB）。其自有 bug（`$4 inconsistent types` ×11、statement 形状 `SELECT gw_session_id,is_final_success FROM ONLY request_logs_2026_xx`）**不计入本轮生产发现**。本报告逐形状归因均为网关生产语句，不受影响。
- pss 累计自 2026-09-11（含病态期），本轮结论以日志窗口 + EXPLAIN ANALYZE 为准。

## §一、上轮修复对账（FIX-5/8 部署后首个观测窗口）

09-24 深夜 154（2240/8b94131d）+245（2246/b21c499c）已带 FIX-5/8 上线（git merge-base 祖先验证）；252-dev 仍 2211 过期（D8）。

| 项 | 基线（第六轮 45min） | 本轮（55min） | 结论 |
|---|---|---|---|
| DSM could not map/attach（E1） | 486 | **0** | ✅ FIX-5 需求侧收口成立，未重建容器（/dev/shm 仍 64MB）也已绝迹 |
| parallel worker terminated（E8） | 654 | 6（残量=252-dev 旧二进制 + REFRESH 取消殉葬） | ✅ 主源消除 |
| cancel 风暴（E7） | 3,953 | 510（≈417/45min） | ✅ **-89%**；构成换了主人（见 §三） |
| 漂移核查 30s 击杀 | ×14 | **0**（×36 全部 ≤22.6s 完成） | ✅ FIX-8 pinned conn+180s 生效 |
| REFRESH 击杀 | 1（pin 正常） | **0**（×12 完成 max 46.9s） | ✅ |
| 744 outbox done 索引 | — | idx_scan **8,456**；outbox 669MB/482,814 done 行 | ✅ 生效（DELETE 当前耗时见 §二） |
| 744 digest NULL 索引 ×7 | — | 全部 indisvalid=t，idx_scan 184/臂 | ✅ 生效 |
| FIX-1/2/3/4 | — | 42703/23505 列错误、analyze 击杀、REFRESH 叠跑均 0 复发 | ✅ |

## §二、慢 SQL（>1s，529 条 / 55min，库内总耗时 2,313s）

> 扣除本轮自噪声（2 次 EXPLAIN ANALYZE 自取证 235s）后生产慢查 ≈2,079s。

| # | 形状 | 量/耗时 | 处置 |
|---|---|---|---|
| S1 | **model_alternatives policy 查询**（`WITH policy AS … v_routable … usage_7d FROM request_logs_with_current_month`） | **EXPLAIN ANALYZE 151.1s**（VACUUM 后 83.8s）；×203 次 kill/55min | **FIX-1 本轮根修**，见 §四 |
| S2 | **auto_index_refresher DELETE**（IN(全量 rollup SELECT)） | ×79/163.9s + INSERT ×53/90.2s（≈2.4 次/min=3 实例×5min tick+LISTEN） | **FIX-2 本轮根修**，见 §四 |
| S3 | **provider catalog 候选构建**（provider/client.go:1498，model_offers×v_routable×LATERAL×recent_success_rate） | **×54/55min、178.4s、max 15.4s、mean 3.3s**，且用户取消 ×3——请求路径，带 maxAttempts=3 重试 | 本轮未修，**登记 D10（P1，下轮主轴）** |
| S4 | REFRESH routing_analytics_7d | ×12/326.8s，max 46.9s，零击杀 | D9 续（观测，预算内） |
| S5 | analyze_llm_gateway_table_stats | ×3/193.1s，max 67.4s（pss 14 天均值 231s） | D7' 续（登记未变） |
| S6 | 探针 featured 周期查询（bg/model_probe.go:842，cfl EXISTS+双探针 gate+traffic EXISTS） | ×12/96.4s，max 24.1s（空闲 3.07s 实测） | D2' 主凶**不再击杀**（<30s），残余成本登记 D2''' |
| S7 | mv_data 漂移核查 ×36（两 matview） | 229s，max 22.6s，零击杀 | FIX-8 验证通过 |

## §三、错误 SQL 分类（55min）

| 类 | 量 | 归因 | 处置 |
|---|---|---|---|
| C1 user-request cancel：**policy/alternatives ×203** | 203 | S1 查询 >84s，客户端必超时——**功能在生产不可用**（零候选建议从未返回过） | **FIX-1** |
| C2 user-request cancel：claim `is_final_success` UPDATE ×142 | 142 | 长流式（trace_events JSON 内 20-75s duration）客户端断开→ctx 取消；非 DB 慢 | 登记（storage-merge 已知 claim 路径，第 4 次建议待拍板，不在本轮） |
| C3 user-request cancel 其余：session_bodies ×15、trace_events ×13、api_keys last_used ×6、probe 队列 ×5、catalog ×3 等 | ~47 | 同 C2（客户端断开伴生） | 观察 |
| C4 statement timeout ×6：fingerprint business ×3、session_key ×2、usage_facts MAX ×1 | 6 | 整点 rollup 族贴 30s rolconfig（D2' 续）；fingerprint 读 canonical 视图 7 天窗（同 S1 病，登记 D11） | 登记 |
| C5 `$4 inconsistent types` ×11 + 语法错 ×1 | 12 | **并行 hotzone 会话测试代码**（非生产），已现场告知 | 移交 |
| C6 smm 库 password auth failed ×12 | 12 | 跨库外部（smm 定时任务 05:50/06:00 整点密集失败） | 登记（移交 smm owner） |
| C7 FATAL connection lost ×26 | 26 | C2/C3 伴生（基线 1,917/45min → 大幅回落） | 观察 |
| C8 parallel worker terminated ×6 | 6 | 252-dev 旧二进制 + REFRESH 取消 | 随 D8 消除 |

## §四、本轮修复

| 项 | 改动 | 验证 |
|---|---|---|
| **FIX-1** | `domains/streaming/model_alternatives.go`：usage_7d CTE 由 `request_logs_with_current_month`（canonical 视图=session_turns 臂+request_logs 臂+多重反连接去重，EXPLAIN ANALYZE 实测 151.1s/VACUUM 后 83.8s；session_turns 2026_09 臂单臂读 47 万行+12,491 次逐行堆探测）改为 `request_logs_hot`（直扫 idx_request_logs_hot_canonical_model_ts）。语义取舍：popular 档热度信号从"7 天"收敛为 hot 保留窗（~8h），对 tier-3 排序信号可接受；与探针查询读 hot 的既有先例一致。 | 252 实测 **96ms**（1500×）；本机 EXPLAIN cost 16.9k（视图形态 318k 的 1/19）。SQL-shape 钉：`TestAlternativesSQL_PopularityReadsHotNotCanonicalView` |
| **FIX-2** | `bg/auto_index_refresher.go`：DELETE 由 `IN(全量 rollup SELECT)` 改 `WHERE bucket = $1`——两个半区产出行 bucket 恒等 $1，删除集合等价且额外清走 fresh 集不再含的 stale 行；并发性质不变（INSERT ON CONFLICT 幂等兜底）。原形态=数据面最重查询每 tick 双跑×3 实例（79 次/55min、mean 2.1s）。 | 252 EXPLAIN：idx_credential_model_index_hot_unique bitmap，cost 127。SQL-shape 钉：`TestRollupSQL_DeleteDoesNotRerunRollup` |

**运维动作（非代码）**：VACUUM (ANALYZE) `request_logs_2026_09`、`candidate_failure_logs_2026_09`(+hot)——前者把 S1 151s→84s（探针臂堆取数→部分索引化），后者建库以来从未 vacuum。未固化为迁移：对残余视图消费者（D11）收益未单独 EXPLAIN 证实，按纪律⑳不盲投。

本轮**无新迁移**（两项均为查询形态修复，无 schema 变更）。

## §五、测试

- `go test ./domains/streaming/ -run TestAlternativesSQL -count=1` → ok（含新钉）
- `go test ./bg/ -run TestRollupSQL -count=1` → ok（含新钉）
- `go build ./... && go vet ./bg/ ./domains/streaming/` → 净
- `go test ./bg/ ./domains/streaming/ -count=1` → ok（24.8s / 88.3s 全量）
- 真库：252（postgres 超管只读+VACUUM）与本机 llm_gateway 双实例实跑 §四 验证列。
- 明确未覆盖：FIX-1/2 属网关代码，需随下一部署窗口上三台；252 生产效果回验（policy 取消归零、index_hot DELETE 耗时）留部署验证轮。

## §六、自审计（同日批判式复核）

| # | 初判 | 复核结果 |
|---|---|---|
| A1 | 曾把 D2' 判为"探针主查询仍慢，需重写"——中期解析把 `cmb…` 前缀慢查归到探针 | 逐条提取全文后修正：×12 慢查中 24.1s 形状是 bg/model_probe.go:842 featured 变体（无 mps join），探针主查询（:653）当前窗口零慢记录且 EXPLAIN cost 348。D2' 主凶已随 FIX-5/744 生态回落，残余登记 D2'''；**教训：同前缀不同语句，归因必须全文提取** |
| A2 | 曾计划 usage_7d 改读 stats_usage_daily | 真库核验否决：canonical_id 全为 0、键是 raw_model_name≠canonical 词汇、当天数据缺失（max day_utc=09-24）——三处语义断裂，弃 |
| A3 | VACUUM 曾被视为可能的不改码根修 | 复测 151s→84s：探针臂收益真实但臂扫成本仍在，且 VM 随插入衰减（autovacuum insert 阈值 0.2×1M 行）。**定性为缓解非根修**，转代码修复；分区 autovacuum 调参收益未证实前不投迁移 |
| A4 | 中期曾把 ×59/×203 policy kill 当作新发现的"路由策略查询"问题 | 全文提取后确认即 model_alternatives 零候选建议查询（chat 失败路径），并非独立新面——避免重复登记 |
| A5 | `$4 inconsistent types` 差点按 E3（pocket 旧案）并案 | 伴随 STATEMENT 是 `FROM ONLY request_logs_2026_xx` 的未知形状，pg_stat_activity 溯源到 241 的 hotzone 测试连接 + 日志 1,792 行 hot_zone 形状——**并行会话测试代码，移出生产发现**。教训：错误并案前先核语句指纹+连接来源 |
| A6 | 自取证污染：本轮自己的 EXPLAIN ANALYZE（235s）和 VACUUM 打进同一窗口 | 慢查总量已在 §二 显式扣除；pss 未重置（历史累计），结论均以窗口+EXPLAIN 为准 |

## §七、登记（本轮不修）

- **D10（P1 新，下轮主轴）**：provider catalog 候选构建（provider/client.go:1498）×54/55min、mean 3.3s、max 15.4s、取消 ×3——请求路径且 maxAttempts=3 放大；巨型 JOIN（v_routable OR-join + 3 capability LATERAL + pricing LATERAL + recent_success_rate CROSS LATERAL 逐候选 50 行下探）。修前先真库 EXPLAIN（纪律⑳）+ 候选缓存命中率取证。
- **D11（P2 新）**：fingerprint business 查询读 canonical 视图 7 天窗 30s 击杀 ×3/55min——同 S1 病但因语义需全窗（去重不可裁窗），候选路径=分区自动 vacuum 调参（insert_scale_factor 收紧保持 VM 新鲜）或视图臂精简；EXPLAIN 先行。
- **D12（P2 新）**：claim `is_final_success` UPDATE 取消 ×142/55min——storage-merge 已知"claim 路径绕过 hooks 静默跳过镜像"（第 4 次建议先修 claim 路径）在日志侧的放大显形，S4 停写前必须处理。
- D2'''（P2）探针 featured 变体 max 24.1s（空闲 3s），不再击杀；node_probe_runs gate 读全史（completed_at 不在索引）是主要残余。
- D6/D7'/D8/D9 顺延：容器重建（连带 LogConfig 100m×3 与 auto.conf 持久化）、analyze 572s/call、252-dev 过期 2211、REFRESH med 20-47s。
- 跨库：smm 库密码认证失败 ×12（移交）；hotzone 会话测试 bug `$4`（移交）。
- 观察项：×203 零候选 chat 请求/55min 本身是路由面信号（为何这么多请求零候选），建议路由轨道关注。

## §八、下一轮提示词（建议）

> 以本文 §七 D10/D11/D12 为起点。优先级：
> 1) D10：catalog 候选构建 EXPLAIN ANALYZE + v_routable OR-join 拆改评估 + 候选缓存面取证（×54/55min 中缓存未命中占比）；
> 2) 部署窗口带 FIX-1/2 上三台后回验：policy 取消归零（基线 203/55min）、index_hot DELETE 耗时（基线 mean 2.1s）、cancel 总量（基线 510/55min）；
> 3) D12：拍板 claim 路径修复（UPDATE 置位补 outbox 登记），与 storage-merge 观察期联动；
> 4) D11：fingerprint 查询 EXPLAIN 后再定调参或视图改造。
> 纪律沿用 ⑪-㉒ + 本轮：㉓ 同前缀语句归因必须全文提取（A1/A4）；㉔ 错误并案前核语句指纹+连接来源（A5）；㉕ 自取证动作计入窗口并在报告显式扣除（A6）。

## §九、handoff 更新

- 记忆库：llm-gateway-go-audit-cycle-progress 追加本轮；pg-252-sql-log-audit-facts 增补（canonical 视图三臂结构+151s 实测、FIX-5/8 部署后三项基线、stats_usage_daily 三处语义断裂、ctr.log 轮转真相、hotzone 并行测试污染）。
- 原始物证：252:/tmp/pg252-snapshot-0925-final.log + /tmp/pg252-audit-0925/。
