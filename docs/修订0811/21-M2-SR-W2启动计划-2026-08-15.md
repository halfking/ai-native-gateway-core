# M2 SR-W2 启动计划（连接内首内容前恢复）— 2026-08-15

基线：main @ `ce39082a5`（M1 四轨 + SC-3/SC-5 已合入，M1 审计见 doc 20）
设计依据：doc 18 §5（总体架构）/§6（分层启用）/§8（错误分类与重试）/§9（客户端协议）/§10.3（ResumeBlocked）/§17 Phase 1；doc 19 §4 M2 里程碑

## 1. 门禁状态核验（doc 18 §17 Phase 1 开启条件）

| 门禁 | 状态 | 证据 |
|---|---|---|
| CO-4 dispatch race 修复 | ✅ M0 交付 | pipeline_shutdown_test.go 三条回归；M1 审计 `-race` 全绿复核 |
| 协议测试（commit gate/classifier/outcome） | ✅ W1 交付 | doc 20 A 轨审计逐条核实；26-kind 穷举矩阵 + 双桥逐字节 E2E |
| survival/streamretry 互斥 | ✅ W0 交付 | main.go 启动校验 survival 胜出 |

**Phase 1 可以启动。** durable（Phase 2）门禁未齐（EncryptWithAAD、PG 回源），本轮不做。

## 2. 已就绪资产（W0/W1 遗产，坐标为 ce39082a5 实测）

| 资产 | 位置 | W2 用法 |
|---|---|---|
| AttemptCommitGate（none/metadata/content/tool_call/terminal 单调 + Buffered 模式） | domains/streaming/attempt_commit_gate.go | 换 `GateModeBuffered` 即得"首内容前可丢弃"语义 |
| GateWriter + ctx 桥 | domains/streaming/gate_writer.go、attempt_gate_context.go | Coordinator 用 `attempt_gate_context.SetAttemptGate` 携带 gate；已预留未接线 |
| wrapAttemptWriter 接线开关 | attempt_gate_wiring.go:37（`LLM_GATEWAY_ATTEMPT_COMMIT_GATE` 默认关）、:63 | survival 开启时强制 gate-on + Buffered；flag-off 恒等 |
| AggregateTaskOutcome（26 kind + ResumeBlocked） | attempt_outcome.go | 即 TaskOutcomeAggregator（SR-06 组件之一，W1 已交付核心） |
| SerializedStreamWriter | serialized_stream_writer.go（唯一构造点 attempt_gate_wiring.go:70） | W2 需扩为 keepalive 统一 renderer 入口 |
| RequestSurvival 配置 + 租户 allowlist | config（RequestSurvival* 全套，默认全关，NormalizeRequestSurvival，InteractiveDeadline 1800s/Retry 2-300s） | SR-07 大部分已在 W0 完成 |
| RetryOwner 冻结原语 | internal/retryowner | ExecuteAttempt 的 ownership 切分基础 |

## 3. 工作项分解（按 doc 18 §18 SR-05/06/07 收口）

### SR-05 ExecuteAttempt 固定语义入口
- 定义 `ExecuteAttempt(ctx, attemptCtx)` 单 attempt 执行契约：**关闭 Handler Goal 外循环、旧 async fallback、跨 attempt 递归**（doc 18 §17 Phase 0 定义）。
- 落点：`executors/executor.go:1901 Execute` 的候选循环（:2659 附近）之上包一层 attempt 级入口；ownership 按 retryowner 区分 survival owner（单次透传，W0 已冻结）与非 survival（沿用旧路径）。
- 验收：无 Goal/async/recursive 嵌套重试；attempt context 真正生效。

### SR-06 SurvivalCoordinator 连接内循环
- 状态机：doc 18 §5.1/§7（running→waiting→retry→终态，30min InteractiveDeadline）。
- 核心循环：ExecuteAttempt → AggregateTaskOutcome → TaskAction 分派（retry-now 立即换候选 / wait-recovery 刷新候选池再试 / terminal 渲染终止 / ResumeBlocked 透传已提交内容）。
- **恢复语义**：可恢复耗尽在 commit_state<content 时不写终止帧 → Discard attempt → 刷新候选（复用 autoroute 候选重取路径）→ 重试；commit_state≥content 一律 ResumeBlocked 不再重试（doc 18 §10.3，W1 outcome 已实现该分类）。
- keepalive：等待/重试间隙经 SerializedStreamWriter 发注释帧（严格客户端只收注释，doc 18 §9.1）。
- 7 个桥接线点（stream.go:439、responses_stream.go:37、responses_bridge.go:296/557、anthropic_stream.go:85、anthropic_bridge.go:134/322）已全部有 MayWriteTerminal 门控（M1 审计确认超前接入），W2 只需在 Coordinator 侧持有 gate 所有权。

### SR-07 配置收口
- survival flag + 租户 allowlist 已有（W0）；补：`RequestSurvivalEnabled` 开启时强制 commit-gate on + Buffered 模式的联动校验（两开关不一致时启动告警，参照 survival/streamretry 互斥实现）。

### W2 顺手修复（doc 20 行动项"随 W2 处理"）
- A-P2-2/A-P2-3：buffer 统一上限——Error 帧与 keepalive 入队也走 64KiB/30s 双上限（flag-on buffered 才暴露，正好在本轮 Buffered 模式启用前修掉）。
- A-P2-5：GateWriter.Finish() 接入 Coordinator 收尾路径。
- A-P2-6：KeepaliveSender（keepalive_sender.go）与 preStreamKeepalive（handler.go:225）并入 SerializedStreamWriter 统一 renderer。
- A-P2-1 余项：fa0baf822 四个细化点直接单测（server_tool_use 归 ToolCall / tool_calls null/[] droppable / CRLF 边界 / 注释+data 混合帧）。

## 4. 测试策略

1. **flag-off 字节等价红线**：现有 6 桥 flag-off vs immediate 逐字节测试（wiring_test.go:126）必须保持全绿；新增 survival 路径不得触碰 flag-off 分支。
2. Coordinator 表驱动：429/5xx 恢复成功、无候选 wait-recovery 后候选回流、commit≥content ResumeBlocked、30min deadline 终止、Discard 后重试不泄漏已写字节。
3. `-race` 全包；S30-S35 故障注入按计划归 M4。
4. 上线：feature flag + tenant allowlist，默认关（全域验收 §5.7）。

## 5. 不做（本轮明确出界）

- Phase 2 durable（门禁未齐：EncryptWithAAD / PG 回源）；MM-2/RT-2/CO-5 为 M2 其他轨，另行分轨；多智能体协同归 ACC。

## 6. 执行记录（2026-08-15，feat/m2-sr-w2）✅

| 工作项 | 交付 | 验证 |
|---|---|---|
| A-P2-2/3 buffer 统一上限 | attempt_commit_gate 抽出 appendBufferedLocked：Error 帧与顺序排队 keepalive 一律走 byte/age 双上限且启动 age 时钟 | 表驱动 2 用例 + streaming 全包 -race 绿 |
| A-P2-1/4 分类器 | 注释+data/event 混合帧回落协议分类（不再整体判 Keepalive）；补 server_tool_use/web_search_tool_result/tool_calls null[]/CRLF 直接单测 17 例 | 混合帧 RED→GREEN + 全部新用例绿 |
| SR-05 ExecuteAttempt | ExecParams.SurvivalAttempt 抑制 executor 五处内部重试出口（probe hold/单节点 5xx 递归/sync retry/model fallback/async fallback）；ExecuteAttempt 适配器（强制 SurvivalAttempt + ExecuteError→CandidateOutcome 折叠 + 无候选合成 no_available_channel + SafeRetry 由 gate 判定 + ExecResult 传播） | executors/streaming -race 绿 + shouldAsyncFallback 直测 |
| SR-06 SurvivalCoordinator | survival_coordinator.go：每 attempt 新建 Buffered gate + GateWriter（共享 SerializedStreamWriter）；决策分派 Succeed/RetryNow（Discard+刷新+退避）/WaitRecovery（keepalive+退避+刷新）/ResumeBlocked/FailTerminal/FailClosed（Terminal seam 渲染，committed 区分完好收尾）；deadline/取消/退避 2s→300s 倍增封顶；已提交 attempt 的 GateWriter.Finish 收尾（A-P2-5 兑现） | 8 用例全绿（成功/恢复/退避 keepalive/终态/ResumeBlocked/deadline/取消/退避序列） |
| SR-07 生产接线 | handler.go：goal loop ExecParams 构造抽为 buildExecParams（两路共用）；survival 分支（isStream+租户门控）goto 跳过 loop，含 responseInterceptor 会话捕获保留（fix-review 修复）；survival_wiring.go：runSurvivalCoordinator（FreezeOwner(Survival)/入口停 preStream 单一写者/Refresh 经 resolveCandidatesForRequest/三协议终态 SSE）+ SetRequestSurvival；wrapAttemptWriter 复用已包裹 GateWriter（UnderlyingAttemptGate，防双重 gate）；main.go cfg.RequestSurvivalEnabled 装配（默认关） | flag-off 字节等价红线全绿 + survival_wiring 单测 + 全包 -race 绿 |

SR-W2 遗留说明：
- KeepaliveSender（keepalive_sender.go）生产者并入串行写通道未做——survival 路径已通过"入口停 preStream + coordinator keepalive 走 sw"实现单一写者；非 survival 路径有意不动（A-P2-6 剩余部分归 W2 后续/W3）。
- survival 指标事件接线（SR-13：coordinator 决策/attempt 维度 Prometheus）未做，metrics 骨架已就绪。
- coordinator 级 e2e（真 executor + 真上游 429→恢复流）依赖部署环境，归 M4 S30-S35 故障注入统一验证。
