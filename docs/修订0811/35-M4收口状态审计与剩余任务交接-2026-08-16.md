# 35 · M4 收口状态审计 + 剩余任务交接（2026-08-16）

- 日期：2026-08-16
- 基线：main @ `f4cd33976`（本会话末态之前，无本会话代码改动——代码已于 `2a87563ba`/`5a765f728` 推送完成）
- 性质：**状态落成 + 任务审计 + 多子代理并行交接**

## 1. 当前状态（已落盘，无需再提交）

| 项 | 状态 | 证据 |
|---|---|---|
| M4 代码修复（pipeline 并行取消测试确定性化） | ✅ 已推送 | `2a87563ba` @ origin/main |
| doc 34（M4 统一验证报告） | ✅ 已推送 | `2a87563ba` @ origin/main |
| doc 18（M4 收口验收闭环段撤回，尊重 w3c 意图） | ✅ 已推送 | `5a765f728` @ origin/main |
| 工作树 | ✅ 干净（仅 w3c 在动的 `web/public/menu-config.json`，非本会话域，不碰） | `git status` |
| /tmp 敏感残留 | ✅ 已 shred（`/tmp/m4smoke/*`、`/tmp/m4smoke-bin/psql` 含密）；bootstrap 驱动脚本已删 | 本审计核查 |
| worktree | ⏸ 收敛：3 个保留项均合理（`distribution-b-c`=ded2aefef 未进 main；`llm-gateway-go-w3c`=w3c 在用；`realtime-routing-self-heal`=含未提交修改保守保留） | `git worktree list` |

**提交/推送结论**：本会话无新代码改动需提交；前序 M4 会话的所有改动（`2a87563ba` 流水线 + `5a765f728` doc 对齐）均已 `git push origin main` 成功，全部在 `origin/main`。

## 2. 任务审计（M4 收口执行质量）

### 2.1 红线遵守 ✅
- **未触碰 w3c 文件域**：domains/streaming、durable、web 全包零改动（M4 修复只在 `domains/pipeline/pipeline_test.go`）。
- **冒烟完全隔离**：一次性库 `llm_gateway_m4smoke`（pg_dump 自规范库恢复，会话末 DROP）+ 独立端口 8783 + 独立 pid/log/env（0600）+ 随机凭据。w3c 的 `llmgw-dv2` 容器（8781）、规范库 `llm_gateway`、8781 端口零干扰。
- **凭据零落盘**：含密临时文件 `shred -u`/session owner 删除，无明文 key 进 git 或上下文。
- **并行会话冲突正确处理**：w3c 在 `2a8fec983` 主动删除 doc 18 的 M4 闭环段（未在他处重建）+ 推 build bump。merge 时我的领先 commit 默认保留该段，我判断 w3c 意图是不想要 + doc 34 已是权威报告，**主动撤回该段**让 doc 18 与 origin/main 一致，避免两会话对同一设计文档反复拉扯。

### 2.2 发现与决策质量
- **252 退役考据（核心新事实）**：SSH 实勘证明 252 直连部署 2026-07-10 19:14 退役切静态，dev 同日迁 kaixuan-1。交接文档的"注册 `LLM_GATEWAY_ADMIN_API_KEY_252`"前提不存在（key 无源），任务正确作废而非伪造占位。
- **deploy-to-252.sh 静态 key 过时**：`/api/admin/handoff/*` 挂 JWT AdminMiddleware（admin/auth.go:93），静态 admin key 只覆盖 /metrics。旧脚本两处 SKIP 目标 API 对当前代码本就过不了——这是真实脚本契约缺陷，已记录待修。
- **pipeline 竞态根因准确**：定位为 `executeParallel` 早退守卫（pipeline.go:222，Phase 0 原生）与测试缺启动同步的调度竞态，修法为测试确定性化（不动实现语义），`-race -count=3` 绿。非掩盖，是钉死真实契约。

### 2.3 风险与待裁决
| 风险 | 影响 | 状态 |
|---|---|---|
| 252 edge nginx `llmgo.itestu.cn.conf → :8780` 死配置（502） | 外部访问 502 | 待清理（§3-R2） |
| llm.itestu.cn（11008→kaixuan-1）当前 502 | dev 冒烟链路不通 | 需先确认 kaixuan-1 网关进程 + npc 隧道（§3-R3） |
| run-migrations-strict.sh 与现代 psql 不兼容 | 空库 bootstrap 在 psql 17/18 均坏 | 待修（§3-D1） |
| seed 契约错配（硬编码 `admin` vs `LLM_GATEWAY_ADMIN_USER`） | local verify 必 401 | 待修（§3-D2） |
| verify.sh metrics 步骤 SIGPIPE 假阴性 | 部署门禁误报 | 待修（§3-D3） |

### 2.4 审计结论
M4 收口执行合规、证据链完整（doc 32 场景 + doc 34 全域回归 + doc 33 稳定性）、无红线违规、无凭据泄露、无并行会话冲突残留。**M0-M4 全部里程碑宣告完成**。

## 3. 剩余任务清单（供新会话多子代理并行）

### D 系列：部署脚本契约修复（非阻塞，但阻塞未来 245/154 晋级 & 本地冒烟可复现）
- **D1**：`scripts/run-migrations-strict.sh` 的 `psql -c "... :'var' ..."` 在现代 psql(17/18) `-c` 路径不插值 → 改用 stdin/heredoc 或 `-f`。位置：`scripts/run-migrations-strict.sh:100-102,114`。
- **D2**：空库 bootstrap 缺守卫——`sql/migrations/startup/002_work_types.sql:38` 的 `ALTER TABLE request_logs` 无表存在性守卫 → 加 `IF EXISTS` 或前置建表。
- **D3**：`scripts/local-deploy.sh:84` metrics 步骤 `curl ... | grep -q` 在 pipefail 下 SIGPIPE 假阴性 → 落盘后 grep 或 `grep -m1`。
- **D4**：`internal/opsreporter`/`cmd/gateway` seed 硬编码 `admin` 与 `local-deploy.sh` 约定 `LLM_GATEWAY_ADMIN_USER` 错配 → 统一为 env 驱动或文档化。

### R 系列：退役/遗留环境清理与链路确认（需网络/授权）
- **R1**：worktree 清理收尾——`realtime-routing-self-heal`（含未提交修改，先 `git stash` 或人工复核后再删）、`distribution-b-c`（ded2aefef 未进 main，确认是否弃用）。
- **R2**：252 edge nginx 死配置清理（`llmgo.itestu.cn.conf` → `:8780` 指向已退役服务）。
- **R3**：kaixuan-1 dev 链路确认（llm.itestu.cn 502 → 查 kaixuan-1 网关进程 + npc 隧道状态）——前置条件满足后补 dev 冒烟（doc 34 §4 JWT 路径已验证可复用）。

### E 系列：晋级部署（需明确授权，按 deploy-test skill 晋级纪律）
- **E1**：245 预生产门禁（deploy-test skill `245` stage gate）。
- **E2**：154 生产晋级（`--promote-154` + `--record-dir` 指向 245 gate 结果）。

### 依赖关系
```
D1/D2/D3/D4（部署脚本修复，独立可并行）
        │
        ├─→ R3（kaixuan-1 链路确认，需家庭网络）
        │        └─→ dev 冒烟（复刻 doc 34 §4 JWT 路径）
        │
        └─→ E1（245 gate，需 D1-D4 通过 + 授权）
                 └─→ E2（154 晋级，需 E1 gate + record-dir + 授权）
R1/R2（环境清理，与上面无依赖，可随时并行）
```

## 4. 多子代理并行执行提示词（复制到新会话）

> 以下提示词已按依赖关系分桶，可一次性粘贴到新会话，由主会话用多 Agent 并行派发。
> 每个子代理独立、只读其文件域、不触碰 w3c 线文件（domains/streaming、durable、web）。

### 桶 A：部署脚本契约修复（4 个独立子代理，可全并行）

```
子代理 A1 — 修复 run-migrations-strict.sh 的 psql 变量插值
仓库：/Users/xutaohuang/workspace/ai-native-tools/llm-gateway/llm-gateway-go
任务：scripts/run-migrations-strict.sh 在 L100-102、L114 用 `psql -c "... :'var' ..."` 做变量插值，
但在现代 psql（17.10/18.4）的 -c 路径不做插值（stdin/-f 路径正常）。请将变量插值改为
stdin heredoc（`printf '...' | psql -f -` 或 `psql <<SQL`）或 -f 传参，保持逐文件校验循环语义不变。
验证：在一个空临时库上 `DATABASE_URL=... ./scripts/run-migrations-strict.sh --bootstrap` 能跑完无语法错误。
红线：不改迁移 SQL 内容、不碰其他脚本、不提交（仅留改动待审）。
```

```
子代理 A2 — 修复空库 bootstrap 的 request_logs 守卫
仓库：同上
任务：sql/migrations/startup/002_work_types.sql:38 的 `ALTER TABLE request_logs ADD COLUMN IF NOT EXISTS work_type TEXT;`
在真空库 bootstrap 时 request_logs 尚不存在会报错（其建表在 sql/quick-init-request-logs.sql 手工路径，
不在迁移链内）。请改为 `DO $$ BEGIN IF EXISTS (SELECT 1 FROM pg_tables WHERE tablename='request_logs') THEN
ALTER TABLE request_logs ADD COLUMN IF NOT EXISTS work_type TEXT; END IF; END $$;`
验证：真空库 `./scripts/run-migrations-strict.sh --bootstrap`（修复 A1 后）通过。
红线：只改这一处、不提交。
```

```
子代理 A3 — 修复 local-deploy.sh metrics 步骤 SIGPIPE 假阴性
仓库：同上
任务：scripts/local-deploy.sh:84 `curl ... /metrics | grep -q '^# TYPE'` 在 set -o pipefail 下因 grep 提前
退出触发 SIGPIPE（curl exit 23），导致 verify 门禁误报失败。改为先把响应落盘再 grep：
`curl -fsS ... "$BASE_URL/metrics" -o /tmp/verify_metrics.txt && grep -q '^# TYPE' /tmp/verify_metrics.txt`。
验证：本地起一个网关实例跑 `bash scripts/local-deploy.sh verify` 全门禁通过（含 metrics）。
红线：只改这一行、不提交。
```

```
子代理 A4 — 统一 admin seed 用户名为 env 驱动
仓库：同上
任务：internal/opsreporter/reporter.go 与 cmd/gateway/main.go 的 EnsureSeedAdmin 硬编码建 `admin` 用户，
而 scripts/local-deploy.sh verify 约定用 `LLM_GATEWAY_ADMIN_USER` 登录——错配时 verify 必 401。
请将 seed 用户名改为读 `LLM_GATEWAY_ADMIN_USER`（缺省回退 `admin`），使 local verify 默认可过。
验证：空 users 表启动后，用 `LLM_GATEWAY_ADMIN_USER` 的值能登录。
红线：不改密码哈希逻辑、不提交。
```

### 桶 B：环境清理与链路确认（独立，可与桶 A 并行）

```
子代理 B1 — worktree 收敛收尾
仓库：同上
任务：1) `git worktree list` 找到含未提交修改的 `2026-07-25-realtime-routing-self-heal`，先 `git -C <path> stash`
（或人工确认修改无用）后 `git worktree remove --force` + `git branch -D`；2) 确认 `distribution-b-c`
（ded2aefef 未进 main）是否弃用，是则 `git worktree remove` + 删分支。绝不碰 `llm-gateway-go-w3c`。
验证：`git worktree list` 只剩主树 + w3c worktree。
红线：w3c 的 worktree 与 feat/m3-sr-w3c 分支绝不删、绝不改。
```

```
子代理 B2 — 252 edge nginx 死配置清理（需 SSH 252 授权）
任务：SSH root@115.29.212.252:25022，确认 llmgo.itestu.cn.conf 的 `proxy_pass http://127.0.0.1:8780`
指向已退役服务（502）。若确认 8780 永久退役，将该 conf 改名 .bak 或删掉，reload nginx。
先读 docs/handoff-20260710-ops-platform.md 确认退役意图再动。
红线：不碰 llm.itestu.cn.conf（11008→kaixuan-1 隧道，仍可能恢复）。
```

```
子代理 B3 — kaixuan-1 dev 链路确认（需家庭网络）
任务：确认 llm.itestu.cn（252:11008→kaixuan-1:8080）当前 502 的原因：kaixuan-1 上 llm-gateway 进程是否存活
（launchd plist / ps）、npc 隧道 task 100 是否在线。修复后按 doc 34 §4 的 JWT 路径复刻 dev 冒烟
（GET /api/admin/handoff/logs、/api/admin/modules/handoff）。
红线：只读诊断 + 重启服务，不改动网关代码。
```

### 桶 C：晋级部署（串行，需明确授权，最后执行）

```
子代理 C1 — 245 预生产门禁
前提：桶 A（D1-D4）全通过 + 明确授权
任务：按 ~/.agents/skills/llm-gateway-deploy-test 的 245 stage 执行门禁（check_contract + endpoint_check）。
验证：245 环境 /healthz + 两个 admin API（JWT 路径）通过，生成 gate.json。
红线：只读 gate 结果，不自动晋级 154。
```

```
子代理 C2 — 154 生产晋级
前提：C1 gate 通过 + record-dir 指向 245 gate 结果 + 明确授权
任务：`--promote-154 --record-dir <C1 gate 目录>`，按 skill 晋级纪律执行。
红线：无 gate / 无 record-dir / 无授权则中止。
```

### 并行编排建议
- **第一轮并行**：A1/A2/A3/A4 + B1/B2/B3（7 个子代理，无相互依赖）
- **第二轮（A 全绿后）**：C1（245 gate）
- **第三轮（C1 通过后）**：C2（154 晋级）
- B 桶结果（尤其 B3 kaixuan-1 链路）决定 dev 冒烟能否补做，不阻塞 C 桶。

---

**本会话执行**：ZCode M4 收口审计 + 交接（2026-08-16）
**下一步**：新会话按 §4 桶 A/B 并行派发子代理；C 桶晋级需单独授权
