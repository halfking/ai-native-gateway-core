# M2 SR-W2 96 小时审计与收尾修正报告 — 2026-08-15

基线：main @ `d2da09ed8`（含 feat/m2-sr-w2 全部交付）；本轮实施分支 `fix/sr-w2-prestream-serialization`。
审计范围：2026-08-11 ~ 2026-08-15 合入 main 的全部修正（96 小时窗口）。
前置文档：doc 18（设计）、doc 19（实施方案）、doc 20（M1 审计）、doc 21（SR-W2 启动计划 + 执行记录）。

## 1. 96 小时修正盘点

| 主线 | 代表提交 | 内容 | 状态 |
|---|---|---|---|
| M1 四轨（SR-W1/MM-1/CO-2/ROUTE） | `08e7beec0` `fc9bcc5fa` `e5d8cdeb9` `46e457fb9` `55824980c` `ce39082a5` | attempt commit gate + 三协议帧分类器 + 7 桥接线；附件存储/出站 URL 化；ActualTokens 回填 + sweep 纳入 `corrected`；MinStandardIQ 硬门禁 + 热门加权 | 已合入；doc 20 审计无 P0/P1 |
| M2 SR-W2 主体 | `13144f838` `de6027f16` `fec69e47b` `183c95d55` `1367812d3` `35c35fef2` `d2da09ed8` | ExecuteAttempt 固定语义入口（抑制 executor 五处内部重试出口）、SurvivalCoordinator 连接内恢复循环、survival 生产接线（默认关）、buffer 统一双上限 + 分类器混合帧 fail-closed + 细化点单测 | 已合入；doc 21 §6 执行记录 |
| SC 会话缓存轨 | `cc897063b` | SC-3 强制压缩 header 触发 + SC-5 patterns.yaml 配置源统一 | 已合入 |
| V3.2 会话优化 | `ff3d64499` `3d69760a0` 等 | LP3/LP5/LP6、schema 统一（迁移 511/513）、245 部署 | 已合入，集成审计见 docs/会话优化v3/ |
| dispatch/流稳定性 | `2a8fec983` 及后续 | CO-4 dispatch race 修复（pipeline_shutdown_test 三条回归） | 已合入，`-race` 复核绿 |

旧文档校正：`EXECUTION_SUMMARY_20260813_FINAL.md` 所列 dispatch T3/T4/T7/T8/T9"待实现"已被 `a74b7c9a4`/`caa066808`/`7f17daa0e` 补齐（9-stage Prometheus 指标、waterfall ring/API 及对应测试均存在），不可再当缺口引用；`domains/dispatch/forwarder.go:174` 的 T8 时间戳仍为"近似首字节"语义，属已记录的已知近似而非缺陷。

## 2. 独立审计与并行交付对账

本轮独立审计（基于 `48b460134`）与并行会话（feat/m2-sr-w2，`35c35fef2`）**独立发现了同一组缺陷且结论一致**，证明 doc 20 P2 清单的有效性：

| 项 | 远端已交付（35c35fef2 / fec69e47b） | 本轮剩余增量（本报告交付） |
|---|---|---|
| A-P2-2/3 buffer 统一双上限 | ✅ appendBufferedLocked（与独立审计方案一致） | 无（不重复交付） |
| A-P2-1/4 分类器混合帧 + 细化点单测 | ✅ 17 例 | 无 |
| A-P2-5 GateWriter.Finish | ✅ coordinator 已提交路径调用 Finish | **契约硬化**：Finish 原实现直写 `gate.writer` 绕过分类/discard/串行通道，未提交 attempt 的尾部字节行为未定义。新增 `AttemptCommitGate.FinishAttempt` 并让 Finish 路由至此：immediate/committed 逐字透传（字节等价）；buffered 未提交入 attempt buffer（Commit 冲刷、Discard 丢弃）；discarded 拒绝（`ErrAttemptDiscarded`） |
| A-P2-6 keepalive 并入串行通道 | ✅ survival 路径（入口停 preStream + coordinator keepalive 走 sw） | **非 survival 路径闭环**（doc 21 遗留说明中"归 W2 后续/W3"的部分）：新增 `serializedResponseWriter`（header/状态委托原 writer，正文走 `SerializedStreamWriter`）；`startPreStreamKeepalive` 安装该适配层，handler 将局部 `w` 重绑定为 `psk.Writer()`，goal-retry 与 survival 两条后续路径的桥接/拦截器/预热错误信封全部经同一串行通道，keepalive 注释与流帧不再可能交错 |

KeepaliveSender（`keepalive_sender.go`）生产零调用点，经 `serializedResponseWriter` 包装即可复用同一通道，无需改动其实现。

## 3. 行为影响面与红线验证

- **flag-off 红线**：`LLM_GATEWAY_ATTEMPT_COMMIT_GATE` 默认 false，`wrapAttemptWriter` 恒等；survival 默认关，handler 分支不进入。6 桥 flag-off vs immediate 逐字节测试全绿。
- **immediate 模式**：`FinishAttempt` 对 immediate/committed 与原 Finish 逐字节等价（TestGateWriterEndToEnd* / TestAttemptCommitGateFinishAttempt 复核）。
- **survival 路径**：`runSurvivalCoordinator` 收到的 `w` 为适配层（其上可能再包 interceptor），内部再包一层 `SerializedStreamWriter` 形成嵌套串行——锁序一致（外层→内层），无死锁路径；入口停 preStream 语义不变。
- **非 survival 预热路径**：header/状态码/200 提交时序不变；仅正文写入统一串行，顺序 [comment → error/content] 不变。

## 4. 测试证据

新增/修改测试（`go test -race ./domains/streaming/... -count=1` 全绿，cmd/gateway 全量回归通过）：

- `TestAttemptCommitGateFinishAttempt`（A-P2-5 硬化四态契约）
- `TestGateWriterFinishRoutesThroughGate` / `TestGateWriterFinishPartialFlushesOnCommit`（Finish 不再绕过 gate）
- `TestSerializedResponseWriterDelegatesHeaderAndStatus` / `ConcurrentProducersNeverInterleave`（32×2 并发不交错）
- `TestPreStreamKeepaliveWriterSharesSerializedChannel` / `CommentsAndFramesNeverInterleave`（A-P2-6 非 survival 路径）
- survival_coordinator `finishGateWriter` 接口随 Finish 签名同步为 `interface{ Finish() error }`，8 用例 coordinator 回归全绿

## 5. 残余缺口（明确未闭环，不得当作已交付）

1. **SR-13 survival 指标事件接线**：coordinator 决策/attempt 维度 Prometheus 未接（metrics 骨架已就绪）——归 W3。
2. **coordinator 级 e2e**：真 executor + 真上游 429→恢复流依赖部署环境——归 M4 S30-S35 故障注入统一验证。
3. **E-P2-3/4/6**：Anthropic→OpenAI 多模态桥、附件租户隔离、executor 级 flag-off 字节回归——归 MM-2。
4. outbox/dispatcher 运维闭环、部署 smoke、真实 PG sweep 不在本轮。
5. 工作区另存 4 个版本文件改动（`VERSION`/`version.json`/`web/public/menu-config.json`/`web/public/version.json`）为本轮之前产生，**未纳入本次提交**。

## 6. 风险与回滚

- 回滚单位：本轮 5 个源文件 + 3 个测试文件 + 文档，单 commit revert 即可，无 schema/配置变更。
- 最大行为变化点为 pre-stream keepalive 连接重绑定（`w = psk.Writer()`）：所有后续正文写入经 `SerializedStreamWriter` 互斥序列化，单连接增加一次锁开销；keepalive 默认 15s 间隔、流帧毫秒级频率下可忽略。
