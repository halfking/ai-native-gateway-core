# 12 小时内修订审计（2026-09-25 16:12）

> 审计基线：`9635b9b17..f746a4f52`（窗口 2026-09-25 04:01–15:41 +0800，39 提交）
> 审计入口：用户指令「12 小时内修正审计——检查所有修改，结合文档分析原因/方案/结果是否合理」
> 关注面：流程闭环、数据来源与去处、并发锁控制、多线程资源竞争、内存/句柄泄漏、异步处理异常、异常错误处理、请求数据转换
> 审计方式：逐 commit message↔diff 对账（吸取 R66 entry ② message 虚构教训）+ 关键路径代码精读 + 实跑验证

## 一、总览

39 提交分类：实质代码 21（fix 14 / perf 3 / feat 1 / test 1 / 工具面 2）、docs 8、merge 6、chore/style/deploy/version 4。改动面 174 文件 +6,773/−676。

**总评：本轮 12h 修订质量高。** 21 个实质提交中 20 个的「原因—方案—结果」链条经逐项对账成立，且仓库自身审计文化在位（R65 批判轮复核 59c7f4233 六声明点、02da86168 揪出 114beda22 次生 NUL 缺陷、R66 entry ② 揪出 message 虚构并 amend）。发现 4 项可落地硬化（1×P2 + 3×P3，见 §四），已全部实施（§六）；另登记 2 项移交待办（§五）。

## 二、逐簇审计结论

### 簇 A：252 PG SQL 审计线（生产数据面，风险最高）

| 提交 | 声明 | 对账结论 |
|---|---|---|
| 61e2f6c85 D10 候选查询 CTE 改写 | 双视图自 join 1831 行后剪枝 → WITH matched AS MATERIALIZED 先收窄；220-390ms→61ms | ✅ 合理。删掉的 sibling-EXISTS 段核实为 `AND FALSE` 常量折叠死代码（2026-08-27 起），行为无变化；12 组对拍 + SQL-shape 钉测试 + 252 真库实测证据链完整。CTE 内先行应用 match/modality/status 谓词与原 WHERE 后置语义等价（LEFT JOIN 集合相同，`_mc_*` 三列替换 `mc.*` 引用闭合） |
| 61e2f6c85 D12 claim outbox 补偿 | claim 置位成功后同事务登记 source='claim' 补偿行，关 G2 漏镜像洞 | ✅ 合理。savepoint `gw_claim_mirror_outbox` 隔离 + `ON CONFLICT (request_id) DO NOTHING` 幂等 + RLS GUC 事务内还原。GUC「还原为空/false」与全仓惯例一致（各写入点均在自身写前 `set_config(..., true)`，is_local 事务结束自还原，无跨段依赖） |
| 61e2f6c85 D11 指纹探针短路 | 全 NULL 指纹列上 30s 击穿 ×3/55min → 进程内信号+一次性探针 | ✅ 合理。`atomic.Value` 信号无竞态；probe 失败不 memoize（下轮重试）；决策纯函数表驱动；tick 单 goroutine 内 plain bool 安全 |
| 46b64bd42 rollup DELETE bucket-range | IN(子查询) → `WHERE bucket=$1` | ✅ 合理。语义=超集删除（顺带清 stale 行，方向正确）；多实例并发 DELETE+upsert 竞态与旧形式等价，无新增风险；测试钉「每桶一次 DELETE、UPDATE 后无 SELECT」 |
| 6e77c80dc alternatives 热表化 | 7 天 canonical view 聚合 151s 全超时（功能 100% 废）→ request_logs_hot 单-digit ms | ✅ 合理。tier-3 排序信号降窗到热保留窗口（~8h）的 trade-off 在注释中如实声明；废功能修复优先于完美口径 |
| ced9eb9b1 cache-v2（详见 §三/§四） | 内存索引 + 请求镜像 + 热区裁剪 | ⚠️ 部分成立。Worker 本体质量好但**双双未接线**（TODO 明示，见 §五-1）；发现 SetRetention/SetMaxBytes 竞态（§四-1） |

### 簇 B：probe 线（探针请求转换与调度）

| 提交 | 对账结论 |
|---|---|
| 8ebdf0a54 探针走原生 /v1/responses + max_output_tokens≥16 | ✅ 直连上游逐字复现 400 + responses 下限 16 实测，修复面覆盖全部 6 条探针路径（数据面本就按协议路由不动），本地 8782 部署实测三路径 healthy。请求数据转换（`{"input","max_output_tokens":32,"stream":false}`）与上游契约一致 |
| b268efbf9 R65 调度门补齐 | ✅ `reconcileStaleNodeProbeStateSQL` 补 `nps.next_retry_at <= now()` 与 pumpDueStatesSQL 口径一致（diff 核实属实）；simple-protocol 裸 nil 参数 → SQL NULL 字面量消除编码歧义 |
| 59c7f4233 健康节点零探测+根因分类 | ✅ 已被 b268efbf9 六声明点（B/C/D/E/F）逐项实证背书 |
| 8993ec2d5 探测请求漏标 origin | ✅ 双缺陷定性成立（DB key owner 注册晚于剥头中间件 + buildEntry 失败路径不应用 origin）；信任判定用 verifier 已解析 owner（api_keys 行），无客户端伪造面 |

### 簇 C：reportrollup / autoroute 数据语义线

| 提交 | 对账结论 |
|---|---|
| 02da86168 scope_key NUL 根修 | ✅ PG TEXT 拒 NUL → 长度前缀编码 `len(tenant):tenant:person`（无歧义：netstring 式前缀）+ RoundTrip/NUL 拒绝/历史裸键回退三守卫 + 真库 E2E。pgxmock 探不到、真库 E2E 抓到——正是 realdb 测试的存在意义 |
| c5514087b / 114beda22 reportrollup 审计轮 | ✅ 追赶/事务/解耦三缺陷修正方向成立；114beda22 的 P1 修复被 02da86168 复审揪出次生 NUL 缺陷并根修——审计链自我纠错闭环健康 |
| e4441a038 autoroute v2 五项修正 | ✅ ①基表→`_all` 视图（分母失真修复）②known_failure 计分对齐包内语义（防未来门禁假红）等，均为批判复审轮自身产出 |
| 9679ee80b 并发 Decide 三件套 | ✅ 测试竞态根修真实（stubClassifier 返回浅拷贝）。**但揭示接口级承重不变量缺失**（§四-2） |
| f746a4f52 ML 选择器 build tag 隔离 | ✅ 教科书式拆分：契约类型无标签 / 实现 `//go:build cgo` / 桩 `!cgo` 返回 ErrMLUnavailable 走既有降级。本次审计实跑 CGO_ENABLED=0/1 双模式 build 复验通过 |

### 簇 D：安全/工具面（速审通过）

- b0f18d00a secret-scan：DSN 正则还原 + OBFUSCATION 规则 + PUBLIC_DOMAINS 白名单显式化——收紧而非放松，✅
- 3957c1423 license `newTrialLicenseKey` panic→error：错误处理方向正确，✅
- dad3377d1 installer 离线 installDir 参数化 / a3831e839 docker save gzip 流式（对齐 maintain §6.2）：工具面低风险，✅
- 2bc70f38e 252 审计第七轮：其 alternatives 151s 根修已被 6e77c80dc 终态覆盖，✅
- 985d23ae9 ollama 流式增量契约：已被 R66 entry ② 批判式审计收口（message 虚构实锤 → amend 重写为真实交付面；零生产调用方），✅

## 三、专项核查（用户关注面逐项）

| 关注面 | 核查对象 | 结论 |
|---|---|---|
| 并发锁控制 | ClassificationCache Get/Set、claim outbox savepoint、trimmer 字段 | 缓存层双 clone 防御到位 ✅；savepoint 隔离 ✅；**trimmer 热重载字段无锁 ❌→已修** |
| 多线程资源竞争 | 并发 Decide × Classification 所有权 | 生产各实现均每次新建对象，无现实竞态；**但契约未文档化 →已补**（§四-2） |
| 内存/句柄泄漏 | MirrorAsync 每 write 一 goroutine 等 done | AsyncFileWriter.Close 排空队列 + execute 保证 done 恰一次（含 panic 恢复），无泄漏 ✅ |
| 异步处理异常 | AsyncFileWriter、hotzone trimmer ctx 取消 | 两处均优雅退出/快速失败 ✅ |
| 异常错误处理 | probe 失败分类、license panic→error、claim 失败降级 | 分层分类 + fail-open/fail-closed 取舍均有据 ✅ |
| 请求数据转换 | 探针 /v1/responses 载荷、ollama 流式增量、scope_key 编码 | 三处均有上游文档/直连复现/真库 E2E 证据 ✅ |
| 数据来源与去处 | DELETE bucket-range（去处=整桶重插）、claim outbox（去处=同事务补偿） | 均闭环 ✅ |
| 流程闭环 | cache-v2 双 worker 接线 | **未闭环（有意分期，TODO 明示）→登记待办**（§五-1） |

## 四、待修正清单（本轮全部实施落地）

| # | 级别 | 问题 | 根因 | 修复 |
|---|---|---|---|---|
| 1 | P2 | `HotZoneTrimmer.SetRetention/SetMaxBytes` 裸字段写，注释宣称「原子替换」不成立；接线后 admin goroutine 热重载与 Start goroutine 的 TrimOnce 构成数据竞争 | ced9eb9b1 引入时未考虑跨 goroutine 热重载（当时无调用方故未暴露） | retention/maxBytes 改 `atomic.Int64`；TrimOnce 每轮快照一次保证轮内口径自洽；注释改为如实描述 |
| 2 | P3 | `Classifier` 接口缺「返回对象独占所有权」契约——Decide 的 PostClassify 原地改写 `cls.Confidence`（decision.go），实现若返回共享/缓存指针即生产竞态；该不变量仅存于 9679ee80b 的测试注释 | 接口文档从未声明所有权语义，靠实现巧合遵守 | Classifier 接口文档补 ownership contract（含 9679ee80b 教训与 cloneClassification 先例引用） |
| 3 | P3 | `RequestMirror.mu sync.Mutex` 死字段 + 注释「线程安全（mutex 保护）」与实现不符（字段构造后不可变，实际无需锁） | ced9eb9b1 防御性预留但从未使用，注释误导后来者 | 删 mu 与 sync import，注释改为如实描述（不可变 + AsyncFileWriter 自身并发安全） |
| 4 | P3 | 9 个文件 HEAD 即未通过 gofmt（bg×2 / storage/file×1 / autoroute×6，含 8ebdf0a54/ced9eb9b1/e4441a038 等历轮遗留；cca46df3d gofmt batch 仅覆盖 round-8 工具触碰文件） | 各轮提交未跑 gofmt 全量校验 | 全部 gofmt -w（纯格式，零语义变化），与审计修复分两个提交 |

## 五、移交待办（不属本轮实施）

1. **cache-v2 双 worker 接线**（P1·流程闭环）：`NewHotZoneTrimmer`/`NewRequestMirror` 生产调用方为零——config 已解析、worker 已测试，但 main/daemon 未启动。接线前 data/hotzone/ 无任何清理与镜像（当前也无生产写入，无实际风险）。按原计划走 feature/wire-hotzone-trimmer + feature/wire-request-mirror 独立 PR，保证 ced9eb9b1 可单独回滚的设计意图。
2. **R66 handoff 既有遗留**（docs/handoff/2026-09-25-r66-entry2-audit-handoff.md §四）：mock-probe 生产接入拍板、ollama 楔约活体抓包、P4 selector 接线、usage_facts occurred_at 索引评审——均维持原登记，本轮不重复处置。

## 六、执行与验证记录（实跑）

```
# 审计验证（改动前基线）
go build ./...                    → exit 0
CGO_ENABLED=0 go build ./...      → exit 0（f746a4f52 声称的修复面复验成立）
go test ./autoroute/... ./routingopt/...        → ok（autoroute 4.018s）
go test ./bg/ ./domains/reportrollup/ ./provider/ ./internal/ir/ \
        ./domains/hooks/observability/telemetry/ ./domains/streaming/ → 全 ok（exit 0）

# 修复落地后复验
gofmt -l bg/ storage/ autoroute/  → 0（修复前 9 文件不合格）
go build ./bg/ ./storage/... ./autoroute/ + CGO_ENABLED=0 同 → exit 0
go vet 同三域                      → exit 0
go test ./bg/ -run 'HotZone|Trim'  → ok 0.668s（含热重载 SetRetention 收缩用例）
go test ./storage/file/            → ok 7.901s
go test ./autoroute/               → ok 3.545s
```

## 七、同步与推送状态（6 仓家族）

| 仓库 | 操作 | 终态 |
|---|---|---|
| llm-gateway/llm-gateway-go（主仓） | f746a4f52 编译双模式验证后推送 + 本轮审计修复提交 | origin/main=codeup 已核验 |
| llm-gateway-go-3 / syncfield/go-2 / syncfield/go-4 | fetch + `merge --ff-only` | = origin/main，0 ahead/0 behind |
| syncfield/go-2 github 镜像 | `push github main`（b0f18d00a→f746a4f52，SSH） | ls-remote 核验 =f746a4f52 |
| llm-gateway-go-cursor | 本地 main 以 plumbing merge 并入 f746a4f52（merge-tree+commit-tree+update-ref，不触碰双 worktree 在制文件；保留其本地 R64 handoff cb2920ac1） | main=merge(45ddc2401)，feature 分支与 dirty 文件未动 |
| llm-gateway-go-5 | **暂停合并**（16:00 索引仍在被并行会话写入，menu-config 等 18 文件在制）——仅 fetch，origin/main 已就位 | 待并行会话收口后自行 merge |

> 并行会话防护：主仓推送前 fetch 核对 1-ahead/0-behind 无搭车；cursor 首次合并误用过期 tracking ref（9679ee80b）已即时发现并以 cb2920ac1+f746a4f52 重做（45ddc2401），旧合并对象 0aa639b0c 留 reflog 可回收。
