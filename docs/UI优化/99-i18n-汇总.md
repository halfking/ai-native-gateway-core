# i18n 多语言审计汇总

> 审计日期：2026-07-14
> 站点：https://llmgo.kxpms.cn
> 静态检查：`cd web && npm run i18n:check`
> 浏览器实测：5 个关键页 × 7 语言（en/zh-TW/ja/de/fr/es/ar）

## 静态审计摘要

| 指标 | 数值 |
|------|------|
| 扫描文件 | 249 |
| t() 引用 | 3789 |
| locale 键总数 | 6827 |
| zh-CN 源键 | 6027 |
| **缺失键（引用但无翻译）** | **310** |
| 未使用键 | 3109 |

## 缺失键按文件（P0 优先）

| 文件 | 缺失规模 | 影响页面 |
|------|----------|----------|
| `PromptInjectionSettingsView.vue` | ~100+ keys (`promptInjectionFull.*`) | 提示词注入检测 — **页面不可用** |
| `ModulesView.vue` | 5 keys (`modulesView.overview.*`) | 模块管理/微信机器人 |
| `ops/AutoUpdateView.vue` | common.status + undefined 枚举 | 自动更新 |
| `ops/VibeCodingView.vue` | common.status + undefined 枚举 | VibeCoding |
| `ops/OpsOverviewView.vue` | common.status | 运维概览 |
| `ops/LicenseManagementView.vue` | common.* | License 管理 |
| `ops/FaultManagementView.vue` | common.status/view | 故障管理 |
| `ops/CenterOpsView.vue` | common.status | 中心运维 |
| `EnhancedDashboardView.vue` | common.error | 未挂路由（遗留） |

## 硬编码中文（未走 i18n，静态检查扫不到）

| 文件 | 严重程度 | 说明 |
|------|----------|------|
| `SessionManagementView.vue` | **P0** | 572 行几乎全硬编码中文；import useI18n 未使用 |
| `SessionListView.vue` | **P0** | 大量硬编码（见 ui-audit 2026-07-07） |
| `SessionStatsPanel.vue` | P1 | 错误静默吞掉 |

## 浏览器语言切换实测

### 测试方法

对 `/`、`/admin/sessions`、`/admin/prompt-injection`、`/providers`、`/ops/overview` 切换 7 语言，检测：
- i18n key 泄漏
- 中文残留
- RTL（ar）

### 结果矩阵

| 页面 | en | zh-TW | ja | de | fr | es | ar | 结论 |
|------|----|-------|----|----|----|----|-----|------|
| 总览 `/` | 部分中文 | 中文 | 空白* | 空白* | 中文 | 中文 | RTL+中文 | **P1** locale 切换不稳定 |
| 会话管理 | 标题英/表头中 | 表头中 | 表头中 | 表头中 | 表头中 | 表头中 | RTL+中文 | **P0** 仅标题部分翻译 |
| 提示词注入 | key泄漏 | key泄漏 | key泄漏 | key泄漏 | key泄漏 | key泄漏 | RTL+key | **P0** 全语言 key 泄漏 |
| 供应商 | 标题翻译 | 标题翻译 | 标题翻译 | 标题翻译 | 标题翻译 | 标题翻译 | RTL+标题翻译 | OK |
| 运维概览 | 英文标题 | 英文标题 | 英文标题 | 英文标题 | 英文标题 | 英文标题 | RTL+英文 | **P1** 非 en 仍显示英文 |

*ja/de 切换后页面短暂空白，可能 lazy load 未完成或 reload 时序问题。

### 已确认泄漏的 key（浏览器实测）

```
promptInjectionFull.loadStatsFailed
promptInjectionFull.loadDetectionsFailed
promptInjectionFull.loadRulesFailed
ops.autoupdate.channel.undefined
ops.autoupdate.logStatus.undefined
ops.vibecoding.status.undefinedInvalid
app.current_tenant
authentication.login          （审计日志操作类型）
```

## RTL（阿拉伯语）检查

- `document.documentElement.dir` 切换为 `rtl`：**正常**
- 侧边栏、主内容区布局：**未明显错位**
- 问题：RTL 下中文/英文/key 混排内容未镜像调整（P2）

## 修复优先级

### P0（立即）

1. 新增 `locales/*/promptInjectionFull.ts`（~100 keys）
2. `SessionManagementView.vue` 全量 i18n 迁移
3. `SessionListView.vue` i18n 迁移
4. ops 页 undefined 枚举 fallback（AutoUpdate/VibeCoding）

### P1（本周）

1. 补 `common.status/view/updateSuccess/operationFailed` 到各语言
2. `AuditLogView` action 类型翻译映射
3. Dashboard locale 切换重渲染修复
4. ops Overview 标题多语言

### P2（排期）

1. 阿拉伯语混排排版优化
2. 图表 legend i18n
3. 清理 3109 unused keys（分批）

## 验证命令

```bash
cd web
npm run i18n:check        # 缺失键报告
npm run i18n:check:strict # 严格模式（CI）
```

修复后应在 8 语言下逐页回归，重点：提示词注入、会话管理、运维平台 6 页。
