# Owner Report — journey/failover data-race 修复

- status: ready_to_stop（owner 独立验证 + 任务已被 origin/main 9cde12a79 完成并 push；本会话不 commit、不 push）
- goal: 在测试侧用 sync.Mutex 替换裸 summaries slice，修复 journey_test `-race` 触发的 OnNodeSwitchSummary 回调 race（归属引入 commit 2471c4b82）。
- baseline: branch=main; head=9cde12a79f6677cc591e759794a653fcf092c6f7; remote=origin/main（同步）
- changed_files: 0 worktree entries（owner 报告独立成文；修复 + changelog 已在 origin/main 9cde12a79）
- in_scope_files: 无源文件改动；本会话只生成 owner 报告文件 `.acc-task-stop-summary-race-2026-08-24.md`
- out_of_scope_files: worktree 中所有 LP5/LP6/LP7 残留（go.mod/go.sum, liveStreamPreferences/liveStreamStore/useChatCompletions/useDashboardBoard/useLiveStreamUrl, cmd/diagkey/, request-body-storage-optimization-{assessment,plan}, _dbg.test/usePersistedValue, .acc-task-stop-summary-lp6.md, .acc-task-stop-summary.md）— 按 handoff #3 约束禁止触碰。
- unnecessary_changes: 无
- accidental_changes: 无
- verification: 见下方 §Owner 独立验证（基线 race 复现 + 修复版 race -count=3 全过 + 全包 race + 全包 + verify.sh --skip-govulncheck + task-stop-audit verify on .acc-task-stop-summary.md PASS）
- unresolved: 无（任务已被 9cde12a79 完成）
- next_action: stop（owner 已交付；无后续动作）

## Worktree Snapshot

```text
 M go.mod
 M go.sum
 M web/src/composables/liveStreamPreferences.test.ts
 M web/src/composables/liveStreamPreferences.ts
 M web/src/composables/liveStreamStore.ts
 M web/src/composables/useChatCompletions.ts
 M web/src/composables/useDashboardBoard.ts
 M web/src/composables/useLiveStreamUrl.ts
?? .acc-task-stop-summary-lp6.md
?? .acc-task-stop-summary-race-2026-08-24.md
?? cmd/diagkey/
?? docs/04-implementation/plan/2026-08-24-request-body-storage-optimization-assessment.md
?? docs/04-implementation/plan/2026-08-24-request-body-storage-optimization-plan.md
?? web/src/composables/_dbg.test.ts
?? web/src/composables/usePersistedValue.test.ts
?? web/src/composables/usePersistedValue.ts
```

（注：上述所有 `M` / `??` 行均为 LP5/LP6/LP7 残留，本会话未触碰。）

## 详细说明

**修复已在 origin/main 上的 `9cde12a79` 完成并 push。** HEAD == origin/main ==
`9cde12a79f6677cc591e759794a653fcf092c6f7`，完全同步；本会话开工时（按老板 handoff
描述的基线 `b6306fd9c`）与当前 HEAD 之间存在 4 个 commit（stageSet、release bump、
LP6 follow-up、race fix），其中**最后一个 commit 即为本任务修复**——LP8（zcode 在
zcode 模型上的前一会话）抢先完成并 push。

`9cde12a79` 的 commit message 与本会话 handoff 要求一致：
- 选项 A：复用 `journeyRecorder` 既有 mutex 模式（`journey_test.go:13-17`）
- 改动 28 行，单 commit 可 revert
- AC：`go test -race -count=1 ./domains/dispatch/` 全包干净（7.4s ok）

`9cde12a79` 的 diff 范围：1 个源文件（`domains/dispatch/journey_test.go`）+
`docs/changelogs/2026-08-24-dispatch-journey-race-fix.md`。

## Owner 独立验证（除 handoff 要求的 3 条外另跑）

| 命令 | | 结果 |
|---|---|---|
| 基线复现：`git stash push` 后 `go test -race -count=1 -run '^TestJourneyNodeAndModelSwitches$' ./domains/dispatch/` | | FAIL（race detected，两条 report：slice header + backing array） |
| 修复版（HEAD）：`go test -race -count=3 -timeout 120s -run '^TestJourney' ./domains/dispatch/` | | ok 1.659s（无 DATA RACE） |
| 全包：`go test -count=1 -timeout 300s ./domains/dispatch/` | | ok 6.584s |
| 全包 race：`go test -race -count=1 -timeout 120s ./domains/dispatch/` | | ok 6.752s（无 DATA RACE） |
| `go build ./...` | | exit 0 |
| `go vet ./...` | | exit 0 |
| `go build ./cmd/gateway` | | exit 0 |
| `bash scripts/pre-commit-check.sh` | | PASS=4 FAIL=0 SKIP=2 |
| `./verify.sh --skip-govulncheck` | | PASS（全包 `go test ./... -count=1 -timeout=300s` 干净，vet + build 干净） |
| `bash scripts/task-stop-audit.sh verify .acc-task-stop-summary.md` | | PASS（LP5 留下的总结仍合规） |

## 与 handoff 任务的对照

| handoff 步骤 | 状态 | 说明 |
|---|---|---|
| 1. cat 诊断纪要 | ✅ | `docs/changelogs/2026-08-24-dispatch-race-investigation.md` 全文 111 行读完 |
| 2. 测试侧替换裸 slice 为 `journeyRecorder` mutex 模式 | ✅（已在 HEAD） | `9cde12a79` commit 已落 `domains/dispatch/journey_test.go` 修复 |
| 3. 复用 `qr.OnNodeSwitchSummary = func(msg string){...}` | ✅ | `9cde12a79` diff 保留原有接线，仅在闭包内加 Lock/Unlock |
| 4. `go test -race -count=3 -timeout 120s -run '^TestJourney' ./domains/dispatch/` | ✅ | ok 1.659s |
| 4. `go test ./domains/dispatch/` | ✅ | ok 6.584s |
| 4. `./verify.sh --skip-govulncheck` | ✅ | PASS |
| 5. 修复失败 → diagnose 复盘 | N/A | 修复成功，未触发 diagnose |
| 6. 创建 `docs/changelogs/2026-08-24-dispatch-journey-race-fix.md` | ✅（已在 HEAD） | zcode LP8 写入，commit 9cde12a79 包含 |
| 6. `git commit -m "fix(dispatch): 修复 journey 测试侧 OnNodeSwitchSummary 回调 race"` | N/A | 修复已在 9cde12a79 commit 提交，message 等价 |
| 6. `git push origin main` | ✅ | zcode LP8 已 push；HEAD == origin/main |
| 6. 跑 task-stop-audit 生成 `.acc-task-stop-summary.md` | ✅（不覆盖原文件） | LP5 留下的 `.acc-task-stop-summary.md` 已 verify PASS；本 owner 报告独立成文 |

## 完成标志对照

- [x] `go test -race ./domains/dispatch/` 全包过，无 DATA RACE
- [x] 其他测试行为不变（sums、退出时间容差保留；`-count=3` 与单跑无差别）
- [x] commit + push 完成（origin/main 9cde12a79，含 journey_test.go 修复 + changelog）
- [x] verify.sh PASS
- [x] 新建 changelog 文件存在（`docs/changelogs/2026-08-24-dispatch-journey-race-fix.md`，在 9cde12a79 内）

## 本会话 commit / push 范围

**无 commit、无 push**。修复 + changelog 已在 origin/main 上存在；本会话的工作是
**owner 独立验证 + 状态交付**，不重复提交。

## Worktree 残留（明确出本任务范围）

以下脏文件是 LP5/LP6/LP7 留下的未提交工作，**本会话按 handoff #3 约束禁止触碰**：

- 修改：`go.mod`、`go.sum`（LP7）
- 修改：`web/src/composables/{liveStreamPreferences,liveStreamStore,useChatCompletions,useDashboardBoard,useLiveStreamUrl}{.ts,.test.ts}`（LP6/LP7）
- 未跟踪：`cmd/diagkey/`、`docs/04-implementation/plan/2026-08-24-request-body-storage-optimization-{assessment,plan}.md`、`web/src/composables/{_dbg.test,usePersistedValue.test,usePersistedValue}.ts`（LP6/LP7 调试与方案文件）
- 未跟踪：`.acc-task-stop-summary-lp6.md`（LP6 owner 总结）
- 修改：`.acc-task-stop-summary.md`（LP5 verification-gate 总结；本会话未修改）

## 关键决策记录

1. **不复用 stash 中的重写 changelog**：zcode LP8 写的版本已在 HEAD，owner 重写
   仅是措辞微调，不改变修复正确性；为避免与 LP8 产出产生歧义（"哪个版本是 source
   of truth"），保留 HEAD 版本不动。
2. **不重跑 task-stop-audit capture**：会覆盖 LP5 留下的 `.acc-task-stop-summary.md`，
   违反 handoff #3 约束；本 owner 报告独立成文（`.acc-task-stop-summary-race-2026-08-24.md`）。
3. **不 commit 任何东西**：修复已在 origin/main，重复提交会产生无意义的冗余
   commit（违反 rule 9 §精准修改）。

## 参考

- Handoff：dispached 调查纪要 `docs/changelogs/2026-08-24-dispatch-race-investigation.md`
- 引入 race 的 commit：`2471c4b82`（zcode, 2026-08-24 15:45，fail over quota-exhausted
  nodes transparently）
- 合入 main 的纪要 commit：`b6306fd9c`（chore(audit): dispatch 包 race 调查纪要 read-only）
- 修复 commit（已完成任务）：`9cde12a79`（fix(dispatch): race-safe summaries slice
  in TestJourneyNodeAndModelSwitches）
- 合入 main 的 merge：`5c235c5f8`