apiVersion: 1

providers:
  - name: 'telemetry-sanitize'
    orgId: 1
    folder: 'LLM Gateway'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/telemetry-dashboards
