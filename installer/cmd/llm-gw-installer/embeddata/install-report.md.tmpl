# llm-gateway-go 部署报告

## 环境信息

| 项目 | 值 |
|------|-----|
| OS | {{.OSInfo.Distribution}} {{.OSInfo.Kernel}} |
| 架构 | {{.OSInfo.Arch}} |
| 包管理器 | {{.OSInfo.PackageMgr}} |
| 容器引擎 | {{.OSInfo.ContainerEng}} |
| Docker 版本 | {{.OSInfo.DockerVersion}} |
| 安装路径 | {{.Config.InstallPath}} |
| 安装时间 | {{.InstallTime}} |

## 端口配置

| 服务 | 端口 |
|------|------|
| 应用 (HTTP) | {{.Config.AppPort}} |
| PostgreSQL | {{.Config.PGPort}} |
| Redis | {{.Config.RedisPort}} |

## 镜像版本

| 镜像 | Tag | 来源 |
|------|-----|------|
| kx-llm-gateway-go | {{.AppImageTag}} | {{.ImageSources.App}} |
| kx-citus | v11.3.0 | {{.ImageSources.Citus}} |
| kx-redis | v7-alpine | {{.ImageSources.Redis}} |

## 健康检查

| 检查项 | 状态 |
|--------|------|
| Docker 容器全部 Running | {{.Health.ContainersOK}} |
| 应用 /healthz 端点 | {{.Health.HealthzOK}} |
| PostgreSQL 可连接 | {{.Health.PGReadyOK}} |
| Redis 可连接 | {{.Health.RedisOK}} |
| 数据库 schema 已加载 | {{.Health.SchemaOK}} |

## 访问地址

- **API**: http://localhost:{{.Config.AppPort}}
- **健康检查**: http://localhost:{{.Config.AppPort}}/healthz
- **Prometheus Metrics**: http://localhost:{{.Config.AppPort}}/metrics

## 容器外持久化目录

| 用途 | 路径 |
|------|------|
| 附件 | {{.Config.InstallPath}}/attachments |
| 日志 | {{.Config.InstallPath}}/app/logs |

## Secrets

- `.env` 文件位于 `{{.Config.InstallPath}}/.env`，权限 600

## 后续操作

```bash
# 进入安装目录
cd {{.Config.InstallPath}}

# 重启服务
docker compose -f {{.Config.InstallPath}}/compose.yml restart

# 查看日志
docker compose -f {{.Config.InstallPath}}/compose.yml logs -f

# 停止服务（保留数据）
docker compose -f {{.Config.InstallPath}}/compose.yml down

# 彻底清理（删除数据）
./uninstall.sh --purge
```

## 故障排查

如果遇到问题：
1. 查看日志：`docker compose logs -f`
2. 进入容器调试：`docker exec -it kx-llm-gateway-go sh`
3. 检查数据库：`docker exec -it kx-citus psql -U kxuser -d llm_gateway`
4. 联系运维支持，提供本报告

---

*Generated by llm-gw-installer v{{.InstallerVersion}}*
*Generated at: {{.InstallTime}}*
