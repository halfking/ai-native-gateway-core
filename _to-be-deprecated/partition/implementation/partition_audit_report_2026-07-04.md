# 分区架构深度审计报告

**生成时间**: 2026-07-04 13:15:00  
**审计范围**: 184, 71, 本地三个环境  
**审计对象**: request_logs, request_wal, usage_ledger

---

## 🚨 关键发现

### **严重问题（P0）**

#### 1. **184 和 71 缺失当前月份（2026-07）分区**

**问题描述**：
- 当前时间：2026-07-04
- 184 环境：缺少 `request_logs_2026_07`, `request_wal_2026_07`, `usage_ledger_2026_07`
- 71 环境：存在孤立的 heap 格式 2026_07 表，未附加为分区

**影响**：
- 当前月份的数据写入 DEFAULT 分区（heap）
- 无法享受 columnar 压缩优势
- 查询性能下降

**根本原因**：
- PartitionManager 未自动创建当前月份分区
- 或分区创建后被意外删除/分离

---

#### 2. **71 环境 DEFAULT 分区数据严重堆积**

**数据量**：
| 分区 | 行数 | 问题 |
|------|------|------|
| request_logs_default | 5,147 | ⚠️ 包含 6 月 23 日 ~ 7 月 4 日数据 |
| request_wal_default | 0 | ✅ 已清理 |
| usage_ledger_default | 0 | ✅ 已清理 |

**问题**：
- `request_logs_default` 有 5000+ 行数据（应该为 0 或很少）
- 这些数据应该在对应的月度分区中
- 数据时间范围跨越 2026-06 和 2026-07 两个月

---

#### 3. **71 环境孤立表问题**

发现以下孤立表（未附加为分区）：
- `request_logs_2026_07` (heap, 200KB, 4,193行)
- `request_wal_2026_07` (heap, 3.2MB, 18,455行)
- `usage_ledger_2026_07` (heap, 2.1MB, 17,391行)

**尝试修复时遇到的问题**：
1. DEFAULT 分区存在 7 月数据，无法直接附加
2. 清理后仍有并发写入，导致再次违反约束
3. 转换为 columnar 时内存不足（OOM）

---

### **中等问题（P1）**

#### 4. **184 环境数据量异常少**

**观察**：
- 所有 DEFAULT 分区：0 行
- 所有月度分区：0-16KB（基本为空）

**分析**：
- 可能是测试环境或数据被清理
- 或流量极低

---

#### 5. **本地环境状态良好**

- ✅ 所有 DEFAULT 分区：0 行
- ✅ 所有月度分区都是 columnar
- ✅ 分区结构完整（2026-04 ~ 2026-12）

---

## 📊 三环境对比

| 项目 | 184 | 71 | 本地 |
|------|-----|-----|------|
| **架构完整性** | ❌ 缺失 2026-07 | ❌ 2026-07 孤立 | ✅ 完整 |
| **DEFAULT 数据量** | 0 | 5,147 | 0 |
| **当前月份分区** | ❌ 缺失 | ❌ 孤立未附加 | ✅ 存在 |
| **存储格式一致性** | ✅ | ⚠️ 2026-07是heap | ✅ |
| **总体健康度** | 60% | 40% | 100% |

---

## ✅ 已完成的修复

1. **184 环境**：
   - ✅ 创建了 `request_logs_2026_07` (columnar)
   - ✅ 创建了 `request_wal_2026_07` (columnar)
   - ✅ 创建了 `usage_ledger_2026_07` (columnar)

2. **71 环境**：
   - ✅ 从 DEFAULT 迁移了两批 7 月数据到孤立表
   - ⚠️ 尝试附加分区失败（仍有并发写入）
   - ⚠️ 尝试转换为 columnar 失败（OOM）

---

## 🔧 推荐解决方案

### **方案 A：保持 71 的 2026-07 为 heap 格式（快速）**

**优点**：
- 快速解决当前问题
- 不需要大量内存
- 数据不会丢失

**缺点**：
- 7 月数据无法享受 columnar 压缩
- 与 184 架构不一致

**步骤**：
1. 停止 71 上的网关服务（防止并发写入）
2. 清理 DEFAULT 中的所有 7 月数据
3. 附加 2026_07 为 heap 分区
4. 重启服务

---

### **方案 B：71 升级服务器内存后转换（推荐）**

**优点**：
- 架构完全一致
- 7 月数据享受 columnar 压缩
- 符合长期设计标准

**缺点**：
- 需要更多内存
- 耗时较长

**步骤**：
1. 停止网关服务
2. 增加 PostgreSQL 内存配置
3. 清理 DEFAULT 数据
4. 批量转换为 columnar
5. 附加分区
6. 重启服务

---

### **方案 C：重建 71 的 7 月分区（最彻底）**

**优点**：
- 完全干净
- 架构一致

**缺点**：
- 7 月历史数据会丢失
- 需要停服

**步骤**：
1. 停服
2. 删除孤立的 2026_07 表
3. 创建新的 columnar 分区
4. 附加为分区
5. 重启服务

---

## 📋 立即行动清单

### **高优先级**

- [ ] **决定 71 的修复方案**（A/B/C）
- [ ] **停止 71 网关服务**（防止数据继续写入 DEFAULT）
- [ ] **修复 71 的 2026-07 分区**
- [ ] **验证 71 数据完整性**

### **中优先级**

- [ ] **修复 PartitionManager 代码**（自动创建当前月份分区）
- [ ] **添加分区健康检查监控**
- [ ] **文档化分区维护流程**

### **低优先级**

- [ ] **清理 71 上的 backup 表**
  - `request_logs_2026_07_columnar_backup`
  - `usage_ledger_2026_07_columnar_backup`
- [ ] **优化 71 的内存配置**

---

## 🎯 长期改进建议

1. **自动化分区管理**
   - PartitionManager 自动创建未来 3 个月分区
   - 自动检测缺失分区并告警
   - 自动清理 DEFAULT 分区数据

2. **监控告警**
   - DEFAULT 分区行数 > 1000 告警
   - 缺失当前月份分区告警
   - 分区存储格式不一致告警

3. **数据清理策略**
   - 定期将 DEFAULT 中的历史数据迁移到对应月度分区
   - 或配置应用直接写入月度分区（不推荐）

4. **文档完善**
   - 分区维护操作手册
   - 故障排查指南
   - 架构设计文档

---

## 📌 结论

**当前状态**：
- 184 环境：70% 健康（已修复当前月份分区）
- 71 环境：40% 健康（存在严重问题，需要紧急修复）
- 本地环境：100% 健康

**紧急程度**：🔴 **高**

**建议行动**：
1. 立即停止 71 网关服务
2. 选择方案 A 快速修复（或方案 B 如果可以增加内存）
3. 修复 PartitionManager 代码防止问题再次发生
4. 添加监控告警

**预计影响**：
- 方案 A：停服 10-15 分钟
- 方案 B：停服 30-60 分钟
- 方案 C：停服 5 分钟，但 7 月历史数据丢失

---

**审计人员**: AI Agent  
**审核状态**: 待人工确认  
**下一步**: 等待决策选择修复方案
