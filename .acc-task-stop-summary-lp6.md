# LP6 Task Stop Summary — useChatSessions 全树 localStorage 写入防抖

- goal: 把 `useChatSessions` 每次 mutation 触发的同步 `localStorage.setItem` 全树写入改为 300ms 防抖；保留 visibilitychange / pagehide / beforeunload 生命周期强制 flush；storage 抛错时降级；补单测 + browser-use 实测
- baseline: branch=main; head=fcf80e9dd916c973d445fe16b4be1face1a939f1 (与 origin/main 同步)
- changed_files: 3 files (web/src/composables/useChatSessions.ts M +113/-5; web/src/composables/useChatSessions.test.ts A; docs/changelogs/2026-08-24-use-chat-sessions-persistence-debounce.md A)
- handoff: `/tmp/opencode/llm-gateway-memory-optimization-phase2-handoff.md`

## changed_files (本任务产出)

```
M  web/src/composables/useChatSessions.ts       (118 行 diff: +113 / -5)
A  web/src/composables/useChatSessions.test.ts  (新文件, 9 个 vitest case)
A  docs/changelogs/2026-08-24-use-chat-sessions-persistence-debounce.md  (新 changelog)
```

## in_scope_files

- `web/src/composables/useChatSessions.ts` — 主要改动
- `web/src/composables/useChatSessions.test.ts` — 单测
- `docs/changelogs/2026-08-24-use-chat-sessions-persistence-debounce.md` — 变更说明

## out_of_scope_files (他人所有，本会话未触碰)

```
M  .acc-task-stop-summary.md                                  (用户自有)
M  docs/changelogs/2026-08-24-session-queue-memory-optimization.md   (Agent A / 其他人)
M  domains/dispatch/queue_projection.go                       (Agent A 之前的改动)
M  domains/dispatch/queue_projection_test.go                  (Agent A 之前的改动)
M  domains/dispatch/waterfall.go                             (Agent A 之前的改动)
 M web/src/components/QueuePerspectivePanel.vue               (他人)
?? docs/04-implementation/plan/2026-08-24-request-body-storage-optimization-assessment.md  (另一会话的评估)
```

## unnecessary_changes: 无

## accidental_changes: 无（git diff --stat 仅命中 3 个 in_scope 文件）

## verification

| 项 | 命令 | 结果 |
|---|---|---|
| 单元测试 | `pnpm vitest run src/composables/useChatSessions.test.ts` | ✅ 9/9 passed |
| Type check | `pnpm vue-tsc --noEmit` | ✅ 无错误 |
| Build | `pnpm build` | ✅ ChatView 33.47 kB |
| Pre-commit | `bash scripts/pre-commit-check.sh` | ✅ 6/6 PASS (vue-tsc + token compliance) |
| Verify | `bash verify.sh --skip-govulncheck --web` | ✅ PASS |
| Browser 实测 | `browser-use` + Chromium (rule 11 §6) | ✅ 见下方截图 |

**browser-use 验证证据**（`.scratch/ui-verify/`）：

- `ui-verify-useChatSessions-01-initial-20260824-201120.png` — chat 初始页
- `ui-verify-useChatSessions-02-after-burst-20260824-201538.png` — 5 次 sync burst → 1 次 setItem (ts=[301ms])
- `ui-verify-useChatSessions-03-after-reload-20260824-201622.png` — 刷新后 model='visibilitychange-test' 仍持久化
- `ui-verify-useChatSessions-04-final-state-20260824-201637.png` — 综合状态

**browser-use 动态断言**：

1. `5 次 sync burst → 1 次 setItem`，ts=[301ms]（debounce 工作正常）
2. `pagehide` 触发 → before=0 calls, after=1 call（同步 flush）
3. `visibilitychange` (hidden) → before=0, after=1（同步 flush）
4. `visibilitychange` (visible) → 0 calls（不 flush，无 idle thrashing）
5. `beforeunload` → before=0, after=2（sync flush；2 因为 ChatView 实例 + 测试注入实例各 flush 一次）
6. `setItem` 抛 QuotaExceededError → 不崩，inMemoryModel='quota-test-2'，flushPersist() 返回 true
7. `location.reload()` → localStorage 仍含 1 session, model='visibilitychange-test'

## unresolved: 无

## next_action: 等老板确认后 commit + push

**commit message 草稿**：

```
perf(web): debounce useChatSessions localStorage writes (LP6)

- Coalesce whole-tree saveAll into 300ms debounced flush
- visibilitychange / pagehide / beforeunload force sync flush
- onScopeDispose cleanup + final flush in Vue scope
- saveAll try/catch for quota / private mode (graceful degrade)
- Track last persisted snapshot (short-circuit idempotent writes)
- Expose flushPersist() for tests and rare sync callers
- 9 vitest cases (debounce / flush / degrade / scope cleanup)
- browser-use verification (ui-verify-useChatSessions-*.png)
```

---

## task-stop-audit 9 字段 shim（rule 26 / rule 50 兼容归档层）

> 该层引用上方会话正文，不复制。脚本 `scripts/task-stop-audit.sh verify` 只检查 `- <field>:` 行首存在，不校验内容真实性。

- in_scope_files: 与上文 §changed_files 一致 — web/src/composables/useChatSessions.ts (M +113/-5) + web/src/composables/useChatSessions.test.ts (A) + docs/changelogs/2026-08-24-use-chat-sessions-persistence-debounce.md (A)
- out_of_scope_files: 与上文 §out_of_scope_files 一致 — `.acc-task-stop-summary.md` (用户自有) + docs/changelogs/2026-08-24-session-queue-memory-optimization.md (Agent A) + domains/dispatch/{queue_projection,waterfall}.go (Agent A) + web/src/components/QueuePerspectivePanel.vue (他人) + docs/04-implementation/plan/2026-08-24-request-body-storage-optimization-assessment.md (另一会话)
- unnecessary_changes: 无
- accidental_changes: 无（git diff --stat 仅命中 3 个 in_scope 文件）
- verification: 见上 §verification 表格 — vitest 9/9 + vue-tsc 0 err + pnpm build PASS + pre-commit 6/6 + verify.sh --web --skip-govulncheck PASS + browser-use 7 动态断言全 PASS（5 burst → 1 setItem + visibilitychange/pagehide/beforeunload flush + QuotaExceededError degrade + reload 持久化）
- unresolved: 无
- next_action: 与上文一致 — commit message 草稿 `perf(web): debounce useChatSessions localStorage writes (LP6)` 待老板确认后发出 + push