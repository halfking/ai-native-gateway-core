# 主分支同步 + 4h 修正闭环审计 — Handoff（2026-09-01）

## TL;DR

仓库：`/Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go`

| 状态 | 值 |
| --- | --- |
| 当前分支 | `main` |
| 本地 HEAD | `bc48559b4d012aa53d03700af89e5cf8a4e26380` |
| origin/main | `bc48559b4d012aa53d03700af89e5cf8a4e26380`（SHA 匹配） |
| 工作区 | clean（`git status --porcelain=v1` 无输出） |
| 推送状态 | 已推送至 origin/main（push `53e6a5158..bc48559b4`） |
| 双轨 commit | A `8e2b428bf` · B `bc48559b4` |
| Pre-commit 钩子 | 6 项全部 PASS（go vet / SQL no SET+placeholder / Migration unique NNN / Migration has down.sql / Vue vue-tsc / Web token compliance） |

## 任务前提修正（事实 vs 任务描述）

任务假设 `HEAD` 落后 `origin/main` 1 个节点、需合并 51d05bddf 与 0dacb1d22。事实：起跑时 `HEAD == origin/main == f6abe0732`，两个目标 commit 已在祖先链中。无需"合并 origin/main"步骤。改用"工作区对齐 + 双轨提交"模型。

工作区曾被覆盖为占位符 `9.9.9-abc12345-20260831-1867`（任务明令禁止）。从 `stash@{1}` (`8e0ad639555e21af9771eaa22303978eaf2bf217`) 恢复真实构建版本 `2.4.7-e3f0330b-20260831-1867`。

用户决策（4 个关键分歧）：
1. 双轨提交
2. `admin/self_check_handlers.go` 工作区版 150 行重构整体丢弃，仅保留 stash 的 17 行 `probeEnqueue` 快速失败路径
3. 全部纳入本次同步提交（`cmd/cfg_dump/`, `data/model-quality/`, `tests/lib/fake-remote-host.sh`）；新增 `.gitignore` 条目屏蔽未来 `data/model-quality/scores/` 累积
4. 审计聚焦最近 24 小时改动 + 关键高风险区

## Commit A `8e2b428bf` — `chore(sync): restore 2.4.7 build artifacts and self-check 503 fast-path`

7 文件 / +1029 / −16：

- `VERSION` / `version.json` / `web/public/version.json` → 真实构建版本 `2.4.7-e3f0330b-20260831-1867`（覆盖 `9.9.9-abc12345` 占位符）
- `web/public/menu-config.json` → 刷新 `exported_at: 2026-08-31T22:10:00.982Z`
- `admin/self_check_handlers.go` → 当 `body.Model` 非空时，单模型通过 `probeEnqueue` 入队；503 on enqueue failure / 200 `mode=probe_queue` on success。`gofmt` 同步修正插入块缩进与下游 `writeJSON` map 字面量对齐
- `tests/deploy_seamless_state_machine_test.sh` + `tests/lib/fake-remote-host.sh` → 从 `stash@{1}^3`（untracked 树）恢复，纳入测试装置

源：`stash@{1}` (`8e0ad639555e21af9771eaa22303978eaf2bf217`) 保留自上一个同步会话，逐字恢复并补 gofmt。

## Commit B `bc48559b4` — `fix(audit-20260901): apply 6 confirmed P0/P1/P2 fixes from 24h audit`

28 文件 / +1448 / −37：6 个修复 + 3 份子代理审计报告 + `docs/audit/2026-08-31-4h-correction-audit.md` §2.8 + `.gitignore`。

| # | 严重度 | 文件 | 修复 |
| --- | --- | --- | --- |
| **F-1** | **P0** | `internal/ir/parse_gemini.go:216` + `parse_gemini_stream.go:189` + `serialize_gemini.go` (`toolUseNameFromID`) | 并行 `functionCall` 同名 parts 加 `_<partIdx>` 后缀；`toolUseNameFromID` 在 serialize 时剥离尾随数字下标以保证 round-trip 不变 |
| **F-2** | **P0** | `domains/session/v2/turn_reader.go:68-70` | `LoadChain(0)` 兑现"全部"语义（替代默认 10）；`lastN<0` 返回错误而非静默退化为 10 |
| **F-3** | **P1** | `errorsx/sanitize.go`（新）+ `domains/streaming/executors/candidate_failure_logger.go:227-235` + `admin/vendor_credential_error_handlers.go:189` | 写入前 + 读出时双重脱敏上游 vendor body：Bearer / `sk-*` API key / `api_key=` 查询参数 / `x-api-key` header echo |
| **F-4** | **P1** | `domains/notification/lark.go:265-307` | `sendJSON` 重试：5xx / 408 / 429 / 网络错误按 100/500/2000 ms 退避；401 / Feishu 99991663 / 99991661 强制 token refresh + 一次重试 |
| **F-5** | **P1** | `admin/credential_success_rate.go:127-133` | `request_logs_hot` DELETE 加 `current_setting('app.current_role')` / `app.current_tenant` 守卫，handler auth 检查之外的回归保险 |
| **F-6** | **P2** | `cmd/cfg_dump/main.go`（重写）+ `cmd/cfg_dump/main_test.go` | 默认仅输出 `length + sha256[:8]` 指纹；同时需要 `--print-secrets` flag 和 `CFG_DUMP_ALLOW_SECRETS=1` 环境变量才输出原文（fail-closed） |

### 回归测试（全部 PASS）

- `TestParseGemini_ParallelFunctionCalls_ProduceUniqueIDs`（gemini_test.go）
- `TestParseGeminiStreamChunk_ParallelFunctionCalls_ProduceUniqueIDs`（gemini_stream_test.go）
- `TestToolUseNameFromID`（serialize_gemini_test.go，新）
- `TestTurnReader_LoadChain_LastNZeroReturnsAllTurns`
- `TestTurnReader_LoadChain_LastNNegativeIsError`
- `TestSanitizeErrorText_*` × 7（errorsx/sanitize_test.go，新）
- `TestHasCredentialLeak_*` × 4
- `TestClassifyLarkSendErr`（12 子用例）+ `TestLarkBotChannel_SendJSON_RetriesOn429`（httptest 429→200）
- `TestFingerprint_Deterministic` + `TestFingerprint_Empty` + `TestSecretFieldsContainExpectedLabels`（cfg_dump）

## 验证结果（真实验证，非声称）

| 阶段 | 命令 | 结果 |
| --- | --- | --- |
| 静态 | `git diff --check` | clean |
| 静态 | `gofmt -l <changed files>` | clean |
| 静态 | `go vet ./admin/... ./errorsx/... ./domains/streaming/executors/... ./domains/session/v2/... ./domains/notification/... ./internal/ir/... ./cmd/cfg_dump/` | 0 输出 |
| 静态 | `grep -RIn '9\.9\.9-abc12345' --include='*' .` | 仅命中审计文档（事实陈述），无残留 |
| Pre-commit | 项目级 6 项 checks | PASS=6 FAIL=0 WARN=0 SKIP=0 |
| 单元 | `go test -race -count=1 -timeout 180s ./internal/ir/ ./errorsx/ ./domains/session/v2/ ./domains/notification/ ./cmd/cfg_dump/ ./domains/streaming/executors/` | 6 / 6 PASS |
| 全量 | `go test -count=1 -timeout 300s ./...` | 246 / 246 packages PASS，0 FAIL |
| Web | `pnpm -C web typecheck` | clean |
| Web | `pnpm -C web test --run` | 101 文件 / 709 tests PASS |
| 数据库脚本 | `scripts/verify-migration-checksums.sh` | 环境无 isolated-PG（记录） |
| 数据库脚本 | `scripts/audit/verify-promote-*.sh` × 3 | 环境无 isolated-PG（记录） |

## 显式 deferred（不在本会话范围）

- **P1-3 agent-2 attachment FS cleanup**（`admin/data_lifecycle_attachments_filesystem.go:221-275`）— 由 `d9b289256` 与 632 doc-comment 显式声明为 "out of scope for this audit pass"，重写需新增 saga/cancel 协议
- **P2 agent-2**：migration checksum ledger 覆盖 `deploy/sql/migrations/`、MV TTL 监控（p99 < 6min）、`pg-table-copy` 当前月分区告警、`apply-routing-mv-fixup` env fallback — 需独立 handoff
- **P1-3 agent-3 self_check validation order** — 用户决策丢弃 150 行重构后该问题不成立
- **P2 agent-3**：goroutine 关闭时序、`runModels` panic semaphore 恢复、web 前端 raw 渲染 — 已修复清单见 `agent-3-provider-reliability-ui.md` "Already-fixed (skipped)"；剩余 8 项属于噪声治理

## 子代理审计报告（落盘）

- `.audit/2026-09-01-sync/agent-1-ir-session-protocol.md` — P0×2 / P1×3 / P2×8
- `.audit/2026-09-01-sync/agent-2-storage-partition-attachment.md` — P0×0 / P1×3 / P2×8
- `.audit/2026-09-01-sync/agent-3-provider-reliability-ui.md` — P0×0 / P1×3 / P2×8

## 审计矩阵（追加到 `docs/audit/2026-08-31-4h-correction-audit.md` §2.8）

| 闭环 | 入口 → 终点 | 关键文件:行 | 状态 | 修复 commit |
| --- | --- | --- | --- | --- |
| 流程 | 入口 → IR → 路由瀑布 → 供应商 → SSE → 客户端 → 结束态 | `internal/ir/parse_gemini.go:216` (P0-1) ; `domains/session/v2/turn_reader.go:68-70` (P0-2) | P0 双确认 | F-1, F-2 |
| 数据 | 原始请求 → IR → hot 表 → 8h promote → columnar → unified view → 详情/replay | `admin/credential_success_rate.go:127-133` (P1-2 storage); `bg/materialized_view_refresher.go` (0dacb1d22 已合); `sql/migrations/startup/614_session_bodies_hot.sql:88-93` (需 staging 探针) | P1 确认 + verification gap | F-5 |
| 反馈 | 供应商错误 → 凭证聚合 → 错误表 → Lark 告警 → admin 详情 | `admin/vendor_credential_error_handlers.go:189` (P1-1 leak); `domains/notification/lark.go:265-307` (P1-2 retry) | P1 双确认 | F-3, F-4 |

完整 IR 字段归类（核心 / 扩展 / 内部元数据）见 §2.8。

## 剩余风险

1. **`session_bodies_unified` 在 `partition_date = today` 边界仍需 staging 探针确认无重复行**（agent-2 P1-3，需 isolated-PG 环境验证）。
2. **`mvRefreshDistLockTTL=6m` vs `RefreshTimeout=5m` 仅 1 分钟裕度** — auto-renew 处理 liveness，但首跑 unique index 构建可能超时，需监控 p99。
3. **Staging-only 验证**：
   - P1-1 实际 vendor body 取样 24h sweep，看 `sk-` / `Bearer` / `api_key=` 命中率
   - P1-2 Lark retry 在真实 Feishu 429 / `Retry-After` / 99991400 envelope 下行为
   - P2-5 aggregator 首跑 watermark 不触发 false-positive
   - 5ca8d0005 P1-1 Class/DueAt reload path（仅内存路径正确，未覆盖 reload-rewrite）
4. **`auth/154` / `245` 等环境的 `app.current_tenant` GUC 注入方式未在本会话验证** — F-5 的 SQL 守卫依赖该 GUC 已被网关在 `apihub.withTenantTx` 模式之外也注入，否则 DELETE 是 no-op。

## 建议 next-session skills

- **`pg-columnar-auto-rotate`** — P2-7 current-month partition 数据静默丢失监控
- **`pg-release-schema-manager`** — P2-2 migration checksum ledger 覆盖 `deploy/sql/migrations/`
- **`trendaradar-deployment-validation`** — staging 后用真实环境跑迁移 + 探针
- **`session-audit-gate`** — 后续修改 IR / session / streaming 时按 §2.8 矩阵自检
- **`fix-review`** — 涉及 IR 字段、admin handler、vendor error 路径时强制过 review
- **`handoff`** — 后续会话起手读这份文档

## 后续会话具体动作

1. 验证 staging 环境是否注入了 `app.current_tenant` / `app.current_role` GUC；若未注入，F-5 在 admin handler 调用路径上是 no-op，需扩展 `withTenantTx` 包装到 admin handlers
2. 跑 isolated-PG verify-promote-* 系列探针（`scripts/audit/`），确认 P2-1 envelope_cleaner + P2-2 checksum ledger 缺口真实存在
3. 24h 抽 sample 50 行 `candidate_failure_logs_hot.upstream_response_preview` 做 secret regex sweep，验证 F-3 命中率
4. Lark alert 通道加 chaos drill（mock Feishu 返回 429），确认 F-4 retry 触发
5. agent-3 P2-7 health_tracker goroutine fan-out 监控，按需加 sync.Pool / 限流
6. 评估是否将 `data/model-quality/scores/` 整体从 stash@{1}^3 重新拉回仓库（用户决策遗留问题）

## 可复制的并行子代理提示词（供下一会话复用）

```
1. IR/会话/协议

只读审计 /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go。
覆盖 internal/ir、domains/session/v2、domains/transformation、adapter/unified
以及 OpenAI Chat/Responses、Anthropic Messages、SSE 和下游 client/agent 适配。
绘制单请求与多轮对话从原始输入到 IR、压缩脱敏、缓存/队列、hot+columnar、
unified view、详情/replay/export 的数据流。
检查 RawContent、未知 block、tool call ID（特别是 Gemini 并行 functionCall
的 _<partIdx> 后缀）、重复消息、round-trip、序列化/反序列化、大整数/大小边界、
Class/DueAt JSONB reload 行为。
只报告确定 bug 或可证实风险，输出 P0-P2、文件:行号、调用链、证据、最小回归
测试建议；不要修改文件。已修复项（5ca8d0005 IR json tags, 3b3497935 turn_reader
unified view, 72e84edcb session aggregator idempotency, a17ccd8a6 WriteFrame
semaphore）不要重复报告。

2. 存储/分区/附件

只读审计 /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go。
覆盖 startup migrations（sql/migrations/startup/）、canonical SQL、installer
embed/staging、session_bodies / session_turns / request_logs /
candidate_failure_logs 的 hot+columnar 结构、8 小时 promote、partition_date
统一视图、更新删除限制、RLS、缓存/队列元数据、附件 DB 元数据和文件系统版本
清理/tombstone/retry。
验证幂等、崩溃恢复、retention=0、跨租户隔离、schema/checksum 闭环。
特别关注：envelope_cleaner.go:54 是否有 set_config('app.bypass_rls')，
verify-migration-checksums.sh 是否覆盖 deploy/sql/migrations/，
session_bodies_unified 在 partition_date = today 边界是否产生重复行，
partition_manager.go cleanupOldModelProbeRuns 在 ttl_days <= 7 时是否走
hourly tick 而非 daily。
输出 P0-P2，文件:行号、SQL/Go 证据和测试建议；不要修改文件。

3. 供应商/可靠性/UI

只读审计 /Users/xutaohuang/workspace/official-deploy/services/llm-gateway-go。
覆盖供应商请求错误分类（errorsx/classify.go）、原始错误摘要、
credential/provider 错误集合、备用供应商 think / 不中断响应、
request/session 关联、并发锁、goroutine/句柄、异步 worker、TCP stream、
请求边界、密钥/API 健康、Lark/结构化日志，以及 web Provider detail
tabs/menu/统一控件和可观测性展示。
特别关注：vendor_credential_error_handlers.go 是否在 JSON 响应前再次
sanitize（errorsx.SanitizeErrorText），Lark sendJSON 是否有 5xx/429 retry，
self_check_handlers.go 是否在多模型路径之前先校验 model_source，
cfg_dump 是否默认 redact secrets，credential_success_rate.go DELETE 是否
带 tenant_id 守卫。
区分已修复项（8e2b428bf sync / bc48559b4 audit-20260901 / 24h 内其它
commits）、确定 bug、需要 staging 验证的风险。
输出 P0-P2，文件:行号、调用链和回归测试建议；不要修改文件。
```

## 安全门禁自检

- [x] 不允许带未解决冲突提交 → `git diff --check` clean
- [x] 不允许用测试占位版本覆盖有效发布版本 → `grep 9.9.9-abc12345` 仅命中审计文档（事实陈述）
- [x] 不允许绕过 pre-commit hook → 6 项 checks 全部 PASS
- [x] 不允许把环境未提供的集成验证写成通过 → isolated-PG / 真实 staging 验证标记为"环境缺失，记录状态"
- [x] 推送前后 main 与 origin/main 对齐 → SHA 比对通过 (`bc48559b4`)
- [x] 工作区干净 → `git status --porcelain=v1` 无输出
- [x] handoff 文档与提示词已生成 → 本文件存在

## 远程仓库（脱敏）

```
https://***@codeup.aliyun.com/kaixuan/official-deploy/llm-gateway-go.git
```

（凭据已脱敏；远端 URL 来源于 `git remote get-url origin`，凭据保留在 git 凭据缓存中）
