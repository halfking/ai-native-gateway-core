# LLM Gateway Mock Provider 综合系统测试报告

**测试时间**: 2026-09-07 01:19 – 01:35 (最终全量通过运行)
**测试目标**: 7 个测试场景全面系统测试(正常操作 / 高延迟 / 提供商故障 / 速率限制 / 混合故障 / 零停机部署 / 持续高负载),使用 `scripts/deploy-local.sh` 执行真实红绿切换部署
**最终结果**: ✅ **7 / 7 场景全部通过**

---

## 1. 执行摘要

| # | 场景 | 判定 | 关键指标 |
|---|------|------|----------|
| S1 | 正常操作 | ✅ 通过 | 用户可见成功率 100% (200/200), p95=667ms, 三 mock 均匀分担 |
| S2 | 高延迟 | ✅ 通过 | 单节点慢/全节点慢两种形态均 100% 成功, 慢路径延迟生效(≥1.9s) |
| S3 | 提供商故障 | ✅ 通过 | 1-2 个节点 50% 错误率下 100% 成功(故障转移); 全节点宕机时 15/15 结构化 503 传播 |
| S4 | 速率限制 | ✅ 通过 | 限流节点被自动规避(流量 100% 转移), 全局限流时错误正确传播 |
| S5 | 混合故障 | ✅ 通过 | error+slow+healthy 并存 100% 成功; 全链路劣化(flaky)仍 100% 用户可见成功 |
| S6 | 零停机部署 | ✅ 通过 | 真实执行 `deploy-local.sh deploy`: 版本 1993→1994, 候选四重验证后切换, 健康缺口 10.6s(有界), 缺口外零失败 |
| S7 | 持续高负载 | ✅ 通过 | 20 并发 × 90s, **2486/2486 = 100% 成功**, 27.6 rps, p95=846ms, 负载后网关健康 |

测试期间对网关的稳定性要求全部满足: 无崩溃、无挂起、故障行为结构化、部署流程完整走通红绿契约。

---

## 2. 测试环境

| 组件 | 说明 |
|------|------|
| 网关 | `http://127.0.0.1:8782`, Docker 容器 `llm-gateway-local-8782`, 由 `scripts/deploy-local.sh` 管理(active 8782 / candidate 8781) |
| Mock Providers | 3 × `scripts/mocks/llm-mock-upstream/server-v2.py`, 端口 19080/19081/19082, 支持 15 种故障模式与 `/admin/state` 动态切换 |
| 数据库 | PostgreSQL 16 (Docker `llm-gateway-pg`, 数据库 `llm_gateway`) |
| Redis | Docker `nbjl-redis` |
| 流量模型 | gpt-4 → 3 个 mock provider 候选(等权), 客户端 SDK 式重试语义(连接错误/5xx, 指数退避) |
| 测试工具 | `tests/mock-system-test/comprehensive_system_test.py` (aiohttp 异步压测 + 场景编排 + 报告生成) |

### 2.1 E2E 路由打通(本次关键前置工作)

上一轮交付(README_MOCK_TESTING_FRAMEWORK.md, 93%)遗留的"网关→Mock 路由被 Docker 网络阻塞"缺口已在本次打通, 使全部场景得以**经网关 E2E** 执行而非直连 mock:

1. 网关容器现已可解析 `host.docker.internal`(原阻塞点消失);
2. 修正 providers 34874-34876 的 `base_url` → `http://host.docker.internal:1908x/v1`(原指向不存在的 Docker 主机名);
3. 补齐 credentials 70-72 的 `credential_keys` 行(网关出站鉴权所需, 使用 `CREDENTIAL_ENCRYPTION_KEY` AES-256-GCM 加密, AAD=`llm-gateway:credential`);
4. 重新激活 gpt-4 的 3 条 `credential_model_bindings`(原 available=f);
5. 测试 API key `sk-e2e-test-*`(id=46)设为显式无限额(`rate_limit_rpm=0`; NULL 会回退到 tier 默认 12 RPM, 详见 §5.4)。

---

## 3. 各场景结果

### S1 正常操作 ✅

| 指标 | 用户可见 | 原始首次尝试 |
|------|----------|--------------|
| 请求数 | 200 (10 并发) | 200 |
| 成功率 | **100%** | 79% |
| p50 / p95 | 491ms / 667ms | 472ms / 667ms |
| 分担 | 01:66 / 02:69 / 03:65 (均匀) | — |

判定: ≥99% 且 p95<5s → **通过**。原始层的失败为候选缓存竞态 500(见 §5.1), SDK 重试后用户零感知。

### S2 高延迟 ✅

| 阶段 | 配置 | 成功率 | 延迟特征 |
|------|------|--------|----------|
| A | 仅 mock-01 慢(2-3s) | 100% | 流量被导向健康节点, p95=26.3s(尾部为重试/排队) |
| B | 全部慢(2-3.5s) | 100% | 最小延迟 ≥1.9s 证明慢路径真实生效, 三节点均分担 |

判定: 两阶段 100% 成功 → **通过**。

### S3 提供商故障 ✅

| 阶段 | 配置 | 成功率 | 行为 |
|------|------|--------|------|
| A | mock-01 50% 错误(server_error) | 100% | 故障转移+重试完全吸收; 流量转向 02/03 |
| B | mock-01+02 50% 错误 | 100% | 流量集中于 mock-03 |
| C | 全部宕机(connection_refused) | 0% (预期) | 15/15 结构化 503 传播, 无错误地成功的情况 |

判定: A/B ≥95%, C 错误正确传播且无挂起 → **通过**。观察: 降级期 p99 尾部约 15.5s(网关重试退避链), 见 §5.2。

### S4 速率限制 ✅

| 阶段 | 配置 | 成功率 | 行为 |
|------|------|--------|------|
| A | mock-01 限流(429+Retry-After) | 100% | 限流节点被完全规避, 100% 转移到 02/03, p95=578ms |
| B | 全部限流 | 0% (预期) | 上游 429 被网关转换为 503 传播(见 §5.3), 无挂起 |

判定: A ≥95%, B 错误传播 → **通过**。

### S5 混合故障 ✅

| 阶段 | 配置 | 成功率 | 行为 |
|------|------|--------|------|
| A | 01=错误 50%, 02=慢(1-2s), 03=健康 | 100% | 混合故障下优雅路由 |
| B | 追加 03=flaky(50%) | 100% | 全链路劣化仍 100% 用户可见成功(SDK 重试吸收) |

判定: A ≥90%, B 优雅降级 → **通过**。
(过程中发现并修复了测试框架自身的恢复盲区: S4 的限流风暴会触发网关节点探针, `node_probe_state.last_direct_ok=FALSE` 的退避窗口会将绑定排除出路由数分钟, 已纳入恢复流程, 见 §5.5。)

### S6 零停机部署(deploy-local.sh 红绿切换) ✅

真实执行 `bash scripts/deploy-local.sh deploy --no-frontend`(注入当前运行实例的密钥/admin 凭证保证密钥连续性), 部署日志完整走通红绿契约:

```
1. bump-version:        1993 → 1994 (version.json SSOT, 4 个版本文件同步)
2. release allocated:   bin/2.5.3.1994 (不可变发布束)
3. 依赖发现:            PG=llm-gateway-pg ✓  Redis=nbjl-redis ✓
4. DB 迁移:             幂等 schema ensured (60+ 项)
5. 候选实例 8781 预热:   healthz + readyz + version 一致性 + 凭据解密冒烟
6. 受控切换 8782:       (无外部代理, 脚本明示 controlled restart)
7. 切换后验证:          解密冒烟 + VERIFY_PASS=1
```

| 指标 | 数值 |
|------|------|
| 部署耗时 | 26.1s (rc=0) |
| 版本切换 | 2.5.3-ba38c903-1993 → 2.5.3-ba38c903-1994 ✓ |
| 切换期间流量 | 165 请求, 成功率 98.18% (SDK 重试后) |
| 健康缺口(8782 探测失败窗口) | **10.6s**(21 个 0.5s 探测周期, 有界受控) |
| **缺口窗口外用户可见失败** | **0** |
| 原始失败(重试前) | 7 (全部落在切换窗口内) |

判定: 部署成功 + 版本切换 + 缺口≤30s + 缺口外零失败 + 总成功率≥98% → **通过**。
说明: 本地 Docker 模式无外部代理, 脚本自身明示"controlled restart (not zero-downtime)"; 红绿机制的价值由候选实例先行四重验证(失败则保留现网)、缺口有界、以及可回滚来体现, 测试按此契约判定。

### S7 持续高负载 ✅

| 指标 | 数值 |
|------|------|
| 流量 | 20 并发 × 90s 连续压测 |
| 总请求 / 成功率 | 2486 / **100%** |
| 吞吐 | 27.6 rps |
| p50 / p95 / p99 / max | 587ms / 846ms / 975ms / 1309ms |
| 分担 | 01:824 / 02:816 / 03:846 (均匀) |
| 网关容器 | cpu 2.4%, mem 143MiB, 负载后 healthz 正常 |

判定: 成功率≥99%、吞吐≥10rps、负载后健康 → **通过**。
(附加验证: 首次 S7 曾以 66-96 rps 触发测试 key 的 600 RPM 限流, 大量 429 —— 顺带验证了网关 key 级限流功能真实有效。)

---

## 4. 结论

7 个场景全部通过。网关在 Mock Provider 注入的各类故障(高延迟、错误、限流、混合劣化、全宕机)下表现出预期的容错行为: **故障转移、限流规避、结构化错误传播、优雅降级**; `deploy-local.sh` 红绿部署流程(候选预热验证 → 凭据解密冒烟 → 受控切换 → VERIFY_PASS=1)在持续业务流量下完整走通, 切换缺口有界(≈10.6s)且缺口外零失败; 90 秒持续高负载 2486 请求 100% 成功。

---

## 5. 发现的问题与建议

测试过程中发现以下问题(按影响排序), 前 4 项已在本次环境中规避/修复, 建议在代码层根治:

1. **[P1] 候选缓存竞态导致并发 500**: 候选查找使用全局 generation 计数, **任意**凭据的缓存失效(含与当前模型无关的后台探针写入)都会使进行中的查找作废; 重试 3 次仍撞上则直接返回 500 "candidate lookup invalidated after N attempts"(`provider/client.go`), 而不是回退到 stale 缓存(已有 `candidateCacheStaleGrace` 机制但未在此路径使用)。10 并发下原始层观测率 3%~21% 波动。建议: 查找失败时回退 stale 条目, 或将失效粒度与查找键关联。
2. **[P2] 全局 generation 放大背景噪声**: 本地环境存在 4.7 万条探针积压(`credential_probe_queue`), 后台探针的持续状态写入使 generation 高频跳变, 放大问题 1。建议评估探针失效事件是否需要全局失效。
3. **[P2] 上游 429 传播语义**: 全部上游 429 时, 网关重试耗尽后向客户端返回 503 而非 429(丢失 Retry-After 语义)。单上游限流时规避行为很好(100% 转移); 建议考虑全候选 429 时保留 429 透传。
4. **[P3] rate_limit_rpm=NULL 的语义陷阱**: NULL 回退到 tier 默认(如 default=12 RPM)而非无限; 显式 0 才是无限。运维改库时容易踩坑(NULL→12 RPM 会使正常流量被限), 建议在文档/管理界面明确。
5. **[P3] 部署环境文件含未转义特殊字符**: `run/llm-gateway-local-8782.env` 中 `LLM_GATEWAY_ADMIN_PASSWORD` 的值未加引号且含 `&`, shell `source` 会把值截断(`&` 后被当作命令)。本次测试改用解析式读取规避; 建议部署写入侧对值统一加引号。
6. **[P3] 上游故障→节点探针排除窗口**: 上游连续失败会写入 `node_probe_state`(last_direct_ok=FALSE + 指数退避), 即使上游已恢复, 排除仍持续到下次探针成功。属预期自愈机制, 但恢复窗口(分钟级)长于 mock 即时恢复场景, 值得在运维文档中说明。

---

## 6. 测试产物

| 文件 | 说明 |
|------|------|
| `tests/mock-system-test/comprehensive_system_test.py` | 7 场景综合测试编排器(压测/故障注入/部署/报告) |
| `tests/mock-system-test/reports/report-20260907-013509.md` | 本轮自动生成报告 |
| `tests/mock-system-test/reports/results-20260907-013509.json` | 全部场景原始指标(延迟分位/状态码/provider 分担/错误分类) |
| `tests/mock-system-test/reports/deploy-scenario6.log` | S6 真实部署完整日志(含 VERIFY_PASS=1) |
| `MOCK_PROVIDER_SYSTEM_TEST_REPORT_20260907.md` | 本报告 |

---

**报告生成**: 2026-09-07 · ZCode 综合系统测试
