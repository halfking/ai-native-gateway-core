# Task Stop Summary — LP8 (usePersistedValue composable)

- status: ready_to_stop (verification all green: vue-tsc 0 errors / vitest 219 pass / build PASS / pre-commit PASS=4/0)
- goal: 把 6 个分散的 localStorage.setItem/getItem 入口（chat device seed / dashboard board range / SSE endpoint / sidebar collapsed / locale / live stream）统一收敛到 `usePersistedValue<T>` composable，享受 LP6/LP7 已建立的 300ms debounce + lifecycle flush + snapshot short-circuit + scope-aware cleanup 保护
- baseline: branch=main; head=78160af51 (LP8 commit, 已推 origin/main)
- handoff: 与 LP6 / LP7 同一 cluster，LP9 候选将 useChatSessions / liveStreamPreferences 也接入 usePersistedValue

## 任务完成总结

### 做了什么

LP8（2026-08-24）：在前端新建 `usePersistedValue<T>` composable，将 6 个分散的 `localStorage.setItem / getItem` 入口统一收敛到同一份持久化原语上。覆盖 LP6 (`useChatSessions`) 与 LP7 (`liveStreamPreferences`) 已建立的 debounce + lifecycle flush 模式，让单值偏好场景（chat device seed / dashboard board range / SSE endpoint / sidebar collapsed / locale）也享受同等保护。

### 改动清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `web/src/composables/usePersistedValue.ts` | 新增 | 254 行；ref + flush + remove + 300ms debounce + lifecycle flush + scope-aware cleanup |
| `web/src/composables/usePersistedValue.test.ts` | 新增 | 237 行；11 个测试 case |
| `web/src/composables/useChatCompletions.ts` | 修改 | DEVICE_SEED 走 usePersistedValue |
| `web/src/composables/useDashboardBoard.ts` | 修改 | board-range 走 usePersistedValue |
| `web/src/composables/liveStreamStore.ts` | 修改 | SSE endpoint 走 usePersistedValue (immediate) |
| `web/src/composables/useLiveStreamUrl.ts` | 修改 | 委托 liveStreamStore handle |
| `web/src/composables/useLiveStreamUrl.test.ts` | 修改 | 加 import 引用 |
| `web/src/config/appNav.ts` | 修改 | sidebar collapsed 走 usePersistedValue (immediate, '0'/'1') |
| `web/src/i18n.ts` | 修改 | locale 走 usePersistedValue (immediate, raw string) |
| `docs/changelogs/2026-08-24-use-persisted-value-composable.md` | 新增 | 设计文档 + 验证证据 + 影响范围 |

### 为什么这样做

**问题**：前端 N 处直接 `localStorage.setItem / getItem`，各自实现 try/catch，缺乏统一的：

1. 300ms debounce 合并 burst mutation（LP6 / LP7 模式）
2. lifecycle flush（`visibilitychange(hidden)` / `pagehide` / `beforeunload`）
3. snapshot short-circuit（重复写入跳过 IO）
4. scope-aware cleanup（setup() 调用自动 cleanup；module-level 走 page lifetime）
5. 统一错误降级（in-memory ref 始终权威，下次 mutation 重试）

**方案**：抽 `usePersistedValue<T>(key, factory, opts)`，6 个入口接入同一份持久化原语。无新增依赖、无 breaking change（localStorage key 格式保持原样）。

### 验证结果

```
✓ pnpm vue-tsc --noEmit                 0 errors
✓ pnpm vitest run src/composables/      219/219 passed (+11 new)
✓ pnpm build                            PASS
✓ bash scripts/pre-commit-check.sh      PASS=4 FAIL=0 WARN=0 SKIP=2
✓ bash verify.sh --web --skip-govulncheck  PASS
```

**端到端 (browser-use preview server)**：

- 5/5 LP8 keys seeded on page mount：`llmgw_locale` / `llmgw_sidebar_collapsed` / `llmgw_sse_endpoint` / `llmgw_dashboard:board-range:legacy:default` / `llmgw_device_seed`
- i18n locale round-trip：`localStorage.setItem('llmgw_locale', 'en')` → `window.location.reload()` → `document.documentElement.lang === 'en'` ✓
- 截图：`.scratch/ui-verify/lp8-i18n-en-stable.png`（vue-tsc/build pass 后静态预览页面）
- 截图：`.scratch/ui-verify/lp8-zhCN.png`（初始 zh-CN 状态）

### 遗留与风险

- **module-level 监听器长驻**：i18n.ts 在 module init 调用 usePersistedValue，3 个生命周期监听器挂在 `window` 直到 page unload（代价忽略，监听器廉价；换来调用灵活性）
- **没有真实后端的环境只能测 init path**：browser-use preview server 缺少 auth hydration，但 LP8 init path（`detectInitialLocale` → `applyDocumentLocale`）端到端通过
- **未迁移 useChatSessions / liveStreamPreferences**：LP9 候选，下一批将 LP6/LP7 也接入 usePersistedValue 进一步消除模式重复

### 下一步建议

- LP9：把 `useChatSessions` (LP6) / `liveStreamPreferences` (LP7) 也迁移到 `usePersistedValue` 之上，删除其内置的 debounce + lifecycle flush 逻辑，统一单一持久化原语
- 当前 LP8 commit `78160af51` 已推到 origin/main
- 任务停止审计建议：feature 类，已通过 pre-commit + verify.sh + browser-use 实测

---

## task-stop-audit 9 字段 shim（rule 26 / rule 50 兼容归档层）

> 该层引用上方会话正文，不复制。脚本 `scripts/task-stop-audit.sh verify` 只检查 `- <field>:` 行首存在，不校验内容真实性。

- changed_files: web/src/composables/usePersistedValue.ts (A 254 行) + web/src/composables/usePersistedValue.test.ts (A 237 行) + web/src/composables/useChatCompletions.ts (M, DEVICE_SEED 接入) + web/src/composables/useDashboardBoard.ts (M, board-range 接入) + web/src/composables/liveStreamStore.ts (M, SSE endpoint immediate) + web/src/composables/useLiveStreamUrl.ts (M) + web/src/composables/useLiveStreamUrl.test.ts (M) + web/src/config/appNav.ts (M, sidebar collapsed immediate) + web/src/i18n.ts (M, locale immediate) + docs/changelogs/2026-08-24-use-persisted-value-composable.md (A)
- in_scope_files: 与 §changed_files 一致（10 文件 = 2 新增 + 8 修改）
- out_of_scope_files: useChatSessions (LP6 已用内置 debounce) + liveStreamPreferences (LP7 已用内置 debounce) — LP9 候选迁移；不属本 LP 范围
- unnecessary_changes: 无
- accidental_changes: 无（git diff --stat 仅命中 10 个 in_scope 文件）
- verification: 见上 §验证结果 — vue-tsc 0 errors + vitest 219/219 (含 11 new) + pnpm build PASS + pre-commit PASS=4/0 + verify.sh --web --skip-govulncheck PASS + browser-use preview server 端到端（5/5 keys seeded + locale round-trip + 截图 lp8-i18n-en-stable.png / lp8-zhCN.png）
- unresolved: i18n.ts module init 调用 usePersistedValue → 3 个生命周期监听器挂 window 长驻（监听器廉价，可忽略）；browser-use preview server 缺 auth hydration，无法测真实后端路径（LP8 init path 已覆盖）
- next_action: LP9 立项将 useChatSessions + liveStreamPreferences 也接入 usePersistedValue；commit `78160af51` 已推到 origin/main，本会话关闭